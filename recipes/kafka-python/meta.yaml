package:
  name: kafka-python
  version: "0.9.3"

source:
  fn: kafka-python-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/k/kafka-python/kafka-python-0.9.3.tar.gz
  md5: 5ebdf993e403b609f5fa982995847a81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kafka-python = kafka-python:main
    #
    # Would create an entry point called kafka-python that calls kafka-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - kafka
    - kafka.consumer
    - kafka.partitioner
    - kafka.producer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mumrah/kafka-python
  license: Apache Software License
  summary: 'Pure Python client for Apache Kafka'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
