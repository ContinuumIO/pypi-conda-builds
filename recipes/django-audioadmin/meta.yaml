package:
  name: django-audioadmin
  version: "0.1.0"

source:
  fn: django-audioadmin-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-audioadmin/django-audioadmin-0.1.0.tar.gz
  md5: 3f76922142418a62e713f3703a195bd5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-audioadmin = django-audioadmin:main
    #
    # Would create an entry point called django-audioadmin that calls django-audioadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.1
    - pillow >=2.6.1
    - django-imagekit >=3.2.4
    - mutagen >=1.22

  run:
    - python
    - django >=1.7.1
    - pillow >=2.6.1
    - django-imagekit >=3.2.4
    - mutagen >=1.22

test:
  # Python imports
  imports:
    - audioadmin
    - audioadmin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ngrinkevich/django-audioadmin
  license: MIT License
  summary: 'A reusable Django app for uploading/previewing audio files plus a simple id3 tag reader (Title/Artist/Album)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
