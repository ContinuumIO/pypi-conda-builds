package:
  name: mopidy-somafm
  version: "0.7.1"

source:
  fn: Mopidy-SomaFM-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-SomaFM/Mopidy-SomaFM-0.7.1.tar.gz
  md5: e9c9b2eab0282ca93f81abd5f7c5bbae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-somafm = mopidy-somafm:main
    #
    # Would create an entry point called mopidy-somafm that calls mopidy-somafm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - requests

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - requests

test:
  # Python imports
  imports:
    - mopidy_somafm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/AlexandrePTJ/mopidy-somafm/
  license: MIT License
  summary: u'SomaFM extension for Mopidy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
