package:
  name: ud-django-activity-stream
  version: "0.5.1"

source:
  fn: ud-django-activity-stream-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/u/ud-django-activity-stream/ud-django-activity-stream-0.5.1.tar.gz
  md5: 0cb29ef151ef3e0a6e79e908dd3e002b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ud-django-activity-stream = ud-django-activity-stream:main
    #
    # Would create an entry point called ud-django-activity-stream that calls ud-django-activity-stream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - actstream
    - actstream.migrations
    - actstream.runtests
    - actstream.runtests.testapp
    - actstream.templatetags
    - actstream.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/udemy/django-activity-stream
  license: BSD License
  summary: "Generate generic activity streams from the actions on your site. Users can follow any actor's activities for personalized streams."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
