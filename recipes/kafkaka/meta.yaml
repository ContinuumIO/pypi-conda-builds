package:
  name: kafkaka
  version: "0.3.0"

source:
  fn: kafkaka-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kafkaka/kafkaka-0.3.0.tar.gz
  md5: 89fb519fe9eef0f1e9444b330f6cc5c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kafkaka = kafkaka:main
    #
    # Would create an entry point called kafkaka that calls kafkaka.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kafkaka

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wesdu/kafkaka
  license: Apache Software License
  summary: 'Kafka Client with smarter protocol described, support for Gevent and tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
