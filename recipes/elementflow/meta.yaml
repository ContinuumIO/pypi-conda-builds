package:
  name: elementflow
  version: "0.5"

source:
  fn: elementflow-0.5.tar.gz
  url: https://pypi.python.org/packages/source/e/elementflow/elementflow-0.5.tar.gz
  md5: 6bd9c00f0412af23215be67828cb3c17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elementflow = elementflow:main
    #
    # Would create an entry point called elementflow that calls elementflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - elementflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/isagalaev/elementflow
  license: BSD License
  summary: 'Python library for generating XML as a stream without building a tree in memory.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
