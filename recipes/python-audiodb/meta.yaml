package:
  name: python-audiodb
  version: "0.1"

source:
  fn: python-audiodb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-audiodb/python-audiodb-0.1.tar.gz
  md5: c481577b695ab8b753f03da43986cbd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-audiodb = python-audiodb:main
    #
    # Would create an entry point called python-audiodb that calls python-audiodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - audiodb
    - audiodb.core
    - audiodb.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyaudiodb.tuxfamily.org/
  license: GNU General Public License v3 (GPLv3)
  summary: 'To manage song library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
