package:
  name: django-coretext
  version: "0.5.2"

source:
  fn: django-coretext-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-coretext/django-coretext-0.5.2.tar.gz
  md5: f5e145f0efb0221e0dab8163162d0e86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-coretext = django-coretext:main
    #
    # Would create an entry point called django-coretext that calls django-coretext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1.1

  run:
    - python
    - django >=1.1.1

test:
  # Python imports
  imports:
    - text
    - text.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-coretext/
  license: BSD License
  summary: 'DOM-based text editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
