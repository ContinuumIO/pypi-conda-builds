package:
  name: django-enumfields
  version: "0.7.3"

source:
  fn: django-enumfields-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-enumfields/django-enumfields-0.7.3.tar.gz
  md5: 4606106f59d216643ce1676794488744
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-enumfields = django-enumfields:main
    #
    # Would create an entry point called django-enumfields that calls django-enumfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - enum34

  run:
    - python
    - six
    - enum34

test:
  # Python imports
  imports:
    - enumfields
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hzdg/django-enumfields
  license: MIT
  summary: 'Real Python Enums for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
