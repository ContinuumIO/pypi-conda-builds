package:
  name: boodler
  version: "2.0.3"

source:
  fn: Boodler-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/B/Boodler/Boodler-2.0.3.tar.gz
  md5: 99e680be8b4b937e0a087e9548e5c359
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boodler = boodler:main
    #
    # Would create an entry point called boodler that calls boodler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - boodle
    - boodler
    - booman
    - boopak

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://boodler.org/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A programmable soundscape tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
