package:
  name: ftp_bruteforce
  version: "1.0.0"

source:
  fn: ftp_bruteforce-1.0.0.zip
  url: https://pypi.python.org/packages/source/f/ftp_bruteforce/ftp_bruteforce-1.0.0.zip
  md5: 019fcca05d87a26c55f78404a366a91e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftp_bruteforce = ftp_bruteforce:main
    #
    # Would create an entry point called ftp_bruteforce that calls ftp_bruteforce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ftp_bruteforce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://doctor-g.posterous.com/
  license: UNKNOWN
  summary: 'A simple ftp bruteforce script'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
