package:
  name: html2data
  version: "0.4.3"

source:
  fn: html2data-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/h/html2data/html2data-0.4.3.tar.gz
  md5: a3710a11125fc0ce353f44e28178e9cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html2data = html2data:main
    #
    # Would create an entry point called html2data that calls html2data.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.0.0
    - httplib2

  run:
    - python
    - lxml >=2.0.0
    - httplib2

test:
  # Python imports
  imports:
    - html2data
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dperezrada/html2data
  license: BSD License
  summary: 'A simple way to transform a HTML file or URL to structured data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
