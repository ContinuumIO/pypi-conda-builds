package:
  name: torndb
  version: "0.3"

source:
  fn: torndb-0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/torndb/torndb-0.3.tar.gz
  md5: cac2e86343779e61d9e1f0bcb63b6d0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torndb = torndb:main
    #
    # Would create an entry point called torndb that calls torndb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - torndb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bdarnell/torndb
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'A lightweight wrapper around MySQLdb.  Originally part of the Tornado framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
