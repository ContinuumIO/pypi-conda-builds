package:
  name: django_runprocess
  version: "0.1.0"

source:
  fn: django_runprocess-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_runprocess/django_runprocess-0.1.0.tar.gz
  md5: 794f8e19be711c0c0e309eaf655535b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_runprocess = django_runprocess:main
    #
    # Would create an entry point called django_runprocess that calls django_runprocess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_runprocess
    - runprocess
    - runprocess.management
    - runprocess.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sephii/django-runprocess
  license: MIT License
  summary: 'Runs processes along with runserver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
