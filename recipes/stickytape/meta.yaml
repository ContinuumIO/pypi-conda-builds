package:
  name: stickytape
  version: "0.1.12"

source:
  fn: stickytape-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/s/stickytape/stickytape-0.1.12.tar.gz
  md5: 8a0462ce2e9eb2a7c08f8267eb50cecf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - stickytape = stickytape:main
    #
    # Would create an entry point called stickytape that calls stickytape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse ==1.2.1

  run:
    - python
    - argparse ==1.2.1

test:
  # Python imports
  imports:
    - stickytape

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mwilliamson/stickytape
  license: UNKNOWN
  summary: 'Convert Python packages into a single script'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
