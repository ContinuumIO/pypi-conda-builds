package:
  name: simpledb
  version: "0.0.5"

source:
  fn: SimpleDB-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleDB/SimpleDB-0.0.5.tar.gz
  md5: 7b6ab1fa1ee72d63a4531df30a2bba3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpledb = simpledb:main
    #
    # Would create an entry point called simpledb that calls simpledb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - SimpleDB
    - simpledb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.kormoc.com
  license: LICENSE.txt
  summary: 'A wrapper around MySQLdb to make development of quick scripts easier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
