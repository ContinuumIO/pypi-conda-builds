package:
  name: tornadohttp
  version: "0.1.1-2"

source:
  fn: tornadohttp-0.1.1-2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadohttp/tornadohttp-0.1.1-2.tar.gz
  md5: d5d4e970ed2b3e23587574af2d9bee38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadohttp = tornadohttp:main
    #
    # Would create an entry point called tornadohttp that calls tornadohttp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.2

  run:
    - python
    - tornado >=3.2

test:
  # Python imports
  imports:
    - tornadohttp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kejbaly2/tornadohttp
  license: GNU General Public License v3 (GPLv3)
  summary: 'Generic Tornado Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
