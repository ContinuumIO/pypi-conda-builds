package:
  name: collective.dexteritypaste
  version: "1.0alpha1.1"

source:
  fn: collective.dexteritypaste-1.0alpha1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.dexteritypaste/collective.dexteritypaste-1.0alpha1.1.tar.gz
  md5: 5b353b5668d4af4abc64e46afbe37fa7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dexteritypaste = collective.dexteritypaste:main
    #
    # Would create an entry point called collective.dexteritypaste that calls collective.dexteritypaste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - zopeskel

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - zopeskel

test:
  # Python imports
  imports:
    - collective
    - collective.dexteritypaste
    - collective.dexteritypaste.localcommands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.dexteritypaste
  license: GPL
  summary: 'Paster templates for dexterity'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
