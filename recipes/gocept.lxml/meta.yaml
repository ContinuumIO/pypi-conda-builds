package:
  name: gocept.lxml
  version: "0.2.1"

source:
  fn: gocept.lxml-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.lxml/gocept.lxml-0.2.1.tar.gz
  md5: 0480242b7c84f2e8e1d42741c2800197
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.lxml = gocept.lxml:main
    #
    # Would create an entry point called gocept.lxml that calls gocept.lxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.app.component
    - lxml

  run:
    - python
    - setuptools
    - zope.interface
    - zope.app.component
    - lxml

test:
  # Python imports
  imports:
    - gocept
    - gocept.lxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.lxml
  license: ZPL 2.1
  summary: 'Primarily proivdes zope3 interface definitions for lxml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
