package:
  name: django-model-blocks
  version: "0.8.10"

source:
  fn: django-model-blocks-0.8.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-blocks/django-model-blocks-0.8.10.tar.gz
  md5: e40ab4c4e624d2ea2f2ab912ca42c63e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-blocks = django-model-blocks:main
    #
    # Would create an entry point called django-model-blocks that calls django-model-blocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.1

  run:
    - python
    - django >=1.2.1

test:
  # Python imports
  imports:
    - model_blocks
    - model_blocks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjumbewu/django-model-blocks/
  license: BSD License
  summary: 'Simple filters and tags for generic Django model template partials'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
