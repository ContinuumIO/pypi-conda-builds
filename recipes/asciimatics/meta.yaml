package:
  name: asciimatics
  version: "0.4.0"

source:
  fn: asciimatics-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/a/asciimatics/asciimatics-0.4.0.tar.gz
  md5: 6ca0f59196785c1712c0c90dd468ef82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asciimatics = asciimatics:main
    #
    # Would create an entry point called asciimatics that calls asciimatics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyfiglet >=0.7.2
    - pillow >=2.7.0

  run:
    - python
    - pyfiglet >=0.7.2
    - pillow >=2.7.0

test:
  # Python imports
  imports:
    - asciimatics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peterbrittain/asciimatics.git
  license: Apache Software License
  summary: 'An ASCII art and text cinematic storyboard/demo library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
