package:
  name: lmiapi
  version: "0.1"

source:
  fn: lmiapi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lmiapi/lmiapi-0.1.tar.gz
  md5: 78cff81cb43cb76092c9d20773d102f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lmiapi = lmiapi:main
    #
    # Would create an entry point called lmiapi that calls lmiapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0

  run:
    - python
    - requests >=2.0

test:
  # Python imports
  imports:
    - lmiapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://projects.ninemoreminutes.com/projects/lmiapi/
  license: BSD License
  summary: 'Basic Python interface to the LogMeIn Central REST API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
