package:
  name: toolserver
  version: "0.4.1"

source:
  fn: Toolserver-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/T/Toolserver/Toolserver-0.4.1.tar.gz
  md5: 0fc1c2619afcedf8820b60476c372fac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - toolserver = toolserver:main
    #
    # Would create an entry point called toolserver that calls toolserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - medusa

  run:
    - python
    - medusa

test:
  # Python imports
  imports:
    - Toolserver
    - toolserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/rfc1437/toolserver/
  license: BSD License
  summary: 'Toolserver Framework for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
