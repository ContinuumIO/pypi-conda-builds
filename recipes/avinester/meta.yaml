package:
  name: avinester
  version: "1.4.0"

source:
  fn: avinester-1.4.0.zip
  url: https://pypi.python.org/packages/source/a/avinester/avinester-1.4.0.zip
  md5: 3c6e2d805fee5066835d51f88546ed34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - avinester = avinester:main
    #
    # Would create an entry point called avinester that calls avinester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - avinester

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.facebook.com/avishekbanerjee.ranchi
  license: UNKNOWN
  summary: 'list printer that can print to files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
