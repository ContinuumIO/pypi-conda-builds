package:
  name: monocle_sample
  version: "0.1.0.2"

source:
  fn: monocle_sample-0.1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle_sample/monocle_sample-0.1.0.2.tar.gz
  md5: cbaf500351fe7895da788051be133bc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle_sample = monocle_sample:main
    #
    # Would create an entry point called monocle_sample that calls monocle_sample.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monocle_sample
    - monocle_youtube

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'App that plays video from youtube channel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
