package:
  name: django-invitation-backend
  version: "0.2b1"

source:
  fn: django-invitation-backend-0.2b1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-invitation-backend/django-invitation-backend-0.2b1.tar.gz
  md5: de7f8ecf34dc75393e8e7b424dbe3bd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-invitation-backend = django-invitation-backend:main
    #
    # Would create an entry point called django-invitation-backend that calls django-invitation-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - invitation
    - invitation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/volrath/django-invitation-backend
  license: BSD License
  summary: 'Registration through invitations backend for django-registration > 0.9'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
