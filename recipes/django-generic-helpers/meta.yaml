package:
  name: django-generic-helpers
  version: "0.3.6"

source:
  fn: django-generic-helpers-0.3.6.tar.bz2
  url: https://pypi.python.org/packages/source/d/django-generic-helpers/django-generic-helpers-0.3.6.tar.bz2
  md5: 5308bd2fa8c02588bccf87f63311271d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-helpers = django-generic-helpers:main
    #
    # Would create an entry point called django-generic-helpers that calls django-generic-helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - generic_helpers
    - generic_helpers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marazmiki/django-generic-helpers
  license: BSD License
  summary: 'The small frameworks that helps to write reusable django apps with generic relations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
