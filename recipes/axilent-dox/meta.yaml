package:
  name: axilent-dox
  version: "0.1.4"

source:
  fn: Axilent-Dox-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/A/Axilent-Dox/Axilent-Dox-0.1.4.tar.gz
  md5: 2d1e0d892fdfb6c1eada2ff1e7f4ec92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - axilent-dox = axilent-dox:main
    #
    # Would create an entry point called axilent-dox that calls axilent-dox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sharrock-client
    - markdown ==2.0.1

  run:
    - python
    - sharrock-client
    - markdown ==2.0.1

test:
  # Python imports
  imports:
    - dox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Axilent/Dox
  license: BSD License
  summary: 'Markdown-oriented content authoring for Axilent.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
