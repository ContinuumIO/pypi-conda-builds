package:
  name: django-admin-utils
  version: "0.3.0"

source:
  fn: django-admin-utils-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-utils/django-admin-utils-0.3.0.tar.gz
  md5: 3b59fa176c5d026f2610cda13d58e286
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-utils = django-admin-utils:main
    #
    # Would create an entry point called django-admin-utils that calls django-admin-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1

  run:
    - python
    - django >=1.1

test:
  # Python imports
  imports:
    - admin_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionelmc/django-admin-utils
  license: BSD License
  summary: 'Utility code and patterns.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
