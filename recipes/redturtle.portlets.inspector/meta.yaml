package:
  name: redturtle.portlets.inspector
  version: "1.0.0"

source:
  fn: redturtle.portlets.inspector-1.0.0.zip
  url: https://pypi.python.org/packages/source/r/redturtle.portlets.inspector/redturtle.portlets.inspector-1.0.0.zip
  md5: 053b2d61f79bc8a65f5d26a8f1caf526
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.portlets.inspector = redturtle.portlets.inspector:main
    #
    # Would create an entry point called redturtle.portlets.inspector that calls redturtle.portlets.inspector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.portlets
    - redturtle.portlets.inspector
    - redturtle.portlets.inspector.browser
    - redturtle.portlets.inspector.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.portlets.inspector
  license: GNU General Public License (GPL)
  summary: 'Redturtle Portlets Inspector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
