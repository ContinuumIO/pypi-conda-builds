package:
  name: django-multilingual-model
  version: "0.6"

source:
  fn: django-multilingual-model-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multilingual-model/django-multilingual-model-0.6.tar.gz
  md5: 7c00536ab0e89d2d19562c537db96ac0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multilingual-model = django-multilingual-model:main
    #
    # Would create an entry point called django-multilingual-model that calls django-multilingual-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - multilingual_model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dokterbob/django-multilingual-model
  license: GNU Affero General Public License v3
  summary: 'Django Simple Multilingual Support for Models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
