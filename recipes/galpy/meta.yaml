package:
  name: galpy
  version: "1.0"

source:
  fn: galpy-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/galpy/galpy-1.0.tar.gz
  md5: 76a526247236f6169dc28d6e8304f040
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - galpy = galpy:main
    #
    # Would create an entry point called galpy that calls galpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.7
    - scipy
    - matplotlib
    - nose

  run:
    - python
    - numpy >=1.7
    - scipy
    - matplotlib
    - nose

test:
  # Python imports
  imports:
    - galpy
    - galpy/actionAngle_src
    - galpy/df_src
    - galpy/orbit_src
    - galpy/potential_src
    - galpy/snapshot_src
    - galpy/util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jobovy/galpy
  license: BSD License
  summary: 'Galactic Dynamics in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
