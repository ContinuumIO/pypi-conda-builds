package:
  name: django-github-hook
  version: "0.1.0"

source:
  fn: django-github-hook-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-github-hook/django-github-hook-0.1.0.tar.gz
  md5: b3021058542088acf183447e012b4784
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-github-hook = django-github-hook:main
    #
    # Would create an entry point called django-github-hook that calls django-github-hook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - djangorestframework

  run:
    - python
    - django
    - djangorestframework

test:
  # Python imports
  imports:
    - github_hook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Django-powered GitHub (& Bitbucket) web hooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
