package:
  name: asyncblink
  version: "0.1.1"

source:
  fn: asyncblink-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncblink/asyncblink-0.1.1.tar.gz
  md5: 33bbcf976ef1f21c285ae3998f4975a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncblink = asyncblink:main
    #
    # Would create an entry point called asyncblink that calls asyncblink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blinker >=1.3

  run:
    - python
    - blinker >=1.3

test:
  # Python imports
  imports:
    - asyncblink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/asyncblink
  license: GNU General Public License (GPL)
  summary: '\nSignals with coroutines!\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
