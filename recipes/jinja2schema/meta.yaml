package:
  name: jinja2schema
  version: "0.1.1"

source:
  fn: jinja2schema-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja2schema/jinja2schema-0.1.1.tar.gz
  md5: f67d8fad05acba9f67eb53c3e77849fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja2schema = jinja2schema:main
    #
    # Would create an entry point called jinja2schema that calls jinja2schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.2

  run:
    - python
    - jinja2 >=2.2

test:
  # Python imports
  imports:
    - jinja2schema
    - jinja2schema.visitors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://jinja2schema.readthedocs.org
  license: BSD License
  summary: 'Type inference for Jinja2 templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
