package:
  name: grovepi
  version: "1.2.2"

source:
  fn: grovepi-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/g/grovepi/grovepi-1.2.2.tar.gz
  md5: fb160f31d663454f156fb0ca4b7ff867
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - grovepi = grovepi:main
    #
    # Would create an entry point called grovepi that calls grovepi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - grovepi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DexterInd/GrovePi
  license: UNKNOWN
  summary: 'This is the python library for GrovePi, which is an open source platform for connecting Grove Sensors to the Raspberry Pi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
