package:
  name: cmsplugin_facebook
  version: "0.2.0"

source:
  fn: cmsplugin_facebook-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_facebook/cmsplugin_facebook-0.2.0.tar.gz
  md5: a105bc23abc420f24718adfe2a2aa59d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_facebook = cmsplugin_facebook:main
    #
    # Would create an entry point called cmsplugin_facebook that calls cmsplugin_facebook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_facebook
    - cmsplugin_facebook.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chrisglass/cmsplugin_facebook
  license: BSD License
  summary: 'Django CMS Facebook Plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
