package:
  name: django-bland
  version: "0.1.0"

source:
  fn: django-bland-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bland/django-bland-0.1.0.tar.gz
  md5: d3afb38da95637a00108f472ce1bca0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bland = django-bland:main
    #
    # Would create an entry point called django-bland that calls django-bland.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bland

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pkqk/django-bland
  license: MIT
  summary: 'A terribly basic file based CMS for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
