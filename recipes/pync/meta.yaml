package:
  name: pync
  version: "1.6.1"

source:
  fn: pync-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pync/pync-1.6.1.tar.gz
  md5: c0ae0aeeef3120cc36379bfa985cc24d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pync = pync:main
    #
    # Would create an entry point called pync that calls pync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=2.0

  run:
    - python
    - python-dateutil >=2.0

test:
  # Python imports
  imports:
    - pync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/setem/pync
  license: MIT
  summary: 'Python Wrapper for Mac OS 10.8 Notification Center'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
