package:
  name: django-email-phone-user
  version: "1.0.3"

source:
  fn: django-email-phone-user-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-phone-user/django-email-phone-user-1.0.3.tar.gz
  md5: 97c9b7f6fdf0bc4b01d64fdc7821d2d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-phone-user = django-email-phone-user:main
    #
    # Would create an entry point called django-email-phone-user that calls django-email-phone-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_phone_user

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/168estate/django-email-phone-user
  license: MIT License
  summary: 'Django Custom User model with email or phone as username'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
