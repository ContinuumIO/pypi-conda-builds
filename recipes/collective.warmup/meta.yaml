package:
  name: collective.warmup
  version: "1.1"

source:
  fn: collective.warmup-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.warmup/collective.warmup-1.1.zip
  md5: 8695b893d24d894726d4578f6706e308
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.warmup = collective.warmup:main
    #
    # Would create an entry point called collective.warmup that calls collective.warmup.main()

    - warmup=collective.warmup.commands:warmup

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - lxml

  run:
    - python
    - setuptools
    - setuptools
    - lxml

test:
  # Python imports
  imports:
    - collective
    - collective.warmup

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - warmup --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License (GPL)
  summary: 'Collective Warmup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
