package:
  name: zgeo.plone.kml
  version: "0.2"

source:
  fn: zgeo.plone.kml-0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zgeo.plone.kml/zgeo.plone.kml-0.2.tar.gz
  md5: ea2795d2fbe86b728e180c45504572e5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zgeo.plone.kml = zgeo.plone.kml:main
    #
    # Would create an entry point called zgeo.plone.kml that calls zgeo.plone.kml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zgeo.kml >=0.4

  run:
    - python
    - setuptools
    - setuptools
    - zgeo.kml >=0.4

test:
  # Python imports
  imports:
    - zgeo
    - zgeo.plone
    - zgeo.plone.kml
    - zgeo.plone.kml.Extensions
    - zgeo.plone.kml.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.gispython.org/projects/PrimaGIS/wiki/zgeo.plone.kml
  license: GPL
  summary: 'KML for Plone content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
