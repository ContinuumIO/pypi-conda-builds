package:
  name: federated_monsters
  version: "0.4.0"

source:
  fn: federated_monsters-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/f/federated_monsters/federated_monsters-0.4.0.tar.gz
  md5: fbf052e165de8421fb1e67778cb9e863
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - federated_monsters = federated_monsters:main
    #
    # Would create an entry point called federated_monsters that calls federated_monsters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bsddb3 >=5.2.0
    - pycrypto >=2.5

  run:
    - python
    - bsddb3 >=5.2.0
    - pycrypto >=2.5

test:
  # Python imports
  imports:
    - federated_monsters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/colatkinson/federated_monsters
  license: BSD License
  summary: 'Federated Monsters is a game that seeks to follow the format of games like Pokemon, but to instead use a federated server format to store and trade creatures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
