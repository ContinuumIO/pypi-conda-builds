package:
  name: django_replicated
  version: "1.2"

source:
  fn: django_replicated-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_replicated/django_replicated-1.2.tar.gz
  md5: a04ccc1321690a856b5a10b7d7431f7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_replicated = django_replicated:main
    #
    # Would create an entry point called django_replicated that calls django_replicated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_replicated

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Django DB router for stateful master-slave replication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
