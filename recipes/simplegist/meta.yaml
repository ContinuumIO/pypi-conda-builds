package:
  name: simplegist
  version: "0.3.2"

source:
  fn: simplegist-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/s/simplegist/simplegist-0.3.2.tar.gz
  md5: 4ea4c9d3db5eb4309cf5f67192945857
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplegist = simplegist:main
    #
    # Would create an entry point called simplegist that calls simplegist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - simplegist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/softvar/gist
  license: MIT
  summary: 'Python wrapper for Gist '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
