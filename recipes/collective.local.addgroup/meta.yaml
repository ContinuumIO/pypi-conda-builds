package:
  name: collective.local.addgroup
  version: "1.2.2"

source:
  fn: collective.local.addgroup-1.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.local.addgroup/collective.local.addgroup-1.2.2.zip
  md5: d2cbccf1ba67b369a94f729b7fb71d8d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.local.addgroup = collective.local.addgroup:main
    #
    # Would create an entry point called collective.local.addgroup that calls collective.local.addgroup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.local.adduser >=1.1
    - plone.app.users >1.0.6
    - plone.app.workflow >=2.0.2

  run:
    - python
    - setuptools
    - collective.local.adduser >=1.1
    - plone.app.users >1.0.6
    - plone.app.workflow >=2.0.2

test:
  # Python imports
  imports:
    - collective
    - collective.local
    - collective.local.addgroup
    - collective.local.addgroup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.local.addgroup
  license: GPL
  summary: 'Allows to create a group from the sharing tab. By Ecreall'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
