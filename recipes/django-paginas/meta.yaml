package:
  name: django-paginas
  version: "0.3.3"

source:
  fn: django-paginas-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paginas/django-paginas-0.3.3.tar.gz
  md5: f728150afb071c8eb14e5ce8b5bc2cb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paginas = django-paginas:main
    #
    # Would create an entry point called django-paginas that calls django-paginas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - paginas
    - paginas.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/semente/django-paginas/
  license: BSD License
  summary: 'A flat pages application for Django based on django.contrib.flatpages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
