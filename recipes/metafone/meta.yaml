package:
  name: metafone
  version: "0.5"

source:
  fn: Metafone-0.5.tar.gz
  url: https://pypi.python.org/packages/source/M/Metafone/Metafone-0.5.tar.gz
  md5: de53b4c513a985cca9b7a48181152b9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metafone = metafone:main
    #
    # Would create an entry point called metafone that calls metafone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - metafone
    - metaphone
    - metaphone.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/al45tair/metaphone
  license: BSD
  summary: 'A Python implementation of the double metaphone algorithms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
