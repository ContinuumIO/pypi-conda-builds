package:
  name: pybip0038
  version: "0.0.7"

source:
  fn: pyBIP0038-0.0.7.zip
  url: https://pypi.python.org/packages/source/p/pyBIP0038/pyBIP0038-0.0.7.zip
  md5: bb6e5b11b593885283a1a47ecd1ea663
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pybip0038 = pybip0038:main
    #
    # Would create an entry point called pybip0038 that calls pybip0038.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyBIP0038
    - pybip0038

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/7trXMk6Z/pyBIP0038
  license: pybird
  summary: 'Python 2/3 functions for implementing BIP0038.  Requires: pycrypto,scrypt.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
