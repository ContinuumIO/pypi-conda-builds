package:
  name: termformat
  version: "0.1.9"

source:
  fn: termformat-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/t/termformat/termformat-0.1.9.tar.gz
  md5: d913fcb604b783e0c2f26bc50346ebb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - termformat = termformat:main
    #
    # Would create an entry point called termformat that calls termformat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - termformat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tyrannosaurus/termformat
  license: MIT
  summary: 'Erlang External Term Format (de)serialization module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
