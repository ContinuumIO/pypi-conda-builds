package:
  name: git-py
  version: "1.0.1"

source:
  fn: git-py-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/git-py/git-py-1.0.1.tar.gz
  md5: 0e5527bb821a6db9634638361c6b8712
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-py = git-py:main
    #
    # Would create an entry point called git-py that calls git-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - infi.execute >=0.1

  run:
    - python
    - infi.execute >=0.1

test:
  # Python imports
  imports:
    - gitpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Python bindings for the Git source control system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
