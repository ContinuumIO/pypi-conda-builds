package:
  name: django-masked-forms
  version: "0.0.2"

source:
  fn: django-masked-forms-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-masked-forms/django-masked-forms-0.0.2.tar.gz
  md5: 2ec7e545150a0f7f2d4ed9e42a258ade
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-masked-forms = django-masked-forms:main
    #
    # Would create an entry point called django-masked-forms that calls django-masked-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - masked_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/comogo/django-masked-forms
  license: MIT
  summary: 'django masked forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
