package:
  name: json-spec
  version: "0.9.9"

source:
  fn: json-spec-0.9.9.tar.gz
  url: https://pypi.python.org/packages/source/j/json-spec/json-spec-0.9.9.tar.gz
  md5: fe5e7d2dfbd2503b294e4e9f99e227ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-spec = json-spec:main
    #
    # Would create an entry point called json-spec that calls json-spec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Implements JSON Schema, JSON Pointer and JSON Reference.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
