package:
  name: aditam.agent
  version: "0.1"

source:
  fn: aditam.agent-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aditam.agent/aditam.agent-0.1.tar.gz
  md5: b9541b03ffa99acbde9990cc699541a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aditam.agent = aditam.agent:main
    #
    # Would create an entry point called aditam.agent that calls aditam.agent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyro >=0.7
    - aditam.core >=0.1
    - setuptools >=0.6c7

  run:
    - python
    - pyro >=0.7
    - aditam.core >=0.1
    - setuptools >=0.6c7

test:
  # Python imports
  imports:
    - aditam
    - aditam.agent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.aditam.org/
  license: GNU General Public License (GPL)
  summary: 'Automated and DIstributed TAsk Manager agent part.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
