package:
  name: django-dotenv
  version: "1.3.0"

source:
  fn: django-dotenv-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dotenv/django-dotenv-1.3.0.tar.gz
  md5: 5b46eee6b2622fbbebe4d6b70f557ccd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dotenv = django-dotenv:main
    #
    # Would create an entry point called django-dotenv that calls django-dotenv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jpadilla/django-dotenv
  license: BSD License
  summary: "foreman reads from .env. manage.py doesn't. Let's fix that."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
