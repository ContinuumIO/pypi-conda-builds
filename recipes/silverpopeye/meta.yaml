package:
  name: silverpopeye
  version: "0.1.0"

source:
  fn: silverpopeye-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/silverpopeye/silverpopeye-0.1.0.tar.gz
  md5: a671eb438a17b8f7db9195dc76e402b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silverpopeye = silverpopeye:main
    #
    # Would create an entry point called silverpopeye that calls silverpopeye.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.0.1
    - lxml >=3.2.4

  run:
    - python
    - requests >=2.0.1
    - lxml >=3.2.4

test:
  # Python imports
  imports:
    - silverpopeye
    - silverpopeye.engage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/silverpopeye/
  license: LICENSE.txt
  summary: "Python client library for Silverpop's API."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
