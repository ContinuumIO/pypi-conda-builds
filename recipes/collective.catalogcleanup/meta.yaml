package:
  name: collective.catalogcleanup
  version: "1.4"

source:
  fn: collective.catalogcleanup-1.4.zip
  url: https://pypi.python.org/packages/source/c/collective.catalogcleanup/collective.catalogcleanup-1.4.zip
  md5: 9c42a7578c9477cdf22cbb5f7719209a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.catalogcleanup = collective.catalogcleanup:main
    #
    # Would create an entry point called collective.catalogcleanup that calls collective.catalogcleanup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.catalogcleanup
    - collective.catalogcleanup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.catalogcleanup
  license: GPL
  summary: 'Remove outdated items from the catalog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
