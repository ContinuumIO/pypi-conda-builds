package:
  name: dj-blog
  version: "0.3.1"

source:
  fn: dj-blog-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dj-blog/dj-blog-0.3.1.tar.gz
  md5: 7b51e1ca0691c5da4e5dc5be2aac6a77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj-blog = dj-blog:main
    #
    # Would create an entry point called dj-blog that calls dj-blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-extensions ==1.3.3
    - mistune ==0.1.0
    - pygments ==1.6

  run:
    - python
    - django >=1.5
    - django-extensions ==1.3.3
    - mistune ==0.1.0
    - pygments ==1.6

test:
  # Python imports
  imports:
    - djblog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/buddylindsey/dj-blog
  license: BSD License
  summary: 'a very basic blog app for a django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
