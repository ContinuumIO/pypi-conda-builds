package:
  name: uclassify
  version: "0.1.2"

source:
  fn: uclassify-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/u/uclassify/uclassify-0.1.2.tar.gz
  md5: cc24f5c939ca085895198ea289349e62
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uclassify = uclassify:main
    #
    # Would create an entry point called uclassify that calls uclassify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - uclassify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/psibi/pyuClassify
  license: GNU General Public License v3 (GPLv3)
  summary: 'Python Wrapper for accessing uClassify services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
