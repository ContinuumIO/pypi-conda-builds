package:
  name: logstash-tail
  version: "0.0.3"

source:
  fn: logstash-tail-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/logstash-tail/logstash-tail-0.0.3.tar.gz
  md5: a55b1e36936ad06319e4807593de4223
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logstash-tail = logstash-tail:main
    #
    # Would create an entry point called logstash-tail that calls logstash-tail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - colorama

  run:
    - python
    - colorama

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ohlol/logstash-tail
  license: BSD License
  summary: 'Tail logstash tcp output'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
