package:
  name: django_auth_krb
  version: "0.1.1"

source:
  fn: django_auth_krb-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_auth_krb/django_auth_krb-0.1.1.tar.gz
  md5: 504c2bbf3e1c89e3e14b5702ee57332d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_auth_krb = django_auth_krb:main
    #
    # Would create an entry point called django_auth_krb that calls django_auth_krb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - kerberos ==1.1.1

  run:
    - python
    - django >=1.3
    - kerberos ==1.1.1

test:
  # Python imports
  imports:
    - django_auth_krb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hlin/django-auth-krb/
  license: MIT License
  summary: 'Django kerberos authentication backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
