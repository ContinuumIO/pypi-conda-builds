package:
  name: glhooks
  version: "0.1.0"

source:
  fn: glhooks-0.1.0.zip
  url: https://pypi.python.org/packages/source/g/glhooks/glhooks-0.1.0.zip
  md5: 74e9c1218e250d92157aead555a4c4dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glhooks = glhooks:main
    #
    # Would create an entry point called glhooks that calls glhooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt >=0.6.1,<0.7

  run:
    - python
    - docopt >=0.6.1,<0.7

test:
  # Python imports
  imports:
    - glhooks
    - glhooks.mailer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/miso-belica/gitlab-webhooks
  license: Apache Software License
  summary: 'Service for automatic updating of git repositories from GitLab.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
