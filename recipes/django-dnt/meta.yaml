package:
  name: django-dnt
  version: "0.1.0"

source:
  fn: django-dnt-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dnt/django-dnt-0.1.0.tar.gz
  md5: edde1e64a9f9d6dfbdf1606f58e1da72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dnt = django-dnt:main
    #
    # Would create an entry point called django-dnt that calls django-dnt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dnt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mozilla/django-dnt
  license: BSD License
  summary: 'Make Django requests aware of the DNT header.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
