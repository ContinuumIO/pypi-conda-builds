package:
  name: zc.loggermonitor
  version: "0.1"

source:
  fn: zc.loggermonitor-0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.loggermonitor/zc.loggermonitor-0.1.tar.gz
  md5: 906c3f2b8a841453a2832e87dbc534c3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.loggermonitor = zc.loggermonitor:main
    #
    # Would create an entry point called zc.loggermonitor that calls zc.loggermonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.loggermonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
