package:
  name: django-smorgasbord
  version: "0.4"

source:
  fn: django-smorgasbord-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smorgasbord/django-smorgasbord-0.4.tar.gz
  md5: 17dffbdf217f79c500944e7bd675308f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smorgasbord = django-smorgasbord:main
    #
    # Would create an entry point called django-smorgasbord that calls django-smorgasbord.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smorgasbord
    - smorgasbord.languages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/smorgasbord/
  license: BSD License
  summary: 'transparent support for multiple templating languages in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
