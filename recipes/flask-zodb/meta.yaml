package:
  name: flask-zodb
  version: "0.1"

source:
  fn: Flask-ZODB-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-ZODB/Flask-ZODB-0.1.tar.gz
  md5: cec1fbfa77809dfdc8af3b139ca2a73d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-zodb = flask-zodb:main
    #
    # Would create an entry point called flask-zodb that calls flask-zodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7
    - zodburi

  run:
    - python
    - flask >=0.7
    - zodburi

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dag/flask-zodb
  license: BSD License
  summary: 'Use the ZODB with Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
