package:
  name: pygments-pprint-sql
  version: "0.1.0"

source:
  fn: pygments-pprint-sql-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pygments-pprint-sql/pygments-pprint-sql-0.1.0.tar.gz
  md5: 91409e6db333c15c6ac72fcedb4d2b2e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygments-pprint-sql = pygments-pprint-sql:main
    #
    # Would create an entry point called pygments-pprint-sql that calls pygments-pprint-sql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments

  run:
    - python
    - setuptools
    - pygments

test:
  # Python imports
  imports:
    - pygments_pprint_sql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/natedub/pygments-pprint-sql
  license: BSD
  summary: 'Pretty format your SQL queries for easier reading.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
