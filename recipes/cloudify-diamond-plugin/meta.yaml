package:
  name: cloudify-diamond-plugin
  version: "1.2a8"

source:
  fn: cloudify-diamond-plugin-1.2a8.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudify-diamond-plugin/cloudify-diamond-plugin-1.2a8.tar.gz
  md5: e846445b97f01e961167ae34a12b2fbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudify-diamond-plugin = cloudify-diamond-plugin:main
    #
    # Would create an entry point called cloudify-diamond-plugin that calls cloudify-diamond-plugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cloudify-plugins-common >=3.2a8
    - diamond ==3.5.0
    - configobj ==5.0.6
    - psutil ==2.1.1

  run:
    - python
    - cloudify-plugins-common >=3.2a8
    - diamond ==3.5.0
    - configobj ==5.0.6
    - psutil ==2.1.1

test:
  # Python imports
  imports:
    - cloudify_handler
    - diamond_agent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Cloudify Diamond monitoring plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
