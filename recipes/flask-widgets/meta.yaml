package:
  name: flask-widgets
  version: "0.4"

source:
  fn: Flask-Widgets-0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Widgets/Flask-Widgets-0.4.tar.gz
  md5: 948768c8857ff404bd8b62972e398416
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-widgets = flask-widgets:main
    #
    # Would create an entry point called flask-widgets that calls flask-widgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rgv151/flask-widgets
  license: MIT License
  summary: 'This extension provides basic template widget feature for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
