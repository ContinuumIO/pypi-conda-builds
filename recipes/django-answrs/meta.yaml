package:
  name: django-answrs
  version: "1.1"

source:
  fn: django-answrs-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-answrs/django-answrs-1.1.tar.gz
  md5: fb4fc4d9d1ec05f78c348cd3671c2144
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-answrs = django-answrs:main
    #
    # Would create an entry point called django-answrs that calls django-answrs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - answrs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.agiliq.com/
  license: GNU General Public License (GPL)
  summary: 'A generic Djnago app to create a question and answer site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
