package:
  name: dj-cmdbuild
  version: "0.1dev-r23"

source:
  fn: dj-cmdbuild-0.1dev-r23.tar.gz
  url: https://pypi.python.org/packages/source/d/dj-cmdbuild/dj-cmdbuild-0.1dev-r23.tar.gz
  md5: 30494cb21b4dab5cf6e1543d5a74bef2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj-cmdbuild = dj-cmdbuild:main
    #
    # Would create an entry point called dj-cmdbuild that calls dj-cmdbuild.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_cmdbuild
    - django_cmdbuild.models
    - django_cmdbuild.remoteapi
    - django_cmdbuild.serializer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dj-cmdbuild
  license: GPL
  summary: 'Tools for interfacing Django with CMDBuild databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
