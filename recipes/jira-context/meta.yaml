package:
  name: jira-context
  version: "1.0.0"

source:
  fn: jira-context-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jira-context/jira-context-1.0.0.tar.gz
  md5: f08ecd6a1673e8d8268e63e977d60aca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jira-context = jira-context:main
    #
    # Would create an entry point called jira-context that calls jira-context.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jira

  run:
    - python
    - jira

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Robpol86/jira-context
  license: MIT License
  summary: 'Cache JIRA basic authentication sessions to disk for console apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
