package:
  name: hl.colours
  version: "0.1.4"

source:
  fn: hl.colours-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hl.colours/hl.colours-0.1.4.tar.gz
  md5: 3615aed62de03724f595bb0c25812604
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hl.colours = hl.colours:main
    #
    # Would create an entry point called hl.colours that calls hl.colours.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hllau/hl.colours
  license: Apache Software License
  summary: 'A simple way to add colours to terminal output in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
