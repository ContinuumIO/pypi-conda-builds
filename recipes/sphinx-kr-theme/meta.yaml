package:
  name: sphinx-kr-theme
  version: "0.2.1"

source:
  fn: sphinx-kr-theme-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-kr-theme/sphinx-kr-theme-0.2.1.tar.gz
  md5: 8ae5518de7ec48f7ff8e6f13e7f3d53d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-kr-theme = sphinx-kr-theme:main
    #
    # Would create an entry point called sphinx-kr-theme that calls sphinx-kr-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - sphinx_kr_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tonyseek/sphinx-kr-theme
  license: BSD License
  summary: 'The third-part package of kennethreitz/kr-sphinx-themes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
