package:
  name: plone.app.folder
  version: "1.1.0"

source:
  fn: plone.app.folder-1.1.0.zip
  url: https://pypi.python.org/packages/source/p/plone.app.folder/plone.app.folder-1.1.0.zip
  md5: cf3c98f4342737fd8c161ec915b0d5c1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.folder = plone.app.folder:main
    #
    # Would create an entry point called plone.app.folder that calls plone.app.folder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - products.cmfcore
    - products.genericsetup
    - zope2
    - plone.folder
    - setuptools
    - plone.app.layout

  run:
    - python
    - products.cmfcore
    - products.genericsetup
    - zope2
    - plone.folder
    - setuptools
    - plone.app.layout

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.folder
    - plone.app.folder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.folder/
  license: GNU General Public License (GPL)
  summary: 'Integration package for `plone.folder` into Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
