package:
  name: blockext
  version: "0.2.0a2"

source:
  fn: blockext-0.2.0a2.tar.gz
  url: https://pypi.python.org/packages/source/b/blockext/blockext-0.2.0a2.tar.gz
  md5: cf4215e1c06a6322da7a6eeea861d586
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockext = blockext:main
    #
    # Would create an entry point called blockext that calls blockext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - future >=0.12

  run:
    - python
    - future >=0.12

test:
  # Python imports
  imports:
    - blockext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blockext/blockext
  license: MIT
  summary: 'Module for writing Scratch 2.0 and Snap! extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
