package:
  name: monary
  version: "0.4.0.post2"

source:
  fn: Monary-0.4.0.post2.tar.gz
  url: https://pypi.python.org/packages/source/M/Monary/Monary-0.4.0.post2.tar.gz
  md5: c9b145bb9c7b2aa4efaac7d851a074e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monary = monary:main
    #
    # Would create an entry point called monary that calls monary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monary

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/djcbeach/monary/
  license: Apache Software License
  summary: 'Monary performs high-performance column queries on MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
