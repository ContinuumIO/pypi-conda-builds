package:
  name: djlime
  version: "0.0.35"

source:
  fn: djlime-0.0.35.tar.gz
  url: https://pypi.python.org/packages/source/d/djlime/djlime-0.0.35.tar.gz
  md5: 013b662f5bd634114c861be721e2e785
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djlime = djlime:main
    #
    # Would create an entry point called djlime that calls djlime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - simplejson

  run:
    - python
    - django >=1.5
    - simplejson

test:
  # Python imports
  imports:
    - djlime
    - djlime.fabfile
    - djlime.forms
    - djlime.templatetags
    - djlime.utils
    - djlime.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/freshlimestudio/djlime/
  license: MIT License
  summary: 'Django goodies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
