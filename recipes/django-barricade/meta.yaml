package:
  name: django-barricade
  version: "0.1.0"

source:
  fn: django-barricade-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-barricade/django-barricade-0.1.0.tar.gz
  md5: fde828fe15abaa01bf6571eb6ca90740
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-barricade = django-barricade:main
    #
    # Would create an entry point called django-barricade that calls django-barricade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_barricade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wancw/django-barricade
  license: MIT
  summary: 'Simulate a slow connection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
