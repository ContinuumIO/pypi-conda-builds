package:
  name: django-prices
  version: "0.3.4"

source:
  fn: django-prices-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-prices/django-prices-0.3.4.tar.gz
  md5: f4261a8326ce877fe730cebaadcdd798
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-prices = django-prices:main
    #
    # Would create an entry point called django-prices that calls django-prices.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - prices >=0.5,<0.6a0

  run:
    - python
    - django
    - prices >=0.5,<0.6a0

test:
  # Python imports
  imports:
    - django_prices
    - django_prices.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mirumee/django-prices
  license: BSD License
  summary: 'Django fields for the prices module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
