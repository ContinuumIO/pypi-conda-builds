package:
  name: googlesheets
  version: "0.1"

source:
  fn: googlesheets-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/googlesheets/googlesheets-0.1.tar.gz
  md5: 36523c9c7a135ea499b436a38c5d435a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googlesheets = googlesheets:main
    #
    # Would create an entry point called googlesheets that calls googlesheets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata >=2.0.18
    - normality

  run:
    - python
    - gdata >=2.0.18
    - normality

test:
  # Python imports
  imports:
    - googlesheets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pudo/googlesheets
  license: MIT License
  summary: 'Simply read and write Google Spreadsheets from Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
