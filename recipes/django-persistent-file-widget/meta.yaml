package:
  name: django-persistent-file-widget
  version: "0.0.1"

source:
  fn: django-persistent-file-widget-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-persistent-file-widget/django-persistent-file-widget-0.0.1.tar.gz
  md5: 0c9f06e7e9584893f7863c42003df494
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-persistent-file-widget = django-persistent-file-widget:main
    #
    # Would create an entry point called django-persistent-file-widget that calls django-persistent-file-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - persistent_widget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sandersnewmedia/django-persistent-file-widget
  license: django-pesapal
  summary: 'A django form file widget that persists between erroneous form submissions!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
