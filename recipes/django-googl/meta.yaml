package:
  name: django-googl
  version: "0.1.3"

source:
  fn: django-googl-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googl/django-googl-0.1.3.tar.gz
  md5: 584a6e079eb3388ed7db784071520c83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googl = django-googl:main
    #
    # Would create an entry point called django-googl that calls django-googl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - googl
    - googl.templatetags
    - googl.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/avelino/django-googl/
  license: BSD License
  summary: u'Django shorturl via goo.gl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
