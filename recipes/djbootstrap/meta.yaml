package:
  name: djbootstrap
  version: "3.0.1"

source:
  fn: djbootstrap-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djbootstrap/djbootstrap-3.0.1.tar.gz
  md5: 8a92ea4530b1978eebb81afad29a1b77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djbootstrap = djbootstrap:main
    #
    # Would create an entry point called djbootstrap that calls djbootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - djbootstrap
    - djbootstrap.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madisona/djbootstrap
  license: BSD License
  summary: 'Kickstart your django app with twitter bootstrap.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
