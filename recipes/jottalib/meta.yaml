package:
  name: jottalib
  version: "0.2.10-1"

source:
  fn: jottalib-0.2.10-1.tar.gz
  url: https://pypi.python.org/packages/source/j/jottalib/jottalib-0.2.10-1.tar.gz
  md5: 61ce2000c42c9532f22702e85d8459e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jottalib = jottalib:main
    #
    # Would create an entry point called jottalib that calls jottalib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.1.0
    - requests_cache ==0.4.4
    - python-dateutil
    - lxml

  run:
    - python
    - requests ==2.1.0
    - requests_cache ==0.4.4
    - python-dateutil
    - lxml

test:
  # Python imports
  imports:
    - jottalib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitorious.org/jottafs
  license: GPLv3
  summary: 'A library to access the JottaCloud API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
