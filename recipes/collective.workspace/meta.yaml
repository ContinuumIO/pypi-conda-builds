package:
  name: collective.workspace
  version: "1.1"

source:
  fn: collective.workspace-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.workspace/collective.workspace-1.1.zip
  md5: 3b721c565dd5a94a2b651c10723b1448
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.workspace = collective.workspace:main
    #
    # Would create an entry point called collective.workspace that calls collective.workspace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.formwidget.autocomplete

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.formwidget.autocomplete

test:
  # Python imports
  imports:
    - collective
    - collective.workspace
    - collective.workspace.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.workspace
  license: gpl
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
