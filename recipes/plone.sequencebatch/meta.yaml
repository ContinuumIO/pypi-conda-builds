package:
  name: plone.sequencebatch
  version: "1.0a1"

source:
  fn: plone.sequencebatch-1.0a1.zip
  url: https://pypi.python.org/packages/source/p/plone.sequencebatch/plone.sequencebatch-1.0a1.zip
  md5: d377eef343af6fc9112bea9cfc8f1e07
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.sequencebatch = plone.sequencebatch:main
    #
    # Would create an entry point called plone.sequencebatch that calls plone.sequencebatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - extensionclass
    - zope2

  run:
    - python
    - setuptools
    - extensionclass
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.sequencebatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.sequencebatch
  license: GPL
  summary: 'Facilities for creating a batched sequence.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
