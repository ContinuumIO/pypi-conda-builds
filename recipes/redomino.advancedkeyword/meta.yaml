package:
  name: redomino.advancedkeyword
  version: "1.4"

source:
  fn: redomino.advancedkeyword-1.4.tar.gz
  url: https://pypi.python.org/packages/source/r/redomino.advancedkeyword/redomino.advancedkeyword-1.4.tar.gz
  md5: cb2954196328f57b73cf3cd8abfba2b0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.advancedkeyword = redomino.advancedkeyword:main
    #
    # Would create an entry point called redomino.advancedkeyword that calls redomino.advancedkeyword.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot
    - plone.browserlayer
    - plone.indexer
    - plone.app.vocabularies
    - plone.app.z3cform
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot
    - plone.browserlayer
    - plone.indexer
    - plone.app.vocabularies
    - plone.app.z3cform
    - products.cmfplone

test:
  # Python imports
  imports:
    - redomino
    - redomino.advancedkeyword
    - redomino.advancedkeyword.browser
    - redomino.advancedkeyword.portlets
    - redomino.advancedkeyword.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.advancedkeyword
  license: GPL
  summary: 'Redomino Advanced Keyword Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
