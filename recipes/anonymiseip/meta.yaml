package:
  name: anonymiseip
  version: "0.0.1"

source:
  fn: anonymiseip-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/anonymiseip/anonymiseip-0.0.1.tar.gz
  md5: 72f4b51be93c59c34dc45a91127886b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anonymiseip = anonymiseip:main
    #
    # Would create an entry point called anonymiseip that calls anonymiseip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - anonymiseip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/anonymiseip
  license: GNU Affero General Public License v3
  summary: 'Web service for IP anonymisation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
