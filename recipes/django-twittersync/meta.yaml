package:
  name: django-twittersync
  version: "0.5"

source:
  fn: django-twittersync-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twittersync/django-twittersync-0.5.tar.gz
  md5: 31ce0e9e86f5536c204c562c9e86309a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twittersync = django-twittersync:main
    #
    # Would create an entry point called django-twittersync that calls django-twittersync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - twittersync
    - twittersync.management
    - twittersync.management.commands
    - twittersync.migrations
    - twittersync.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/petersanchez/twittersync/
  license: BSD License
  summary: 'Django app to sync Twitter stream to local DB.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
