package:
  name: pymultinest
  version: "1.4"

source:
  fn: pymultinest-1.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pymultinest/pymultinest-1.4.tar.gz
  md5: 2c476d4502cde31430800cec2dcf69f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pymultinest = pymultinest:main
    #
    # Would create an entry point called pymultinest that calls pymultinest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyapemost
    - pycuba
    - pymultinest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://johannesbuchner.github.com/PyMultiNest/
  license: GPLv3
  summary: 'Access modules for MultiNest and APEMoST'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
