package:
  name: miniature
  version: "0.2.0"

source:
  fn: Miniature-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Miniature/Miniature-0.2.0.tar.gz
  md5: bae589bfeed10e2de79a16cf45cf08d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - miniature = miniature:main
    #
    # Would create an entry point called miniature that calls miniature.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - miniature
    - miniature.processor
    - miniature.thumbnails
    - miniature.thumbnails.templatetags
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/olivier-m/miniature
  license: BSD License
  summary: 'Multi backend image processing and thumbnailer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
