package:
  name: django_env_server
  version: "0.0.2"

source:
  fn: django_env_server-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_env_server/django_env_server-0.0.2.tar.gz
  md5: 038a5df528fb789793107a522d8fad95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_env_server = django_env_server:main
    #
    # Would create an entry point called django_env_server that calls django_env_server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_env_server
    - django_env_server.management
    - django_env_server.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mcfstr/django-env-server
  license: MIT
  summary: 'Load environment variables into the Django development server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
