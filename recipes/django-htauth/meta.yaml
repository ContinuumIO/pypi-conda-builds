package:
  name: django-htauth
  version: "0.1"

source:
  fn: django-htauth-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-htauth/django-htauth-0.1.tar.gz
  md5: e82b899b59c62328b73d5d84154d4cf5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-htauth = django-htauth:main
    #
    # Would create an entry point called django-htauth that calls django-htauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - htauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'htpasswd authentication backend for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
