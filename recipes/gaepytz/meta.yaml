package:
  name: gaepytz
  version: "2011h"

source:
  fn: gaepytz-2011h.tar.gz
  url: https://pypi.python.org/packages/source/g/gaepytz/gaepytz-2011h.tar.gz
  md5: b7abe173cd98b417fab3e91c1498cdd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaepytz = gaepytz:main
    #
    # Would create an entry point called gaepytz that calls gaepytz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gaepytz
    - pytz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/gae-pytz/
  license: MIT License
  summary: 'A version of pytz that works well on Google App Engine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
