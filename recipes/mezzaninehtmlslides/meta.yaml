package:
  name: mezzaninehtmlslides
  version: "0.1.5dev"

source:
  fn: mezzanineHtmlSlides-0.1.5dev.tar.gz
  url: https://pypi.python.org/packages/source/m/mezzanineHtmlSlides/mezzanineHtmlSlides-0.1.5dev.tar.gz
  md5: bb2f17f2014d4cae9f344bdd2131f88e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mezzaninehtmlslides = mezzaninehtmlslides:main
    #
    # Would create an entry point called mezzaninehtmlslides that calls mezzaninehtmlslides.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mezzanine

  run:
    - python
    - mezzanine

test:
  # Python imports
  imports:
    - mezzaninehtmlslides

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tjankov/mezzanineHtmlSlides
  license: BSD License
  summary: 'Plug a html slideshow into your mezzanine website on all pages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
