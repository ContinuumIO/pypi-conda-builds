package:
  name: django-conversejs
  version: "0.3.4"

source:
  fn: django-conversejs-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-conversejs/django-conversejs-0.3.4.tar.gz
  md5: f8914210306590700203293cd5702771
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-conversejs = django-conversejs:main
    #
    # Would create an entry point called django-conversejs that calls django-conversejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pure-sasl ==0.1.5
    - sleekxmpp ==1.1.11
    - dnspython ==1.11.1

  run:
    - python
    - pure-sasl ==0.1.5
    - sleekxmpp ==1.1.11
    - dnspython ==1.11.1

test:
  # Python imports
  imports:
    - conversejs
    - conversejs.migrations
    - conversejs.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TracyWebTech/django-conversejs
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Adds converse.js (javascript XMPP client) to Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
