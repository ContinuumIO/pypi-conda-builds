package:
  name: django-charsleft-widget
  version: "0.1.4"

source:
  fn: django-charsleft-widget-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-charsleft-widget/django-charsleft-widget-0.1.4.tar.gz
  md5: 6e97d9ad742b7f123798de13abaadfc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-charsleft-widget = django-charsleft-widget:main
    #
    # Would create an entry point called django-charsleft-widget that calls django-charsleft-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - charsleft_widget
    - charsleft_widget.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bashu/django-charsleft-widget
  license: BSD License
  summary: 'Custom widget that limits the number of characters that can be entered in a textarea field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
