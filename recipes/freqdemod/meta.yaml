package:
  name: freqdemod
  version: "0.2.1"

source:
  fn: FreqDemod-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FreqDemod/FreqDemod-0.2.1.tar.gz
  md5: fd15deb520a2b9a77076a1760bd17c95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - freqdemod = freqdemod:main
    #
    # Would create an entry point called freqdemod that calls freqdemod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - matplotlib
    - h5py

  run:
    - python
    - numpy
    - scipy
    - matplotlib
    - h5py

test:
  # Python imports
  imports:
    - freqdemod
    - freqdemod.hdf5
    - freqdemod.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JohnMarohn/FreqDemod
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'This package provides functions for analyzing and plotting the\ntime-dependent frequency and amplitude of a sinusoidally oscillating signal.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
