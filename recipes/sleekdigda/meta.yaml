package:
  name: sleekdigda
  version: "0.2.0"

source:
  fn: sleekdigda-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sleekdigda/sleekdigda-0.2.0.tar.gz
  md5: 973d220294088833fec15426121810e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sleekdigda = sleekdigda:main
    #
    # Would create an entry point called sleekdigda that calls sleekdigda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sleekxmpp >=1.3.0,<1.4.0

  run:
    - python
    - sleekxmpp >=1.3.0,<1.4.0

test:
  # Python imports
  imports:
    - sleekdigda

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'XEP-Digda - SleekXMPP plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
