package:
  name: collective.namedblobfile
  version: "0.3"

source:
  fn: collective.namedblobfile-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.namedblobfile/collective.namedblobfile-0.3.tar.gz
  md5: b1cdeb2e332ef29903cc22060f020dd2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.namedblobfile = collective.namedblobfile:main
    #
    # Would create an entry point called collective.namedblobfile that calls collective.namedblobfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.namedfile ==1.1
    - z3c.blobfile ==0.1.0

  run:
    - python
    - setuptools
    - collective.namedfile ==1.1
    - z3c.blobfile ==0.1.0

test:
  # Python imports
  imports:
    - collective
    - collective.namedblobfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.namedblobfile
  license: Zope Public License
  summary: 'File field and widget with enhancements for zope3 (using blobs for storage).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
