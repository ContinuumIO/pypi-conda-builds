package:
  name: django-cms-named-menus
  version: "0.1.10"

source:
  fn: django-cms-named-menus-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-named-menus/django-cms-named-menus-0.1.10.tar.gz
  md5: 2699902043b6910530fa4f84dc8cfad2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-named-menus = django-cms-named-menus:main
    #
    # Would create an entry point called django-cms-named-menus that calls django-cms-named-menus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-cms >=3.0
    - jsonfield >=1.0.0
    - django-autoslug ==1.7.2

  run:
    - python
    - django >=1.6
    - django-cms >=3.0
    - jsonfield >=1.0.0
    - django-autoslug ==1.7.2

test:
  # Python imports
  imports:
    - cms_named_menus
    - cms_named_menus.migrations
    - cms_named_menus.south_migrations
    - cms_named_menus.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryanbagwell/django-cms-named-menus/
  license: BSD
  summary: 'Allows you to add named menus like Wordpress'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
