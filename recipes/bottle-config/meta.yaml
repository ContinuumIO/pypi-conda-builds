package:
  name: bottle-config
  version: "0.1.2"

source:
  fn: bottle-config-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-config/bottle-config-0.1.2.tar.gz
  md5: 7a40422c1714ec93a217d318239540eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-config = bottle-config:main
    #
    # Would create an entry point called bottle-config that calls bottle-config.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle

  run:
    - python
    - bottle

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klen/bottle-config
  license: BSD License
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
