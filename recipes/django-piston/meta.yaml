package:
  name: django-piston
  version: "0.2.3"

source:
  fn: django-piston-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-piston/django-piston-0.2.3.tar.gz
  md5: 8b040d426793cf22ce89543e059cd6e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-piston = django-piston:main
    #
    # Would create an entry point called django-piston that calls django-piston.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - piston

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jespern/django-piston/wiki/Home
  license: BSD License
  summary: 'Piston is a Django mini-framework creating APIs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
