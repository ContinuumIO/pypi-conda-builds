package:
  name: django-uc
  version: "0.0.1"

source:
  fn: django-uc-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uc/django-uc-0.0.1.tar.gz
  md5: f823e674da6f37a9924d4f7bef4358de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uc = django-uc:main
    #
    # Would create an entry point called django-uc that calls django-uc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.15

  run:
    - python
    - django >=1.4.15

test:
  # Python imports
  imports:
    - uc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/FundedByMe/django-uc
  license: UNKNOWN
  summary: 'Django package that helps in your UC integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
