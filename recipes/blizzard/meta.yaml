package:
  name: blizzard
  version: "0.0.3"

source:
  fn: blizzard-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/blizzard/blizzard-0.0.3.tar.gz
  md5: f8deb2994beeb4f53c3f4ed310614050
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blizzard = blizzard:main
    #
    # Would create an entry point called blizzard that calls blizzard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - networkx
    - special_snowflake
    - requests
    - jumble
    - picklecache

  run:
    - python
    - networkx
    - special_snowflake
    - requests
    - jumble
    - picklecache

test:
  # Python imports
  imports:
    - blizzard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/blizzard.git
  license: AGPL
  summary: 'Find the unique indices for a ton of datasets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
