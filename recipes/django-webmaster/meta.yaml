package:
  name: django-webmaster
  version: "0.0.2"

source:
  fn: django-webmaster-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webmaster/django-webmaster-0.0.2.tar.gz
  md5: 4fb36b47854c004e298eac65397bdcab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webmaster = django-webmaster:main
    #
    # Would create an entry point called django-webmaster that calls django-webmaster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - webmaster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joncombe/django-webmaster
  license: BSD License
  summary: 'A very basic tool to keep track of your domain names and servers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
