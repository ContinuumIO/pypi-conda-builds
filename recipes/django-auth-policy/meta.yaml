package:
  name: django-auth-policy
  version: "0.9.10"

source:
  fn: django-auth-policy-0.9.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-policy/django-auth-policy-0.9.10.tar.gz
  md5: 89909ac4e78c221fe222d89a0711e794
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-policy = django-auth-policy:main
    #
    # Would create an entry point called django-auth-policy that calls django-auth-policy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - django_auth_policy
    - django_auth_policy.management
    - django_auth_policy.management.commands
    - django_auth_policy.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rudolphfroger/django-auth-policy
  license: BSD License
  summary: 'Enforces a couple of common authentication policies for the Django web framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
