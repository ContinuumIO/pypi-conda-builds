package:
  name: l_i_s
  version: "0.2"

source:
  fn: L_I_S.tar.gz
  url: https://pypi.python.org/packages/source/L/L_I_S/L_I_S.tar.gz
  md5: 2d4c75ff9a9a12fa47a3113336d3066b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - l_i_s = l_i_s:main
    #
    # Would create an entry point called l_i_s that calls l_i_s.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - list_operations.unnest

  run:
    - python
    - list_operations.unnest

test:
  # Python imports
  imports:
    - L_I_S
    - l_i_s

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Jana-A/L_I_S/releases
  license: MIT License
  summary: 'A function that outputs the longest increasing subsequenes of a list of unique numbers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
