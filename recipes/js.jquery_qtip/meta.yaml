package:
  name: js.jquery_qtip
  version: "1.0.0"

source:
  fn: js.jquery_qtip-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_qtip/js.jquery_qtip-1.0.0.tar.gz
  md5: fe9705101b8d9011a09fdeafda69be2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_qtip = js.jquery_qtip:main
    #
    # Would create an entry point called js.jquery_qtip that calls js.jquery_qtip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_qtip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jquery.qTip'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
