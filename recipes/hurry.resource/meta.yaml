package:
  name: hurry.resource
  version: "0.10"

source:
  fn: hurry.resource-0.10.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.resource/hurry.resource-0.10.tar.gz
  md5: 0187bee8cbb72f30d73ccf54446e3137
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.resource = hurry.resource:main
    #
    # Would create an entry point called hurry.resource that calls hurry.resource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hurry
    - hurry.resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hurry.resource
  license: ZPL 2.1
  summary: 'Flexible resources for web applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
