package:
  name: allink_essentials
  version: "1.0.13"

source:
  fn: allink_essentials-1.0.13.tar.gz
  url: https://pypi.python.org/packages/source/a/allink_essentials/allink_essentials-1.0.13.tar.gz
  md5: 292002d72658850fb347ff4c24d145a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - allink_essentials = allink_essentials:main
    #
    # Would create an entry point called allink_essentials that calls allink_essentials.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - allink_essentials
    - allink_essentials.analytics
    - allink_essentials.analytics.templatetags
    - allink_essentials.fabfiles
    - allink_essentials.feincms_extensions
    - allink_essentials.in_footer
    - allink_essentials.in_footer.templatetags
    - allink_essentials.logging
    - allink_essentials.mailchimp_api
    - allink_essentials.middleware
    - allink_essentials.search
    - allink_essentials.storage
    - allink_essentials.view

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/allink/allink-essentials/
  license: BSD License
  summary: 'collection of code fragments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
