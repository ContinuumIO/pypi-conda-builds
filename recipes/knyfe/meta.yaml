package:
  name: knyfe
  version: "0.4.2"

source:
  fn: knyfe-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/k/knyfe/knyfe-0.4.2.tar.gz
  md5: 0f7ce1b5b1b7b188852f2a6a12f62e8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - knyfe = knyfe:main
    #
    # Would create an entry point called knyfe that calls knyfe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - tablib
    - simplejson

  run:
    - python
    - numpy
    - tablib
    - simplejson

test:
  # Python imports
  imports:
    - knyfe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://maebert.github.com/knyfe
  license: MIT License
  summary: 'A utility for rapid exploration and preprocessing of datasets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
