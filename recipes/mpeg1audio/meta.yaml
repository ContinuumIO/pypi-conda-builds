package:
  name: mpeg1audio
  version: "0.5.2"

source:
  fn: mpeg1audio-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mpeg1audio/mpeg1audio-0.5.2.tar.gz
  md5: 6305ff22d6b0135d80b3cd407b0b4bbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpeg1audio = mpeg1audio:main
    #
    # Would create an entry point called mpeg1audio that calls mpeg1audio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mpeg1audio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Ciantic/mpeg1audio/
  license: FreeBSD
  summary: 'Python MPEG-1 Audio information retrieval package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
