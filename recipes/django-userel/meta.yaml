package:
  name: django-userel
  version: "0.0.3"

source:
  fn: django-userel-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-userel/django-userel-0.0.3.tar.gz
  md5: a6e507ddd2d8b4e235e98a2c8caf4e48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-userel = django-userel:main
    #
    # Would create an entry point called django-userel that calls django-userel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - setuptools-git

  run:
    - python
    - distribute
    - setuptools-git

test:
  # Python imports
  imports:
    - packageutils
    - userel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-userel
  license: MIT License
  summary: 'Extend ForeignKey field for User. It support ``auto_now`` and ``auto_now_add``'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
