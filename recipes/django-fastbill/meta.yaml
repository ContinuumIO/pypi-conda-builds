package:
  name: django-fastbill
  version: "0.2.6"

source:
  fn: django-fastbill-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fastbill/django-fastbill-0.2.6.tar.gz
  md5: d22beef6284a5d71ae06fd0f3c9fba09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fastbill = django-fastbill:main
    #
    # Would create an entry point called django-fastbill that calls django-fastbill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - fastbill

  run:
    - python
    - requests
    - fastbill

test:
  # Python imports
  imports:
    - django_fastbill
    - django_fastbill.management.commands
    - django_fastbill.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jayfk/django-fastbill
  license: MIT License
  summary: 'A thin python wrapper for the fastbill API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
