package:
  name: django-inlinify
  version: "0.0.15"

source:
  fn: django-inlinify-0.0.15.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inlinify/django-inlinify-0.0.15.tar.gz
  md5: d9c5f17f98c5d903c06f5a092aaad106
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inlinify = django-inlinify:main
    #
    # Would create an entry point called django-inlinify that calls django-inlinify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - cssselect
    - cssutils
    - django >=1.5
    - requests

  run:
    - python
    - lxml
    - cssselect
    - cssutils
    - django >=1.5
    - requests

test:
  # Python imports
  imports:
    - django_inlinify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ssaw/django-inlinify/
  license: Apache Software License
  summary: "In-lines CSS into HTML and leverages Django's caching framework."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
