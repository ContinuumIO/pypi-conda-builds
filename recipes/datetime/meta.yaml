package:
  name: datetime
  version: "4.0.1"

source:
  fn: DateTime-4.0.1.zip
  url: https://pypi.python.org/packages/source/D/DateTime/DateTime-4.0.1.zip
  md5: 2241f4a18a7cf16f1d6e8e324fca0ef0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datetime = datetime:main
    #
    # Would create an entry point called datetime that calls datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - pytz

  run:
    - python
    - zope.interface
    - pytz

test:
  # Python imports
  imports:
    - DateTime
    - DateTime.tests
    - datetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/DateTime
  license: Zope Public License
  summary: "This package provides a DateTime data type, as known from Zope 2.\nUnless you need to communicate with Zope 2 APIs, you're probably\nbetter off using Python's built-in datetime module."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
