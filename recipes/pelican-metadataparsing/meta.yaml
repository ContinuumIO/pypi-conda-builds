package:
  name: pelican-metadataparsing
  version: "0.1.0"

source:
  fn: pelican-metadataparsing-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-metadataparsing/pelican-metadataparsing-0.1.0.tar.gz
  md5: 6c685ff3ae7205c54ac5beaa1f63518e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-metadataparsing = pelican-metadataparsing:main
    #
    # Would create an entry point called pelican-metadataparsing that calls pelican-metadataparsing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican >=3.4.0

  run:
    - python
    - pelican >=3.4.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AlexJF/pelican-metadataparsing
  license: Apache
  summary: 'Allow definition of custom metadata parsers for Pelican content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
