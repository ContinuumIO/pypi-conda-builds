package:
  name: easy_test
  version: "0.0.1"

source:
  fn: easy_test-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/easy_test/easy_test-0.0.1.tar.gz
  md5: 3d0889eee297f1c500c35fb7e184784c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy_test = easy_test:main
    #
    # Would create an entry point called easy_test that calls easy_test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - selenium >=2.42.1

  run:
    - python
    - selenium >=2.42.1

test:
  # Python imports
  imports:
    - easy_test
    - easy_test_selenium
    - easy_test_selenium.commands
    - easy_test_selenium.templates
    - easy_test_selenium.templates.project
    - easy_test_selenium.templates.project.principal
    - easy_test_selenium.templates.test_name

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carloshs92/easy_test_selenium
  license: UNKNOWN
  summary: 'A python mini framework for tests that use Selenium'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
