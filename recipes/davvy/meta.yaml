package:
  name: davvy
  version: "0.1.1"

source:
  fn: davvy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/davvy/davvy-0.1.1.tar.gz
  md5: b0cee7a60218800e432c12c3fa70beef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - davvy = davvy:main
    #
    # Would create an entry point called davvy that calls davvy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - lxml

  run:
    - python
    - django
    - lxml

test:
  # Python imports
  imports:
    - davvy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unbit/davvy
  license: MIT License
  summary: 'A Django application for building WebDAV services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
