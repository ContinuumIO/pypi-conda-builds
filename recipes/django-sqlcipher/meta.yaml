package:
  name: django-sqlcipher
  version: "0.1.1"

source:
  fn: django-sqlcipher-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sqlcipher/django-sqlcipher-0.1.1.tar.gz
  md5: bc43cfa5db1cfe62071761e61254818c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sqlcipher = django-sqlcipher:main
    #
    # Would create an entry point called django-sqlcipher that calls django-sqlcipher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sqlcipher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codasus/django-sqlcipher
  license: Creative Commons Attribution-ShareAlike 3.0 Unported License
  summary: 'SQLCipher support for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
