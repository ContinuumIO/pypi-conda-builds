package:
  name: commandor
  version: "0.1.6"

source:
  fn: commandor-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/commandor/commandor-0.1.6.tar.gz
  md5: 9e1096358eb02127f4b13016e4cc9367
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commandor = commandor:main
    #
    # Would create an entry point called commandor that calls commandor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - commandor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Lispython/commandor
  license: BSD License
  summary: 'Simple wrapper to parse nested script options and args'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
