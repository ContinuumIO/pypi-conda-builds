package:
  name: mediawiki
  version: "0.0.1"

source:
  fn: mediawiki-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mediawiki/mediawiki-0.0.1.tar.gz
  md5: 37ac1c4c5b2ebf8d8537292bc0c9dd92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mediawiki = mediawiki:main
    #
    # Would create an entry point called mediawiki that calls mediawiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mediawiki
    - mediawiki.doc
    - mediawiki.wikimarkup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zikzakmedia/python-mediawiki
  license: GNU General Public License (GPL)
  summary: 'MediaWiki Syntax to HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
