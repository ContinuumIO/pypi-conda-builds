package:
  name: czipfile
  version: "1.0.0"

source:
  fn: czipfile-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/czipfile/czipfile-1.0.0.tar.gz
  md5: c1dd2af2ade7e216214c35a5301910b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - czipfile = czipfile:main
    #
    # Would create an entry point called czipfile that calls czipfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - czipfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/czipfile
  license: Python Software Foundation License
  summary: 'A replacement for the builtin zipfile module, with fast, C-based zipfile decryption'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
