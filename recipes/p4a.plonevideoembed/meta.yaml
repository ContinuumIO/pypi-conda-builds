package:
  name: p4a.plonevideoembed
  version: "1.3"

source:
  fn: p4a.plonevideoembed-1.3.zip
  url: https://pypi.python.org/packages/source/p/p4a.plonevideoembed/p4a.plonevideoembed-1.3.zip
  md5: a5f4e47762f1b1fe10c1aac5da8d5aa5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.plonevideoembed = p4a.plonevideoembed:main
    #
    # Would create an entry point called p4a.plonevideoembed that calls p4a.plonevideoembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - p4a.video >1.3
    - p4a.videoembed >1.2
    - products.fatsyndication

  run:
    - python
    - setuptools
    - setuptools
    - p4a.video >1.3
    - p4a.videoembed >1.2
    - products.fatsyndication

test:
  # Python imports
  imports:
    - p4a
    - p4a.plonevideoembed
    - p4a.plonevideoembed.metadata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plone4artistsvideo
  license: GPL
  summary: 'Plone4Artists video embedding add-on for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
