package:
  name: docutils_html5_writer
  version: "0.1.1"

source:
  fn: docutils_html5_writer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/docutils_html5_writer/docutils_html5_writer-0.1.1.tar.gz
  md5: c9a2aa3ed1d02199bd6a15a216aae306
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docutils_html5_writer = docutils_html5_writer:main
    #
    # Would create an entry point called docutils_html5_writer that calls docutils_html5_writer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils
    - html5lib

  run:
    - python
    - docutils
    - html5lib

test:
  # Python imports
  imports:
    - docutils_html5
    - docutils_html5_writer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: public domain
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
