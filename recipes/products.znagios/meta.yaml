package:
  name: products.znagios
  version: "0.6"

source:
  fn: Products.ZNagios-0.6.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.ZNagios/Products.ZNagios-0.6.tar.gz
  md5: ccc620fa81ce86dff20612549c543abd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.znagios = products.znagios:main
    #
    # Would create an entry point called products.znagios that calls products.znagios.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.ZNagios

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.ZNagios
  license: Zope Public License
  summary: 'ZNagios provides the ability for Nagios and munin to tap into the Zope2 server and retrieve status and performance data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
