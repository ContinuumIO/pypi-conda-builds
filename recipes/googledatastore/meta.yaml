package:
  name: googledatastore
  version: "v1beta2-rev1-2.1.0"

source:
  fn: googledatastore-v1beta2-rev1-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/googledatastore/googledatastore-v1beta2-rev1-2.1.0.tar.gz
  md5: 21feeaba49da0d5867c30ba85c44c812
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googledatastore = googledatastore:main
    #
    # Would create an entry point called googledatastore that calls googledatastore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - protobuf ==2.5.0
    - oauth2client ==1.2
    - uritemplate ==0.6

  run:
    - python
    - protobuf ==2.5.0
    - oauth2client ==1.2
    - uritemplate ==0.6

test:
  # Python imports
  imports:
    - googledatastore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GoogleCloudPlatform/google-cloud-datastore
  license: UNKNOWN
  summary: 'google cloud datastore protobuf client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
