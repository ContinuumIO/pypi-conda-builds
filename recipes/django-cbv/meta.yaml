package:
  name: django-cbv
  version: "0.2"

source:
  fn: django-cbv-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cbv/django-cbv-0.2.tar.gz
  md5: 3af842a5a7c66ca422272bdd9db2cb66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cbv = django-cbv:main
    #
    # Would create an entry point called django-cbv that calls django-cbv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cbv
    - cbv.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brutasse/django-cbv
  license: BSD License
  summary: 'Django class-based views, backported from Django trunk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
