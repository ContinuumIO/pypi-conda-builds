package:
  name: miniuri
  version: "0.0.2"

source:
  fn: miniuri-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/miniuri/miniuri-0.0.2.tar.gz
  md5: 4f65224e0b3b16067dfd9f17ada6e38d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - miniuri = miniuri:main
    #
    # Would create an entry point called miniuri that calls miniuri.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - miniuri

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/russellballestrini/miniuri/src
  license: Public Domain
  summary: 'miniuri: The Universal URI Parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
