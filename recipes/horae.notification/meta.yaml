package:
  name: horae.notification
  version: "1.0a1"

source:
  fn: horae.notification-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.notification/horae.notification-1.0a1.tar.gz
  md5: fc73fa319726e5236f7a52a1ada4335c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.notification = horae.notification:main
    #
    # Would create an entry point called horae.notification that calls horae.notification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pyandoc
    - zope.sendmail
    - horae.auth
    - horae.core
    - horae.layout
    - horae.subscription
    - horae.ticketing

  run:
    - python
    - setuptools
    - setuptools
    - pyandoc
    - zope.sendmail
    - horae.auth
    - horae.core
    - horae.layout
    - horae.subscription
    - horae.ticketing

test:
  # Python imports
  imports:
    - horae
    - horae.notification

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides notification functionality for horae'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
