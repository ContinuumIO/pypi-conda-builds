package:
  name: zope.renderer
  version: "4.0.0a1"

source:
  fn: zope.renderer-4.0.0a1.zip
  url: https://pypi.python.org/packages/source/z/zope.renderer/zope.renderer-4.0.0a1.zip
  md5: df83dfb43bfee2b863b781331d6a5310
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.renderer = zope.renderer:main
    #
    # Would create an entry point called zope.renderer that calls zope.renderer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.5
    - roman
    - zope.component
    - zope.configuration
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.structuredtext

  run:
    - python
    - setuptools
    - docutils >=0.5
    - roman
    - zope.component
    - zope.configuration
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.structuredtext

test:
  # Python imports
  imports:
    - zope
    - zope.renderer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.renderer
  license: Zope Public License
  summary: 'Text Renderer Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
