package:
  name: ciscoconfparse
  version: "1.2.19"

source:
  fn: ciscoconfparse-1.2.19.tar.gz
  url: https://pypi.python.org/packages/source/c/ciscoconfparse/ciscoconfparse-1.2.19.tar.gz
  md5: a5605b0076873b59014c782f376ea655
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ciscoconfparse = ciscoconfparse:main
    #
    # Would create an entry point called ciscoconfparse that calls ciscoconfparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipaddr >=2.1.11

  run:
    - python
    - ipaddr >=2.1.11

test:
  # Python imports
  imports:
    - ciscoconfparse
    - version_info

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pennington.net/py/ciscoconfparse/
  license: GNU General Public License (GPL)
  summary: 'Parse, Audit, Query, Build, and Modify Cisco IOS-style configurations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
