package:
  name: archetypes.memberdatastorage
  version: "1.1"

source:
  fn: archetypes.memberdatastorage-1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/archetypes.memberdatastorage/archetypes.memberdatastorage-1.1.tar.gz
  md5: 57c9a621ba093e79f95a1047888414a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.memberdatastorage = archetypes.memberdatastorage:main
    #
    # Would create an entry point called archetypes.memberdatastorage that calls archetypes.memberdatastorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.memberdatastorage
    - archetypes.memberdatastorage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/archetypes.memberdatastorage
  license: BSD
  summary: 'A facade storage layer for Archetypes delegating to the Plone memberdata tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
