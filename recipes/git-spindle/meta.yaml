package:
  name: git-spindle
  version: "2.5"

source:
  fn: git-spindle-2.5.tar.gz
  url: https://pypi.python.org/packages/source/g/git-spindle/git-spindle-2.5.tar.gz
  md5: 0f6ec31cc1e8820ec2e9130639f0f563
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-spindle = git-spindle:main
    #
    # Would create an entry point called git-spindle that calls git-spindle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - github3.py >=0.9.0
    - whelk >=1.11
    - docopt >=0.5.0

  run:
    - python
    - github3.py >=0.9.0
    - whelk >=1.11
    - docopt >=0.5.0

test:
  # Python imports
  imports:
    - gitspindle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seveas/git-spindle
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Git subcommands for integrating with central services like github, gitlab and bitbucket'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
