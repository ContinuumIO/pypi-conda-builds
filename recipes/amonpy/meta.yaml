package:
  name: amonpy
  version: "0.4.4"

source:
  fn: amonpy-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/a/amonpy/amonpy-0.4.4.tar.gz
  md5: 9af34717a2b1bb4478e79577ffe66e45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amonpy = amonpy:main
    #
    # Would create an entry point called amonpy that calls amonpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - amonpy
    - amonpy.adapters
    - amonpy.protocols

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martinrusev/amonpy
  license: BSD
  summary: 'Python client for the Amon API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
