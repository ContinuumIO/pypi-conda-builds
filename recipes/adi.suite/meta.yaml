package:
  name: adi.suite
  version: "0.6"

source:
  fn: adi.suite-0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/adi.suite/adi.suite-0.6.tar.gz
  md5: df96d7b6a450bfb0284c0513f4609557
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adi.suite = adi.suite:main
    #
    # Would create an entry point called adi.suite that calls adi.suite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.contentleadimage
    - collective.fancybox
    - collective.quickupload

  run:
    - python
    - setuptools
    - setuptools
    - collective.contentleadimage
    - collective.fancybox
    - collective.quickupload

test:
  # Python imports
  imports:
    - adi
    - adi.suite
    - adi.suite.browser
    - adi.suite.content
    - adi.suite.interfaces
    - adi.suite.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ida-ebkes.eu
  license: GNU General Public License (GPL)
  summary: 'A suite for javascript multimedia galleries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
