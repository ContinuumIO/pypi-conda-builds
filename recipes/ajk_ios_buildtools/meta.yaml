package:
  name: ajk_ios_buildtools
  version: "0.1"

source:
  fn: ajk_ios_buildTools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/ajk_ios_buildTools/ajk_ios_buildTools-0.1.tar.gz
  md5: 12e6bb9bb7f1c495dbd332261a08ed61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajk_ios_buildtools = ajk_ios_buildtools:main
    #
    # Would create an entry point called ajk_ios_buildtools that calls ajk_ios_buildtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajk_ios_buildtools
    - buildTools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: git@github.com:wadecong/buildTools_py_ios.git
  license: MIT
  summary: 'for daily build,RC test...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
