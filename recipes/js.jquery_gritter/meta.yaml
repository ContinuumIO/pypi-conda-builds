package:
  name: js.jquery_gritter
  version: "1.7.4"

source:
  fn: js.jquery_gritter-1.7.4.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_gritter/js.jquery_gritter-1.7.4.tar.gz
  md5: babba8647a81c96601a0b6d1bf5f8ed8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_gritter = js.jquery_gritter:main
    #
    # Would create an entry point called js.jquery_gritter that calls js.jquery_gritter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_gritter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/s_nikolai/js.jquery_gritter
  license: BSD
  summary: 'Fanstatic packaging of Gritter for jQuery (jquery.gritter.js)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
