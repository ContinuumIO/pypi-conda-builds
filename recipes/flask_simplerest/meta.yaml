package:
  name: flask_simplerest
  version: "1.0.3"

source:
  fn: flask_simplerest-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_simplerest/flask_simplerest-1.0.3.tar.gz
  md5: bef7ddfe45c5ad7da2d4707cb93b18a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_simplerest = flask_simplerest:main
    #
    # Would create an entry point called flask_simplerest that calls flask_simplerest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flask_simplerest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unk2k/flask_simplerest
  license: BSD License
  summary: 'A Flask extension for easy ReSTful API generation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
