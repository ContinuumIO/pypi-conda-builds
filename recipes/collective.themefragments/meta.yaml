package:
  name: collective.themefragments
  version: "0.10.0"

source:
  fn: collective.themefragments-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.themefragments/collective.themefragments-0.10.0.tar.gz
  md5: c0a137c8addb4367598016b593373eb5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.themefragments = collective.themefragments:main
    #
    # Would create an entry point called collective.themefragments that calls collective.themefragments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.theming
    - setuptools

  run:
    - python
    - setuptools
    - plone.app.theming
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.themefragments
    - collective.themefragments.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.themefragments/
  license: GPL
  summary: 'Theme fragments for plone.app.theming'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
