package:
  name: django-mediagenie
  version: "0.1.5"

source:
  fn: django-mediagenie-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mediagenie/django-mediagenie-0.1.5.tar.gz
  md5: 4eba1fb9c5d37b848cbb21a7b389d111
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mediagenie = django-mediagenie:main
    #
    # Would create an entry point called django-mediagenie that calls django-mediagenie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mediagenie
    - mediagenie.contrib
    - mediagenie.generators
    - mediagenie.management
    - mediagenie.management.commands
    - mediagenie.templatetags
    - mediagenie.transforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mcamac/django-media-genie
  license: MIT
  summary: 'Static files management for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
