package:
  name: corpora
  version: "1.0"

source:
  fn: Corpora-1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/Corpora/Corpora-1.0.tar.gz
  md5: 02781c45591ff458819e13120394828d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corpora = corpora:main
    #
    # Would create an entry point called corpora that calls corpora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - corpora
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/corpora
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Lightweight, fast and scalable text corpus library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
