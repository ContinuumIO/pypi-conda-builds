package:
  name: support-cdm
  version: "1.0"

source:
  fn: support-cdm-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/support-cdm/support-cdm-1.0.tar.gz
  md5: 1fa2442c4aef92ef329fc41b73388be0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - support-cdm = support-cdm:main
    #
    # Would create an entry point called support-cdm that calls support-cdm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ally >=1.0

  run:
    - python
    - ally >=1.0

test:
  # Python imports
  imports:
    - __plugin__
    - __plugin__.cdm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sourcefabric.org/en/superdesk/
  license: GPL v3
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
