package:
  name: silva.core.references
  version: "3.0.4"

source:
  fn: silva.core.references-3.0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.core.references/silva.core.references-3.0.4.tar.gz
  md5: eacacb3a562e5b538a86641315527c30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.core.references = silva.core.references:main
    #
    # Would create an entry point called silva.core.references that calls silva.core.references.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.formulator
    - dolmen.relations
    - five.grok
    - five.intid
    - grokcore.chameleon
    - infrae.rest
    - megrok.pagetemplate
    - setuptools
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.views
    - silva.translations
    - silva.fanstatic
    - silva.ui
    - zc.relation
    - zeam.component
    - zeam.form.base
    - zeam.form.silva
    - zeam.form.ztk
    - zope.cachedescriptors
    - zope.component
    - zope.event
    - zope.interface
    - zope.intid
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema
    - zope.site
    - zope.traversing

  run:
    - python
    - setuptools
    - products.formulator
    - dolmen.relations
    - five.grok
    - five.intid
    - grokcore.chameleon
    - infrae.rest
    - megrok.pagetemplate
    - setuptools
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.views
    - silva.translations
    - silva.fanstatic
    - silva.ui
    - zc.relation
    - zeam.component
    - zeam.form.base
    - zeam.form.silva
    - zeam.form.ztk
    - zope.cachedescriptors
    - zope.component
    - zope.event
    - zope.interface
    - zope.intid
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema
    - zope.site
    - zope.traversing

test:
  # Python imports
  imports:
    - silva
    - silva.core
    - silva.core.references
    - silva.core.references.tests
    - silva.core.references.tests.widget
    - silva.core.references.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.core.references
  license: BSD License
  summary: 'Define a reference engine usable by Silva content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
