package:
  name: autoxinit
  version: "0.1.2"

source:
  fn: autoxinit-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/autoxinit/autoxinit-0.1.2.tar.gz
  md5: a369fe137f170cbaf0c1f7c1d3a879c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autoxinit = autoxinit:main
    #
    # Would create an entry point called autoxinit that calls autoxinit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - autoxinit
    - autoxinit.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/AutoXInit/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: '\nautomodinit + X\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
