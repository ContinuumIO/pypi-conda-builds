package:
  name: teamcity-messages
  version: "1.13"

source:
  fn: teamcity-messages-1.13.tar.gz
  url: https://pypi.python.org/packages/source/t/teamcity-messages/teamcity-messages-1.13.tar.gz
  md5: 87297843333c91719dff1af0be131361
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - teamcity-messages = teamcity-messages:main
    #
    # Would create an entry point called teamcity-messages that calls teamcity-messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - teamcity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JetBrains/teamcity-python
  license: Apache Software License
  summary: 'Send test results to TeamCity continuous integration server from unittest, nose and py.test (Python 2.6+)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
