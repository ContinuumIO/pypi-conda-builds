package:
  name: django_audit_trail
  version: "0.0.34"

source:
  fn: django_audit_trail-0.0.34.tar.gz
  url: https://pypi.python.org/packages/source/d/django_audit_trail/django_audit_trail-0.0.34.tar.gz
  md5: 733b4a38104e39142530bdf3fb553530
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_audit_trail = django_audit_trail:main
    #
    # Would create an entry point called django_audit_trail that calls django_audit_trail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.8
    - jsonfield ==1.0.0

  run:
    - python
    - django >=1.8
    - jsonfield ==1.0.0

test:
  # Python imports
  imports:
    - audit_trail
    - audit_trail.migrations
    - audit_trail.south_migrations
    - django_audit_trail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TriplePoint-Software/django_audit_trail
  license: Apache License, Version 2.0
  summary: 'App for tracking django model changes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
