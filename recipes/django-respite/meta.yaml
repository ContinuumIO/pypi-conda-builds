package:
  name: django-respite
  version: "1.4.0"

source:
  fn: django-respite-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-respite/django-respite-1.4.0.tar.gz
  md5: bb3003794760f1322ab770c23f0cbb07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-respite = django-respite:main
    #
    # Would create an entry point called django-respite that calls django-respite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - respite
    - respite.lib
    - respite.serializers
    - respite.urls
    - respite.utils
    - respite.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jgorset/django-respite
  license: UNKNOWN
  summary: 'Respite conforms Django to Representational State Transfer (REST)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
