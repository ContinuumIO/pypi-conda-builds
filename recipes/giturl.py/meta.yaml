package:
  name: giturl.py
  version: "0.2.1"

source:
  fn: giturl.py-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/giturl.py/giturl.py-0.2.1.tar.gz
  md5: 4a212022c015d8a601f0169c14c4666c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - giturl.py = giturl.py:main
    #
    # Would create an entry point called giturl.py that calls giturl.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/willyg302/giturl
  license: MIT License
  summary: 'A tool for parsing all sorts of Git URLs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
