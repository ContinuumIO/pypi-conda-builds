package:
  name: flask_json_multidict
  version: "1.0.0"

source:
  fn: flask_json_multidict-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_json_multidict/flask_json_multidict-1.0.0.tar.gz
  md5: 7d7eece96d522ad7847113d9bf5b25fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_json_multidict = flask_json_multidict:main
    #
    # Would create an entry point called flask_json_multidict that calls flask_json_multidict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.10.1

  run:
    - python
    - werkzeug >=0.10.1

test:
  # Python imports
  imports:
    - flask_json_multidict
    - flask_json_multidict.test
    - flask_json_multidict.test.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/underdogio/flask-json-multidict
  license: MIT License
  summary: "Convert Flask's `request.get_json` dict into a MultiDict like `request.form`"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
