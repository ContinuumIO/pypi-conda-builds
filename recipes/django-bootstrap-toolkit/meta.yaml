package:
  name: django-bootstrap-toolkit
  version: "2.15.0"

source:
  fn: django-bootstrap-toolkit-2.15.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-toolkit/django-bootstrap-toolkit-2.15.0.tar.gz
  md5: 7ee049b17a0ef51d26acca45abf663d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-toolkit = django-bootstrap-toolkit:main
    #
    # Would create an entry point called django-bootstrap-toolkit that calls django-bootstrap-toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap_toolkit
    - bootstrap_toolkit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dyve/django-bootstrap-toolkit
  license: Apache Software License
  summary: 'Bootstrap support for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
