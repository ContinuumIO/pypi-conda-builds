package:
  name: infrae.buildout
  version: "0.1dev-r27686"

source:
  fn: infrae.buildout-0.1dev-r27686.tar.gz
  url: https://pypi.python.org/packages/source/i/infrae.buildout/infrae.buildout-0.1dev-r27686.tar.gz
  md5: 0582e162f0b3a560e51b964819694dae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infrae.buildout = infrae.buildout:main
    #
    # Would create an entry point called infrae.buildout that calls infrae.buildout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - infrae
    - infrae.buildout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.infrae.com/buildout/infrae.buildout
  license: Zope Public License
  summary: 'Buildout recipe to install a buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
