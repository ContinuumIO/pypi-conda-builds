package:
  name: django-debugtools
  version: "1.3"

source:
  fn: django-debugtools-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debugtools/django-debugtools-1.3.tar.gz
  md5: 47b4a51442f11eda94f234d6a021f902
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debugtools = django-debugtools:main
    #
    # Would create an entry point called django-debugtools that calls django-debugtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - debugtools
    - debugtools.middleware
    - debugtools.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/django-debugtools
  license: Apache Software License
  summary: 'A toolbox of small utilities to assist Django development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
