package:
  name: hapicake
  version: "0.1.1"

source:
  fn: hapicake-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hapicake/hapicake-0.1.1.tar.gz
  md5: c0542a1b53cabbde8e55bacd9e6180b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hapicake = hapicake:main
    #
    # Would create an entry point called hapicake that calls hapicake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0,<1
    - cython >=0,<1
    - gevent <2
    - sanetime >=4,<5
    - utilspy >=0,<1
    - giftwrap >=1,<2

  run:
    - python
    - requests >=0,<1
    - cython >=0,<1
    - gevent <2
    - sanetime >=4,<5
    - utilspy >=0,<1
    - giftwrap >=1,<2

test:
  # Python imports
  imports:
    - cake
    - cake.test
    - hapicake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HubSpot/hapicake
  license: LICENSE.txt
  summary: 'a next generation hapipy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
