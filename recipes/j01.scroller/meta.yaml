package:
  name: j01.scroller
  version: "1.0.0"

source:
  fn: j01.scroller-1.0.0.zip
  url: https://pypi.python.org/packages/source/j/j01.scroller/j01.scroller-1.0.0.zip
  md5: 6981abe0bf31f5138b723e78e94cdb51
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.scroller = j01.scroller:main
    #
    # Would create an entry point called j01.scroller that calls j01.scroller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.jsonrpc
    - z3c.template
    - zope.security
    - zope.traversing

  run:
    - python
    - setuptools
    - z3c.jsonrpc
    - z3c.template
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - j01
    - j01.scroller

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.scroller
  license: Zope Public License
  summary: 'JQuery and j01.jsonrpc based pagination for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
