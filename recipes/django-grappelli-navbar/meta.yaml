package:
  name: django-grappelli-navbar
  version: "0.3.0"

source:
  fn: django-grappelli-navbar-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-grappelli-navbar/django-grappelli-navbar-0.3.0.tar.gz
  md5: c582a7746bcf8f01823e543d139ffbc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-grappelli-navbar = django-grappelli-navbar:main
    #
    # Would create an entry point called django-grappelli-navbar that calls django-grappelli-navbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-apptemplates >=0.0.1
    - django-grappelli >=2.4.7
    - django-classy-tags >=0.4

  run:
    - python
    - django-apptemplates >=0.0.1
    - django-grappelli >=2.4.7
    - django-classy-tags >=0.4

test:
  # Python imports
  imports:
    - grappelli_navbar
    - grappelli_navbar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mauler/django-grappelli-navbar
  license: Apache Software License
  summary: u'Navigation bar on Django administration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
