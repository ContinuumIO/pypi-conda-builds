package:
  name: bandpage
  version: "1.0.1"

source:
  fn: bandpage-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bandpage/bandpage-1.0.1.tar.gz
  md5: 7387bf199c08e7a344a051aec3b9efeb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bandpage = bandpage:main
    #
    # Would create an entry point called bandpage that calls bandpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - bandpage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattrobenolt/python-bandpage
  license: GNU General Public License (GPL)
  summary: 'The Python library for BandPage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
