package:
  name: django-payslip
  version: "0.2.2"

source:
  fn: django-payslip-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-payslip/django-payslip-0.2.2.tar.gz
  md5: 3bf592e63f7a7223207d2708cb76c762
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-payslip = django-payslip:main
    #
    # Would create an entry point called django-payslip that calls django-payslip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - xhtml2pdf

  run:
    - python
    - django
    - xhtml2pdf

test:
  # Python imports
  imports:
    - payslip
    - payslip.migrations
    - payslip.templatetags
    - payslip.tests
    - payslip.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-payslip
  license: The MIT License
  summary: 'A reusable Django app that allows you to enter salary data of your employees\nso that authorized persons can view, print and export (PDF) their payslips.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
