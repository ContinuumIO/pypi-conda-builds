package:
  name: imapclient
  version: "0.12"

source:
  fn: IMAPClient-0.12.tar.gz
  url: https://pypi.python.org/packages/source/I/IMAPClient/IMAPClient-0.12.tar.gz
  md5: bb7dc4cb2b4ab82ebc73e0f01cb60e1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imapclient = imapclient:main
    #
    # Would create an entry point called imapclient that calls imapclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - imapclient
    - imapclient.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://imapclient.freshfoo.com/
  license: BSD License
  summary: 'Easy-to-use, Pythonic and complete IMAP client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
