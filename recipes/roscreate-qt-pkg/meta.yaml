package:
  name: roscreate-qt-pkg
  version: "0.1.4"

source:
  fn: roscreate-qt-pkg-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/r/roscreate-qt-pkg/roscreate-qt-pkg-0.1.4.tar.gz
  md5: d642bc5fa7a482f1156011e953ae58ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - roscreate-qt-pkg = roscreate-qt-pkg:main
    #
    # Would create an entry point called roscreate-qt-pkg that calls roscreate-qt-pkg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - qt_create

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/roscreate-qt-pkg/
  license: BSD License
  summary: 'Creates qt package templates for RoS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
