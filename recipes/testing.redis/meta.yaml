package:
  name: testing.redis
  version: "1.0.3"

source:
  fn: testing.redis-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/testing.redis/testing.redis-1.0.3.tar.gz
  md5: e967307770ffb7399134b7815852a7dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testing.redis = testing.redis:main
    #
    # Would create an entry point called testing.redis that calls testing.redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tk0miya/testing.redis
  license: Apache Software License
  summary: 'automatically setups a redis instance in a temporary directory, and destroys it after testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
