package:
  name: redis-dump-load
  version: "0.3.0"

source:
  fn: redis-dump-load-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-dump-load/redis-dump-load-0.3.0.tar.gz
  md5: 39f933a44772029f1d51236f3d25cdc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-dump-load = redis-dump-load:main
    #
    # Would create an entry point called redis-dump-load that calls redis-dump-load.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/p/redis-dump-load
  license: UNKNOWN
  summary: 'Dump and load redis databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
