package:
  name: django-quieter-formset
  version: "0.4"

source:
  fn: django-quieter-formset-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quieter-formset/django-quieter-formset-0.4.tar.gz
  md5: fa7fae50009ebe22d3527775b4d62344
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quieter-formset = django-quieter-formset:main
    #
    # Would create an entry point called django-quieter-formset that calls django-quieter-formset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - quieter_formset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mozilla.com
  license: BSD
  summary: "Django formset that's a bit quieter"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
