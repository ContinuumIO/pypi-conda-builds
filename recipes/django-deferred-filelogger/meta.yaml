package:
  name: django-deferred-filelogger
  version: "0.1"

source:
  fn: django-deferred-filelogger-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-deferred-filelogger/django-deferred-filelogger-0.1.tar.gz
  md5: f5e3eb532dcc9a0d7d8d4882566d2c6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-deferred-filelogger = django-deferred-filelogger:main
    #
    # Would create an entry point called django-deferred-filelogger that calls django-deferred-filelogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - deferred_filelogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codeinthehole/django-deferred-filelogger
  license: django_deferred_polymorph
  summary: 'A logging handler for Django that defers evaluation of the filepath'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
