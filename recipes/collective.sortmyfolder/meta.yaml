package:
  name: collective.sortmyfolder
  version: "1.0.7"

source:
  fn: collective.sortmyfolder-1.0.7.zip
  url: https://pypi.python.org/packages/source/c/collective.sortmyfolder/collective.sortmyfolder-1.0.7.zip
  md5: a97b674e9c671f0dc20449ed7289428a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sortmyfolder = collective.sortmyfolder:main
    #
    # Would create an entry point called collective.sortmyfolder that calls collective.sortmyfolder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.sortmyfolder
    - collective.sortmyfolder.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.sortmyfolder
  license: GPL
  summary: 'Reveal some not-so-hidden Plone feature for sorting your folders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
