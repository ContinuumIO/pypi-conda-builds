package:
  name: django-star
  version: "0.999"

source:
  fn: django-star-0.999.tar.gz
  url: https://pypi.python.org/packages/source/d/django-star/django-star-0.999.tar.gz
  md5: 22cfd5e4c566f90cc49d96d7a4b2ada6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-star = django-star:main
    #
    # Would create an entry point called django-star that calls django-star.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - star
    - star.api
    - star.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/giginet/django-star
  license: BSD License
  summary: 'Django plugin for adding a star to Django model object universally.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
