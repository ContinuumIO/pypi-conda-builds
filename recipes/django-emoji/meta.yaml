package:
  name: django-emoji
  version: "2.0.0"

source:
  fn: django-emoji-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emoji/django-emoji-2.0.0.tar.gz
  md5: 82600b3a8f50955dbd51b965290937ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emoji = django-emoji:main
    #
    # Would create an entry point called django-emoji that calls django-emoji.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - emoji
    - emoji.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gaqzi/django-emoji/
  license: BSD License
  summary: 'A simple django app to use emojis on your website'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
