package:
  name: django-theherk-updates
  version: "1.3.0"

source:
  fn: django-theherk-updates-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-theherk-updates/django-theherk-updates-1.3.0.tar.gz
  md5: c9649d6e525e5a2b5880eb75387d278d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-theherk-updates = django-theherk-updates:main
    #
    # Would create an entry point called django-theherk-updates that calls django-theherk-updates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - updates
    - updates.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theherk/django-theherk-updates/archive/1.3.0.zip
  license: see file LICENSE
  summary: 'Django app for tracking and displaying updates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
