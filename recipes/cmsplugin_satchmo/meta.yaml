package:
  name: cmsplugin_satchmo
  version: "0.2.2"

source:
  fn: cmsplugin_satchmo-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_satchmo/cmsplugin_satchmo-0.2.2.tar.gz
  md5: 7347472099f2dd97cd89174e5bfa77d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_satchmo = cmsplugin_satchmo:main
    #
    # Would create an entry point called cmsplugin_satchmo that calls cmsplugin_satchmo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_satchmo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tomaasch/cmsplugin_satchmo.git
  license: MIT
  summary: 'The Satchmo plugin for Django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
