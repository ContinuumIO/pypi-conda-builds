package:
  name: django-templatepages
  version: "0.2.3"

source:
  fn: django-templatepages-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templatepages/django-templatepages-0.2.3.tar.gz
  md5: 15f39685bc2a1cc61e06b95f077e32f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templatepages = django-templatepages:main
    #
    # Would create an entry point called django-templatepages that calls django-templatepages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - templatepages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bryanchow/django-templatepages
  license: BSD
  summary: 'Django app for mapping URLs to templates on the filesystem'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
