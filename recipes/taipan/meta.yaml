package:
  name: taipan
  version: "0.0.3"

source:
  fn: taipan-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/taipan/taipan-0.0.3.tar.gz
  md5: 635bd791d43fa3e5fe147a8b6d843a7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taipan = taipan:main
    #
    # Would create an entry point called taipan that calls taipan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - taipan
    - taipan.api
    - taipan.collections
    - taipan.functional
    - taipan.objective
    - tests.test_api
    - tests.test_collections
    - tests.test_functional
    - tests.test_objective

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Xion/taipan
  license: BSD License
  summary: 'General purpose toolkit for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
