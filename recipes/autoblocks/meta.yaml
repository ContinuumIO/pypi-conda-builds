package:
  name: autoblocks
  version: "1.0.4"

source:
  fn: autoblocks-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/autoblocks/autoblocks-1.0.4.tar.gz
  md5: b90926beb1373f402fe0e76978a9648f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autoblocks = autoblocks:main
    #
    # Would create an entry point called autoblocks that calls autoblocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.4.1

  run:
    - python
    - django-cms >=2.4.1

test:
  # Python imports
  imports:
    - autoblocks
    - autoblocks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Strathcom/django-cms-autoblocks
  license: BSD License
  summary: 'Place arbitrary django-cms placeholders into templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
