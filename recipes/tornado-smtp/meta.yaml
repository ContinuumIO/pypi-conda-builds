package:
  name: tornado-smtp
  version: "0.1.1"

source:
  fn: tornado-smtp-0.1.1.zip
  url: https://pypi.python.org/packages/source/t/tornado-smtp/tornado-smtp-0.1.1.zip
  md5: ab9da468770b68aedaef56ca0a7aa434
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-smtp = tornado-smtp:main
    #
    # Would create an entry point called tornado-smtp that calls tornado-smtp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.2.0

  run:
    - python
    - tornado >=3.2.0

test:
  # Python imports
  imports:
    - tornado_smtp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rmoritz/tornado-smtp
  license: MIT License
  summary: 'An asynchronous SMTP client for Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
