package:
  name: heat-cfntools
  version: "1.2.8"

source:
  fn: heat-cfntools-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/h/heat-cfntools/heat-cfntools-1.2.8.tar.gz
  md5: 0d5f8e7685e7f9a34dbd2552ffa40984
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heat-cfntools = heat-cfntools:main
    #
    # Would create an entry point called heat-cfntools that calls heat-cfntools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Tools required to be installed on Heat provisioned cloud instances'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
