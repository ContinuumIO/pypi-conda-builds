package:
  name: log_colorizer
  version: "1.6"

source:
  fn: log_colorizer-1.6.tar.gz
  url: https://pypi.python.org/packages/source/l/log_colorizer/log_colorizer-1.6.tar.gz
  md5: f052908c63a1bfb0cda4287414f43bef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - log_colorizer = log_colorizer:main
    #
    # Would create an entry point called log_colorizer that calls log_colorizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - log_colorizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A color formater for python logging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
