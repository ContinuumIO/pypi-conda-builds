package:
  name: bibtex-pygments-lexer
  version: "0.0.1"

source:
  fn: bibtex-pygments-lexer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bibtex-pygments-lexer/bibtex-pygments-lexer-0.0.1.tar.gz
  md5: ed3cf11f45c10f09981a6f247d1a8307
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bibtex-pygments-lexer = bibtex-pygments-lexer:main
    #
    # Would create an entry point called bibtex-pygments-lexer that calls bibtex-pygments-lexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - bibtex_lexer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Pygments Lexer for BibTeX'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
