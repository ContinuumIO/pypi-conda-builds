package:
  name: growlnotify
  version: "0.0.1"

source:
  fn: growlnotify-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/growlnotify/growlnotify-0.0.1.tar.gz
  md5: 0222394d4f7632bdc58ad48f3030d67e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - growlnotify = growlnotify:main
    #
    # Would create an entry point called growlnotify that calls growlnotify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - envoy
    - its

  run:
    - python
    - envoy
    - its

test:
  # Python imports
  imports:
    - growlnotify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cancerhermit/Pygrowlnotify/
  license: GNU General Public License (GPL)
  summary: 'growlnotify (Mac OS) Python wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
