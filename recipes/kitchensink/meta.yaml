package:
  name: kitchensink
  version: "0.0.33"

source:
  fn: KitchenSink-0.0.33.tar.gz
  url: https://pypi.python.org/packages/source/K/KitchenSink/KitchenSink-0.0.33.tar.gz
  md5: aee28e4805d2308dc5bcd052315e71a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kitchensink = kitchensink:main
    #
    # Would create an entry point called kitchensink that calls kitchensink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - urwid >=1.0
    - pygments

  run:
    - python
    - urwid >=1.0
    - pygments

test:
  # Python imports
  imports:
    - kitchen_sink
    - kitchensink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/okayzed/kk
  license: MIT
  summary: 'a smarter pager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
