package:
  name: audio.wave
  version: "4.0.2"

source:
  fn: audio.wave-4.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/audio.wave/audio.wave-4.0.2.tar.gz
  md5: d8f0cd7810c177bf5cf9629d3279a70c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audio.wave = audio.wave:main
    #
    # Would create an entry point called audio.wave that calls audio.wave.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bitstream
    - logfile
    - wish

  run:
    - python
    - bitstream
    - logfile
    - wish

test:
  # Python imports
  imports:
    - audio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/boisgera/audio.wave
  license: MIT License
  summary: 'WAV Files Reader/Writer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
