package:
  name: django-security
  version: "0.2.0b"

source:
  fn: django-security-0.2.0b.tar.gz
  url: https://pypi.python.org/packages/source/d/django-security/django-security-0.2.0b.tar.gz
  md5: 4248e84ada28651020ce75618b24dc0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-security = django-security:main
    #
    # Would create an entry point called django-security that calls django-security.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - security
    - security.auth_throttling
    - security.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sdelements/django-security
  license: BSD License
  summary: 'A collection of tools to help secure a Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
