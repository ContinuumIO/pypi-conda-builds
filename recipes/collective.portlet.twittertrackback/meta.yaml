package:
  name: collective.portlet.twittertrackback
  version: "1.0"

source:
  fn: collective.portlet.twittertrackback-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.twittertrackback/collective.portlet.twittertrackback-1.0.tar.gz
  md5: 4fec3c489480c7b14112e512a890f68d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.twittertrackback = collective.portlet.twittertrackback:main
    #
    # Would create an entry point called collective.portlet.twittertrackback that calls collective.portlet.twittertrackback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.twittertrackback
    - collective.portlet.twittertrackback.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/collective.portlet.twittertrackback
  license: GPL
  summary: "A portlet that displays mentions of the current page's url via Topsy"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
