package:
  name: sfjson
  version: "1.0.1"

source:
  fn: sfjson-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sfjson/sfjson-1.0.1.tar.gz
  md5: f4aeaf0b538aa0981771bf5dacac35e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sfjson = sfjson:main
    #
    # Would create an entry point called sfjson that calls sfjson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sleekxmpp
    - tlslite
    - pyasn1
    - pyasn1-modules
    - python-dateutil

  run:
    - python
    - sleekxmpp
    - tlslite
    - pyasn1
    - pyasn1-modules
    - python-dateutil

test:
  # Python imports
  imports:
    - sfjson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iAcquire/sfjson-python
  license: MIT License
  summary: 'sfjson'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
