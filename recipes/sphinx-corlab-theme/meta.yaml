package:
  name: sphinx-corlab-theme
  version: "0.1.3"

source:
  fn: sphinx-corlab-theme-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-corlab-theme/sphinx-corlab-theme-0.1.3.tar.gz
  md5: 947a063a3efe2c9cb43d8e12dcf5780e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-corlab-theme = sphinx-corlab-theme:main
    #
    # Would create an entry point called sphinx-corlab-theme that calls sphinx-corlab-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.2

  run:
    - python
    - sphinx >=1.2

test:
  # Python imports
  imports:
    - corlab_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.cor-lab.org/projects/sphinx-corlab-theme
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Theme for documentation projects related to CoR-Lab software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
