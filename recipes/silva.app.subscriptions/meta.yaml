package:
  name: silva.app.subscriptions
  version: "3.0.3"

source:
  fn: silva.app.subscriptions-3.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.app.subscriptions/silva.app.subscriptions-3.0.3.tar.gz
  md5: 7896d4ea82dcceee9af721cf61132e58
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.app.subscriptions = silva.app.subscriptions:main
    #
    # Would create an entry point called silva.app.subscriptions that calls silva.app.subscriptions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - products.silva
    - five.grok
    - grokcore.chameleon
    - setuptools
    - silva.captcha
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.layout
    - silva.core.references
    - silva.core.services
    - silva.core.smi
    - silva.core.upgrade
    - silva.core.views
    - silva.translations
    - silva.ui
    - z3c.schema
    - zeam.form.silva
    - zope.annotation
    - zope.component
    - zope.interface
    - zope.lifecycleevent
    - zope.schema

  run:
    - python
    - products.silva
    - five.grok
    - grokcore.chameleon
    - setuptools
    - silva.captcha
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.layout
    - silva.core.references
    - silva.core.services
    - silva.core.smi
    - silva.core.upgrade
    - silva.core.views
    - silva.translations
    - silva.ui
    - z3c.schema
    - zeam.form.silva
    - zope.annotation
    - zope.component
    - zope.interface
    - zope.lifecycleevent
    - zope.schema

test:
  # Python imports
  imports:
    - silva
    - silva.app
    - silva.app.subscriptions
    - silva.app.subscriptions.tests
    - silva.app.subscriptions.upgrader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.app.subscriptions
  license: BSD License
  summary: 'Let people subscribe to content changes in Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
