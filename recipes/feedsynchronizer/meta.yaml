package:
  name: feedsynchronizer
  version: "1.0.1"

source:
  fn: FeedSynchronizer-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FeedSynchronizer/FeedSynchronizer-1.0.1.tar.gz
  md5: c6d6079a6b8925e20f8e6b51cd7dd663
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feedsynchronizer = feedsynchronizer:main
    #
    # Would create an entry point called feedsynchronizer that calls feedsynchronizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pytumblr
    - facebook
    - linkedin
    - twitterapi
    - python-wordpress-xmlrpc
    - feedparser
    - flup

  run:
    - python
    - pytumblr
    - facebook
    - linkedin
    - twitterapi
    - python-wordpress-xmlrpc
    - feedparser
    - flup

test:
  # Python imports
  imports:
    - FeedSynchronizer
    - feedsynchronizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Quentin-G/Synchronisation-flux-RSS
  license: UNKNOWN
  summary: 'Synchronizing a RSS feed to social networks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
