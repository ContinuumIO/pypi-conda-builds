package:
  name: django-elements
  version: "0.1.1"

source:
  fn: django-elements-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-elements/django-elements-0.1.1.tar.gz
  md5: 4e9fb8bfb02e3d36277e4f5ac2707e5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-elements = django-elements:main
    #
    # Would create an entry point called django-elements that calls django-elements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown
    - markdown-macros

  run:
    - python
    - markdown
    - markdown-macros

test:
  # Python imports
  imports:
    - elements
    - elements.extensions
    - elements.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wnielson/django-elements
  license: MIT License
  summary: 'Enhanced Markdown editing for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
