package:
  name: django-livestats
  version: "0.0.2.4"

source:
  fn: django-livestats-0.0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livestats/django-livestats-0.0.2.4.tar.gz
  md5: 608bb7642afacaf5d19b6d19d85133ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livestats = django-livestats:main
    #
    # Would create an entry point called django-livestats that calls django-livestats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - livestats
    - livestats.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A django livestats framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
