package:
  name: products.collage
  version: "1.3.11"

source:
  fn: Products.Collage-1.3.11.zip
  url: https://pypi.python.org/packages/source/P/Products.Collage/Products.Collage-1.3.11.zip
  md5: a376cba561fd58bb37004437d336a302
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.collage = products.collage:main
    #
    # Would create an entry point called products.collage that calls products.collage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.archetypes

  run:
    - python
    - setuptools
    - setuptools
    - products.archetypes

test:
  # Python imports
  imports:
    - Products
    - Products.Collage
    - Products.Collage.browser
    - Products.Collage.content
    - Products.Collage.skindemo
    - Products.Collage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plone.org/products/collage
  license: GNU General Public License (GPL)
  summary: 'A product to create page compositions in Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
