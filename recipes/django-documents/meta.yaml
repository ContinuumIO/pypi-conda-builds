package:
  name: django-documents
  version: "0.0.3"

source:
  fn: django-documents-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-documents/django-documents-0.0.3.tar.gz
  md5: 5b927975824bece245a997a0b06ef225
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-documents = django-documents:main
    #
    # Would create an entry point called django-documents that calls django-documents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django_autoslug
    - django_generic_m2m
    - django_autocomplete_light

  run:
    - python
    - django
    - django_autoslug
    - django_generic_m2m
    - django_autocomplete_light

test:
  # Python imports
  imports:
    - documents
    - documents.migrations
    - documents.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yourlabs/django-documents
  license: MIT License
  summary: 'Attach documents to django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
