package:
  name: django-richtext-blog
  version: "0.8.6"

source:
  fn: django-richtext-blog-0.8.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-richtext-blog/django-richtext-blog-0.8.6.tar.gz
  md5: f5b1a719c6ab3888d4fe9ae63b70ed73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-richtext-blog = django-richtext-blog:main
    #
    # Would create an entry point called django-richtext-blog that calls django-richtext-blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3
    - django-filebrowser ==3.4.2
    - feedparser
    - pil
    - django-tinymce ==1.5.1b2
    - mysql-python
    - django-autoslug
    - django-simple-captcha
    - pygments
    - beautifulsoup

  run:
    - python
    - django ==1.3
    - django-filebrowser ==3.4.2
    - feedparser
    - pil
    - django-tinymce ==1.5.1b2
    - mysql-python
    - django-autoslug
    - django-simple-captcha
    - pygments
    - beautifulsoup

test:
  # Python imports
  imports:
    - richtext_blog
    - richtext_blog.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timmygee/django-richtext-blog
  license: BSD License
  summary: "A django app that implements a blog with the features we've grown accustomed to. Features: Rich text editing with TinyMCE; Full image upload support; Tags; Comments; Spam prevention; Atom/RSS feeds; Example templates; Code syntax highlighting; SEO optimised urls and more."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
