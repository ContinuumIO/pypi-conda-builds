package:
  name: cellmethy
  version: "1.2.0"

source:
  fn: CellMethy-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/C/CellMethy/CellMethy-1.2.0.tar.gz
  md5: 5f9dd46cb192c5400ffa174d1d6f606e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cellmethy = cellmethy:main
    #
    # Would create an entry point called cellmethy that calls cellmethy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cellmethy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pypa/CellMethyproject
  license: Python Software Foundation License
  summary: 'Identifying focal full methylation of cell subpopulation and inferring fraction'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
