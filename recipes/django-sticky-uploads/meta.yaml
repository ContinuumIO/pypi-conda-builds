package:
  name: django-sticky-uploads
  version: "0.3.0"

source:
  fn: django-sticky-uploads-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sticky-uploads/django-sticky-uploads-0.3.0.tar.gz
  md5: 5192b571838ee75709d7fde733dfa8fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sticky-uploads = django-sticky-uploads:main
    #
    # Would create an entry point called django-sticky-uploads that calls django-sticky-uploads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stickyuploads
    - stickyuploads.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caktus/django-sticky-uploads/
  license: BSD License
  summary: 'Enhanced file input widget for Django which uploads the file in the background and retains value on form errors.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
