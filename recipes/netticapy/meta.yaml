package:
  name: netticapy
  version: "1.1"

source:
  fn: netticapy-1.1.tar.gz
  url: https://pypi.python.org/packages/source/n/netticapy/netticapy-1.1.tar.gz
  md5: 3e42546dfdaa8d572f98542975c24b53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netticapy = netticapy:main
    #
    # Would create an entry point called netticapy that calls netticapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds

  run:
    - python
    - suds

test:
  # Python imports
  imports:
    - netticapy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fabian/netticapy
  license: MIT License
  summary: 'A nettica.com API wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
