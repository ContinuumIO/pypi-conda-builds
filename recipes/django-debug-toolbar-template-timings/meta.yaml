package:
  name: django-debug-toolbar-template-timings
  version: "0.6.4"

source:
  fn: django-debug-toolbar-template-timings-0.6.4.zip
  url: https://pypi.python.org/packages/source/d/django-debug-toolbar-template-timings/django-debug-toolbar-template-timings-0.6.4.zip
  md5: af54ceddd8eacad108de9e4270e6ecc4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-toolbar-template-timings = django-debug-toolbar-template-timings:main
    #
    # Would create an entry point called django-debug-toolbar-template-timings that calls django-debug-toolbar-template-timings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - django-debug-toolbar >=1.0

  run:
    - python
    - django
    - django-debug-toolbar >=1.0

test:
  # Python imports
  imports:
    - template_timings_panel
    - template_timings_panel.panels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orf/django-debug-toolbar-template-timings
  license: django-debug-toolbar-user-panel-fc
  summary: 'A django-debug-toolbar panel that shows you template rendering times for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
