package:
  name: djantimat
  version: "0.1.2"

source:
  fn: djantimat-0.1.2.zip
  url: https://pypi.python.org/packages/source/d/djantimat/djantimat-0.1.2.zip
  md5: d53dad28338d9acaf02f72d42acd63bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djantimat = djantimat:main
    #
    # Would create an entry point called djantimat that calls djantimat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymorphy2 ==0.8

  run:
    - python
    - pymorphy2 ==0.8

test:
  # Python imports
  imports:
    - djantimat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PixxxeL/djantimat
  license: GPL3
  summary: 'Detect dirty slang in russian texts and process it'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
