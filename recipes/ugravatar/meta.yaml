package:
  name: ugravatar
  version: "0.0.2"

source:
  fn: ugravatar-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ugravatar/ugravatar-0.0.2.tar.gz
  md5: daca2c16ce661de4e05160b5cb48695f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ugravatar = ugravatar:main
    #
    # Would create an entry point called ugravatar that calls ugravatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ugravatar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/umgeher/ugravatar/
  license: http://changelog.umgeher.mitgnu.com/mitgnu/mitgnus-bsd-license/
  summary: "Simple way to use Gravatar's service."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
