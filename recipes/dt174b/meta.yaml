package:
  name: dt174b
  version: "0.1"

source:
  fn: dt174b-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dt174b/dt174b-0.1.tar.gz
  md5: 2d788943030c64c84a30f26cbd21df53
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dt174b = dt174b:main
    #
    # Would create an entry point called dt174b that calls dt174b.main()

    - dt174b = dt174b.dt174b:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dt174b

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - dt174b --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaryn/dt174b
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Python tool for interfacing the CEM DT-174B Weather Datalogger that \nis talking on USB by some specific protocol.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
