package:
  name: pyexcel
  version: "0.1.4"

source:
  fn: pyexcel-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pyexcel/pyexcel-0.1.4.tar.gz
  md5: cfd2ff913fb8005b5baccf72ec2d4de7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyexcel = pyexcel:main
    #
    # Would create an entry point called pyexcel that calls pyexcel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyexcel-io >=0.0.3
    - texttable >=0.8.2

  run:
    - python
    - pyexcel-io >=0.0.3
    - texttable >=0.8.2

test:
  # Python imports
  imports:
    - pyexcel
    - pyexcel.ext
    - pyexcel.io
    - pyexcel.sheets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chfw/pyexcel
  license: BSD License
  summary: 'A wrapper library that provides one API to read, manipulate and write data in different excel formats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
