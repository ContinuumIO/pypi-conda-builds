package:
  name: fontawesome-markdown
  version: "0.2.3"

source:
  fn: fontawesome-markdown-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fontawesome-markdown/fontawesome-markdown-0.2.3.tar.gz
  md5: 642c3900e368ee848303709ae4e72e91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fontawesome-markdown = fontawesome-markdown:main
    #
    # Would create an entry point called fontawesome-markdown that calls fontawesome-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown

  run:
    - python
    - markdown

test:
  # Python imports
  imports:
    - fontawesome_markdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bmcorser.github.com/fontawesome-markdown
  license: GPL
  summary: 'Font Awesome support for Markdown'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
