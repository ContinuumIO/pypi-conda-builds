package:
  name: django-frontend-skeleton
  version: "3.0.0"

source:
  fn: django-frontend-skeleton-3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-frontend-skeleton/django-frontend-skeleton-3.0.0.tar.gz
  md5: bc9cf3de56cd0ef0364d78446f02e2cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-frontend-skeleton = django-frontend-skeleton:main
    #
    # Would create an entry point called django-frontend-skeleton that calls django-frontend-skeleton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-frontend >=1.0.0

  run:
    - python
    - django-frontend >=1.0.0

test:
  # Python imports
  imports:
    - djfrontend
    - djfrontend.skeleton

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jonfaustman/django-frontend-skeleton
  license: MIT License
  summary: 'A basic Django template skeleton built on HTML5 Boilerplate and Twitter Bootstrap.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
