package:
  name: foxtrot-client
  version: "0.2.3"

source:
  fn: foxtrot-client-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/f/foxtrot-client/foxtrot-client-0.2.3.tar.gz
  md5: 00fb3643f829181e068b15db301751dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foxtrot-client = foxtrot-client:main
    #
    # Would create an entry point called foxtrot-client that calls foxtrot-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - foxtrot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FoxtrotSystems/api-client-python
  license: MIT License
  summary: 'Foxtrot Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
