package:
  name: django-db-logger
  version: "0.1.2"

source:
  fn: django-db-logger-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-logger/django-db-logger-0.1.2.tar.gz
  md5: 74335fe913b236261e567e16667cca12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-logger = django-db-logger:main
    #
    # Would create an entry point called django-db-logger that calls django-db-logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - django_db_logger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CiCiUi/django-db-logger
  license: MIT License
  summary: 'Django logging in database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
