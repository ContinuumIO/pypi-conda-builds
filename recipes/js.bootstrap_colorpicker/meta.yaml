package:
  name: js.bootstrap_colorpicker
  version: "2012.09.29-1"

source:
  fn: js.bootstrap_colorpicker-2012.09.29-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.bootstrap_colorpicker/js.bootstrap_colorpicker-2012.09.29-1.tar.gz
  md5: 7467b273c520ef16d9ef72fdef6898ab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.bootstrap_colorpicker = js.bootstrap_colorpicker:main
    #
    # Would create an entry point called js.bootstrap_colorpicker that calls js.bootstrap_colorpicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.bootstrap

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.bootstrap

test:
  # Python imports
  imports:
    - js
    - js.bootstrap_colorpicker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/s_nikolai/js.bootstrap_colorpicker
  license: BSD
  summary: 'Fanstatic packaging of Colorpicker for Bootstrap (bootstrap-colorpicker.js)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
