package:
  name: d2lvalence
  version: "1.1.1"

source:
  fn: D2LValence-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/D/D2LValence/D2LValence-1.1.1.tar.gz
  md5: 6be7810bb286cf3dda13165d6e10a5f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - d2lvalence = d2lvalence:main
    #
    # Would create an entry point called d2lvalence that calls d2lvalence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.0

  run:
    - python
    - requests >=1.2.0

test:
  # Python imports
  imports:
    - d2lvalence

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.desire2learn.com/r/valencehome
  license: Apache Software License
  summary: 'D2LValence client library for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
