package:
  name: hurry.jstree
  version: "1.0rc2.1"

source:
  fn: hurry.jstree-1.0rc2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.jstree/hurry.jstree-1.0rc2.1.tar.gz
  md5: cc26372ac1ada3c6f38b8d80f0b34cda
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.jstree = hurry.jstree:main
    #
    # Would create an entry point called hurry.jstree that calls hurry.jstree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10
    - hurry.jquery

  run:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10
    - hurry.jquery

test:
  # Python imports
  imports:
    - hurry
    - hurry.jstree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'hurry.resource resources for JsTree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
