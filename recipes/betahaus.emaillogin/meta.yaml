package:
  name: betahaus.emaillogin
  version: "0.6.1"

source:
  fn: betahaus.emaillogin-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/b/betahaus.emaillogin/betahaus.emaillogin-0.6.1.tar.gz
  md5: b89b4ff9fdb9690eeed158092752a4bc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betahaus.emaillogin = betahaus.emaillogin:main
    #
    # Would create an entry point called betahaus.emaillogin that calls betahaus.emaillogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.pluggableauthservice

  run:
    - python
    - setuptools
    - setuptools
    - products.pluggableauthservice

test:
  # Python imports
  imports:
    - betahaus
    - betahaus.emaillogin
    - betahaus.emaillogin.Extensions
    - betahaus.emaillogin.plugins
    - betahaus.emaillogin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/betahaus.emaillogin
  license: GNU General Public License (GPL)
  summary: 'Enables you to login using your emailaddress.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
