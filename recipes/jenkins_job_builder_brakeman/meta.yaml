package:
  name: jenkins_job_builder_brakeman
  version: "0.1"

source:
  fn: jenkins_job_builder_brakeman-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jenkins_job_builder_brakeman/jenkins_job_builder_brakeman-0.1.tar.gz
  md5: 98b929c8e6a3948c805881e5e1ffcd00
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jenkins_job_builder_brakeman = jenkins_job_builder_brakeman:main
    #
    # Would create an entry point called jenkins_job_builder_brakeman that calls jenkins_job_builder_brakeman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jenkins-job-builder >=0.6.0

  run:
    - python
    - setuptools
    - jenkins-job-builder >=0.6.0

test:
  # Python imports
  imports:
    - jenkins_job_builder_brakeman
    - jenkins_job_builder_brakeman.modules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/garethr/jenkins-job-builder-brakeman
  license: Apache Software License
  summary: 'A jenkins job builder plugin to allow enabling Brakeman'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
