package:
  name: zenircbot_api
  version: "2.2.6"

source:
  fn: zenircbot_api-2.2.6.tar.gz
  url: https://pypi.python.org/packages/source/z/zenircbot_api/zenircbot_api-2.2.6.tar.gz
  md5: 1c38428408d6c81a4f7d4a77323f2bc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zenircbot_api = zenircbot_api:main
    #
    # Would create an entry point called zenircbot_api that calls zenircbot_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - gevent

  run:
    - python
    - redis
    - gevent

test:
  # Python imports
  imports:
    - zenircbot_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zenircbot.rtfd.org/
  license: MIT License
  summary: 'API for ZenIRCBot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
