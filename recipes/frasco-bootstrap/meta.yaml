package:
  name: frasco-bootstrap
  version: "0.1"

source:
  fn: frasco-bootstrap-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-bootstrap/frasco-bootstrap-0.1.tar.gz
  md5: 86caad8bb70560575feff2dc7e420a28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-bootstrap = frasco-bootstrap:main
    #
    # Would create an entry point called frasco-bootstrap that calls frasco-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - frasco-assets

  run:
    - python
    - frasco
    - frasco-assets

test:
  # Python imports
  imports:
    - frasco_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-bootstrap
  license: MIT
  summary: 'Bootstrap (frontend framework) integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
