package:
  name: django-user-profile
  version: "1.0.3"

source:
  fn: django-user-profile-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-profile/django-user-profile-1.0.3.tar.gz
  md5: 77fed11860660a2ba412314279abc339
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-profile = django-user-profile:main
    #
    # Would create an entry point called django-user-profile that calls django-user-profile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - user_profile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.wisgoon.com/
  license: UNKNOWN
  summary: 'user profile for pinterest apllication like in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
