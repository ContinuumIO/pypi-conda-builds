package:
  name: ledcontroller
  version: "1.1.0"

source:
  fn: ledcontroller-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ledcontroller/ledcontroller-1.1.0.tar.gz
  md5: 2e27505429513cb6f8050af5af70a911
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ledcontroller = ledcontroller:main
    #
    # Would create an entry point called ledcontroller that calls ledcontroller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ledcontroller

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ojarva/python-ledcontroller
  license: BSD License
  summary: 'Controller library for limitlessled/easybulb/milight Wi-Fi LEDs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
