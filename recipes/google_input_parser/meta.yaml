package:
  name: google_input_parser
  version: "0.1"

source:
  fn: google_input_parser-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/google_input_parser/google_input_parser-0.1.tar.gz
  md5: 83e9c17f93298be9a8c29c65d2eee684
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google_input_parser = google_input_parser:main
    #
    # Would create an entry point called google_input_parser that calls google_input_parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google_input_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/an_example_pypi_project
  license: GNU General Public License v3 (GPLv3)
  summary: 'A quick-and-dirty input parser for Google Code Jams.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
