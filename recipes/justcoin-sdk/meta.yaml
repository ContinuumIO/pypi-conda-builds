package:
  name: justcoin-sdk
  version: "0.0.9"

source:
  fn: justcoin-sdk-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/j/justcoin-sdk/justcoin-sdk-0.0.9.tar.gz
  md5: 4123809d15ae69adbf52e77311726e05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - justcoin-sdk = justcoin-sdk:main
    #
    # Would create an entry point called justcoin-sdk that calls justcoin-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - justcoin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vascop/justcoin-sdk
  license: MIT License
  summary: 'Python SDK for the Justcoin API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
