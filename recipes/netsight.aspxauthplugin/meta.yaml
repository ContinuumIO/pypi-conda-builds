package:
  name: netsight.aspxauthplugin
  version: "1.0.3"

source:
  fn: netsight.aspxauthplugin-1.0.3.zip
  url: https://pypi.python.org/packages/source/n/netsight.aspxauthplugin/netsight.aspxauthplugin-1.0.3.zip
  md5: 2fde51668401f55990a07b95acd65075
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netsight.aspxauthplugin = netsight.aspxauthplugin:main
    #
    # Would create an entry point called netsight.aspxauthplugin that calls netsight.aspxauthplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pycrypto

  run:
    - python
    - setuptools
    - setuptools
    - pycrypto

test:
  # Python imports
  imports:
    - netsight
    - netsight.aspxauthplugin
    - netsight.aspxauthplugin.browser
    - netsight.aspxauthplugin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'PAS Plugin for Zope/Plone that can authenticate a user via the .NET ASPXAUTH cookie'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
