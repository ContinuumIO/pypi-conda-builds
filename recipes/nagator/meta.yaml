package:
  name: nagator
  version: "0.0.7"

source:
  fn: nagator-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/n/nagator/nagator-0.0.7.tar.gz
  md5: 2c788450738706c578479b15d98cbeba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nagator = nagator:main
    #
    # Would create an entry point called nagator that calls nagator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pynag
    - clint

  run:
    - python
    - pynag
    - clint

test:
  # Python imports
  imports:
    - nagator
    - nagator.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Socketubs/Nagator
  license: Naghni
  summary: 'Nagios configuration viewer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
