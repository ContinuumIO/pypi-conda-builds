package:
  name: monocle_contacts
  version: "0.1.0"

source:
  fn: monocle_contacts-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle_contacts/monocle_contacts-0.1.0.tar.gz
  md5: 9386377382564e90fe44b26193d79f90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle_contacts = monocle_contacts:main
    #
    # Would create an entry point called monocle_contacts that calls monocle_contacts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monocle_contacts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Sample app for django-monocle project generated by scaffolder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
