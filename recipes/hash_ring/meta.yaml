package:
  name: hash_ring
  version: "1.3.1"

source:
  fn: hash_ring-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hash_ring/hash_ring-1.3.1.tar.gz
  md5: 16905a5bc16927a03a1ec29f7f762649
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hash_ring = hash_ring:main
    #
    # Would create an entry point called hash_ring that calls hash_ring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hash_ring

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.amix.dk/
  license: BSD License
  summary: 'Implements consistent hashing in Python (using md5 as hashing function).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
