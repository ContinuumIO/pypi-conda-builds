package:
  name: django-minimizer
  version: "0.1.4"

source:
  fn: django-minimizer-0.1.4.tar.bz2
  url: https://pypi.python.org/packages/source/d/django-minimizer/django-minimizer-0.1.4.tar.bz2
  md5: 28b99ddd043d3241961e217dccd89ea3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-minimizer = django-minimizer:main
    #
    # Would create an entry point called django-minimizer that calls django-minimizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - djangominimizer
    - djangominimizer.management
    - djangominimizer.management.commands
    - djangominimizer.migrations
    - djangominimizer.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-minimizer.alageek.com
  license: GPLv3
  summary: 'Django application for minimizing static files and using simple html5 template\n with jQuery and modernizr.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
