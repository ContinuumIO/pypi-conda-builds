package:
  name: codetransformer
  version: "0.4.0"

source:
  fn: codetransformer-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/codetransformer/codetransformer-0.4.0.tar.gz
  md5: 915f4f1b4933fd301848bdf3dc62cb69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codetransformer = codetransformer:main
    #
    # Would create an entry point called codetransformer that calls codetransformer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - codetransformer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/llllllllll/codetransformer
  license: GNU General Public License v2 (GPLv2)
  summary: 'Python code object transformers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
