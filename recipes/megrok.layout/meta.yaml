package:
  name: megrok.layout
  version: "1.3"

source:
  fn: megrok.layout-1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.layout/megrok.layout-1.3.tar.gz
  md5: adb6db928573b15f100559bef15e9843
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.layout = megrok.layout:main
    #
    # Would create an entry point called megrok.layout that calls megrok.layout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - grokcore.component
    - grokcore.formlib
    - grokcore.message
    - grokcore.security
    - grokcore.view >=2.3
    - martian
    - setuptools
    - zope.component >=3.9.1
    - zope.interface
    - zope.publisher

  run:
    - python
    - grokcore.component
    - grokcore.formlib
    - grokcore.message
    - grokcore.security
    - grokcore.view >=2.3
    - martian
    - setuptools
    - zope.component >=3.9.1
    - zope.interface
    - zope.publisher

test:
  # Python imports
  imports:
    - megrok
    - megrok.layout
    - megrok.layout.tests
    - megrok.layout.tests.errors
    - megrok.layout.tests.layout
    - megrok.layout.tests.layout.static_fixture
    - megrok.layout.tests.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/megrok.layout
  license: GPL
  summary: 'A layout component package for zope3 and Grok.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
