package:
  name: collective.recipe.plonesite
  version: "1.9.1"

source:
  fn: collective.recipe.plonesite-1.9.1.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.plonesite/collective.recipe.plonesite-1.9.1.zip
  md5: 3aee9214829204932e15fb0d3f2d2b37
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.plonesite = collective.recipe.plonesite:main
    #
    # Would create an entry point called collective.recipe.plonesite that calls collective.recipe.plonesite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.plonesite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sixfeetup.com
  license: Zope Public License
  summary: 'A buildout recipe to create and update a plone site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
