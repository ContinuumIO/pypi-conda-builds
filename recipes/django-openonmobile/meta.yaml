package:
  name: django-openonmobile
  version: "0.2"

source:
  fn: django-openonmobile-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-openonmobile/django-openonmobile-0.2.tar.gz
  md5: 37aa2eb337a529b6dbfbbcdd3ce36e6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-openonmobile = django-openonmobile:main
    #
    # Would create an entry point called django-openonmobile that calls django-openonmobile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pil ==1.1.7
    - qrcode ==2.4.1

  run:
    - python
    - pil ==1.1.7
    - qrcode ==2.4.1

test:
  # Python imports
  imports:
    - openonmobile
    - openonmobile.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ferranp/django-openonmobile
  license: GNU General Public License (GPL)
  summary: 'Open current URL on a mobile with a QR code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
