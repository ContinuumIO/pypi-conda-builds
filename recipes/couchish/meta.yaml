package:
  name: couchish
  version: "0.2.2"

source:
  fn: couchish-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/couchish/couchish-0.2.2.tar.gz
  md5: e0342d283b37517c0acbc65bd46ae448
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchish = couchish:main
    #
    # Would create an entry point called couchish that calls couchish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - couchdb-session
    - dottedish
    - jsonish
    - schemaish

  run:
    - python
    - setuptools
    - pyyaml
    - couchdb-session
    - dottedish
    - jsonish
    - schemaish

test:
  # Python imports
  imports:
    - couchish
    - couchish.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ish.io
  license: UNKNOWN
  summary: 'Couchdb library that includes reference cacheing triggers and consolidates updates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
