package:
  name: django-jsonis
  version: "0.1.12"

source:
  fn: django-jsonis-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsonis/django-jsonis-0.1.12.tar.gz
  md5: ddb326293a4c635bd51ab46fea146c70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsonis = django-jsonis:main
    #
    # Would create an entry point called django-jsonis that calls django-jsonis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0
    - pyjwt

  run:
    - python
    - django >=1.4.0
    - pyjwt

test:
  # Python imports
  imports:
    - jsonis
    - jsonis.jwt
    - jsonis.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rychlis/django-jsonis
  license: MIT License
  summary: 'Django JSON Utils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
