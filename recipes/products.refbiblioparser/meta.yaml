package:
  name: products.refbiblioparser
  version: "1.2"

source:
  fn: Products.RefBiblioParser-1.2.zip
  url: https://pypi.python.org/packages/source/P/Products.RefBiblioParser/Products.RefBiblioParser-1.2.zip
  md5: 14550fe8a9ebd3e6d675fc648aef417e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.refbiblioparser = products.refbiblioparser:main
    #
    # Would create an entry point called products.refbiblioparser that calls products.refbiblioparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - plone
    - plone.app.upgrade
    - bibliograph.core
    - bibliograph.parsing
    - bibliograph.rendering
    - products.cmfbibliographyat

  run:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - plone
    - plone.app.upgrade
    - bibliograph.core
    - bibliograph.parsing
    - bibliograph.rendering
    - products.cmfbibliographyat

test:
  # Python imports
  imports:
    - Products
    - Products.RefBiblioParser
    - Products.RefBiblioParser.Extensions
    - Products.RefBiblioParser.browser
    - Products.RefBiblioParser.migrations
    - Products.RefBiblioParser.migrations.v11
    - Products.RefBiblioParser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.RefBiblioParser
  license: GPL
  summary: 'Addon for CMFBibliographyAT to parse/export REF files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
