package:
  name: django-runner
  version: "0.1.1"

source:
  fn: django-runner-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-runner/django-runner-0.1.1.tar.gz
  md5: c90dc9b06b7c736ecf0a716d468e0028
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-runner = django-runner:main
    #
    # Would create an entry point called django-runner that calls django-runner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_runner
    - django_runner.management
    - django_runner.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eagletmt/django-runner
  license: MIT
  summary: 'Run arbitrary script within Django environment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
