package:
  name: argvalidate
  version: "0.9.0"

source:
  fn: argvalidate-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/a/argvalidate/argvalidate-0.9.0.tar.gz
  md5: 32fc8e39734fab44c00bd3d33d711225
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argvalidate = argvalidate:main
    #
    # Would create an entry point called argvalidate that calls argvalidate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - argvalidate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/sp/python-argvalidate
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Simple argument validator library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
