package:
  name: hlsclient
  version: "0.5.3"

source:
  fn: hlsclient-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hlsclient/hlsclient-0.5.3.tar.gz
  md5: 39d9c07549b6d623049019fc2d5b0430
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hlsclient = hlsclient:main
    #
    # Would create an entry point called hlsclient that calls hlsclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - futures ==2.1.3
    - m3u8 >=0.1.1
    - pycrypto >=2.5
    - lockfile >=0.9.1

  run:
    - python
    - futures ==2.1.3
    - m3u8 >=0.1.1
    - pycrypto >=2.5
    - lockfile >=0.9.1

test:
  # Python imports
  imports:
    - hlsclient
    - hlsclient.workers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/hlsclient
  license: UNKNOWN
  summary: 'Client to download all files from HLS streams'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
