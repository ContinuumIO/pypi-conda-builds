package:
  name: pygsr
  version: "0.1"

source:
  fn: pygsr-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pygsr/pygsr-0.1.tar.gz
  md5: 993b3cdb6651d05a250963bf423909d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygsr = pygsr:main
    #
    # Would create an entry point called pygsr that calls pygsr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyaudio >=0.2.4

  run:
    - python
    - pyaudio >=0.2.4

test:
  # Python imports
  imports:
    - pygsr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/drneox/pygsr
  license: GPL
  summary: 'Simple way to access google api for speech recognition with python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
