package:
  name: chimpy
  version: "0.2b2"

source:
  fn: chimpy-0.2b2.tar.gz
  url: https://pypi.python.org/packages/source/c/chimpy/chimpy-0.2b2.tar.gz
  md5: da1dd149d947559462d9101e589fa15c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chimpy = chimpy:main
    #
    # Would create an entry point called chimpy that calls chimpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - chimpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/chimpy/
  license: BSD License
  summary: 'Python wrapper for the MailChimp API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
