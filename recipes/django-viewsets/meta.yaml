package:
  name: django-viewsets
  version: "0.1.7"

source:
  fn: django-viewsets-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewsets/django-viewsets-0.1.7.tar.gz
  md5: 4ca8c87ef8c688fd4be42cf1e9073fef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewsets = django-viewsets:main
    #
    # Would create an entry point called django-viewsets that calls django-viewsets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - viewsets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BertrandBordage/django-viewsets
  license: BSD License
  summary: 'Avoid boring views and urls.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
