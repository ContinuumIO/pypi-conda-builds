package:
  name: astmonkey
  version: "0.1.1"

source:
  fn: astmonkey-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/astmonkey/astmonkey-0.1.1.tar.gz
  md5: 5247bf00b281c297398a37d52975580b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astmonkey = astmonkey:main
    #
    # Would create an entry point called astmonkey that calls astmonkey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pydot

  run:
    - python
    - pydot

test:
  # Python imports
  imports:
    - astmonkey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/konradhalas/astmonkey
  license: Apache Software License
  summary: 'astmonkey is a set of tools to play with Python AST.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
