package:
  name: django-contact-form-fc
  version: "0.5.1"

source:
  fn: django-contact-form-fc-0.5.1.zip
  url: https://pypi.python.org/packages/source/d/django-contact-form-fc/django-contact-form-fc-0.5.1.zip
  md5: ca7a6aa63f30337096c5afe919d3ca87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contact-form-fc = django-contact-form-fc:main
    #
    # Would create an entry point called django-contact-form-fc that calls django-contact-form-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-templated-email >=0.4.7,<0.5

  run:
    - python
    - django-templated-email >=0.4.7,<0.5

test:
  # Python imports
  imports:
    - contact_form

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-contact-form
  license: BSD License
  summary: 'Django Contact Form using class based views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
