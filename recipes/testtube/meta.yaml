package:
  name: testtube
  version: "1.0.0"

source:
  fn: testtube-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/t/testtube/testtube-1.0.0.tar.gz
  md5: 84ad4d9cf17c296141f07c28a4f7d8e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testtube = testtube:main
    #
    # Would create an entry point called testtube that calls testtube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.2.0
    - termcolor ==1.1.0
    - watchdog ==0.7.1

  run:
    - python
    - six >=1.2.0
    - termcolor ==1.1.0
    - watchdog ==0.7.1

test:
  # Python imports
  imports:
    - testtube
    - testtube.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thomasw/testtube
  license: UNKNOWN
  summary: 'Testtube watches a specified directory for file changes and runs a set of defined tests against those files whenever a change occurs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
