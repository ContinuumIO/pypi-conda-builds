package:
  name: js_project
  version: "0.1.4"

source:
  fn: js_project-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/j/js_project/js_project-0.1.4.tar.gz
  md5: adb7d90619b11139367ec29c151c4af8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js_project = js_project:main
    #
    # Would create an entry point called js_project that calls js_project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - fanstatic

  run:
    - python
    - setuptools
    - pyramid
    - fanstatic

test:
  # Python imports
  imports:
    - js_project
    - js_project.scaffolds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ppinette/pyramid_foundation
  license: LICENSE.txt
  summary: 'pcreate scaffold for fanstatic resource library projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
