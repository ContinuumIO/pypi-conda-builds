package:
  name: chessnut
  version: "0.2.14"

source:
  fn: Chessnut-0.2.14.tar.gz
  url: https://pypi.python.org/packages/source/C/Chessnut/Chessnut-0.2.14.tar.gz
  md5: 68a7334484ca45e21ae684ba47894181
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chessnut = chessnut:main
    #
    # Would create an entry point called chessnut that calls chessnut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - Chessnut
    - chessnut

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cgearhart/Chessnut
  license: UNLICENSE
  summary: 'A basic chess model to imports/export FEN & finds moves.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
