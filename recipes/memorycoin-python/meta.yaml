package:
  name: memorycoin-python
  version: "0.1"

source:
  fn: memorycoin-python-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/memorycoin-python/memorycoin-python-0.1.tar.gz
  md5: 0ce188c5b813dd0dc324c2bf6476bf26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memorycoin-python = memorycoin-python:main
    #
    # Would create an entry point called memorycoin-python that calls memorycoin-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - memorycoinrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/emre
  license: MIT License
  summary: 'Friendly Memorycoin JSON-RPC API binding for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
