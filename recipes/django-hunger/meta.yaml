package:
  name: django-hunger
  version: "2.1.2"

source:
  fn: django-hunger-2.1.2.zip
  url: https://pypi.python.org/packages/source/d/django-hunger/django-hunger-2.1.2.zip
  md5: f0cf17ea0cb72851407bc2315e3ed394
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hunger = django-hunger:main
    #
    # Would create an entry point called django-hunger that calls django-hunger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hunger
    - hunger.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joshuakarjala/django-hunger/
  license: MIT License
  summary: 'A Django app to manage a private beta phase.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
