package:
  name: dataset_owners
  version: "0.0.1"

source:
  fn: dataset_owners-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dataset_owners/dataset_owners-0.0.1.tar.gz
  md5: ff44c8155e3fa0dab4c66df2887648a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataset_owners = dataset_owners:main
    #
    # Would create an entry point called dataset_owners that calls dataset_owners.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pluplusch >=0.0.7

  run:
    - python
    - pluplusch >=0.0.7

test:
  # Python imports
  imports:
    - dataset_owners

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/dataset-owners
  license: AGPL
  summary: 'Dataset owners from Socrata Open Data Catalog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
