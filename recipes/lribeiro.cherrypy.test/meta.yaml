package:
  name: lribeiro.cherrypy.test
  version: "1.1.0"

source:
  fn: lribeiro.cherrypy.test-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lribeiro.cherrypy.test/lribeiro.cherrypy.test-1.1.0.tar.gz
  md5: 06922b56c73ed3252324b3de368256f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lribeiro.cherrypy.test = lribeiro.cherrypy.test:main
    #
    # Would create an entry point called lribeiro.cherrypy.test that calls lribeiro.cherrypy.test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy

  run:
    - python
    - cherrypy

test:
  # Python imports
  imports:
    - lribeiro
    - lribeiro.cherrypy
    - lribeiro.cherrypy.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/livioribeiro/cherrypy-test
  license: BSD License
  summary: 'Test utilities for CherryPy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
