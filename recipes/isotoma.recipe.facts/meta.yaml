package:
  name: isotoma.recipe.facts
  version: "0.0.8"

source:
  fn: isotoma.recipe.facts-0.0.8.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.facts/isotoma.recipe.facts-0.0.8.zip
  md5: 96d3bea1e0ec5b3417bf32a570c6a93a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.facts = isotoma.recipe.facts:main
    #
    # Would create an entry point called isotoma.recipe.facts that calls isotoma.recipe.facts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.facts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'A recipe to provide facts about the context a buildout is running in'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
