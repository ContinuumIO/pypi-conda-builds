package:
  name: fmn.web
  version: "0.6.0"

source:
  fn: fmn.web-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fmn.web/fmn.web-0.6.0.tar.gz
  md5: 2ee578f37a74f2458471b22ae474f355
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fmn.web = fmn.web:main
    #
    # Would create an entry point called fmn.web that calls fmn.web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.8
    - fmn.lib
    - python-openid
    - python-openid-cla
    - python-openid-teams
    - flask
    - flask-openid >=1.2.4
    - wtforms
    - docutils
    - markupsafe
    - pylibravatar
    - pydns
    - urllib3
    - datanommer.models
    - arrow

  run:
    - python
    - sqlalchemy >=0.8
    - fmn.lib
    - python-openid
    - python-openid-cla
    - python-openid-teams
    - flask
    - flask-openid >=1.2.4
    - wtforms
    - docutils
    - markupsafe
    - pylibravatar
    - pydns
    - urllib3
    - datanommer.models
    - arrow

test:
  # Python imports
  imports:
    - fmn
    - fmn.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fedora-infra/fmn.web/
  license: LGPLv2+
  summary: 'Frontend Web Application for Fedora Notifications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
