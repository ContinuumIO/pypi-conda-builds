package:
  name: cilantro
  version: "2.0.11"

source:
  fn: cilantro-2.0.11.tar.gz
  url: https://pypi.python.org/packages/source/c/cilantro/cilantro-2.0.11.tar.gz
  md5: 81dc68cd208925ad16854c2e5405def2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cilantro = cilantro:main
    #
    # Would create an entry point called cilantro that calls cilantro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - serrano >=2.0.16,<2.1

  run:
    - python
    - serrano >=2.0.16,<2.1

test:
  # Python imports
  imports:
    - cilantro
    - cilantro.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cbmi.github.com/cilantro
  license: BSD License
  summary: 'A client implementation for Serrano 2.x'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
