package:
  name: sphinxit
  version: "0.3.2"

source:
  fn: sphinxit-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxit/sphinxit-0.3.2.tar.gz
  md5: ecc94890c7fef26913ec7ea3436da6e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxit = sphinxit:main
    #
    # Would create an entry point called sphinxit that calls sphinxit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.1.0
    - oursql >=0.9.3
    - ordereddict >=1.1

  run:
    - python
    - six >=1.1.0
    - oursql >=0.9.3
    - ordereddict >=1.1

test:
  # Python imports
  imports:
    - sphinxit
    - sphinxit.core
    - sphinxit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/semirook/sphinxit
  license: BSD License
  summary: 'Lite and powerful SphinxQL query constructor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
