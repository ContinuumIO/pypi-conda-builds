package:
  name: excelsior
  version: "0.0.5"

source:
  fn: excelsior-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/e/excelsior/excelsior-0.0.5.tar.gz
  md5: bea8c4255118ecd6e07c51ea37360ce3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - excelsior = excelsior:main
    #
    # Would create an entry point called excelsior that calls excelsior.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.11
    - docopt ==0.6.2
    - xlrd ==0.9.3

  run:
    - python
    - pyyaml ==3.11
    - docopt ==0.6.2
    - xlrd ==0.9.3

test:
  # Python imports
  imports:
    - excelsior

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peterhil/excelsior
  license: BSD License
  summary: 'Convert Excel spreadsheets into TSV, CSV, Json or Yaml format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
