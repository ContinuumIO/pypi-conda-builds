package:
  name: daybed
  version: "1.1.0"

source:
  fn: daybed-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/daybed/daybed-1.1.0.tar.gz
  md5: e8f75397a260e64cc79d4c95726dafce
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daybed = daybed:main
    #
    # Would create an entry point called daybed that calls daybed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - couchdb
    - colander
    - cornice
    - elasticsearch
    - hiredis
    - koremutake
    - pyramid
    - pyramid_hawkauth
    - pyramid_multiauth
    - redis
    - setuptools
    - six

  run:
    - python
    - setuptools
    - couchdb
    - colander
    - cornice
    - elasticsearch
    - hiredis
    - koremutake
    - pyramid
    - pyramid_hawkauth
    - pyramid_multiauth
    - redis
    - setuptools
    - six

test:
  # Python imports
  imports:
    - daybed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://daybed.readthedocs.org/
  license: BSD License
  summary: 'Form validation and data storage API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
