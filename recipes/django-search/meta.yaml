package:
  name: django-search
  version: "1.0"

source:
  fn: django-search-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-search/django-search-1.0.tar.gz
  md5: b6b0ebc0cd992b00e1e7bb99787e5e08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-search = django-search:main
    #
    # Would create an entry point called django-search that calls django-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - search
    - search.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/hellmoon666/django-search
  license: UNKNOWN
  summary: 'A django reusable app that adds a simple search to your project using a single tag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
