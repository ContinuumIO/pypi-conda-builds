package:
  name: entschachtler
  version: "1.0.0"

source:
  fn: entschachtler-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/e/entschachtler/entschachtler-1.0.0.tar.gz
  md5: 842d484e0aa5f17f7b7f26f00b18af0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - entschachtler = entschachtler:main
    #
    # Would create an entry point called entschachtler that calls entschachtler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - entschachtler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.headfirstlabs.com
  license: UNKNOWN
  summary: 'Einfache Ausgabe geschachtleter Listen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
