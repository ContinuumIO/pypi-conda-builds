package:
  name: pytidylib
  version: "0.2.4"

source:
  fn: pytidylib-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pytidylib/pytidylib-0.2.4.tar.gz
  md5: 2a28267370c9409b592cdb786649cb25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pytidylib = pytidylib:main
    #
    # Would create an entry point called pytidylib that calls pytidylib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tidylib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://countergram.com/open-source/pytidylib/
  license: MIT License
  summary: 'Python wrapper for HTML Tidy (tidylib) on Python 2 and 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
