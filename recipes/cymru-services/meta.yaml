package:
  name: cymru-services
  version: "1.4"

source:
  fn: cymru-services-1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cymru-services/cymru-services-1.4.tar.gz
  md5: 75bb1a8f35cf1eb0da5adf7a91802319
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cymru-services = cymru-services:main
    #
    # Would create an entry point called cymru-services that calls cymru-services.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cymru
    - cymru.bogon
    - cymru.core
    - cymru.ip2asn
    - cymru.mhr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/cymru-services/
  license: GNU General Public License (GPL)
  summary: 'Python API for the Cymru Services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
