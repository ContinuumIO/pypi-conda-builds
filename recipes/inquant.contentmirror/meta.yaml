package:
  name: inquant.contentmirror
  version: "0.1"

source:
  fn: inquant.contentmirror-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/inquant.contentmirror/inquant.contentmirror-0.1.tar.gz
  md5: bffc5c1601181f3d4e4917384fe64291
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inquant.contentmirror = inquant.contentmirror:main
    #
    # Would create an entry point called inquant.contentmirror that calls inquant.contentmirror.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - inquant
    - inquant.contentmirror
    - inquant.contentmirror.browser
    - inquant.contentmirror.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/inquant.contentmirror/trunk
  license: GPL
  summary: 'mirror plone content transparently in a plone site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
