package:
  name: tranchitella.recipe.fs
  version: "0.3"

source:
  fn: tranchitella.recipe.fs-0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tranchitella.recipe.fs/tranchitella.recipe.fs-0.3.tar.gz
  md5: 849871907d854f5b1008513b7e7fe1f3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tranchitella.recipe.fs = tranchitella.recipe.fs:main
    #
    # Would create an entry point called tranchitella.recipe.fs that calls tranchitella.recipe.fs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - tranchitella
    - tranchitella.recipe
    - tranchitella.recipe.fs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/tranchitella.recipe.fs
  license: GPL 2
  summary: 'Buildout recipe to create files and directories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
