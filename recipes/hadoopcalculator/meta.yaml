package:
  name: hadoopcalculator
  version: "1.0"

source:
  fn: HadoopCalculator-1.0.tar.gz
  url: https://pypi.python.org/packages/source/H/HadoopCalculator/HadoopCalculator-1.0.tar.gz
  md5: 3a57f230f2bdaeabf439875d31c5f977
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hadoopcalculator = hadoopcalculator:main
    #
    # Would create an entry point called hadoopcalculator that calls hadoopcalculator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hadoopcalculator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cloud.github.com/downloads/jonEbird/Hadoop-Utils/hadoop_calculator.py
  license: Apache Software License
  summary: 'Hadoop Calculator - Providing an educated guess for performance of your Hadoop cluster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
