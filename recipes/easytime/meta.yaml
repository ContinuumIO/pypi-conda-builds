package:
  name: easytime
  version: "0.1.2"

source:
  fn: easytime-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/easytime/easytime-0.1.2.tar.gz
  md5: 0a55aba541a8033394aecd16c2082c5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easytime = easytime:main
    #
    # Would create an entry point called easytime that calls easytime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz >=2012h

  run:
    - python
    - pytz >=2012h

test:
  # Python imports
  imports:
    - easytime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/easytime
  license: UNLICENSE
  summary: 'The simplest python time library ever written.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
