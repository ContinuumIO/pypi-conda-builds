package:
  name: django-ical
  version: "1.3"

source:
  fn: django-ical-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ical/django-ical-1.3.tar.gz
  md5: a19f4a6a02a0794b7e954ef986c81a1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ical = django-ical:main
    #
    # Would create an entry point called django-ical that calls django-ical.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.4
    - icalendar >=3.1

  run:
    - python
    - django >=1.3.4
    - icalendar >=3.1

test:
  # Python imports
  imports:
    - django_ical

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/IanLewis/django-ical
  license: BSD License
  summary: "iCal feeds for Django based on Django's syndication feed framework."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
