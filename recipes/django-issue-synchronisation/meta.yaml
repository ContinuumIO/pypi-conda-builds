package:
  name: django-issue-synchronisation
  version: "0.1.6"

source:
  fn: django-issue-synchronisation-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-issue-synchronisation/django-issue-synchronisation-0.1.6.tar.gz
  md5: 81317179545c3bb6102dc285390d837e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-issue-synchronisation = django-issue-synchronisation:main
    #
    # Would create an entry point called django-issue-synchronisation that calls django-issue-synchronisation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - issues
    - issues.backends
    - issues.hooks
    - issues.management
    - issues.management.commands
    - issues.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-issue-synchronisation.googlecode.com
  license: GNU General Public License (GPL)
  summary: 'Issue synchronisation for django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
