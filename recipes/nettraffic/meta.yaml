package:
  name: nettraffic
  version: "0.0.2"

source:
  fn: nettraffic-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nettraffic/nettraffic-0.0.2.tar.gz
  md5: a9b695cd0efc8199631c66df0adee06e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nettraffic = nettraffic:main
    #
    # Would create an entry point called nettraffic that calls nettraffic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysnmp
    - pysnmp-mibs

  run:
    - python
    - pysnmp
    - pysnmp-mibs

test:
  # Python imports
  imports:
    - nettraffic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ojarva/python-network-traffic
  license: BSD License
  summary: 'Small SNMP helper library for network traffic calculations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
