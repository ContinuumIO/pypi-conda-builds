package:
  name: audio.quantizers
  version: "2.2"

source:
  fn: audio.quantizers-2.2.tar.gz
  url: https://pypi.python.org/packages/source/a/audio.quantizers/audio.quantizers-2.2.tar.gz
  md5: aa3c6f7e5fb35abcef14313116f74714
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audio.quantizers = audio.quantizers:main
    #
    # Would create an entry point called audio.quantizers that calls audio.quantizers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - audio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
