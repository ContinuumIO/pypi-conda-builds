package:
  name: plone.recipe.plone21install
  version: "1.0"

source:
  fn: plone.recipe.plone21install-1.0.zip
  url: https://pypi.python.org/packages/source/p/plone.recipe.plone21install/plone.recipe.plone21install-1.0.zip
  md5: 14c49aaeaafa3053a4d99da9e9792f0f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.plone21install = plone.recipe.plone21install:main
    #
    # Would create an entry point called plone.recipe.plone21install that calls plone.recipe.plone21install.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.plone21install

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/plone.recipe.plone21install/trunk
  license: Zope Public License
  summary: 'ZC Buildout recipe for installing Plone 2.1.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
