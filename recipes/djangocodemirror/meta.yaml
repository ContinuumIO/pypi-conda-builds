package:
  name: djangocodemirror
  version: "0.9.7"

source:
  fn: djangocodemirror-0.9.7.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocodemirror/djangocodemirror-0.9.7.tar.gz
  md5: ad49aab2d3db4d9a007e8f494a1723a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocodemirror = djangocodemirror:main
    #
    # Would create an entry point called djangocodemirror that calls djangocodemirror.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rstview >=0.1.2

  run:
    - python
    - rstview >=0.1.2

test:
  # Python imports
  imports:
    - djangocodemirror
    - djangocodemirror.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/djangocodemirror
  license: MIT License
  summary: '\nRich Textarea for Django with CodeMirror and some enhancement\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
