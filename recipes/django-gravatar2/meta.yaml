package:
  name: django-gravatar2
  version: "1.3.0"

source:
  fn: django-gravatar2-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gravatar2/django-gravatar2-1.3.0.tar.gz
  md5: bb6214febb84b7fb09e5203b91534374
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gravatar2 = django-gravatar2:main
    #
    # Would create an entry point called django-gravatar2 that calls django-gravatar2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_gravatar
    - django_gravatar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twaddington/django-gravatar
  license: MIT License
  summary: 'Essential Gravatar support for Django. Features helper methods, templatetags and a full test suite!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
