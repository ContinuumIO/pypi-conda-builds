package:
  name: dynamodbfdw
  version: "0.1.1"

source:
  fn: DynamodbFdw-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DynamodbFdw/DynamodbFdw-0.1.1.tar.gz
  md5: 9fed6e5a698d5c3725b24addafd54192
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamodbfdw = dynamodbfdw:main
    #
    # Would create an entry point called dynamodbfdw that calls dynamodbfdw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - dynamodbfdw

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/avances123/dynamodb_fdw
  license: LICENSE.txt
  summary: 'Postgresql Foregin Data Wrapper mapping Amazon DynamoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
