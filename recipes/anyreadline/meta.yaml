package:
  name: anyreadline
  version: "0.1.1"

source:
  fn: anyreadline-0.1.1.zip
  url: https://pypi.python.org/packages/source/a/anyreadline/anyreadline-0.1.1.zip
  md5: 932d4f60ad3d53b99bd4d8f522dcd62f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anyreadline = anyreadline:main
    #
    # Would create an entry point called anyreadline that calls anyreadline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - anyreadline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pombredanne/anyreadline
  license: Public Domain
  summary: 'readline on most OS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
