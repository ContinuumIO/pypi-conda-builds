package:
  name: mozcrash
  version: "0.14"

source:
  fn: mozcrash-0.14.tar.gz
  url: https://pypi.python.org/packages/source/m/mozcrash/mozcrash-0.14.tar.gz
  md5: 3354712d7aee63a0870a86077a70590f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozcrash = mozcrash:main
    #
    # Would create an entry point called mozcrash that calls mozcrash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mozfile >=1.0
    - mozlog

  run:
    - python
    - mozfile >=1.0
    - mozlog

test:
  # Python imports
  imports:
    - mozcrash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://wiki.mozilla.org/Auto-tools/Projects/Mozbase
  license: MPL
  summary: 'Library for printing stack traces from minidumps left behind by crashed processes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
