package:
  name: infi.winapi.file_version_information
  version: "0.0.8"

source:
  fn: infi.winapi.file_version_information-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.winapi.file_version_information/infi.winapi.file_version_information-0.0.8.tar.gz
  md5: 8c6d68f09a2dd364b16d6503bac4cf3e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.winapi.file_version_information = infi.winapi.file_version_information:main
    #
    # Would create an entry point called infi.winapi.file_version_information that calls infi.winapi.file_version_information.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - infi.cwrap
    - infi.instruct
    - setuptools

  run:
    - python
    - infi.cwrap
    - infi.instruct
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.winapi
    - infi.winapi.file_version_information
    - infi.winapi.file_version_information.api
    - infi.winapi.file_version_information.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.winapi.file_version_information
  license: Python Software Foundation License
  summary: 'Simple API to retreive a Windows file version'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
