package:
  name: ludolph-erigones
  version: "1.1"

source:
  fn: ludolph-erigones-1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/ludolph-erigones/ludolph-erigones-1.1.tar.gz
  md5: 18c63db759820b7d24d00f81b88f9962
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ludolph-erigones = ludolph-erigones:main
    #
    # Would create an entry point called ludolph-erigones that calls ludolph-erigones.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ludolph
    - requests

  run:
    - python
    - ludolph
    - requests

test:
  # Python imports
  imports:
    - ludolph_erigones

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erigones/ludolph-erigones/
  license: MIT License
  summary: 'Ludolph: Erigones API plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
