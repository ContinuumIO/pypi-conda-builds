package:
  name: horae.app
  version: "1.0a1"

source:
  fn: horae.app-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.app/horae.app-1.0a1.tar.gz
  md5: 5fb93b1e4912f2efa2d96bbcfd1fac0d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.app = horae.app:main
    #
    # Would create an entry point called horae.app that calls horae.app.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - grokui.admin
    - grokcore.startup
    - zope.i18n[compile]
    - horae.auth
    - horae.core
    - horae.properties
    - horae.search
    - horae.ticketing

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - grokui.admin
    - grokcore.startup
    - zope.i18n[compile]
    - horae.auth
    - horae.core
    - horae.properties
    - horae.search
    - horae.ticketing

test:
  # Python imports
  imports:
    - horae
    - horae.app
    - horae.app.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides the Horae resource planning system as a Grok application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
