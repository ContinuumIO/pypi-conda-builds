package:
  name: lt2opencorpora
  version: "1.0.1"

source:
  fn: LT2OpenCorpora-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LT2OpenCorpora/LT2OpenCorpora-1.0.1.tar.gz
  md5: 2bfc215334ce0af3e17c6dd6f58d3f63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lt2opencorpora = lt2opencorpora:main
    #
    # Would create an entry point called lt2opencorpora that calls lt2opencorpora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blinker >=1.3
    - liquer >=0.0.4
    - unicodecsv >=0.9.4
    - bz2file >=0.98
    - requests

  run:
    - python
    - blinker >=1.3
    - liquer >=0.0.4
    - unicodecsv >=0.9.4
    - bz2file >=0.98
    - requests

test:
  # Python imports
  imports:
    - lt2opencorpora

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dchaplinsky/LT2OpenCorpora
  license: MIT License
  summary: 'Python script to convert Ukrainian morphological dictionary from LanguageTool project to OpenCorpora forma'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
