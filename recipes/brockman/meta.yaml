package:
  name: brockman
  version: "0.1dev"

source:
  fn: brockman-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/b/brockman/brockman-0.1dev.tar.gz
  md5: f84d52e4f84219be05a25f839d40f79b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brockman = brockman:main
    #
    # Would create an entry point called brockman that calls brockman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=0.13.0
    - omnijson >=0.1.2

  run:
    - python
    - requests >=0.13.0
    - omnijson >=0.1.2

test:
  # Python imports
  imports:
    - brockman
    - brockman.django
    - brockman.django.celery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FocusLab/brockman
  license: BSD License
  summary: 'A Python client for the FocusLab API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
