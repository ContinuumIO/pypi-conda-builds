package:
  name: collective.recipe.realpath
  version: "0.1"

source:
  fn: collective.recipe.realpath-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.realpath/collective.recipe.realpath-0.1.tar.gz
  md5: 6932b7ae25d48171cc76de01f5a0d230
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.realpath = collective.recipe.realpath:main
    #
    # Would create an entry point called collective.recipe.realpath that calls collective.recipe.realpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.realpath
    - collective.recipe.realpath.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective
  license: MIT License
  summary: 'Buildout recipe normalizes directory/path options.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
