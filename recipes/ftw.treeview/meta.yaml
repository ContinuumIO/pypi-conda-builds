package:
  name: ftw.treeview
  version: "1.2.0"

source:
  fn: ftw.treeview-1.2.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.treeview/ftw.treeview-1.2.0.zip
  md5: 6e292f9cda65a0c8d74efe50331dd52a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.treeview = ftw.treeview:main
    #
    # Would create an entry point called ftw.treeview that calls ftw.treeview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.jqcookie

  run:
    - python
    - setuptools
    - setuptools
    - collective.jqcookie

test:
  # Python imports
  imports:
    - ftw
    - ftw.treeview
    - ftw.treeview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.treeview
  license: GPL
  summary: 'Adds a navigation treeview widget to plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
