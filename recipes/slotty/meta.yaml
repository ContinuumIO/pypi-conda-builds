package:
  name: slotty
  version: "0.1dev"

source:
  fn: slotty-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/s/slotty/slotty-0.1dev.tar.gz
  md5: 9f699f91802cb558bdd8a6020e46bc95
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slotty = slotty:main
    #
    # Would create an entry point called slotty that calls slotty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gunicorn
    - flask
    - wsaccel
    - setuptools
    - gevent_socketio
    - pyzmq

  run:
    - python
    - gunicorn
    - flask
    - wsaccel
    - setuptools
    - gevent_socketio
    - pyzmq

test:
  # Python imports
  imports:
    - slotty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Web Racing Management System'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
