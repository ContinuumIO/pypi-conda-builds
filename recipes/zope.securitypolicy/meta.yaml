package:
  name: zope.securitypolicy
  version: "4.0.0"

source:
  fn: zope.securitypolicy-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.securitypolicy/zope.securitypolicy-4.0.0.tar.gz
  md5: b150a9065f649c63ec7f61167f3f8e53
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.securitypolicy = zope.securitypolicy:main
    #
    # Would create an entry point called zope.securitypolicy that calls zope.securitypolicy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - persistent
    - setuptools
    - zope.annotation
    - zope.authentication
    - zope.component
    - zope.configuration
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.schema
    - zope.security

  run:
    - python
    - persistent
    - setuptools
    - zope.annotation
    - zope.authentication
    - zope.component
    - zope.configuration
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.schema
    - zope.security

test:
  # Python imports
  imports:
    - zope
    - zope.securitypolicy
    - zope.securitypolicy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.securitypolicy
  license: Zope Public License
  summary: 'Default security policy for Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
