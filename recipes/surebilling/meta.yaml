package:
  name: surebilling
  version: "1.2"

source:
  fn: surebilling-1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/surebilling/surebilling-1.2.tar.gz
  md5: 03b4263f6f7f21ad27588ffed51da430
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - surebilling = surebilling:main
    #
    # Would create an entry point called surebilling that calls surebilling.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - surebilling

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://surebilling.com
  license: UNKNOWN
  summary: 'Python wrapper for the SureBilling API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
