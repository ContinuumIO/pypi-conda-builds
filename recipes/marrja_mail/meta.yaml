package:
  name: marrja_mail
  version: "0.2.2"

source:
  fn: Marrja_Mail-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Marrja_Mail/Marrja_Mail-0.2.2.tar.gz
  md5: d14e7fd30a935d68fb264d679868c6c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marrja_mail = marrja_mail:main
    #
    # Would create an entry point called marrja_mail that calls marrja_mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jinja2 >=2.6
    - marrow.mailer >=4.0.0

  run:
    - python
    - jinja2 >=2.6
    - marrow.mailer >=4.0.0

test:
  # Python imports
  imports:
    - marrja_mail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Dotnetwill/marrja_mail/
  license: BSD
  summary: 'Small wrapper for marrow.mailer to use Jinja2 templates and automatically send html and plain text emails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
