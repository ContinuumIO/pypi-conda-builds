package:
  name: django_scaff
  version: "0.2rc1"

source:
  fn: django_scaff-0.2rc1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_scaff/django_scaff-0.2rc1.tar.gz
  md5: 3c787d044f91686da64510e2f87055fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_scaff = django_scaff:main
    #
    # Would create an entry point called django_scaff that calls django_scaff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_scaff
    - management
    - management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juliosmelo/django_scaffold/
  license: MIT License
  summary: 'Scaffold creator for Django 1.5+ projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
