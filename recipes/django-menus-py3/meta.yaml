package:
  name: django-menus-py3
  version: "1.1.3"

source:
  fn: django-menus-py3-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-menus-py3/django-menus-py3-1.1.3.tar.gz
  md5: cfc1c3a60249bc5c9b00eb5621ef0f1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-menus-py3 = django-menus-py3:main
    #
    # Would create an entry point called django-menus-py3 that calls django-menus-py3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - menus
    - menus.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alrusdi/django-admin-bootstrap-legacy
  license: BSD License
  summary: 'Menu helpers for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
