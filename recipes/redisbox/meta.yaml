package:
  name: redisbox
  version: "0.0.1"

source:
  fn: redisbox-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redisbox/redisbox-0.0.1.tar.gz
  md5: 4e247bd1c91640693e6b15d0c2d9ea0c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redisbox = redisbox:main
    #
    # Would create an entry point called redisbox that calls redisbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - redisbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ziadsawalha/redisbox
  license: Apache Software License
  summary: 'Run sandboxed Redis instance from a python application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
