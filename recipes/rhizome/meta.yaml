package:
  name: rhizome
  version: "0.2.4"

source:
  fn: rhizome-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/r/rhizome/rhizome-0.2.4.tar.gz
  md5: 5ca5d2c3a58cb136d6165eb975469a10
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rhizome = rhizome:main
    #
    # Would create an entry point called rhizome that calls rhizome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rdflib ==2.3.1events
    - five.intid >=0.1.3

  run:
    - python
    - setuptools
    - rdflib ==2.3.1events
    - five.intid >=0.1.3

test:
  # Python imports
  imports:
    - rhizome
    - rhizome.five

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://openplans.org/projects/yucca
  license: GPL
  summary: "'generalized rdf store for zope2 and zope3'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
