package:
  name: chatrelater
  version: "0.1"

source:
  fn: ChatRelater-0.1.zip
  url: https://pypi.python.org/packages/source/C/ChatRelater/ChatRelater-0.1.zip
  md5: 5eaea267b65c4ab928469706047c8d77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chatrelater = chatrelater:main
    #
    # Would create an entry point called chatrelater that calls chatrelater.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pydot >=0.9.10
    - pyparsing >=1.4.6
    - pyyaml >=3.05

  run:
    - python
    - pydot >=0.9.10
    - pyparsing >=1.4.6
    - pyyaml >=3.05

test:
  # Python imports
  imports:
    - chatrelater
    - chatrelater.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://homework.nwsnet.de/
  license: MIT License
  summary: 'Analyze and visualize relations between chat users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
