package:
  name: chromelogger
  version: "0.4.1"

source:
  fn: chromelogger-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chromelogger/chromelogger-0.4.1.tar.gz
  md5: cb789620cfd07ef12e500645b6ff2979
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chromelogger = chromelogger:main
    #
    # Would create an entry point called chromelogger that calls chromelogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonpickle

  run:
    - python
    - jsonpickle

test:
  # Python imports
  imports:
    - chromelogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ccampbell/chromelogger-python
  license: Apache 2.0
  summary: 'Python library for logging data to Google Chrome console.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
