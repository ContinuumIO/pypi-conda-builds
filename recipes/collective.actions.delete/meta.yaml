package:
  name: collective.actions.delete
  version: "0.1.5"

source:
  fn: collective.actions.delete-0.1.5.zip
  url: https://pypi.python.org/packages/source/c/collective.actions.delete/collective.actions.delete-0.1.5.zip
  md5: 96d23466a209574f2b4aad7b3c509839
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.actions.delete = collective.actions.delete:main
    #
    # Would create an entry point called collective.actions.delete that calls collective.actions.delete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.actions
    - collective.actions.delete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.actions.delete
  license: GPL
  summary: 'Delete items in folder content with confirmation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
