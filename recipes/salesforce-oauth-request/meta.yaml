package:
  name: salesforce-oauth-request
  version: "1.0.6"

source:
  fn: salesforce-oauth-request-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/salesforce-oauth-request/salesforce-oauth-request-1.0.6.tar.gz
  md5: d8a82064bfd35c2e5a2a8fe3dcef4488
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - salesforce-oauth-request = salesforce-oauth-request:main
    #
    # Would create an entry point called salesforce-oauth-request that calls salesforce-oauth-request.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - salesforce_oauth_request

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/heroku/salesforce-oauth-request
  license: MIT License
  summary: 'Util package to drive Salesforce Oauth Web flow for testing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
