package:
  name: chromedriver_installer
  version: "0.0.4"

source:
  fn: chromedriver_installer-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/chromedriver_installer/chromedriver_installer-0.0.4.tar.gz
  md5: b6ce7b49b09fcb8997d20cb6550a1c2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chromedriver_installer = chromedriver_installer:main
    #
    # Would create an entry point called chromedriver_installer that calls chromedriver_installer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chromedriver_installer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peterhudec/chromedriver_installer
  license: MIT License
  summary: 'Chromedriver Installer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
