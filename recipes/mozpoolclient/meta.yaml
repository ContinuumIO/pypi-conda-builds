package:
  name: mozpoolclient
  version: "0.1.5"

source:
  fn: mozpoolclient-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mozpoolclient/mozpoolclient-0.1.5.tar.gz
  md5: 22e4ca70ad1e5ed41775e28a1b19fb1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozpoolclient = mozpoolclient:main
    #
    # Would create an entry point called mozpoolclient that calls mozpoolclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0

  run:
    - python
    - requests >=1.0.0

test:
  # Python imports
  imports:
    - mozpoolclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/mozpoolclient/
  license: MPL
  summary: 'It allows you to interact with devices managed by Mozpool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
