package:
  name: dbutils
  version: "1.1"

source:
  fn: DBUtils-1.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DBUtils/DBUtils-1.1.tar.gz
  md5: aa13d60db0377234c5f9469212da5022
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbutils = dbutils:main
    #
    # Would create an entry point called dbutils that calls dbutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - DBUtils
    - DBUtils.Examples
    - DBUtils.Tests
    - dbutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.webwareforpython.org/DBUtils
  license: Open Software License
  summary: 'Database connections for multi-threaded environments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
