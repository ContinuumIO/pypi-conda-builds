package:
  name: interactivecommandline
  version: "0.3.0"

source:
  fn: InteractiveCommandLine-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/I/InteractiveCommandLine/InteractiveCommandLine-0.3.0.tar.gz
  md5: e23afdea862c9acb751bb6c4e3a194f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - interactivecommandline = interactivecommandline:main
    #
    # Would create an entry point called interactivecommandline that calls interactivecommandline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - recursivedocument

  run:
    - python
    - recursivedocument

test:
  # Python imports
  imports:
    - InteractiveCommandLine
    - InteractiveCommandLine.Foundations
    - InteractiveCommandLine.tests
    - interactivecommandline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/InteractiveCommandLine
  license: MIT License
  summary: 'Framework for interactive and command-line programs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
