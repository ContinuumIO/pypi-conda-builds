package:
  name: jinja2-commentif
  version: "0.1"

source:
  fn: jinja2-commentif-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja2-commentif/jinja2-commentif-0.1.tar.gz
  md5: 7929c25ec6556552e769b2eb1bfd2935
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja2-commentif = jinja2-commentif:main
    #
    # Would create an entry point called jinja2-commentif that calls jinja2-commentif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.4
    - pygments >=1.5

  run:
    - python
    - jinja2 >=2.4
    - pygments >=1.5

test:
  # Python imports
  imports:
    - jinja2_commentif

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zehome/jinja2-commentif
  license: BSD License
  summary: 'Jinja2 extension to automatically add a comment prefix on each line'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
