package:
  name: appomatic
  version: "0.0.30"

source:
  fn: appomatic-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic/appomatic-0.0.30.tar.gz
  md5: 0daec2e9c6a345d146db2698eb849b2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic = appomatic:main
    #
    # Would create an entry point called appomatic that calls appomatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appomatic_appadmin ==0.0.30
    - appomaticcore ==0.0.30

  run:
    - python
    - appomatic_appadmin ==0.0.30
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/redhog/appomatic
  license: GPL
  summary: 'Appomatic is a userfriendly Django environment with automatic plugin (app) management based on pip.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
