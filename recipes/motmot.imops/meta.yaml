package:
  name: motmot.imops
  version: "0.5.7"

source:
  fn: motmot.imops-0.5.7.tar.gz
  url: https://pypi.python.org/packages/source/m/motmot.imops/motmot.imops-0.5.7.tar.gz
  md5: 5085dd96827a68fd5a9bd0b10d2afe68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motmot.imops = motmot.imops:main
    #
    # Would create an entry point called motmot.imops that calls motmot.imops.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - motmot
    - motmot.imops

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.astraw.com/projects/motmot/imops.html
  license: BSD
  summary: 'image format conversion (e.g. between MONO8, YUV422, and RGB)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
