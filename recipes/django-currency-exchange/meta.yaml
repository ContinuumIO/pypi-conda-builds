package:
  name: django-currency-exchange
  version: "0.2.0"

source:
  fn: django-currency-exchange-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-currency-exchange/django-currency-exchange-0.2.0.tar.gz
  md5: cdc729b67d9b4fccf9a30f50dc7429e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-currency-exchange = django-currency-exchange:main
    #
    # Would create an entry point called django-currency-exchange that calls django-currency-exchange.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - requests
    - py-moneyed

  run:
    - python
    - django
    - requests
    - py-moneyed

test:
  # Python imports
  imports:
    - django_currency_exchange

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gizmag/django-currency-exchange
  license: UNKNOWN
  summary: 'Django Foreign Exchange'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
