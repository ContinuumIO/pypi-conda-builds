package:
  name: django-backbone
  version: "0.3.2"

source:
  fn: django-backbone-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-backbone/django-backbone-0.3.2.tar.gz
  md5: 8f522bf92e6712dce701b4bbc0a8455f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-backbone = django-backbone:main
    #
    # Would create an entry point called django-backbone that calls django-backbone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - backbone
    - backbone.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zmathew/django-backbone
  license: BSD License
  summary: 'Provides a Backbone.js compatible REST API for your models using Django Admin style registration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
