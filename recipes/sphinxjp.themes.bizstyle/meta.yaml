package:
  name: sphinxjp.themes.bizstyle
  version: "0.1.6"

source:
  fn: sphinxjp.themes.bizstyle-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.themes.bizstyle/sphinxjp.themes.bizstyle-0.1.6.tar.gz
  md5: 381136d2e33e46227dc4b0c6b3a42f19
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.themes.bizstyle = sphinxjp.themes.bizstyle:main
    #
    # Would create an entry point called sphinxjp.themes.bizstyle that calls sphinxjp.themes.bizstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - docutils
    - sphinx

  run:
    - python
    - setuptools
    - setuptools
    - docutils
    - sphinx

test:
  # Python imports
  imports:
    - sphinxjp
    - sphinxjp.themes
    - sphinxjp.themes.bizstyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/shkumagai/sphinxjp.themes.bizstyle
  license: MIT License
  summary: 'A sphinx theme for Business style Documentation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
