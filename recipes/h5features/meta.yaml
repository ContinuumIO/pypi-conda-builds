package:
  name: h5features
  version: "0.1.2"

source:
  fn: h5features-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/h5features/h5features-0.1.2.tar.gz
  md5: 1f4cbb1ee521853345abaeac1aba5bbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - h5features = h5features:main
    #
    # Would create an entry point called h5features that calls h5features.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - python >=2.7
    - h5py >=2.3.0
    - numpy >=1.8.0
    - scipy >=0.13.0

  run:
    - python
    - python >=2.7
    - h5py >=2.3.0
    - numpy >=1.8.0
    - scipy >=0.13.0

test:
  # Python imports
  imports:
    - h5features

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/h5features/
  license: licence/LICENSE.txt
  summary: 'h5features format file handler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
