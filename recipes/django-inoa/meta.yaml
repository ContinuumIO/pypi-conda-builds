package:
  name: django-inoa
  version: "0.5.23"

source:
  fn: django-inoa-0.5.23.zip
  url: https://pypi.python.org/packages/source/d/django-inoa/django-inoa-0.5.23.zip
  md5: a4beb98ecee850288c0153b8e85b229f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inoa = django-inoa:main
    #
    # Would create an entry point called django-inoa that calls django-inoa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - inoa
    - inoa.admin
    - inoa.backends
    - inoa.forms
    - inoa.http
    - inoa.management
    - inoa.management.commands
    - inoa.middleware
    - inoa.models
    - inoa.models.fields
    - inoa.settings
    - inoa.tastypie
    - inoa.templatetags
    - inoa.utils
    - inoa.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/inoa/django-inoa
  license: BSD License
  summary: 'Collection of django tools and snippets commonly used by Inoa.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
