package:
  name: celery-schedulers
  version: "0.0.2"

source:
  fn: celery-schedulers-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/celery-schedulers/celery-schedulers-0.0.2.tar.gz
  md5: 68d1e426c886d2a942c489dca0631fed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celery-schedulers = celery-schedulers:main
    #
    # Would create an entry point called celery-schedulers that calls celery-schedulers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - celery_schedulers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bits.btubbs.com/celery-schedulers
  license: UNKNOWN
  summary: 'Celery scheduler backends for Redis and MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
