package:
  name: django-sslify
  version: "0.2.7"

source:
  fn: django-sslify-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sslify/django-sslify-0.2.7.tar.gz
  md5: aba428b5d1a23fa62beb50b4ee7732af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sslify = django-sslify:main
    #
    # Would create an entry point called django-sslify that calls django-sslify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - sslify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/django-sslify
  license: UNLICENSE
  summary: 'Force SSL on your Django site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
