package:
  name: megrok.yui
  version: "2.5.2.1"

source:
  fn: megrok.yui-2.5.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.yui/megrok.yui-2.5.2.1.tar.gz
  md5: f8a5b021eddca0ececf7e58adf7c9b30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.yui = megrok.yui:main
    #
    # Would create an entry point called megrok.yui that calls megrok.yui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - megrok.resourcelibrary

  run:
    - python
    - setuptools
    - setuptools
    - megrok.resourcelibrary

test:
  # Python imports
  imports:
    - megrok
    - megrok.yui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'YUI library packaged as a Grok resource library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
