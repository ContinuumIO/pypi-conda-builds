package:
  name: itunes-iap
  version: "0.6.6"

source:
  fn: itunes-iap-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/i/itunes-iap/itunes-iap-0.6.6.tar.gz
  md5: 619e3fd32ead8918109aa632fddea092
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - itunes-iap = itunes-iap:main
    #
    # Would create an entry point called itunes-iap that calls itunes-iap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - prettyexc >=0.5.1
    - six

  run:
    - python
    - requests
    - prettyexc >=0.5.1
    - six

test:
  # Python imports
  imports:
    - itunesiap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youknowone/itunes-iap
  license: UNKNOWN
  summary: 'Itunes In-app purchase verification api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
