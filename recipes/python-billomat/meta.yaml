package:
  name: python-billomat
  version: "0.3.8"

source:
  fn: python-billomat-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/p/python-billomat/python-billomat-0.3.8.tar.gz
  md5: fce3f62fa2a9d3e9a617b96355399019
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-billomat = python-billomat:main
    #
    # Would create an entry point called python-billomat that calls python-billomat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bunch
    - urllib3

  run:
    - python
    - bunch
    - urllib3

test:
  # Python imports
  imports:
    - pybillomat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gerold-penz/python-billomat
  license: GNU Library or Lesser General Public License (LGPL) or MIT License
  summary: 'Python Billomat API Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
