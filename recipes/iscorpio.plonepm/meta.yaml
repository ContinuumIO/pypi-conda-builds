package:
  name: iscorpio.plonepm
  version: "2.0beta2-r382"

source:
  fn: iscorpio.plonepm-2.0beta2-r382.tar.gz
  url: https://pypi.python.org/packages/source/i/iscorpio.plonepm/iscorpio.plonepm-2.0beta2-r382.tar.gz
  md5: 2e450d60690cb1f7094bd22d66282b2c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iscorpio.plonepm = iscorpio.plonepm:main
    #
    # Would create an entry point called iscorpio.plonepm that calls iscorpio.plonepm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - iscorpio.themes.redmaple

  run:
    - python
    - setuptools
    - setuptools
    - iscorpio.themes.redmaple

test:
  # Python imports
  imports:
    - iscorpio
    - iscorpio.plonepm
    - iscorpio.plonepm.browser
    - iscorpio.plonepm.content
    - iscorpio.plonepm.interfaces
    - iscorpio.plonepm.portlets
    - iscorpio.plonepm.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plonexp.leocorn.com/plonepm
  license: GNU General Public License (GPL)
  summary: 'A Plone way to manage project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
