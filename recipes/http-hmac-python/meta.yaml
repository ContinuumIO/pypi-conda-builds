package:
  name: http-hmac-python
  version: "1.0.0"

source:
  fn: http-hmac-python-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/http-hmac-python/http-hmac-python-1.0.0.tar.gz
  md5: a1b8c87835716f09f1cd1af5796a3090
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - http-hmac-python = http-hmac-python:main
    #
    # Would create an entry point called http-hmac-python that calls http-hmac-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - httphmac

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/baliame/http-hmac-python
  license: MIT License
  summary: 'An implementation of the Acquia HTTP HMAC Spec (https://github.com/acquia/http-hmac-spec) in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
