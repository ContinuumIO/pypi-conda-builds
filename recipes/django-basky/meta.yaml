package:
  name: django-basky
  version: "0.9.9"

source:
  fn: django-basky-0.9.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-basky/django-basky-0.9.9.tar.gz
  md5: 70c8b7e234660ce39f17c7f339dc2257
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-basky = django-basky:main
    #
    # Would create an entry point called django-basky that calls django-basky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.10

  run:
    - python
    - django >=1.4.10

test:
  # Python imports
  imports:
    - basky
    - basky.management
    - basky.management.commands
    - basky.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-basky.readthedocs.org
  license: BSD 3 Clause
  summary: 'A extensible basket for your django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
