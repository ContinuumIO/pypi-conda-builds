package:
  name: atreal.richfile.qualifier
  version: "1.1.2"

source:
  fn: atreal.richfile.qualifier-1.1.2.zip
  url: https://pypi.python.org/packages/source/a/atreal.richfile.qualifier/atreal.richfile.qualifier-1.1.2.zip
  md5: c20db85972e5fb1777017df5441d34d7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.richfile.qualifier = atreal.richfile.qualifier:main
    #
    # Would create an entry point called atreal.richfile.qualifier that calls atreal.richfile.qualifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - atreal.filestorage.common
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - atreal.filestorage.common
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - atreal
    - atreal.richfile
    - atreal.richfile.qualifier
    - atreal.richfile.qualifier.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/atreal.richfile.qualifier/
  license: GPL
  summary: 'System to enrich a file content type in Plone with a marker interface regarding its mimetype.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
