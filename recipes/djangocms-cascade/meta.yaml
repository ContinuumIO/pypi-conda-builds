package:
  name: djangocms-cascade
  version: "0.5.0"

source:
  fn: djangocms-cascade-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-cascade/djangocms-cascade-0.5.0.tar.gz
  md5: e482b195e600a8cb81aacba0e75bdbf1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-cascade = djangocms-cascade:main
    #
    # Would create an entry point called djangocms-cascade that calls djangocms-cascade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonfield

  run:
    - python
    - jsonfield

test:
  # Python imports
  imports:
    - cmsplugin_cascade
    - cmsplugin_cascade.bootstrap3
    - cmsplugin_cascade.extra_fields
    - cmsplugin_cascade.gs960
    - cmsplugin_cascade.link
    - cmsplugin_cascade.migrations
    - cmsplugin_cascade.segmentation
    - cmsplugin_cascade.sharable
    - cmsplugin_cascade.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jrief/djangocms-cascade
  license: MIT License
  summary: 'Collection of plugins for DjangoCMS >3.0 to add various HTML elements from CSS frameworks to any CMS placeholder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
