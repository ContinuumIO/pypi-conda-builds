package:
  name: django-react
  version: "0.11.1"

source:
  fn: django-react-0.11.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-react/django-react-0.11.1.tar.gz
  md5: 066f3132cb845e62ae724bdfdfca301f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-react = django-react:main
    #
    # Would create an entry point called django-react that calls django-react.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-node ==4.0.0
    - django-webpack ==3.1.0
    - optional-django ==0.2.1

  run:
    - python
    - django-node ==4.0.0
    - django-webpack ==3.1.0
    - optional-django ==0.2.1

test:
  # Python imports
  imports:
    - django_react
    - django_react.services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markfinger/django-react
  license: UNKNOWN
  summary: 'Render and bundle React components from a Django application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
