package:
  name: appomatic_django_filer
  version: "0.0.30"

source:
  fn: appomatic_django_filer-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_django_filer/appomatic_django_filer-0.0.30.tar.gz
  md5: 435ab73e7b757514a326a6211ca1d492
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_django_filer = appomatic_django_filer:main
    #
    # Would create an entry point called appomatic_django_filer that calls appomatic_django_filer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_filer
    - appomaticcore ==0.0.30

  run:
    - python
    - django_filer
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_django_filer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-filer.readthedocs.org
  license: GPL
  summary: '.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
