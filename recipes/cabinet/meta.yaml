package:
  name: cabinet
  version: "1.1.3"

source:
  fn: cabinet-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cabinet/cabinet-1.1.3.tar.gz
  md5: d5b12b2c30b058724aae26f6d9b50af8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cabinet = cabinet:main
    #
    # Would create an entry point called cabinet that calls cabinet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cabinet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bickfordb/tokyo/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Tokyo Cabinet IDL compatible bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
