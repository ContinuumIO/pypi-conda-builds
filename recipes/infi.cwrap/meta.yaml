package:
  name: infi.cwrap
  version: "0.2.9"

source:
  fn: infi.cwrap-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.cwrap/infi.cwrap-0.2.9.tar.gz
  md5: e1168f9443025eac62f2286cdda39d7e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.cwrap = infi.cwrap:main
    #
    # Would create an entry point called infi.cwrap that calls infi.cwrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.cwrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.cwrap
  license: Python Software Foundation License
  summary: 'easy function wrapping for ctypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
