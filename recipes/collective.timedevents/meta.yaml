package:
  name: collective.timedevents
  version: "0.3"

source:
  fn: collective.timedevents-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.timedevents/collective.timedevents-0.3.zip
  md5: 8a3c4ca719785b2e435c978314290828
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.timedevents = collective.timedevents:main
    #
    # Would create an entry point called collective.timedevents that calls collective.timedevents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.app.session

  run:
    - python
    - setuptools
    - setuptools
    - zope.app.session

test:
  # Python imports
  imports:
    - collective
    - collective.timedevents

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective-timedevents
  license: GPL
  summary: 'Plone/Zope time based event mechanism'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
