package:
  name: exifrenamer
  version: "0.3.1"

source:
  fn: exifrenamer-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/e/exifrenamer/exifrenamer-0.3.1.tar.gz
  md5: fa2e1f11ec1364e5428e75612ae387d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exifrenamer = exifrenamer:main
    #
    # Would create an entry point called exifrenamer that calls exifrenamer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse >=1.0.0
    - exifread >=1.4.1

  run:
    - python
    - argparse >=1.0.0
    - exifread >=1.4.1

test:
  # Python imports
  imports:
    - exifrenamer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wting/exifrenamer
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Organize photos based on timestamp metadata.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
