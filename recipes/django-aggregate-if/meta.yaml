package:
  name: django-aggregate-if
  version: "0.5"

source:
  fn: django-aggregate-if-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aggregate-if/django-aggregate-if-0.5.tar.gz
  md5: 88f8c8874a1c73748a9fce1e232ff94f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aggregate-if = django-aggregate-if:main
    #
    # Would create an entry point called django-aggregate-if that calls django-aggregate-if.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.6.1

  run:
    - python
    - six >=1.6.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/henriquebastos/django-aggregate-if/
  license: MIT License
  summary: 'Conditional aggregates for Django, just like the famous SumIf in Excel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
