package:
  name: django-post_office
  version: "1.1.1"

source:
  fn: django-post_office-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-post_office/django-post_office-1.1.1.tar.gz
  md5: 850f473b5d6b7f3f76a40b73078ce806
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-post_office = django-post_office:main
    #
    # Would create an entry point called django-post_office that calls django-post_office.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - jsonfield

  run:
    - python
    - django >=1.4
    - jsonfield

test:
  # Python imports
  imports:
    - post_office

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ui/django-post_office
  license: MIT License
  summary: 'A Django app to monitor and send mail asynchronously, complete with template support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
