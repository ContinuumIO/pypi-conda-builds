package:
  name: django-preflight
  version: "0.1.5"

source:
  fn: django-preflight-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-preflight/django-preflight-0.1.5.tar.gz
  md5: 542765e9efa51b22ca7df1326c9fb54a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-preflight = django-preflight:main
    #
    # Would create an entry point called django-preflight that calls django-preflight.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - preflight
    - preflight.management
    - preflight.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-preflight
  license: GNU Affero General Public License v3
  summary: 'Create a page for making sure all settings are correct.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
