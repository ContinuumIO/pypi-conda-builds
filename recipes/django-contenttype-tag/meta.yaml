package:
  name: django-contenttype-tag
  version: "1.0.1"

source:
  fn: django-contenttype-tag-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-contenttype-tag/django-contenttype-tag-1.0.1.tar.gz
  md5: 2ac20267c7bb7b3c2712f9716ca64f17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contenttype-tag = django-contenttype-tag:main
    #
    # Would create an entry point called django-contenttype-tag that calls django-contenttype-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - generic_ct_tag

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/DrMartiner/django-contenttype-tag
  license: MIT License
  summary: 'Easy application for get content type at template'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
