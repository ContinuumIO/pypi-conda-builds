package:
  name: lookover
  version: "0.1dev"

source:
  fn: lookOver-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/l/lookOver/lookOver-0.1dev.tar.gz
  md5: ceeeed808dd10f293b2163f74e8227e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lookover = lookover:main
    #
    # Would create an entry point called lookover that calls lookover.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rpi.gpio
    - picamera
    - colorama

  run:
    - python
    - rpi.gpio
    - picamera
    - colorama

test:
  # Python imports
  imports:
    - lookOver
    - lookover

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ricco386/lookOver/
  license: BSD License
  summary: 'Raspberry Pi application for recording pictures/videos with PiCamera when motion is detected by PIR sensor.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
