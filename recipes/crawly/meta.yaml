package:
  name: crawly
  version: "0.1b"

source:
  fn: crawly-0.1b.tar.gz
  url: https://pypi.python.org/packages/source/c/crawly/crawly-0.1b.tar.gz
  md5: 4a3ff1b915af23c0a3e007dc7de69e0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crawly = crawly:main
    #
    # Would create an entry point called crawly that calls crawly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent ==0.13.7
    - requests ==0.14
    - lxml ==3.0.1

  run:
    - python
    - gevent ==0.13.7
    - requests ==0.14
    - lxml ==3.0.1

test:
  # Python imports
  imports:
    - crawly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mouad/crawly
  license: BSD License
  summary: 'A micro python crawler/scraper library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
