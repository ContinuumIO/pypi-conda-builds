package:
  name: avery
  version: "0.1.0"

source:
  fn: Avery-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/A/Avery/Avery-0.1.0.tar.gz
  md5: 12b3ec51ae05dbe27676e4c660d78a55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - avery = avery:main
    #
    # Would create an entry point called avery that calls avery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython >=2.2
    - ipython[notebook] >=2.2

  run:
    - python
    - ipython >=2.2
    - ipython[notebook] >=2.2

test:
  # Python imports
  imports:
    - Avery
    - Avery.interaction
    - avery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/Avery
  license: UNKNOWN
  summary: 'Miscellaneous packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
