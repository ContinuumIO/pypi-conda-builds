package:
  name: canopener
  version: "0.1.6"

source:
  fn: canopener-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/canopener/canopener-0.1.6.tar.gz
  md5: 5dc5e9e1ebeb6a72ab5c7b31049eae60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - canopener = canopener:main
    #
    # Would create an entry point called canopener that calls canopener.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - pystaticconfiguration

  run:
    - python
    - boto
    - pystaticconfiguration

test:
  # Python imports
  imports:
    - canopener

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/selassid/canopener
  license: canopy
  summary: u'Python convenience function for opening compressed URLs and files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
