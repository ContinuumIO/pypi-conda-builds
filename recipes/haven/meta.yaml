package:
  name: haven
  version: "1.1.76"

source:
  fn: haven-1.1.76.tar.gz
  url: https://pypi.python.org/packages/source/h/haven/haven-1.1.76.tar.gz
  md5: dce498573ac7f59914bc2f2f209837bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haven = haven:main
    #
    # Would create an entry point called haven that calls haven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - events
    - netkit

  run:
    - python
    - events
    - netkit

test:
  # Python imports
  imports:
    - haven
    - haven.contrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/haven
  license: MIT
  summary: "flask's style binary server framework"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
