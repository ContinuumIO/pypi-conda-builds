package:
  name: django-fsm-admin
  version: "1.2.1"

source:
  fn: django-fsm-admin-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fsm-admin/django-fsm-admin-1.2.1.tar.gz
  md5: dd90bbf466a60e6dd54d5ae06b13e2e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fsm-admin = django-fsm-admin:main
    #
    # Would create an entry point called django-fsm-admin that calls django-fsm-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-fsm >=2.1.0

  run:
    - python
    - django >=1.6
    - django-fsm >=2.1.0

test:
  # Python imports
  imports:
    - fsm_admin
    - fsm_admin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gadventures/django-fsm-admin
  license: MIT License
  summary: 'Integrate django-fsm state transitions into the django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
