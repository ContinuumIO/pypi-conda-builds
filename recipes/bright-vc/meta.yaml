package:
  name: bright-vc
  version: "1.1.0"

source:
  fn: bright-vc-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bright-vc/bright-vc-1.1.0.tar.gz
  md5: a59e76403c243eee72cbec06c38bdffd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bright-vc = bright-vc:main
    #
    # Would create an entry point called bright-vc that calls bright-vc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bright_vc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brightinteractive/bright-vc/
  license: BSD
  summary: 'Bright Interactive Version Control'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
