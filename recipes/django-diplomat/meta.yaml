package:
  name: django-diplomat
  version: "0.2.0"

source:
  fn: django-diplomat-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-diplomat/django-diplomat-0.2.0.tar.gz
  md5: 2139176d66310f91fc0609b9d2fa0138
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-diplomat = django-diplomat:main
    #
    # Would create an entry point called django-diplomat that calls django-diplomat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-autoslug
    - pycountry

  run:
    - python
    - django-autoslug
    - pycountry

test:
  # Python imports
  imports:
    - diplomat
    - diplomat.management
    - diplomat.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/oberlin/django-diplomat/
  license: BSD License
  summary: 'Language and country models for Django derived from the pycountry module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
