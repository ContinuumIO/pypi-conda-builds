package:
  name: blocked-table
  version: "1.1"

source:
  fn: blocked-table-1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/blocked-table/blocked-table-1.1.tar.gz
  md5: d29aba3a6f474bf750cc0dbeccad5ada
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blocked-table = blocked-table:main
    #
    # Would create an entry point called blocked-table that calls blocked-table.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rjpower.org/browse.cgi/blocked-table
  license: BSD License
  summary: 'Efficient on-disk sorted table format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
