package:
  name: mopidy-local-sqlite
  version: "0.10.0"

source:
  fn: Mopidy-Local-SQLite-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Local-SQLite/Mopidy-Local-SQLite-0.10.0.tar.gz
  md5: 11b52f4a51a95b0e365234ac1820f55b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-local-sqlite = mopidy-local-sqlite:main
    #
    # Would create an entry point called mopidy-local-sqlite that calls mopidy-local-sqlite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - uritools >=0.11

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - uritools >=0.11

test:
  # Python imports
  imports:
    - mopidy_local_sqlite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tkem/mopidy-local-sqlite
  license: Apache Software License
  summary: u'Mopidy SQLite local library extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
