package:
  name: djangorestframework-yaml
  version: "1.0.2"

source:
  fn: djangorestframework-yaml-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-yaml/djangorestframework-yaml-1.0.2.tar.gz
  md5: c3902ae1e2938a08d7a1bc83e3163647
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-yaml = djangorestframework-yaml:main
    #
    # Would create an entry point called djangorestframework-yaml that calls djangorestframework-yaml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.10

  run:
    - python
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - rest_framework_yaml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpadilla/django-rest-framework-yaml
  license: BSD License
  summary: 'YAML support for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
