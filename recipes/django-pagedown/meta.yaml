package:
  name: django-pagedown
  version: "0.1.0"

source:
  fn: django-pagedown-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pagedown/django-pagedown-0.1.0.tar.gz
  md5: 3a9a0fa4038eb72ea7c580dcc8c79630
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pagedown = django-pagedown:main
    #
    # Would create an entry point called django-pagedown that calls django-pagedown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - pagedown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timmyomahony/django-pagedown
  license: LICENSE.txt
  summary: "A django app that allows the easy addition of Stack Overflow's 'PageDown' markdown editor to a django form field"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
