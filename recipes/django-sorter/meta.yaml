package:
  name: django-sorter
  version: "0.2"

source:
  fn: django-sorter-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sorter/django-sorter-0.2.tar.gz
  md5: c975bf5864fb3d153bf6af327536901c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sorter = django-sorter:main
    #
    # Would create an entry point called django-sorter that calls django-sorter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf >=0.4
    - django-ttag >=2.3
    - urlobject >=2.0.1

  run:
    - python
    - django-appconf >=0.4
    - django-ttag >=2.3
    - urlobject >=2.0.1

test:
  # Python imports
  imports:
    - sorter
    - sorter.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-sorter.readthedocs.org/
  license: BSD License
  summary: 'A helper app for sorting objects in Django templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
