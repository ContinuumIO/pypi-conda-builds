package:
  name: django-twitterflux
  version: "0.0.3"

source:
  fn: django-twitterflux-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twitterflux/django-twitterflux-0.0.3.tar.gz
  md5: 29bb29c9d547ef296b2ed3a2fe6b8fe7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twitterflux = django-twitterflux:main
    #
    # Would create an entry point called django-twitterflux that calls django-twitterflux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-twitter >=0.8.7
    - django >=1.3
    - factory_boy ==1.3.0

  run:
    - python
    - python-twitter >=0.8.7
    - django >=1.3
    - factory_boy ==1.3.0

test:
  # Python imports
  imports:
    - twitterflux
    - twitterflux.management
    - twitterflux.management.commands
    - twitterflux.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sgarcez/django-twitter-flux/
  license: BSD
  summary: 'Retrieval and storage of recent tweets in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
