package:
  name: ewscal
  version: "0.0"

source:
  fn: ewscal-0.0.tar.gz
  url: https://pypi.python.org/packages/source/e/ewscal/ewscal-0.0.tar.gz
  md5: 4377f39b10404e1d18356db9b0f90e05
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ewscal = ewscal:main
    #
    # Would create an entry point called ewscal that calls ewscal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - pyramid_debugtoolbar
    - waitress
    - ewsclient
    - python-dateutil <2.0

  run:
    - python
    - setuptools
    - pyramid
    - pyramid_debugtoolbar
    - waitress
    - ewsclient
    - python-dateutil <2.0

test:
  # Python imports
  imports:
    - ewscal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Render Exchange calendars to SVG using d3.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
