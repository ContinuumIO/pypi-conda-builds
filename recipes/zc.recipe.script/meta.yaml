package:
  name: zc.recipe.script
  version: "1.0.2"

source:
  fn: zc.recipe.script-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.recipe.script/zc.recipe.script-1.0.2.tar.gz
  md5: 9c35cb84ac844ba545a02528552a5bdd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.recipe.script = zc.recipe.script:main
    #
    # Would create an entry point called zc.recipe.script that calls zc.recipe.script.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - zc
    - zc.recipe
    - zc.recipe.script

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.com/repos/main/zc.recipe.script/
  license: ZVSL
  summary: 'zc.buildout recipe for scripts in Unix deployments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
