package:
  name: damn-simple-jsonrpc-server
  version: "0.4.2"

source:
  fn: damn-simple-jsonrpc-server-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/damn-simple-jsonrpc-server/damn-simple-jsonrpc-server-0.4.2.tar.gz
  md5: 8abbd619545d82c324ebe6b3b443df77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - damn-simple-jsonrpc-server = damn-simple-jsonrpc-server:main
    #
    # Would create an entry point called damn-simple-jsonrpc-server that calls damn-simple-jsonrpc-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonrpcserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marcinn/json-rpc-server
  license: BSD License
  summary: 'Damn simple, framework-agnostic JSON-RPC server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
