package:
  name: findmodules
  version: "0.6"

source:
  fn: findmodules-0.6.tar.gz
  url: https://pypi.python.org/packages/source/f/findmodules/findmodules-0.6.tar.gz
  md5: a7c03aa7fed5d94c2ff3069a96c2e01d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - findmodules = findmodules:main
    #
    # Would create an entry point called findmodules that calls findmodules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - findmodules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benwbooth/python-findmodules
  license: MIT
  summary: 'Find project module directory relative to the running script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
