package:
  name: atomisator.outputs
  version: "0.1.2"

source:
  fn: atomisator.outputs-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/atomisator.outputs/atomisator.outputs-0.1.2.tar.gz
  md5: 7dfa842e985706dd904825c0dba32678
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomisator.outputs = atomisator.outputs:main
    #
    # Would create an entry point called atomisator.outputs that calls atomisator.outputs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - atomisator
    - atomisator.outputs
    - atomisator.outputs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atomisator.ziade.org
  license: GPL
  summary: 'Expert Python Programming - Outputs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
