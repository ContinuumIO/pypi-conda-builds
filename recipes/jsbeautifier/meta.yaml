package:
  name: jsbeautifier
  version: "1.5.5"

source:
  fn: jsbeautifier-1.5.5.tar.gz
  url: https://pypi.python.org/packages/source/j/jsbeautifier/jsbeautifier-1.5.5.tar.gz
  md5: 837cc804b62078758bfbd834836e0c5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsbeautifier = jsbeautifier:main
    #
    # Would create an entry point called jsbeautifier that calls jsbeautifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - six >=1.6.1

  run:
    - python
    - six >=1.6.1

test:
  # Python imports
  imports:
    - jsbeautifier
    - jsbeautifier.tests
    - jsbeautifier.unpackers
    - jsbeautifier.unpackers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jsbeautifier.org
  license: MIT
  summary: 'JavaScript unobfuscator and beautifier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
