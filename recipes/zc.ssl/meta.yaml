package:
  name: zc.ssl
  version: "1.2"

source:
  fn: zc.ssl-1.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.ssl/zc.ssl-1.2.tar.gz
  md5: c32cd32a6cb5f70b4cc18c38be488115
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.ssl = zc.ssl:main
    #
    # Would create an entry point called zc.ssl that calls zc.ssl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.testing

  run:
    - python
    - setuptools
    - zope.testing

test:
  # Python imports
  imports:
    - zc
    - zc.ssl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'An HTTPSConnection implementation with the new ssl module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
