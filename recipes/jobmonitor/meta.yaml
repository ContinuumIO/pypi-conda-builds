package:
  name: jobmonitor
  version: "0.0.2"

source:
  fn: jobmonitor-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jobmonitor/jobmonitor-0.0.2.tar.gz
  md5: cfa4b23198564660f9272512a245e5d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobmonitor = jobmonitor:main
    #
    # Would create an entry point called jobmonitor that calls jobmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - rq >=0.4.6
    - redis ==2.10.1

  run:
    - python
    - flask >=0.10.1
    - rq >=0.4.6
    - redis ==2.10.1

test:
  # Python imports
  imports:
    - jobmonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alexpearce/jobmonitor
  license: MIT License
  summary: 'Physics-orientated job monitoring over HTTP with Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
