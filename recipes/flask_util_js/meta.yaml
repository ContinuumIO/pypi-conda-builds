package:
  name: flask_util_js
  version: "0.2.25"

source:
  fn: flask_util_js-0.2.25.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_util_js/flask_util_js-0.2.25.tar.gz
  md5: edff7f07936888523234ab69293d023f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_util_js = flask_util_js:main
    #
    # Would create an entry point called flask_util_js that calls flask_util_js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_util_js

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/flask_util_js
  license: BSD
  summary: "flask's util in javascript. such as url_for etc."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
