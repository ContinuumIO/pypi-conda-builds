package:
  name: jsondate
  version: "0.1.2"

source:
  fn: jsondate-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsondate/jsondate-0.1.2.tar.gz
  md5: 74ea0b9da2a9a900bc35001dd40e4581
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsondate = jsondate:main
    #
    # Would create an entry point called jsondate that calls jsondate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsondate
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rconradharris/jsondate
  license: MIT License
  summary: 'JSON with datetime support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
