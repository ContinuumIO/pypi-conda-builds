package:
  name: cobertura-clover-transform
  version: "1.1.2"

source:
  fn: cobertura-clover-transform-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cobertura-clover-transform/cobertura-clover-transform-1.1.2.tar.gz
  md5: 753455202e53e2b3a07b11c12706f715
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cobertura-clover-transform = cobertura-clover-transform:main
    #
    # Would create an entry point called cobertura-clover-transform that calls cobertura-clover-transform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - cobertura_clover_transform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cwacek/cobertura-clover-transform
  license: MIT
  summary: 'Tools for transforming Cobertura test coverage XML into Clover-style XML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
