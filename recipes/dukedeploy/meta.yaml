package:
  name: dukedeploy
  version: "0.1.3"

source:
  fn: dukedeploy-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dukedeploy/dukedeploy-0.1.3.tar.gz
  md5: 71464cfc5cc3c0f4893405ecaff41b3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dukedeploy = dukedeploy:main
    #
    # Would create an entry point called dukedeploy that calls dukedeploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dukedeploy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dukeanalytics.com/products/dukedeployPython.php
  license: MIT License
  summary: 'Predictive model deployment with Duke Analytics.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
