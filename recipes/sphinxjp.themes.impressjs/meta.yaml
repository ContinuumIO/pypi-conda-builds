package:
  name: sphinxjp.themes.impressjs
  version: "0.1.3"

source:
  fn: sphinxjp.themes.impressjs-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.themes.impressjs/sphinxjp.themes.impressjs-0.1.3.tar.gz
  md5: c6c1090d71337e645e9a70e2dbfa4841
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.themes.impressjs = sphinxjp.themes.impressjs:main
    #
    # Would create an entry point called sphinxjp.themes.impressjs that calls sphinxjp.themes.impressjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - docutils
    - sphinx

  run:
    - python
    - setuptools
    - setuptools
    - docutils
    - sphinx

test:
  # Python imports
  imports:
    - sphinxjp
    - sphinxjp.themes
    - sphinxjp.themes.impressjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shkumagai/sphinxjp.themes.impressjs
  license: MIT License
  summary: 'A sphinx theme for HTML presentation style.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
