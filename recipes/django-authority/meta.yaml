package:
  name: django-authority
  version: "0.8"

source:
  fn: django-authority-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-authority/django-authority-0.8.tar.gz
  md5: 32bd5c2bac9f7b9c6f5ebc1b434b96c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-authority = django-authority:main
    #
    # Would create an entry point called django-authority that calls django-authority.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - authority
    - authority.templatetags
    - example
    - example.exampleapp
    - example.users

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jezdez/django-authority/
  license: BSD License
  summary: "A Django app that provides generic per-object-permissions for Django's auth app."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
