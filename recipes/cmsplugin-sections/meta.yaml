package:
  name: cmsplugin-sections
  version: "0.1.1"

source:
  fn: cmsplugin-sections-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-sections/cmsplugin-sections-0.1.1.tar.gz
  md5: 237d8264dd88f26868c43f9314827cbd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-sections = cmsplugin-sections:main
    #
    # Would create an entry point called cmsplugin-sections that calls cmsplugin-sections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.0
    - django-cms >=3.0.6

  run:
    - python
    - django >=1.7.0
    - django-cms >=3.0.6

test:
  # Python imports
  imports:
    - cmsplugin_sections
    - cmsplugin_sections.migrations
    - cmsplugin_sections.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mkoistinen/cmsplugin-sections
  license: BSD License
  summary: 'Plugins to aide creating \xe2\x80\x9csingle page scroller\xe2\x80\x9d websites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
