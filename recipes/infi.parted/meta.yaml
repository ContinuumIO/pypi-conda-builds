package:
  name: infi.parted
  version: "0.2.9"

source:
  fn: infi.parted-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.parted/infi.parted-0.2.9.tar.gz
  md5: fe1b08fe23de9cdaa4a994c0ac2a33ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.parted = infi.parted:main
    #
    # Would create an entry point called infi.parted that calls infi.parted.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - capacity
    - infi.exceptools
    - infi.execute
    - infi.pyutils
    - setuptools

  run:
    - python
    - capacity
    - infi.exceptools
    - infi.execute
    - infi.pyutils
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.parted
    - infi.parted.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.parted
  license: Python Software Foundation License
  summary: 'Python library for adding/removing disk partitions in linux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
