package:
  name: hachoir-parser
  version: "1.3.4"

source:
  fn: hachoir-parser-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hachoir-parser/hachoir-parser-1.3.4.tar.gz
  md5: e578b779c4b30d94ca4e775747877755
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hachoir-parser = hachoir-parser:main
    #
    # Would create an entry point called hachoir-parser that calls hachoir-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hachoir_parser
    - hachoir_parser.archive
    - hachoir_parser.audio
    - hachoir_parser.common
    - hachoir_parser.container
    - hachoir_parser.file_system
    - hachoir_parser.game
    - hachoir_parser.image
    - hachoir_parser.misc
    - hachoir_parser.network
    - hachoir_parser.program
    - hachoir_parser.video

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/haypo/hachoir/wiki/hachoir-parser
  license: GNU General Public License (GPL)
  summary: 'Package of Hachoir parsers used to open binary files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
