package:
  name: djangocms-twitter
  version: "0.0.2"

source:
  fn: djangocms-twitter-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-twitter/djangocms-twitter-0.0.2.tar.gz
  md5: a31bc23a3f5836dded0449ded34e929c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-twitter = djangocms-twitter:main
    #
    # Would create an entry point called djangocms-twitter that calls djangocms-twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0

  run:
    - python
    - django-cms >=3.0

test:
  # Python imports
  imports:
    - djangocms_twitter
    - djangocms_twitter.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/djangocms_twitter
  license: BSD License
  summary: 'Twitter plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
