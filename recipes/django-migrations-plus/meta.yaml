package:
  name: django-migrations-plus
  version: "0.1.1"

source:
  fn: django-migrations-plus-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-migrations-plus/django-migrations-plus-0.1.1.tar.gz
  md5: 057d91ae9e2dd99f370c9317cd584483
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-migrations-plus = django-migrations-plus:main
    #
    # Would create an entry point called django-migrations-plus that calls django-migrations-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - migrations_plus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Provides a method to run raw SQL in migrations with multiple databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
