package:
  name: django-intercom-io
  version: "0.4.1"

source:
  fn: django-intercom-io-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-intercom-io/django-intercom-io-0.4.1.tar.gz
  md5: 917fb11b6286641e4f3c69b68d85c17d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-intercom-io = django-intercom-io:main
    #
    # Would create an entry point called django-intercom-io that calls django-intercom-io.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - intercom
    - intercom.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eldarion/django-intercom-io
  license: BSD License
  summary: 'a pluggable app for adding intercom.io to your website'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
