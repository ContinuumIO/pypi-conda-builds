package:
  name: blog.policy
  version: "0.1b8"

source:
  fn: blog.policy-0.1b8.zip
  url: https://pypi.python.org/packages/source/b/blog.policy/blog.policy-0.1b8.zip
  md5: 9f3457ef49aadb0d53bb1416ac89b9ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blog.policy = blog.policy:main
    #
    # Would create an entry point called blog.policy that calls blog.policy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.api
    - setuptools
    - blog.post
    - sc.social.like
    - collective.quickupload
    - collective.contentrules.yearmonth
    - plonetheme.bootstrap
    - plone.formwidget.captcha
    - collective.atomrss

  run:
    - python
    - setuptools
    - plone.api
    - setuptools
    - blog.post
    - sc.social.like
    - collective.quickupload
    - collective.contentrules.yearmonth
    - plonetheme.bootstrap
    - plone.formwidget.captcha
    - collective.atomrss

test:
  # Python imports
  imports:
    - blog
    - blog.policy
    - blog.policy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IMIO/blog.policy
  license: GPL
  summary: 'A policy for blog suite'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
