package:
  name: flask-statsd
  version: "0.1.1"

source:
  fn: Flask-StatsD-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-StatsD/Flask-StatsD-0.1.1.tar.gz
  md5: c9d0d1e9c7c0d2e71900e98f2ec776d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-statsd = flask-statsd:main
    #
    # Would create an entry point called flask-statsd that calls flask-statsd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - statsd

  run:
    - python
    - flask
    - statsd

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberdelia/flask-statsd
  license: BSD License
  summary: 'Access to statsd in your app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
