package:
  name: hdf5pickle
  version: "0.3"

source:
  fn: hdf5pickle-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hdf5pickle/hdf5pickle-0.3.tar.gz
  md5: fd12c1a87cd947ab28e8f32dda95acba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hdf5pickle = hdf5pickle:main
    #
    # Would create an entry point called hdf5pickle that calls hdf5pickle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tables >=3
    - six

  run:
    - python
    - tables >=3
    - six

test:
  # Python imports
  imports:
    - hdf5pickle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rmcgibbo/hdf5pickle
  license: BSD & Python Software Foundation License
  summary: 'Pickle Python objects to HDF5 files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
