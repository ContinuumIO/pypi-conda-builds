package:
  name: flufl.enum
  version: "4.0.1"

source:
  fn: flufl.enum-4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flufl.enum/flufl.enum-4.0.1.tar.gz
  md5: b3ad23761a78232bc78499ced7cb85d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flufl.enum = flufl.enum:main
    #
    # Would create an entry point called flufl.enum that calls flufl.enum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flufl
    - flufl.enum
    - flufl.enum.docs
    - flufl.enum.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/flufl.enum
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'A Python enumeration package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
