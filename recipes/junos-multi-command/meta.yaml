package:
  name: junos-multi-command
  version: "1.0"

source:
  fn: junos-multi-command-1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/junos-multi-command/junos-multi-command-1.0.tar.gz
  md5: c8d7e3e5b04a5731ed5b1b1c4c4df547
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - junos-multi-command = junos-multi-command:main
    #
    # Would create an entry point called junos-multi-command that calls junos-multi-command.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyyaml >=3.10
    - ncclient >=0.1a

  run:
    - python
    - pyyaml >=3.10
    - ncclient >=0.1a

test:
  # Python imports
  imports:
    - junos-multi-command

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/junos-multi-command/
  license: LICENSE.txt
  summary: 'Run commands against multiple Juniper Junos OS devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
