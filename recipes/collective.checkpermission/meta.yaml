package:
  name: collective.checkpermission
  version: "1.0"

source:
  fn: collective.checkpermission-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.checkpermission/collective.checkpermission-1.0.tar.gz
  md5: 42381cd98b362a06dd678221601de7f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.checkpermission = collective.checkpermission:main
    #
    # Would create an entry point called collective.checkpermission that calls collective.checkpermission.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.checkpermission
    - collective.checkpermission.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://redomino.com
  license: GPL
  summary: 'Provides a decorator for protect methods of browser views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
