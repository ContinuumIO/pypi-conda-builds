package:
  name: logilab-constraint
  version: "0.5.0"

source:
  fn: logilab-constraint-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logilab-constraint/logilab-constraint-0.5.0.tar.gz
  md5: c6cb6d18460b01a9d5b0bdb1a73361f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logilab-constraint = logilab-constraint:main
    #
    # Would create an entry point called logilab-constraint that calls logilab-constraint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logilab.constraint
    - logilab.constraint.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.logilab.org/projects/constraint
  license: LGPL
  summary: 'constraints satisfaction solver in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
