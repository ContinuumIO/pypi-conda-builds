package:
  name: test_steps
  version: "0.6.2"

source:
  fn: test_steps-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/t/test_steps/test_steps-0.6.2.tar.gz
  md5: b4a9ada217169413126c33cbd9d6f6ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - test_steps = test_steps:main
    #
    # Would create an entry point called test_steps that calls test_steps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - test_steps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/steven004/TestSteps
  license: MIT License
  summary: 'Simple way to run test steps and automatic logging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
