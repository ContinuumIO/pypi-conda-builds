package:
  name: flask-classy
  version: "0.6.10"

source:
  fn: Flask-Classy-0.6.10.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Classy/Flask-Classy-0.6.10.tar.gz
  md5: 12d01eca775061d214101e73cb2e1b2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-classy = flask-classy:main
    #
    # Would create an entry point called flask-classy that calls flask-classy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9

  run:
    - python
    - flask >=0.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apiguy/flask-classy
  license: BSD License
  summary: 'Class based views for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
