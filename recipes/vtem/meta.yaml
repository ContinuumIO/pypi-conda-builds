package:
  name: vtem
  version: "0.1.1"

source:
  fn: 0.1.1.tar.gz
  url: https://github.com/bionet/vtem/archive/0.1.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vtem = vtem:main
    #
    # Would create an entry point called vtem that calls vtem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scikits.cuda >=0.042
    - pycuda >=2011.1
    - tables >=2.4.0
    - numexpr >=2.0.0
    - numpy >=1.6.0

  run:
    - python
    - scikits.cuda >=0.042
    - pycuda >=2011.1
    - tables >=2.4.0
    - numexpr >=2.0.0
    - numpy >=1.6.0

test:
  # Python imports
  imports:
    - vtem
    - vtem.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bionet/vtem/
  license: BSD License
  summary: 'Video Time Encoding and Decoding Machines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
