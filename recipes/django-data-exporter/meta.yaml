package:
  name: django-data-exporter
  version: "0.2"

source:
  fn: django-data-exporter-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-data-exporter/django-data-exporter-0.2.tar.gz
  md5: 789cec4eb6ddc056a627c1a5a38ddb0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-data-exporter = django-data-exporter:main
    #
    # Would create an entry point called django-data-exporter that calls django-data-exporter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - data_exporter
    - data_exporter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-data-exporter
  license: MIT License
  summary: 'Export asynchronously your data from your Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
