package:
  name: haufe.hrs.configuration
  version: "0.5.18"

source:
  fn: haufe.hrs.configuration-0.5.18.tar.gz
  url: https://pypi.python.org/packages/source/h/haufe.hrs.configuration/haufe.hrs.configuration-0.5.18.tar.gz
  md5: 3e3cd6bab33660b86a6ebc14b1039d12
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haufe.hrs.configuration = haufe.hrs.configuration:main
    #
    # Would create an entry point called haufe.hrs.configuration that calls haufe.hrs.configuration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.event
    - zope.interface
    - zope.component
    - zope.configuration
    - cfgparse

  run:
    - python
    - setuptools
    - setuptools
    - zope.event
    - zope.interface
    - zope.component
    - zope.configuration
    - cfgparse

test:
  # Python imports
  imports:
    - haufe
    - haufe.hrs
    - haufe.hrs.configuration
    - haufe.hrs.configuration.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Zope Public License
  summary: 'A central configuration service for Zope 2/3-based applications  based on a pseudo-hierarchical INI-file format with model support for defining the configuration schema'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
