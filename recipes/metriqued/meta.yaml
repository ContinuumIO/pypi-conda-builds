package:
  name: metriqued
  version: "0.2.7-1a"

source:
  fn: metriqued-0.2.7-1a.tar.gz
  url: https://pypi.python.org/packages/source/m/metriqued/metriqued-0.2.7-1a.tar.gz
  md5: 0863cb2de602a80d97809791e4befcc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metriqued = metriqued:main
    #
    # Would create an entry point called metriqued that calls metriqued.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - metriquet >=0.2.7
    - metriqueu >=0.2.7
    - gnupg ==1.2.5
    - passlib ==1.6.2
    - pymongo ==2.6.3

  run:
    - python
    - metriquet >=0.2.7
    - metriqueu >=0.2.7
    - gnupg ==1.2.5
    - passlib ==1.6.2
    - pymongo ==2.6.3

test:
  # Python imports
  imports:
    - metriqued

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drpoovilleorg/metrique
  license: GPLv3
  summary: 'Python/MongoDB Information Platform - Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
