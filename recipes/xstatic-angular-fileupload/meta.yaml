package:
  name: xstatic-angular-fileupload
  version: "1.4.0.1"

source:
  fn: XStatic-Angular-FileUpload-1.4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Angular-FileUpload/XStatic-Angular-FileUpload-1.4.0.1.tar.gz
  md5: bcae6f58da1d8270bcb6fe6b149591c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-angular-fileupload = xstatic-angular-fileupload:main
    #
    # Would create an entry point called xstatic-angular-fileupload that calls xstatic-angular-fileupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.angular_fileupload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danialfarid/angular-file-upload
  license: (same as Angular-FileUpload)
  summary: 'Angular-FileUpload 1.4.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
