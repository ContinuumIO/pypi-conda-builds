package:
  name: manicscript
  version: "0.1.2"

source:
  fn: manicscript-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/manicscript/manicscript-0.1.2.tar.gz
  md5: 5df24b2f9cab23cd98656c775c5ba2e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - manicscript = manicscript:main
    #
    # Would create an entry point called manicscript that calls manicscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stomper

  run:
    - python
    - stomper

test:
  # Python imports
  imports:
    - manicscript
    - manicscript.crypto
    - manicscript.djchat
    - manicscript.jsoncore
    - manicscript.kerby
    - manicscript.replay
    - manicscript.twisted

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/manicscript/
  license: UNKNOWN
  summary: 'Python and JavaScript libraries for event-driven interactive web applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
