package:
  name: ilmwetter
  version: "0.2"

source:
  fn: ilmwetter-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/ilmwetter/ilmwetter-0.2.tar.gz
  md5: 37df4f8a673a9a5ef1ac2746b450d71e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ilmwetter = ilmwetter:main
    #
    # Would create an entry point called ilmwetter that calls ilmwetter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scrapy

  run:
    - python
    - setuptools
    - scrapy

test:
  # Python imports
  imports:
    - ilmwetter
    - ilmwetter.spiders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mineo/ilmwetter
  license: MIT License
  summary: 'Scrapy spider for the weather in Ilmenau'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
