package:
  name: inqbus.collection.proxy
  version: "1.0b1"

source:
  fn: inqbus.collection.proxy-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/i/inqbus.collection.proxy/inqbus.collection.proxy-1.0b1.tar.gz
  md5: 67ff1c5b4edd916c8401450807a8b310
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inqbus.collection.proxy = inqbus.collection.proxy:main
    #
    # Would create an entry point called inqbus.collection.proxy that calls inqbus.collection.proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - inqbus
    - inqbus.collection
    - inqbus.collection.proxy
    - inqbus.collection.proxy.browser
    - inqbus.collection.proxy.content
    - inqbus.collection.proxy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Allow Plone Collections to proxy the content to not switch the context.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
