package:
  name: riakalchemy
  version: "0.1a6"

source:
  fn: riakalchemy-0.1a6.tar.gz
  url: https://pypi.python.org/packages/source/r/riakalchemy/riakalchemy-0.1a6.tar.gz
  md5: 1211a5ad4f9353ef3adb43665b4b6573
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - riakalchemy = riakalchemy:main
    #
    # Would create an entry point called riakalchemy that calls riakalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - riak

  run:
    - python
    - riak

test:
  # Python imports
  imports:
    - riakalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Linux2Go/riakalchemy
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Object Mapper for Riak'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
