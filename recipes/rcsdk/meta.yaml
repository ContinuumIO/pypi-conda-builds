package:
  name: rcsdk
  version: "0.3.0"

source:
  fn: rcsdk-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/r/rcsdk/rcsdk-0.3.0.tar.gz
  md5: 50cc486230de07c24bdd81d2d3763767
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rcsdk = rcsdk:main
    #
    # Would create an entry point called rcsdk that calls rcsdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pubnub >=3.7.0

  run:
    - python
    - pubnub >=3.7.0

test:
  # Python imports
  imports:
    - rcsdk
    - rcsdk.http
    - rcsdk.platform
    - rcsdk.subscription

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ringcentral/python-sdk
  license: UNKNOWN
  summary: 'RingPlatform Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
