package:
  name: helga-reviewboard
  version: "0.2.0"

source:
  fn: helga-reviewboard-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-reviewboard/helga-reviewboard-0.2.0.tar.gz
  md5: 6a0ce1522ba60ae6a46f94af109db7e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-reviewboard = helga-reviewboard:main
    #
    # Would create an entry point called helga-reviewboard that calls helga-reviewboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-reviewboard
  license: MIT License
  summary: 'A helga plugin for expanding shortcodes for code reviews on ReviewBoard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
