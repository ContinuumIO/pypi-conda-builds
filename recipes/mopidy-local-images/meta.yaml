package:
  name: mopidy-local-images
  version: "0.3.1"

source:
  fn: Mopidy-Local-Images-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Local-Images/Mopidy-Local-Images-0.3.1.tar.gz
  md5: b66318687e1aae468062e837b9ef6494
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-local-images = mopidy-local-images:main
    #
    # Would create an entry point called mopidy-local-images that calls mopidy-local-images.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19
    - uritools >=0.10

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19
    - uritools >=0.10

test:
  # Python imports
  imports:
    - mopidy_local_images

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tkem/mopidy-local-images
  license: Apache Software License
  summary: u'Mopidy local library proxy extension for handling embedded album art'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
