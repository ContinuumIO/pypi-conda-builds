package:
  name: distext
  version: "0.0.4"

source:
  fn: distext-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/distext/distext-0.0.4.tar.gz
  md5: 1e4321a60edb7661d57e4b687c32495e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distext = distext:main
    #
    # Would create an entry point called distext that calls distext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - distext
    - silveregg
    - silveregg.distext
    - silveregg.distext.command

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Silveregg distutils extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
