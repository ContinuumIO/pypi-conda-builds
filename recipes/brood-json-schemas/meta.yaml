package:
  name: brood-json-schemas
  version: "0.3.0"

source:
  fn: brood-json-schemas-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/brood-json-schemas/brood-json-schemas-0.3.0.tar.gz
  md5: 6b20ffff1b6abbfc96a91d8ac290cfe8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brood-json-schemas = brood-json-schemas:main
    #
    # Would create an entry point called brood-json-schemas that calls brood-json-schemas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brood_json_schemas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
