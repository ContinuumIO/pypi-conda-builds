package:
  name: collective.googleforms
  version: "1.0a1"

source:
  fn: collective.googleforms-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.googleforms/collective.googleforms-1.0a1.tar.gz
  md5: 7687c98985127045dfd812d5aaf5a39b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.googleforms = collective.googleforms:main
    #
    # Would create an entry point called collective.googleforms that calls collective.googleforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

test:
  # Python imports
  imports:
    - collective
    - collective.googleforms
    - collective.googleforms.browser
    - collective.googleforms.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Display google forms (spreadsheet) in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
