package:
  name: bottle-utils-csrf
  version: "0.3.2"

source:
  fn: bottle-utils-csrf-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-utils-csrf/bottle-utils-csrf-0.3.2.tar.gz
  md5: fd3db59f65b5e309ce80c018e5bc2396
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-utils-csrf = bottle-utils-csrf:main
    #
    # Would create an entry point called bottle-utils-csrf that calls bottle-utils-csrf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.12.7
    - bottle-utils-html >=0.3,<0.4
    - bottle-utils-common >=0.3,<0.4

  run:
    - python
    - bottle >=0.12.7
    - bottle-utils-html >=0.3,<0.4
    - bottle-utils-common >=0.3,<0.4

test:
  # Python imports
  imports:
    - bottle_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://outernet-project.github.io/bottle-utils/
  license: GNU General Public License v3 (GPLv3)
  summary: 'CSRF utilities for developing apps with Bottle web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
