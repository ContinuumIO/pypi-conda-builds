package:
  name: hurry.jqueryutils
  version: "0.8.5.1"

source:
  fn: hurry.jqueryutils-0.8.5.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.jqueryutils/hurry.jqueryutils-0.8.5.1.tar.gz
  md5: 3e76d9bf28f81549906aa054d8ba04d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.jqueryutils = hurry.jqueryutils:main
    #
    # Would create an entry point called hurry.jqueryutils that calls hurry.jqueryutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10
    - hurry.jquery

  run:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10
    - hurry.jquery

test:
  # Python imports
  imports:
    - hurry
    - hurry.jqueryutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hurry.jqueryutils
  license: MIT
  summary: 'Hurry resource for JQuery-utils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
