package:
  name: heroku-bouncer
  version: "1.3"

source:
  fn: heroku-bouncer-1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/heroku-bouncer/heroku-bouncer-1.3.tar.gz
  md5: c953fa06558e9b407242471fc524dc2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heroku-bouncer = heroku-bouncer:main
    #
    # Would create an entry point called heroku-bouncer that calls heroku-bouncer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2
    - wsgi-oauth2 >=0.1.3

  run:
    - python
    - requests >=1.2
    - wsgi-oauth2 >=0.1.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/heroku/heroku-bouncer-python
  license: BSD License
  summary: 'WSGI middleware that requires Heroku OAuth for all requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
