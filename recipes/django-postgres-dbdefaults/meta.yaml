package:
  name: django-postgres-dbdefaults
  version: "0.0.2"

source:
  fn: django-postgres-dbdefaults-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-postgres-dbdefaults/django-postgres-dbdefaults-0.0.2.tar.gz
  md5: 35a85424eeb1365071ac427fa1692078
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-postgres-dbdefaults = django-postgres-dbdefaults:main
    #
    # Would create an entry point called django-postgres-dbdefaults that calls django-postgres-dbdefaults.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - postgresql_psycopg2_dbdefaults

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ringly/django-postgres-dbdefaults
  license: MIT License
  summary: 'Maintain database defaults in sql migrations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
