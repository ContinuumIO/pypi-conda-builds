package:
  name: plone.jsonapi.routes
  version: "0.6"

source:
  fn: plone.jsonapi.routes-0.6.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.jsonapi.routes/plone.jsonapi.routes-0.6.tar.gz
  md5: 5cfc484bb8e1cdce3a0915b4ff626ca2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.jsonapi.routes = plone.jsonapi.routes:main
    #
    # Would create an entry point called plone.jsonapi.routes that calls plone.jsonapi.routes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.api
    - plone.jsonapi.core >=0.3

  run:
    - python
    - setuptools
    - setuptools
    - plone.api
    - plone.jsonapi.core >=0.3

test:
  # Python imports
  imports:
    - plone
    - plone.jsonapi
    - plone.jsonapi.routes
    - plone.jsonapi.routes.providers
    - plone.jsonapi.routes.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramonski/plone.jsonapi
  license: MIT
  summary: 'Plone JSON API -- Routes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
