package:
  name: django-notifyme-onsite
  version: "0.0.2a1"

source:
  fn: django-notifyme-onsite-0.0.2a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-notifyme-onsite/django-notifyme-onsite-0.0.2a1.tar.gz
  md5: 6c412f3f8f5330c77b34b078496a6717
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-notifyme-onsite = django-notifyme-onsite:main
    #
    # Would create an entry point called django-notifyme-onsite that calls django-notifyme-onsite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - notifyme_onsite
    - notifyme_onsite.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stefanfoulis/django-notifyme-onsite
  license: BSD License
  summary: 'An on site delivery backend for django-notifyme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
