package:
  name: django-object-attachments
  version: "0.1"

source:
  fn: django-object-attachments-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-object-attachments/django-object-attachments-0.1.tar.gz
  md5: cb0bf05a3a070a02c529e484b8b8634a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-object-attachments = django-object-attachments:main
    #
    # Would create an entry point called django-object-attachments that calls django-object-attachments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - object_attachments
    - object_attachments.migrations
    - object_attachments.tests
    - object_attachments.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-object-attachments
  license: The MIT License
  summary: 'Allows to add django-document-library objects to a any other object.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
