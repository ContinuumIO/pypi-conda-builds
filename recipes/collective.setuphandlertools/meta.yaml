package:
  name: collective.setuphandlertools
  version: "1.0b4"

source:
  fn: collective.setuphandlertools-1.0b4.zip
  url: https://pypi.python.org/packages/source/c/collective.setuphandlertools/collective.setuphandlertools-1.0b4.zip
  md5: e34ca8d8c6ee7b5a6f52a83e38d50ec7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.setuphandlertools = collective.setuphandlertools:main
    #
    # Would create an entry point called collective.setuphandlertools that calls collective.setuphandlertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone
    - products.atcontenttypes
    - products.cmfcore
    - products.portaltransforms

  run:
    - python
    - setuptools
    - plone
    - products.atcontenttypes
    - products.cmfcore
    - products.portaltransforms

test:
  # Python imports
  imports:
    - collective
    - collective.setuphandlertools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.setuphandlertools
  license: GPL
  summary: 'Tools for setting up a Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
