package:
  name: django-genericimports
  version: "0.1b6"

source:
  fn: django-genericimports-0.1b6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-genericimports/django-genericimports-0.1b6.tar.gz
  md5: c13d72dbbd262987f8c4c2aa2cb66783
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-genericimports = django-genericimports:main
    #
    # Would create an entry point called django-genericimports that calls django-genericimports.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.12
    - openpyxl >=2.1.4
    - python-dateutil >=1.5
    - django-rq >=0.7.0

  run:
    - python
    - django >=1.5.12
    - openpyxl >=2.1.4
    - python-dateutil >=1.5
    - django-rq >=0.7.0

test:
  # Python imports
  imports:
    - genericimports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/clione/django-genericimports
  license: BSD License
  summary: 'Import any CSV or XLS file regardless of data types, headers or foreignkeys.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
