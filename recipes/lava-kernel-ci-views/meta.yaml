package:
  name: lava-kernel-ci-views
  version: "0.4.1"

source:
  fn: lava-kernel-ci-views-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-kernel-ci-views/lava-kernel-ci-views-0.4.1.tar.gz
  md5: 9c815d68c6070960e30588f1845e8e0a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-kernel-ci-views = lava-kernel-ci-views:main
    #
    # Would create an entry point called lava-kernel-ci-views that calls lava-kernel-ci-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lava-server
    - versiontools >=1.8

  run:
    - python
    - setuptools
    - lava-server
    - versiontools >=1.8

test:
  # Python imports
  imports:
    - lava_kernel_ci_views_app
    - lava_kernel_ci_views_app.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: AGPL
  summary: 'LAVA Kernel CI Views Application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
