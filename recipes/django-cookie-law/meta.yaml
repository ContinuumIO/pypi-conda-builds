package:
  name: django-cookie-law
  version: "1.0.2"

source:
  fn: django-cookie-law-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cookie-law/django-cookie-law-1.0.2.tar.gz
  md5: 97ed59bd2518715f2b9290bea0143b7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cookie-law = django-cookie-law:main
    #
    # Would create an entry point called django-cookie-law that calls django-cookie-law.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-classy-tags >=0.3.0

  run:
    - python
    - django >=1.2
    - django-classy-tags >=0.3.0

test:
  # Python imports
  imports:
    - cookielaw
    - cookielaw.templatetags
    - cookielaw.test_project
    - cookielaw.test_project.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TyMaszWeb/django-cookie-law
  license: BSD License
  summary: 'Helps your Django project comply with EU cookie law regulations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
