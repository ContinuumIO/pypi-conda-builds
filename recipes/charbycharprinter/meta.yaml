package:
  name: charbycharprinter
  version: "1.1.0"

source:
  fn: CharByCharPrinter-1.1.0.zip
  url: https://pypi.python.org/packages/source/C/CharByCharPrinter/CharByCharPrinter-1.1.0.zip
  md5: 5bc140373e3c637e76a3b76e967a299b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - charbycharprinter = charbycharprinter:main
    #
    # Would create an entry point called charbycharprinter that calls charbycharprinter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - charbycharprinter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Prints a text char by char within milliseconds'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
