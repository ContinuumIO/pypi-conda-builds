package:
  name: products.colorfield
  version: "0.3.1"

source:
  fn: Products.ColorField-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.ColorField/Products.ColorField-0.3.1.tar.gz
  md5: e50c7a40aa07bab436b3e840e3ee9986
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.colorfield = products.colorfield:main
    #
    # Would create an entry point called products.colorfield that calls products.colorfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.ColorField
    - Products.ColorField.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.ColorField/
  license: GPL
  summary: 'ColorField is a color picker and converter for Plone Archetypes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
