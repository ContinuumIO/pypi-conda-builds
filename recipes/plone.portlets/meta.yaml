package:
  name: plone.portlets
  version: "2.2"

source:
  fn: plone.portlets-2.2.zip
  url: https://pypi.python.org/packages/source/p/plone.portlets/plone.portlets-2.2.zip
  md5: 5b7e06bee6e40af83694b82e1fee8c2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.portlets = plone.portlets:main
    #
    # Would create an entry point called plone.portlets that calls plone.portlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - plone.memoize
    - zope.annotation
    - zope.component
    - zope.container
    - zope.contentprovider
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.site

  run:
    - python
    - setuptools
    - zodb3
    - plone.memoize
    - zope.annotation
    - zope.component
    - zope.container
    - zope.contentprovider
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.site

test:
  # Python imports
  imports:
    - plone
    - plone.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.portlets
  license: GNU General Public License (GPL)
  summary: 'An extension of zope.viewlet to support dynamic portlets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
