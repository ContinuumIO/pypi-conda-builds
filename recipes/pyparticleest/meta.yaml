package:
  name: pyparticleest
  version: "1.0"

source:
  fn: pyParticleEst-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyParticleEst/pyParticleEst-1.0.tar.gz
  md5: 9ebe5592efb5a3b1c498a92c4e9a4fca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyparticleest = pyparticleest:main
    #
    # Would create an entry point called pyparticleest that calls pyparticleest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyparticleest
    - pyparticleest/models
    - pyparticleest/paramest
    - pyparticleest/utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.control.lth.se/Staff/JerkerNordh/pyparticleest.html
  license: LGPL
  summary: 'Framework for particle based estimation methods, such as particle filtering and smoothing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
