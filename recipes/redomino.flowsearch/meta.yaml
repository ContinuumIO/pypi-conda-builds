package:
  name: redomino.flowsearch
  version: "0.13"

source:
  fn: redomino.flowsearch-0.13.zip
  url: https://pypi.python.org/packages/source/r/redomino.flowsearch/redomino.flowsearch-0.13.zip
  md5: 7db67c75457631147bc489db79d92071
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.flowsearch = redomino.flowsearch:main
    #
    # Would create an entry point called redomino.flowsearch that calls redomino.flowsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - redomino.breadcrumbsbrain

  run:
    - python
    - setuptools
    - setuptools
    - redomino.breadcrumbsbrain

test:
  # Python imports
  imports:
    - redomino
    - redomino.flowsearch
    - redomino.flowsearch.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.flowsearch
  license: GPL
  summary: 'Redomino Flowsearch is a new plugin for Plone CMS that provides an advanced search view'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
