package:
  name: django-user_agent_detector
  version: "0.1dev"

source:
  fn: django-user_agent_detector-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user_agent_detector/django-user_agent_detector-0.1dev.tar.gz
  md5: e712cbd8c3f3552540d3b8e81ddd2f4a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user_agent_detector = django-user_agent_detector:main
    #
    # Would create an entry point called django-user_agent_detector that calls django-user_agent_detector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_user_agent_detector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/rwilcox/django-user_agent_detector
  license: BSD
  summary: 'Detect and handle desktop vs mobile user agents with django-annoying'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
