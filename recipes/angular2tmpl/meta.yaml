package:
  name: angular2tmpl
  version: "0.1.1"

source:
  fn: angular2tmpl-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/angular2tmpl/angular2tmpl-0.1.1.tar.gz
  md5: ad11b02c50bfdb0c745cc9e339af15a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - angular2tmpl = angular2tmpl:main
    #
    # Would create an entry point called angular2tmpl that calls angular2tmpl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html5lib >=1.0b3
    - jinja2 >=2.7.1

  run:
    - python
    - html5lib >=1.0b3
    - jinja2 >=2.7.1

test:
  # Python imports
  imports:
    - angular2tmpl
    - angular2tmpl.modules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/emosenkis/angular2tmpl
  license: MIT
  summary: 'Convert AngularJS templates to Jinja2 templates for SEO.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
