package:
  name: mpylayer
  version: "0.2a1"

source:
  fn: mpylayer-0.2a1.tar.gz
  url: https://pypi.python.org/packages/source/m/mpylayer/mpylayer-0.2a1.tar.gz
  md5: 5de9be5cc02dcea03dbf0ad941435cb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpylayer = mpylayer:main
    #
    # Would create an entry point called mpylayer that calls mpylayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mpylayer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/mpylayer/
  license: GNU General Public License (GPL)
  summary: 'Pythonic mplayer controller library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
