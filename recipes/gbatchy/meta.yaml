package:
  name: gbatchy
  version: "0.4.2"

source:
  fn: gbatchy-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gbatchy/gbatchy-0.4.2.tar.gz
  md5: 8bdb19f5889f184434f65759722e2801
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gbatchy = gbatchy:main
    #
    # Would create an entry point called gbatchy that calls gbatchy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=1.0.1
    - proxytypes >=0.9

  run:
    - python
    - gevent >=1.0.1
    - proxytypes >=0.9

test:
  # Python imports
  imports:
    - gbatchy
    - gbatchy.clients

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikekap/gbatchy
  license: UNKNOWN
  summary: 'gevent based batching framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
