package:
  name: openerp_proxy
  version: "0.5"

source:
  fn: openerp_proxy-0.5.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp_proxy/openerp_proxy-0.5.tar.gz
  md5: bab8be12ac912bfe7b74e1715ff581fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp_proxy = openerp_proxy:main
    #
    # Would create an entry point called openerp_proxy that calls openerp_proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - extend_me >=1.1.1

  run:
    - python
    - extend_me >=1.1.1

test:
  # Python imports
  imports:
    - openerp_proxy
    - openerp_proxy.connection
    - openerp_proxy.ext
    - openerp_proxy.orm
    - openerp_proxy.plugins
    - openerp_proxy.service

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/katyukha/openerp-proxy
  license: GNU General Public License (GPL)
  summary: 'Odoo/OpenERP CLI interface and library for RPC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
