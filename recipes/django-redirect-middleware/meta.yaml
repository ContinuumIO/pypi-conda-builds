package:
  name: django-redirect-middleware
  version: "0.0.1"

source:
  fn: django-redirect-middleware-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redirect-middleware/django-redirect-middleware-0.0.1.tar.gz
  md5: f5c6706bf219eaf47d36e6f8e3020ccc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redirect-middleware = django-redirect-middleware:main
    #
    # Would create an entry point called django-redirect-middleware that calls django-redirect-middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - redirects

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Remake of standart Django redirect middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
