package:
  name: python-hue
  version: "0.1.2"

source:
  fn: python-hue-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hue/python-hue-0.1.2.tar.gz
  md5: 730d253bf4a430779d27ebaba24e7f42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hue = python-hue:main
    #
    # Would create an entry point called python-hue that calls python-hue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - colorpy
    - numpy

  run:
    - python
    - requests
    - colorpy
    - numpy

test:
  # Python imports
  imports:
    - hue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/issackelly/python-hue
  license: MIT
  summary: 'Python client for Philips Hue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
