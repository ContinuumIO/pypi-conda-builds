package:
  name: baler
  version: "1.0.4"

source:
  fn: baler-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/baler/baler-1.0.4.tar.gz
  md5: f423442221b7bdfecc807293cf00d0a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baler = baler:main
    #
    # Would create an entry point called baler that calls baler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.6
    - argparse >=1.1
    - biplist >=0.5
    - six >=1.2.0
    - wsgiref >=0.1.2

  run:
    - python
    - jinja2 >=2.6
    - argparse >=1.1
    - biplist >=0.5
    - six >=1.2.0
    - wsgiref >=0.1.2

test:
  # Python imports
  imports:
    - baler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paypal/baler
  license: balloon
  summary: 'Bundle assets into iOS static libraries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
