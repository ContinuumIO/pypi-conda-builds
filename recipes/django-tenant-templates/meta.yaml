package:
  name: django-tenant-templates
  version: "0.5"

source:
  fn: django-tenant-templates-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tenant-templates/django-tenant-templates-0.5.tar.gz
  md5: 2a265f5d4d31a7cc7d5e9ec4692708f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tenant-templates = django-tenant-templates:main
    #
    # Would create an entry point called django-tenant-templates that calls django-tenant-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_tenant_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/grampajoe/django-tenant-templates
  license: MIT
  summary: 'Tenant-aware templating for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
