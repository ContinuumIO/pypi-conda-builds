package:
  name: mtfileutil
  version: "0.6.2"

source:
  fn: mtFileUtil-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mtFileUtil/mtFileUtil-0.6.2.tar.gz
  md5: b12a99e3a49a53b10da800ddda83eba5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mtfileutil = mtfileutil:main
    #
    # Would create an entry point called mtfileutil that calls mtfileutil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mtfileutil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/mtFileUtil/
  license: LICENSE.txt
  summary: 'High-performance tools for reading data from text files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
