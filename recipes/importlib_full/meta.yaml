package:
  name: importlib_full
  version: "0.0.1"

source:
  fn: importlib_full-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/importlib_full/importlib_full-0.0.1.tar.gz
  md5: 59ffd2f31fd4d65074d42aaac47c217a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importlib_full = importlib_full:main
    #
    # Would create an entry point called importlib_full that calls importlib_full.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - importlib_full
    - importlib_full.test
    - importlib_full.test.import_

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/importlib_full
  license: Python Software Foundation License
  summary: "Backport of Brett Cannon's full importlib module."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
