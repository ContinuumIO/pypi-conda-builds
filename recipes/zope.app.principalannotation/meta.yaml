package:
  name: zope.app.principalannotation
  version: "3.7.1"

source:
  fn: zope.app.principalannotation-3.7.1.zip
  url: https://pypi.python.org/packages/source/z/zope.app.principalannotation/zope.app.principalannotation-3.7.1.zip
  md5: 56d3658d441bc7f91c92e5769722b2a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.principalannotation = zope.app.principalannotation:main
    #
    # Would create an entry point called zope.app.principalannotation that calls zope.app.principalannotation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - transaction
    - zope.app.appsetup >=3.11.0
    - zope.processlifetime
    - zope.principalannotation

  run:
    - python
    - setuptools
    - transaction
    - zope.app.appsetup >=3.11.0
    - zope.processlifetime
    - zope.principalannotation

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.principalannotation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.principalannotation
  license: Zope Public License
  summary: 'Bootstrap subscriber and browser menu item for zope.principalannotation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
