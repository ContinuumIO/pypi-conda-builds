package:
  name: collective.skinny
  version: "0.6"

source:
  fn: collective.skinny-0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.skinny/collective.skinny-0.6.tar.gz
  md5: c267231115bba7fac9089cf3e08164a7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.skinny = collective.skinny:main
    #
    # Would create an entry point called collective.skinny that calls collective.skinny.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup
    - plone.postpublicationhook

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup
    - plone.postpublicationhook

test:
  # Python imports
  imports:
    - collective
    - collective.skinny

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.skinny
  license: LGPL
  summary: 'Plone theming for mortals: A simple example to get you started quickly with your own public skin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
