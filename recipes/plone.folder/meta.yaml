package:
  name: plone.folder
  version: "1.0.5"

source:
  fn: plone.folder-1.0.5.zip
  url: https://pypi.python.org/packages/source/p/plone.folder/plone.folder-1.0.5.zip
  md5: 2722124a411e2d267b08d6f4d81e262c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.folder = plone.folder:main
    #
    # Would create an entry point called plone.folder that calls plone.folder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.memoize
    - zope.interface
    - zope.component
    - zope.annotation
    - zope.container

  run:
    - python
    - setuptools
    - plone.memoize
    - zope.interface
    - zope.component
    - zope.annotation
    - zope.container

test:
  # Python imports
  imports:
    - plone
    - plone.folder
    - plone.folder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.folder
  license: GNU General Public License (GPL)
  summary: 'BTree-based folder implementation with order support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
