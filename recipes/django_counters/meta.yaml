package:
  name: django_counters
  version: "0.2"

source:
  fn: django_counters-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_counters/django_counters-0.2.tar.gz
  md5: c409699b42ae821a67599d4ee8d4c948
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_counters = django_counters:main
    #
    # Would create an entry point called django_counters that calls django_counters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycounters >=0.6
    - django >=1.2,<1.6

  run:
    - python
    - pycounters >=0.6
    - django >=1.2,<1.6

test:
  # Python imports
  imports:
    - django_counters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: apache
  summary: 'A Django app adding PyCounters to Django and offering admin like interface for the monitored info.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
