package:
  name: django-generic-positions
  version: "0.1.7"

source:
  fn: django-generic-positions-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-positions/django-generic-positions-0.1.7.tar.gz
  md5: 5df6a539e8858ab4e0ba32bbb91fa7ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-positions = django-generic-positions:main
    #
    # Would create an entry point called django-generic-positions that calls django-generic-positions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - generic_positions
    - generic_positions.migrations
    - generic_positions.templatetags
    - generic_positions.tests
    - generic_positions.tests.integration_tests
    - generic_positions.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-generic-positions
  license: The MIT License
  summary: 'Generic app for using a drag & drop position field, wherever you want to.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
