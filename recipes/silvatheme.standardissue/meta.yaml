package:
  name: silvatheme.standardissue
  version: "1.5"

source:
  fn: silvatheme.standardissue-1.5.tar.gz
  url: https://pypi.python.org/packages/source/s/silvatheme.standardissue/silvatheme.standardissue-1.5.tar.gz
  md5: 32fa5b78cb352df3936b1a16bdfa70bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silvatheme.standardissue = silvatheme.standardissue:main
    #
    # Would create an entry point called silvatheme.standardissue that calls silvatheme.standardissue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grokcore.chameleon
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.layout
    - zeam.form.silva
    - zope.cachedescriptors
    - zope.traversing

  run:
    - python
    - setuptools
    - grokcore.chameleon
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.layout
    - zeam.form.silva
    - zope.cachedescriptors
    - zope.traversing

test:
  # Python imports
  imports:
    - silvatheme
    - silvatheme.standardissue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silvatheme.standardissue
  license: BSD License
  summary: 'Standard Issue theme for Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
