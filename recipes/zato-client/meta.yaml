package:
  name: zato-client
  version: "2.0.3.5"

source:
  fn: zato-client-2.0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/z/zato-client/zato-client-2.0.3.5.tar.gz
  md5: 862eb61ef36e68a8d016dec4f727ec4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zato-client = zato-client:main
    #
    # Would create an entry point called zato-client that calls zato-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - anyjson
    - bunch
    - lxml
    - mock
    - nose
    - requests
    - zato-common

  run:
    - python
    - anyjson
    - bunch
    - lxml
    - mock
    - nose
    - requests
    - zato-common

test:
  # Python imports
  imports:
    - zato
    - zato.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://zato.io
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Convenience Python client for Zato ESB and app server (https://zato.io)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
