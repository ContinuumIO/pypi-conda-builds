package:
  name: horae.datetime
  version: "1.0a1"

source:
  fn: horae.datetime-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.datetime/horae.datetime-1.0a1.tar.gz
  md5: 5bc7c7b2d1d3edf75cd7ee4e2d9ec450
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.datetime = horae.datetime:main
    #
    # Would create an entry point called horae.datetime that calls horae.datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - fanstatic
    - zope.fanstatic
    - js.jquery
    - js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - fanstatic
    - zope.fanstatic
    - js.jquery
    - js.jqueryui

test:
  # Python imports
  imports:
    - horae
    - horae.datetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'zope.formlib datetime widget used by Horae'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
