package:
  name: django-simple-links
  version: "0.1.1"

source:
  fn: django-simple-links-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-links/django-simple-links-0.1.1.tar.gz
  md5: a4d4f8fa4a3c60026facd38bd17c0f74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-links = django-simple-links:main
    #
    # Would create an entry point called django-simple-links that calls django-simple-links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simple_links
    - simple_links.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kplaube/django-simple-links
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Simple bookmarking application for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
