package:
  name: interlegis.recipe.sapl
  version: "1.0a4"

source:
  fn: interlegis.recipe.sapl-1.0a4.tar.gz
  url: https://pypi.python.org/packages/source/i/interlegis.recipe.sapl/interlegis.recipe.sapl-1.0a4.tar.gz
  md5: 8c1d7b922dff7293cd4ff523c14a8dfe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - interlegis.recipe.sapl = interlegis.recipe.sapl:main
    #
    # Would create an entry point called interlegis.recipe.sapl that calls interlegis.recipe.sapl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zope.globalrequest ==1.0

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zope.globalrequest ==1.0

test:
  # Python imports
  imports:
    - interlegis
    - interlegis.recipe
    - interlegis.recipe.sapl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/interlegis/interlegis.recipe.sapl
  license: Zope Public License
  summary: 'Recipe to create SAPL as part of a buildout run'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
