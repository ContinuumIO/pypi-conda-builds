package:
  name: collective.lead
  version: "1.0"

source:
  fn: collective.lead-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.lead/collective.lead-1.0.tar.gz
  md5: 141ff4e636dc6d7484faf7b3859e21c9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.lead = collective.lead:main
    #
    # Would create an entry point called collective.lead that calls collective.lead.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sqlalchemy >=0.3.10,<0.5dev

  run:
    - python
    - setuptools
    - setuptools
    - sqlalchemy >=0.3.10,<0.5dev

test:
  # Python imports
  imports:
    - collective
    - collective.lead

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.lead
  license: LGPL
  summary: 'SQLAlchemy/Zope2 transaction integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
