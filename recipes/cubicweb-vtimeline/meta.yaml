package:
  name: cubicweb-vtimeline
  version: "0.5.0"

source:
  fn: cubicweb-vtimeline-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-vtimeline/cubicweb-vtimeline-0.5.0.tar.gz
  md5: 19b8f01b26c2133050ee172670faccce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-vtimeline = cubicweb-vtimeline:main
    #
    # Would create an entry point called cubicweb-vtimeline that calls cubicweb-vtimeline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.15.6

  run:
    - python
    - cubicweb >=3.15.6

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-vtimeline
  license: LGPL
  summary: 'VeriteCo / TimelineJS cube'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
