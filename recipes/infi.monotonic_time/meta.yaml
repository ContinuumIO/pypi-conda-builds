package:
  name: infi.monotonic_time
  version: "0.1.5"

source:
  fn: infi.monotonic_time-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.monotonic_time/infi.monotonic_time-0.1.5.tar.gz
  md5: 2932e15b6025f58161904b9650018006
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.monotonic_time = infi.monotonic_time:main
    #
    # Would create an entry point called infi.monotonic_time that calls infi.monotonic_time.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.monotonic_time

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.monotonic_time
  license: Python Software Foundation License
  summary: 'monotonic time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
