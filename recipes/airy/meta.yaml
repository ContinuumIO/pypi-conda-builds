package:
  name: airy
  version: "0.1.3"

source:
  fn: airy-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/airy/airy-0.1.3.tar.gz
  md5: 59a3347f0dcf6c3f07c63e57f35fadcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - airy = airy:main
    #
    # Would create an entry point called airy that calls airy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - airy
    - airy.core
    - airy.core.files
    - airy.core.mail
    - airy.core.mail.backends
    - airy.forms
    - airy.forms.extras
    - airy.utils
    - airy.utils.simplejson
    - airy.utils.translation
    - airy.utils.unittest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://airy.letolab.com
  license: BSD License
  summary: 'Web Application Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
