package:
  name: openerp-point-of-sale
  version: "7.0.406"

source:
  fn: openerp-point-of-sale-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-point-of-sale/openerp-point-of-sale-7.0.406.tar.gz
  md5: 4f9cbd4761c11402de24819605d911b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-point-of-sale = openerp-point-of-sale:main
    #
    # Would create an entry point called openerp-point-of-sale that calls openerp-point-of-sale.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-sale-stock

  run:
    - python
    - openerp-sale-stock

test:
  # Python imports
  imports:
    - openerp.addons.point_of_sale
    - openerp.addons.point_of_sale.controllers
    - openerp.addons.point_of_sale.report
    - openerp.addons.point_of_sale.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Point of Sale'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
