package:
  name: cobertura2clover
  version: "0.1"

source:
  fn: cobertura2clover-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cobertura2clover/cobertura2clover-0.1.tar.gz
  md5: 76c64a6ca7283d04b480fdb1089cc2fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cobertura2clover = cobertura2clover:main
    #
    # Would create an entry point called cobertura2clover that calls cobertura2clover.main()

    - cobertura2clover=cobertura2clover.parser:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - lxml

  run:
    - python
    - setuptools
    - beautifulsoup4
    - lxml

test:
  # Python imports
  imports:
    - cobertura2clover

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - cobertura2clover --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'cobertura2clover'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
