package:
  name: django-tidings
  version: "1.1"

source:
  fn: django-tidings-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tidings/django-tidings-1.1.tar.gz
  md5: 26692f029dd91270a2a00fa60c1d33eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tidings = django-tidings:main
    #
    # Would create an entry point called django-tidings that calls django-tidings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - celery >=2.1.1

  run:
    - python
    - django
    - celery >=2.1.1

test:
  # Python imports
  imports:
    - tidings
    - tidings.migrations
    - tidings.south_migrations
    - tidings.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/erikrose/django-tidings
  license: BSD License
  summary: 'Framework for asynchronous email notifications from Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
