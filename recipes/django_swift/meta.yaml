package:
  name: django_swift
  version: "0.9.3"

source:
  fn: django_swift-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_swift/django_swift-0.9.3.tar.gz
  md5: 0891cb277db97922fa85efe677392862
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_swift = django_swift:main
    #
    # Would create an entry point called django_swift that calls django_swift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-swiftclient

  run:
    - python
    - python-swiftclient

test:
  # Python imports
  imports:
    - django_swift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/duanhongyi/django_swift/
  license: BSD License
  summary: 'a django storage for swift'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
