package:
  name: ymci-ext-mail-alerts
  version: "1.1.0"

source:
  fn: ymci-ext-mail-alerts-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/y/ymci-ext-mail-alerts/ymci-ext-mail-alerts-1.1.0.tar.gz
  md5: 8418a606f0e3de24c03921a36da390c2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ymci-ext-mail-alerts = ymci-ext-mail-alerts:main
    #
    # Would create an entry point called ymci-ext-mail-alerts that calls ymci-ext-mail-alerts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ymci_ext_mail_alerts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Mail alert plugin for ymci'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
