package:
  name: assertionchain
  version: "0.1.1"

source:
  fn: assertionchain-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/assertionchain/assertionchain-0.1.1.tar.gz
  md5: 4473fe687449299d4f78f32e0c4b8079
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - assertionchain = assertionchain:main
    #
    # Would create an entry point called assertionchain that calls assertionchain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - assertionchain
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/justiniso/assertionchain
  license: UNKNOWN
  summary: 'Utility for chaining commands and incrementally checking the results'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
