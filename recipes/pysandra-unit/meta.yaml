package:
  name: pysandra-unit
  version: "0.4.3"

source:
  fn: pysandra-unit-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pysandra-unit/pysandra-unit-0.4.3.tar.gz
  md5: da3c70c3f3dbcb9af7f5a587cbf6f513
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysandra-unit = pysandra-unit:main
    #
    # Would create an entry point called pysandra-unit that calls pysandra-unit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - pysandraunit
    - pysandraunit.django
    - pysandraunit.unittest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zemanta/pysandra-unit
  license: GPLv3
  summary: 'Python wrapper for cassandra-unit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
