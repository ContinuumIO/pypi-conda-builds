package:
  name: auto_getlogger
  version: "1.0"

source:
  fn: auto_getlogger-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/auto_getlogger/auto_getlogger-1.0.tar.gz
  md5: 0fc90ee6384be4c26e5068670414abf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auto_getlogger = auto_getlogger:main
    #
    # Would create an entry point called auto_getlogger that calls auto_getlogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - auto_getlogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Consistent category logging via automated calls to logging.getLogger()'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
