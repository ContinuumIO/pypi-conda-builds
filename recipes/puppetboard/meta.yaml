package:
  name: puppetboard
  version: "0.0.4"

source:
  fn: puppetboard-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/puppetboard/puppetboard-0.0.4.tar.gz
  md5: 441d98db4126f7cf5c7394c8d3cd90b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - puppetboard = puppetboard:main
    #
    # Would create an entry point called puppetboard that calls puppetboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - flask-wtf >=0.9.4
    - pypuppetdb >=0.1.0

  run:
    - python
    - flask >=0.10.1
    - flask-wtf >=0.9.4
    - pypuppetdb >=0.1.0

test:
  # Python imports
  imports:
    - puppetboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nedap/puppetboard
  license: Apache Software License
  summary: 'Web frontend for PuppetDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
