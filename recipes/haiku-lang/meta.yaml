package:
  name: haiku-lang
  version: "0.0.7"

source:
  fn: haiku-lang-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/h/haiku-lang/haiku-lang-0.0.7.tar.gz
  md5: 3722a86bc746583ec2b7a05cd7e144b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haiku-lang = haiku-lang:main
    #
    # Would create an entry point called haiku-lang that calls haiku-lang.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - lepl >=5.1.1
    - bitstring >=3.0.2
    - python-patterns >=0.0.1

  run:
    - python
    - lepl >=5.1.1
    - bitstring >=3.0.2
    - python-patterns >=0.0.1

test:
  # Python imports
  imports:
    - haiku
    - haiku.builtin
    - haiku.interpreter
    - haiku.pickle
    - haiku.types
    - haiku.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/monetizeio/haiku-lang/
  license: UNKNOWN
  summary: 'An embedable LISP implemented on top of the Python interpreter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
