package:
  name: inirama
  version: "0.8.0"

source:
  fn: Inirama-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/I/Inirama/Inirama-0.8.0.tar.gz
  md5: dccfe5977ea98ba20dbbabb9482fa08c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inirama = inirama:main
    #
    # Would create an entry point called inirama that calls inirama.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - inirama

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home:  http://github.com/klen/inirama
  license: BSD License
  summary: 'Inirama -- Simplest parser for INI files. Interpolation supported.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
