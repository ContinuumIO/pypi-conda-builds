package:
  name: django-oauth-backend
  version: "0.2.3"

source:
  fn: django-oauth-backend-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauth-backend/django-oauth-backend-0.2.3.tar.gz
  md5: 6e275d127ed7cabc86a3e6e8381a09ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauth-backend = django-oauth-backend:main
    #
    # Would create an entry point called django-oauth-backend that calls django-oauth-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oauth_backend
    - oauth_backend.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: AGPLv3
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
