package:
  name: repoze.zcml
  version: "1.0b1"

source:
  fn: repoze.zcml-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/r/repoze.zcml/repoze.zcml-1.0b1.tar.gz
  md5: 666f8d0335062550a15fb20fdcf0c718
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - repoze.zcml = repoze.zcml:main
    #
    # Would create an entry point called repoze.zcml that calls repoze.zcml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.component >=3.5.0
    - zope.configuration

  run:
    - python
    - setuptools
    - setuptools
    - zope.component >=3.5.0
    - zope.configuration

test:
  # Python imports
  imports:
    - repoze
    - repoze.zcml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.repoze.org
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Core directives for use in ZCML-based applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
