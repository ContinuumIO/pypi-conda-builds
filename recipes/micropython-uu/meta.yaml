package:
  name: micropython-uu
  version: "0.5"

source:
  fn: micropython-uu-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-uu/micropython-uu-0.5.tar.gz
  md5: e00547931f1f6aa0afb35a4f0d9413a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-uu = micropython-uu:main
    #
    # Would create an entry point called micropython-uu that calls micropython-uu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - micropython-binascii
    - micropython-os

  run:
    - python
    - micropython-binascii
    - micropython-os

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: Python
  summary: 'CPython uu module ported to MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
