package:
  name: gloss.policy
  version: "0.5.1"

source:
  fn: gloss.policy-0.5.1.zip
  url: https://pypi.python.org/packages/source/g/gloss.policy/gloss.policy-0.5.1.zip
  md5: fad800277eb50ba857a1d082d3b4da57
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gloss.policy = gloss.policy:main
    #
    # Would create an entry point called gloss.policy that calls gloss.policy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - gloss.theme

  run:
    - python
    - setuptools
    - setuptools
    - gloss.theme

test:
  # Python imports
  imports:
    - gloss
    - gloss.policy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Gloss Installer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
