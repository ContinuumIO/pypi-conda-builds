package:
  name: gasp
  version: "0.3.3"

source:
  fn: gasp-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gasp/gasp-0.3.3.tar.gz
  md5: e4e72d596a869646443c2084cc8d755d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gasp = gasp:main
    #
    # Would create an entry point called gasp that calls gasp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycairo >=1.4
    - pygobject
    - pygtk >=2.0

  run:
    - python
    - setuptools
    - pycairo >=1.4
    - pygobject
    - pygtk >=2.0

test:
  # Python imports
  imports:
    - gasp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.laptop.org/go/GASP
  license: GNU General Public License (GPL)
  summary: 'GASP provides a simple, procedural graphics API for beginning students using Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
