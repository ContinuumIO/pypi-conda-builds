package:
  name: medialog.galleryviewlet
  version: "0.4.1"

source:
  fn: medialog.galleryviewlet-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.galleryviewlet/medialog.galleryviewlet-0.4.1.tar.gz
  md5: 557c40ce8674291d00e95ba7781fbc41
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.galleryviewlet = medialog.galleryviewlet:main
    #
    # Would create an entry point called medialog.galleryviewlet that calls medialog.galleryviewlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery
    - plone.api

test:
  # Python imports
  imports:
    - medialog
    - medialog.galleryviewlet
    - medialog.galleryviewlet.browser
    - medialog.galleryviewlet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/medialog.galleryviewlet
  license: gpl
  summary: 'Adds a viewlet to display a truegallery whereever you want'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
