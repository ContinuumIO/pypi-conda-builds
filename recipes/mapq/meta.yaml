package:
  name: mapq
  version: "0.3"

source:
  fn: mapq-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mapq/mapq-0.3.tar.gz
  md5: d3ff2705acd6cd75b3f43e0d240ebab0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mapq = mapq:main
    #
    # Would create an entry point called mapq that calls mapq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - simplejson

  run:
    - python
    - requests
    - simplejson

test:
  # Python imports
  imports:
    - mapq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zachwill/mapq
  license: MIT License
  summary: 'An easy-to-use Mapquest Geocoding API wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
