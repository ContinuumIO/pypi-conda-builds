package:
  name: fourdigits.recipe.stud
  version: "1.0.5"

source:
  fn: fourdigits.recipe.stud-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fourdigits.recipe.stud/fourdigits.recipe.stud-1.0.5.tar.gz
  md5: 77f1bb7abdf7ee8b0ac73cec642ea50e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fourdigits.recipe.stud = fourdigits.recipe.stud:main
    #
    # Would create an entry point called fourdigits.recipe.stud that calls fourdigits.recipe.stud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - fourdigits
    - fourdigits.recipe
    - fourdigits.recipe.stud
    - fourdigits.recipe.stud.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/fourdigits.recipe.stud
  license: GPL
  summary: 'Buildout recipe to install stud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
