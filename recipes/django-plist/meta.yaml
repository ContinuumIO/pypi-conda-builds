package:
  name: django-plist
  version: "0.6"

source:
  fn: django-plist-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-plist/django-plist-0.6.tar.gz
  md5: dfcc48823e80f4292c5bf61cf2aa2c52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-plist = django-plist:main
    #
    # Would create an entry point called django-plist that calls django-plist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_plist
    - django_plist.templatetags
    - django_plist.tests
    - django_plist.tests.testapp
    - django_plist.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.github.com/steingrd/django-plist
  license: BSD License
  summary: 'Django app for serializing objects into Cocoa Property List XML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
