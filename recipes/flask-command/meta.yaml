package:
  name: flask-command
  version: "0.0.3"

source:
  fn: flask-command-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-command/flask-command-0.0.3.tar.gz
  md5: ec70b38673064dd613e06e7b04487a71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-command = flask-command:main
    #
    # Would create an entry point called flask-command that calls flask-command.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gunicorn
    - flask

  run:
    - python
    - gunicorn
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ravenac95/flask-command
  license: MIT License
  summary: 'flask-command - Run you flask+gunicorn app as a command'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
