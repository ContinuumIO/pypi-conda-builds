package:
  name: csp_twisted
  version: "0.2.0"

source:
  fn: csp_twisted-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/csp_twisted/csp_twisted-0.2.0.tar.gz
  md5: 4812ae231a765998720b0a43282eb803
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csp_twisted = csp_twisted:main
    #
    # Would create an entry point called csp_twisted that calls csp_twisted.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - csp_twisted

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.orbited.org
  license: MIT License
  summary: 'An implemention of the Comet Session protocol specification for twisted: http://orbited.org/blog/files/cps.html'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
