package:
  name: django-principals
  version: "0.1.5"

source:
  fn: django-principals-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-principals/django-principals-0.1.5.tar.gz
  md5: fad498c218d068e1a515128966ae5c8a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-principals = django-principals:main
    #
    # Would create an entry point called django-principals that calls django-principals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - principals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mogga/django-principals
  license: BSD License
  summary: 'A Django model field for defining a principal instance.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
