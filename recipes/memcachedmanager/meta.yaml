package:
  name: memcachedmanager
  version: "0.2"

source:
  fn: MemcachedManager-0.2.tar.bz2
  url: https://pypi.python.org/packages/source/M/MemcachedManager/MemcachedManager-0.2.tar.bz2
  md5: d1db34e502804749c429e73308ccca99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memcachedmanager = memcachedmanager:main
    #
    # Would create an entry point called memcachedmanager that calls memcachedmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - MemcachedManager
    - MemcachedManager.Dialogs
    - MemcachedManager.Tabs
    - MemcachedManager.memcached
    - memcachedmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/memcached-manager
  license: UNKNOWN
  summary: 'Memcached Server Manager allows you to view stats about your Memcached cluster and Manage the data in that cluster and much more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
