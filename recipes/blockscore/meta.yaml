package:
  name: blockscore
  version: "4.1.0"

source:
  fn: blockscore-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/blockscore/blockscore-4.1.0.tar.gz
  md5: 985e7972fc09fab910172f3aa1d6a150
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockscore = blockscore:main
    #
    # Would create an entry point called blockscore that calls blockscore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0

  run:
    - python
    - requests >=2.1.0

test:
  # Python imports
  imports:
    - blockscore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://blockscore.com
  license: MIT License
  summary: 'Official BlockScore API library client for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
