package:
  name: collective.atimage.transformtoolbar
  version: "1.2.3"

source:
  fn: collective.atimage.transformtoolbar-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.atimage.transformtoolbar/collective.atimage.transformtoolbar-1.2.3.tar.gz
  md5: 0101c164bd5af725c62312df785cf117
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.atimage.transformtoolbar = collective.atimage.transformtoolbar:main
    #
    # Would create an entry point called collective.atimage.transformtoolbar that calls collective.atimage.transformtoolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - collective
    - collective.atimage
    - collective.atimage.transformtoolbar
    - collective.atimage.transformtoolbar.browser
    - collective.atimage.transformtoolbar.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.atimage.transformtoolbar
  license: GPL
  summary: "'Transforms' toolbar in Image and News Item to perform image transformations"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
