package:
  name: horae.js.jqplot
  version: "1.0.0b2"

source:
  fn: horae.js.jqplot-1.0.0b2.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.js.jqplot/horae.js.jqplot-1.0.0b2.tar.gz
  md5: 3a4e8b26577c0e8634887afae9369989
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.js.jqplot = horae.js.jqplot:main
    #
    # Would create an entry point called horae.js.jqplot that calls horae.js.jqplot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - fanstatic
    - js.jquery

  run:
    - python
    - setuptools
    - setuptools
    - fanstatic
    - js.jquery

test:
  # Python imports
  imports:
    - horae
    - horae.js
    - horae.js.jqplot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides the jqPlot jQuery plugin as fanstatic resources'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
