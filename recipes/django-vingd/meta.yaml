package:
  name: django-vingd
  version: "0.1.1"

source:
  fn: django-vingd-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vingd/django-vingd-0.1.1.tar.gz
  md5: ddd5cfb5b215b4b8da6f0989b27f4b1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vingd = django-vingd:main
    #
    # Would create an entry point called django-vingd that calls django-vingd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.2
    - vingd >=0.1.1

  run:
    - python
    - django >=1.4.2
    - vingd >=0.1.1

test:
  # Python imports
  imports:
    - django_vingd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vingd/django-vingd
  license: MIT License
  summary: 'Django App for Vingd integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
