package:
  name: collective.portlet.filtersearch
  version: "0.3"

source:
  fn: collective.portlet.filtersearch-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.filtersearch/collective.portlet.filtersearch-0.3.zip
  md5: 970feba37af8bb1b26da34bea51f8f95
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.filtersearch = collective.portlet.filtersearch:main
    #
    # Would create an entry point called collective.portlet.filtersearch that calls collective.portlet.filtersearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.filtersearch
    - collective.portlet.filtersearch.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.portlet.filtersearch
  license: GPL
  summary: 'Shows a definable filter portlet that allows to filter search results'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
