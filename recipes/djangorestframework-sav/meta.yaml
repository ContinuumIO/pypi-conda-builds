package:
  name: djangorestframework-sav
  version: "0.1.0"

source:
  fn: djangorestframework-sav-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-sav/djangorestframework-sav-0.1.0.tar.gz
  md5: 582daab2a5c65cc7bd91bc19065bc9cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-sav = djangorestframework-sav:main
    #
    # Would create an entry point called djangorestframework-sav that calls djangorestframework-sav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework >=3.0.5

  run:
    - python
    - djangorestframework >=3.0.5

test:
  # Python imports
  imports:
    - rest_framework_sav

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JamesRitchie/django-rest-framework-sav
  license: BSD License
  summary: 'Session Authentication View for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
