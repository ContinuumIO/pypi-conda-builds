package:
  name: slurchemy
  version: "0.2.6"

source:
  fn: slurchemy-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/s/slurchemy/slurchemy-0.2.6.tar.gz
  md5: d698ca71f7af8163f14c4d82bf0d9abe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slurchemy = slurchemy:main
    #
    # Would create an entry point called slurchemy that calls slurchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mysql-python
    - sqlalchemy
    - zope.sqlalchemy

  run:
    - python
    - setuptools
    - mysql-python
    - sqlalchemy
    - zope.sqlalchemy

test:
  # Python imports
  imports:
    - slurchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/slurchemy
  license: GNU General Public License (GPL)
  summary: 'SQLAlchemy bindings for your slurmdbd (SLURM database)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
