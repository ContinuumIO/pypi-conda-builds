package:
  name: django-mantis-stix-importer
  version: "0.2.0"

source:
  fn: django-mantis-stix-importer-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mantis-stix-importer/django-mantis-stix-importer-0.2.0.tar.gz
  md5: 0c0f2667bc6230bfbd049234db8c3baf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mantis-stix-importer = django-mantis-stix-importer:main
    #
    # Would create an entry point called django-mantis-stix-importer that calls django-mantis-stix-importer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.5
    - django-dingos >=0.1.0
    - django-mantis-core >=0.1.0
    - django-mantis-openioc-importer >=0.1.0

  run:
    - python
    - django >=1.5.5
    - django-dingos >=0.1.0
    - django-mantis-core >=0.1.0
    - django-mantis-openioc-importer >=0.1.0

test:
  # Python imports
  imports:
    - mantis_stix_importer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/siemens/django-mantis-stix-importer
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'A module implementing import of STIX and CybOX XML files for the Mantis Cyber Threat Intelligence Mgmt. Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
