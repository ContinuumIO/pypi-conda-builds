package:
  name: lapurge
  version: "0.2"

source:
  fn: lapurge-0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lapurge/lapurge-0.2.tar.gz
  md5: 32ce8507ac4d4338f2e65a05c5f467be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lapurge = lapurge:main
    #
    # Would create an entry point called lapurge that calls lapurge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lapurge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madmatah/lapurge
  license: MIT License
  summary: 'A tool to purge your backup directories according to your retention rules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
