package:
  name: bigml
  version: "4.1.2"

source:
  fn: bigml-4.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bigml/bigml-4.1.2.tar.gz
  md5: f667285dfae5515ab3a31197c07f6296
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigml = bigml:main
    #
    # Would create an entry point called bigml that calls bigml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - unidecode
    - poster

  run:
    - python
    - requests
    - unidecode
    - poster

test:
  # Python imports
  imports:
    - bigml
    - bigml.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bigml.com/developers
  license: Apache Software License
  summary: 'An open source binding to BigML.io, the public BigML API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
