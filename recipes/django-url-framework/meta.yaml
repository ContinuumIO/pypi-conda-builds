package:
  name: django-url-framework
  version: "0.3.2"

source:
  fn: django-url-framework-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-framework/django-url-framework-0.3.2.tar.gz
  md5: be6453d112cf96cfc4a7d702b3bdd58e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-framework = django-url-framework:main
    #
    # Would create an entry point called django-url-framework that calls django-url-framework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_url_framework
    - django_url_framework.decorators
    - django_url_framework.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zeraien/django-url-framework/
  license: MIT License
  summary: 'Automagically discover urls in a django application, similar to the Ruby on Rails Controller/Action/View implementation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
