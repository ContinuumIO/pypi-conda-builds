package:
  name: flask-xml-rpc
  version: "0.1.2"

source:
  fn: Flask-XML-RPC-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-XML-RPC/Flask-XML-RPC-0.1.2.tar.gz
  md5: fc1513156e82c792fcddd23bb802e766
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-xml-rpc = flask-xml-rpc:main
    #
    # Would create an entry point called flask-xml-rpc that calls flask-xml-rpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/leafstorm/flask-xml-rpc/
  license: MIT License
  summary: 'Adds support for creating XML-RPC APIs to Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
