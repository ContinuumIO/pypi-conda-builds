package:
  name: ajaxuploader
  version: "0.3.8"

source:
  fn: ajaxuploader-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/a/ajaxuploader/ajaxuploader-0.3.8.tar.gz
  md5: 4667e38b4f47053b6bfff30156423f02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajaxuploader = ajaxuploader:main
    #
    # Would create an entry point called ajaxuploader that calls ajaxuploader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajaxuploader
    - ajaxuploader.backends
    - ajaxuploader.tests
    - ajaxuploader.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GoodCloud/django-ajax-uploader
  license: UNKNOWN
  summary: 'AJAX file uploader for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
