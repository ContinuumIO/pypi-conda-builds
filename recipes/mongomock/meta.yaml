package:
  name: mongomock
  version: "2.1.0"

source:
  fn: mongomock-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mongomock/mongomock-2.1.0.tar.gz
  md5: bdfc07f606a70835b31b35294a898393
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongomock = mongomock:main
    #
    # Would create an entry point called mongomock that calls mongomock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sentinels
    - six

  run:
    - python
    - sentinels
    - six

test:
  # Python imports
  imports:
    - mongomock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fake pymongo stub for testing simple MongoDB-dependent code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
