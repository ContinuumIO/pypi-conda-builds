package:
  name: flask-mongoalchemy
  version: "0.7.1"

source:
  fn: Flask-MongoAlchemy-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MongoAlchemy/Flask-MongoAlchemy-0.7.1.tar.gz
  md5: 4fd695930f2e0fc7fea67f8b1492b6eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mongoalchemy = flask-mongoalchemy:main
    #
    # Would create an entry point called flask-mongoalchemy that calls flask-mongoalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - mongoalchemy >=0.15

  run:
    - python
    - flask >=0.9
    - mongoalchemy >=0.15

test:
  # Python imports
  imports:
    - flask_mongoalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cobrateam/flask-mongoalchemy
  license: BSD License
  summary: 'Add Flask support for MongoDB using MongoAlchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
