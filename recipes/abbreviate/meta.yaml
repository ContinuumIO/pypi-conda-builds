package:
  name: abbreviate
  version: "0.1.1"

source:
  fn: abbreviate-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/abbreviate/abbreviate-0.1.1.tar.gz
  md5: 51e922e02e19f983a5882c683832b13e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abbreviate = abbreviate:main
    #
    # Would create an entry point called abbreviate that calls abbreviate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyenchant

  run:
    - python
    - pyenchant

test:
  # Python imports
  imports:
    - abbreviate
    - abbreviate.resources
    - abbreviate.resources.abbreviations
    - abbreviate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ppannuto/python-abbreviate
  license: MIT License
  summary: 'Automatically abbreviate text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
