package:
  name: gottwall-storage-redis
  version: "0.0.1"

source:
  fn: gottwall-storage-redis-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gottwall-storage-redis/gottwall-storage-redis-0.0.1.tar.gz
  md5: 88c081ab0acef10d5e774d9e63f9d35a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gottwall-storage-redis = gottwall-storage-redis:main
    #
    # Would create an entry point called gottwall-storage-redis that calls gottwall-storage-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis ==2.9.0
    - tornado-redis ==2.4.15
    - gottwall ==0.5.0

  run:
    - python
    - redis ==2.9.0
    - tornado-redis ==2.4.15
    - gottwall ==0.5.0

test:
  # Python imports
  imports:
    - gw_storage_redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GottWall/gottwall-storage-redis
  license: BSD License
  summary: 'Redis storage for GottWall statistics aggregator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
