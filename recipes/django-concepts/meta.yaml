package:
  name: django-concepts
  version: "0.5"

source:
  fn: django-concepts-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-concepts/django-concepts-0.5.tar.gz
  md5: 01ae653d05df788c6772300d867f0602
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-concepts = django-concepts:main
    #
    # Would create an entry point called django-concepts that calls django-concepts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-taggit ==0.9.3

  run:
    - python
    - django-taggit ==0.9.3

test:
  # Python imports
  imports:
    - concepts
    - concepts.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/callowayproject
  license: Apache Software License
  summary: 'django-concepts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
