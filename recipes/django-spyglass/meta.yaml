package:
  name: django-spyglass
  version: "1.0"

source:
  fn: django-spyglass-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spyglass/django-spyglass-1.0.tar.gz
  md5: aef743492b49abbc9e9ada0754d4ab2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spyglass = django-spyglass:main
    #
    # Would create an entry point called django-spyglass that calls django-spyglass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - spyglass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/afein/django-spyglass.git
  license: UNKNOWN
  summary: 'Web content Monitoring Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
