package:
  name: antlr_python_runtime
  version: "3.1.3"

source:
  fn: antlr_python_runtime-3.1.3.tar.gz
  url: http://antlr3.org/download/Python/antlr_python_runtime-3.1.3.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - antlr_python_runtime = antlr_python_runtime:main
    #
    # Would create an entry point called antlr_python_runtime that calls antlr_python_runtime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - antlr3
    - antlr_python_runtime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.antlr.org/
  license: BSD
  summary: 'Runtime package for ANTLR3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
