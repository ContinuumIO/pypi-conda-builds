package:
  name: cubicweb-oauth
  version: "0.3.1"

source:
  fn: cubicweb-oauth-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-oauth/cubicweb-oauth-0.3.1.tar.gz
  md5: 3fa4f2731b3b36ad293026c49c5f2cb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-oauth = cubicweb-oauth:main
    #
    # Would create an entry point called cubicweb-oauth that calls cubicweb-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0
    - rauth >=0.6.1

  run:
    - python
    - cubicweb >=3.19.0
    - rauth >=0.6.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-oauth
  license: LGPL
  summary: 'External service (Facebook, Twitter, Google+, etc) Authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
