package:
  name: datacache
  version: "0.4.14"

source:
  fn: datacache-0.4.14.tar.gz
  url: https://pypi.python.org/packages/source/d/datacache/datacache-0.4.14.tar.gz
  md5: 5b567e3b716877fd55cf589409d5715c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datacache = datacache:main
    #
    # Would create an entry point called datacache that calls datacache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas >=0.15.2
    - appdirs >=1.4.0
    - progressbar33 >=2.4
    - biopython >=1.65
    - requests >=2.5.1
    - typechecks >=0.0.2

  run:
    - python
    - pandas >=0.15.2
    - appdirs >=1.4.0
    - progressbar33 >=2.4
    - biopython >=1.65
    - requests >=2.5.1
    - typechecks >=0.0.2

test:
  # Python imports
  imports:
    - datacache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hammerlab/datacache
  license: Apache Software License
  summary: 'Helpers for transparently downloading datasets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
