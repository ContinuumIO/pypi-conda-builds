package:
  name: django-easyoptions
  version: "0.1.0"

source:
  fn: django-easyoptions-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easyoptions/django-easyoptions-0.1.0.tar.gz
  md5: 048a0fcf7633118d2b41a2098f4d6e3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easyoptions = django-easyoptions:main
    #
    # Would create an entry point called django-easyoptions that calls django-easyoptions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - easyoptions
    - easyoptions.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-easyoptions
  license: UNKNOWN
  summary: 'Easily create extra Meta option classes on forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
