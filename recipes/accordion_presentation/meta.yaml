package:
  name: accordion_presentation
  version: "1.0.1"

source:
  fn: accordion_presentation-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/accordion_presentation/accordion_presentation-1.0.1.tar.gz
  md5: de38bb96b78c58e7e33a3053085071da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - accordion_presentation = accordion_presentation:main
    #
    # Would create an entry point called accordion_presentation that calls accordion_presentation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-paintstore

  run:
    - python
    - django
    - django-paintstore

test:
  # Python imports
  imports:
    - accordion_presentation
    - accordion_presentation.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/luisza/accordion_presentation
  license: GNU General Public License v2 (GPLv2)
  summary: 'It is a simple horizontal accordion for django cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
