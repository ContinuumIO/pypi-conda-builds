package:
  name: applicake
  version: "0.0.7"

source:
  fn: applicake-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/a/applicake/applicake-0.0.7.tar.gz
  md5: f2ba39317633879371e6cdebd5d065c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - applicake = applicake:main
    #
    # Would create an entry point called applicake that calls applicake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - applicake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/applicake
  license: Attribution-NonCommercial-ShareAlike 3.0 Unported (CC BY-NC-SA 3.0)
  summary: 'A framework that simplifies the wrapping of external tools by standardizing input parameters, logging messages and output streams.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
