package:
  name: uamd
  version: "0.1rc4"

source:
  fn: uamd-0.1rc4.tar.gz
  url: https://pypi.python.org/packages/source/u/uamd/uamd-0.1rc4.tar.gz
  md5: c6d55e53366837f4ced5121e114a4179
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uamd = uamd:main
    #
    # Would create an entry point called uamd that calls uamd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipy
    - beautifulsoup

  run:
    - python
    - setuptools
    - ipy
    - beautifulsoup

test:
  # Python imports
  imports:
    - test
    - uamd
    - uamd.device

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/uamd
  license: BSD License
  summary: 'User Agent Mobile Detect'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
