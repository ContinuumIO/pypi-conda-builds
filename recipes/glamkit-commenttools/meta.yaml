package:
  name: glamkit-commenttools
  version: "0.5.1"

source:
  fn: glamkit-commenttools-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/g/glamkit-commenttools/glamkit-commenttools-0.5.1.tar.gz
  md5: 0da9d30db0241e38aee39c715ca39f82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glamkit-commenttools = glamkit-commenttools:main
    #
    # Would create an entry point called glamkit-commenttools that calls glamkit-commenttools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - commenttools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/glamkit/glamkit-commenttools
  license: BSD License
  summary: 'Useful bits to spice up comments in your Django site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
