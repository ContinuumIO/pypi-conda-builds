package:
  name: extremes
  version: "1.1.1"

source:
  fn: Extremes-1.1.1.zip
  url: https://pypi.python.org/packages/source/E/Extremes/Extremes-1.1.1.zip
  md5: 744c685f3ab2b94819fa34e41ca7e268
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extremes = extremes:main
    #
    # Would create an entry point called extremes that calls extremes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - extremes
    - peak
    - peak.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Extremes
  license: PSF or ZPL
  summary: "Production-quality 'Min' and 'Max' objects (adapted from PEP 326)"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
