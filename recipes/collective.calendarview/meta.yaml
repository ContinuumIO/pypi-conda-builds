package:
  name: collective.calendarview
  version: "1.0"

source:
  fn: collective.calendarview-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.calendarview/collective.calendarview-1.0.zip
  md5: 1527085945d975de07573b35548c0071
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.calendarview = collective.calendarview:main
    #
    # Would create an entry point called collective.calendarview that calls collective.calendarview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.configviews
    - collective.js.fullcalendar

  run:
    - python
    - setuptools
    - setuptools
    - collective.configviews
    - collective.js.fullcalendar

test:
  # Python imports
  imports:
    - collective
    - collective.calendarview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Fullcalendar view'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
