package:
  name: django-kb
  version: "0.2.0"

source:
  fn: django-kb-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kb/django-kb-0.2.0.tar.gz
  md5: b0e3c39d9335f66d83e60dc5bf0ba247
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kb = django-kb:main
    #
    # Would create an entry point called django-kb that calls django-kb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.7
    - south >=0.8.4,<1.0
    - django-braces >=1.4.0
    - django-choices >=1.3.0
    - django-haystack >=2.2.0
    - django-model-utils >=2.2.0
    - django-taggit >=0.12.1
    - django-markupfield ==1.2.1
    - pytz >=2014.4

  run:
    - python
    - django >=1.6,<1.7
    - south >=0.8.4,<1.0
    - django-braces >=1.4.0
    - django-choices >=1.3.0
    - django-haystack >=2.2.0
    - django-model-utils >=2.2.0
    - django-taggit >=0.12.1
    - django-markupfield ==1.2.1
    - pytz >=2014.4

test:
  # Python imports
  imports:
    - kb
    - kb.article
    - kb.base
    - kb.category
    - kb.migrations
    - kb.templatetags
    - kb.vote

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eliostvs/django-kb
  license: BSD License
  summary: 'Simple knowledge base made with django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
