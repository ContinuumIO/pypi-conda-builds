package:
  name: django_temporal
  version: "0.3"

source:
  fn: django_temporal-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_temporal/django_temporal-0.3.tar.gz
  md5: e966dd043cd9214d36f36fb66b1f8bf7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_temporal = django_temporal:main
    #
    # Would create an entry point called django_temporal that calls django_temporal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_temporal
    - django_temporal.db
    - django_temporal.db.backends
    - django_temporal.db.backends.postgresql
    - django_temporal.db.models
    - django_temporal.db.models.sql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zejn/django_temporal
  license: BSD License
  summary: 'Temporal database extension for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
