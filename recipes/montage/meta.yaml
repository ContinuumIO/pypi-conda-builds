package:
  name: montage
  version: "0.3.6"

source:
  fn: montage-0.3.6.tar.gz
  url: https://pypi.python.org/packages/source/m/montage/montage-0.3.6.tar.gz
  md5: 736ac9054a836ff125a0f3940682c3ad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - montage = montage:main
    #
    # Would create an entry point called montage that calls montage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decoupage >=0.8
    - cropresize

  run:
    - python
    - setuptools
    - decoupage >=0.8
    - cropresize

test:
  # Python imports
  imports:
    - montage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org/hg/montage
  license: GPL
  summary: 'photogallery using decoupage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
