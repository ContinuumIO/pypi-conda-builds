package:
  name: icepick
  version: "0.0.1"

source:
  fn: icePick-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/icePick/icePick-0.0.1.tar.gz
  md5: 958bafa3b0439bafa2c7b962fe8557c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icepick = icepick:main
    #
    # Would create an entry point called icepick that calls icepick.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aiohttp ==0.14.4
    - beautifulsoup4 ==4.3.2
    - nose ==1.3.6
    - pymongo ==3.0

  run:
    - python
    - aiohttp ==0.14.4
    - beautifulsoup4 ==4.3.2
    - nose ==1.3.6
    - pymongo ==3.0

test:
  # Python imports
  imports:
    - icePick
    - icepick
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/teitei-tk/ice-pick
  license: MIT License
  summary: 'icePick is a All in one Package library for easy Scraping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
