package:
  name: goldencage
  version: "3.6"

source:
  fn: goldencage-3.6.tar.gz
  url: https://pypi.python.org/packages/source/g/goldencage/goldencage-3.6.tar.gz
  md5: 955089194100cc409a7c4917f6cb8dbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goldencage = goldencage:main
    #
    # Would create an entry point called goldencage that calls goldencage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - wechat
    - pycrypto
    - jsonfield
    - pytz
    - simplejson
    - dicttoxml
    - xmltodict

  run:
    - python
    - requests
    - wechat
    - pycrypto
    - jsonfield
    - pytz
    - simplejson
    - dicttoxml
    - xmltodict

test:
  # Python imports
  imports:
    - goldencage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeffkit/goldencage
  license: UNKNOWN
  summary: 'virtual coin & task management for mobile app (specially for china)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
