package:
  name: langacore.kit.common
  version: "0.2.4"

source:
  fn: langacore.kit.common-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/l/langacore.kit.common/langacore.kit.common-0.2.4.tar.gz
  md5: 39f58f25df6159810884efaf404102de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - langacore.kit.common = langacore.kit.common:main
    #
    # Would create an entry point called langacore.kit.common that calls langacore.kit.common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - lockfile

  run:
    - python
    - distribute
    - lockfile

test:
  # Python imports
  imports:
    - langacore
    - langacore.kit
    - langacore.kit.cache
    - langacore.kit.collections
    - langacore.kit.concurrency
    - langacore.kit.config
    - langacore.kit.files
    - langacore.kit.lang

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/langacore.kit.common/
  license: GNU General Public License (GPL)
  summary: "A library of various simple common routines that keep being rewritten all over again in every project I'm working on."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
