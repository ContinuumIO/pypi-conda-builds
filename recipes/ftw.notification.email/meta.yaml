package:
  name: ftw.notification.email
  version: "2.0.9"

source:
  fn: ftw.notification.email-2.0.9.zip
  url: https://pypi.python.org/packages/source/f/ftw.notification.email/ftw.notification.email-2.0.9.zip
  md5: 5a1966ee35d0a4d9bd06f803f332e4bd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.notification.email = ftw.notification.email:main
    #
    # Would create an entry point called ftw.notification.email that calls ftw.notification.email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - stoneagehtml
    - beautifulsoup
    - ftw.journal
    - ftw.notification.base

  run:
    - python
    - setuptools
    - setuptools
    - stoneagehtml
    - beautifulsoup
    - ftw.journal
    - ftw.notification.base

test:
  # Python imports
  imports:
    - ftw
    - ftw.notification
    - ftw.notification.email
    - ftw.notification.email.events
    - ftw.notification.email.templates
    - ftw.notification.email.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.notification.email
  license: GPL2
  summary: 'Send edit-notifications by email.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
