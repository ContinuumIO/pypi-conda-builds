package:
  name: coffin-absolute
  version: "0.1.0"

source:
  fn: coffin-absolute-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coffin-absolute/coffin-absolute-0.1.0.tar.gz
  md5: 31cddc3930c376307c315cbe87f7ea16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coffin-absolute = coffin-absolute:main
    #
    # Would create an entry point called coffin-absolute that calls coffin-absolute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coffin_absolute

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pacemkr/coffin-absolute
  license: BSD License
  summary: 'Provides absolute_url tag and filter in Jinja2 templates when using the coffin package with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
