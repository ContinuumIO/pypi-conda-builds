package:
  name: deltas
  version: "0.3.0"

source:
  fn: deltas-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/deltas/deltas-0.3.0.tar.gz
  md5: 2fb922df9bd21e0156f0ae0c411a0e13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deltas = deltas:main
    #
    # Would create an entry point called deltas that calls deltas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - yamlconf

  run:
    - python
    - yamlconf

test:
  # Python imports
  imports:
    - deltas
    - deltas.algorithms
    - deltas.algorithms.tests
    - deltas.segmenters
    - deltas.segmenters.tests
    - deltas.tests
    - deltas.tokenizers
    - deltas.tokenizers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/halfak/Deltas
  license: MIT License
  summary: 'An experimental diff library for generating operation deltas that represent the difference between two sequences of comparable items.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
