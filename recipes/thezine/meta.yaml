package:
  name: thezine
  version: "0.0.1"

source:
  fn: TheZine-0.0.1.zip
  url: https://pypi.python.org/packages/source/T/TheZine/TheZine-0.0.1.zip
  md5: a28a53721d91ee561aa40d84fb23ab12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thezine = thezine:main
    #
    # Would create an entry point called thezine that calls thezine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.3.1
    - requests

  run:
    - python
    - beautifulsoup4 >=4.3.1
    - requests

test:
  # Python imports
  imports:
    - tests
    - thezine
    - tz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/voidabhi/TheZineAPI
  license: MIT
  summary: 'Python API for The Zine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
