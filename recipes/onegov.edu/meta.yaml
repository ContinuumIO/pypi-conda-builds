package:
  name: onegov.edu
  version: "1.0.3"

source:
  fn: onegov.edu-1.0.3.zip
  url: https://pypi.python.org/packages/source/o/onegov.edu/onegov.edu-1.0.3.zip
  md5: 82acd7f78e64792636d4fdf7d2dd2b7f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onegov.edu = onegov.edu:main
    #
    # Would create an entry point called onegov.edu that calls onegov.edu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - products.cmfplacefulworkflow
    - ftw.inflator
    - ftw.contentpage
    - simplelayout.types.common
    - simplelayout.types.flowplayerblock
    - products.ploneformgen
    - ftw.calendar
    - ftw.lawgiver
    - plonetheme.onegov
    - ftw.subsite
    - ftw.slider

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - products.cmfplacefulworkflow
    - ftw.inflator
    - ftw.contentpage
    - simplelayout.types.common
    - simplelayout.types.flowplayerblock
    - products.ploneformgen
    - ftw.calendar
    - ftw.lawgiver
    - plonetheme.onegov
    - ftw.subsite
    - ftw.slider

test:
  # Python imports
  imports:
    - onegov
    - onegov.edu
    - onegov.edu.tests
    - onegov.edu.upgrades
    - onegov.edu.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/onegov/onegov.edu
  license: GPL2
  summary: 'Policy package for OneGov.edu pages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
