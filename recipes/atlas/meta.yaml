package:
  name: atlas
  version: "0.27.0"

source:
  fn: atlas-0.27.0.tar.gz
  url: https://pypi.python.org/packages/source/a/atlas/atlas-0.27.0.tar.gz
  md5: ed88dc6ddf7d2567157ea6d8c5fce084
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atlas = atlas:main
    #
    # Would create an entry point called atlas that calls atlas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - threadpool >=1.2.4

  run:
    - python
    - threadpool >=1.2.4

test:
  # Python imports
  imports:
    - atlas
    - src
    - src.mod

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/atlas-build-tool
  license: Apache 2.0
  summary: 'Atlas Build Tool - simple, extensible build system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
