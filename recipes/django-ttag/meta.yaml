package:
  name: django-ttag
  version: "2.4"

source:
  fn: django-ttag-2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ttag/django-ttag-2.4.tar.gz
  md5: c778bda3f8f34b5626a1db37361075e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ttag = django-ttag:main
    #
    # Would create an entry point called django-ttag that calls django-ttag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ttag
    - ttag.helpers
    - ttag.tests
    - ttag.tests.setup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lincolnloop/django-ttag
  license: BSD License
  summary: 'A template tag constructor library for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
