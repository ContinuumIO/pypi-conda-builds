package:
  name: django-instagram-api
  version: "0.1.5"

source:
  fn: django-instagram-api-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-instagram-api/django-instagram-api-0.1.5.tar.gz
  md5: 239f28eb8f6f44c0129e9c75b20f1c10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-instagram-api = django-instagram-api:main
    #
    # Would create an entry point called django-instagram-api that calls django-instagram-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.5.3
    - python-instagram >=1.3.0
    - django-m2m-history >=0.2.2

  run:
    - python
    - requests >=2.5.3
    - python-instagram >=1.3.0
    - django-m2m-history >=0.2.2

test:
  # Python imports
  imports:
    - instagram_api
    - instagram_api.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-instagram-api
  license: BSD License
  summary: 'Django implementation for instagram API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
