package:
  name: django-dburlresolvers
  version: "0.1.0-beta2"

source:
  fn: django-dburlresolvers-0.1.0-beta2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dburlresolvers/django-dburlresolvers-0.1.0-beta2.tar.gz
  md5: 3ae600ab30c77d69cf70a4458a827d55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dburlresolvers = django-dburlresolvers:main
    #
    # Would create an entry point called django-dburlresolvers that calls django-dburlresolvers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-classy-tags
    - django-configfield
    - surlex

  run:
    - python
    - django-classy-tags
    - django-configfield
    - surlex

test:
  # Python imports
  imports:
    - dbresolver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django-based URL resolving mechanism, which store the URL patterns in database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
