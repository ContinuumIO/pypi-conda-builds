package:
  name: noseapp_alchemy
  version: "1.0.0"

source:
  fn: noseapp_alchemy-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/n/noseapp_alchemy/noseapp_alchemy-1.0.0.tar.gz
  md5: 3f93671bdc2f633f7216cf294ef90bd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - noseapp_alchemy = noseapp_alchemy:main
    #
    # Would create an entry point called noseapp_alchemy that calls noseapp_alchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - noseapp >=1.0.9
    - sqlalchemy ==0.9.8

  run:
    - python
    - noseapp >=1.0.9
    - sqlalchemy ==0.9.8

test:
  # Python imports
  imports:
    - noseapp_alchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trifonovmixail/noseapp_alchemy
  license: UNKNOWN
  summary: 'SqlAlchemy extension for noseapp lib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
