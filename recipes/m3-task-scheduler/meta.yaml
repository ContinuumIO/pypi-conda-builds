package:
  name: m3-task-scheduler
  version: "1.0"

source:
  fn: m3-task-scheduler-1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-task-scheduler/m3-task-scheduler-1.0.tar.gz
  md5: d54bdf8ff01c8de1dd7cc10bd79d6850
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-task-scheduler = m3-task-scheduler:main
    #
    # Would create an entry point called m3-task-scheduler that calls m3-task-scheduler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - m3_task_scheduler
    - m3_task_scheduler.management
    - m3_task_scheduler.management.commands
    - m3_task_scheduler.migrations
    - tests
    - tests.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: u'\u041f\u043b\u0430\u043d\u0438\u0440\u043e\u0432\u0449\u0438\u043a'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
