package:
  name: gherkin-parser
  version: "0.1"

source:
  fn: gherkin_parser-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gherkin-parser/gherkin_parser-0.1.tar.gz
  md5: ec8fa62f5e5cb3514016f3e4b29e2fd3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gherkin-parser = gherkin-parser:main
    #
    # Would create an entry point called gherkin-parser that calls gherkin-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - gherkin_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nathforge/gherkin-parser
  license: MIT License
  summary: 'Gherkin BDD feature file parser. Compatible with Cucumber.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
