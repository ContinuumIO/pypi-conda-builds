package:
  name: collective.psc.mirroring
  version: "0.1.4"

source:
  fn: collective.psc.mirroring-0.1.4.zip
  url: https://pypi.python.org/packages/source/c/collective.psc.mirroring/collective.psc.mirroring-0.1.4.zip
  md5: d2bb1d82e9924bcd523b3fb4655e4748
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.psc.mirroring = collective.psc.mirroring:main
    #
    # Would create an entry point called collective.psc.mirroring that calls collective.psc.mirroring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.psc
    - collective.psc.mirroring
    - collective.psc.mirroring.browser
    - collective.psc.mirroring.subscribers
    - collective.psc.mirroring.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.psc.mirroring
  license: GPL
  summary: 'Event-based mirroring tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
