package:
  name: docit
  version: "1.0.0.2-r3"

source:
  fn: docit-1.0.0.2-r3.tar.bz2
  url: https://pypi.python.org/packages/source/d/docit/docit-1.0.0.2-r3.tar.bz2
  md5: 22f9af1ec977b4c93f78735d5209a45e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docit = docit:main
    #
    # Would create an entry point called docit that calls docit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - docit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/bmearns/docit
  license: GNU General Public License v3 (GPLv3)
  summary: 'Simple tools for sphinx autodoc to control whether members are documented or not.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
