package:
  name: cs.navigation
  version: "0.4"

source:
  fn: cs.navigation-0.4.zip
  url: https://pypi.python.org/packages/source/c/cs.navigation/cs.navigation-0.4.zip
  md5: 169b3d793da99a310cd131dba6029c00
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.navigation = cs.navigation:main
    #
    # Would create an entry point called cs.navigation that calls cs.navigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.form
    - plone.app.portlets

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.form
    - plone.app.portlets

test:
  # Python imports
  imports:
    - cs
    - cs.navigation
    - cs.navigation.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.codesyntax.com/private/cs.navigation
  license: GPL
  summary: "Simple navigation portlet based on plone's but with additional markup checks"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
