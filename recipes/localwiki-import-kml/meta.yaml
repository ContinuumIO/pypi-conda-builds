package:
  name: localwiki-import-kml
  version: "0.1.2"

source:
  fn: localwiki-import-kml-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/localwiki-import-kml/localwiki-import-kml-0.1.2.tar.gz
  md5: a5052f0cbd3662b97dfd5227264cd47f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - localwiki-import-kml = localwiki-import-kml:main
    #
    # Would create an entry point called localwiki-import-kml that calls localwiki-import-kml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - slumber ==0.5.3-custom1
    - django ==1.3

  run:
    - python
    - slumber ==0.5.3-custom1
    - django ==1.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/philipn/localwiki_kml_import
  license: UNKNOWN
  summary: 'A script to import KML files into a LocalWiki instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
