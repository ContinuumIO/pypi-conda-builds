package:
  name: django-audit-log
  version: "0.7.0"

source:
  fn: django-audit-log-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-audit-log/django-audit-log-0.7.0.tar.gz
  md5: 940f8fdbf9200009f7cf344f62aeaf6b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-audit-log = django-audit-log:main
    #
    # Would create an entry point called django-audit-log that calls django-audit-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - audit_log
    - audit_log.models
    - audit_log.tests
    - audit_log.tests.audit_log_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Atomidata/django-audit-log
  license: BSD License
  summary: 'Audit trail for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
