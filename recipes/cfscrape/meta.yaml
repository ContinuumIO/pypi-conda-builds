package:
  name: cfscrape
  version: "1.3"

source:
  fn: cfscrape-1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cfscrape/cfscrape-1.3.tar.gz
  md5: 8a060fc973003a1942e24a2a5195b56b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cfscrape = cfscrape:main
    #
    # Would create an entry point called cfscrape that calls cfscrape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyexecjs >=1.1.0
    - requests >=2.0.0

  run:
    - python
    - pyexecjs >=1.1.0
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - cfscrape

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Anorov/cloudflare-scrape
  license: UNKNOWN
  summary: "A simple Python module to bypass Cloudflare's anti-bot page. See https://github.com/Anorov/cloudflare-scrape for more information."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
