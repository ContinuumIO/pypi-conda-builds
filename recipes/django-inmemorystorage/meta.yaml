package:
  name: django-inmemorystorage
  version: "0.1.1"

source:
  fn: django-inmemorystorage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inmemorystorage/django-inmemorystorage-0.1.1.tar.gz
  md5: 2d596ff23cd01bbbb34d3202a1c536c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inmemorystorage = django-inmemorystorage:main
    #
    # Would create an entry point called django-inmemorystorage that calls django-inmemorystorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - inmemorystorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codysoyland/django-inmemorystorage
  license: BSD License
  summary: 'A non-persistent in-memory data storage backend for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
