package:
  name: mongo-cleaner
  version: "0.1"

source:
  fn: mongo-cleaner-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mongo-cleaner/mongo-cleaner-0.1.tar.gz
  md5: 527d25e58cbaf08344711ff9ffcc4d25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongo-cleaner = mongo-cleaner:main
    #
    # Would create an entry point called mongo-cleaner that calls mongo-cleaner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - mongo_cleaner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cenkalti/mongo-cleaner
  license: BSD License
  summary: 'Stale data cleaner for MongoDB.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
