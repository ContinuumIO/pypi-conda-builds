package:
  name: django-cid
  version: "0.1.0"

source:
  fn: django-cid-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cid/django-cid-0.1.0.tar.gz
  md5: 0b508e608d2e24b56ee2c495e4c8b384
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cid = django-cid:main
    #
    # Would create an entry point called django-cid that calls django-cid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/snowball-digital/cid
  license: BSD License
  summary: 'Correlation IDs in Django for debugging requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
