package:
  name: gwebsockets
  version: "0.4"

source:
  fn: gwebsockets-0.4.tar.gz
  url: https://pypi.python.org/packages/source/g/gwebsockets/gwebsockets-0.4.tar.gz
  md5: a0d1bd9e6e1b2aae5754e35234e3b838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gwebsockets = gwebsockets:main
    #
    # Would create an entry point called gwebsockets that calls gwebsockets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gwebsockets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dnarvaez/gwebsockets
  license: Apache Software License
  summary: 'GLib based websockets server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
