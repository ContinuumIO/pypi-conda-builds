package:
  name: collective.duplicateaction
  version: "1.0"

source:
  fn: collective.duplicateaction-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.duplicateaction/collective.duplicateaction-1.0.zip
  md5: 59ee21d364c63c0a1bcb8ecaff6718e8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.duplicateaction = collective.duplicateaction:main
    #
    # Would create an entry point called collective.duplicateaction that calls collective.duplicateaction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

test:
  # Python imports
  imports:
    - collective
    - collective.duplicateaction

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.duplicateaction
  license: GPL
  summary: 'Adds additional action to content actions menu for duplicating content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
