package:
  name: vdist
  version: "0.3.4"

source:
  fn: vdist-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/v/vdist/vdist-0.3.4.tar.gz
  md5: 113bdfc00ad755efe6ac267fa820ea27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vdist = vdist:main
    #
    # Would create an entry point called vdist that calls vdist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 ==2.7.3
    - docker-py ==0.7.2

  run:
    - python
    - jinja2 ==2.7.3
    - docker-py ==0.7.2

test:
  # Python imports
  imports:
    - vdist
    - vdist.profiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/objectified/vdist
  license: MIT License
  summary: 'Create OS packages from Python projects using Docker containers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
