package:
  name: django-admin-langswitch
  version: "0.1.5"

source:
  fn: django-admin-langswitch-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-langswitch/django-admin-langswitch-0.1.5.tar.gz
  md5: 054e3c6be96367b31877ab5f48567714
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-langswitch = django-admin-langswitch:main
    #
    # Would create an entry point called django-admin-langswitch that calls django-admin-langswitch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_langswitch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/centralniak
  license: BSD License
  summary: 'Adds easy language switch in admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
