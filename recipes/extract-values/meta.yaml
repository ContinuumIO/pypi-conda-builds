package:
  name: extract-values
  version: "1.0"

source:
  fn: extract-values-1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/extract-values/extract-values-1.0.tar.gz
  md5: 42b16180e9e6a311916158b59146dd9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extract-values = extract-values:main
    #
    # Would create an entry point called extract-values that calls extract-values.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gnrfan/extract-values
  license: BSD License
  summary: 'A Python module for extracting values out of a string using a simple pattern instead of a regular expression.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
