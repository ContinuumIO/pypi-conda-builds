package:
  name: envoy-beta
  version: "0.0.2"

source:
  fn: envoy-beta-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/envoy-beta/envoy-beta-0.0.2.tar.gz
  md5: a8f82cfb156fbca91dca91df00be84ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - envoy-beta = envoy-beta:main
    #
    # Would create an entry point called envoy-beta that calls envoy-beta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - envoy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennethreitz/envoy
  license: MIT License
  summary: 'Simple API for running external processes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
