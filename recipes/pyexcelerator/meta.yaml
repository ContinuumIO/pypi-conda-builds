package:
  name: pyexcelerator
  version: "0.6.4a"

source:
  fn: pyexcelerator-0.6.4.1.tar.bz2
  url: https://pypi.python.org/packages/source/p/pyExcelerator/pyexcelerator-0.6.4.1.tar.bz2
  md5: 478c79a74be39d27eee9bd1b3032dffe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyexcelerator = pyexcelerator:main
    #
    # Would create an entry point called pyexcelerator that calls pyexcelerator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyExcelerator
    - pyexcelerator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/pyexcelerator/
  license: BSD License
  summary: 'generating Excel 97+ files; importing Excel 95+ files; Excel files dumper; OLE2 files dumper; xls2txt, xls2csv, xls2html'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
