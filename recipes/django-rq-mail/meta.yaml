package:
  name: django-rq-mail
  version: "0.1.3.alpha"

source:
  fn: django-rq-mail-0.1.3.alpha.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rq-mail/django-rq-mail-0.1.3.alpha.tar.gz
  md5: a306de17d04aa1b34bc3ef21c37b255f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rq-mail = django-rq-mail:main
    #
    # Would create an entry point called django-rq-mail that calls django-rq-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rq_mail
    - rq_mail.management
    - rq_mail.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-rq-mail
  license: MIT License
  summary: 'simple Python library based on rq to store emails sent by Django and process them in the background with workers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
