package:
  name: plone.app.cmsui
  version: "1.0a2"

source:
  fn: plone.app.cmsui-1.0a2.zip
  url: https://pypi.python.org/packages/source/p/plone.app.cmsui/plone.app.cmsui-1.0a2.zip
  md5: 9886bf80fe0af1c148251c2560127967
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.cmsui = plone.app.cmsui:main
    #
    # Would create an entry point called plone.app.cmsui that calls plone.app.cmsui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - products.cmfplone
    - plone.namedfile[blobs]
    - plone.formwidget.namedfile
    - zope.browserresource

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - products.cmfplone
    - plone.namedfile[blobs]
    - plone.formwidget.namedfile
    - zope.browserresource

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.cmsui
    - plone.app.cmsui.bbb
    - plone.app.cmsui.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.cmsui
  license: GPL
  summary: 'CMS user interface for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
