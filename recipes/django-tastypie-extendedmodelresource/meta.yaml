package:
  name: django-tastypie-extendedmodelresource
  version: "0.22"

source:
  fn: django-tastypie-extendedmodelresource-0.22.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tastypie-extendedmodelresource/django-tastypie-extendedmodelresource-0.22.tar.gz
  md5: 38172a12fa6a44999621cba08edc38b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tastypie-extendedmodelresource = django-tastypie-extendedmodelresource:main
    #
    # Would create an entry point called django-tastypie-extendedmodelresource that calls django-tastypie-extendedmodelresource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-tastypie >=0.9.11

  run:
    - python
    - django >=1.3
    - django-tastypie >=0.9.11

test:
  # Python imports
  imports:
    - extendedmodelresource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tryolabs/django-tastypie-extendedmodelresource
  license: BSD License
  summary: "An extension of TastyPie's ModelResource to easily support nested resources, and more."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
