package:
  name: textblob
  version: "0.9.1"

source:
  fn: textblob-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/t/textblob/textblob-0.9.1.tar.gz
  md5: 1b78719f08f4004f27c50fb0c976bdff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - textblob = textblob:main
    #
    # Would create an entry point called textblob that calls textblob.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nltk >=3.0

  run:
    - python
    - nltk >=3.0

test:
  # Python imports
  imports:
    - textblob
    - textblob.en
    - textblob.unicodecsv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - mock
    - nose
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sloria/TextBlob
  license: MIT License
  summary: 'Simple, Pythonic text processing. Sentiment analysis, POS tagging, noun phrase parsing, and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
