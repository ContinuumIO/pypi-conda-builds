package:
  name: django-jquery-lightbox
  version: "2.6"

source:
  fn: django-jquery-lightbox-2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery-lightbox/django-jquery-lightbox-2.6.tar.gz
  md5: ca77b0357efd26455bb6472959386624
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery-lightbox = django-jquery-lightbox:main
    #
    # Would create an entry point called django-jquery-lightbox that calls django-jquery-lightbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jquery >=1.9

  run:
    - python
    - django-jquery >=1.9

test:
  # Python imports
  imports:
    - jquery_lightbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikebryant/django-jquery-lightbox
  license: Creative Commons Attribution 2.5 License
  summary: 'Django package for Lightbox: a small javascript library used to overlay images on top of the current page.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
