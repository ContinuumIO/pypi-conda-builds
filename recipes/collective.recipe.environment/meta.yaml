package:
  name: collective.recipe.environment
  version: "0.2.0"

source:
  fn: collective.recipe.environment-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.environment/collective.recipe.environment-0.2.0.tar.gz
  md5: 6f8d64130a12aca3e7923c3e561f7669
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.environment = collective.recipe.environment:main
    #
    # Would create an entry point called collective.recipe.environment that calls collective.recipe.environment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.environment
    - collective.recipe.environment.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/collective.recipe.environment
  license: GNU General Public License (GPL)
  summary: 'zc.buildout recipe to set environment variables during the execution of a buildout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
