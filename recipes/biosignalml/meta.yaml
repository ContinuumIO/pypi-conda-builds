package:
  name: biosignalml
  version: "0.6.0b"

source:
  fn: BioSignalML-0.6.0b.tar.gz
  url: https://pypi.python.org/packages/source/B/BioSignalML/BioSignalML-0.6.0b.tar.gz
  md5: 22c0147402407ec196eb9349f103b557
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biosignalml = biosignalml:main
    #
    # Would create an entry point called biosignalml that calls biosignalml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - isodate >=0.4.7
    - pyparsing >=1.5.0,<2.0.0
    - httplib2 >=0.7.7
    - python-dateutil >=2.0
    - ws4py >=0.2.4
    - rdflib >=4.1.0
    - pint >=0.5.1
    - numpy >=1.8.1

  run:
    - python
    - distribute
    - isodate >=0.4.7
    - pyparsing >=1.5.0,<2.0.0
    - httplib2 >=0.7.7
    - python-dateutil >=2.0
    - ws4py >=0.2.4
    - rdflib >=4.1.0
    - pint >=0.5.1
    - numpy >=1.8.1

test:
  # Python imports
  imports:
    - biosignalml
    - biosignalml.client
    - biosignalml.data
    - biosignalml.formats
    - biosignalml.formats.edf
    - biosignalml.formats.hdf5
    - biosignalml.formats.raw
    - biosignalml.formats.wfdb
    - biosignalml.model
    - biosignalml.rdf
    - biosignalml.repository
    - biosignalml.transports
    - biosignalml.units
    - biosignalml.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dbrnz/biosignalml-corelib
  license: Apache Software License
  summary: 'Python BioSignal Abstraction Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
