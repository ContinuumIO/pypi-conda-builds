package:
  name: list_style_dict
  version: "0.00"

source:
  fn: list_Style_Dict-0.00.tar.gz
  url: https://pypi.python.org/packages/source/l/list_Style_Dict/list_Style_Dict-0.00.tar.gz
  md5: 5fb3c82c5c2813a5836e57445405d4ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - list_style_dict = list_style_dict:main
    #
    # Would create an entry point called list_style_dict that calls list_style_dict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - list_style_dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ihgazni/list_Style_Dict.git
  license: LGPL
  summary: 'a list-like thing implemented via dict whose keys are continuous intergs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
