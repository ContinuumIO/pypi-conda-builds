package:
  name: metapub
  version: "0.3.8.1"

source:
  fn: metapub-0.3.8.1.tar.gz
  url: https://pypi.python.org/packages/source/m/metapub/metapub-0.3.8.1.tar.gz
  md5: 877ced6e07c6a9927815b9e3669b6d60
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metapub = metapub:main
    #
    # Would create an entry point called metapub that calls metapub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - requests
    - eutils
    - tabulate
    - cssselect

  run:
    - python
    - setuptools
    - lxml
    - requests
    - eutils
    - tabulate
    - cssselect

test:
  # Python imports
  imports:
    - metapub
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nthmost/metapub
  license: Apache 2.0
  summary: 'Pubmed / NCBI / eutils interaction library, handling the metadata of pubmed papers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
