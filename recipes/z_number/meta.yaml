package:
  name: z_number
  version: "1.0.0"

source:
  fn: z_number-1.0.0.zip
  url: https://pypi.python.org/packages/source/z/z_number/z_number-1.0.0.zip
  md5: ede305d893f2804896327a3f27df8ade
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - z_number = z_number:main
    #
    # Would create an entry point called z_number that calls z_number.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - z_number

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.headfirstlabs.com
  license: UNKNOWN
  summary: 'Organize the structure of an substance in the linus Pauling diagram'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
