package:
  name: bisheng
  version: "0.10.0"

source:
  fn: bisheng-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bisheng/bisheng-0.10.0.tar.gz
  md5: ad5f152514068efb0c998fe2df425624
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bisheng = bisheng:main
    #
    # Would create an entry point called bisheng that calls bisheng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - regex ==2014.02.19

  run:
    - python
    - regex ==2014.02.19

test:
  # Python imports
  imports:
    - bisheng

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eliangcs/bisheng
  license: BSD License
  summary: 'A set of utility functions for processing Chinese text.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
