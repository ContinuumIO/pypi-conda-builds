package:
  name: happybase
  version: "0.9"

source:
  fn: happybase-0.9.tar.gz
  url: https://pypi.python.org/packages/source/h/happybase/happybase-0.9.tar.gz
  md5: 0d9ca6e22a7edb03ccd75e99d0acf1ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - happybase = happybase:main
    #
    # Would create an entry point called happybase that calls happybase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - thrift >=0.8.0

  run:
    - python
    - thrift >=0.8.0

test:
  # Python imports
  imports:
    - happybase
    - happybase.hbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wbolster/happybase
  license: MIT License
  summary: 'A developer-friendly Python library to interact with Apache HBase'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
