package:
  name: flask-twitter-oembedder
  version: "0.1.8"

source:
  fn: Flask-Twitter-OEmbedder-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Twitter-OEmbedder/Flask-Twitter-OEmbedder-0.1.8.tar.gz
  md5: 1b687934857f584df919fb14776a1df0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-twitter-oembedder = flask-twitter-oembedder:main
    #
    # Would create an entry point called flask-twitter-oembedder that calls flask-twitter-oembedder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-cache
    - requests
    - requests_oauthlib

  run:
    - python
    - flask
    - flask-cache
    - requests
    - requests_oauthlib

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eriktaubeneck/flask-twitter-oembedder
  license: MIT License
  summary: 'Embedded tweets in Flask Jinja2 Templates with only the Tweet_ID'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
