package:
  name: haufe.monitoring
  version: "0.2.1.4"

source:
  fn: haufe.monitoring-0.2.1.4.tar.gz
  url: https://pypi.python.org/packages/source/h/haufe.monitoring/haufe.monitoring-0.2.1.4.tar.gz
  md5: c72bc2eb07bbb7625d5f9ad68e9cd4f0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haufe.monitoring = haufe.monitoring:main
    #
    # Would create an entry point called haufe.monitoring that calls haufe.monitoring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ipcalc
    - demjson

  run:
    - python
    - setuptools
    - setuptools
    - ipcalc
    - demjson

test:
  # Python imports
  imports:
    - haufe
    - haufe.monitoring
    - haufe.monitoring.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL
  summary: 'Zope 2 ZEO Client Watch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
