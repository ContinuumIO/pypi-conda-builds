package:
  name: js.jquery_markitup
  version: "1.1.10-1"

source:
  fn: js.jquery_markitup-1.1.10-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_markitup/js.jquery_markitup-1.1.10-1.tar.gz
  md5: d0573b0a9ec4ccd19d575c33212b519e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_markitup = js.jquery_markitup:main
    #
    # Would create an entry point called js.jquery_markitup that calls js.jquery_markitup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_markitup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/do3cc/js.jquery_markitup
  license: UNKNOWN
  summary: 'Fanstatic packaging of markitup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
