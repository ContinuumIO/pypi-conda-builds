package:
  name: ldappas
  version: "0.8.3"

source:
  fn: ldappas-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/l/ldappas/ldappas-0.8.3.tar.gz
  md5: 2ace16ce87f1a4ebc033c75dd246753f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldappas = ldappas:main
    #
    # Would create an entry point called ldappas that calls ldappas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3
    - ldapadapter >0.6
    - setuptools
    - zope.pluggableauth
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - zodb3
    - ldapadapter >0.6
    - setuptools
    - zope.pluggableauth
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - ldappas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/ldappas
  license: ZPL 2.1
  summary: 'LDAP-based authenticator for Zope 3. It uses ldapadapter to talk to an\nLDAP server.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
