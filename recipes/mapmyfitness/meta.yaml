package:
  name: mapmyfitness
  version: "0.1.2"

source:
  fn: mapmyfitness-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mapmyfitness/mapmyfitness-0.1.2.tar.gz
  md5: 89db4880d1b275776ccd0d0865855737
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mapmyfitness = mapmyfitness:main
    #
    # Would create an entry point called mapmyfitness that calls mapmyfitness.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0

  run:
    - python
    - requests >=2.1.0

test:
  # Python imports
  imports:
    - mapmyfitness
    - mapmyfitness.api
    - mapmyfitness.objects
    - mapmyfitness.validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JasonSanford/mapmyfitness-python
  license: Apache License
  summary: 'A python wrapper for the MapMyFitness API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
