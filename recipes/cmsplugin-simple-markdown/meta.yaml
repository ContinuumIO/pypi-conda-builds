package:
  name: cmsplugin-simple-markdown
  version: "1.5"

source:
  fn: cmsplugin-simple-markdown-1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-simple-markdown/cmsplugin-simple-markdown-1.5.tar.gz
  md5: 2741c54d47096e9fbebe6bf803da33ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-simple-markdown = cmsplugin-simple-markdown:main
    #
    # Would create an entry point called cmsplugin-simple-markdown that calls cmsplugin-simple-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - markdown
    - django-markdown
    - django-cms

  run:
    - python
    - django
    - markdown
    - django-markdown
    - django-cms

test:
  # Python imports
  imports:
    - cmsplugin_simple_markdown
    - cmsplugin_simple_markdown.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/Alir3z4/cmsplugin-simple-markdown
  license: cmsplugin-socialbuttons
  summary: 'A plugin for django-cms that provides just a markdown plugin and nothing more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
