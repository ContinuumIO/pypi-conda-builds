package:
  name: django-actionviews
  version: "0.2.1"

source:
  fn: django-actionviews-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-actionviews/django-actionviews-0.2.1.tar.gz
  md5: 4dbba5a9597b901a38f2067349453e24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-actionviews = django-actionviews:main
    #
    # Would create an entry point called django-actionviews that calls django-actionviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - actionviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lig/django-actionviews
  license: Apache Software License
  summary: 'Alternative Class Based Views for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
