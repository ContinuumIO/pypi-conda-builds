package:
  name: datatxt-py
  version: "0.0.1"

source:
  fn: datatxt-py-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/datatxt-py/datatxt-py-0.0.1.tar.gz
  md5: 6720104d3764473dbdd13fda12bd6498
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datatxt-py = datatxt-py:main
    #
    # Would create an entry point called datatxt-py that calls datatxt-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - datatxt-py

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zenkay/datatxt-python
  license: UNKNOWN
  summary: "Python package for Dandelion's dataTXT service"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
