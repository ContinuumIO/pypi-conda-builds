package:
  name: pysat
  version: "0.2.1"

source:
  fn: pysat-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pysat/pysat-0.2.1.tar.gz
  md5: 4274a7f3c2c3e1e94a9b64d03058fcd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysat = pysat:main
    #
    # Would create an entry point called pysat that calls pysat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas

  run:
    - python
    - pandas

test:
  # Python imports
  imports:
    - pysat
    - pysat.instruments
    - pysat.ssnl
    - pysat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rstoneback/pysat
  license: BSD License
  summary: 'Supports science data analysis across measurement platforms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
