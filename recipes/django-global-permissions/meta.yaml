package:
  name: django-global-permissions
  version: "0.2.0"

source:
  fn: django-global-permissions-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-global-permissions/django-global-permissions-0.2.0.tar.gz
  md5: 459da29e3b19bb9560caeafd8091dbb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-global-permissions = django-global-permissions:main
    #
    # Would create an entry point called django-global-permissions that calls django-global-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - global_permissions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eduardo-matos/django-global-permissions
  license: BSD License
  summary: 'Implementation of permissions not related to models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
