package:
  name: quintagroup.ploneformgen.readonlystringfield
  version: "0.2"

source:
  fn: quintagroup.ploneformgen.readonlystringfield-0.2.tar.gz
  url: https://pypi.python.org/packages/source/q/quintagroup.ploneformgen.readonlystringfield/quintagroup.ploneformgen.readonlystringfield-0.2.tar.gz
  md5: 5a888a3a256bae65ad94c4f6c86565ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.ploneformgen.readonlystringfield = quintagroup.ploneformgen.readonlystringfield:main
    #
    # Would create an entry point called quintagroup.ploneformgen.readonlystringfield that calls quintagroup.ploneformgen.readonlystringfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.ploneformgen
    - quintagroup.ploneformgen.readonlystringfield
    - quintagroup.ploneformgen.readonlystringfield.Extensions
    - quintagroup.ploneformgen.readonlystringfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://quintagroup.com/services/plone-development/products/
  license: GPL
  summary: 'Readonly String Field for PloneFormGen product'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
