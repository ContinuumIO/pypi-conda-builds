package:
  name: dynamicform
  version: "0.7.0"

source:
  fn: DynamicForm-0.7.0.tar.gz
  url: https://github.com/timothycrosley/DynamicForm/blob/master/dist/DynamicForm-0.7.0.tar.gz?raw=true
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamicform = dynamicform:main
    #
    # Would create an entry point called dynamicform that calls dynamicform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - webelements >=1.0.0-beta.1

  run:
    - python
    - webelements >=1.0.0-beta.1

test:
  # Python imports
  imports:
    - DynamicForm
    - dynamicform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dynamicform.org/
  license: GNU GPLv2
  summary: 'DynamicForm is an AJAX abstraction library for python - that enables different sections of a page to operate and be interacted with independently.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
