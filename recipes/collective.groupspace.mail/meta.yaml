package:
  name: collective.groupspace.mail
  version: "1.0.1"

source:
  fn: collective.groupspace.mail-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.groupspace.mail/collective.groupspace.mail-1.0.1.zip
  md5: 24ceea5446c5577ca199171d3c1c49a7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.groupspace.mail = collective.groupspace.mail:main
    #
    # Would create an entry point called collective.groupspace.mail that calls collective.groupspace.mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.groupspace
    - collective.groupspace.mail
    - collective.groupspace.mail.browser
    - collective.groupspace.mail.portlets
    - collective.groupspace.mail.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.groupspace.mail
  license: GNU General Public License (GPL)
  summary: 'Plone collective.groupspace mail service for contacting other participants'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
