package:
  name: django-markitup-field
  version: "1.2.10"

source:
  fn: django-markitup-field-1.2.10.zip
  url: https://pypi.python.org/packages/source/d/django-markitup-field/django-markitup-field-1.2.10.zip
  md5: 0a1e055ba712c1f7a7fd3dea1a38a794
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markitup-field = django-markitup-field:main
    #
    # Would create an entry point called django-markitup-field that calls django-markitup-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - markitup_field
    - markitup_field.management
    - markitup_field.management.commands
    - markitup_field.templatetags
    - markitup_field.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dimka665/django-markitup-field/
  license: BSD License
  summary: 'Custom Django field for easy use of markup in text fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
