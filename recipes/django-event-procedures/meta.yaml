package:
  name: django-event-procedures
  version: "0.1.3"

source:
  fn: django-event-procedures-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-event-procedures/django-event-procedures-0.1.3.tar.gz
  md5: 4468af2c6574e147f9253797be85db29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-event-procedures = django-event-procedures:main
    #
    # Would create an entry point called django-event-procedures that calls django-event-procedures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0
    - django-genericadmin >=0.6.1

  run:
    - python
    - django >=1.5.0
    - django-genericadmin >=0.6.1

test:
  # Python imports
  imports:
    - event_procedures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/MindClickGlobal/django-event-procedures
  license: GNU
  summary: 'An event driven code execution system backed by Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
