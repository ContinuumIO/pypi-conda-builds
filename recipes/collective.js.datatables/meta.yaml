package:
  name: collective.js.datatables
  version: "3.1.10.4"

source:
  fn: collective.js.datatables-3.1.10.4.zip
  url: https://pypi.python.org/packages/source/c/collective.js.datatables/collective.js.datatables-3.1.10.4.zip
  md5: 3c66f18e8f9f6c15745508ede4d21499
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.datatables = collective.js.datatables:main
    #
    # Would create an entry point called collective.js.datatables that calls collective.js.datatables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.datatables
    - collective.js.datatables.example
    - collective.js.datatables.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.datatables
  license: GPL
  summary: 'Plone Integration of jquery.dataTables plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
