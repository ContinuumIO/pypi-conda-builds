package:
  name: django-twitter-relations-history
  version: "0.3.8"

source:
  fn: django-twitter-relations-history-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twitter-relations-history/django-twitter-relations-history-0.3.8.tar.gz
  md5: 43a1044a00249778b64526a3090bdcb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twitter-relations-history = django-twitter-relations-history:main
    #
    # Would create an entry point called django-twitter-relations-history that calls django-twitter-relations-history.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-twitter-api >=0.1.1

  run:
    - python
    - django-twitter-api >=0.1.1

test:
  # Python imports
  imports:
    - twitter_relations_history

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-twitter-relations-history
  license: BSD License
  summary: 'Django implementation for storing twitter user relations history'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
