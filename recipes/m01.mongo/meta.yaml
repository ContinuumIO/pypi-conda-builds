package:
  name: m01.mongo
  version: "1.0.0"

source:
  fn: m01.mongo-1.0.0.zip
  url: https://pypi.python.org/packages/source/m/m01.mongo/m01.mongo-1.0.0.zip
  md5: c9dc55a6655de33afc272d7c8e54dbfb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m01.mongo = m01.mongo:main
    #
    # Would create an entry point called m01.mongo that calls m01.mongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.4.1
    - zope.component
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.schema
    - zope.security
    - zope.securitypolicy

  run:
    - python
    - setuptools
    - pymongo >=2.4.1
    - zope.component
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.schema
    - zope.security
    - zope.securitypolicy

test:
  # Python imports
  imports:
    - m01
    - m01.mongo
    - m01.mongo.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/m01.mongo
  license: Zope Public License
  summary: 'MongoDB connection pool and container implementation for Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
