package:
  name: gdelt_util
  version: "0.15"

source:
  fn: gdelt_util-0.15.tar.gz
  url: https://pypi.python.org/packages/source/g/gdelt_util/gdelt_util-0.15.tar.gz
  md5: c4b15d288f6cc06948ab4e670a2aa6db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gdelt_util = gdelt_util:main
    #
    # Would create an entry point called gdelt_util that calls gdelt_util.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas
    - numpy
    - scipy
    - matplotlib
    - seaborn

  run:
    - python
    - pandas
    - numpy
    - scipy
    - matplotlib
    - seaborn

test:
  # Python imports
  imports:
    - gdelt_util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/davefol/gdelt_util
  license: GNU
  summary: 'Utilites exploring GDELT data set'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
