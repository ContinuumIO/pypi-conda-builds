package:
  name: django-coinbase
  version: "0.2"

source:
  fn: django-coinbase-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-coinbase/django-coinbase-0.2.tar.gz
  md5: b0a1acebbeb14544aa74b98c406b17c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-coinbase = django-coinbase:main
    #
    # Would create an entry point called django-coinbase that calls django-coinbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jsonfield >=0.8
    - django >=1.4
    - requests >=1.2.0

  run:
    - python
    - django-jsonfield >=0.8
    - django >=1.4
    - requests >=1.2.0

test:
  # Python imports
  imports:
    - coinbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eldarion/django-coinbase
  license: BSD License
  summary: 'a Django app for receiving payment notifications from Coinbase'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
