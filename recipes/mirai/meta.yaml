package:
  name: mirai
  version: "0.1.5"

source:
  fn: mirai-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mirai/mirai-0.1.5.tar.gz
  md5: 984f375501a32043d32db51247bdcc7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mirai = mirai:main
    #
    # Would create an entry point called mirai that calls mirai.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - futures >=2.1.6
    - joblib >=0.7.1

  run:
    - python
    - futures >=2.1.6
    - joblib >=0.7.1

test:
  # Python imports
  imports:
    - mirai
    - mirai.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/duckworthd/mirai
  license: BSD License
  summary: 'Twitter Futures in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
