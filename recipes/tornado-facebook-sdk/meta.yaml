package:
  name: tornado-facebook-sdk
  version: "0.1.0"

source:
  fn: tornado-facebook-sdk-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-facebook-sdk/tornado-facebook-sdk-0.1.0.tar.gz
  md5: 7c444c7330208cae4093ab73d8ad01cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-facebook-sdk = tornado-facebook-sdk:main
    #
    # Would create an entry point called tornado-facebook-sdk that calls tornado-facebook-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=2.0

  run:
    - python
    - tornado >=2.0

test:
  # Python imports
  imports:
    - facebook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pauloalem/tornado-facebook-sdk
  license: OSI
  summary: 'A tornado based facebook graph api wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
