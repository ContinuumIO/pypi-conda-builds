package:
  name: django-subdomains-handler
  version: "0.2"

source:
  fn: django-subdomains-handler-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-subdomains-handler/django-subdomains-handler-0.2.tar.gz
  md5: d5c399e0ce3661ae9a684c34970e2008
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-subdomains-handler = django-subdomains-handler:main
    #
    # Would create an entry point called django-subdomains-handler that calls django-subdomains-handler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lightsub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.piribo.net
  license: BSD License
  summary: 'Lightweight django subdomains handler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
