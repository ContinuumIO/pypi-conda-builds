package:
  name: collective.richdescription
  version: "2.0"

source:
  fn: collective.richdescription-2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.richdescription/collective.richdescription-2.0.tar.gz
  md5: c4b761cf808eefd8f320f4530e232a96
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.richdescription = collective.richdescription:main
    #
    # Would create an entry point called collective.richdescription that calls collective.richdescription.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.component
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - zope.component
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.richdescription
    - collective.richdescription.at
    - collective.richdescription.dx
    - collective.richdescription.dx.migration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/collective.richdescription
  license: GPLv2
  summary: "Turns Plone 'Description' field into Richtext/HTML"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
