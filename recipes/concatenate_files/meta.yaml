package:
  name: concatenate_files
  version: "1.2"

source:
  fn: concatenate_files-1.2.zip
  url: https://pypi.python.org/packages/source/c/concatenate_files/concatenate_files-1.2.zip
  md5: a54e73102c703e7fb194f2f950d7374a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - concatenate_files = concatenate_files:main
    #
    # Would create an entry point called concatenate_files that calls concatenate_files.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - concatenate_files

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/drk4/concatenate_files/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Concatenates the content of files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
