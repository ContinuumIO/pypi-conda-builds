package:
  name: django-filtered-form
  version: "1.0.3"

source:
  fn: django-filtered-form-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filtered-form/django-filtered-form-1.0.3.tar.gz
  md5: ad1293e4f8db6742ae6ae7a2e3f6fc2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filtered-form = django-filtered-form:main
    #
    # Would create an entry point called django-filtered-form that calls django-filtered-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - filtered_form

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-filtered-form
  license: BSD License
  summary: 'A Form that can have per-field queryset filters declaratively defined'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
