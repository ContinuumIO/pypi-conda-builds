package:
  name: rosinstall_generator
  version: "0.1.11"

source:
  fn: rosinstall_generator-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/r/rosinstall_generator/rosinstall_generator-0.1.11.tar.gz
  md5: 7196f4670d546b803158562276d4f0e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rosinstall_generator = rosinstall_generator:main
    #
    # Would create an entry point called rosinstall_generator that calls rosinstall_generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - catkin_pkg >=0.1.28
    - distribute
    - rosdistro >=0.3.4
    - rospkg
    - pyyaml

  run:
    - python
    - argparse
    - catkin_pkg >=0.1.28
    - distribute
    - rosdistro >=0.3.4
    - rospkg
    - pyyaml

test:
  # Python imports
  imports:
    - rosinstall_generator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.ros.org/rosinstall_generator
  license: BSD License or MIT License
  summary: 'A tool to generator rosinstall files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
