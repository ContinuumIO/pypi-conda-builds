package:
  name: xsltemplates
  version: "0.6.1"

source:
  fn: XSLTemplates-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XSLTemplates/XSLTemplates-0.6.1.tar.gz
  md5: dad500e5b338856ad337f8523aa0a750
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xsltemplates = xsltemplates:main
    #
    # Would create an entry point called xsltemplates that calls xsltemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - amara >=1.2
    - wsgixml >=0.3

  run:
    - python
    - amara >=1.2
    - wsgixml >=0.3

test:
  # Python imports
  imports:
    - xsltemplates
    - xsltemplates.ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'An XSLT based template system for WSGI applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
