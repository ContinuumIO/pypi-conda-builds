package:
  name: flask-flarf
  version: "0.0.5"

source:
  fn: Flask-Flarf-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Flarf/Flask-Flarf-0.0.5.tar.gz
  md5: 43e6a0d8f6325c71188dcb4b355da2ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-flarf = flask-flarf:main
    #
    # Would create an entry point called flask-flarf that calls flask-flarf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9

  run:
    - python
    - flask >=0.9

test:
  # Python imports
  imports:
    - flask_flarf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thrisp/flarf
  license: MIT License
  summary: 'Flask request filtering'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
