package:
  name: zc.intid
  version: "1.0.1"

source:
  fn: zc.intid-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.intid/zc.intid-1.0.1.tar.gz
  md5: a0662b7e25e295422e7208151e972e66
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.intid = zc.intid:main
    #
    # Would create an entry point called zc.intid that calls zc.intid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.component
    - zope.event
    - zope.interface
    - zope.security

  run:
    - python
    - setuptools
    - zodb3
    - zope.component
    - zope.event
    - zope.interface
    - zope.security

test:
  # Python imports
  imports:
    - zc
    - zc.intid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.intid
  license: Zope Public License
  summary: 'Reduced-conflict Integer Id Utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
