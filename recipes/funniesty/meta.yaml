package:
  name: funniesty
  version: "0.3"

source:
  fn: funniesty-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/funniesty/funniesty-0.3.tar.gz
  md5: 66eeebb8f0c1b9fad1a47ac38fc89be1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - funniesty = funniesty:main
    #
    # Would create an entry point called funniesty that calls funniesty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown

  run:
    - python
    - markdown

test:
  # Python imports
  imports:
    - funniesty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.stangggtestsite/wp
  license: MIT
  summary: 'The funniest joke in the world. NOT SO MUCH'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
