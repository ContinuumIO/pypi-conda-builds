package:
  name: xstatic-ckeditor
  version: "3.6.4.0"

source:
  fn: XStatic-CKEditor-3.6.4.0.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-CKEditor/XStatic-CKEditor-3.6.4.0.tar.gz
  md5: 2bad6c684f7700133e5ea9c4d7d56d7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-ckeditor = xstatic-ckeditor:main
    #
    # Would create an entry point called xstatic-ckeditor that calls xstatic-ckeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.ckeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ckeditor.com/
  license: (same as CKEditor)
  summary: 'CKEditor 3.6.4 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
