package:
  name: django-girls
  version: "0.0.1"

source:
  fn: django-girls-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-girls/django-girls-0.0.1.tar.gz
  md5: 4f4a4145ccea3ce7810f3c4c5075fa79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-girls = django-girls:main
    #
    # Would create an entry point called django-girls that calls django-girls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - gunicorn
    - dj-database-url
    - dj-static

  run:
    - python
    - django
    - gunicorn
    - dj-database-url
    - dj-static

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Set of packages that allows to deploy into heroku (without local PosgreSQL database)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
