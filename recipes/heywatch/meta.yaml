package:
  name: heywatch
  version: "2.0.0"

source:
  fn: heywatch-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/heywatch/heywatch-2.0.0.tar.gz
  md5: 51002acef30333535151bd1a15e73e81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heywatch = heywatch:main
    #
    # Would create an entry point called heywatch that calls heywatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - heywatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.heywatchencoding.com
  license: MIT License
  summary: 'A python wrapper around the HeyWatch API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
