package:
  name: vollib
  version: "0.1.4"

source:
  fn: vollib-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/v/vollib/vollib-0.1.4.tar.gz
  md5: 1c3296a0aabfb3c8fe91778eeb2b29bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vollib = vollib:main
    #
    # Would create an entry point called vollib that calls vollib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lets_be_rational
    - simplejson
    - matplotlib
    - pandas
    - scipy

  run:
    - python
    - lets_be_rational
    - simplejson
    - matplotlib
    - pandas
    - scipy

test:
  # Python imports
  imports:
    - vollib
    - vollib.black
    - vollib.black.greeks
    - vollib.black_scholes
    - vollib.black_scholes.greeks
    - vollib.black_scholes_merton
    - vollib.black_scholes_merton.greeks
    - vollib.helper
    - vollib.plots
    - vollib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://vollib.org
  license: MIT
  summary: 'Another quant library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
