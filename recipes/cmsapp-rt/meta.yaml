package:
  name: cmsapp-rt
  version: "0.0.4"

source:
  fn: cmsapp-rt-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsapp-rt/cmsapp-rt-0.0.4.tar.gz
  md5: 7455f102e1737851cccecb0abcd5eece
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsapp-rt = cmsapp-rt:main
    #
    # Would create an entry point called cmsapp-rt that calls cmsapp-rt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-singleton-admin

  run:
    - python
    - django-singleton-admin

test:
  # Python imports
  imports:
    - cmsapp_rt
    - cmsapp_rt.bsglobals
    - cmsapp_rt.bsglobals.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RacingTadpole/cmsapp-rt
  license: BSD License
  summary: 'This package contains one app (including a context processor and example template) to help your Django-CMS project work with Twitter Bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
