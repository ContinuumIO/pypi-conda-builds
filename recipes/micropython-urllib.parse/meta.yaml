package:
  name: micropython-urllib.parse
  version: "0.5"

source:
  fn: micropython-urllib.parse-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-urllib.parse/micropython-urllib.parse-0.5.tar.gz
  md5: ee85120cdf65526e5753e97171bc7b5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-urllib.parse = micropython-urllib.parse:main
    #
    # Would create an entry point called micropython-urllib.parse that calls micropython-urllib.parse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - micropython-re-pcre
    - micropython-collections

  run:
    - python
    - micropython-re-pcre
    - micropython-collections

test:
  # Python imports
  imports:
    - urllib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: Python
  summary: 'CPython urllib.parse module ported to MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
