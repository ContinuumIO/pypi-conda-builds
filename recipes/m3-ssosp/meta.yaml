package:
  name: m3-ssosp
  version: "1.1.3"

source:
  fn: m3-ssosp-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-ssosp/m3-ssosp-1.1.3.tar.gz
  md5: e7f5913d2470b96fae17a93dd54feab8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-ssosp = m3-ssosp:main
    #
    # Would create an entry point called m3-ssosp that calls m3-ssosp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demo
    - ssosp
    - ssosp.backends
    - xmldsig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barsgroup/ssosp
  license: MIT License
  summary: u'Single Sign-On Service Provider'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
