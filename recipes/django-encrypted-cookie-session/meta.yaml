package:
  name: django-encrypted-cookie-session
  version: "2.0.0"

source:
  fn: django-encrypted-cookie-session-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-encrypted-cookie-session/django-encrypted-cookie-session-2.0.0.tar.gz
  md5: e77f4fdf984965224b4769d9cade3b61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-encrypted-cookie-session = django-encrypted-cookie-session:main
    #
    # Would create an entry point called django-encrypted-cookie-session that calls django-encrypted-cookie-session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - m2crypto >=0.21.1
    - m2secret >=0.1.1

  run:
    - python
    - django >=1.4
    - m2crypto >=0.21.1
    - m2secret >=0.1.1

test:
  # Python imports
  imports:
    - encrypted_cookies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brightinteractive/django-encrypted-cookie-session/
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
