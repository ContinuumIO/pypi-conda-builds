package:
  name: redturtle.portlet.contextualsearch
  version: "2.0.1"

source:
  fn: redturtle.portlet.contextualsearch-2.0.1.zip
  url: https://pypi.python.org/packages/source/r/redturtle.portlet.contextualsearch/redturtle.portlet.contextualsearch-2.0.1.zip
  md5: 2288310eb8058b3cbc841e40944a4d48
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.portlet.contextualsearch = redturtle.portlet.contextualsearch:main
    #
    # Would create an entry point called redturtle.portlet.contextualsearch that calls redturtle.portlet.contextualsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.portlet
    - redturtle.portlet.contextualsearch
    - redturtle.portlet.contextualsearch.browser
    - redturtle.portlet.contextualsearch.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/redturtle.portlet.contextualsearch/trunk
  license: GPL
  summary: 'A contextual search portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
