package:
  name: yo-fabric
  version: "0.1.3"

source:
  fn: yo-fabric-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/y/yo-fabric/yo-fabric-0.1.3.tar.gz
  md5: d6b61b4a01569be0d9846f746987f47f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - yo-fabric = yo-fabric:main
    #
    # Would create an entry point called yo-fabric that calls yo-fabric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - yo_fabric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DigitalDemiurge/yo-fabric
  license: MIT License
  summary: 'Default fabric tasks for you python project (Simplified version of Fabricator).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
