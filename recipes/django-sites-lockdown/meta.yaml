package:
  name: django-sites-lockdown
  version: "0.0.3"

source:
  fn: Django-Sites-Lockdown-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Sites-Lockdown/Django-Sites-Lockdown-0.0.3.tar.gz
  md5: 23b4beb535bd860dbc09485fb73aa893
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sites-lockdown = django-sites-lockdown:main
    #
    # Would create an entry point called django-sites-lockdown that calls django-sites-lockdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - sites_lockdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RichardOfWard/django-sites-lockdown
  license: MIT License
  summary: "Remove the ability to add or edit sites in Django's admin panel."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
