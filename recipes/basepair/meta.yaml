package:
  name: basepair
  version: "0.8.2"

source:
  fn: basepair-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/b/basepair/basepair-0.8.2.tar.gz
  md5: da566d958ed38f25e0cd3d8e0b069289
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basepair = basepair:main
    #
    # Would create an entry point called basepair that calls basepair.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - awscli
    - boto
    - openpyxl

  run:
    - python
    - requests
    - awscli
    - boto
    - openpyxl

test:
  # Python imports
  imports:
    - basepair

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ausinha/bp-basepair
  license: UNKNOWN
  summary: "Python bindings for Basepair's API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
