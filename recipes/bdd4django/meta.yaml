package:
  name: bdd4django
  version: "0.3.13"

source:
  fn: BDD4Django-0.3.13.tar.gz
  url: https://pypi.python.org/packages/source/B/BDD4Django/BDD4Django-0.3.13.tar.gz
  md5: 4513cee24650ab83f4b943c894f4bb29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bdd4django = bdd4django:main
    #
    # Would create an entry point called bdd4django that calls bdd4django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bdd4django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danielfranca/BDD4Django
  license: MIT License
  summary: '"Behavior Driven Development" (BDD) -- Integrating Morelia + Splinter + Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
