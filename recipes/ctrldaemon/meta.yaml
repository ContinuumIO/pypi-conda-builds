package:
  name: ctrldaemon
  version: "0.2"

source:
  fn: ctrldaemon-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/ctrldaemon/ctrldaemon-0.2.tar.gz
  md5: 66aa69b307d4c6715ef8ce06e2e947ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ctrldaemon = ctrldaemon:main
    #
    # Would create an entry point called ctrldaemon that calls ctrldaemon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psutil

  run:
    - python
    - psutil

test:
  # Python imports
  imports:
    - ctrldaemon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.yograterol.me
  license: BSD License
  summary: 'Service command wrapper for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
