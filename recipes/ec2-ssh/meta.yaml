package:
  name: ec2-ssh
  version: "1.2.1"

source:
  fn: ec2-ssh-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ec2-ssh/ec2-ssh-1.2.1.tar.gz
  md5: 38c3b3be7031f9cbe78f457a25e891a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ec2-ssh = ec2-ssh:main
    #
    # Would create an entry point called ec2-ssh that calls ec2-ssh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=1.0

  run:
    - python
    - boto >=1.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Instagram/ec2-ssh
  license: MIT License
  summary: 'SSH into EC2 instances via tag name'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
