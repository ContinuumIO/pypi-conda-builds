package:
  name: canonicalize
  version: "1.3"

source:
  fn: canonicalize-1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/canonicalize/canonicalize-1.3.tar.gz
  md5: 0a9508123b672d9f89c09980b535171d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - canonicalize = canonicalize:main
    #
    # Would create an entry point called canonicalize that calls canonicalize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - affinegap

  run:
    - python
    - numpy
    - affinegap

test:
  # Python imports
  imports:
    - canonicalize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datamade/canonicalize
  license: MIT License
  summary: 'canonicalize a cluster of records'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
