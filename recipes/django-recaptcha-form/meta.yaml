package:
  name: django-recaptcha-form
  version: "0.1.2"

source:
  fn: django-recaptcha-form-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-recaptcha-form/django-recaptcha-form-0.1.2.tar.gz
  md5: 20e366df29101baf4b0cabb18d577b5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-recaptcha-form = django-recaptcha-form:main
    #
    # Would create an entry point called django-recaptcha-form that calls django-recaptcha-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.6
    - recaptcha-client >=1.0.6

  run:
    - python
    - django >=1.6
    - recaptcha-client >=1.0.6

test:
  # Python imports
  imports:
    - recaptcha_form
    - recaptcha_form.account_backend
    - recaptcha_form.pinax_backend
    - recaptcha_form.registration_backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ntucker/django-recaptcha
  license: BSD License
  summary: 'Recaptcha for Django Forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
