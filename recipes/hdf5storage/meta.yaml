package:
  name: hdf5storage
  version: "0.1.4"

source:
  fn: hdf5storage-0.1.4.zip
  url: https://pypi.python.org/packages/source/h/hdf5storage/hdf5storage-0.1.4.zip
  md5: b02c2511ba85c5e6af06ab73d8f6c468
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hdf5storage = hdf5storage:main
    #
    # Would create an entry point called hdf5storage that calls hdf5storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hdf5storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frejanordsiek/hdf5storage
  license: BSD License
  summary: 'Utilities to read/write Python types to/from HDF5 files, including MATLAB v7.3 MAT files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
