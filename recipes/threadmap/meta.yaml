package:
  name: threadmap
  version: "0.1.1"

source:
  fn: threadmap-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/threadmap/threadmap-0.1.1.tar.gz
  md5: c438425914a3597a54b1a33ab44beed6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - threadmap = threadmap:main
    #
    # Would create an entry point called threadmap that calls threadmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - threadmap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/informationsea/threadmap
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Multi-thread map'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
