package:
  name: django-actuary
  version: "0.0.2.1dev"

source:
  fn: Django-Actuary-0.0.2.1dev.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Actuary/Django-Actuary-0.0.2.1dev.tar.gz
  md5: 0f602b380a5e376c595d70d67876f6cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-actuary = django-actuary:main
    #
    # Would create an entry point called django-actuary that calls django-actuary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - actuary

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Django-Actuary/
  license: LICENSE.txt
  summary: 'Django auditing application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
