package:
  name: freeform
  version: "0.3"

source:
  fn: freeform-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/freeform/freeform-0.3.tar.gz
  md5: 4d93caff62ffceee78209ce4c1199221
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - freeform = freeform:main
    #
    # Would create an entry point called freeform that calls freeform.main()

    - freeformc = freeform.tool:compile_tool

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - freeform
    - freeform_tests

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - freeformc --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.wiretooth.com/downloads/freeform.html
  license: Academic Free License (AFL) or MIT License
  summary: 'natural interpretation of loosely structured user phrases\n:Download: `freeform-0.3.tar.gz`_, `freeform-0.2-1.tar.gz`_'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
