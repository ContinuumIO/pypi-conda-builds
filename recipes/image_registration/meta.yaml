package:
  name: image_registration
  version: "0.2.1"

source:
  fn: image_registration-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/image_registration/image_registration-0.2.1.tar.gz
  md5: 1398211b9b3eeeb06e14b52baaadf314
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - image_registration = image_registration:main
    #
    # Would create an entry point called image_registration that calls image_registration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - image_registration
    - image_registration/FITS_tools
    - image_registration/fft_tools
    - image_registration/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keflavich/image_registration
  license: UNKNOWN
  summary: 'Image Registration Tools for extended images in astronomy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
