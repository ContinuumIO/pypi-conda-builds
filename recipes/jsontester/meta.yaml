package:
  name: jsontester
  version: "1.1.2"

source:
  fn: jsontester-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsontester/jsontester-1.1.2.tar.gz
  md5: 1e74e6c3393cfde058b294c129e0f24e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsontester = jsontester:main
    #
    # Would create an entry point called jsontester that calls jsontester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - nose
    - configobj
    - systematic >=4.0.4

  run:
    - python
    - requests >=1.2.3
    - nose
    - configobj
    - systematic >=4.0.4

test:
  # Python imports
  imports:
    - jsontester
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hile/jsontester
  license: PSF
  summary: 'Scripts to test JSON API requests from command line'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
