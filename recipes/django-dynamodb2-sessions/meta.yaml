package:
  name: django-dynamodb2-sessions
  version: "0.3"

source:
  fn: django-dynamodb2-sessions-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamodb2-sessions/django-dynamodb2-sessions-0.3.tar.gz
  md5: 057e177ea53685b5fe0bef97973abe53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamodb2-sessions = django-dynamodb2-sessions:main
    #
    # Would create an entry point called django-dynamodb2-sessions that calls django-dynamodb2-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - boto >=2.10.0

  run:
    - python
    - django
    - boto >=2.10.0

test:
  # Python imports
  imports:
    - dynamodb2_sessions
    - dynamodb2_sessions.backends
    - dynamodb2_sessions.management
    - dynamodb2_sessions.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Solution4Future/django-dynamodb2-sessions
  license: BSD License
  summary: 'Sessions backend dedicated for Django uses Amazon DynamoDB v.2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
