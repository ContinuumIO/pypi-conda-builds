package:
  name: hurr-durr
  version: "0.2.2"

source:
  fn: hurr-durr-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hurr-durr/hurr-durr-0.2.2.tar.gz
  md5: 26e8ef209690458539f7ac970021a4ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurr-durr = hurr-durr:main
    #
    # Would create an entry point called hurr-durr that calls hurr-durr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado ==4.0.2

  run:
    - python
    - tornado ==4.0.2

test:
  # Python imports
  imports:
    - hurr_durr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/emilssolmanis/hurr-durr
  license: Apache License (2.0)
  summary: 'Streaming async interface to 4chan boards'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
