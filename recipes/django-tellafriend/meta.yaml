package:
  name: django-tellafriend
  version: "0.0.10"

source:
  fn: django-tellafriend-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tellafriend/django-tellafriend-0.0.10.tar.gz
  md5: f480b576ad2c0df64154f8dd3c12276a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tellafriend = django-tellafriend:main
    #
    # Would create an entry point called django-tellafriend that calls django-tellafriend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tellafriend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/philippbosch/django-tellafriend/
  license: MIT License
  summary: 'Tell-a-friend functionality for Django-based websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
