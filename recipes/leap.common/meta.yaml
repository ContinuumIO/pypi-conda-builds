package:
  name: leap.common
  version: "0.3.10"

source:
  fn: leap.common-0.3.10.tar.gz
  url: https://pypi.python.org/packages/source/l/leap.common/leap.common-0.3.10.tar.gz
  md5: e9bed5aa46c6ad7bb32a64763f58928b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leap.common = leap.common:main
    #
    # Would create an entry point called leap.common that calls leap.common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema
    - dirspec
    - protobuf >=2.4.1
    - protobuf.socketrpc
    - pyopenssl
    - python-dateutil

  run:
    - python
    - jsonschema
    - dirspec
    - protobuf >=2.4.1
    - protobuf.socketrpc
    - pyopenssl
    - python-dateutil

test:
  # Python imports
  imports:
    - leap
    - leap.common
    - leap.common.config
    - leap.common.events
    - leap.common.testing
    - leap.common.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://leap.se/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Common files used by the LEAP project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
