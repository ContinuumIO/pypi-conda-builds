package:
  name: ikwi
  version: "0.2.1"

source:
  fn: ikwi-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ikwi/ikwi-0.2.1.tar.gz
  md5: 71d6e7a71fc954cd3cc8fa5539191362
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ikwi = ikwi:main
    #
    # Would create an entry point called ikwi that calls ikwi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - bcrypt >=1.1.1
    - html5lib ==0.999
    - jinja2 >=2.7.3
    - lxml >=3.4.3
    - pygit2 ==0.22.0
    - pypandoc ==0.9.7
    - pyyaml ==3.11
    - werkzeug >=0.10.4
    - whoosh >=2.7.0

  run:
    - python
    - bcrypt >=1.1.1
    - html5lib ==0.999
    - jinja2 >=2.7.3
    - lxml >=3.4.3
    - pygit2 ==0.22.0
    - pypandoc ==0.9.7
    - pyyaml ==3.11
    - werkzeug >=0.10.4
    - whoosh >=2.7.0

test:
  # Python imports
  imports:
    - ikwi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dpk/ikwi
  license: UNKNOWN
  summary: 'A personal wikiware.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
