package:
  name: collective.contentrules.runscript
  version: "0.4"

source:
  fn: collective.contentrules.runscript-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentrules.runscript/collective.contentrules.runscript-0.4.tar.gz
  md5: 74a82496bc38c3be1c0bc5c1c9e83809
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.runscript = collective.contentrules.runscript:main
    #
    # Would create an entry point called collective.contentrules.runscript that calls collective.contentrules.runscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.runscript
    - collective.contentrules.runscript.actions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.contentrules.runscript
  license: GPL
  summary: 'An action for the contentrules framework to run a script on the object that triggered the rule'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
