package:
  name: products.gopantspockets
  version: "1.0.0"

source:
  fn: Products.GoPantsPockets-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.GoPantsPockets/Products.GoPantsPockets-1.0.0.tar.gz
  md5: 7d3fb66db8b1bccf3e1b01c2c7ccdf07
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.gopantspockets = products.gopantspockets:main
    #
    # Would create an entry point called products.gopantspockets that calls products.gopantspockets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone
    - products.relations

  run:
    - python
    - setuptools
    - plone
    - products.relations

test:
  # Python imports
  imports:
    - Products
    - Products.GoPantsPockets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Products.GoPantsPockets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
