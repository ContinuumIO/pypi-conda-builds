package:
  name: mopidy-websettings
  version: "0.1.4.2"

source:
  fn: Mopidy-WebSettings-0.1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-WebSettings/Mopidy-WebSettings-0.1.4.2.tar.gz
  md5: b5dd6f78731f020385de36f471af0fbe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-websettings = mopidy-websettings:main
    #
    # Would create an entry point called mopidy-websettings that calls mopidy-websettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19
    - pykka >=1.1
    - configobj >=4.0.0
    - jinja2 >=2.7

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19
    - pykka >=1.1
    - configobj >=4.0.0
    - jinja2 >=2.7

test:
  # Python imports
  imports:
    - mopidy_websettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/woutervanwijk/mopidy-websettings
  license: Apache Software License
  summary: u'Mopidy extension for editing settings in a webinterface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
