package:
  name: django-mindscape
  version: "0.3.0"

source:
  fn: django-mindscape-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mindscape/django-mindscape-0.3.0.tar.gz
  md5: 4912982418af949d849cb330f5d6c405
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mindscape = django-mindscape:main
    #
    # Would create an entry point called django-mindscape that calls django-mindscape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - setuptools
    - django >=1.7

test:
  # Python imports
  imports:
    - django_mindscape
    - django_mindscape.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/podhmo/django-mindscape
  license: UNKNOWN
  summary: 'a library traversing django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
