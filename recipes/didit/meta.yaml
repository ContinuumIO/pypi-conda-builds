package:
  name: didit
  version: "0.3.0dev"

source:
  fn: didit-0.3.0dev.tar.gz
  url: https://pypi.python.org/packages/source/d/didit/didit-0.3.0dev.tar.gz
  md5: 7d356dee66486ab889d8cb9f834adc4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - didit = didit:main
    #
    # Would create an entry point called didit that calls didit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mako

  run:
    - python
    - setuptools
    - mako

test:
  # Python imports
  imports:
    - didit
    - didit.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/didit
  license: GNU General Public License (GPL)
  summary: 'Lightweight, commandline tool to remember what I did last week'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
