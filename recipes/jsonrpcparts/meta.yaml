package:
  name: jsonrpcparts
  version: "0.3.4"

source:
  fn: jsonrpcparts-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonrpcparts/jsonrpcparts-0.3.4.tar.gz
  md5: 32e54927945e57b9dc179445141c1c9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonrpcparts = jsonrpcparts:main
    #
    # Would create an entry point called jsonrpcparts that calls jsonrpcparts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - jsonrpcparts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dvdotsenko/jsonrpc.py
  license: MIT License
  summary: 'JSON-RPC client and server components'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
