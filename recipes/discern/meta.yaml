package:
  name: discern
  version: "0.1.0"

source:
  fn: discern-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/discern/discern-0.1.0.zip
  md5: e3b89706d39fd8659903a37706a17bc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - discern = discern:main
    #
    # Would create an entry point called discern that calls discern.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.8.0
    - rpy2 >=2.4.4
    - matplotlib >=1.3.1
    - pandas >=0.14.1

  run:
    - python
    - numpy >=1.8.0
    - rpy2 >=2.4.4
    - matplotlib >=1.3.1
    - pandas >=0.14.1

test:
  # Python imports
  imports:
    - discern

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/discern/
  license: LICENSE.txt
  summary: 'DISCERN is a method for identifying perturbations between two similar graphs. It takes in samples from two networks, and identifies which features are conditionally dependent on different features between the two'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
