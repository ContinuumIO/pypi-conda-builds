package:
  name: horizon_api_mask
  version: "0.0.1.post2"

source:
  fn: horizon_api_mask-0.0.1.post2.tar.gz
  url: https://pypi.python.org/packages/source/h/horizon_api_mask/horizon_api_mask-0.0.1.post2.tar.gz
  md5: 4e1bba7bb0986b207937d7acb66d888b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horizon_api_mask = horizon_api_mask:main
    #
    # Would create an entry point called horizon_api_mask that calls horizon_api_mask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - horizon_api_mask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Dashboard API mask - override service endpoint table'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
