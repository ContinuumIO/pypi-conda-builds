package:
  name: clang
  version: "3.7.dev234765"

source:
  fn: clang-3.7.dev234765.tar.gz
  url: https://pypi.python.org/packages/source/c/clang/clang-3.7.dev234765.tar.gz
  md5: ba92345673b5cdeee030685b5b4f058c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clang = clang:main
    #
    # Would create an entry point called clang that calls clang.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clang

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trolldbois/python-clang
  license: GNU General Public License (GPL)
  summary: 'libclang python bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
