package:
  name: django-report
  version: "0.1"

source:
  fn: django-report-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-report/django-report-0.1.tar.gz
  md5: f8870ffbeeec63f01c1ba21ad55e343c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-report = django-report:main
    #
    # Would create an entry point called django-report that calls django-report.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - requests

  run:
    - python
    - django
    - requests

test:
  # Python imports
  imports:
    - report
    - report.backends
    - report.management
    - report.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mining/django-report/
  license: MIT License
  summary: 'Pluggable model report for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
