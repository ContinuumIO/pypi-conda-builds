package:
  name: django-sms-support
  version: "0.1.3"

source:
  fn: django-sms-support-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sms-support/django-sms-support-0.1.3.tar.gz
  md5: 9fed6c4a030b53fe667b8709db03f744
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sms-support = django-sms-support:main
    #
    # Would create an entry point called django-sms-support that calls django-sms-support.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sms-support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ona.io/
  license: BSD License
  summary: 'SMS support for OnaData.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
