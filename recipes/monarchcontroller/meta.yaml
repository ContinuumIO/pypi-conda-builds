package:
  name: monarchcontroller
  version: "0.1"

source:
  fn: monarchcontroller-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/monarchcontroller/monarchcontroller-0.1.tar.gz
  md5: e46821548cbdbb46baa4886d8db885e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monarchcontroller = monarchcontroller:main
    #
    # Would create an entry point called monarchcontroller that calls monarchcontroller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - monarchcontroller

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smpsolutions/monarchcontroller
  license: UNKNOWN
  summary: 'Python Library to manage Matrox Monarch HD Devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
