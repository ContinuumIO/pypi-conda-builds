package:
  name: zeam.form.viewlet
  version: "1.2.1"

source:
  fn: zeam.form.viewlet-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zeam.form.viewlet/zeam.form.viewlet-1.2.1.tar.gz
  md5: ea2aabe5cef8b12ddcb486b460b91040
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeam.form.viewlet = zeam.form.viewlet:main
    #
    # Would create an entry point called zeam.form.viewlet that calls zeam.form.viewlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - grokcore.component
    - grokcore.view
    - grokcore.viewlet
    - grokcore.chameleon
    - martian
    - megrok.pagetemplate
    - setuptools
    - zeam.form.base >=1.0

  run:
    - python
    - grokcore.component
    - grokcore.view
    - grokcore.viewlet
    - grokcore.chameleon
    - martian
    - megrok.pagetemplate
    - setuptools
    - zeam.form.base >=1.0

test:
  # Python imports
  imports:
    - zeam
    - zeam.form
    - zeam.form.viewlet
    - zeam.form.viewlet.ftests
    - zeam.form.viewlet.ftests.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zeam.form.viewlet
  license: BSD
  summary: 'zeam.form forms in a viewlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
