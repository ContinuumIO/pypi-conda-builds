package:
  name: django-cached_authentication_middleware
  version: "0.2.0"

source:
  fn: django-cached_authentication_middleware-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cached_authentication_middleware/django-cached_authentication_middleware-0.2.0.tar.gz
  md5: 882c86ab0afac86d4f3a12b691f812e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cached_authentication_middleware = django-cached_authentication_middleware:main
    #
    # Would create an entry point called django-cached_authentication_middleware that calls django-cached_authentication_middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cached_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ui/django-cached_authentication_middleware
  license: MIT License
  summary: "A drop in replacement for django's built in AuthenticationMiddleware that utilizes caching."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
