package:
  name: secure-smtplib
  version: "0.1.1"

source:
  fn: secure-smtplib-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/secure-smtplib/secure-smtplib-0.1.1.tar.gz
  md5: 7f534e0344cf387688343821c8e41557
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secure-smtplib = secure-smtplib:main
    #
    # Would create an entry point called secure-smtplib that calls secure-smtplib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - secure_smtplib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/graingert/secure-smtplib
  license: UNKNOWN
  summary: 'Secure SMTP subclasses for Python 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
