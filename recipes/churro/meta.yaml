package:
  name: churro
  version: "1.0a3"

source:
  fn: churro-1.0a3.tar.gz
  url: https://pypi.python.org/packages/source/c/churro/churro-1.0a3.tar.gz
  md5: 5f154882e2c01d2d759b0ba3aa1480aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - churro = churro:main
    #
    # Would create an entry point called churro that calls churro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - acidfs

  run:
    - python
    - acidfs

test:
  # Python imports
  imports:
    - churro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pylonsproject.org
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Simple object persistence using JSON and AcidFS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
