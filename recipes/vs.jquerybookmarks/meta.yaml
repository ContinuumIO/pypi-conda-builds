package:
  name: vs.jquerybookmarks
  version: "0.1.5"

source:
  fn: vs.jquerybookmarks-0.1.5.zip
  url: https://pypi.python.org/packages/source/v/vs.jquerybookmarks/vs.jquerybookmarks-0.1.5.zip
  md5: a2903ffdd9bfa5ff926858eb3036d1e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vs.jquerybookmarks = vs.jquerybookmarks:main
    #
    # Would create an entry point called vs.jquerybookmarks that calls vs.jquerybookmarks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - vs
    - vs.jquerybookmarks
    - vs.jquerybookmarks.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/vs.jquerybookmarks
  license: GPL
  summary: 'jquery.bookmark integration with Plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
