package:
  name: plone.app.iterate
  version: "3.0.1"

source:
  fn: plone.app.iterate-3.0.1.zip
  url: https://pypi.python.org/packages/source/p/plone.app.iterate/plone.app.iterate-3.0.1.zip
  md5: aef0dae4e156e02211f9b38a2f0256b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.iterate = plone.app.iterate:main
    #
    # Would create an entry point called plone.app.iterate that calls plone.app.iterate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.locking
    - plone.memoize
    - zope.annotation
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.viewlet
    - acquisition
    - datetime
    - products.archetypes
    - products.cmfcore
    - products.cmfeditions
    - products.cmfplacefulworkflow
    - products.dcworkflow
    - products.statusmessages
    - zodb3
    - zope2

  run:
    - python
    - setuptools
    - setuptools
    - plone.locking
    - plone.memoize
    - zope.annotation
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.viewlet
    - acquisition
    - datetime
    - products.archetypes
    - products.cmfcore
    - products.cmfeditions
    - products.cmfplacefulworkflow
    - products.dcworkflow
    - products.statusmessages
    - zodb3
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.iterate
    - plone.app.iterate.browser
    - plone.app.iterate.subscribers
    - plone.app.iterate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.iterate
  license: GNU General Public License (GPL)
  summary: 'check-out/check-in staging for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
