package:
  name: agileclu
  version: "0.4.2"

source:
  fn: AgileCLU-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/A/AgileCLU/AgileCLU-0.4.2.tar.gz
  md5: d93f9cdbd2da33cf847c04a06a390ade
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agileclu = agileclu:main
    #
    # Would create an entry point called agileclu that calls agileclu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - poster
    - progressbar
    - pydes
    - jsonrpclib

  run:
    - python
    - poster
    - progressbar
    - pydes
    - jsonrpclib

test:
  # Python imports
  imports:
    - AgileCLU
    - agileclu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/AgileCLU
  license: BSD License
  summary: 'Agile Command Line Utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
