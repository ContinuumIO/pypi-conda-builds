package:
  name: forestbus
  version: "1.0"

source:
  fn: ForestBus-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/ForestBus/ForestBus-1.0.3.tar.gz
  md5: 0486a7b2e07f9f8d2d89534641f36c5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - forestbus = forestbus:main
    #
    # Would create an entry point called forestbus that calls forestbus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - forestbus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.owlfish.com/software/ForestBus/
  license: MIT License
  summary: 'ForestBus Python Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
