package:
  name: collective.randomcontent
  version: "1.0"

source:
  fn: collective.randomcontent-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.randomcontent/collective.randomcontent-1.0.zip
  md5: 3e7dd31d392ffdc27200da16f07ace05
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.randomcontent = collective.randomcontent:main
    #
    # Would create an entry point called collective.randomcontent that calls collective.randomcontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.randomcontent
    - collective.randomcontent.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.randomcontent
  license: GPL
  summary: 'Redirect to random content, especially images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
