package:
  name: django-emailer
  version: "0.2.1"

source:
  fn: django-emailer-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailer/django-emailer-0.2.1.tar.gz
  md5: bfa7d4d2fea544abc63205bbc43b4cd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailer = django-emailer:main
    #
    # Would create an entry point called django-emailer that calls django-emailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - emailer
    - emailer.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/7ws/django-emailer
  license: MIT License
  summary: 'Template based email sending with SMTP connection management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
