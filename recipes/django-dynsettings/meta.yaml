package:
  name: django-dynsettings
  version: "0.1.0"

source:
  fn: django-dynsettings-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-dynsettings/django-dynsettings-0.1.0.zip
  md5: c2aaf719790079fc67baca82392caabb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynsettings = django-dynsettings:main
    #
    # Would create an entry point called django-dynsettings that calls django-dynsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.1.1

  run:
    - python
    - django >=1.1.1

test:
  # Python imports
  imports:
    - dynsettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Django Dynamic Settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
