package:
  name: django-inline-wrestler
  version: "4.4"

source:
  fn: django-inline-wrestler-4.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inline-wrestler/django-inline-wrestler-4.4.tar.gz
  md5: dbae78807e515aee35c1f5c6efe7dd6e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inline-wrestler = django-inline-wrestler:main
    #
    # Would create an entry point called django-inline-wrestler that calls django-inline-wrestler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-grappelli

  run:
    - python
    - setuptools
    - django
    - django-grappelli

test:
  # Python imports
  imports:
    - django_inline_wrestler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ninapavlich/django-inline-wrestler
  license: BSD
  summary: 'Wrestle your django inlines with jQuery and Grappelli.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
