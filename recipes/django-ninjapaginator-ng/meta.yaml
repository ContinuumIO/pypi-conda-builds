package:
  name: django-ninjapaginator-ng
  version: "0.1.6"

source:
  fn: django-ninjapaginator-ng-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ninjapaginator-ng/django-ninjapaginator-ng-0.1.6.tar.gz
  md5: a9fade3b052c3b6c35a48f2f4c270458
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ninjapaginator-ng = django-ninjapaginator-ng:main
    #
    # Would create an entry point called django-ninjapaginator-ng that calls django-ninjapaginator-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-annoying

  run:
    - python
    - django-annoying

test:
  # Python imports
  imports:
    - ninjapaginator
    - ninjapaginator.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/barbuza/django-ninjapaginator/
  license: BSD
  summary: 'Django application with multiple type of pagination integrated'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
