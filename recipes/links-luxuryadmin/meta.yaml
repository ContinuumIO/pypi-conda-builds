package:
  name: links-luxuryadmin
  version: "0.1.16"

source:
  fn: links-luxuryadmin-0.1.16.tar.gz
  url: https://pypi.python.org/packages/source/l/links-luxuryadmin/links-luxuryadmin-0.1.16.tar.gz
  md5: 4b827c0646fdd97f1113d86fd959abc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - links-luxuryadmin = links-luxuryadmin:main
    #
    # Would create an entry point called links-luxuryadmin that calls links-luxuryadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - luxuryadmin
    - luxuryadmin.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linkscreative.co.uk/
  license: LICENSE.txt
  summary: 'Luxury Admin for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
