package:
  name: chrome-edit-server-gmail-filter
  version: "0.3.1"

source:
  fn: chrome-edit-server-gmail-filter-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chrome-edit-server-gmail-filter/chrome-edit-server-gmail-filter-0.3.1.tar.gz
  md5: 3096aebc5a7369394b056c3c8ed0eb7e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chrome-edit-server-gmail-filter = chrome-edit-server-gmail-filter:main
    #
    # Would create an entry point called chrome-edit-server-gmail-filter that calls chrome-edit-server-gmail-filter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six <1.8.0,>=1.7.3
    - html2text ==2014.7.3

  run:
    - python
    - setuptools
    - six <1.8.0,>=1.7.3
    - html2text ==2014.7.3

test:
  # Python imports
  imports:
    - chrome_edit_server_gmail_filter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coddingtonbear/chrome-edit-server-gmail-filter
  license: UNKNOWN
  summary: 'A chrome-edit-server plugin allowing one to edit Gmail messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
