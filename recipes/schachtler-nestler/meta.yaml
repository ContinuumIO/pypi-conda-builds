package:
  name: schachtler-nestler
  version: "1.0.0"

source:
  fn: schachtler-nestler-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/schachtler-nestler/schachtler-nestler-1.0.0.tar.gz
  md5: 7156dad17511c8b028f419e38cc259c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - schachtler-nestler = schachtler-nestler:main
    #
    # Would create an entry point called schachtler-nestler that calls schachtler-nestler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hshl.de/vita-prof-dr-simon-nestler/
  license: UNKNOWN
  summary: 'Eine einfache Demo zu Programmieren 1 an der HSHL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
