package:
  name: pignacio_scripts
  version: "0.0.2"

source:
  fn: pignacio_scripts-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pignacio_scripts/pignacio_scripts-0.0.2.tar.gz
  md5: 5febc917b2970fafa1e88ee4ce4a6b56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pignacio_scripts = pignacio_scripts:main
    #
    # Would create an entry point called pignacio_scripts that calls pignacio_scripts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - pignacio_scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pignacio/pignacio_scripts
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Reusable python scripts and snippets I find useful'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
