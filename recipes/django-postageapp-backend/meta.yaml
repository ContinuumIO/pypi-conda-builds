package:
  name: django-postageapp-backend
  version: "1.0.3"

source:
  fn: django-postageapp-backend-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-postageapp-backend/django-postageapp-backend-1.0.3.tar.gz
  md5: 2a138238d312e92ddf4e10412f5272b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-postageapp-backend = django-postageapp-backend:main
    #
    # Would create an entry point called django-postageapp-backend that calls django-postageapp-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - django >=1.3

  run:
    - python
    - setuptools >=0.6b1
    - django >=1.3

test:
  # Python imports
  imports:
    - postageapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Postageapp email backend for Django framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
