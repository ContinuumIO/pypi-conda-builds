package:
  name: django-qrmixin
  version: "1.0.1"

source:
  fn: django-qrmixin-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qrmixin/django-qrmixin-1.0.1.tar.gz
  md5: 971f35349a61d55e4c6673d3473b7f79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qrmixin = django-qrmixin:main
    #
    # Would create an entry point called django-qrmixin that calls django-qrmixin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - qrcode

  run:
    - python
    - qrcode

test:
  # Python imports
  imports:
    - qrmixin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/freakypie/django-qrmixin
  license: UNKNOWN
  summary: 'Provides a QR code generating mixin for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
