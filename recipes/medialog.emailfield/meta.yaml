package:
  name: medialog.emailfield
  version: "0.1b1"

source:
  fn: medialog.emailfield-0.1b1.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.emailfield/medialog.emailfield-0.1b1.tar.gz
  md5: 2e9a9c2581475e7c2132de1b23534d23
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.emailfield = medialog.emailfield:main
    #
    # Would create an entry point called medialog.emailfield that calls medialog.emailfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.archetypes

  run:
    - python
    - setuptools
    - setuptools
    - products.archetypes

test:
  # Python imports
  imports:
    - medialog
    - medialog.emailfield
    - medialog.emailfield.content
    - medialog.emailfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GPL
  summary: 'Archetypes Widget dealing with email.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
