package:
  name: mondb
  version: "0.0.2.5"

source:
  fn: mondb-0.0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mondb/mondb-0.0.2.5.tar.gz
  md5: 82084d3a39229673c06d6291a94b69ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mondb = mondb:main
    #
    # Would create an entry point called mondb that calls mondb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo ==2.8
    - six >=1.9.0

  run:
    - python
    - pymongo ==2.8
    - six >=1.9.0

test:
  # Python imports
  imports:
    - mondb
    - mondb/urltools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RevelutionWind/MDB.git
  license: GNU General Public License (GPL)
  summary: 'MongoDB Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
