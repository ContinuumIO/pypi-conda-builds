package:
  name: plone.directives.form
  version: "2.0.1"

source:
  fn: plone.directives.form-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.directives.form/plone.directives.form-2.0.1.tar.gz
  md5: c04c80c710b270156dcb0d58a8dbc339
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.directives.form = plone.directives.form:main
    #
    # Would create an entry point called plone.directives.form that calls plone.directives.form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.deferredimport
    - five.grok >=1.0b2
    - grokcore.view >=2.2
    - plone.autoform >=1.2dev
    - plone.z3cform >=0.6.0
    - products.statusmessages
    - plone.supermodel >=1.1dev
    - zope.i18nmessageid
    - zope.publisher
    - plone.rfc822

  run:
    - python
    - setuptools
    - setuptools
    - zope.deferredimport
    - five.grok >=1.0b2
    - grokcore.view >=2.2
    - plone.autoform >=1.2dev
    - plone.z3cform >=0.6.0
    - products.statusmessages
    - plone.supermodel >=1.1dev
    - zope.i18nmessageid
    - zope.publisher
    - plone.rfc822

test:
  # Python imports
  imports:
    - plone
    - plone.directives
    - plone.directives.form
    - plone.directives.form.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dexterity
  license: GPL
  summary: 'Grok-like directives configuring forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
