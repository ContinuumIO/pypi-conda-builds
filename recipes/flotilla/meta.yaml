package:
  name: flotilla
  version: "0.2.5"

source:
  fn: flotilla-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/f/flotilla/flotilla-0.2.5.tar.gz
  md5: b69b87051cf4de4ae02a75e5639331dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flotilla = flotilla:main
    #
    # Would create an entry point called flotilla that calls flotilla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8.0
    - scipy >=0.14
    - matplotlib >=1.3.1
    - scikit-learn >=0.13.0
    - gspread
    - brewer2mpl
    - pymongo >=2.7
    - ipython >=2.0.0
    - husl
    - patsy >=0.2.1
    - pandas >=0.13.1
    - statsmodels >=0.5.0
    - seaborn >=0.3
    - networkx
    - tornado >=3.2.1
    - pyzmq
    - six
    - pytest-cov
    - python-coveralls
    - jinja2
    - semantic_version
    - joblib >=0.8.4

  run:
    - python
    - setuptools
    - numpy >=1.8.0
    - scipy >=0.14
    - matplotlib >=1.3.1
    - scikit-learn >=0.13.0
    - gspread
    - brewer2mpl
    - pymongo >=2.7
    - ipython >=2.0.0
    - husl
    - patsy >=0.2.1
    - pandas >=0.13.1
    - statsmodels >=0.5.0
    - seaborn >=0.3
    - networkx
    - tornado >=3.2.1
    - pyzmq
    - six
    - pytest-cov
    - python-coveralls
    - jinja2
    - semantic_version
    - joblib >=0.8.4

test:
  # Python imports
  imports:
    - flotilla
    - flotilla.compute
    - flotilla.data_model
    - flotilla.test
    - flotilla.visualize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/YeoLab/flotilla
  license: BSD License
  summary: 'Embark on a journey of single-cell data exploration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
