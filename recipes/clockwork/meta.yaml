package:
  name: clockwork
  version: "1.1.0"

source:
  fn: Clockwork-1.1.0.zip
  url: https://pypi.python.org/packages/source/C/Clockwork/Clockwork-1.1.0.zip
  md5: 87a98a36e6396db67b242bd576b954a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clockwork = clockwork:main
    #
    # Would create an entry point called clockwork that calls clockwork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - clockwork

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mediaburst/clockwork-python
  license: MIT
  summary: 'Python wrapper for the clockwork SMS Api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
