package:
  name: django-easy-watermark
  version: "0.1.7-final"

source:
  fn: django-easy-watermark-0.1.7-final.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-watermark/django-easy-watermark-0.1.7-final.tar.gz
  md5: 2d326e95aaae3d01cf8f86e2e278e13e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-watermark = django-easy-watermark:main
    #
    # Would create an entry point called django-easy-watermark that calls django-easy-watermark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cache-machine

  run:
    - python
    - django-cache-machine

test:
  # Python imports
  imports:
    - watermarker
    - watermarker.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/codekoala/django-watermark/
  license: BSD License
  summary: 'Quick and efficient way to apply watermarks to images in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
