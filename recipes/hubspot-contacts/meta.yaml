package:
  name: hubspot-contacts
  version: "1.0"

source:
  fn: hubspot-contacts-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hubspot-contacts/hubspot-contacts-1.0.tar.gz
  md5: 7b4edeaeb228852a0b3f6ebb4885cb2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hubspot-contacts = hubspot-contacts:main
    #
    # Would create an entry point called hubspot-contacts that calls hubspot-contacts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hubspot-connection >=1.0rc2
    - pyrecord >=1.0a1
    - voluptuous >=0.8.4

  run:
    - python
    - hubspot-connection >=1.0rc2
    - pyrecord >=1.0a1
    - voluptuous >=0.8.4

test:
  # Python imports
  imports:
    - hubspot
    - hubspot.contacts
    - hubspot.contacts._schemas
    - hubspot.contacts.request_data_formatters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/hubspot-contacts/
  license: BSD License
  summary: 'Pythonic wrapper for HubSpot API methods in the Contacts, Contact Lists and Contact Properties APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
