package:
  name: ukpostcodeparser
  version: "1.0.3"

source:
  fn: UkPostcodeParser-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/U/UkPostcodeParser/UkPostcodeParser-1.0.3.tar.gz
  md5: 929fec4374d48e926032373e8dc552e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ukpostcodeparser = ukpostcodeparser:main
    #
    # Would create an entry point called ukpostcodeparser that calls ukpostcodeparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ukpostcodeparser
    - ukpostcodeparser.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hamstah/ukpostcodeparser
  license: MIT
  summary: 'UK Postcode parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
