package:
  name: columnize
  version: "0.3.8"

source:
  fn: columnize-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/c/columnize/columnize-0.3.8.tar.gz
  md5: c8ffef9c94c6ccd11e75be15f8c2fc54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - columnize = columnize:main
    #
    # Would create an entry point called columnize that calls columnize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - backports.shutil_get_terminal_size

  run:
    - python
    - backports.shutil_get_terminal_size

test:
  # Python imports
  imports:
    - columnize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rocky/pycolumnize
  license: Python Software Foundation License
  summary: 'Format a simple (i.e. not nested) list into aligned columns.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
