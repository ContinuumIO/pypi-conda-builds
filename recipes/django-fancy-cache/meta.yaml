package:
  name: django-fancy-cache
  version: "0.6.0"

source:
  fn: django-fancy-cache-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fancy-cache/django-fancy-cache-0.6.0.tar.gz
  md5: 47557c3418047f80d3efebb943614d8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fancy-cache = django-fancy-cache:main
    #
    # Would create an entry point called django-fancy-cache that calls django-fancy-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fancy_cache
    - fancy_cache.management
    - fancy_cache.management.commands
    - fancy_tests
    - fancy_tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/peterbe/django-fancy-cache
  license: BSD License
  summary: 'clears the junk out of your CSS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
