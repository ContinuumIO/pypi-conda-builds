package:
  name: django-multimail
  version: "0.1.4"

source:
  fn: django-multimail-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multimail/django-multimail-0.1.4.tar.gz
  md5: da755f7ba3664b6a872e284920ffb373
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multimail = django-multimail:main
    #
    # Would create an entry point called django-multimail that calls django-multimail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demo
    - demo.core
    - demo.core.settings
    - demo.main
    - multimail
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-multimail.com
  license: MIT License
  summary: 'Enable multiple email addresses per user in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
