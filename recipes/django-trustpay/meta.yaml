package:
  name: django-trustpay
  version: "0.2.2"

source:
  fn: django-trustpay-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-trustpay/django-trustpay-0.2.2.tar.gz
  md5: db775f672e03ae548b994b1e217665eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-trustpay = django-trustpay:main
    #
    # Would create an entry point called django-trustpay that calls django-trustpay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - trustpay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-trustpay
  license: BSD License
  summary: 'Django app for Trustpay payment gateway API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
