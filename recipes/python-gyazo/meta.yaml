package:
  name: python-gyazo
  version: "0.7.0"

source:
  fn: python-gyazo-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gyazo/python-gyazo-0.7.0.tar.gz
  md5: a4a9d390b44a3a6c07e38a6a203bea80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gyazo = python-gyazo:main
    #
    # Would create an entry point called python-gyazo that calls python-gyazo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.7.0
    - progress >=1.2
    - python-dateutil >=2.4
    - requests >=2.5.0
    - six >=1.9.0
    - futures >=2.2.0

  run:
    - python
    - jinja2 >=2.7.0
    - progress >=1.2
    - python-dateutil >=2.4
    - requests >=2.5.0
    - six >=1.9.0
    - futures >=2.2.0

test:
  # Python imports
  imports:
    - gyazo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ymyzk/python-gyazo
  license: MIT License
  summary: 'A Python wrapper for Gyazo API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
