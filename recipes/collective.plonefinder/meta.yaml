package:
  name: collective.plonefinder
  version: "1.0.7"

source:
  fn: collective.plonefinder-1.0.7.zip
  url: https://pypi.python.org/packages/source/c/collective.plonefinder/collective.plonefinder-1.0.7.zip
  md5: 8fb71fb440bc06c91418642b03f366af
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.plonefinder = collective.plonefinder:main
    #
    # Would create an entry point called collective.plonefinder that calls collective.plonefinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.quickupload

  run:
    - python
    - setuptools
    - setuptools
    - collective.quickupload

test:
  # Python imports
  imports:
    - collective
    - collective.plonefinder
    - collective.plonefinder.browser
    - collective.plonefinder.interfaces
    - collective.plonefinder.tests
    - collective.plonefinder.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.alterway.fr
  license: GNU General Public License (GPL)
  summary: 'A finder to search/select portal objects for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
