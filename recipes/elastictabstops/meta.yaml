package:
  name: elastictabstops
  version: "1.0.0"

source:
  fn: ElasticTabstops-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/E/ElasticTabstops/ElasticTabstops-1.0.0.tar.gz
  md5: a0e474dee5c532e684b2550bb86a2a81
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elastictabstops = elastictabstops:main
    #
    # Would create an entry point called elastictabstops that calls elastictabstops.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - elastictabstops

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nickgravgaard.com/elastictabstops/
  license: MIT License
  summary: 'Converts text indented/aligned with elastic tabstops'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
