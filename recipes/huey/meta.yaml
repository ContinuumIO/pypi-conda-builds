package:
  name: huey
  version: "0.4.7"

source:
  fn: huey-0.4.7.tar.gz
  url: https://pypi.python.org/packages/source/h/huey/huey-0.4.7.tar.gz
  md5: aa4032d3218cff73c841c72b241a2969
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - huey = huey:main
    #
    # Would create an entry point called huey that calls huey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - huey
    - huey.backends
    - huey.bin
    - huey.djhuey
    - huey.djhuey.management
    - huey.djhuey.management.commands
    - huey.peewee_helpers
    - huey.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coleifer/huey/
  license: MIT License
  summary: 'huey, a little task queue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
