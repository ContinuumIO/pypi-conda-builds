package:
  name: django-template-repl
  version: "0.3.0"

source:
  fn: django-template-repl-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-repl/django-template-repl-0.3.0.tar.gz
  md5: 389e7df1b844905c6b3653d5a44854a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-repl = django-template-repl:main
    #
    # Would create an entry point called django-template-repl that calls django-template-repl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - template_repl
    - template_repl.management
    - template_repl.management.commands
    - template_repl.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codysoyland/django-template-repl/
  license: BSD License
  summary: 'A readline shell for the Django template language.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
