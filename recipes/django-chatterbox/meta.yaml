package:
  name: django-chatterbox
  version: "0.0.4"

source:
  fn: django-chatterbox-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chatterbox/django-chatterbox-0.0.4.tar.gz
  md5: c064d90be8a40e20d84bd297f96ab30b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chatterbox = django-chatterbox:main
    #
    # Would create an entry point called django-chatterbox that calls django-chatterbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonfield ==1.0.1
    - requests ==2.5.3
    - oauthlib ==0.7.2
    - requests ==2.5.1
    - requests-oauthlib ==0.4.2

  run:
    - python
    - jsonfield ==1.0.1
    - requests ==2.5.3
    - oauthlib ==0.7.2
    - requests ==2.5.1
    - requests-oauthlib ==0.4.2

test:
  # Python imports
  imports:
    - chatterbox
    - chatterbox.api
    - chatterbox.collectors
    - chatterbox.drivers
    - chatterbox.migrations
    - chatterbox.signals
    - chatterbox.tests
    - chatterbox.tests.api
    - chatterbox.tests.collectors
    - chatterbox.tests.services
    - chatterbox.utils
    - chatterbox.views
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blitzagency/django-chatterbox
  license: BSD License
  summary: 'Social listening made easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
