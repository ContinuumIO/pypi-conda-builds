package:
  name: bitstampclient
  version: "2.0.3"

source:
  fn: BitstampClient-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/B/BitstampClient/BitstampClient-2.0.3.tar.gz
  md5: c9e88886222a081a296893aeb6682b30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitstampclient = bitstampclient:main
    #
    # Would create an entry point called bitstampclient that calls bitstampclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - bitstamp
    - bitstampclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Bitstamp API python implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
