package:
  name: django-secureadmin
  version: "1.4"

source:
  fn: django-secureadmin-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-secureadmin/django-secureadmin-1.4.tar.gz
  md5: 2f739b081653cfc24f77b495bd749a23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-secureadmin = django-secureadmin:main
    #
    # Would create an entry point called django-secureadmin that calls django-secureadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - secureadmin
    - secureadmin.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/irandjango/django-secureadmin/
  license: MIT License
  summary: 'Django-SecureAdmin send verification mail when user last and new ip not equals.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
