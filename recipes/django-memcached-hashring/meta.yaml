package:
  name: django-memcached-hashring
  version: "0.1.2"

source:
  fn: django-memcached-hashring-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-memcached-hashring/django-memcached-hashring-0.1.2.tar.gz
  md5: e625b5108f2e0e03096242971cefafff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-memcached-hashring = django-memcached-hashring:main
    #
    # Would create an entry point called django-memcached-hashring that calls django-memcached-hashring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached
    - hash_ring

  run:
    - python
    - python-memcached
    - hash_ring

test:
  # Python imports
  imports:
    - memcached_hashring

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jezdez/django-memcached-hashring
  license: BSD License
  summary: 'A Django cache backend for Memcached with consistent consistent hashing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
