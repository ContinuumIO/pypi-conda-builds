package:
  name: pys3fs
  version: "0.2"

source:
  fn: pyS3fs-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyS3fs/pyS3fs-0.2.tar.gz
  md5: 82c95ee6bb667f2ddec5871ccded4c12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pys3fs = pys3fs:main
    #
    # Would create an entry point called pys3fs that calls pys3fs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fusepy
    - boto

  run:
    - python
    - fusepy
    - boto

test:
  # Python imports
  imports:
    - pyS3fs
    - pyS3fs.fs
    - pyS3fs.net
    - pys3fs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rbaron/pyS3fs
  license: UNKNOWN
  summary: "Mount Amazon's S3 bucket as a filesystem"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
