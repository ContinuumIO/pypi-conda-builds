package:
  name: sphinx.themes.plone
  version: "0.1.1"

source:
  fn: sphinx.themes.plone-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx.themes.plone/sphinx.themes.plone-0.1.1.tar.gz
  md5: 78cc6c7d61ade8ff075ee245b09ef3f8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx.themes.plone = sphinx.themes.plone:main
    #
    # Would create an entry point called sphinx.themes.plone that calls sphinx.themes.plone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sphinx
    - docutils
    - pygments

  run:
    - python
    - setuptools
    - setuptools
    - sphinx
    - docutils
    - pygments

test:
  # Python imports
  imports:
    - sphinx
    - sphinx.themes
    - sphinx.themes.plone
    - sphinx.themes.plone.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/sphinx.themes.plone.git
  license: GNU General Public License (GPL)
  summary: 'Collection of Sphinx Themes for Plone Documentations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
