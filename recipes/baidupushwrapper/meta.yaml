package:
  name: baidupushwrapper
  version: "0.0.0.2.pre"

source:
  fn: baiduPushWrapper-0.0.0.2.pre.tar.gz
  url: https://pypi.python.org/packages/source/b/baiduPushWrapper/baiduPushWrapper-0.0.0.2.pre.tar.gz
  md5: 82bc68d620442d84410ee267a4401a4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baidupushwrapper = baidupushwrapper:main
    #
    # Would create an entry point called baidupushwrapper that calls baidupushwrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.0

  run:
    - python
    - requests >=1.2.0

test:
  # Python imports
  imports:
    - baiduPushWrapper
    - baidupushwrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ipconfiger/baiduPushWrapper
  license: LGPL
  summary: 'Wrapper of baidu push api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
