package:
  name: ubuntupaste
  version: "0.0.1"

source:
  fn: UbuntuPaste-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/U/UbuntuPaste/UbuntuPaste-0.0.1.tar.gz
  md5: d52e6e99212d504fe868489230a843db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ubuntupaste = ubuntupaste:main
    #
    # Would create an entry point called ubuntupaste that calls ubuntupaste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ubuntupaste

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hxy9243/UbuntuPaste
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'A gadget to paste file to UbuntuPaste from cli'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
