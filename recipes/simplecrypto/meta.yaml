package:
  name: simplecrypto
  version: "0.3.2"

source:
  fn: simplecrypto-0.3.2.zip
  url: https://pypi.python.org/packages/source/s/simplecrypto/simplecrypto-0.3.2.zip
  md5: 2d0521b48e812019bcfa0cb2bbfbfdaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplecrypto = simplecrypto:main
    #
    # Would create an entry point called simplecrypto that calls simplecrypto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto

  run:
    - python
    - pycrypto

test:
  # Python imports
  imports:
    - simplecrypto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/boppreh/simplecrypto
  license: MIT License
  summary: 'Simple cryptographic library for hashing and encrypting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
