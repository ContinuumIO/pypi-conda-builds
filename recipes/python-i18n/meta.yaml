package:
  name: python-i18n
  version: "0.3.1"

source:
  fn: python-i18n-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-i18n/python-i18n-0.3.1.tar.gz
  md5: 08b8c247add9f90806c4742a685fdb88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-i18n = python-i18n:main
    #
    # Would create an entry point called python-i18n that calls python-i18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - i18n
    - i18n.loaders
    - i18n.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tuvistavie/python-i18n
  license: MIT License
  summary: 'Translation library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
