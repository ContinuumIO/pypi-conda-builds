package:
  name: l1l2signature
  version: "0.2.2"

source:
  fn: L1L2Signature-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/L/L1L2Signature/L1L2Signature-0.2.2.tar.gz
  md5: f716f158fe4c17940ad438e08c21e63c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - l1l2signature = l1l2signature:main
    #
    # Would create an entry point called l1l2signature that calls l1l2signature.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - l1l2signature

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://slipguru.disi.unige.it/Software/L1L2Signature/
  license: BSD License
  summary: 'Unbiased framework for gene expression analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
