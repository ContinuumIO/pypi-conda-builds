package:
  name: asyncssh
  version: "1.0.1"

source:
  fn: asyncssh-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncssh/asyncssh-1.0.1.tar.gz
  md5: 309ea69679a4420ae595e1a527c49580
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncssh = asyncssh:main
    #
    # Would create an entry point called asyncssh that calls asyncssh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asyncssh
    - asyncssh.crypto
    - asyncssh.crypto.pyca
    - asyncssh.crypto.pycrypto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://asyncssh.timeheart.net
  license: Eclipse Public License v1.0
  summary: 'AsyncSSH: Asynchronous SSHv2 client and server library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
