package:
  name: testoob
  version: "1.15"

source:
  fn: testoob-1.15.tar.bz2
  url: https://pypi.python.org/packages/source/t/testoob/testoob-1.15.tar.bz2
  md5: 604d2556ac01ca8f5d189ad8ffe2c8e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testoob = testoob:main
    #
    # Would create an entry point called testoob that calls testoob.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - testoob
    - testoob.commandline
    - testoob.compatibility
    - testoob.reporting
    - testoob.running

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/testoob
  license: Apache Software License
  summary: 'Testoob - An advanced unit testing framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
