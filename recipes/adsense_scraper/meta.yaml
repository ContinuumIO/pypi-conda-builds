package:
  name: adsense_scraper
  version: "0.5"

source:
  fn: adsense_scraper-0.5.tar.gz
  url: https://pypi.python.org/packages/source/a/adsense_scraper/adsense_scraper-0.5.tar.gz
  md5: 5dd5a3b151f8723d2775f2a288d83ffa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adsense_scraper = adsense_scraper:main
    #
    # Would create an entry point called adsense_scraper that calls adsense_scraper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twill
    - html5lib

  run:
    - python
    - twill
    - html5lib

test:
  # Python imports
  imports:
    - adsense_scraper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/etrepum/adsense_scraper/tree/master
  license: MIT License
  summary: 'Scrapes Google AdSense earnings data with Python using Twill'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
