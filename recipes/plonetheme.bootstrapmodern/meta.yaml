package:
  name: plonetheme.bootstrapmodern
  version: "0.50"

source:
  fn: plonetheme.bootstrapModern-0.50.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.bootstrapModern/plonetheme.bootstrapModern-0.50.tar.gz
  md5: b48a4d486aaa71a79b1098c2c0b681d4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.bootstrapmodern = plonetheme.bootstrapmodern:main
    #
    # Would create an entry point called plonetheme.bootstrapmodern that calls plonetheme.bootstrapmodern.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.bootstrapModern
    - plonetheme.bootstrapModern.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'An installable theme for Plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
