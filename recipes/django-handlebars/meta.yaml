package:
  name: django-handlebars
  version: "0.1"

source:
  fn: django-handlebars-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-handlebars/django-handlebars-0.1.tar.gz
  md5: 4bd51a15fa3ebcd35c36b1854dfb1f9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-handlebars = django-handlebars:main
    #
    # Would create an entry point called django-handlebars that calls django-handlebars.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_handlebars
    - django_handlebars.management
    - django_handlebars.management.commands
    - django_handlebars.management.observer
    - django_handlebars.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yavorskiy/django-handlebars
  license: BSD
  summary: 'Handlebars for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
