package:
  name: sphinxjp.usaturn
  version: "0.2.0"

source:
  fn: sphinxjp.usaturn-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.usaturn/sphinxjp.usaturn-0.2.0.tar.gz
  md5: 5958401bb8ab354c01f3c3e4a804e6c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.usaturn = sphinxjp.usaturn:main
    #
    # Would create an entry point called sphinxjp.usaturn that calls sphinxjp.usaturn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sphinxjp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.com/tk0miya/sphinxjp.usaturn
  license: Apache Software License
  summary: 'a sphinx extension to add new admonition named `usaturn`'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
