package:
  name: django-generic-aggregation
  version: "0.3.2"

source:
  fn: django-generic-aggregation-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-aggregation/django-generic-aggregation-0.3.2.tar.gz
  md5: 8e726545e4bbd6a3745707d9ff80318a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-aggregation = django-generic-aggregation:main
    #
    # Would create an entry point called django-generic-aggregation that calls django-generic-aggregation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - generic_aggregation
    - generic_aggregation.generic_aggregation_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coleifer/django-generic-aggregation/
  license: MIT License
  summary: 'annotate() and aggregate() for generically related data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
