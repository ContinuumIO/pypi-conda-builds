package:
  name: django-metasettings
  version: "0.2.2"

source:
  fn: django-metasettings-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-metasettings/django-metasettings-0.2.2.tar.gz
  md5: a8dccb53dc9482d842f11db064273b31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-metasettings = django-metasettings:main
    #
    # Would create an entry point called django-metasettings that calls django-metasettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - metasettings
    - metasettings.management
    - metasettings.management.commands
    - metasettings.migrations
    - metasettings.templatetags
    - metasettings.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-metasettings
  license: MIT License
  summary: 'A reusable Django application to control the currency rate and favorite language code, inspired by etsy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
