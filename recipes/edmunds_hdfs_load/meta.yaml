package:
  name: edmunds_hdfs_load
  version: "4.0.1"

source:
  fn: edmunds_hdfs_load-4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/edmunds_hdfs_load/edmunds_hdfs_load-4.0.1.tar.gz
  md5: f58e58bf946260a851e8f4a7a1c8b52b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edmunds_hdfs_load = edmunds_hdfs_load:main
    #
    # Would create an entry point called edmunds_hdfs_load that calls edmunds_hdfs_load.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - client_script
    - edmunds_hdfs_load

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: None
  summary: 'Moves files to hdfs by creating hive tables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
