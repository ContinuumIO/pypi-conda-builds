package:
  name: dogeon
  version: "1.0.1"

source:
  fn: Dogeon-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Dogeon/Dogeon-1.0.1.tar.gz
  md5: 92cc27fd7ff1501e9c27aeb51bb82c14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dogeon = dogeon:main
    #
    # Would create an entry point called dogeon that calls dogeon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dogeon
    - dson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/soasme/dogeon
  license: MIT License
  summary: 'Simple, fast, extensible DSON encoder/decoder for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
