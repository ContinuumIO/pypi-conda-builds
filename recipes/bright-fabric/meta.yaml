package:
  name: bright-fabric
  version: "0.0.2"

source:
  fn: bright-fabric-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bright-fabric/bright-fabric-0.0.2.tar.gz
  md5: 37c489268843e58b65313d104a5f5889
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bright-fabric = bright-fabric:main
    #
    # Would create an entry point called bright-fabric that calls bright-fabric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.8.0,<2
    - bright-vc >=1.1.0,<1.2.0

  run:
    - python
    - fabric >=1.8.0,<2
    - bright-vc >=1.1.0,<1.2.0

test:
  # Python imports
  imports:
    - bright_fabric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brightinteractive/bright-fabric/
  license: BSD
  summary: "Bright Interactive's Fabric Utilities"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
