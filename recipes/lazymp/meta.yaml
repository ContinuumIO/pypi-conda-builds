package:
  name: lazymp
  version: "0.1.0.0.3"

source:
  fn: lazymp-0.1.0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/lazymp/lazymp-0.1.0.0.3.tar.gz
  md5: cc8330a76fcc8f4546ca1dc0c77c0e6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazymp = lazymp:main
    #
    # Would create an entry point called lazymp that calls lazymp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyre >=0.8.2.0-pathos
    - processing >=0.52-pathos
    - ppft >=1.6.4.5
    - dill >=0.2.2
    - pillow >=2.8.1
    - pathos >=0.2a1.dev0
    - ppft >=1.6.4.5
    - dill >=0.2.2
    - pyre >=0.8.2.0-pathos
    - processing >=0.52-pathos

  run:
    - python
    - pyre >=0.8.2.0-pathos
    - processing >=0.52-pathos
    - ppft >=1.6.4.5
    - dill >=0.2.2
    - pillow >=2.8.1
    - pathos >=0.2a1.dev0
    - ppft >=1.6.4.5
    - dill >=0.2.2
    - pyre >=0.8.2.0-pathos
    - processing >=0.52-pathos

test:
  # Python imports
  imports:
    - lazymp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://leohung.net/pymp
  license: MIT
  summary: 'This is an Example Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
