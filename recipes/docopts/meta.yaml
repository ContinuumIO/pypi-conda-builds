package:
  name: docopts
  version: "0.6.1-fix2"

source:
  fn: docopts-0.6.1-fix2.tar.gz
  url: https://pypi.python.org/packages/source/d/docopts/docopts-0.6.1-fix2.tar.gz
  md5: f3fe69ed7b09c4b87c547a212a861c81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docopts = docopts:main
    #
    # Would create an entry point called docopts that calls docopts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt ==0.6.1

  run:
    - python
    - docopt ==0.6.1

test:
  # Python imports
  imports:
    - docopts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/docopt/docopts
  license: MIT License
  summary: 'Shell interface for docopt, the command-line interface description language.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
