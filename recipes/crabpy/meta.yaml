package:
  name: crabpy
  version: "0.5.0"

source:
  fn: crabpy-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/c/crabpy/crabpy-0.5.0.tar.gz
  md5: 1379438841c31ca54c322b68e30bb718
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crabpy = crabpy:main
    #
    # Would create an entry point called crabpy that calls crabpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds-jurko >=0.6.0
    - dogpile.cache
    - six

  run:
    - python
    - suds-jurko >=0.6.0
    - dogpile.cache
    - six

test:
  # Python imports
  imports:
    - crabpy
    - crabpy.gateway
    - crabpy.tests
    - crabpy.tests.gateway

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/onroerenderfgoed/crabpy
  license: MIT License
  summary: 'Interact with AGIV webservices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
