package:
  name: fluent-test
  version: "3.0.0"

source:
  fn: Fluent-Test-3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Fluent-Test/Fluent-Test-3.0.0.tar.gz
  md5: 9f57ebdcd5d36eb1268d093f67e00131
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fluent-test = fluent-test:main
    #
    # Would create an entry point called fluent-test that calls fluent-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock

  run:
    - python
    - mock

test:
  # Python imports
  imports:
    - fluenttest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dave-shawley/fluent-test/
  license: BSD License
  summary: 'Fluent testing for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
