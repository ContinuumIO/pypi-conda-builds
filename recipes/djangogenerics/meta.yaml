package:
  name: djangogenerics
  version: "0.5.6"

source:
  fn: djangogenerics-0.5.6.tar.gz
  url: https://pypi.python.org/packages/source/d/djangogenerics/djangogenerics-0.5.6.tar.gz
  md5: 3caef5eebf5485e0bd175e905074b342
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangogenerics = djangogenerics:main
    #
    # Would create an entry point called djangogenerics that calls djangogenerics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangogenerics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nrocco.github.io/
  license: django-generic-scaffold
  summary: 'A collection of Django 1.5+ utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
