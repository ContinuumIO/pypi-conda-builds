package:
  name: django-tastypie-mongoengine
  version: "0.4.6"

source:
  fn: django-tastypie-mongoengine-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tastypie-mongoengine/django-tastypie-mongoengine-0.4.6.tar.gz
  md5: a0385a0e195fd1018de608e614a14926
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tastypie-mongoengine = django-tastypie-mongoengine:main
    #
    # Would create an entry point called django-tastypie-mongoengine that calls django-tastypie-mongoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-tastypie >=0.9.12
    - mongoengine >=0.8.1,<0.8.2
    - python-dateutil >=2.1
    - lxml
    - defusedxml
    - pyyaml
    - biplist
    - python-mimeparse >=0.1.4

  run:
    - python
    - django >=1.5
    - django-tastypie >=0.9.12
    - mongoengine >=0.8.1,<0.8.2
    - python-dateutil >=2.1
    - lxml
    - defusedxml
    - pyyaml
    - biplist
    - python-mimeparse >=0.1.4

test:
  # Python imports
  imports:
    - tastypie_mongoengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wlanslovenija/django-tastypie-mongoengine
  license: GNU Affero General Public License v3
  summary: 'MongoEngine support for django-tastypie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
