package:
  name: bugle-sites
  version: "0.0.4"

source:
  fn: bugle-sites-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bugle-sites/bugle-sites-0.0.4.tar.gz
  md5: 8d83274ab8361eda2fd767169d4760e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bugle-sites = bugle-sites:main
    #
    # Would create an entry point called bugle-sites that calls bugle-sites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric ==1.5.1
    - jinja2 ==2.6
    - markdown ==2.2.1
    - pyyaml ==3.10
    - unidecode ==0.04.10
    - nose ==1.2.1
    - paramiko ==1.9.0
    - pycrypto ==2.6
    - wsgiref ==0.1.2

  run:
    - python
    - fabric ==1.5.1
    - jinja2 ==2.6
    - markdown ==2.2.1
    - pyyaml ==3.10
    - unidecode ==0.04.10
    - nose ==1.2.1
    - paramiko ==1.9.0
    - pycrypto ==2.6
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - bugle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yosemitebandit/bugle
  license: MIT License
  summary: 'static sites from markdown and yaml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
