package:
  name: hoarder
  version: "0.0.6"

source:
  fn: hoarder-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/h/hoarder/hoarder-0.0.6.tar.gz
  md5: 96bddf322165ad3b3ba3e25966f0cd8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hoarder = hoarder:main
    #
    # Would create an entry point called hoarder that calls hoarder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-celery

  run:
    - python
    - django-celery

test:
  # Python imports
  imports:
    - hoarder
    - hoarder.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/InviteBox/hoarder
  license: BSD License
  summary: 'Django analytics kit for the data-obsessed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
