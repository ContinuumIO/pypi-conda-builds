package:
  name: bolos
  version: "0.1"

source:
  fn: bolos-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bolos/bolos-0.1.tar.gz
  md5: 18903e230f706a3bd9b04ccf363fcdbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bolos = bolos:main
    #
    # Would create an entry point called bolos that calls bolos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy

  run:
    - python
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - bolos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aluque/bolos
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'A library to solve the Boltzmann equation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
