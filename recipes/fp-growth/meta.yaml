package:
  name: fp-growth
  version: "0.1.2"

source:
  fn: fp-growth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fp-growth/fp-growth-0.1.2.tar.gz
  md5: 5153aa0675c37b0f9c460d26982d3eb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fp-growth = fp-growth:main
    #
    # Would create an entry point called fp-growth that calls fp-growth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/enaeseth/python-fp-growth/
  license: UNKNOWN
  summary: 'A pure-python implementation of the FP-growth algorithm.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
