package:
  name: collective.dewplayer
  version: "1.2"

source:
  fn: collective.dewplayer-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.dewplayer/collective.dewplayer-1.2.tar.gz
  md5: 84dad9343b07825fb39cc7596405ec89
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dewplayer = collective.dewplayer:main
    #
    # Would create an entry point called collective.dewplayer that calls collective.dewplayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.swfobject
    - collective.configviews

  run:
    - python
    - setuptools
    - setuptools
    - collective.swfobject
    - collective.configviews

test:
  # Python imports
  imports:
    - collective
    - collective.dewplayer
    - collective.dewplayer.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.dewplayer/trunk
  license: GPL
  summary: 'A simple package using Dewplayer for video content - By Makina Corpus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
