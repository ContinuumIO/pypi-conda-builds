package:
  name: django-profiletools
  version: "0.2.1"

source:
  fn: django-profiletools-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-profiletools/django-profiletools-0.2.1.tar.gz
  md5: 36a929601b604da813cbb02c774e8d5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-profiletools = django-profiletools:main
    #
    # Would create an entry point called django-profiletools that calls django-profiletools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-braces ==0.1.5

  run:
    - python
    - django-braces ==0.1.5

test:
  # Python imports
  imports:
    - profiletools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pydanny/django-profiletools
  license: BSD License
  summary: 'Tools for Profile models in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
