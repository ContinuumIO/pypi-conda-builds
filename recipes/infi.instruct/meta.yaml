package:
  name: infi.instruct
  version: "0.6.26"

source:
  fn: infi.instruct-0.6.26.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.instruct/infi.instruct-0.6.26.tar.gz
  md5: 7b9a2fc32ad7a75b57bdb6b1f5d13164
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.instruct = infi.instruct:main
    #
    # Would create an entry point called infi.instruct that calls infi.instruct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - infi.exceptools >=0.2.1
    - infi.pyutils
    - setuptools

  run:
    - python
    - infi.exceptools >=0.2.1
    - infi.pyutils
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.instruct
    - infi.instruct.buffer
    - infi.instruct.buffer.reference
    - infi.instruct.struct
    - infi.instruct.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.instruct
  license: Python Software Foundation License
  summary: 'declarative structure marshalling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
