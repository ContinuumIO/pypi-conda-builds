package:
  name: test-out
  version: "0.1.7"

source:
  fn: test-out-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/t/test-out/test-out-0.1.7.tar.gz
  md5: fe6c26647db8262b9bceb21e6a923213
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - test-out = test-out:main
    #
    # Would create an entry point called test-out that calls test-out.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - box-it-up

  run:
    - python
    - box-it-up

test:
  # Python imports
  imports:
    - test_out

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rpappalax/test-out
  license: MIT
  summary: 'Python class to provide helpful logging, test run data and summary statistics for automated tests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
