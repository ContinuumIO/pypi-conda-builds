package:
  name: django-suggestions
  version: "0.2.0"

source:
  fn: django-suggestions-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-suggestions/django-suggestions-0.2.0.tar.gz
  md5: 523f3cb0e20e11dcb814d4393da3cbc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-suggestions = django-suggestions:main
    #
    # Would create an entry point called django-suggestions that calls django-suggestions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangosuggestions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-suggestions
  license: UNKNOWN
  summary: 'A Django form widget that implements the HTML5 form list element'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
