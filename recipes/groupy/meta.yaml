package:
  name: groupy
  version: "0.0.2"

source:
  fn: groupy-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/groupy/groupy-0.0.2.tar.gz
  md5: bbe362b8e77903bdcb6ce2f6e8946da7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - groupy = groupy:main
    #
    # Would create an entry point called groupy that calls groupy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse ==1.2.1
    - backports.ssl-match-hostname ==3.4.0.2
    - clowncar ==0.1.1
    - futures ==2.1.6
    - tornado ==3.2.2
    - wsgiref ==0.1.2

  run:
    - python
    - argparse ==1.2.1
    - backports.ssl-match-hostname ==3.4.0.2
    - clowncar ==0.1.1
    - futures ==2.1.6
    - tornado ==3.2.2
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - groupy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dropbox/groupy
  license: Apache
  summary: 'Python client library for Grouper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
