package:
  name: django-newsletter-subscription
  version: "0.2.0"

source:
  fn: django-newsletter-subscription-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-newsletter-subscription/django-newsletter-subscription-0.2.0.tar.gz
  md5: 60aae71dd6b68b8d4dad94a72054e408
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-newsletter-subscription = django-newsletter-subscription:main
    #
    # Would create an entry point called django-newsletter-subscription that calls django-newsletter-subscription.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - newsletter_subscription

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/matthiask/django-newsletter-subscription/
  license: BSD License
  summary: 'Another newsletter subscription app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
