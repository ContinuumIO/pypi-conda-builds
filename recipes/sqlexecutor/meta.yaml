package:
  name: sqlexecutor
  version: "0.1.1"

source:
  fn: sqlexecutor-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlexecutor/sqlexecutor-0.1.1.tar.gz
  md5: 1f70e305f0d7695e71ee7a8d69a51947
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlexecutor = sqlexecutor:main
    #
    # Would create an entry point called sqlexecutor that calls sqlexecutor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - spur.local >=0.3.5,<0.4
    - mysql-python ==1.2.4

  run:
    - python
    - spur.local >=0.3.5,<0.4
    - mysql-python ==1.2.4

test:
  # Python imports
  imports:
    - sqlexecutor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mwilliamson/sqlexecutor
  license: BSD 2-Clause
  summary: 'Execute SQL queries in an isolated database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
