package:
  name: m3-ui
  version: "2.0.7.8"

source:
  fn: m3-ui-2.0.7.8.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-ui/m3-ui-2.0.7.8.tar.gz
  md5: 3fdf720eb1f953e49b3a4a7be8c91876
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-ui = m3-ui:main
    #
    # Would create an entry point called m3-ui that calls m3-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - m3_ext
    - m3_ext.gears
    - m3_ext.ui
    - m3_ext.ui.containers
    - m3_ext.ui.controls
    - m3_ext.ui.fields
    - m3_ext.ui.menus
    - m3_ext.ui.misc
    - m3_ext.ui.panels
    - m3_ext.ui.windows

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barsgroup/m3-ext
  license: MIT License
  summary: 'UI ExtJS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
