package:
  name: beyondskins.ploneday.site2009
  version: "1.0"

source:
  fn: beyondskins.ploneday.site2009-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/beyondskins.ploneday.site2009/beyondskins.ploneday.site2009-1.0.tar.gz
  md5: 77c046cddbe2ea4e47b5fc6ad55cff10
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beyondskins.ploneday.site2009 = beyondskins.ploneday.site2009:main
    #
    # Would create an entry point called beyondskins.ploneday.site2009 that calls beyondskins.ploneday.site2009.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - beyondskins
    - beyondskins.ploneday
    - beyondskins.ploneday.site2009
    - beyondskins.ploneday.site2009.browser
    - beyondskins.ploneday.site2009.doc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Installable Plone 3 theme developed to promote World Plone Day 2009 (April 22nd)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
