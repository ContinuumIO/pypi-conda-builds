package:
  name: ggtracker-client
  version: "0.1"

source:
  fn: ggtracker-client-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/ggtracker-client/ggtracker-client-0.1.tar.gz
  md5: ba3147036c57c461829bd41a3dce0016
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ggtracker-client = ggtracker-client:main
    #
    # Would create an entry point called ggtracker-client that calls ggtracker-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - ggtracker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tthieman/ggtracker-python
  license: BSD License
  summary: 'API client for ggtracker.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
