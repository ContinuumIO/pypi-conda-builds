package:
  name: django-vkontakte-groups-migration
  version: "0.6.6"

source:
  fn: django-vkontakte-groups-migration-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-groups-migration/django-vkontakte-groups-migration-0.6.6.tar.gz
  md5: 99dc66cc65b4475e0eb187879bff4859
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-groups-migration = django-vkontakte-groups-migration:main
    #
    # Would create an entry point called django-vkontakte-groups-migration that calls django-vkontakte-groups-migration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-vkontakte-api >=0.4.11
    - django-vkontakte-groups >=0.6.0
    - django-vkontakte-users >=0.6.0

  run:
    - python
    - django-vkontakte-api >=0.4.11
    - django-vkontakte-groups >=0.6.0
    - django-vkontakte-users >=0.6.0

test:
  # Python imports
  imports:
    - vkontakte_groups_migration
    - vkontakte_groups_migration.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-vkontakte-groups-migration
  license: BSD License
  summary: 'Django implementation for vkontakte API Groups Users Migration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
