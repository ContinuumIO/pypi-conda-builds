package:
  name: zopyx.plone.cassandra
  version: "0.3.5"

source:
  fn: zopyx.plone.cassandra-0.3.5.zip
  url: https://pypi.python.org/packages/source/z/zopyx.plone.cassandra/zopyx.plone.cassandra-0.3.5.zip
  md5: a4cdcc948918c37dca304ee09b5f72a0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.plone.cassandra = zopyx.plone.cassandra:main
    #
    # Would create an entry point called zopyx.plone.cassandra that calls zopyx.plone.cassandra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zopyx
    - zopyx.plone
    - zopyx.plone.cassandra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zopyx.plone.cassandra
  license: ZPL
  summary: 'Show all assigned local roles within a subtree for any Plone 4 site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
