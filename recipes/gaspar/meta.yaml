package:
  name: gaspar
  version: "1.0"

source:
  fn: gaspar-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gaspar/gaspar-1.0.tar.gz
  md5: 5770d4b14cd45d6e1c07d6885708b43d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaspar = gaspar:main
    #
    # Would create an entry point called gaspar that calls gaspar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eventlet
    - pyzmq

  run:
    - python
    - setuptools
    - eventlet
    - pyzmq

test:
  # Python imports
  imports:
    - gaspar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jmoiron/gaspar
  license: MIT License
  summary: 'gaspar eventlet zmq parallel worker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
