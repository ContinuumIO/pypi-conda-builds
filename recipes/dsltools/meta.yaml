package:
  name: dsltools
  version: "0.2.6"

source:
  fn: dsltools-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/dsltools/dsltools-0.2.6.tar.gz
  md5: aa29e465e83471d0824df2be1ad1e8d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dsltools = dsltools:main
    #
    # Would create an entry point called dsltools that calls dsltools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dsltools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/iskandr/dsltools
  license: BSD License
  summary: 'Helpers for creating and traversing recursively nested data structures in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
