package:
  name: tornadoes
  version: "2.3.1"

source:
  fn: tornadoes-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadoes/tornadoes-2.3.1.tar.gz
  md5: 7cb894c020237314e045c9cedcae3c7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadoes = tornadoes:main
    #
    # Would create an entry point called tornadoes that calls tornadoes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.0.0,<4.1.0
    - six ==1.7.3

  run:
    - python
    - tornado >=3.0.0,<4.1.0
    - six ==1.7.3

test:
  # Python imports
  imports:
    - tornadoes
    - tornadoes.tests
    - tornadoes.tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/globocom/tornado-es
  license: MIT License
  summary: 'A tornado-powered python library that provides asynchronous access to elasticsearch.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
