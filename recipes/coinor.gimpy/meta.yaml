package:
  name: coinor.gimpy
  version: "1.3.5"

source:
  fn: coinor.gimpy-1.3.5.zip
  url: https://pypi.python.org/packages/source/c/coinor.gimpy/coinor.gimpy-1.3.5.zip
  md5: 80e05bc830ee64fc567ab133af349db1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinor.gimpy = coinor.gimpy:main
    #
    # Would create an entry point called coinor.gimpy that calls coinor.gimpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coinor.blimpy >=1.2.0

  run:
    - python
    - coinor.blimpy >=1.2.0

test:
  # Python imports
  imports:
    - coinor
    - coinor.gimpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tkralphs/GiMPy/
  license: Eclipse Public License
  summary: 'Graph Methods in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
