package:
  name: django-sitepush
  version: "0.1"

source:
  fn: django-sitepush-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sitepush/django-sitepush-0.1.tar.gz
  md5: 19f108afb765b8ddb87fa3eb6bb3fe7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sitepush = django-sitepush:main
    #
    # Would create an entry point called django-sitepush that calls django-sitepush.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - fabric >=1.3.3

  run:
    - python
    - django >=1.3
    - fabric >=1.3.3

test:
  # Python imports
  imports:
    - sitepush
    - sitepush.management
    - sitepush.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yprez/django-sitepush
  license: ISC License (ISCL)
  summary: 'Redeploy Django projects using management commands'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
