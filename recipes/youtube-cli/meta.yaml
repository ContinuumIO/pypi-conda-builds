package:
  name: youtube-cli
  version: "Beta.2"

source:
  fn: youtube-cli-Beta.2.tar.gz
  url: https://pypi.python.org/packages/source/y/youtube-cli/youtube-cli-Beta.2.tar.gz
  md5: 50fcff48de66f28fc463f1914ceea8c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - youtube-cli = youtube-cli:main
    #
    # Would create an entry point called youtube-cli that calls youtube-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - youtube-cli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/0xPr0xy/youtube-cli
  license: BSD License
  summary: 'Youtube command line interface with stream and download functionality'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
