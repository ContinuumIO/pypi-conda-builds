package:
  name: django-contact
  version: "0.1.1"

source:
  fn: django-contact-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-contact/django-contact-0.1.1.tar.gz
  md5: 47a6dbf5b20e272b1ebcf7f1ef2f6b82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contact = django-contact:main
    #
    # Would create an entry point called django-contact that calls django-contact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-model-utils >=1.4.0,<2.0

  run:
    - python
    - django-model-utils >=1.4.0,<2.0

test:
  # Python imports
  imports:
    - contact

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/LimpidTech/django-contact
  license: MIT License
  summary: 'System for working with contacts/identities in django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
