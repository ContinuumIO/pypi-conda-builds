package:
  name: dateinfer
  version: "0.2.0"

source:
  fn: dateinfer-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dateinfer/dateinfer-0.2.0.tar.gz
  md5: 7b9a675d33d005aec9f9c65b3e28b43a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dateinfer = dateinfer:main
    #
    # Would create an entry point called dateinfer that calls dateinfer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - dateinfer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeffreystarr/dateinfer
  license: Apache Software License
  summary: 'Infers date format from examples'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
