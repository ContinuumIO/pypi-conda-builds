package:
  name: wille
  version: "2.3"

source:
  fn: wille-2.3.zip
  url: https://pypi.python.org/packages/source/w/wille/wille-2.3.zip
  md5: b59805fb9afd42fa36c873838ad835b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wille = wille:main
    #
    # Would create an entry point called wille that calls wille.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.6.0
    - pyjavaproperties >=0.5
    - feedparser >=5.0
    - argparse >=1.1

  run:
    - python
    - httplib2 >=0.6.0
    - pyjavaproperties >=0.5
    - feedparser >=5.0
    - argparse >=1.1

test:
  # Python imports
  imports:
    - tests
    - wille
    - wille.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/wille
  license: BSD License
  summary: 'Wille Visualisation Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
