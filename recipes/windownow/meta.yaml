package:
  name: windownow
  version: "0.9.6"

source:
  fn: windownow-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/w/windownow/windownow-0.9.6.tar.gz
  md5: 3a556eeaeb54dcbe42af5dafd788d4c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - windownow = windownow:main
    #
    # Would create an entry point called windownow that calls windownow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - windownow
    - windownow/plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/windownow/
  license: BSD License
  summary: "Switch desktop's windows by typing their titles, name, etc."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
