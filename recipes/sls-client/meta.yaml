package:
  name: sls-client
  version: "1.0.7"

source:
  fn: sls-client-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/s/sls-client/sls-client-1.0.7.tar.gz
  md5: 283cb320fec19d482a23ae37b1a838f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sls-client = sls-client:main
    #
    # Would create an entry point called sls-client that calls sls-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipy
    - yurl
    - isodate
    - futures
    - requests
    - voluptuous

  run:
    - python
    - ipy
    - yurl
    - isodate
    - futures
    - requests
    - voluptuous

test:
  # Python imports
  imports:
    - sls_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/esnet/python-sls-client
  license: BSD License
  summary: 'A python client for the Simple Lookup Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
