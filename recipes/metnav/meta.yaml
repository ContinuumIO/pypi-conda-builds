package:
  name: metnav
  version: "4.0.2"

source:
  fn: metnav-4.0.2.zip
  url: https://pypi.python.org/packages/source/m/metnav/metnav-4.0.2.zip
  md5: 8922b598678aaac8af7150a3b92c353f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metnav = metnav:main
    #
    # Would create an entry point called metnav that calls metnav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pisa
    - reportlab
    - html5lib
    - z3c.form

  run:
    - python
    - setuptools
    - setuptools
    - pisa
    - reportlab
    - html5lib
    - z3c.form

test:
  # Python imports
  imports:
    - metnav
    - metnav.browser
    - metnav.interfaces
    - metnav.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://unis.ens-lyon.fr
  license: CeCILL-B
  summary: 'Facet Metadata Navigation from eXist and LOM-fr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
