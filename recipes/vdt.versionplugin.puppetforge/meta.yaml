package:
  name: vdt.versionplugin.puppetforge
  version: "0.2"

source:
  fn: vdt.versionplugin.puppetforge-0.2.tar.gz
  url: https://pypi.python.org/packages/source/v/vdt.versionplugin.puppetforge/vdt.versionplugin.puppetforge-0.2.tar.gz
  md5: 060a316b8dfa0c25f191043b8606f221
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vdt.versionplugin.puppetforge = vdt.versionplugin.puppetforge:main
    #
    # Would create an entry point called vdt.versionplugin.puppetforge that calls vdt.versionplugin.puppetforge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vdt.version
    - vdt.versionplugin.default
    - pyyaml

  run:
    - python
    - setuptools
    - vdt.version
    - vdt.versionplugin.default
    - pyyaml

test:
  # Python imports
  imports:
    - vdt
    - vdt.versionplugin
    - vdt.versionplugin.puppetforge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Version Increment Plugin that builds a package from a puppet module from puppet forge'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
