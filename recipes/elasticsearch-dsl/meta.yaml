package:
  name: elasticsearch-dsl
  version: "0.0.4"

source:
  fn: elasticsearch-dsl-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/e/elasticsearch-dsl/elasticsearch-dsl-0.0.4.tar.gz
  md5: f37c49bb7a14557c2d2fe7e375a9b11e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticsearch-dsl = elasticsearch-dsl:main
    #
    # Would create an entry point called elasticsearch-dsl that calls elasticsearch-dsl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - python-dateutil
    - elasticsearch >=1.0.0

  run:
    - python
    - six
    - python-dateutil
    - elasticsearch >=1.0.0

test:
  # Python imports
  imports:
    - elasticsearch_dsl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - mock
    - pytest
    - pytest-cov
    - pytz
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elasticsearch/elasticsearch-dsl-py
  license: Apache Software License
  summary: 'Python client for Elasticsearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
