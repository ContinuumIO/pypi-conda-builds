package:
  name: django-primary-filters
  version: "0.1.4"

source:
  fn: django-primary-filters-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-primary-filters/django-primary-filters-0.1.4.tar.gz
  md5: faaf6e449f02a3d97631af1bed828c58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-primary-filters = django-primary-filters:main
    #
    # Would create an entry point called django-primary-filters that calls django-primary-filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - primary_filters
    - primary_filters.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-primary-filters
  license: Apache Software License
  summary: 'django-primary-filters provides some extra filters that are often needed by Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
