package:
  name: oath
  version: "1.4.0"

source:
  fn: oath-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oath/oath-1.4.0.tar.gz
  md5: 23f48f6301e0b29533e843ef6565419f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oath = oath:main
    #
    # Would create an entry point called oath that calls oath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bdauvergne/python-oath
  license: MIT License
  summary: 'Python implementation of the three main OATH specifications: HOTP, TOTP and OCRA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
