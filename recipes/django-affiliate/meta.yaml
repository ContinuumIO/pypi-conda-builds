package:
  name: django-affiliate
  version: "0.1.1"

source:
  fn: django-affiliate-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-affiliate/django-affiliate-0.1.1.tar.gz
  md5: 1048c9e4fc9b4c59a2023cb8c7126b93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-affiliate = django-affiliate:main
    #
    # Would create an entry point called django-affiliate that calls django-affiliate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.7
    - django-model-utils ==2.0.3
    - relish ==0.1.1
    - pillow

  run:
    - python
    - django >=1.4,<1.7
    - django-model-utils ==2.0.3
    - relish ==0.1.1
    - pillow

test:
  # Python imports
  imports:
    - affiliate
    - affiliate.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/st4lk/django-affiliate
  license: BSD License
  summary: 'Affiliate system for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
