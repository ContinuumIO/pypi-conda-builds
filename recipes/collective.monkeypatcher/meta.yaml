package:
  name: collective.monkeypatcher
  version: "1.1.1"

source:
  fn: collective.monkeypatcher-1.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.monkeypatcher/collective.monkeypatcher-1.1.1.zip
  md5: d9ebb0d2a5a4f0f03b0bc21f0c40f531
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.monkeypatcher = collective.monkeypatcher:main
    #
    # Would create an entry point called collective.monkeypatcher that calls collective.monkeypatcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.monkeypatcher
    - collective.monkeypatcher.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.monkeypatcher
  license: BSD
  summary: 'Support for applying monkey patches late in the startup         cycle by using ZCML configuration actions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
