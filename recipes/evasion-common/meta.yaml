package:
  name: evasion-common
  version: "1.0.3"

source:
  fn: evasion-common-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/evasion-common/evasion-common-1.0.3.tar.gz
  md5: a563dcb2d4897436b4c64fe481093892
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evasion-common = evasion-common:main
    #
    # Would create an entry point called evasion-common that calls evasion-common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - evasion
    - evasion.common
    - evasion.common.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/oisinmulvihill/evasion-common/tarball/master#egg=evasion_common
  license: Evasion Project CDDL License
  summary: 'Helper functions collected together from other evasion modules to aid reuse such as get_free_port() and wait_for_ready().'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
