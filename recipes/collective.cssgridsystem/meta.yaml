package:
  name: collective.cssgridsystem
  version: "1.0"

source:
  fn: collective.cssgridsystem-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.cssgridsystem/collective.cssgridsystem-1.0.zip
  md5: eb533d5d35c454f12da259fbcf37ec7b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cssgridsystem = collective.cssgridsystem:main
    #
    # Would create an entry point called collective.cssgridsystem that calls collective.cssgridsystem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.cssgridsystem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.cssgridsystem
  license: GPL
  summary: '960CSS And Blue print packaged as zope browser resource'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
