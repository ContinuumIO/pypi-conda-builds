package:
  name: saklient
  version: "0.0.2.5"

source:
  fn: saklient-0.0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/s/saklient/saklient-0.0.2.5.tar.gz
  md5: 43854271ac772f72a9cda213d2896c19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - saklient = saklient:main
    #
    # Would create an entry point called saklient that calls saklient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - saklient
    - saklient.cloud
    - saklient.cloud.enums
    - saklient.cloud.errors
    - saklient.cloud.models
    - saklient.cloud.resources
    - saklient.errors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cloud.sakura.ad.jp/
  license: MIT License
  summary: 'SAKURA Internet API Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
