package:
  name: dumbmonitor
  version: "0.0.1"

source:
  fn: dumbmonitor-0.0.1.tgz
  url: https://pypi.python.org/packages/source/d/dumbmonitor/dumbmonitor-0.0.1.tgz
  md5: 43894fb1a6889e4b99740c7b23cee56a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dumbmonitor = dumbmonitor:main
    #
    # Would create an entry point called dumbmonitor that calls dumbmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dumbmonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dumbmonitor.com
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Dumb Monitory Utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
