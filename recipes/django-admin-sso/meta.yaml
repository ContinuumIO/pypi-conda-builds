package:
  name: django-admin-sso
  version: "1.0.1"

source:
  fn: django-admin-sso-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-sso/django-admin-sso-1.0.1.tar.gz
  md5: 28750b88f116a969e389edbf4d660594
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-sso = django-admin-sso:main
    #
    # Would create an entry point called django-admin-sso that calls django-admin-sso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - oauth2client >=1.2

  run:
    - python
    - django >=1.4
    - oauth2client >=1.2

test:
  # Python imports
  imports:
    - admin_sso
    - admin_sso.openid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frog32/django-admin-sso/
  license: BSD License
  summary: 'django sso solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
