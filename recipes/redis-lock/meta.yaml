package:
  name: redis-lock
  version: "0.2.0"

source:
  fn: redis-lock-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-lock/redis-lock-0.2.0.tar.gz
  md5: f5dedd2439cf33e7b068f284837a9424
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-lock = redis-lock:main
    #
    # Would create an entry point called redis-lock that calls redis-lock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_lock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cezarsa/redis_lock
  license: MIT
  summary: 'A simple pessimistic lock implementation using Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
