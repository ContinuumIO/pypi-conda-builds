package:
  name: memojito
  version: "0.6"

source:
  fn: memojito-0.6.tar.gz
  url: https://pypi.python.org/packages/source/m/memojito/memojito-0.6.tar.gz
  md5: a879104457ba254d1031f565f3a10d6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memojito = memojito:main
    #
    # Would create an entry point called memojito that calls memojito.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - memojito

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/plonents/components/memojito/trunk#egg=memojito
  license: MIT
  summary: 'simple library of memoizing decorators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
