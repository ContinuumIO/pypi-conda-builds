package:
  name: django_cropper
  version: "0.1.4a"

source:
  fn: django_cropper-0.1.4a.tar.gz
  url: https://pypi.python.org/packages/source/d/django_cropper/django_cropper-0.1.4a.tar.gz
  md5: 2343e93704016caae34eb5aa980f84b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_cropper = django_cropper:main
    #
    # Would create an entry point called django_cropper that calls django_cropper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cropper
    - django_cropper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/simonw/django_cropper
  license: MIT License
  summary: 'Image cropping for the Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
