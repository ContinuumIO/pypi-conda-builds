package:
  name: eea.downloads
  version: "1.2"

source:
  fn: eea.downloads-1.2.zip
  url: https://pypi.python.org/packages/source/e/eea.downloads/eea.downloads-1.2.zip
  md5: 771a5f1c20a5df2142cae42a1ce7ace3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eea.downloads = eea.downloads:main
    #
    # Would create an entry point called eea.downloads that calls eea.downloads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - eea
    - eea.downloads
    - eea.downloads.content
    - eea.downloads.storage
    - eea.downloads.tests
    - eea.downloads.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://eea.github.io
  license: GNU General Public License (GPL) or Mozilla Public License 1.0 (MPL)
  summary: 'EEA Downloads Media Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
