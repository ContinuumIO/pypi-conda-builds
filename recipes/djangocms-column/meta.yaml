package:
  name: djangocms-column
  version: "1.5"

source:
  fn: djangocms-column-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-column/djangocms-column-1.5.tar.gz
  md5: bef5c496463646dfed81f752ca3b6c74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-column = djangocms-column:main
    #
    # Would create an entry point called djangocms-column that calls djangocms-column.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_column
    - djangocms_column.migrations
    - djangocms_column.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-column
  license: BSD License
  summary: 'Column Plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
