package:
  name: collective.opengraph
  version: "1.0.1"

source:
  fn: collective.opengraph-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.opengraph/collective.opengraph-1.0.1.zip
  md5: 21d6f67e5fcc8913e835a16582668970
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.opengraph = collective.opengraph:main
    #
    # Would create an entry point called collective.opengraph that calls collective.opengraph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ordereddict

  run:
    - python
    - setuptools
    - setuptools
    - ordereddict

test:
  # Python imports
  imports:
    - collective
    - collective.opengraph
    - collective.opengraph.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.opengraph
  license: GPL
  summary: 'Plone opengraph integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
