package:
  name: django-user_agents
  version: "0.3.0"

source:
  fn: django-user_agents-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user_agents/django-user_agents-0.3.0.tar.gz
  md5: d56d5f15da833826df9cb0a4d760e394
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user_agents = django-user_agents:main
    #
    # Would create an entry point called django-user_agents that calls django-user_agents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - user-agents

  run:
    - python
    - django
    - user-agents

test:
  # Python imports
  imports:
    - django_user_agents

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/selwin/django-user_agents
  license: MIT License
  summary: "A django package that allows easy identification of visitors' browser, operating system and device information (mobile phone, tablet or has touch capabilities)."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
