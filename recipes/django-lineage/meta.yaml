package:
  name: django-lineage
  version: "0.2.0"

source:
  fn: django-lineage-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lineage/django-lineage-0.2.0.tar.gz
  md5: a92910bf0ea69427880769ab8f05b0cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lineage = django-lineage:main
    #
    # Would create an entry point called django-lineage that calls django-lineage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lineage
    - lineage.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marcuswhybrow/django-lineage
  license: BSD License
  summary: 'Lightweight template tags for neater hierarchical navigation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
