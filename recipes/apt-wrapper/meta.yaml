package:
  name: apt-wrapper
  version: "1.11"

source:
  fn: apt-wrapper-1.11.tar.gz
  url: https://pypi.python.org/packages/source/a/apt-wrapper/apt-wrapper-1.11.tar.gz
  md5: e2695bedffdcf0c8411d5faa396c58cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apt-wrapper = apt-wrapper:main
    #
    # Would create an entry point called apt-wrapper that calls apt-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mixmastamyk/apt
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A simpler interface to the Debian APT command-line tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
