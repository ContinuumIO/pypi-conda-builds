package:
  name: cubicweb-forum
  version: "0.1.1"

source:
  fn: cubicweb-forum-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-forum/cubicweb-forum-0.1.1.tar.gz
  md5: ca3e001159adb7e0feaf41348d70f936
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-forum = cubicweb-forum:main
    #
    # Would create an entry point called cubicweb-forum that calls cubicweb-forum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.4
    - cubicweb-comment
    - cubicweb-nosylist

  run:
    - python
    - cubicweb >=3.16.4
    - cubicweb-comment
    - cubicweb-nosylist

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-forum
  license: LGPL
  summary: 'forum'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
