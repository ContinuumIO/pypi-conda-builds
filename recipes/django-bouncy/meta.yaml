package:
  name: django-bouncy
  version: "0.0.1"

source:
  fn: django-bouncy-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bouncy/django-bouncy-0.0.1.tar.gz
  md5: fc1d556967a62de750b8030c292ad5b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bouncy = django-bouncy:main
    #
    # Would create an entry point called django-bouncy that calls django-bouncy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - python-dateutil >=2.1
    - pyopenssl >=0.13.1
    - pem >=0.1.0

  run:
    - python
    - django >=1.4
    - python-dateutil >=2.1
    - pyopenssl >=0.13.1
    - pem >=0.1.0

test:
  # Python imports
  imports:
    - django_bouncy
    - django_bouncy.migrations
    - django_bouncy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ofa/django-bouncy
  license: UNKNOWN
  summary: "A way to handle bounce and abuse reports delivered by Amazon's Simple Notification Service regarding emails sent by Simple Email Service"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
