package:
  name: ditail
  version: "0.1.0"

source:
  fn: ditail-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/ditail/ditail-0.1.0.tar.gz
  md5: c22a654bf9ea714353f04930af49cc08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ditail = ditail:main
    #
    # Would create an entry point called ditail that calls ditail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyinotify >=0.9.5
    - asyncio >=3.4.2

  run:
    - python
    - pyinotify >=0.9.5
    - asyncio >=3.4.2

test:
  # Python imports
  imports:
    - ditail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ASzc/ditail
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Tail for directories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
