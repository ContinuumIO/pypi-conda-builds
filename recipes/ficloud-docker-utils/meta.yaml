package:
  name: ficloud-docker-utils
  version: "0.1.2"

source:
  fn: ficloud-docker-utils-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/ficloud-docker-utils/ficloud-docker-utils-0.1.2.tar.gz
  md5: e41d59461ebde19cbdb312d6affef519
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ficloud-docker-utils = ficloud-docker-utils:main
    #
    # Would create an entry point called ficloud-docker-utils that calls ficloud-docker-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pywizard/ficloud-docker-utils
  license: MIT
  summary: 'Tool that implements chef/puppet -like approach for server management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
