package:
  name: diy-django
  version: "1.3.1"

source:
  fn: diy-django-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/diy-django/diy-django-1.3.1.tar.gz
  md5: ee029744bde68c426d4cce5460fffb23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diy-django = diy-django:main
    #
    # Would create an entry point called diy-django that calls diy-django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - diydjango

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collinanderson/diy-django
  license: CC0
  summary: 'diy-django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
