package:
  name: collective.cdn.alternatehostname
  version: "0.5"

source:
  fn: collective.cdn.alternatehostname-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.cdn.alternatehostname/collective.cdn.alternatehostname-0.5.zip
  md5: 3ed687d7c33d2f753f7dd17a2e782113
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cdn.alternatehostname = collective.cdn.alternatehostname:main
    #
    # Would create an entry point called collective.cdn.alternatehostname that calls collective.cdn.alternatehostname.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.cdn.core

  run:
    - python
    - setuptools
    - setuptools
    - collective.cdn.core

test:
  # Python imports
  imports:
    - collective
    - collective.cdn
    - collective.cdn.alternatehostname
    - collective.cdn.alternatehostname.docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Alternate hostname CDN support for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
