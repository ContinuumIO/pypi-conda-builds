package:
  name: google-measurement-protocol
  version: "0.1.4"

source:
  fn: google-measurement-protocol-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/g/google-measurement-protocol/google-measurement-protocol-0.1.4.tar.gz
  md5: a07a82f1c46eeb24d5c3db2127133c13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google-measurement-protocol = google-measurement-protocol:main
    #
    # Would create an entry point called google-measurement-protocol that calls google-measurement-protocol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google_measurement_protocol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A Python implementation of Google Analytics Measurement Protocol'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
