package:
  name: django-mysql-manager
  version: "0.1.3"

source:
  fn: django-mysql-manager-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mysql-manager/django-mysql-manager-0.1.3.tar.gz
  md5: 05d36de9fef0a54f79890ce5cb7b4d94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mysql-manager = django-mysql-manager:main
    #
    # Would create an entry point called django-mysql-manager that calls django-mysql-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mysql_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-mysql-manager
  license: Apache Software License
  summary: 'django-mysql-manager is a Django based management interface for MySQL users and databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
