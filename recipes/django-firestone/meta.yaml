package:
  name: django-firestone
  version: "0.5.3"

source:
  fn: django-firestone-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-firestone/django-firestone-0.5.3.tar.gz
  md5: 44f6c5e3efad81a509aec9d385051988
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-firestone = django-firestone:main
    #
    # Would create an entry point called django-firestone that calls django-firestone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-preserialize
    - django-endless-pagination
    - itsdangerous
    - django-extensions
    - model-mommy
    - coverage
    - sphinx_rtd_theme
    - tablib

  run:
    - python
    - django
    - django-preserialize
    - django-endless-pagination
    - itsdangerous
    - django-extensions
    - model-mommy
    - coverage
    - sphinx_rtd_theme
    - tablib

test:
  # Python imports
  imports:
    - firestone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stargazer/django-firestone
  license: WTFPL
  summary: 'REST API Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
