package:
  name: alternatives
  version: "0.1.0"

source:
  fn: alternatives-0.1.0.zip
  url: https://pypi.python.org/packages/source/a/alternatives/alternatives-0.1.0.zip
  md5: ce8d73e29db2549ae45d662c1fe3f913
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alternatives = alternatives:main
    #
    # Would create an entry point called alternatives that calls alternatives.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - alternatives

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Alternatives api is just a syntax sugar for selecting alternative variants from some set of values'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
