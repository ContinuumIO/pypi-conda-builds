package:
  name: isotoma.recipe.eggpaths
  version: "0.1.0"

source:
  fn: isotoma.recipe.eggpaths-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.eggpaths/isotoma.recipe.eggpaths-0.1.0.tar.gz
  md5: 540766f28b35a54238d0c52209532498
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.eggpaths = isotoma.recipe.eggpaths:main
    #
    # Would create an entry point called isotoma.recipe.eggpaths that calls isotoma.recipe.eggpaths.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.eggpaths

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.eggpaths
  license: Apache Software License
  summary: 'Get the paths to certain eggs that have been installed, so they can be referenced in other buildout sections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
