package:
  name: collective.inventions
  version: "1.1"

source:
  fn: collective.inventions-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.inventions/collective.inventions-1.1.zip
  md5: 6e5b6bd6b267784437f1816d7a626489
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.inventions = collective.inventions:main
    #
    # Would create an entry point called collective.inventions that calls collective.inventions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

test:
  # Python imports
  imports:
    - collective
    - collective.inventions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/xdvtheme.inventions
  license: GPL V2
  summary: 'A Diazo Skin for Plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
