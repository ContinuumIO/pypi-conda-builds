package:
  name: django-treasuremap
  version: "0.2"

source:
  fn: django-treasuremap-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-treasuremap/django-treasuremap-0.2.tar.gz
  md5: 53f22d789cab69a5ee4554b904f69c05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-treasuremap = django-treasuremap:main
    #
    # Would create an entry point called django-treasuremap that calls django-treasuremap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - treasuremap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silentsokolov/django-treasuremap
  license: MIT License
  summary: 'django-treasuremap app, makes it easy to store and display the location on the map using different providers (Google, Yandex).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
