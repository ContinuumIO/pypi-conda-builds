package:
  name: goog_dns
  version: "0.0.4"

source:
  fn: goog_dns-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/g/goog_dns/goog_dns-0.0.4.tar.gz
  md5: 8668e3553a3201aa90e320cc7419d333
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goog_dns = goog_dns:main
    #
    # Would create an entry point called goog_dns that calls goog_dns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - goog_dns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MrJester/goog_dns
  license: GNU General Public License v3 (GPLv3)
  summary: 'Python program to update Google Dynamic DNS. Useful for cronjob or some other automated mechanism.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
