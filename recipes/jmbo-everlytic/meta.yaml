package:
  name: jmbo-everlytic
  version: "0.1.3"

source:
  fn: jmbo-everlytic-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-everlytic/jmbo-everlytic-0.1.3.tar.gz
  md5: 4a7bf0ddb1b71805d7d33655778c0277
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-everlytic = jmbo-everlytic:main
    #
    # Would create an entry point called jmbo-everlytic that calls jmbo-everlytic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmlrpclib
    - jmbo-foundry
    - django-celery

  run:
    - python
    - xmlrpclib
    - jmbo-foundry
    - django-celery

test:
  # Python imports
  imports:
    - everlytic
    - everlytic.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-everlytic
  license: BSD License
  summary: 'Jmbo Everlytic mailing list subscription API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
