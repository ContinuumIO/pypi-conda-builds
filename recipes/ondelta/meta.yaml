package:
  name: ondelta
  version: "0.5.0"

source:
  fn: ondelta-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/o/ondelta/ondelta-0.5.0.tar.gz
  md5: 22e493cb55a65c6f205f176fa4764a71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ondelta = ondelta:main
    #
    # Would create an entry point called ondelta that calls ondelta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - ondelta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamhaney/django-ondelta
  license: MIT License
  summary: 'A mixin that allows models to register methods that are notified when their values change,or register a method that is notified of all changes. Basically, OnDeltaMixin implementsthe observer pattern.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
