package:
  name: ferra
  version: "0.1.0"

source:
  fn: Ferra-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Ferra/Ferra-0.1.0.tar.gz
  md5: d465f91f119b1f9079c8cb82bb9fa56d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ferra = ferra:main
    #
    # Would create an entry point called ferra that calls ferra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - Ferra
    - ferra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v3 (GPLv3) or GNU General Public License v3 or later (GPLv3+)
  summary: 'A pyglet wrapper providing new functions and classes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
