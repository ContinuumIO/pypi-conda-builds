package:
  name: django-organice-theme-fullpage
  version: "0.2"

source:
  fn: django-organice-theme-fullpage-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-organice-theme-fullpage/django-organice-theme-fullpage-0.2.tar.gz
  md5: 82c8ba495825c2ee2894f22521e4afbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-organice-theme-fullpage = django-organice-theme-fullpage:main
    #
    # Would create an entry point called django-organice-theme-fullpage that calls django-organice-theme-fullpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-organice-theme

  run:
    - python
    - django-organice-theme

test:
  # Python imports
  imports:
    - organice_theme_fullpage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://organice.io/themes/
  license: Apache Software License
  summary: 'A responsive full-page theme for django-organice.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
