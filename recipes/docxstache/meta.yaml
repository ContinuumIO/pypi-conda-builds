package:
  name: docxstache
  version: "0.0.3"

source:
  fn: docxstache-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/docxstache/docxstache-0.0.3.tar.gz
  md5: a8ddc25add3af23d5dc656afdbb2d169
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docxstache = docxstache:main
    #
    # Would create an entry point called docxstache that calls docxstache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pystache >=0.5.4

  run:
    - python
    - pystache >=0.5.4

test:
  # Python imports
  imports:
    - docxstache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/docxstache/
  license: LICENSE.txt
  summary: 'Mustache for docx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
