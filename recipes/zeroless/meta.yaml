package:
  name: zeroless
  version: "0.6.0"

source:
  fn: zeroless-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zeroless/zeroless-0.6.0.tar.gz
  md5: 72fef75fc0bf5f23098bc330da191c75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeroless = zeroless:main
    #
    # Would create an entry point called zeroless that calls zeroless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq

  run:
    - python
    - pyzmq

test:
  # Python imports
  imports:
    - zeroless
    - zeroless_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zmqless/zeroless
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'ZeroMQ for Pythonistas\xe2\x84\xa2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
