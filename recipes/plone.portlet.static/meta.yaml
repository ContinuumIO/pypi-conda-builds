package:
  name: plone.portlet.static
  version: "2.0.4"

source:
  fn: plone.portlet.static-2.0.4.zip
  url: https://pypi.python.org/packages/source/p/plone.portlet.static/plone.portlet.static-2.0.4.zip
  md5: ed7056b8148ff6e4c2b9f3779dca1298
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.portlet.static = plone.portlet.static:main
    #
    # Would create an entry point called plone.portlet.static that calls plone.portlet.static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.portlets
    - plone.app.portlets
    - plone.app.form >=1.1
    - plone.i18n
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope2

  run:
    - python
    - setuptools
    - setuptools
    - plone.portlets
    - plone.app.portlets
    - plone.app.form >=1.1
    - plone.i18n
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.portlet
    - plone.portlet.static
    - plone.portlet.static.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.portlet.static
  license: GNU General Public License (GPL)
  summary: 'A simple static HTML portlet for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
