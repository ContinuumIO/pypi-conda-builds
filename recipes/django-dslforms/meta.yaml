package:
  name: django-dslforms
  version: "1.1.1"

source:
  fn: django-dslforms-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dslforms/django-dslforms-1.1.1.tar.gz
  md5: 52c26bf9285fd0d38f59b4edf7ac6fe7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dslforms = django-dslforms:main
    #
    # Would create an entry point called django-dslforms that calls django-dslforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rply >=0.7.2

  run:
    - python
    - rply >=0.7.2

test:
  # Python imports
  imports:
    - dslforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/prestontimmons/django-dslforms
  license: UNKNOWN
  summary: 'Create simple Django forms with a mini-language in your templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
