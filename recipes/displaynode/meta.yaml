package:
  name: displaynode
  version: "0.2.0"

source:
  fn: DisplayNode-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DisplayNode/DisplayNode-0.2.0.tar.gz
  md5: a19522836e96a8ec028965c9aafd7dc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - displaynode = displaynode:main
    #
    # Would create an entry point called displaynode that calls displaynode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - DisplayNode
    - DisplayNode.examples
    - DisplayNode.tests
    - displaynode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.occiput.io/
  license: BSD License
  summary: 'Web-based plotting and data visualisation package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
