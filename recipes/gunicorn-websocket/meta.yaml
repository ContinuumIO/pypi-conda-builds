package:
  name: gunicorn-websocket
  version: "0.0.3"

source:
  fn: gunicorn-websocket-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gunicorn-websocket/gunicorn-websocket-0.0.3.tar.gz
  md5: 57a88f1e4c4dd15f808008b352d2859a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gunicorn-websocket = gunicorn-websocket:main
    #
    # Would create an entry point called gunicorn-websocket that calls gunicorn-websocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gunicorn
    - gevent

  run:
    - python
    - gunicorn
    - gevent

test:
  # Python imports
  imports:
    - gunicornwebsocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.dapps.douban.com/gunicorn-websocket
  license: BSD License
  summary: 'Websocket handler for the gunicorn server, a Python wsgi server, fork and modify from gevent-websocket'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
