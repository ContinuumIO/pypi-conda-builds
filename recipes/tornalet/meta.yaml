package:
  name: tornalet
  version: "1.0.2"

source:
  fn: tornalet-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornalet/tornalet-1.0.2.tar.gz
  md5: 71a9f4b13cef6d4718081bddb04ef38b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornalet = tornalet:main
    #
    # Would create an entry point called tornalet that calls tornalet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - greenlet
    - tornado

  run:
    - python
    - greenlet
    - tornado

test:
  # Python imports
  imports:
    - tornalet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Gawen/tornalet
  license: Apache Software License
  summary: 'Tornado + Greenlet = Beautiful'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
