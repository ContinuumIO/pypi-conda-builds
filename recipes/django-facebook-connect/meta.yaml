package:
  name: django-facebook-connect
  version: "1.0.1"

source:
  fn: django-facebook-connect-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-connect/django-facebook-connect-1.0.1.tar.gz
  md5: d5fa2ae9a10e8372079944ef9cdbfc7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-connect = django-facebook-connect:main
    #
    # Would create an entry point called django-facebook-connect that calls django-facebook-connect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - facebook_connect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/noamsu/django-facebook-connect
  license: MIT License
  summary: 'Add facebook connect authentication to your Django website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
