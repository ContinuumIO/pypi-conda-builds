package:
  name: django-formtools
  version: "1.0"

source:
  fn: django-formtools-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formtools/django-formtools-1.0.tar.gz
  md5: cec890c65bf0a6575162df153e5e3064
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formtools = django-formtools:main
    #
    # Would create an entry point called django-formtools that calls django-formtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - formtools
    - formtools.wizard
    - formtools.wizard.storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-formtools.readthedocs.org/en/latest/
  license: BSD License
  summary: 'A set of high-level abstractions for Django forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
