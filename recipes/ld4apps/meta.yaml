package:
  name: ld4apps
  version: "0.9.6"

source:
  fn: LD4Apps-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/l/ld4apps/LD4Apps-0.9.6.tar.gz
  md5: eb989dd76e7efd179160ee8e2fb51b2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ld4apps = ld4apps:main
    #
    # Would create an entry point called ld4apps that calls ld4apps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.2.1
    - python-dateutil ==2.2
    - pyjwt ==0.2.1
    - webob ==1.4
    - pymongo ==2.7
    - isodate ==0.5.0
    - rdflib ==4.2.0
    - rdflib-jsonld ==0.2
    - werkzeug ==0.9.5

  run:
    - python
    - requests ==2.2.1
    - python-dateutil ==2.2
    - pyjwt ==0.2.1
    - webob ==1.4
    - pymongo ==2.7
    - isodate ==0.5.0
    - rdflib ==4.2.0
    - rdflib-jsonld ==0.2
    - werkzeug ==0.9.5

test:
  # Python imports
  imports:
    - ld4apps
    - ld4apps.mongodbstorage
    - ld4apps.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ld4apps/lda-serverlib/tree/ld4apps
  license: Apache Software License
  summary: 'Linked Data for Applications Server Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
