package:
  name: django-openinghours
  version: "0.0.26"

source:
  fn: django-openinghours-0.0.26.tar.gz
  url: https://pypi.python.org/packages/source/d/django-openinghours/django-openinghours-0.0.26.tar.gz
  md5: 959ee32f0e8c07081f66af62044cf409
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-openinghours = django-openinghours:main
    #
    # Would create an entry point called django-openinghours that calls django-openinghours.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - openinghours
    - openinghours.templatetags
    - openinghours.tests
    - openinghours.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arteria.ch/django-openinghours
  license: The MIT License
  summary: 'A reusable Django app to work with opening hours.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
