package:
  name: bottledaemon
  version: "0.1.1"

source:
  fn: BottleDaemon-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BottleDaemon/BottleDaemon-0.1.1.tar.gz
  md5: e1624932850f64fab96d9a347fca97c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottledaemon = bottledaemon:main
    #
    # Would create an entry point called bottledaemon that calls bottledaemon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle
    - lockfile
    - python-daemon

  run:
    - python
    - bottle
    - lockfile
    - python-daemon

test:
  # Python imports
  imports:
    - bottledaemon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Bottle-DebugToolbar
  summary: 'A simple tool to help daemonize bottle applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
