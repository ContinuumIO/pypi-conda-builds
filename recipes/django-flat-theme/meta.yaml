package:
  name: django-flat-theme
  version: "0.9.3"

source:
  fn: django-flat-theme-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flat-theme/django-flat-theme-0.9.3.tar.gz
  md5: ce87d556d6080d3c8195f37149c02351
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flat-theme = django-flat-theme:main
    #
    # Would create an entry point called django-flat-theme that calls django-flat-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elky/django-flat-theme
  license: BSD
  summary: 'A flat theme for Django admin interface. Modern, fresh, simple.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
