package:
  name: django-threatmetrix
  version: "0.0.1"

source:
  fn: django-threatmetrix-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-threatmetrix/django-threatmetrix-0.0.1.tar.gz
  md5: 14d5e4b6064cd9450825fa35333dd293
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-threatmetrix = django-threatmetrix:main
    #
    # Would create an entry point called django-threatmetrix that calls django-threatmetrix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - threatmetrix
    - threatmetrix.templatetags
    - threatmetrix.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CashStar/django-threatmetrix
  license: UNKNOWN
  summary: 'Django App for adding the threatmetrix script tag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
