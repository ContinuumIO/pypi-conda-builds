package:
  name: java-random
  version: "1.0"

source:
  fn: java-random-1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/java-random/java-random-1.0.tar.gz
  md5: 5ba543de33dc0e6ad7df3c9c05453792
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - java-random = java-random:main
    #
    # Would create an entry point called java-random that calls java-random.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/MostAwesomeDude/java-random
  license: UNKNOWN
  summary: "Implementation of Java's Random"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
