package:
  name: collective.contacts
  version: "1.5.2"

source:
  fn: collective.contacts-1.5.2.zip
  url: https://pypi.python.org/packages/source/c/collective.contacts/collective.contacts-1.5.2.zip
  md5: 568ba7e8bb5623cd9fcb8dd5dd10f1c7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contacts = collective.contacts:main
    #
    # Would create an entry point called collective.contacts that calls collective.contacts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.atextensions
    - products.advancedquery

  run:
    - python
    - setuptools
    - setuptools
    - products.atextensions
    - products.advancedquery

test:
  # Python imports
  imports:
    - collective
    - collective.contacts
    - collective.contacts.Extensions
    - collective.contacts.browser
    - collective.contacts.content
    - collective.contacts.interfaces
    - collective.contacts.iso3166
    - collective.contacts.tests
    - collective.contacts.tests.integration
    - collective.contacts.tests.unit
    - collective.contacts.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.contacts
  license: GNU General Public License (GPL)
  summary: 'Address book product for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
