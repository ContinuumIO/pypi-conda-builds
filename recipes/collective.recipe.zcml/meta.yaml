package:
  name: collective.recipe.zcml
  version: "0.1"

source:
  fn: collective.recipe.zcml-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.zcml/collective.recipe.zcml-0.1.tar.gz
  md5: ca9679ccfeed2f0ef1520d54000ab2c0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.zcml = collective.recipe.zcml:main
    #
    # Would create an entry point called collective.recipe.zcml that calls collective.recipe.zcml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.zcml
    - collective.recipe.zcml.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home:  https://svn.plone.org/svn/collective/buildout/collective.recipe.zcml/trunk
  license: Zope Public License
  summary: 'ZCML slug generation to be used separately e.g for repoze based setups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
