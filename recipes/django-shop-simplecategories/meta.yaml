package:
  name: django-shop-simplecategories
  version: "0.0.5"

source:
  fn: django-shop-simplecategories-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-simplecategories/django-shop-simplecategories-0.0.5.tar.gz
  md5: e596570ea3a699386a4123c86049693f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-simplecategories = django-shop-simplecategories:main
    #
    # Would create an entry point called django-shop-simplecategories that calls django-shop-simplecategories.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-shop

  run:
    - python
    - django >=1.2
    - django-shop

test:
  # Python imports
  imports:
    - shop_simplecategories
    - shop_simplecategories.migrations
    - shop_simplecategories.templatetags
    - shop_simplecategories.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.django-shop.org/
  license: BSD License
  summary: 'A simple to use product categories module for django SHOP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
