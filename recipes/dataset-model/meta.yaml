package:
  name: dataset-model
  version: "0.1.2"

source:
  fn: dataset-model-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dataset-model/dataset-model-0.1.2.tar.gz
  md5: 3e1f1421187525919d0a91e876362205
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataset-model = dataset-model:main
    #
    # Would create an entry point called dataset-model that calls dataset-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dataset

  run:
    - python
    - dataset

test:
  # Python imports
  imports:
    - dataset_model
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fmartingr/dataset-model
  license: GPLv2
  summary: 'Easy model object to work with the dataset framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
