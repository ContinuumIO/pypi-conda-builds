package:
  name: plone.multilingual
  version: "1.2.1"

source:
  fn: plone.multilingual-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.multilingual/plone.multilingual-1.2.1.tar.gz
  md5: d999fd657b494ed5d4f2c0f993f8b1c0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.multilingual = plone.multilingual:main
    #
    # Would create an entry point called plone.multilingual that calls plone.multilingual.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.uuid
    - plone.app.uuid

  run:
    - python
    - setuptools
    - setuptools
    - plone.uuid
    - plone.app.uuid

test:
  # Python imports
  imports:
    - plone
    - plone.multilingual
    - plone.multilingual.tests
    - plone.multilingual.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.multilingual/
  license: GPL
  summary: 'Multilingual extensions core package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
