package:
  name: lava-fastmodels
  version: "0.1.dev12"

source:
  fn: lava-fastmodels-0.1.dev12.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-fastmodels/lava-fastmodels-0.1.dev12.tar.gz
  md5: ae019a301ce14f6e2d665e973d509610
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-fastmodels = lava-fastmodels:main
    #
    # Would create an entry point called lava-fastmodels that calls lava-fastmodels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - json-document
    - lava-tool >=0.3a1
    - versiontools >=1.8.2
    - lava-utils-interface >=1.0

  run:
    - python
    - setuptools
    - json-document
    - lava-tool >=0.3a1
    - versiontools >=1.8.2
    - lava-utils-interface >=1.0

test:
  # Python imports
  imports:
    - lava
    - lava.builder
    - lava.fastmodels
    - lava.nfs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3
  summary: 'ARM Fast Models support code for LAVA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
