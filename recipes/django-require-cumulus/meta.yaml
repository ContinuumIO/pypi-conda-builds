package:
  name: django-require-cumulus
  version: "0.2"

source:
  fn: django-require-cumulus-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-require-cumulus/django-require-cumulus-0.2.tar.gz
  md5: 31a0cc2cc41a453c84332cf4765ff080
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-require-cumulus = django-require-cumulus:main
    #
    # Would create an entry point called django-require-cumulus that calls django-require-cumulus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-require ==1.0.6
    - django-cumulus ==1.0.13

  run:
    - python
    - django-require ==1.0.6
    - django-cumulus ==1.0.13

test:
  # Python imports
  imports:
    - require_cumulus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sourcelair/django-require-cumulus
  license: django-require-i18n
  summary: 'Manage static files in Django with r.js, serve from Rackspace Cloud files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
