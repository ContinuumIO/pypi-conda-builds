package:
  name: boutiquecommons
  version: "0.1.3"

source:
  fn: BoutiqueCommons-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/B/BoutiqueCommons/BoutiqueCommons-0.1.3.tar.gz
  md5: 0d695849d37b925d07cd95618d6f048a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boutiquecommons = boutiquecommons:main
    #
    # Would create an entry point called boutiquecommons that calls boutiquecommons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.10.1
    - jinja2 ==2.7.3

  run:
    - python
    - flask ==0.10.1
    - jinja2 ==2.7.3

test:
  # Python imports
  imports:
    - boutiquecommons
    - boutiquecommons.template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Boutique Commons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
