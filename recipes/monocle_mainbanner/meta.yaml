package:
  name: monocle_mainbanner
  version: "0.1.2"

source:
  fn: monocle_mainbanner-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/monocle_mainbanner/monocle_mainbanner-0.1.2.tar.gz
  md5: 1a7a9cc550b608a77250955a8d51eb29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monocle_mainbanner = monocle_mainbanner:main
    #
    # Would create an entry point called monocle_mainbanner that calls monocle_mainbanner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monocle_mainbanner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Sample app for django-monocle project generated by scaffolder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
