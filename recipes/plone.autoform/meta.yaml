package:
  name: plone.autoform
  version: "1.6.1"

source:
  fn: plone.autoform-1.6.1.zip
  url: https://pypi.python.org/packages/source/p/plone.autoform/plone.autoform-1.6.1.zip
  md5: 211ad073876a9d5385efae27da985548
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.autoform = plone.autoform:main
    #
    # Would create an entry point called plone.autoform that calls plone.autoform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.schema
    - zope.security
    - zope.dottedname
    - plone.supermodel >=1.1dev
    - plone.z3cform
    - z3c.form

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.schema
    - zope.security
    - zope.dottedname
    - plone.supermodel >=1.1dev
    - plone.z3cform
    - z3c.form

test:
  # Python imports
  imports:
    - plone
    - plone.autoform
    - plone.autoform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dexterity
  license: LGPL
  summary: 'Tools to construct z3c.form forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
