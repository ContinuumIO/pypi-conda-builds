package:
  name: django-account
  version: "0.1.14"

source:
  fn: django-account-0.1.14.tar.gz
  url: https://pypi.python.org/packages/source/d/django-account/django-account-0.1.14.tar.gz
  md5: 7a7403807db3abe1b3a0fe1a276fb97b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-account = django-account:main
    #
    # Would create an entry point called django-account that calls django-account.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - account
    - account.management
    - account.management.commands
    - account.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/django-account/
  license: BSD License
  summary: 'Django application for registration and authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
