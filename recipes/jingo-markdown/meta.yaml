package:
  name: jingo-markdown
  version: "1.3"

source:
  fn: jingo-markdown-1.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jingo-markdown/jingo-markdown-1.3.tar.gz
  md5: 99085604f29e9ce7790546bb94494c3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jingo-markdown = jingo-markdown:main
    #
    # Would create an entry point called jingo-markdown that calls jingo-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown

  run:
    - python
    - markdown

test:
  # Python imports
  imports:
    - jingo_markdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mayapurmedia/jingo-markdown
  license: MIT License
  summary: 'Jingo (Jinja2) extension and filter for parse markdown text markup.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
