package:
  name: budgetdatapackage
  version: "0.0.4"

source:
  fn: budgetdatapackage-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/budgetdatapackage/budgetdatapackage-0.0.4.tar.gz
  md5: ff56a45cd5f50f1f63a2bfca2499b24e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - budgetdatapackage = budgetdatapackage:main
    #
    # Would create an entry point called budgetdatapackage that calls budgetdatapackage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datapackage >=0.5.2

  run:
    - python
    - datapackage >=0.5.2

test:
  # Python imports
  imports:
    - budgetdatapackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tryggvib/budgetdatapackage
  license: GNU General Public License v3 (GPLv3)
  summary: 'Manage and load Budget Data Packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
