package:
  name: django-oembed
  version: "0.1.3"

source:
  fn: django-oembed-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oembed/django-oembed-0.1.3.tar.gz
  md5: 0496abe7e49e2a720556683c37354b99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oembed = django-oembed:main
    #
    # Would create an entry point called django-oembed that calls django-oembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oembed
    - oembed.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-oembed.googlecode.com
  license: BSD License
  summary: 'A collection of Django tools which make it easy to changetext filled with oembed links into the embedded objects themselves.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
