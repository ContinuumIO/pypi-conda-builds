package:
  name: flask-bootstrap3
  version: "3.1.1.3"

source:
  fn: Flask-Bootstrap3-3.1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Bootstrap3/Flask-Bootstrap3-3.1.1.3.tar.gz
  md5: 2cecd5e093674ba9453481efd7a10f22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bootstrap3 = flask-bootstrap3:main
    #
    # Would create an entry point called flask-bootstrap3 that calls flask-bootstrap3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_bootstrap3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryanolson/flask-bootstrap3
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
