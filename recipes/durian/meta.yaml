package:
  name: durian
  version: "0.1.0"

source:
  fn: durian-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/durian/durian-0.1.0.tar.gz
  md5: 32ac3b5d8f3135ad25b660b7421bdea9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - durian = durian:main
    #
    # Would create an entry point called durian that calls durian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-unittest-depth
    - celery

  run:
    - python
    - django-unittest-depth
    - celery

test:
  # Python imports
  imports:
    - durian
    - durian.match
    - durian.tests
    - testproj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ask/durian/
  license: BSD License
  summary: 'Webhooks for Django **EXPERIMENTAL**'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
