package:
  name: thingsbus
  version: "0.1.0"

source:
  fn: thingsbus-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/thingsbus/thingsbus-0.1.0.tar.gz
  md5: 22017be5cf3e832b9e9cb1d595aec957
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thingsbus = thingsbus:main
    #
    # Would create an entry point called thingsbus that calls thingsbus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - dnspython
    - zmqfan >=0.7
    - msgpack-python

  run:
    - python
    - dnspython
    - zmqfan >=0.7
    - msgpack-python

test:
  # Python imports
  imports:
    - thingsbus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eastein/thingsbus/
  license: UNKNOWN
  summary: 'Thingsbus data broker, input, output, and dispatch.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
