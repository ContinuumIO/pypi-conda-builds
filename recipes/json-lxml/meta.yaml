package:
  name: json-lxml
  version: "0.1.3"

source:
  fn: json-lxml-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/j/json-lxml/json-lxml-0.1.3.tar.gz
  md5: cb3df437510a3e4d9a7d5e5cd95bbc8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-lxml = json-lxml:main
    #
    # Would create an entry point called json-lxml that calls json-lxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tengu/py-json-lxml
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Convert JSON to lxml.etree so that xpath can be applied.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
