package:
  name: globetrotter
  version: "0.1.0"

source:
  fn: globetrotter-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/globetrotter/globetrotter-0.1.0.tar.gz
  md5: 1e2ab42d5505c619d6201009554bbe12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - globetrotter = globetrotter:main
    #
    # Would create an entry point called globetrotter that calls globetrotter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycountry ==0.14.3
    - wsgiref ==0.1.2

  run:
    - python
    - pycountry ==0.14.3
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - globetrotter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/larsyencken/globetrotter
  license: ISC
  summary: 'Approximate country and language name matching.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
