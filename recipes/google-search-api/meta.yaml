package:
  name: google-search-api
  version: "1.1.1"

source:
  fn: Google-Search-API-1.1.1.zip
  url: https://pypi.python.org/packages/source/G/Google-Search-API/Google-Search-API-1.1.1.zip
  md5: 28f8045d3bf55397ef705da4a187c5a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google-search-api = google-search-api:main
    #
    # Would create an entry point called google-search-api that calls google-search-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abenassi/Google-Search-API
  license: MIT
  summary: 'Search in google'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
