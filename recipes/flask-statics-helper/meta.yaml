package:
  name: flask-statics-helper
  version: "1.0.0"

source:
  fn: Flask-Statics-Helper-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Statics-Helper/Flask-Statics-Helper-1.0.0.tar.gz
  md5: 50f8a8a7edbbd07a9e2207800366378c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-statics-helper = flask-statics-helper:main
    #
    # Would create an entry point called flask-statics-helper that calls flask-statics-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_statics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Robpol86/Flask-Statics-Helper
  license: MIT License
  summary: 'Provides Bootstrap3 and other static resources in a modular fashion.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
