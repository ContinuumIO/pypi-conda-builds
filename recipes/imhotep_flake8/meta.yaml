package:
  name: imhotep_flake8
  version: "0.2.1"

source:
  fn: imhotep_flake8-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/imhotep_flake8/imhotep_flake8-0.2.1.tar.gz
  md5: 82904b9a810d1572dfd8468e5f345e69
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imhotep_flake8 = imhotep_flake8:main
    #
    # Would create an entry point called imhotep_flake8 that calls imhotep_flake8.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8

  run:
    - python
    - setuptools
    - flake8

test:
  # Python imports
  imports:
    - imhotep_flake8

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glogiotatidis/imhotep_flake8
  license: UNKNOWN
  summary: 'An imhotep plugin for flake8 validation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
