package:
  name: hentry
  version: "0.1"

source:
  fn: hentry-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hentry/hentry-0.1.tar.gz
  md5: 4d44f9a1745e5851ec6e84d20e49fd02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hentry = hentry:main
    #
    # Would create an entry point called hentry that calls hentry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - lxml
    - cssselect
    - python-dateutil
    - pyopenssl
    - ndg-httpsclient
    - pyasn1

  run:
    - python
    - requests
    - lxml
    - cssselect
    - python-dateutil
    - pyopenssl
    - ndg-httpsclient
    - pyasn1

test:
  # Python imports
  imports:
    - hentry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lepture/hentry
  license: BSD License
  summary: 'Parse hentry from microformats.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
