package:
  name: django-generic-links
  version: "0.2.0"

source:
  fn: django-generic-links-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-links/django-generic-links-0.2.0.tar.gz
  md5: 8b3a43a74fe77386cd3f6252c6232b93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-links = django-generic-links:main
    #
    # Would create an entry point called django-generic-links that calls django-generic-links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - generic_links
    - generic_links.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://matagus.github.com/django-generic-links/
  license: BSD License
  summary: 'Simple and generic application for Django projects to attach and handle links for any object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
