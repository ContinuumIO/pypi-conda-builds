package:
  name: colour-runner
  version: "0.0.4"

source:
  fn: colour-runner-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/colour-runner/colour-runner-0.0.4.tar.gz
  md5: 4caa1f0d7c6c2c502be71fc7b181cd25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colour-runner = colour-runner:main
    #
    # Would create an entry point called colour-runner that calls colour-runner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blessings
    - pygments

  run:
    - python
    - blessings
    - pygments

test:
  # Python imports
  imports:
    - colour_runner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/meshy/colour-runner
  license: MIT License
  summary: 'Colour formatting for unittest tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
