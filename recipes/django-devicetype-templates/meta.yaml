package:
  name: django-devicetype-templates
  version: "0.1.9"

source:
  fn: django-devicetype-templates-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-devicetype-templates/django-devicetype-templates-0.1.9.tar.gz
  md5: 2e6e4ddd7c305b8f7d4afcf8f96a0ea3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-devicetype-templates = django-devicetype-templates:main
    #
    # Would create an entry point called django-devicetype-templates that calls django-devicetype-templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - django >=1.3.1,<1.7

  run:
    - python
    - setuptools >=0.6b1
    - django >=1.3.1,<1.7

test:
  # Python imports
  imports:
    - devicetype

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/whit/django-devicetype-templates
  license: BSD License
  summary: 'Django library to serve different templates for different device types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
