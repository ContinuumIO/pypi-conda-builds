package:
  name: flask-edits
  version: "0.8"

source:
  fn: Flask-Edits-0.8.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Edits/Flask-Edits-0.8.tar.gz
  md5: 733290b75ce0d3d15ece537b7db80797
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-edits = flask-edits:main
    #
    # Would create an entry point called flask-edits that calls flask-edits.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7

  run:
    - python
    - flask >=0.7

test:
  # Python imports
  imports:
    - flask_edits

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nathancahill/flask-edits
  license: BSD
  summary: 'Editable Content in Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
