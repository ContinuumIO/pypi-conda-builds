package:
  name: cubicweb-pdfexport
  version: "0.1.0"

source:
  fn: cubicweb-pdfexport-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-pdfexport/cubicweb-pdfexport-0.1.0.tar.gz
  md5: fbb174718b72bac80084885c9abebc58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-pdfexport = cubicweb-pdfexport:main
    #
    # Would create an entry point called cubicweb-pdfexport that calls cubicweb-pdfexport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.8.0
    - pysixt >=0.1.0

  run:
    - python
    - cubicweb >=3.8.0
    - pysixt >=0.1.0

test:
  # Python imports
  imports:
    - pdfexport
    - pdfexport.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-pdfexport
  license: LGPL
  summary: 'export any page as a pdf document'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
