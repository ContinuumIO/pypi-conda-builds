package:
  name: pypi-classifiers
  version: "0.1"

source:
  fn: pypi-classifiers-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pypi-classifiers/pypi-classifiers-0.1.tar.gz
  md5: 4f392cdccd0a3812a2fb79d516062b9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypi-classifiers = pypi-classifiers:main
    #
    # Would create an entry point called pypi-classifiers that calls pypi-classifiers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pypiclassifiers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/pypi-classifiers
  license: MIT License
  summary: 'Use a GUI to select PyPI-classifiers and include them in asetup.py'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
