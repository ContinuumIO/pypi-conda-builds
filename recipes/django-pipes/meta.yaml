package:
  name: django-pipes
  version: "0.2"

source:
  fn: django-pipes-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pipes/django-pipes-0.2.tar.gz
  md5: 34acae7b3276e0f3ec9d4767d0ce0410
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pipes = django-pipes:main
    #
    # Would create an entry point called django-pipes that calls django-pipes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_pipes
    - django_pipes.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mallipeddi/django-pipes/
  license: UNKNOWN
  summary: 'Django plugin to handle remote-JSON data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
