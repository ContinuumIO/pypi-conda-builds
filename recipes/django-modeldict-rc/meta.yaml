package:
  name: django-modeldict-rc
  version: "1.3.1"

source:
  fn: django-modeldict-rc-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modeldict-rc/django-modeldict-rc-1.3.1.tar.gz
  md5: 5d2372d1386689f2a64ce3240906c8d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modeldict-rc = django-modeldict-rc:main
    #
    # Would create an entry point called django-modeldict-rc that calls django-modeldict-rc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - modeldict
    - tests
    - tests.modeldict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/olevinsky/django-modeldict
  license: UNKNOWN
  summary: 'Stores a model as a dictionary'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
