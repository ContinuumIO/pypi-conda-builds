package:
  name: insight-bert
  version: "1.0.1"

source:
  fn: insight-bert-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/insight-bert/insight-bert-1.0.1.tar.gz
  md5: 2079f3e852a4f5c789328a741267de64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - insight-bert = insight-bert:main
    #
    # Would create an entry point called insight-bert that calls insight-bert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - erlastic

  run:
    - python
    - erlastic

test:
  # Python imports
  imports:
    - bert

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbgo/python-bert/tree/insight-bert
  license: BSD License
  summary: 'BERT Serialization Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
