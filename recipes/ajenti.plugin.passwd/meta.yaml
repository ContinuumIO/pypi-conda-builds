package:
  name: ajenti.plugin.passwd
  version: "0.12"

source:
  fn: ajenti.plugin.passwd-0.12.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.passwd/ajenti.plugin.passwd-0.12.tar.gz
  md5: 54c84ddb186d8c26182809c14e2c4104
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.passwd = ajenti.plugin.passwd:main
    #
    # Would create an entry point called ajenti.plugin.passwd that calls ajenti.plugin.passwd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core

  run:
    - python
    - aj
    - ajenti.plugin.core

test:
  # Python imports
  imports:
    - ajenti_plugin_passwd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'User DB API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
