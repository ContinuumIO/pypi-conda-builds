package:
  name: antigravity
  version: "0.1"

source:
  fn: antigravity-0.1.zip
  url: https://pypi.python.org/packages/source/a/antigravity/antigravity-0.1.zip
  md5: 371367b1f429f8fe19857c5eef1de491
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - antigravity = antigravity:main
    #
    # Would create an entry point called antigravity that calls antigravity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - antigravity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fabien.schwob.org/antigravity/
  license: UNKNOWN
  summary: 'A really simple module that allow everyone to do "import antigravity"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
