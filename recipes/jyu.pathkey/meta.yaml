package:
  name: jyu.pathkey
  version: "1.1.3"

source:
  fn: jyu.pathkey-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jyu.pathkey/jyu.pathkey-1.1.3.tar.gz
  md5: 8548eacb53055deb93a2d7aca0550241
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jyu.pathkey = jyu.pathkey:main
    #
    # Would create an entry point called jyu.pathkey that calls jyu.pathkey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - jyu
    - jyu.pathkey
    - jyu.pathkey.browser
    - jyu.pathkey.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/jyu.pathkey
  license: GPL
  summary: 'Restricts access to Plone content without proper pathkey'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
