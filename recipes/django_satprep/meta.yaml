package:
  name: django_satprep
  version: "0.1.4"

source:
  fn: django_satprep-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django_satprep/django_satprep-0.1.4.tar.gz
  md5: 818c04704044edb1d091b076e4b11934
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_satprep = django_satprep:main
    #
    # Would create an entry point called django_satprep that calls django_satprep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_satprep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mtrichardson/django_satprep/
  license: UNKNOWN
  summary: 'Basic testing conveniences for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
