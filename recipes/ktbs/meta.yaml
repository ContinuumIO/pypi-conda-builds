package:
  name: ktbs
  version: "0.3"

source:
  fn: kTBS-0.3.tar.gz
  url: https://pypi.python.org/packages/source/k/kTBS/kTBS-0.3.tar.gz
  md5: 991bbe42b6b32e91fec97fb9879e2893
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ktbs = ktbs:main
    #
    # Would create an entry point called ktbs that calls ktbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rdflib ==4.1.2
    - httplib2 ==0.9
    - webob ==1.4
    - pyld ==0.5.1
    - posix_ipc ==0.9.8

  run:
    - python
    - rdflib ==4.1.2
    - httplib2 ==0.9
    - webob ==1.4
    - pyld ==0.5.1
    - posix_ipc ==0.9.8

test:
  # Python imports
  imports:
    - ktbs
    - ktbs.api
    - ktbs.engine
    - ktbs.methods
    - ktbs.plugins
    - ktbs.serpar
    - rdfrest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ktbs/ktbs
  license: LGPL v3
  summary: 'A kernel for trace-based systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
