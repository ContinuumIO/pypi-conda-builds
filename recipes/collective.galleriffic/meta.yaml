package:
  name: collective.galleriffic
  version: "1.1.6"

source:
  fn: collective.galleriffic-1.1.6.zip
  url: https://pypi.python.org/packages/source/c/collective.galleriffic/collective.galleriffic-1.1.6.zip
  md5: 5e16d0c5bbd1c2739cdb4c0380f2d3de
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.galleriffic = collective.galleriffic:main
    #
    # Would create an entry point called collective.galleriffic that calls collective.galleriffic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.gallery
    - collective.configviews
    - collective.js.galleriffic

  run:
    - python
    - setuptools
    - setuptools
    - collective.gallery
    - collective.configviews
    - collective.js.galleriffic

test:
  # Python imports
  imports:
    - collective
    - collective.galleriffic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.galleriffic
  license: GPL
  summary: 'Galleriffic integration into Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
