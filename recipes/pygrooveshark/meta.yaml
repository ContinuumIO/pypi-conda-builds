package:
  name: pygrooveshark
  version: "3.2"

source:
  fn: pygrooveshark-3.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pygrooveshark/pygrooveshark-3.2.tar.gz
  md5: e8cf14ea44ee332df31ee8d28b66d041
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygrooveshark = pygrooveshark:main
    #
    # Would create an entry point called pygrooveshark that calls pygrooveshark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - grooveshark
    - grooveshark.classes
    - pygrooveshark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/koehlma/pygrooveshark
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'An unofficial Grooveshark API for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
