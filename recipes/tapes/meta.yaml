package:
  name: tapes
  version: "0.3"

source:
  fn: tapes-0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tapes/tapes-0.3.tar.gz
  md5: aabc1e6c987a624800077ed4f504f18e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tapes = tapes:main
    #
    # Would create an entry point called tapes that calls tapes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.9.0
    - addict ==0.3.2
    - sortedcontainers ==0.9.5

  run:
    - python
    - six ==1.9.0
    - addict ==0.3.2
    - sortedcontainers ==0.9.5

test:
  # Python imports
  imports:
    - tapes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/emilssolmanis/tapes
  license: Apache Software License
  summary: 'Metrics for Python processes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
