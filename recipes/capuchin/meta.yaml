package:
  name: capuchin
  version: "0.0.6"

source:
  fn: capuchin-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/capuchin/capuchin-0.0.6.tar.gz
  md5: d5c497df6656fe09559142f72f47f2a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - capuchin = capuchin:main
    #
    # Would create an entry point called capuchin that calls capuchin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=4.0.0
    - requests >=2.3.0

  run:
    - python
    - tornado >=4.0.0
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - capuchin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/capuchin
  license: BSD License
  summary: 'Simple wrapper around tornado that uses environment variables for host, port, endpoint prefix. Also uses the py-hancock library for the ability to sign endpoints. Endpoints for `time`, `ping`, and `status` are automatically added as well.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
