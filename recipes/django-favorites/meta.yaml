package:
  name: django-favorites
  version: "0.2"

source:
  fn: django-favorites-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-favorites/django-favorites-0.2.tar.gz
  md5: 2fcb6a30bc6ce58432dce28431ee7f42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-favorites = django-favorites:main
    #
    # Would create an entry point called django-favorites that calls django-favorites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - favorites
    - favorites.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeffkit/django-favorites
  license: UNKNOWN
  summary: 'favorites app for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
