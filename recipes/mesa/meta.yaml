package:
  name: mesa
  version: "0.5.1"

source:
  fn: Mesa-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mesa/Mesa-0.5.1.tar.gz
  md5: cafd5040134826bc8ad984d732ec8e10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mesa = mesa:main
    #
    # Would create an entry point called mesa that calls mesa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - numpy
    - pandas
    - scipy

  run:
    - python
    - tornado
    - numpy
    - pandas
    - scipy

test:
  # Python imports
  imports:
    - mesa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/projectmesa/mesa
  license: MIT License
  summary: 'Agent-based modeling framework in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
