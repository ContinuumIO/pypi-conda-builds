package:
  name: taiga-contrib-slack
  version: "0.0.5"

source:
  fn: taiga-contrib-slack-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/t/taiga-contrib-slack/taiga-contrib-slack-0.0.5.tar.gz
  md5: 65c66d5ab9a7c812aeaa545af3a2fd0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taiga-contrib-slack = taiga-contrib-slack:main
    #
    # Would create an entry point called taiga-contrib-slack that calls taiga-contrib-slack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - taiga_contrib_slack
    - taiga_contrib_slack.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/taigaio/taiga-contrib-slack
  license: GNU Affero General Public License v3
  summary: 'The Taiga plugin for slack integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
