package:
  name: collective.registry
  version: "1.0"

source:
  fn: collective.registry-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.registry/collective.registry-1.0.tar.gz
  md5: 37417e29159fe95dc6b327bc9622414f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.registry = collective.registry:main
    #
    # Would create an entry point called collective.registry that calls collective.registry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.registry
    - collective.registry.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.registry
  license: GPL
  summary: 'Extend plone.app.registry by implementing IRegistry with an adapter on context'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
