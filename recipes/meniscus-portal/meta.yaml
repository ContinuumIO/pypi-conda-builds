package:
  name: meniscus-portal
  version: "0.2.1.7"

source:
  fn: meniscus-portal-0.2.1.7.tar.gz
  url: https://pypi.python.org/packages/source/m/meniscus-portal/meniscus-portal-0.2.1.7.tar.gz
  md5: f8e7770aff76a4a779cc3103709443ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meniscus-portal = meniscus-portal:main
    #
    # Would create an entry point called meniscus-portal that calls meniscus-portal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - pyzmq
    - simplejson

  run:
    - python
    - tornado
    - pyzmq
    - simplejson

test:
  # Python imports
  imports:
    - portal
    - portal.input
    - portal.input.syslog
    - portal.output

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ProjectMeniscus/portal
  license: Apache 2.0
  summary: 'low level parsing bindings for meniscus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
