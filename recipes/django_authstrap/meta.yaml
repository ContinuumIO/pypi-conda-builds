package:
  name: django_authstrap
  version: "0.2.2"

source:
  fn: django_authstrap-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_authstrap/django_authstrap-0.2.2.tar.gz
  md5: a575b0a14652fb48637026d2065f1f90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_authstrap = django_authstrap:main
    #
    # Would create an entry point called django_authstrap that calls django_authstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-bootstrap-toolkit

  run:
    - python
    - django-bootstrap-toolkit

test:
  # Python imports
  imports:
    - authstrap
    - django_authstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alixedi/django_authstrap
  license: BSD License
  summary: 'Bootstrap templates for django.contrib.auth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
