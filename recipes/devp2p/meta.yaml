package:
  name: devp2p
  version: "0.0.4"

source:
  fn: devp2p-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/devp2p/devp2p-0.0.4.tar.gz
  md5: f7708a2c250b7d09bd547902678a2a9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - devp2p = devp2p:main
    #
    # Would create an entry point called devp2p that calls devp2p.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wheel
    - tox
    - pytest-capturelog
    - pytest-timeout
    - pysha3
    - bitcoin
    - gevent
    - pyelliptic
    - coverage
    - pytest
    - tinyrpc
    - pycrypto
    - ipaddress
    - rlp >=0.3.7

  run:
    - python
    - wheel
    - tox
    - pytest-capturelog
    - pytest-timeout
    - pysha3
    - bitcoin
    - gevent
    - pyelliptic
    - coverage
    - pytest
    - tinyrpc
    - pycrypto
    - ipaddress
    - rlp >=0.3.7

test:
  # Python imports
  imports:
    - devp2p
    - devp2p.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ethereum/pydevp2p
  license: BSD License
  summary: 'Python implementation of the Ethereum P2P stack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
