package:
  name: flask-bundle-system
  version: "0.1"

source:
  fn: flask-bundle-system-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-bundle-system/flask-bundle-system-0.1.tar.gz
  md5: ec7ff8128d5637c2333454e556fbf14b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bundle-system = flask-bundle-system:main
    #
    # Would create an entry point called flask-bundle-system that calls flask-bundle-system.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_bundlesystem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yograterol/flask-bundle-system
  license: BSD License
  summary: 'Flask extension for work with blueprints as bundles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
