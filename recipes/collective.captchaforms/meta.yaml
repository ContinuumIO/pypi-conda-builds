package:
  name: collective.captchaforms
  version: "0.1-20090814"

source:
  fn: collective.captchaforms-0.1-20090814.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.captchaforms/collective.captchaforms-0.1-20090814.tar.gz
  md5: f2846e14ae1234c6b26dc0a6061e987e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.captchaforms = collective.captchaforms:main
    #
    # Would create an entry point called collective.captchaforms that calls collective.captchaforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.captchaforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.captchaforms
  license: GPL
  summary: 'Plone forms with captcha'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
