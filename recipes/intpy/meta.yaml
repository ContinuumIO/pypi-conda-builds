package:
  name: intpy
  version: "0.1.3"

source:
  fn: IntPy-0.1.3.tar.bz2
  url: https://pypi.python.org/packages/source/I/IntPy/IntPy-0.1.3.tar.bz2
  md5: 6cb67a6b41fe00f70d876a084e183c4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intpy = intpy:main
    #
    # Would create an entry point called intpy that calls intpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - intpy
    - intpy.ireal
    - intpy.support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rafaelbarreto.wordpress.com/intpy/
  license: GNU General Public License (GPL)
  summary: 'Interval Arithmetic package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
