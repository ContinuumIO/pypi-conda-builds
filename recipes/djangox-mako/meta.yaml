package:
  name: djangox-mako
  version: "1.0.7"

source:
  fn: djangox-mako-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/djangox-mako/djangox-mako-1.0.7.tar.gz
  md5: 79ef1e819420cae332602b102c5b9a36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangox-mako = djangox-mako:main
    #
    # Would create an entry point called djangox-mako that calls djangox-mako.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mako
    - django

  run:
    - python
    - mako
    - django

test:
  # Python imports
  imports:
    - djangox
    - djangox.mako

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youngrok/djangox-mako
  license: BSD License
  summary: 'Mako template adapter for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
