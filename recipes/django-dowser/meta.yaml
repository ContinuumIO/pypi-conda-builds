package:
  name: django-dowser
  version: "0.1.3"

source:
  fn: django-dowser-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dowser/django-dowser-0.1.3.tar.gz
  md5: 50c136d6bf62b229eb5c73b4850aab2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dowser = django-dowser:main
    #
    # Would create an entry point called django-dowser that calls django-dowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_dowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/munhitsu/django-dowser
  license: BSD License
  summary: 'Django fork of amazing memory leaks tracking application for python wsgi - the Dowser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
