package:
  name: products.ploneontology
  version: "1.0.0"

source:
  fn: Products.PloneOntology-1.0.0.zip
  url: https://pypi.python.org/packages/source/P/Products.PloneOntology/Products.PloneOntology-1.0.0.zip
  md5: 4a82da73aa6795d93e3a33400f37c69b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.ploneontology = products.ploneontology:main
    #
    # Would create an entry point called products.ploneontology that calls products.ploneontology.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.relations

  run:
    - python
    - setuptools
    - setuptools
    - products.relations

test:
  # Python imports
  imports:
    - Products
    - Products.PloneOntology
    - Products.PloneOntology.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/Products.PloneOntology
  license: GPL
  summary: 'A discussion board for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
