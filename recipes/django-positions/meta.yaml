package:
  name: django-positions
  version: "0.5.2"

source:
  fn: django-positions-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-positions/django-positions-0.5.2.tar.gz
  md5: e65da747d883fccb6d676c64bed95d7a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-positions = django-positions:main
    #
    # Would create an entry point called django-positions that calls django-positions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - setuptools

  run:
    - python
    - django
    - setuptools

test:
  # Python imports
  imports:
    - positions
    - positions.examples
    - positions.examples.generic
    - positions.examples.lists
    - positions.examples.nodes
    - positions.examples.photos
    - positions.examples.restaurants
    - positions.examples.school
    - positions.examples.store
    - positions.examples.todo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jpwatts/django-positions
  license: BSD License
  summary: '(Django Positions needs a new maintainer) A Django field for custom model ordering'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
