package:
  name: eastofeaton.paypal
  version: "1.0a1"

source:
  fn: eastofeaton.paypal-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/e/eastofeaton.paypal/eastofeaton.paypal-1.0a1.tar.gz
  md5: f8a533a22995218ecc896942df31e84b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eastofeaton.paypal = eastofeaton.paypal:main
    #
    # Would create an entry point called eastofeaton.paypal that calls eastofeaton.paypal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - paypal

  run:
    - python
    - setuptools
    - setuptools
    - paypal

test:
  # Python imports
  imports:
    - eastofeaton
    - eastofeaton.paypal
    - eastofeaton.paypal.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'A zca utility providing the paypal API for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
