package:
  name: correcthorsebatterystaple
  version: "0.1"

source:
  fn: correcthorsebatterystaple-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/correcthorsebatterystaple/correcthorsebatterystaple-0.1.tar.gz
  md5: 6bda77940002ddd4e8337e4fded99579
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - correcthorsebatterystaple = correcthorsebatterystaple:main
    #
    # Would create an entry point called correcthorsebatterystaple that calls correcthorsebatterystaple.main()

    - correcthorsebatterystaple=correcthorsebatterystaple.app:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - click ==3.3

  run:
    - python
    - click ==3.3

test:
  # Python imports
  imports:
    - correcthorsebatterystaple

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - correcthorsebatterystaple --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Frozenball/correcthorsebatterystaple
  license: BSD License
  summary: 'Test application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
