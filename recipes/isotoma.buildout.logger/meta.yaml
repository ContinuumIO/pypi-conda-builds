package:
  name: isotoma.buildout.logger
  version: "0.0.0"

source:
  fn: isotoma.buildout.logger-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.buildout.logger/isotoma.buildout.logger-0.0.0.tar.gz
  md5: 6f94a49bf47eef7b4b7d75ac81982576
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.buildout.logger = isotoma.buildout.logger:main
    #
    # Would create an entry point called isotoma.buildout.logger that calls isotoma.buildout.logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.buildout
    - isotoma.buildout.logger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.buildout.logger
  license: Apache Software License
  summary: 'Buildout extension to produce a log of actions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
