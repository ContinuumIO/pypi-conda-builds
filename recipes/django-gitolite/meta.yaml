package:
  name: django-gitolite
  version: "0.1.4"

source:
  fn: django-gitolite-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gitolite/django-gitolite-0.1.4.tar.gz
  md5: 2f389d3f6c08cc17cd9519a0c43c51a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gitolite = django-gitolite:main
    #
    # Would create an entry point called django-gitolite that calls django-gitolite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_gitolite
    - django_gitolite.management
    - django_gitolite.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eyolfson/django-gitolite/
  license: UNKNOWN
  summary: 'A basic Django app for using Gitolite'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
