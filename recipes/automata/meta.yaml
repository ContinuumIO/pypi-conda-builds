package:
  name: automata
  version: "0.1.4"

source:
  fn: automata-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/a/automata/automata-0.1.4.tar.gz
  md5: 8a7fec4820a236b2afd2380e3ca4cb9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - automata = automata:main
    #
    # Would create an entry point called automata that calls automata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - symath

  run:
    - python
    - symath

test:
  # Python imports
  imports:
    - automata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bniemczyk/automata
  license: BSD License
  summary: 'finite automata for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
