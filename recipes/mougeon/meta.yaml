package:
  name: mougeon
  version: "0.1.0dev-r48"

source:
  fn: mougeon-0.1.0dev-r48.tar.gz
  url: https://pypi.python.org/packages/source/m/mougeon/mougeon-0.1.0dev-r48.tar.gz
  md5: f828401a03240f6ac6dd5e6cf8a5edad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mougeon = mougeon:main
    #
    # Would create an entry point called mougeon that calls mougeon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata >=2.0.9,<=2.0.14

  run:
    - python
    - gdata >=2.0.9,<=2.0.14

test:
  # Python imports
  imports:
    - mougeon
    - mougeon.common
    - mougeon.core
    - mougeon.core.persistence
    - mougeon.gui
    - mougeon.gui.gtk
    - mougeon.gui.hildon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.mougeon.bressure.net
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Free Mobile Cell Tower usage tracker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
