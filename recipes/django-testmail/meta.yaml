package:
  name: django-testmail
  version: "0.0.2"

source:
  fn: django-testmail-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-testmail/django-testmail-0.0.2.tar.gz
  md5: dbd9b8590dc8adc0b918507d5c0d475e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-testmail = django-testmail:main
    #
    # Would create an entry point called django-testmail that calls django-testmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - testmail
    - testmail.management
    - testmail.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/django-testmail
  license: BSD License
  summary: 'Send a test mail for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
