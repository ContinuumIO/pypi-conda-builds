package:
  name: django-notification-op
  version: "0.1.6"

source:
  fn: django-notification-op-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-notification-op/django-notification-op-0.1.6.tar.gz
  md5: 14c06f7d2f1c8fcc626b55f9cfca1216
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-notification-op = django-notification-op:main
    #
    # Would create an entry point called django-notification-op that calls django-notification-op.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - notification
    - notification.management
    - notification.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OpenProximity/django-notification
  license: MIT License
  summary: 'User notification management for the Django web framework modified for OpenProximity'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
