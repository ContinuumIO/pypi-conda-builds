package:
  name: ipyk
  version: "0.1.2"

source:
  fn: ipyk-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/ipyk/ipyk-0.1.2.tar.gz
  md5: d407f4ab981ed30ef359b64fef3bec5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipyk = ipyk:main
    #
    # Would create an entry point called ipyk that calls ipyk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython >=2.2.0
    - setproctitle
    - six

  run:
    - python
    - ipython >=2.2.0
    - setproctitle
    - six

test:
  # Python imports
  imports:
    - ipyk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lebedov/ipyk/
  license: BSD License
  summary: 'IPython local kernel management utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
