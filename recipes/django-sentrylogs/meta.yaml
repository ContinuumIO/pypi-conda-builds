package:
  name: django-sentrylogs
  version: "1.0.1"

source:
  fn: django-sentrylogs-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sentrylogs/django-sentrylogs-1.0.1.tar.gz
  md5: d407e99dbc9f91612789acdc4c2ad6c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sentrylogs = django-sentrylogs:main
    #
    # Would create an entry point called django-sentrylogs that calls django-sentrylogs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - sentrylogs >=0.1.0
    - raven >=5.0.0

  run:
    - python
    - django >=1.4
    - sentrylogs >=0.1.0
    - raven >=5.0.0

test:
  # Python imports
  imports:
    - djsentrylogs
    - djsentrylogs.management
    - djsentrylogs.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pulilab/django-sentrylogs
  license: MIT
  summary: 'A django command to run sentrylogs easily'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
