package:
  name: django-user-extension
  version: "1.0.0"

source:
  fn: django-user-extension-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-extension/django-user-extension-1.0.0.tar.gz
  md5: a9b13d3cc6d98cc32ae1280a8762a135
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-extension = django-user-extension:main
    #
    # Would create an entry point called django-user-extension that calls django-user-extension.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-model-utils >=1.0.0

  run:
    - python
    - django-model-utils >=1.0.0

test:
  # Python imports
  imports:
    - user_extension

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dbinit/django-user-extension/
  license: MIT License
  summary: "Subclass the Django User model to your heart's delight."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
