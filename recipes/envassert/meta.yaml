package:
  name: envassert
  version: "0.1.8"

source:
  fn: envassert-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/e/envassert/envassert-0.1.8.tar.gz
  md5: ac978ba0e58f2f8856a8d97a8b909813
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - envassert = envassert:main
    #
    # Would create an entry point called envassert that calls envassert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric

  run:
    - python
    - fabric

test:
  # Python imports
  imports:
    - envassert

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r_rudi/envassert
  license: MIT License
  summary: 'Test your servers environments by using fabric.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
