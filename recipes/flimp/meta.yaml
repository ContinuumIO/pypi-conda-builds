package:
  name: flimp
  version: "0.7.alpha.0"

source:
  fn: flimp-0.7.alpha.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flimp/flimp-0.7.alpha.0.tar.gz
  md5: 49cbd049f268f7f1321013e9813c6f4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flimp = flimp:main
    #
    # Would create an entry point called flimp that calls flimp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - flimp
    - flimp.parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fluidinfo/flimp
  license: MIT
  summary: 'FLuiddb IMPorter - automates importing data into FluidDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
