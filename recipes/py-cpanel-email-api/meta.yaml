package:
  name: py-cpanel-email-api
  version: "0.9"

source:
  fn: py-cpanel-email-api-0.9.tar.gz
  url: https://pypi.python.org/packages/source/p/py-cpanel-email-api/py-cpanel-email-api-0.9.tar.gz
  md5: 725cea3bfc6294e0b621d4a35783f844
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-cpanel-email-api = py-cpanel-email-api:main
    #
    # Would create an entry point called py-cpanel-email-api that calls py-cpanel-email-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bensnyde/py-cpanel-email-api/
  license: UNKNOWN
  summary: "Python client library for interacting with Cpanel's API2 Email Module"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
