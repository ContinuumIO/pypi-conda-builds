package:
  name: django-downloadview
  version: "1.6"

source:
  fn: django-downloadview-1.6.zip
  url: https://pypi.python.org/packages/source/d/django-downloadview/django-downloadview-1.6.zip
  md5: 96f056f584ad98dbfed9a2622d5e97d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-downloadview = django-downloadview:main
    #
    # Would create an entry point called django-downloadview that calls django-downloadview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - requests
    - six
    - mock

  run:
    - python
    - setuptools
    - django >=1.5
    - requests
    - six
    - mock

test:
  # Python imports
  imports:
    - django_downloadview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-downloadview.readthedocs.org/
  license: BSD License
  summary: 'Serve files with Django and reverse-proxies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
