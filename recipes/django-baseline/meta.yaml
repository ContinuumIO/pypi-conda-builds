package:
  name: django-baseline
  version: "0.2.2"

source:
  fn: django-baseline-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-baseline/django-baseline-0.2.2.tar.gz
  md5: a1e51e3b045d64b57f44c467aa55d6ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-baseline = django-baseline:main
    #
    # Would create an entry point called django-baseline that calls django-baseline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-crispy-forms >=1.4
    - django-countries >=2.0

  run:
    - python
    - django >=1.6
    - django-crispy-forms >=1.4
    - django-countries >=2.0

test:
  # Python imports
  imports:
    - django_baseline
    - django_baseline.migrations
    - django_baseline.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theduke/django-baseline
  license: BSD License
  summary: 'Convenienve functionality for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
