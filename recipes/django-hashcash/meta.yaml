package:
  name: django-hashcash
  version: "0.2.1"

source:
  fn: django-hashcash-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hashcash/django-hashcash-0.2.1.tar.gz
  md5: 967aebf0a8b43d891f21267746ea3678
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hashcash = django-hashcash:main
    #
    # Would create an entry point called django-hashcash that calls django-hashcash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_hashcash
    - django_hashcash.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mkoistinen/django-hashcash
  license: BSD License
  summary: 'Adds HashCash.IO "proof-of-work" control to django login form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
