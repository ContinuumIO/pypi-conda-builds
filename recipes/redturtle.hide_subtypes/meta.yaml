package:
  name: redturtle.hide_subtypes
  version: "0.1.0"

source:
  fn: redturtle.hide_subtypes-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.hide_subtypes/redturtle.hide_subtypes-0.1.0.tar.gz
  md5: 8945a24472c8e262de24cbd3909c604c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.hide_subtypes = redturtle.hide_subtypes:main
    #
    # Would create an entry point called redturtle.hide_subtypes that calls redturtle.hide_subtypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.hide_subtypes
    - redturtle.hide_subtypes.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/redturtle.hide_subtypes
  license: GPL
  summary: 'A jQuery snippet that hide the "Sub-types" menu of p4a.subtyper when empty'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
