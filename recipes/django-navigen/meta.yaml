package:
  name: django-navigen
  version: "0.1.3"

source:
  fn: django-navigen-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-navigen/django-navigen-0.1.3.tar.gz
  md5: a9674e7b80a844cafe1a29b5932b921f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-navigen = django-navigen:main
    #
    # Would create an entry point called django-navigen that calls django-navigen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - navigen
    - navigen.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/LimpidTech/django-navigen
  license: MIT License
  summary: 'Dynamic navigation menu generation for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
