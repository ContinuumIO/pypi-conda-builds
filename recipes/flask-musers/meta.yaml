package:
  name: flask-musers
  version: "0.1.1"

source:
  fn: flask-musers-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-musers/flask-musers-0.1.1.tar.gz
  md5: 4358f58c5206f354955771a4559385e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-musers = flask-musers:main
    #
    # Would create an entry point called flask-musers that calls flask-musers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - flask-login >=0.2.7
    - wtforms >=1.0.4
    - jinja2 >=2.7
    - passlib >=1.6.1
    - flask-mongoengine >=0.7.0
    - mongoengine >=0.8.7
    - passlib >=1.6.1
    - blinker >=1.3
    - six >=1.7.3
    - pymongo <3.0

  run:
    - python
    - flask >=0.10.1
    - flask-login >=0.2.7
    - wtforms >=1.0.4
    - jinja2 >=2.7
    - passlib >=1.6.1
    - flask-mongoengine >=0.7.0
    - mongoengine >=0.8.7
    - passlib >=1.6.1
    - blinker >=1.3
    - six >=1.7.3
    - pymongo <3.0

test:
  # Python imports
  imports:
    - flask_musers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/s-m-i-t-a/flask-musers
  license: BSD License
  summary: 'Flask app for store user in MongoDB and simple views for login, logout and registration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
