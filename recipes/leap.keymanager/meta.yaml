package:
  name: leap.keymanager
  version: "0.3.8"

source:
  fn: leap.keymanager-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/l/leap.keymanager/leap.keymanager-0.3.8.tar.gz
  md5: 8b3fd8444cd2839544481dc377259460
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leap.keymanager = leap.keymanager:main
    #
    # Would create an entry point called leap.keymanager that calls leap.keymanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - leap.common >=0.3.7
    - simplejson
    - requests
    - gnupg >=1.2.3

  run:
    - python
    - leap.common >=0.3.7
    - simplejson
    - requests
    - gnupg >=1.2.3

test:
  # Python imports
  imports:
    - leap
    - leap.keymanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://leap.se/
  license: GNU General Public License v3 (GPLv3)
  summary: "LEAP's Key Manager"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
