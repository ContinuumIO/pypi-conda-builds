package:
  name: openerp-warning
  version: "7.0.406"

source:
  fn: openerp-warning-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-warning/openerp-warning-7.0.406.tar.gz
  md5: 6b3c0e0d2770d6e4d7f484874589b748
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-warning = openerp-warning:main
    #
    # Would create an entry point called openerp-warning that calls openerp-warning.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-core
    - openerp-sale
    - openerp-purchase

  run:
    - python
    - openerp-core
    - openerp-sale
    - openerp-purchase

test:
  # Python imports
  imports:
    - openerp.addons.warning

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Warning Messages and Alerts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
