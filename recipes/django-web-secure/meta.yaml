package:
  name: django-web-secure
  version: "0.1"

source:
  fn: django-web-secure-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-web-secure/django-web-secure-0.1.tar.gz
  md5: b88aff84e3f126cca23dc752ae13eb16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-web-secure = django-web-secure:main
    #
    # Would create an entry point called django-web-secure that calls django-web-secure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.0

  run:
    - python
    - django >=1.6.0

test:
  # Python imports
  imports:
    - django_web_secure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/syndbg/django-web-secure
  license: MIT License
  summary: 'The middleware that makes ponies fly with a great sense of security.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
