package:
  name: collective.sugarcrm
  version: "1.2.1"

source:
  fn: collective.sugarcrm-1.2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.sugarcrm/collective.sugarcrm-1.2.1.zip
  md5: cb3e7371481e9e1223928ea2a9558554
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sugarcrm = collective.sugarcrm:main
    #
    # Would create an entry point called collective.sugarcrm that calls collective.sugarcrm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - suds
    - z3c.suds

  run:
    - python
    - setuptools
    - setuptools
    - suds
    - z3c.suds

test:
  # Python imports
  imports:
    - collective
    - collective.sugarcrm
    - collective.sugarcrm.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.sugarcrm
  license: GNU General Public License (GPL)
  summary: 'SugarCRM components for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
