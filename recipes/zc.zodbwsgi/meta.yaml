package:
  name: zc.zodbwsgi
  version: "1.2.0"

source:
  fn: zc.zodbwsgi-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.zodbwsgi/zc.zodbwsgi-1.2.0.tar.gz
  md5: e0dac638d69ef40157718b9a24d925a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.zodbwsgi = zc.zodbwsgi:main
    #
    # Would create an entry point called zc.zodbwsgi that calls zc.zodbwsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - repoze.retry
    - zconfig
    - zodb
    - transaction
    - zope.exceptions

  run:
    - python
    - setuptools
    - setuptools
    - repoze.retry
    - zconfig
    - zodb
    - transaction
    - zope.exceptions

test:
  # Python imports
  imports:
    - zc
    - zc.zodbwsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: '.. image:: https://travis-ci.org/zopefoundation/zc.zodbwsgi.png?branch=master'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
