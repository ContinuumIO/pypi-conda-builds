package:
  name: mamaslemonpy
  version: "0.97"

source:
  fn: mamaslemonpy-0.97.zip
  url: https://pypi.python.org/packages/source/m/mamaslemonpy/mamaslemonpy-0.97.zip
  md5: 77a3325821d28c4b899b3edee4bba09d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mamaslemonpy = mamaslemonpy:main
    #
    # Would create an entry point called mamaslemonpy that calls mamaslemonpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mamaslemonpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/svenrahmann/mamaslemonpy/
  license: MIT License
  summary: 'index biological sequences and run maximial matches queries on them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
