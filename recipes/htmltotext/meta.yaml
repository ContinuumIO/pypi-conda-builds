package:
  name: htmltotext
  version: "0.7.3"

source:
  fn: htmltotext-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/h/htmltotext/htmltotext-0.7.3.tar.gz
  md5: 3b6125bb2b8ebe0c60235dbb45a3205f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmltotext = htmltotext:main
    #
    # Would create an entry point called htmltotext that calls htmltotext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - htmltotext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/flaxcode/wiki/HtmlToText
  license: GNU General Public License (GPL)
  summary: 'Extract text and some metainfo from HTML, coping with malformed pages as well as possible.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
