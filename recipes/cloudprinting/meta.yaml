package:
  name: cloudprinting
  version: "0.3.2"

source:
  fn: cloudprinting-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudprinting/cloudprinting-0.3.2.tar.gz
  md5: acaa80b08b2581758708365a260b0055
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudprinting = cloudprinting:main
    #
    # Would create an entry point called cloudprinting that calls cloudprinting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - cloudprinting

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cloudprinting/
  license: UNKNOWN
  summary: 'Simple API for Google Cloud Print'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
