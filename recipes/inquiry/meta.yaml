package:
  name: inquiry
  version: "0.0.3"

source:
  fn: inquiry-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/inquiry/inquiry-0.0.3.tar.gz
  md5: 469432293fc3171b369f7f4b0d934b4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inquiry = inquiry:main
    #
    # Would create an entry point called inquiry that calls inquiry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - valideer >=0.3
    - timestring

  run:
    - python
    - valideer >=0.3
    - timestring

test:
  # Python imports
  imports:
    - inquiry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tasks/inquiry
  license: Apache Software License
  summary: 'psql query generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
