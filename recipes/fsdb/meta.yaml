package:
  name: fsdb
  version: "0.3.2"

source:
  fn: Fsdb-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Fsdb/Fsdb-0.3.2.tar.gz
  md5: 4b176ac8851051511b24d01947bcbd8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fsdb = fsdb:main
    #
    # Would create an entry point called fsdb that calls fsdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fsdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ael-code/pyFsdb
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'File system database. Easily manage file storing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
