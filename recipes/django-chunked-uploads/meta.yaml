package:
  name: django-chunked-uploads
  version: "0.3"

source:
  fn: django-chunked-uploads-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chunked-uploads/django-chunked-uploads-0.3.tar.gz
  md5: f53f28fdc44456bd3a1de3c8a136aaf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chunked-uploads = django-chunked-uploads:main
    #
    # Would create an entry point called django-chunked-uploads that calls django-chunked-uploads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chunked_uploads

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eldarion/django-chunked-uploads
  license: BSD License
  summary: 'a django app for chunking uploads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
