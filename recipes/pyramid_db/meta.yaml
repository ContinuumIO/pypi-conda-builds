package:
  name: pyramid_db
  version: "0.3.4"

source:
  fn: pyramid_db-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_db/pyramid_db-0.3.4.tar.gz
  md5: 01b141b07c11974d749f1bbba40cdb0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_db = pyramid_db:main
    #
    # Would create an entry point called pyramid_db that calls pyramid_db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - sqlalchemy
    - pymongo
    - redis

  run:
    - python
    - pyramid
    - sqlalchemy
    - pymongo
    - redis

test:
  # Python imports
  imports:
    - pyramid_db

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'SQLAlchemy, mongodb, redis, sqllite support for pyramid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
