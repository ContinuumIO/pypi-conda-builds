package:
  name: mercadolibre.py
  version: "0.1.7"

source:
  fn: mercadolibre.py-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/m/mercadolibre.py/mercadolibre.py-0.1.7.tar.gz
  md5: 1a896980bf5ab2fb12a2e0f6ebd6828f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mercadolibre.py = mercadolibre.py:main
    #
    # Would create an entry point called mercadolibre.py that calls mercadolibre.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0

  run:
    - python
    - requests >=2.0

test:
  # Python imports
  imports:
    - mercadolibre

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/santiagobasulto/mercadolibre.py
  license: MIT License
  summary: 'Mercadolibre Python SDK (for humans)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
