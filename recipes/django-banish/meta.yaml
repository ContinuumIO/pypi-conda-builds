package:
  name: django-banish
  version: "0.2"

source:
  fn: django-banish-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-banish/django-banish-0.2.tar.gz
  md5: 33161fa4714d9e0c5e547a1d13447f42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-banish = django-banish:main
    #
    # Would create an entry point called django-banish that calls django-banish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached

  run:
    - python
    - python-memcached

test:
  # Python imports
  imports:
    - banish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yourabi/django-banish
  license: Apache Software License
  summary: 'django middleware to ban users, prevent too many concurrent connections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
