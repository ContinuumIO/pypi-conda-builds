package:
  name: diderot
  version: "0.1"

source:
  fn: diderot-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/diderot/diderot-0.1.tar.gz
  md5: 114d46dc8f3fed81a27b523b8f9b4969
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diderot = diderot:main
    #
    # Would create an entry point called diderot that calls diderot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fuxi ==1.4.1.production

  run:
    - python
    - fuxi ==1.4.1.production

test:
  # Python imports
  imports:
    - diderot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/icaromedeiros/diderot
  license: PSF
  summary: 'Diderot - Test Driven Development for RDF/OWL Ontologies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
