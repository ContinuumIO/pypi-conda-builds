package:
  name: sloth
  version: "0.0.8"

source:
  fn: SLOTH-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/S/SLOTH/SLOTH-0.0.8.tar.gz
  md5: f787e51f2d730445eef0c4518f375c53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sloth = sloth:main
    #
    # Would create an entry point called sloth that calls sloth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - matplotlib >=0.99.3
    - numpy >=1.5.1
    - scipy >=0.8.0

  run:
    - python
    - matplotlib >=0.99.3
    - numpy >=1.5.1
    - scipy >=0.8.0

test:
  # Python imports
  imports:
    - sloth
    - sloth.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Sloth/
  license: LICENSE.txt
  summary: 'stick-like object tracker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
