package:
  name: django-submodel
  version: "0.1"

source:
  fn: django-submodel-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-submodel/django-submodel-0.1.tar.gz
  md5: b52f3eb2ecc23055b40e443ddcd308cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-submodel = django-submodel:main
    #
    # Would create an entry point called django-submodel that calls django-submodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - submodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/liokm/django-submodel
  license: MIT License
  summary: 'A Django model field which value works like a model instance and supports seamless inline editing in Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
