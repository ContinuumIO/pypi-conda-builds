package:
  name: garbas.forum
  version: "0.1b9"

source:
  fn: garbas.forum-0.1b9.tar.gz
  url: https://pypi.python.org/packages/source/g/garbas.forum/garbas.forum-0.1b9.tar.gz
  md5: 3be4b7a068dfd7833e3d28c8e9a60619
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - garbas.forum = garbas.forum:main
    #
    # Would create an entry point called garbas.forum that calls garbas.forum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.captcha

  run:
    - python
    - setuptools
    - setuptools
    - collective.captcha

test:
  # Python imports
  imports:
    - garbas
    - garbas.forum
    - garbas.forum.browser
    - garbas.forum.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/garbas/garbas.forum/tree/master
  license: GPL
  summary: 'Forum for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
