package:
  name: mathexpressions
  version: "1.1.0"

source:
  fn: mathexpressions-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mathexpressions/mathexpressions-1.1.0.tar.gz
  md5: 93138804a1ea1edaabef7bff9599b594
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mathexpressions = mathexpressions:main
    #
    # Would create an entry point called mathexpressions that calls mathexpressions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mathexpressions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/NoahPeeters/pymathexpressions
  license: MIT License or MIT License
  summary: 'Python library for parsing and solving math expressions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
