package:
  name: zope.app.boston
  version: "3.5.1"

source:
  fn: zope.app.boston-3.5.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.boston/zope.app.boston-3.5.1.tar.gz
  md5: 9f92938677bfe6fee7007c3ff34c394f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.boston = zope.app.boston:main
    #
    # Would create an entry point called zope.app.boston that calls zope.app.boston.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.app.basicskin
    - zope.app.skins
    - zope.app.testing
    - zope.browsermenu
    - zope.component
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher >=3.12
    - zope.viewlet

  run:
    - python
    - setuptools
    - zope.app.basicskin
    - zope.app.skins
    - zope.app.testing
    - zope.browsermenu
    - zope.component
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher >=3.12
    - zope.viewlet

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.boston
    - zope.app.boston.browser
    - zope.app.boston.viewlets
    - zope.app.boston.viewlets.addinginfo
    - zope.app.boston.viewlets.toolbar
    - zope.app.boston.viewlets.xmltree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.boston
  license: Zope Public License
  summary: 'Boston -- A Zope 3 ZMI Skin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
