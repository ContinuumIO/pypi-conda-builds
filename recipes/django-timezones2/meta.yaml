package:
  name: django-timezones2
  version: "1.0.4"

source:
  fn: django-timezones2-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timezones2/django-timezones2-1.0.4.tar.gz
  md5: de1aa343d0c210b52e9eedb0008bc38e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timezones2 = django-timezones2:main
    #
    # Would create an entry point called django-timezones2 that calls django-timezones2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - timezones2
    - timezones2.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sayane/django-timezones2
  license: Beer-ware
  summary: 'Timezone utilities for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
