package:
  name: flask-ldap
  version: "0.1.6"

source:
  fn: Flask-LDAP-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-LDAP/Flask-LDAP-0.1.6.tar.gz
  md5: e6a56312938c6080bde82b79ffea8616
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-ldap = flask-ldap:main
    #
    # Would create an entry point called flask-ldap that calls flask-ldap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - python-ldap

  run:
    - python
    - flask
    - python-ldap

test:
  # Python imports
  imports:
    - flask_ldap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/3f17/flask-ldap/
  license: BSD License
  summary: 'Flask extension for LDAP auth and profile user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
