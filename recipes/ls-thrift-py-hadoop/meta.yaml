package:
  name: ls-thrift-py-hadoop
  version: "1-cdh4.3.0"

source:
  fn: ls-thrift-py-hadoop-1-cdh4.3.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ls-thrift-py-hadoop/ls-thrift-py-hadoop-1-cdh4.3.0.tar.gz
  md5: 9d38c9b1a3ac299bf00a3eb3e5907b96
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ls-thrift-py-hadoop = ls-thrift-py-hadoop:main
    #
    # Would create an entry point called ls-thrift-py-hadoop that calls ls-thrift-py-hadoop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - TCLIService
    - fb303
    - fb303_scripts
    - hive_metastore
    - hive_serde
    - hive_service
    - queryplan
    - thrift
    - thrift.protocol
    - thrift.reflection
    - thrift.reflection.limited
    - thrift.server
    - thrift.transport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lovelysystems/ls-thrift-py-hadoop
  license: Apache License, Version 2.0
  summary: 'Hadoop and Hive Python Thrift Libs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
