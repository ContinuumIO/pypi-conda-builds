package:
  name: caliope_iliada
  version: "0.1dev"

source:
  fn: Caliope_Iliada-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/C/Caliope_Iliada/Caliope_Iliada-0.1dev.tar.gz
  md5: f760e3193586d7d36e658ae8c19bd9ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caliope_iliada = caliope_iliada:main
    #
    # Would create an entry point called caliope_iliada that calls caliope_iliada.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - psycopg2

  run:
    - python
    - psycopg2

test:
  # Python imports
  imports:
    - caliope_iliada
    - iliada

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://proyectos.correlibre.org/caliope/caliope_storage_la_iliada
  license: GNU AFFERO GENERAL PUBLIC LICENSE
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
