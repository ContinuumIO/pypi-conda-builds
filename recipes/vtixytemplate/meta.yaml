package:
  name: vtixytemplate
  version: "1.0.1"

source:
  fn: VTIXyTemplate-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/V/VTIXyTemplate/VTIXyTemplate-1.0.1.tar.gz
  md5: a22c95dd36ae7eeaf23c5ae151498660
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vtixytemplate = vtixytemplate:main
    #
    # Would create an entry point called vtixytemplate that calls vtixytemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - slumber
    - reportlab
    - pypdf

  run:
    - python
    - django
    - slumber
    - reportlab
    - pypdf

test:
  # Python imports
  imports:
    - vtixy_template
    - vtixytemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'PDF Ticket generator for web applications of VTIX.RU clients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
