package:
  name: django-prefetch
  version: "1.0.0"

source:
  fn: django-prefetch-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-prefetch/django-prefetch-1.0.0.tar.gz
  md5: 0e3668f17bde4cc4df6cc9774338f7ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-prefetch = django-prefetch:main
    #
    # Would create an entry point called django-prefetch that calls django-prefetch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1

  run:
    - python
    - django >=1.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionelmc/django-prefetch
  license: BSD License
  summary: 'Generic model related data prefetch framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
