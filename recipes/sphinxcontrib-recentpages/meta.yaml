package:
  name: sphinxcontrib-recentpages
  version: "0.7.1"

source:
  fn: sphinxcontrib-recentpages-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-recentpages/sphinxcontrib-recentpages-0.7.1.tar.gz
  md5: 7bbdf96a833d7f40b0c96d164f3cc1d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-recentpages = sphinxcontrib-recentpages:main
    #
    # Would create an entry point called sphinxcontrib-recentpages that calls sphinxcontrib-recentpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx

  run:
    - python
    - sphinx

test:
  # Python imports
  imports:
    - sphinxcontrib
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/shiumachi/sphinxcontrib-recentpages
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
