package:
  name: flask-tweepy
  version: "0.1"

source:
  fn: Flask-Tweepy-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Tweepy/Flask-Tweepy-0.1.tar.gz
  md5: bc54b66a7c32291f6b58cc1639de2ca9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-tweepy = flask-tweepy:main
    #
    # Would create an entry point called flask-tweepy that calls flask-tweepy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - tweepy >=1.8,<1.9

  run:
    - python
    - flask >=0.8
    - tweepy >=1.8,<1.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://flask-tweepy.readthedocs.org/
  license: BSD License
  summary: 'Tweet easily from Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
