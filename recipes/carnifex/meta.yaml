package:
  name: carnifex
  version: "0.2.4"

source:
  fn: carnifex-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/c/carnifex/carnifex-0.2.4.tar.gz
  md5: 5907704f84d3fce3b268cc288913f2dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - carnifex = carnifex:main
    #
    # Would create an entry point called carnifex that calls carnifex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - carnifex
    - carnifex.ssh

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sporsh/carnifex
  license: MIT License
  summary: 'Execute commands and run processes locally and remote'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
