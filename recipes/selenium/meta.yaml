package:
  name: selenium
  version: "2.46.0"

source:
  fn: selenium-2.46.0.tar.gz
  url: https://pypi.python.org/packages/source/s/selenium/selenium-2.46.0.tar.gz
  md5: 6db65e1e7653da0fdc514df5db76d662
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - selenium = selenium:main
    #
    # Would create an entry point called selenium that calls selenium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - selenium
    - selenium.common
    - selenium.webdriver
    - selenium.webdriver.android
    - selenium.webdriver.chrome
    - selenium.webdriver.common
    - selenium.webdriver.common.html5
    - selenium.webdriver.firefox
    - selenium.webdriver.ie
    - selenium.webdriver.opera
    - selenium.webdriver.phantomjs
    - selenium.webdriver.remote
    - selenium.webdriver.support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SeleniumHQ/selenium/
  license: Apache Software License
  summary: 'Python bindings for Selenium'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
