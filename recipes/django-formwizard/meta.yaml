package:
  name: django-formwizard
  version: "1.0"

source:
  fn: django-formwizard-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formwizard/django-formwizard-1.0.tar.gz
  md5: d615089a66a3767bcaa67a0e032afd6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formwizard = django-formwizard:main
    #
    # Would create an entry point called django-formwizard that calls django-formwizard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formwizard
    - formwizard.storage
    - formwizard.tests
    - formwizard.tests.namedwizardtests
    - formwizard.tests.wizardtests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stephrdev/django-formwizard/
  license: BSD License
  summary: 'A FormWizard for Django with multiple storage backends'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
