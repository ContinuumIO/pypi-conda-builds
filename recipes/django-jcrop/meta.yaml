package:
  name: django-jcrop
  version: "0.0.2"

source:
  fn: django-jcrop-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jcrop/django-jcrop-0.0.2.tar.gz
  md5: 0e32b9a0eb891de25337a790419c290b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jcrop = django-jcrop:main
    #
    # Would create an entry point called django-jcrop that calls django-jcrop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sorl-thumbnail

  run:
    - python
    - sorl-thumbnail

test:
  # Python imports
  imports:
    - django_jcrop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mauler/django-jcrop/
  license: BSD License
  summary: ('Django app providing cropping functionnality with jcrop',)

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
