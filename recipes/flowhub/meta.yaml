package:
  name: flowhub
  version: "0.5.4"

source:
  fn: flowhub-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/f/flowhub/flowhub-0.5.4.tar.gz
  md5: 2085f82e6a1fe66900f7d7c43b5153c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flowhub = flowhub:main
    #
    # Would create an entry point called flowhub that calls flowhub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython ==0.3.2.RC1
    - pygithub >1.14
    - argcomplete >=0.6.3

  run:
    - python
    - gitpython ==0.3.2.RC1
    - pygithub >1.14
    - argcomplete >=0.6.3

test:
  # Python imports
  imports:
    - flowhub
    - flowhub.managers
    - tests
    - tests.managers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/haaksmash/flowhub
  license: UNKNOWN
  summary: 'Git-flow adapted for GitHub'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
