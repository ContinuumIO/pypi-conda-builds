package:
  name: openerp-portal-project
  version: "7.0.406"

source:
  fn: openerp-portal-project-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-portal-project/openerp-portal-project-7.0.406.tar.gz
  md5: b34e9a6cd4fb11906577e692ccc61eab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-portal-project = openerp-portal-project:main
    #
    # Would create an entry point called openerp-portal-project that calls openerp-portal-project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-project
    - openerp-portal

  run:
    - python
    - openerp-project
    - openerp-portal

test:
  # Python imports
  imports:
    - openerp.addons.portal_project
    - openerp.addons.portal_project.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Portal Project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
