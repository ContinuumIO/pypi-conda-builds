package:
  name: django-cached-functions
  version: "0.1.1"

source:
  fn: django-cached-functions-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cached-functions/django-cached-functions-0.1.1.tar.gz
  md5: ca7df4fe3e4ad5bf13e88ed38f1c80ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cached-functions = django-cached-functions:main
    #
    # Would create an entry point called django-cached-functions that calls django-cached-functions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3
  summary: 'Prevent expensive computations by storing low level calues in the django cache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
