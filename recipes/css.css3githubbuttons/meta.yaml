package:
  name: css.css3githubbuttons
  version: "2.1"

source:
  fn: css.css3githubbuttons-2.1.zip
  url: https://pypi.python.org/packages/source/c/css.css3githubbuttons/css.css3githubbuttons-2.1.zip
  md5: c98f12a7ff237528273fd00621bda1b5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - css.css3githubbuttons = css.css3githubbuttons:main
    #
    # Would create an entry point called css.css3githubbuttons that calls css.css3githubbuttons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - css
    - css.css3githubbuttons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidjb/css.css3githubbuttons
  license: BSD
  summary: 'Fanstatic packaging of CSS3 GitHub Buttons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
