package:
  name: creoleparser
  version: "0.7.4"

source:
  fn: Creoleparser-0.7.4.zip
  url: https://pypi.python.org/packages/source/C/Creoleparser/Creoleparser-0.7.4.zip
  md5: 9619057d20a53d440eb591c904d29791
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - creoleparser = creoleparser:main
    #
    # Would create an entry point called creoleparser that calls creoleparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - genshi >=0.4

  run:
    - python
    - genshi >=0.4

test:
  # Python imports
  imports:
    - creoleparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/creoleparser/
  license: MIT License
  summary: 'Parser for the Creole common wiki markup language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
