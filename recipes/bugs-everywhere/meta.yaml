package:
  name: bugs-everywhere
  version: "1.1.1"

source:
  fn: bugs-everywhere-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bugs-everywhere/bugs-everywhere-1.1.1.tar.gz
  md5: 7d17c6d6475419be424ce78259592fac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bugs-everywhere = bugs-everywhere:main
    #
    # Would create an entry point called bugs-everywhere that calls bugs-everywhere.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - libbe
    - libbe.command
    - libbe.storage
    - libbe.storage.util
    - libbe.storage.vcs
    - libbe.ui
    - libbe.ui.util
    - libbe.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bugseverywhere.org/
  license: GNU General Public License (GPL) or GNU General Public License v2 (GPLv2) or GNU General Public License v2 or later (GPLv2+) or GNU General Public License v3 (GPLv3)
  summary: 'Bugtracker supporting distributed revision control'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
