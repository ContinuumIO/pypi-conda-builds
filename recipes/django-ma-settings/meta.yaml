package:
  name: django-ma-settings
  version: "1.0.9"

source:
  fn: django-ma-settings-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ma-settings/django-ma-settings-1.0.9.tar.gz
  md5: 9885a7ed949666d091daeca1c0ecf059
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ma-settings = django-ma-settings:main
    #
    # Would create an entry point called django-ma-settings that calls django-ma-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ma_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MasterAlish/django-ma-settings
  license: BSD License
  summary: 'A simple Django app to have simple typed settings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
