package:
  name: redsolutioncms.django-generic-ratings
  version: "0.6.1"

source:
  fn: redsolutioncms.django-generic-ratings-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-generic-ratings/redsolutioncms.django-generic-ratings-0.6.1.tar.gz
  md5: a0ab896d678c7a94e306081a6d1defa3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-generic-ratings = redsolutioncms.django-generic-ratings:main
    #
    # Would create an entry point called redsolutioncms.django-generic-ratings that calls redsolutioncms.django-generic-ratings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ratings
    - ratings.forms
    - ratings.management
    - ratings.management.commands
    - ratings.redsolution_setup
    - ratings.templatetags
    - ratings.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/frankban/django-generic-ratings/downloads
  license: MIT License
  summary: 'This is our fork of original `Django-generic-ratings <http://pypi.python.org/pypi/django-generic-ratings/>`_ project. \n\nIn our fork we added star widget from `this site <http://www.fyneworks.com/jquery/star-rating/>`_ and fixed some errors. Anyway, follow main project for updates.\n\nDjango ratings tools supporting ajax, generic content type scores, multiple ratings for each content object.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
