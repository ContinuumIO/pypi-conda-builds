package:
  name: tornado_eventsource
  version: "1.0.0rc6"

source:
  fn: tornado_eventsource-1.0.0rc6.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_eventsource/tornado_eventsource-1.0.0rc6.tar.gz
  md5: b98079bc0ed8c766bb3d84122a0817c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_eventsource = tornado_eventsource:main
    #
    # Would create an entry point called tornado_eventsource that calls tornado_eventsource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=4.1.0

  run:
    - python
    - tornado >=4.1.0

test:
  # Python imports
  imports:
    - tornado_eventsource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guilhermef/tornado-eventsource
  license: MIT
  summary: 'EventSource handler for tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
