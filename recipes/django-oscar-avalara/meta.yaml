package:
  name: django-oscar-avalara
  version: "0.2.1"

source:
  fn: django-oscar-avalara-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oscar-avalara/django-oscar-avalara-0.2.1.tar.gz
  md5: a011c22582a7dc1c97d49ff48b00d583
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oscar-avalara = django-oscar-avalara:main
    #
    # Would create an entry point called django-oscar-avalara that calls django-oscar-avalara.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-oscar >=1.0
    - requests
    - purl >=0.8

  run:
    - python
    - django-oscar >=1.0
    - requests
    - purl >=0.8

test:
  # Python imports
  imports:
    - avalara

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django-oscar/django-oscar-avalara
  license: BSD License
  summary: 'Avalara integration for django-oscar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
