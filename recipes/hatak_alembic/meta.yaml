package:
  name: hatak_alembic
  version: "0.1.3"

source:
  fn: Hatak_Alembic-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/H/Hatak_Alembic/Hatak_Alembic-0.1.3.tar.gz
  md5: 267af599714c8d32a193493dd40e743b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hatak_alembic = hatak_alembic:main
    #
    # Would create an entry point called hatak_alembic that calls hatak_alembic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hatak >=0.2.7.5
    - alembic
    - hatak_sql >=0.1.8

  run:
    - python
    - hatak >=0.2.7.5
    - alembic
    - hatak_sql >=0.1.8

test:
  # Python imports
  imports:
    - haplugin
    - haplugin.alembic
    - hatak_alembic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'Alembic plugin for Hatak.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
