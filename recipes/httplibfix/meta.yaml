package:
  name: httplibfix
  version: "1.0"

source:
  fn: httplibfix-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httplibfix/httplibfix-1.0.tar.gz
  md5: ae342398e65f3f2312008f59b6e2f5f6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httplibfix = httplibfix:main
    #
    # Would create an entry point called httplibfix that calls httplibfix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - httplibfix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/httplibfix
  license: GPL
  summary: 'A fix to httplib in python2.4 to back port a patch from trunk to fix issues with rss reads.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
