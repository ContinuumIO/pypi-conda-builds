package:
  name: dbpost
  version: "0.2.7"

source:
  fn: dbpost-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dbpost/dbpost-0.2.7.tar.gz
  md5: 4d3f33eafd1393e1ec47e39aa15d4a87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbpost = dbpost:main
    #
    # Would create an entry point called dbpost that calls dbpost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo
    - dataset

  run:
    - python
    - pymongo
    - dataset

test:
  # Python imports
  imports:
    - dbpost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/dbpost
  license: BSD
  summary: 'proxy for save db data. support mysql\xe3\x80\x81sqlite\xe3\x80\x81mongodb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
