package:
  name: collective.perseoschema
  version: "0.1.1"

source:
  fn: collective.perseoschema-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.perseoschema/collective.perseoschema-0.1.1.tar.gz
  md5: bece1906d6413325ce40c5425386650b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.perseoschema = collective.perseoschema:main
    #
    # Would create an entry point called collective.perseoschema that calls collective.perseoschema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.perseo

  run:
    - python
    - setuptools
    - setuptools
    - collective.perseo

test:
  # Python imports
  imports:
    - collective
    - collective.perseoschema
    - collective.perseoschema.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Adds Schema.org attributes to collective.perseo package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
