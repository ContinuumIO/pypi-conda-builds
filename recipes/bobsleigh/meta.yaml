package:
  name: bobsleigh
  version: "1.0.3"

source:
  fn: bobsleigh-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bobsleigh/bobsleigh-1.0.3.tar.gz
  md5: 2a5c4893488babb2b4e840d62587432f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bobsleigh = bobsleigh:main
    #
    # Would create an entry point called bobsleigh that calls bobsleigh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bobsleigh
    - bobsleigh.conf
    - bobsleigh.runner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seddonym/bobsleigh/
  license: UNKNOWN
  summary: 'Helps write less code when configuring Django installations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
