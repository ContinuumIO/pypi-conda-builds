package:
  name: msrx
  version: "0.1"

source:
  fn: msrx-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/msrx/msrx-0.1.tar.gz
  md5: 7fc69ad340a0f36a55e6755d87c0f105
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msrx = msrx:main
    #
    # Would create an entry point called msrx that calls msrx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - msrx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oxplot/msrx
  license: GNU General Public License v3 (GPLv3)
  summary: u'Library and command line utils to use MSR605 magnetic card reader/writer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
