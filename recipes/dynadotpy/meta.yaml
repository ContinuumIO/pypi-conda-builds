package:
  name: dynadotpy
  version: "0.1.0"

source:
  fn: dynadotpy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dynadotpy/dynadotpy-0.1.0.tar.gz
  md5: b35e37de112c95e176e26b16f8b21a9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynadotpy = dynadotpy:main
    #
    # Would create an entry point called dynadotpy that calls dynadotpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - dynadotpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brack3t/dynadotpy
  license: BSD License
  summary: 'Simple Python wrapper for dynadot.com v2 API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
