package:
  name: flatland
  version: "0.8"

source:
  fn: flatland-0.8.tar.gz
  url: https://pypi.python.org/packages/source/f/flatland/flatland-0.8.tar.gz
  md5: 5aec3c3c2ce228539769494290d7318e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flatland = flatland:main
    #
    # Would create an entry point called flatland that calls flatland.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blinker

  run:
    - python
    - blinker

test:
  # Python imports
  imports:
    - flatland
    - flatland.out
    - flatland.schema
    - flatland.validation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://discorporate.us/jek/projects/flatland/
  license: MIT License
  summary: 'HTML form management and validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
