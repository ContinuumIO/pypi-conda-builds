package:
  name: flask-neo4j
  version: "0.3.0"

source:
  fn: Flask-Neo4j-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Neo4j/Flask-Neo4j-0.3.0.tar.gz
  md5: 2d976da0bd949af2a4b6c27c9acd70ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-neo4j = flask-neo4j:main
    #
    # Would create an entry point called flask-neo4j that calls flask-neo4j.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10
    - py2neo >=1.6

  run:
    - python
    - flask >=0.10
    - py2neo >=1.6

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lashex/flask-neo4j
  license: MIT License
  summary: 'Flask extension providing integration with Neo4j.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
