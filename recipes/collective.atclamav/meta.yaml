package:
  name: collective.atclamav
  version: "1.2"

source:
  fn: collective.ATClamAV-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.ATClamAV/collective.ATClamAV-1.2.zip
  md5: 8d0b2d6afe235bb230870508d079219e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.atclamav = collective.atclamav:main
    #
    # Would create an entry point called collective.atclamav that calls collective.atclamav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.app.blob
    - plone.app.controlpanel
    - plone.app.testing
    - products.archetypes
    - products.atcontenttypes
    - products.cmfcore
    - products.cmfdefault
    - products.validation
    - unittest2
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.app.blob
    - plone.app.controlpanel
    - plone.app.testing
    - products.archetypes
    - products.atcontenttypes
    - products.cmfcore
    - products.cmfdefault
    - products.validation
    - unittest2
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.ATClamAV
    - collective.ATClamAV.browser
    - collective.ATClamAV.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.ATClamAV
  license: GNU General Public License (GPL)
  summary: 'Provides ClamAV antivirus integration for Archetypes based content types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
