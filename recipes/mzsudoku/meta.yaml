package:
  name: mzsudoku
  version: "1.0.0"

source:
  fn: mzsudoku-1.0.0.zip
  url: https://pypi.python.org/packages/source/m/mzsudoku/mzsudoku-1.0.0.zip
  md5: 17f2717c1c17e272dbe7acfd825751d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mzsudoku = mzsudoku:main
    #
    # Would create an entry point called mzsudoku that calls mzsudoku.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mzsudoku

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/zvolsky/sudoku/
  license: open source GPL
  summary: 'Module/script to solve classical 9x9 Sudoku step by step'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
