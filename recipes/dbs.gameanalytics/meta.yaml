package:
  name: dbs.gameanalytics
  version: "0.1.1"

source:
  fn: dbs.gameanalytics-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dbs.gameanalytics/dbs.gameanalytics-0.1.1.tar.gz
  md5: 18fe7b7d9b016e4a6c317fb0c0893999
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbs.gameanalytics = dbs.gameanalytics:main
    #
    # Would create an entry point called dbs.gameanalytics that calls dbs.gameanalytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbs
    - dbs.gameanalytics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dreambitstudios.com
  license: BSD License
  summary: 'Http://GameAnalytics.com REST API implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
