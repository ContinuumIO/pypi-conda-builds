package:
  name: bizdays
  version: "v0.2.0"

source:
  fn: bizdays-v0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bizdays/bizdays-v0.2.0.tar.gz
  md5: 5686219a9d2a4d7db1cbc80a8d35607f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bizdays = bizdays:main
    #
    # Would create an entry point called bizdays that calls bizdays.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bizdays

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wilsonfreitas/bizdays
  license: MIT License
  summary: 'Functions to handle business days calculations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
