package:
  name: django-account-balances
  version: "0.4"

source:
  fn: django-account-balances-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-account-balances/django-account-balances-0.4.tar.gz
  md5: 35f1fea4d54b08f181a151ef0929cf48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-account-balances = django-account-balances:main
    #
    # Would create an entry point called django-account-balances that calls django-account-balances.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=2.1,<2.2
    - django-treebeard >=2.0b2

  run:
    - python
    - python-dateutil >=2.1,<2.2
    - django-treebeard >=2.0b2

test:
  # Python imports
  imports:
    - accounts
    - accounts.api
    - accounts.checkout
    - accounts.dashboard
    - accounts.management
    - accounts.management.commands
    - accounts.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Track account credits in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
