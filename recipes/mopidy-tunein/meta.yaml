package:
  name: mopidy-tunein
  version: "0.2.2"

source:
  fn: Mopidy-TuneIn-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-TuneIn/Mopidy-TuneIn-0.2.2.tar.gz
  md5: 65961a3f65498ea483c334e20379f287
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-tunein = mopidy-tunein:main
    #
    # Would create an entry point called mopidy-tunein that calls mopidy-tunein.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - requests >=2.0.0

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - mopidy_tunein

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kingosticks/mopidy-tunein
  license: Apache Software License
  summary: u'Mopidy extension for playing music from TuneIn.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
