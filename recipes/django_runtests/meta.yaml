package:
  name: django_runtests
  version: "0.4.0"

source:
  fn: django_runtests-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_runtests/django_runtests-0.4.0.tar.gz
  md5: c12de9eac7df08e9df012307ab24d54c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_runtests = django_runtests:main
    #
    # Would create an entry point called django_runtests that calls django_runtests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - django_runtests
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rbarrois/django-runtests
  license: MIT License
  summary: 'A base framework to write a simple runtests.py script for django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
