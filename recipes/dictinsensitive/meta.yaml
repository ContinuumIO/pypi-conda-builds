package:
  name: dictinsensitive
  version: "0.1.0"

source:
  fn: DictInsensitive-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DictInsensitive/DictInsensitive-0.1.0.tar.gz
  md5: b73320a93e7e1879f9c6266ea511d519
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictinsensitive = dictinsensitive:main
    #
    # Would create an entry point called dictinsensitive that calls dictinsensitive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.4.1

  run:
    - python
    - six >=1.4.1

test:
  # Python imports
  imports:
    - dictinsensitive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/orige/dictinsensitive
  license: MIT
  summary: 'Python dictionaries with case insensitive keys'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
