package:
  name: masheryclient
  version: "0.1.0"

source:
  fn: masheryclient-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/masheryclient/masheryclient-0.1.0.tar.gz
  md5: e40a27534fd9d311f7432a9e5e0f5627
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - masheryclient = masheryclient:main
    #
    # Would create an entry point called masheryclient that calls masheryclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.0
    - requests-auth-mashery

  run:
    - python
    - requests >=1.2.0
    - requests-auth-mashery

test:
  # Python imports
  imports:
    - masheryclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dasevilla/mashery-python
  license: MIT License
  summary: 'A simple Python client library for the Mashery API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
