package:
  name: cubric
  version: "0.1.0"

source:
  fn: Cubric-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/Cubric/Cubric-0.1.0.tar.gz
  md5: 32d270b6105cc144feb8e51f12f52513
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubric = cubric:main
    #
    # Would create an entry point called cubric that calls cubric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cuisine

  run:
    - python
    - cuisine

test:
  # Python imports
  imports:
    - cubric
    - cubric.contrib
    - cubric.contrib.servers
    - cubric.contrib.servers.ubuntu
    - cubric.contrib.servers.ubuntu.default
    - cubric.providers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattupstate/cubric
  license: MIT License
  summary: 'Simple server setup and WSGI application deployment for EC2 and Rackspace'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
