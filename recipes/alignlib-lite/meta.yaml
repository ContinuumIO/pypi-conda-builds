package:
  name: alignlib-lite
  version: "0.2.3"

source:
  fn: alignlib-lite-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/a/alignlib-lite/alignlib-lite-0.2.3.tar.gz
  md5: d9088eff6542ca417718874605b0d062
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alignlib-lite = alignlib-lite:main
    #
    # Would create an entry point called alignlib-lite that calls alignlib-lite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/alignlib/
  license: BSD
  summary: 'alignlib-lite - simple wrapper around alignlib C++ library for sequence alignment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
