package:
  name: salib
  version: "0.4"

source:
  fn: SALib-0.4.zip
  url: https://pypi.python.org/packages/source/S/SALib/SALib-0.4.zip
  md5: a697bbc8acea769320a8c6516205fee8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - salib = salib:main
    #
    # Would create an entry point called salib that calls salib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy

  run:
    - python
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - SALib
    - SALib.analyze
    - SALib.sample
    - SALib.test_functions
    - SALib.util
    - salib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jdherman/SALib
  license: saliere
  summary: 'Tools for sensitivity analysis. Contains Sobol, Morris, and FAST methods.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
