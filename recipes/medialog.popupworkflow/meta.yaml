package:
  name: medialog.popupworkflow
  version: "4.1.5.1"

source:
  fn: medialog.popupworkflow-4.1.5.1.zip
  url: https://pypi.python.org/packages/source/m/medialog.popupworkflow/medialog.popupworkflow-4.1.5.1.zip
  md5: ce2eed5d076bccdc707fddd20d46c077
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.popupworkflow = medialog.popupworkflow:main
    #
    # Would create an entry point called medialog.popupworkflow that calls medialog.popupworkflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.prettyphoto

  run:
    - python
    - setuptools
    - setuptools
    - collective.prettyphoto

test:
  # Python imports
  imports:
    - medialog
    - medialog.popupworkflow
    - medialog.popupworkflow.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenmn/medialog.popupworkflow
  license: GPL
  summary: 'A workflow for Plone >= 4.1  with a state that opens documents in an overlay'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
