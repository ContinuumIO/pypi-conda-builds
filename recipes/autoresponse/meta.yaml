package:
  name: autoresponse
  version: "0.3.1"

source:
  fn: autoresponse-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/autoresponse/autoresponse-0.3.1.tar.gz
  md5: 512c5c209014e2e04c1de43bdbf0e020
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autoresponse = autoresponse:main
    #
    # Would create an entry point called autoresponse that calls autoresponse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scrapy

  run:
    - python
    - scrapy

test:
  # Python imports
  imports:
    - autoresponse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
