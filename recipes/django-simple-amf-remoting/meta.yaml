package:
  name: django-simple-amf-remoting
  version: "0.1.0b"

source:
  fn: django-simple-amf-remoting-0.1.0b.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-amf-remoting/django-simple-amf-remoting-0.1.0b.tar.gz
  md5: a2507e0f1f5afe406ddb13ee63da9248
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-amf-remoting = django-simple-amf-remoting:main
    #
    # Would create an entry point called django-simple-amf-remoting that calls django-simple-amf-remoting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/benhodgson/django-simple-amf-remoting
  license: Public Domain
  summary: "A simplified API for AmFast's AMF remoting functionality, designed to make Django AMF remoting simple and painless."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
