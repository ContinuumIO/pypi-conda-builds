package:
  name: django-static-jquery
  version: "2.1.4"

source:
  fn: django-static-jquery-2.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-static-jquery/django-static-jquery-2.1.4.tar.gz
  md5: 05e9783453c7d9fca65a89def83a2828
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-static-jquery = django-static-jquery:main
    #
    # Would create an entry point called django-static-jquery that calls django-static-jquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_static_jquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Haos616/django-static-jquery
  license: BSD License or MIT License
  summary: 'jQuery packaged in an handy django app to speed up new applications and deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
