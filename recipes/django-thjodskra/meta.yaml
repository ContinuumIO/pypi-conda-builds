package:
  name: django-thjodskra
  version: "0.65"

source:
  fn: django-thjodskra-0.65.tar.gz
  url: https://pypi.python.org/packages/source/d/django-thjodskra/django-thjodskra-0.65.tar.gz
  md5: 74ce527071475793afa4f5604f09771f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-thjodskra = django-thjodskra:main
    #
    # Would create an entry point called django-thjodskra that calls django-thjodskra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - thjodskra
    - thjodskra.management
    - thjodskra.management.commands
    - thjodskra.parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ulfur/django-thjodskra
  license: UNKNOWN
  summary: 'django-thjodskra is a simple app for integrating the icelandic national registry into your django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
