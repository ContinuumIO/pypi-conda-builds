package:
  name: junos-eznc
  version: "1.1.2"

source:
  fn: junos-eznc-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/junos-eznc/junos-eznc-1.1.2.tar.gz
  md5: 1bc92f4d27c33fa1eab1b83696e96b09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - junos-eznc = junos-eznc:main
    #
    # Would create an entry point called junos-eznc that calls junos-eznc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.2.4
    - ncclient >=0.4.3
    - paramiko
    - scp >=0.7.0
    - jinja2 >=2.7.1
    - pyyaml >=3.10
    - netaddr

  run:
    - python
    - lxml >=3.2.4
    - ncclient >=0.4.3
    - paramiko
    - scp >=0.7.0
    - jinja2 >=2.7.1
    - pyyaml >=3.10
    - netaddr

test:
  # Python imports
  imports:
    - jnpr
    - jnpr.junos
    - jnpr.junos.cfg
    - jnpr.junos.cfg.phyport
    - jnpr.junos.cfgro
    - jnpr.junos.factory
    - jnpr.junos.facts
    - jnpr.junos.op
    - jnpr.junos.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/Juniper/py-junos-eznc
  license: Apache Software License
  summary: "Junos 'EZ' automation for non-programmers"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
