package:
  name: hfch5levi
  version: "1.0.4"

source:
  fn: HfCh5Levi-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/H/HfCh5Levi/HfCh5Levi-1.0.4.tar.gz
  md5: 6cfa8f3229fd7a267b4a85b2e1be6a9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hfch5levi = hfch5levi:main
    #
    # Would create an entry point called hfch5levi that calls hfch5levi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hfch5levi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.headfirstlabs
  license: UNKNOWN
  summary: 'now refiene the code for removing the duplicates using set() rather than list(),and refine the common code for extracting data from \tfiles as a function'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
