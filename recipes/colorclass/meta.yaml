package:
  name: colorclass
  version: "1.2.0"

source:
  fn: colorclass-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/colorclass/colorclass-1.2.0.tar.gz
  md5: 190754d4698f97c3a90c518410e359f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorclass = colorclass:main
    #
    # Would create an entry point called colorclass that calls colorclass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colorclass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Robpol86/colorclass
  license: MIT License
  summary: 'Colorful worry-free console applications for Linux, Mac OS X, and Windows.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
