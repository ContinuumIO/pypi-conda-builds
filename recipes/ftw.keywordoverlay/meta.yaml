package:
  name: ftw.keywordoverlay
  version: "1.2.1"

source:
  fn: ftw.keywordoverlay-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/f/ftw.keywordoverlay/ftw.keywordoverlay-1.2.1.tar.gz
  md5: 4450f1687c2c09f30c0f1e5bb3a150ab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.keywordoverlay = ftw.keywordoverlay:main
    #
    # Would create an entry point called ftw.keywordoverlay that calls ftw.keywordoverlay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui
    - collective.js.ui.multiselect

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui
    - collective.js.ui.multiselect

test:
  # Python imports
  imports:
    - ftw
    - ftw.keywordoverlay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.keywordoverlay
  license: GNU General Public License (GPL)
  summary: "Puts a nice overlay over Archetypes' default keywordwidget for plone."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
