package:
  name: cykooz.thumbs
  version: "0.3.1"

source:
  fn: cykooz.thumbs-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cykooz.thumbs/cykooz.thumbs-0.3.1.tar.gz
  md5: 92b2d6c6dcacd60707d48b510a7f0045
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cykooz.thumbs = cykooz.thumbs:main
    #
    # Would create an entry point called cykooz.thumbs that calls cykooz.thumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - paste
    - webob
    - zope.datetime
    - pil >=1.1.7

  run:
    - python
    - setuptools
    - distribute
    - paste
    - webob
    - zope.datetime
    - pil >=1.1.7

test:
  # Python imports
  imports:
    - cykooz
    - cykooz.thumbs
    - cykooz.thumbs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cykooz/cykooz.thumbs
  license: GPL
  summary: 'WSGI middleware for image resizing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
