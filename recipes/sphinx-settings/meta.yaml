package:
  name: sphinx-settings
  version: "0.1.2"

source:
  fn: sphinx-settings-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-settings/sphinx-settings-0.1.2.tar.gz
  md5: f278dd75c2555a5ab2616c4e386a2bc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-settings = sphinx-settings:main
    #
    # Would create an entry point called sphinx-settings that calls sphinx-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx

  run:
    - python
    - sphinx

test:
  # Python imports
  imports:
    - sphinx_settings
    - sphinx_settings.helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/respect31/sphinx-settings
  license: MIT License
  summary: 'Class-based settings for Sphinx.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
