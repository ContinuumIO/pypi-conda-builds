package:
  name: eventlet_log
  version: "0.0.4"

source:
  fn: eventlet_log-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/e/eventlet_log/eventlet_log-0.0.4.tar.gz
  md5: a2dd0061d2219481dc39ff5f15b8b40c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eventlet_log = eventlet_log:main
    #
    # Would create an entry point called eventlet_log that calls eventlet_log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eventlet

  run:
    - python
    - eventlet

test:
  # Python imports
  imports:
    - eventlet_log

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/little-arhat/eventlet_log
  license: MIT License
  summary: 'Damn simple logging wrapper compatible with eventlet.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
