package:
  name: moz_inapp_pay
  version: "2.0.0"

source:
  fn: moz_inapp_pay-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/moz_inapp_pay/moz_inapp_pay-2.0.0.tar.gz
  md5: d3b576e9f14bbef4cfa76d6a851575ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moz_inapp_pay = moz_inapp_pay:main
    #
    # Would create an entry point called moz_inapp_pay that calls moz_inapp_pay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyjwt
    - m2crypto >=0.2.0

  run:
    - python
    - pyjwt
    - m2crypto >=0.2.0

test:
  # Python imports
  imports:
    - moz_inapp_pay
    - moz_inapp_pay.djangoapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kumar303/moz_inapp_pay
  license: MPL 2.0 (Mozilla Public License)
  summary: 'Utility for working with Mozilla in-app payments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
