package:
  name: mtirc
  version: "0.2.1"

source:
  fn: mtirc-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mtirc/mtirc-0.2.1.tar.gz
  md5: 8ff2ad2aad94bdd7d37b325802133bac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mtirc = mtirc:main
    #
    # Would create an entry point called mtirc that calls mtirc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - irc >=8.3
    - simplejson

  run:
    - python
    - irc >=8.3
    - simplejson

test:
  # Python imports
  imports:
    - mtirc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/legoktm/mtirc/
  license: MIT License
  summary: 'A fully functioning multi-threaded IRC client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
