package:
  name: django-user-media
  version: "1.1.6"

source:
  fn: django-user-media-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-media/django-user-media-1.1.6.tar.gz
  md5: 1286e78be1f8fdb4e57ac42bed6698bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-media = django-user-media:main
    #
    # Would create an entry point called django-user-media that calls django-user-media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-generic-positions
    - django-libs
    - simplejson

  run:
    - python
    - django
    - django-generic-positions
    - django-libs
    - simplejson

test:
  # Python imports
  imports:
    - user_media
    - user_media.migrations
    - user_media.tests
    - user_media.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-user-media
  license: The MIT License
  summary: 'A reusable app to graceously handle files that your users might upload, such\nas profile pictures, foto albums etc...\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
