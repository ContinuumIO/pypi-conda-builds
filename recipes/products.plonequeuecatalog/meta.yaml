package:
  name: products.plonequeuecatalog
  version: "2.0"

source:
  fn: Products.PloneQueueCatalog-2.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PloneQueueCatalog/Products.PloneQueueCatalog-2.0.tar.gz
  md5: 36673a8a14917377c4a4fae3909757de
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.plonequeuecatalog = products.plonequeuecatalog:main
    #
    # Would create an entry point called products.plonequeuecatalog that calls products.plonequeuecatalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PloneQueueCatalog
    - Products.PloneQueueCatalog.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/PloneQueueCatalog/trunk
  license: GPL
  summary: 'Products.PloneQueueCatalog makes it easier to use QueueCatalog in your Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
