package:
  name: glespy
  version: "0.1.7.2"

source:
  fn: glespy-0.1.7.2.tar.gz
  url: https://pypi.python.org/packages/source/g/glespy/glespy-0.1.7.2.tar.gz
  md5: 64df6a21d1e1370e2724c10a500b56a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glespy = glespy:main
    #
    # Would create an entry point called glespy that calls glespy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - glespy
    - glespy.ext
    - glespy.test
    - glespy.test_data
    - glespy.tools
    - glespy.tools.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/glespy/
  license: UNKNOWN
  summary: 'Bindings for GLESP for calculations with spherical harmonics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
