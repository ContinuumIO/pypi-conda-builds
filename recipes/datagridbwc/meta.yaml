package:
  name: datagridbwc
  version: "0.2.2"

source:
  fn: DataGridBWC-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/D/DataGridBWC/DataGridBWC-0.2.2.tar.gz
  md5: 991be8f9a5afd8ee28ab1ed56e0a36ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datagridbwc = datagridbwc:main
    #
    # Would create an entry point called datagridbwc that calls datagridbwc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blazeutils >=0.3.8
    - blazeweb ==dev,>0.4.4
    - sqlalchemybwc >=0.1
    - python-dateutil
    - webgrid >=0.1.6

  run:
    - python
    - blazeutils >=0.3.8
    - blazeweb ==dev,>0.4.4
    - sqlalchemybwc >=0.1
    - python-dateutil
    - webgrid >=0.1.6

test:
  # Python imports
  imports:
    - datagridbwc
    - datagridbwc.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/blazelibs/datagridbwc/
  license: BSD License
  summary: 'A BlazeWeb component for turning SQLAlchemy recordsets into HTML tables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
