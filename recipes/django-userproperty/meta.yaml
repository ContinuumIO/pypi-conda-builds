package:
  name: django-userproperty
  version: "1.0.3"

source:
  fn: django-userproperty-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-userproperty/django-userproperty-1.0.3.tar.gz
  md5: 5e9b3efa41a71a5a2144374151eaa994
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-userproperty = django-userproperty:main
    #
    # Would create an entry point called django-userproperty that calls django-userproperty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-compat ==0.0.1

  run:
    - python
    - django-compat ==0.0.1

test:
  # Python imports
  imports:
    - userproperty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'django userproperty'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
