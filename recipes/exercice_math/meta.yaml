package:
  name: exercice_math
  version: "0.1"

source:
  fn: exercice_math-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/exercice_math/exercice_math-0.1.tar.gz
  md5: 39f804441f90058536e8c632b9744ee1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exercice_math = exercice_math:main
    #
    # Would create an entry point called exercice_math that calls exercice_math.main()

    - exercise_math = exercice_math.exercice_math:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - exercice_math

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - exercise_math --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://
  license: GNU GPL v3.0 License
  summary: 'Exercise de Mathematiques'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
