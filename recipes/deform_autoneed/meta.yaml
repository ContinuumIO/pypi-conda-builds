package:
  name: deform_autoneed
  version: "0.2.2b"

source:
  fn: deform_autoneed-0.2.2b.tar.gz
  url: https://pypi.python.org/packages/source/d/deform_autoneed/deform_autoneed-0.2.2b.tar.gz
  md5: c834f9766e097e4d62cbcf7bb60092ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deform_autoneed = deform_autoneed:main
    #
    # Would create an entry point called deform_autoneed that calls deform_autoneed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - deform
    - colander

  run:
    - python
    - setuptools
    - fanstatic
    - deform
    - colander

test:
  # Python imports
  imports:
    - deform_autoneed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robinharms/deform_autoneed
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Auto include resources in deform via Fanstatic.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
