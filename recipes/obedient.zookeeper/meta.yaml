package:
  name: obedient.zookeeper
  version: "3.1.0"

source:
  fn: obedient.zookeeper-3.1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/obedient.zookeeper/obedient.zookeeper-3.1.0.tar.gz
  md5: 0b0a300f0d603df75e6ea7be106586c3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - obedient.zookeeper = obedient.zookeeper:main
    #
    # Would create an entry point called obedient.zookeeper that calls obedient.zookeeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dominator[full] >=15.1a

  run:
    - python
    - setuptools
    - dominator[full] >=15.1a

test:
  # Python imports
  imports:
    - obedient.zookeeper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yandex-sysmon/obedient.zookeeper
  license: LGPLv3
  summary: 'Zookeeper obedient for Dominator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
