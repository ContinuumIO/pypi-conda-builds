package:
  name: django-dummy-plug
  version: "0.1.0"

source:
  fn: django-dummy-plug-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dummy-plug/django-dummy-plug-0.1.0.tar.gz
  md5: 31239f97c4ce44d5c8da271261aced28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dummy-plug = django-dummy-plug:main
    #
    # Would create an entry point called django-dummy-plug that calls django-dummy-plug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - dummyplug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lqez/django-dummy-plug
  license: MIT License
  summary: 'Dummies bone to be failed for testing django apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
