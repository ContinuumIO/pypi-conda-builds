package:
  name: hoka.browser.pcheck
  version: "2.13.19.3"

source:
  fn: hoka.browser.pcheck-2.13.19.3.zip
  url: https://pypi.python.org/packages/source/h/hoka.browser.pcheck/hoka.browser.pcheck-2.13.19.3.zip
  md5: 2bb568eb900bfbe08f77c5b5fbdc23cc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hoka.browser.pcheck = hoka.browser.pcheck:main
    #
    # Would create an entry point called hoka.browser.pcheck that calls hoka.browser.pcheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2
    - hoka.adapter.auth
    - hoka.browser.base
    - hoka.patches.get_browser
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - zope2
    - hoka.adapter.auth
    - hoka.browser.base
    - hoka.patches.get_browser
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - hoka
    - hoka.browser
    - hoka.browser.pcheck
    - hoka.browser.pcheck.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hoka/hoka.browser.pcheck
  license: GNU General Public License (GPL)
  summary: 'BrowserView to check permissions and authorize user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
