package:
  name: gaico
  version: "0.2.0"

source:
  fn: gaico-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gaico/gaico-0.2.0.tar.gz
  md5: df77f6ee6ca4369c70a3c9f472a3ce0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaico = gaico:main
    #
    # Would create an entry point called gaico that calls gaico.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent ==1.0.1

  run:
    - python
    - gevent ==1.0.1

test:
  # Python imports
  imports:
    - gaico
    - gaico.net

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lmeunier/gaico
  license: BSD License
  summary: 'A set of network tools written in pure Python with gevent.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
