package:
  name: ggeocoder
  version: "1.0.1"

source:
  fn: ggeocoder-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/ggeocoder/ggeocoder-1.0.1.tar.gz
  md5: 794588dfab931a8f6df5c6e8ec00cddd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ggeocoder = ggeocoder:main
    #
    # Would create an entry point called ggeocoder that calls ggeocoder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ggeocoder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imtapps/ggeocoder
  license: BSD License
  summary: 'A Python library for working with Google Geocoding API V3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
