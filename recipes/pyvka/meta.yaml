package:
  name: pyvka
  version: "0.1"

source:
  fn: pyvka-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyvka/pyvka-0.1.tar.gz
  md5: 88e4f493e10dcdef0e87c09df8a03667
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvka = pyvka:main
    #
    # Would create an entry point called pyvka that calls pyvka.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - pyvka

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saippuakauppias/pyvka
  license: ISC License (ISCL)
  summary: 'Python package for authorize and getaccess_token for vkontakte api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
