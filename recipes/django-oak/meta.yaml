package:
  name: django-oak
  version: "0.1.2"

source:
  fn: django-oak-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oak/django-oak-0.1.2.tar.gz
  md5: 7fbabd90aa6f4e3d3c7ee0e0efde754c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oak = django-oak:main
    #
    # Would create an entry point called django-oak that calls django-oak.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oak

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/weholt/django-oak
  license: BSD License
  summary: 'Rapid prototyping for views and models in django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
