package:
  name: django-rss-widget
  version: "0.5"

source:
  fn: django-rss-widget-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rss-widget/django-rss-widget-0.5.tar.gz
  md5: f8214a883558f416e127052543553e59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rss-widget = django-rss-widget:main
    #
    # Would create an entry point called django-rss-widget that calls django-rss-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - rss_widget
    - rss_widget.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattcaldwell/django-rss-widget/
  license: UNKNOWN
  summary: 'Django RSS Widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
