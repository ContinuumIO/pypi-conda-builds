package:
  name: cs.togglecanonical
  version: "1.1"

source:
  fn: cs.togglecanonical-1.1.zip
  url: https://pypi.python.org/packages/source/c/cs.togglecanonical/cs.togglecanonical-1.1.zip
  md5: 2b3bea115f3edfe39af6439434f3aef2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.togglecanonical = cs.togglecanonical:main
    #
    # Would create an entry point called cs.togglecanonical that calls cs.togglecanonical.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - cs
    - cs.togglecanonical

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codesyntax/cs.togglecanonical
  license: GPL
  summary: 'Hide and show canonical language fields in Plone edit form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
