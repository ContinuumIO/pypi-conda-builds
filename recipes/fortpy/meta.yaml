package:
  name: fortpy
  version: "1.5.6"

source:
  fn: Fortpy-1.5.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Fortpy/Fortpy-1.5.6.tar.gz
  md5: c39b1d1f2513efb0fcac7e76c592bdf4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fortpy = fortpy:main
    #
    # Would create an entry point called fortpy that calls fortpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - pyparsing
    - python-dateutil
    - paramiko
    - termcolor
    - numpy
    - matplotlib
    - scipy

  run:
    - python
    - argparse
    - pyparsing
    - python-dateutil
    - paramiko
    - termcolor
    - numpy
    - matplotlib
    - scipy

test:
  # Python imports
  imports:
    - fortpy
    - fortpy.interop
    - fortpy.isense
    - fortpy.parsers
    - fortpy.printing
    - fortpy.templates
    - fortpy.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rosenbrockc/fortpy
  license: MIT License
  summary: 'Fortran Parsing, Unit Testing and Intellisense'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
