package:
  name: django-cookie-law-nl
  version: "0.2.1"

source:
  fn: django-cookie-law-nl-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cookie-law-nl/django-cookie-law-nl-0.2.1.tar.gz
  md5: 3f17158d14f081818c9c8e93a8c0ca6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cookie-law-nl = django-cookie-law-nl:main
    #
    # Would create an entry point called django-cookie-law-nl that calls django-cookie-law-nl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cookie_law
    - cookie_law.migrations
    - cookie_law.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/getlogic/lib_django_cookie_law
  license: BSD License
  summary: 'Django specific cookie implementation compliant with Dutch law, use at your own risk.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
