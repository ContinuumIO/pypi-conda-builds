package:
  name: areciboware
  version: "1.0b1"

source:
  fn: areciboware-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/a/areciboware/areciboware-1.0b1.tar.gz
  md5: 142825b27f7f0cecd59781a3bd50e30f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - areciboware = areciboware:main
    #
    # Would create an entry point called areciboware that calls areciboware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - arecibo
    - weberror

  run:
    - python
    - setuptools
    - arecibo
    - weberror

test:
  # Python imports
  imports:
    - areciboware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/areciboware
  license: BSD License
  summary: 'Arecibo error logging WSGI middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
