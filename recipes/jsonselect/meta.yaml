package:
  name: jsonselect
  version: "0.2.3"

source:
  fn: jsonselect-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonselect/jsonselect-0.2.3.tar.gz
  md5: b2fc1e9910d3c2a0b27977b6e74b4ef0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonselect = jsonselect:main
    #
    # Would create an entry point called jsonselect that calls jsonselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonselect
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mwhooker/jsonselect
  license: ISC License (ISCL)
  summary: 'Python implementation of jsonselect (http://jsonselect.org/)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
