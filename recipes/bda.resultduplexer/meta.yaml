package:
  name: bda.resultduplexer
  version: "1.0-beta2"

source:
  fn: bda.resultduplexer-1.0-beta2.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.resultduplexer/bda.resultduplexer-1.0-beta2.tar.gz
  md5: 0a2ed8813c913b69461bacbbed31babf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.resultduplexer = bda.resultduplexer:main
    #
    # Would create an entry point called bda.resultduplexer that calls bda.resultduplexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bda.contentproxy

  run:
    - python
    - setuptools
    - setuptools
    - bda.contentproxy

test:
  # Python imports
  imports:
    - bda
    - bda.resultduplexer
    - bda.resultduplexer.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'module providing duplexing of search results in plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
