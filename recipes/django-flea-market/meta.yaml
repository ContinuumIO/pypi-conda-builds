package:
  name: django-flea-market
  version: "0.1.0"

source:
  fn: django-flea-market-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flea-market/django-flea-market-0.1.0.tar.gz
  md5: 11947c97e0d33455d0a41744ac0c0d1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flea-market = django-flea-market:main
    #
    # Would create an entry point called django-flea-market that calls django-flea-market.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - flea_market

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/feinheit/django-flea-market/
  license: BSD License
  summary: 'Various crap not suitable for a Django app on its own.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
