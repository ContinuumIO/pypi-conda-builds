package:
  name: medialog.maritheme
  version: "0.1.3"

source:
  fn: medialog.maritheme-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.maritheme/medialog.maritheme-0.1.3.tar.gz
  md5: f279977a1856de4b46d274d73d56051f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.maritheme = medialog.maritheme:main
    #
    # Would create an entry point called medialog.maritheme that calls medialog.maritheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plonetheme.classic
    - webcouturier.dropdownmenu
    - wildcard.foldercontents
    - medialog.portlet.placeholder
    - medialog.qrcode

  run:
    - python
    - setuptools
    - setuptools
    - plonetheme.classic
    - webcouturier.dropdownmenu
    - wildcard.foldercontents
    - medialog.portlet.placeholder
    - medialog.qrcode

test:
  # Python imports
  imports:
    - medialog
    - medialog.maritheme
    - medialog.maritheme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenmn/medialog.maritheme/
  license: GPL
  summary: 'Medialog Maritheme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
