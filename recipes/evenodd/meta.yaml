package:
  name: evenodd
  version: "1.4.0"

source:
  fn: evenodd-1.4.0.zip
  url: https://pypi.python.org/packages/source/e/evenodd/evenodd-1.4.0.zip
  md5: 6d46edf86ea631d44e8e4d2bef728d38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evenodd = evenodd:main
    #
    # Would create an entry point called evenodd that calls evenodd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - evenodd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: pypi.python.org
  license: UNKNOWN
  summary: 'A simple printer which will tell you whether the number you enter is an odd number or an even number.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
