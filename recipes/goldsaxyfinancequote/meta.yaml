package:
  name: goldsaxyfinancequote
  version: "1.132"

source:
  fn: GoldSaxYFinanceQuote-1.132.zip
  url: https://pypi.python.org/packages/source/G/GoldSaxYFinanceQuote/GoldSaxYFinanceQuote-1.132.zip
  md5: 62e6015e60970b4503a6b3b25937f6ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goldsaxyfinancequote = goldsaxyfinancequote:main
    #
    # Would create an entry point called goldsaxyfinancequote that calls goldsaxyfinancequote.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - GoldSaxYFinanceQuote
    - goldsaxyfinancequote

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/VanceKingSaxbeA/GoldSaxCreateTablesGFinance.git
  license: Python Software Foundation License
  summary: 'Auto aggregation, parsing of FIXML, FPML, XML, Json, FIX messages realtime data for storage, analytics to the GoldSaxEngine-****Markets or any other engine. This can be used by any Trader, Market Maker, a Retail Investor, Instituitional Investor, Hedge Fund Managers, and Asset Managers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
