package:
  name: django-celery
  version: "3.1.16"

source:
  fn: django-celery-3.1.16.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery/django-celery-3.1.16.tar.gz
  md5: 626c3c5fa91097ab16a7709108b82756
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery = django-celery:main
    #
    # Would create an entry point called django-celery that calls django-celery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=3.1.15

  run:
    - python
    - celery >=3.1.15

test:
  # Python imports
  imports:
    - djcelery
    - djcelery.backends
    - djcelery.contrib
    - djcelery.management
    - djcelery.management.commands
    - djcelery.migrations
    - djcelery.monproj
    - djcelery.tests
    - djcelery.tests.test_backends
    - djcelery.transport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://celeryproject.org
  license: BSD License
  summary: 'Old django celery integration project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
