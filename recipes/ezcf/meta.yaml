package:
  name: ezcf
  version: "0.2.0"

source:
  fn: ezcf-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/ezcf/ezcf-0.2.0.tar.gz
  md5: 5d14b96aeff2c1f80f7a7adbbe341953
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezcf = ezcf:main
    #
    # Would create an entry point called ezcf that calls ezcf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.11
    - configobj ==5.0.6
    - xmltodict ==0.9.2

  run:
    - python
    - pyyaml ==3.11
    - configobj ==5.0.6
    - xmltodict ==0.9.2

test:
  # Python imports
  imports:
    - ezcf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/laike9m/ezcf/
  license: MIT License
  summary: 'Import JSON/YAML/INI/XML like .py files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
