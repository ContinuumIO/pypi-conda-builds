package:
  name: jsonmodels
  version: "2.0.1"

source:
  fn: jsonmodels-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonmodels/jsonmodels-2.0.1.tar.gz
  md5: 764eb84b5d22e4bf3660fa7fb686fa29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonmodels = jsonmodels:main
    #
    # Would create an entry point called jsonmodels that calls jsonmodels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - six

  run:
    - python
    - python-dateutil
    - six

test:
  # Python imports
  imports:
    - jsonmodels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/beregond/jsonmodels
  license: BSD License
  summary: 'Models to make easier to deal with structures that are converted to, or read from JSON.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
