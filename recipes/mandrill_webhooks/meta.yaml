package:
  name: mandrill_webhooks
  version: "0.1.0"

source:
  fn: mandrill_webhooks-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mandrill_webhooks/mandrill_webhooks-0.1.0.tar.gz
  md5: df6f3a5844c358b95439841d1f39ea43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mandrill_webhooks = mandrill_webhooks:main
    #
    # Would create an entry point called mandrill_webhooks that calls mandrill_webhooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - blinker

  run:
    - python
    - flask
    - blinker

test:
  # Python imports
  imports:
    - mandrill_webhooks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ecarreras/mandrill_webhooks
  license: MIT
  summary: 'Flask Webhooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
