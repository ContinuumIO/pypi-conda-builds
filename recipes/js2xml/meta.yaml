package:
  name: js2xml
  version: "0.1.1"

source:
  fn: js2xml-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js2xml/js2xml-0.1.1.tar.gz
  md5: 16ba842acfd6df0b0d396d098e1356a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js2xml = js2xml:main
    #
    # Would create an entry point called js2xml that calls js2xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - slimit
    - lxml

  run:
    - python
    - slimit
    - lxml

test:
  # Python imports
  imports:
    - js2xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redapple/js2xml
  license: MIT License
  summary: 'Convert Javascript code to XML document'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
