package:
  name: deuce-client
  version: "0.1-beta10.dev210.g4faa860"

source:
  fn: deuce-client-0.1-beta10.dev210.g4faa860.tar.gz
  url: https://pypi.python.org/packages/source/d/deuce-client/deuce-client-0.1-beta10.dev210.g4faa860.tar.gz
  md5: 6ba73a0fa502a4aefbb986d1937bbc80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deuce-client = deuce-client:main
    #
    # Would create an entry point called deuce-client that calls deuce-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Client for Deuce De-Duplication-As-A-Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
