package:
  name: neuropy
  version: "0.1"

source:
  fn: NeuroPy-0.1.zip
  url: https://pypi.python.org/packages/source/N/NeuroPy/NeuroPy-0.1.zip
  md5: 11bf2d31edbdcf23d709a29f188a2f96
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - neuropy = neuropy:main
    #
    # Would create an entry point called neuropy that calls neuropy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - NeuroPy
    - neuropy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lihas/NeuroPy
  license: BSD3 license
  summary: "Library for interfacing with Neurosky's Mindwave EEG headset"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
