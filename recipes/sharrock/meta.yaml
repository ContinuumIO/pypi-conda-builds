package:
  name: sharrock
  version: "0.6.3.6"

source:
  fn: Sharrock-0.6.3.6.tar.gz
  url: https://pypi.python.org/packages/source/S/Sharrock/Sharrock-0.6.3.6.tar.gz
  md5: f297cf5f64b4f3b18cacd6dc576724db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sharrock = sharrock:main
    #
    # Would create an entry point called sharrock that calls sharrock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - django >=1.4
    - markdown ==2.0.1

  run:
    - python
    - requests
    - django >=1.4
    - markdown ==2.0.1

test:
  # Python imports
  imports:
    - sharrock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Axilent/sharrock
  license: BSD License
  summary: 'Python remote procedure call framework with server and client components.  RESTful when you need it to be.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
