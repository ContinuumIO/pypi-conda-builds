package:
  name: treeherder-client
  version: "1.2"

source:
  fn: treeherder-client-1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/treeherder-client/treeherder-client-1.2.tar.gz
  md5: 71b2563c3ec7880c28fc712b595b9c86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - treeherder-client = treeherder-client:main
    #
    # Would create an entry point called treeherder-client that calls treeherder-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2
    - requests

  run:
    - python
    - oauth2
    - requests

test:
  # Python imports
  imports:
    - thclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

requires:
    - mock
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/treeherder-client
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Python library to submit data to treeherder-service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
