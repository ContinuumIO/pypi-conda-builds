package:
  name: hizashi-utils
  version: "0.4.1"

source:
  fn: hizashi-utils-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hizashi-utils/hizashi-utils-0.4.1.tar.gz
  md5: 93c1dd309c0e5c05b50d4cd2dae4ce08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hizashi-utils = hizashi-utils:main
    #
    # Would create an entry point called hizashi-utils that calls hizashi-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7,<1.8

  run:
    - python
    - django >=1.7,<1.8

test:
  # Python imports
  imports:
    - hizashi_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dodobas/hizashi-utils
  license: MIT License
  summary: 'Utils for managing Django Hizashi environment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
