package:
  name: soma-workflow
  version: "2.6.1"

source:
  fn: soma-workflow-2.6.1.tar.gz
  url: https://pypi.python.org/packages/source/s/soma-workflow/soma-workflow-2.6.1.tar.gz
  md5: 4ed1a149677edc10e3d8757d9f7615dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - soma-workflow = soma-workflow:main
    #
    # Would create an entry point called soma-workflow that calls soma-workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - soma_workflow
    - soma_workflow.check_requirement
    - soma_workflow.gui
    - soma_workflow.test
    - somadrmaa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.brainvisa.info/soma-workflow
  license: CeCILL v2
  summary: '\nSoma-workflow is a unified and simple interface to parallel computing\nresources. It aims at making easier the use of parallel resources by\nnon expert users and software.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
