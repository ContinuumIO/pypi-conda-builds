package:
  name: networking-cisco
  version: "0.2.0"

source:
  fn: networking-cisco-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/n/networking-cisco/networking-cisco-0.2.0.tar.gz
  md5: 6d482e1f63ed9908d7559e694fadcd37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - networking-cisco = networking-cisco:main
    #
    # Would create an entry point called networking-cisco that calls networking-cisco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Networking Cisco contains the Cisco vendor code for Openstack Neutron'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
