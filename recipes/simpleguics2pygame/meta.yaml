package:
  name: simpleguics2pygame
  version: "01.09.00"

source:
  fn: SimpleGUICS2Pygame-01.09.00.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleGUICS2Pygame/SimpleGUICS2Pygame-01.09.00.tar.gz
  md5: fb5e074bdd4d444dc64190a46c0ae1ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleguics2pygame = simpleguics2pygame:main
    #
    # Would create an entry point called simpleguics2pygame that calls simpleguics2pygame.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - SimpleGUICS2Pygame
    - simpleguics2pygame

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/OPiMedia/simpleguics2pygame
  license: GNU General Public License v3 (GPLv3)
  summary: 'Primarily a standard Python module reimplementing the SimpleGUI particular module of CodeSkulptor (a browser Python interpreter).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
