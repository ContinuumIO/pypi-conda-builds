package:
  name: on.caroufredsel
  version: "0.1.1a"

source:
  fn: on.caroufredsel-0.1.1a.tar.gz
  url: https://pypi.python.org/packages/source/o/on.caroufredsel/on.caroufredsel-0.1.1a.tar.gz
  md5: 06504aa1a3bb3d5cd38a72e88be94a94
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - on.caroufredsel = on.caroufredsel:main
    #
    # Would create an entry point called on.caroufredsel that calls on.caroufredsel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - on
    - on.caroufredsel
    - on.caroufredsel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/muellert/on.caroufredsel.git
  license: MIT + GPL
  summary: 'Shim to load Caroufredsel as a resource'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
