package:
  name: jquery.javascript
  version: "1.0.0"

source:
  fn: jquery.javascript-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jquery.javascript/jquery.javascript-1.0.0.tar.gz
  md5: 979f12f0b31e7e62bff98d9fca5f0432
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jquery.javascript = jquery.javascript:main
    #
    # Would create an entry point called jquery.javascript that calls jquery.javascript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.viewlet

  run:
    - python
    - setuptools
    - zope.viewlet

test:
  # Python imports
  imports:
    - jquery
    - jquery.javascript

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/jquery.javascript
  license: Zope Public License
  summary: 'JQuery integration into Zope 3 via Viewlets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
