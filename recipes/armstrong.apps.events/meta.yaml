package:
  name: armstrong.apps.events
  version: "0.1.2.1"

source:
  fn: armstrong.apps.events-0.1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.apps.events/armstrong.apps.events-0.1.2.1.tar.gz
  md5: 760913c86cf236e18cff1ee36310b3ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.apps.events = armstrong.apps.events:main
    #
    # Would create an entry point called armstrong.apps.events that calls armstrong.apps.events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.3
    - armstrong.core.arm_content >=0.1

  run:
    - python
    - django ==1.3
    - armstrong.core.arm_content >=0.1

test:
  # Python imports
  imports:
    - armstrong.apps.events
    - armstrong.apps.events.tests
    - armstrong.apps.events.tests.events_support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.apps.events/
  license: Apache Software License
  summary: 'Basic events for Armstrong CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
