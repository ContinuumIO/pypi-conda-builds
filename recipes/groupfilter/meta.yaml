package:
  name: groupfilter
  version: "0.2"

source:
  fn: groupfilter-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/groupfilter/groupfilter-0.2.tar.gz
  md5: 676c027bfd602a0701155060293561e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - groupfilter = groupfilter:main
    #
    # Would create an entry point called groupfilter that calls groupfilter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - groupfilter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.theorchromo.ru/groupfilter
  license: License :: OSI Approved :: Apache Software License
  summary: 'A software tool for efficient filtering of Morpheus search engine results.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
