package:
  name: products.zodbfriendlycounter
  version: "1.0a2"

source:
  fn: Products.ZODBFriendlyCounter-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.ZODBFriendlyCounter/Products.ZODBFriendlyCounter-1.0a2.tar.gz
  md5: afe54c40e6cfc82f69c0ce009c70ea84
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.zodbfriendlycounter = products.zodbfriendlycounter:main
    #
    # Would create an entry point called products.zodbfriendlycounter that calls products.zodbfriendlycounter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.ZODBFriendlyCounter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: GPL
  summary: 'A counter which generates unique IDs and does so without bloating the ZODB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
