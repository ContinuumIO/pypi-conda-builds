package:
  name: longtask
  version: "0.1.1"

source:
  fn: longtask-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/longtask/longtask-0.1.1.tar.gz
  md5: fee7b5b701cebe17814687e53e5392b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - longtask = longtask:main
    #
    # Would create an entry point called longtask that calls longtask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - progressbar

  run:
    - python
    - progressbar

test:
  # Python imports
  imports:
    - longtask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/konradhalas/longtask
  license: Apache Software License
  summary: 'Long task runner.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
