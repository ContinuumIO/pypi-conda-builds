package:
  name: ks.channel
  version: "1.0.1"

source:
  fn: ks.channel-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/ks.channel/ks.channel-1.0.1.tar.gz
  md5: 395dcc5bf7a1f20be1d19ce42a20be8c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ks.channel = ks.channel:main
    #
    # Would create an entry point called ks.channel that calls ks.channel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.cachedescriptors
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.app.component

  run:
    - python
    - setuptools
    - zope.cachedescriptors
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.app.component

test:
  # Python imports
  imports:
    - ks
    - ks.channel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.keysolutionscorp.com/downloads/public/pypi/ks.channel
  license: Zope Public License
  summary: 'Base classes and interfaces for message send utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
