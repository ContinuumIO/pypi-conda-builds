package:
  name: butteur
  version: "0.1"

source:
  fn: Butteur-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/Butteur/Butteur-0.1.tar.gz
  md5: afda62464e16b376f8d1620d657b1b32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - butteur = butteur:main
    #
    # Would create an entry point called butteur that calls butteur.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - ply

  run:
    - python
    - jinja2
    - ply

test:
  # Python imports
  imports:
    - butteur

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Psycojoker/butteur
  license: gplv3+
  summary: 'a preprocessor to write beamers slide more easily'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
