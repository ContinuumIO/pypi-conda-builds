package:
  name: django-bootstrap-navtags
  version: "0.1.1"

source:
  fn: django-bootstrap-navtags-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-navtags/django-bootstrap-navtags-0.1.1.tar.gz
  md5: 155dbebf1f44475106dd214149c8ecf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-navtags = django-bootstrap-navtags:main
    #
    # Would create an entry point called django-bootstrap-navtags that calls django-bootstrap-navtags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - bootstrapnavtags
    - bootstrapnavtags.templatetags
    - bootstrapnavtags.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/noxan/django-bootstrap-navtags
  license: MIT License
  summary: 'Navigation templatetags for django with twitter bootstrap.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
