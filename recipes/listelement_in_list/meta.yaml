package:
  name: listelement_in_list
  version: "1.0.0"

source:
  fn: listElement_in_list-1.0.0.zip
  url: https://pypi.python.org/packages/source/l/listElement_in_list/listElement_in_list-1.0.0.zip
  md5: 8793cfd597c4d0dc646bcfda188d3ee4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - listelement_in_list = listelement_in_list:main
    #
    # Would create an entry point called listelement_in_list that calls listelement_in_list.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - listelement_in_list

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hegus.info
  license: UNKNOWN
  summary: 'Ausgabe von Listen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
