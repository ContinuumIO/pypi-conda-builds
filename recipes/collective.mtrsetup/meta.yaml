package:
  name: collective.mtrsetup
  version: "1.5.2"

source:
  fn: collective.mtrsetup-1.5.2.zip
  url: https://pypi.python.org/packages/source/c/collective.mtrsetup/collective.mtrsetup-1.5.2.zip
  md5: 04b0b15b0b415c979dcad594dffe2afb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mtrsetup = collective.mtrsetup:main
    #
    # Would create an entry point called collective.mtrsetup that calls collective.mtrsetup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.genericsetup
    - products.mimetypesregistry
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - products.genericsetup
    - products.mimetypesregistry
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - collective
    - collective.mtrsetup
    - collective.mtrsetup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.mtrsetup
  license: GPL
  summary: 'Extension for GenericSetup, adding support for import / export of mimetypes_registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
