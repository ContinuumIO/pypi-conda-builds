package:
  name: kr-gitpython
  version: "0.3.1.1"

source:
  fn: kr-GitPython-0.3.1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kr-GitPython/kr-GitPython-0.3.1.1.tar.gz
  md5: 9e2bc1b665998aff943887deab465aca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kr-gitpython = kr-gitpython:main
    #
    # Would create an entry point called kr-gitpython that calls kr-gitpython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kr-gitdb >=0.5.1

  run:
    - python
    - kr-gitdb >=0.5.1

test:
  # Python imports
  imports:
    - git
    - git.db
    - git.db.cmd
    - git.db.dulwich
    - git.db.py
    - git.db.pygit2
    - git.index
    - git.objects
    - git.objects.submodule
    - git.refs
    - git.test
    - git.test.db
    - git.test.db.cmd
    - git.test.db.dulwich
    - git.test.db.py
    - git.test.db.pygit2
    - git.test.lib
    - git.test.objects
    - git.test.performance
    - git.test.performance.db
    - git.test.performance.objects
    - git.test.refs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitorious.org/projects/git-python/
  license: BSD License
  summary: 'Python Git Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
