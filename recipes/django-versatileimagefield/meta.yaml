package:
  name: django-versatileimagefield
  version: "1.0"

source:
  fn: django-versatileimagefield-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versatileimagefield/django-versatileimagefield-1.0.tar.gz
  md5: 2d2feee76709b371dbfba3571e81ed33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versatileimagefield = django-versatileimagefield:main
    #
    # Would create an entry point called django-versatileimagefield that calls django-versatileimagefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow >=2.4.0

  run:
    - python
    - pillow >=2.4.0

test:
  # Python imports
  imports:
    - tests
    - tests.test_autodiscover
    - versatileimagefield
    - versatileimagefield.datastructures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/WGBH/django-versatileimagefield/
  license: MIT License
  summary: "A drop-in replacement for django's ImageField that provides a flexible, intuitive and easily-extensible interface for creating new images from the one assigned to the field."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
