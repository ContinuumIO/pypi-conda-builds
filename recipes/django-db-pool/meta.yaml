package:
  name: django-db-pool
  version: "0.0.10"

source:
  fn: django-db-pool-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-pool/django-db-pool-0.0.10.tar.gz
  md5: 26b6a7805b26edd123c74bbe62ffd468
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-pool = django-db-pool:main
    #
    # Would create an entry point called django-db-pool that calls django-db-pool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3,<1.6.99
    - psycopg2 >=2.4.2

  run:
    - python
    - django >=1.3,<1.6.99
    - psycopg2 >=2.4.2

test:
  # Python imports
  imports:
    - dbpool
    - dbpool.db
    - dbpool.db.backends
    - dbpool.db.backends.postgis
    - dbpool.db.backends.postgresql_psycopg2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gmcguire/django-db-pool
  license: BSD License
  summary: 'Basic database persistance / connection pooling for Django + Postgres.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
