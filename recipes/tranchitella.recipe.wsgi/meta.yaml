package:
  name: tranchitella.recipe.wsgi
  version: "0.4"

source:
  fn: tranchitella.recipe.wsgi-0.4.tar.gz
  url: https://pypi.python.org/packages/source/t/tranchitella.recipe.wsgi/tranchitella.recipe.wsgi-0.4.tar.gz
  md5: a0ca982553cf554b731505e947635d9c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tranchitella.recipe.wsgi = tranchitella.recipe.wsgi:main
    #
    # Would create an entry point called tranchitella.recipe.wsgi that calls tranchitella.recipe.wsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - tranchitella
    - tranchitella.recipe
    - tranchitella.recipe.wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/tranchitella.recipe.wsgi
  license: GNU General Public License (GPL)
  summary: 'Buildout recipe to create paste.deploy entry points for mod_wsgi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
