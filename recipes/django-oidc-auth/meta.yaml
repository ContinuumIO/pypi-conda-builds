package:
  name: django-oidc-auth
  version: "0.0.5"

source:
  fn: django-oidc-auth-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oidc-auth/django-oidc-auth-0.0.5.tar.gz
  md5: 98d2ca2a24d54c893d61ea94faffa271
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oidc-auth = django-oidc-auth:main
    #
    # Would create an entry point called django-oidc-auth that calls django-oidc-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - pyjwkest
    - requests

  run:
    - python
    - django
    - south
    - pyjwkest
    - requests

test:
  # Python imports
  imports:
    - oidc_auth
    - oidc_auth.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'OpenID Connect client for Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
