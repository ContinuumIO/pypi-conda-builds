package:
  name: gocept.paypal
  version: "0.1.10"

source:
  fn: gocept.paypal-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.paypal/gocept.paypal-0.1.10.tar.gz
  md5: c74e3d3ed2b6d52ef9e18b7ccbbc75be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.paypal = gocept.paypal:main
    #
    # Would create an entry point called gocept.paypal that calls gocept.paypal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.testbrowser
    - zope.interface
    - zope.component

  run:
    - python
    - zc.testbrowser
    - zope.interface
    - zope.component

test:
  # Python imports
  imports:
    - gocept
    - gocept.paypal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.paypal/
  license: Zope Public License
  summary: 'Paypal Utility providing the paypal API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
