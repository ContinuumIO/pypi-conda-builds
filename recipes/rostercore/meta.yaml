package:
  name: rostercore
  version: "0.17"

source:
  fn: RosterCore-0.17.tar.gz
  url: https://pypi.python.org/packages/source/R/RosterCore/RosterCore-0.17.tar.gz
  md5: 3cadd954cda12d3442df31686f6ad681
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rostercore = rostercore:main
    #
    # Would create an entry point called rostercore that calls rostercore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipy >=0.62
    - mysql-python >=1.2.2
    - iscpy

  run:
    - python
    - ipy >=0.62
    - mysql-python >=1.2.2
    - iscpy

test:
  # Python imports
  imports:
    - roster_core
    - rostercore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/roster-dns-management/
  license: BSD License
  summary: 'RosterCore contains the Core and  DB libraries for Roster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
