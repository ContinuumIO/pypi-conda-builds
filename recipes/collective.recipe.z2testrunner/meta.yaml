package:
  name: collective.recipe.z2testrunner
  version: "0.3.1"

source:
  fn: collective.recipe.z2testrunner-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.z2testrunner/collective.recipe.z2testrunner-0.3.1.tar.gz
  md5: dc3bf37087da929f18e2750bbc8c9727
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.z2testrunner = collective.recipe.z2testrunner:main
    #
    # Would create an entry point called collective.recipe.z2testrunner that calls collective.recipe.z2testrunner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.z2testrunner
    - collective.recipe.z2testrunner.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/buildout/collective.recipe.z2testrunner
  license: GPL
  summary: 'zc.buildout recipe for generating zope2-based test runner'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
