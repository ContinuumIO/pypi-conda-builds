package:
  name: flask-beans
  version: "1.2"

source:
  fn: flask-beans-1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-beans/flask-beans-1.2.tar.gz
  md5: fb44229b7098ea49d5bcf7297c115caa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-beans = flask-beans:main
    #
    # Would create an entry point called flask-beans that calls flask-beans.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask
    - redis

  run:
    - python
    - flask
    - redis

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joshourisman/flask-beans
  license: BSD License
  summary: 'A simple API for counting beans with Flask and Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
