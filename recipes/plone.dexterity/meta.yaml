package:
  name: plone.dexterity
  version: "2.3.0"

source:
  fn: plone.dexterity-2.3.0.zip
  url: https://pypi.python.org/packages/source/p/plone.dexterity/plone.dexterity-2.3.0.zip
  md5: 59ae5e7e6d3c542e57ab1ea309b5c651
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.dexterity = plone.dexterity:main
    #
    # Would create an entry point called plone.dexterity that calls plone.dexterity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfcore
    - products.cmfdynamicviewfti
    - products.statusmessages
    - zodb3
    - zope2
    - plone.alterego
    - plone.autoform >=1.0b2
    - plone.behavior >=1.0b5
    - plone.folder
    - plone.memoize
    - plone.rfc822
    - plone.supermodel >=1.0b2
    - plone.synchronize
    - plone.uuid
    - plone.z3cform >=0.6.0
    - setuptools
    - zope.annotation
    - zope.browser
    - zope.component
    - zope.container
    - zope.dottedname
    - zope.filerepresentation >=3.6.0
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.size

  run:
    - python
    - setuptools
    - products.cmfcore
    - products.cmfdynamicviewfti
    - products.statusmessages
    - zodb3
    - zope2
    - plone.alterego
    - plone.autoform >=1.0b2
    - plone.behavior >=1.0b5
    - plone.folder
    - plone.memoize
    - plone.rfc822
    - plone.supermodel >=1.0b2
    - plone.synchronize
    - plone.uuid
    - plone.z3cform >=0.6.0
    - setuptools
    - zope.annotation
    - zope.browser
    - zope.component
    - zope.container
    - zope.dottedname
    - zope.filerepresentation >=3.6.0
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.size

test:
  # Python imports
  imports:
    - plone
    - plone.dexterity
    - plone.dexterity.browser
    - plone.dexterity.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/plone.dexterity
  license: GPL version 2
  summary: 'Framework for content types as filesystem code and TTW (Zope/CMF/Plone)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
