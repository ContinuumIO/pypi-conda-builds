package:
  name: nose-alembic-attrib
  version: "0.1"

source:
  fn: nose-alembic-attrib-0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nose-alembic-attrib/nose-alembic-attrib-0.1.tar.gz
  md5: 715e609e99e2244654c1e1987d3066ba
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nose-alembic-attrib = nose-alembic-attrib:main
    #
    # Would create an entry point called nose-alembic-attrib that calls nose-alembic-attrib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - nose >=1.2.0
    - alembic >=0.4.2

  run:
    - python
    - setuptools
    - setuptools
    - nose >=1.2.0
    - alembic >=0.4.2

test:
  # Python imports
  imports:
    - nose_alembic_attrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jsok/nose-alembic-attrib
  license: BSD
  summary: 'Alembic attributes for nose'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
