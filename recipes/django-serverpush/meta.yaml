package:
  name: django-serverpush
  version: "2.1.0"

source:
  fn: django-serverpush.zip
  url: https://pypi.python.org/packages/source/d/django-serverpush/django-serverpush.zip
  md5: fed596f7eb7cf2967c026b0f9bbb71e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-serverpush = django-serverpush:main
    #
    # Would create an entry point called django-serverpush that calls django-serverpush.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornadio2 >=0.0.2
    - django >=1.0.0

  run:
    - python
    - tornadio2 >=0.0.2
    - django >=1.0.0

test:
  # Python imports
  imports:
    - serverpush
    - serverpush.management
    - serverpush.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hamax/django-serverpush/
  license: django_service_admin
  summary: 'Easy to use django server push solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
