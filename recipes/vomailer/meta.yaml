package:
  name: vomailer
  version: "1.0.0"

source:
  fn: VOMailer-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/V/VOMailer/VOMailer-1.0.0.tar.gz
  md5: 964d7febc82be2ed985de6101eac3677
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vomailer = vomailer:main
    #
    # Would create an entry point called vomailer that calls vomailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - vomailer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/VOMailer/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Volunteer Ottawa Email Application Processor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
