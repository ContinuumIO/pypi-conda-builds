package:
  name: zmqdump
  version: "0.1"

source:
  fn: zmqdump-0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zmqdump/zmqdump-0.1.tar.gz
  md5: a262ff289f87bd94b5f56ef0740412d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zmqdump = zmqdump:main
    #
    # Would create an entry point called zmqdump that calls zmqdump.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq >=14.0.1

  run:
    - python
    - pyzmq >=14.0.1

test:
  # Python imports
  imports:
    - zmqdump

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/HeinrichHartmann/zmqdump
  license: UNKNOWN
  summary: 'Dump messages on zeromq socket.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
