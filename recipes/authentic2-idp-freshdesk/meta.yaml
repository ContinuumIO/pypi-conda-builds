package:
  name: authentic2-idp-freshdesk
  version: "0.2"

source:
  fn: authentic2-idp-freshdesk-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/authentic2-idp-freshdesk/authentic2-idp-freshdesk-0.2.tar.gz
  md5: 0f4457454dee16e749840fd76d0fffde
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authentic2-idp-freshdesk = authentic2-idp-freshdesk:main
    #
    # Would create an entry point called authentic2-idp-freshdesk that calls authentic2-idp-freshdesk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - authentic2_idp_freshdesk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: AGPLv3
  summary: 'Freshdesk simple SSO support for Authentic2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
