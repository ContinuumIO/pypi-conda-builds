package:
  name: eav-django
  version: "1.4.7"

source:
  fn: eav-django-1.4.7.tar.gz
  url: https://pypi.python.org/packages/source/e/eav-django/eav-django-1.4.7.tar.gz
  md5: 2bd572bbb7336038e8f48c49aac16e02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eav-django = eav-django:main
    #
    # Would create an entry point called eav-django that calls eav-django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eav

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/neithere/eav-django/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'a reusable Django application which implements the Entity-Attribute-Value data model.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
