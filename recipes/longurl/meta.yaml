package:
  name: longurl
  version: "0.1"

source:
  fn: longurl-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/longurl/longurl-0.1.tar.gz
  md5: a039b1a6a3624ca9d3339e0406060875
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - longurl = longurl:main
    #
    # Would create an entry point called longurl that calls longurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - longurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mswbb/python-longurl/overview
  license: BSD License
  summary: 'a wrapper for the LongURL expansion service at longurl.org'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
