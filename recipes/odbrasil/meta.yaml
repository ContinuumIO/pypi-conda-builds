package:
  name: odbrasil
  version: "0.1"

source:
  fn: odbrasil-0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/odbrasil/odbrasil-0.1.tar.gz
  md5: 59026631658a8397d3af8358cfb71a66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - odbrasil = odbrasil:main
    #
    # Would create an entry point called odbrasil that calls odbrasil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.13.0
    - pandas >=0.8.0

  run:
    - python
    - requests >=0.13.0
    - pandas >=0.8.0

test:
  # Python imports
  imports:
    - odbrasil
    - odbrasil.legislativo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/perone/odbrasil/
  license: Apache Software License
  summary: 'A Python package for retrieving and parsing Brazilian government open data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
