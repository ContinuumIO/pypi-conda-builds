package:
  name: django-vooster
  version: "0.0.39"

source:
  fn: django-vooster-0.0.39.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vooster/django-vooster-0.0.39.tar.gz
  md5: 845fd8803eea13c8967d49c273b5c444
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vooster = django-vooster:main
    #
    # Would create an entry point called django-vooster that calls django-vooster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_vooster
    - django_vooster.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mufularo.ru/
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
