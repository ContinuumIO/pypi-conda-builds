package:
  name: python-loaders
  version: "0.2.3"

source:
  fn: python-loaders-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/p/python-loaders/python-loaders-0.2.3.tar.gz
  md5: 2a3d006d7adfaca1af4879fcf08a8bc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-loaders = python-loaders:main
    #
    # Would create an entry point called python-loaders that calls python-loaders.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - proxytypes ==0.9

  run:
    - python
    - proxytypes ==0.9

test:
  # Python imports
  imports:
    - loaders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/FelixLoether/python-loaders
  license: MIT License
  summary: 'Small collection of custom module loaders for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
