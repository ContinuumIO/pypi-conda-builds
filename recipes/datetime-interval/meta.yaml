package:
  name: datetime-interval
  version: "0.2"

source:
  fn: datetime-interval-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/datetime-interval/datetime-interval-0.2.tar.gz
  md5: 0d43381ba3269878fe4b51a0c5ac7555
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datetime-interval = datetime-interval:main
    #
    # Would create an entry point called datetime-interval that calls datetime-interval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datetime_interval

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/zeckalpha/datetime-interval
  license: MIT License
  summary: 'A representation of a duration of time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
