package:
  name: ofxstatement-austrian
  version: "0.0.1"

source:
  fn: ofxstatement-austrian-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/ofxstatement-austrian/ofxstatement-austrian-0.0.1.tar.gz
  md5: 367c42a15b04fe0e6518953ef4ec5a34
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ofxstatement-austrian = ofxstatement-austrian:main
    #
    # Would create an entry point called ofxstatement-austrian that calls ofxstatement-austrian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ofxstatement

  run:
    - python
    - setuptools
    - ofxstatement

test:
  # Python imports
  imports:
    - ofxstatement
    - ofxstatement.plugins
    - ofxstatement.plugins.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nblock/ofxstatement-austrian
  license: GNU General Public License v3 (GPLv3)
  summary: 'Statement parser for several austrian banks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
