package:
  name: django-quick-photos
  version: "0.1"

source:
  fn: django-quick-photos-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quick-photos/django-quick-photos-0.1.tar.gz
  md5: 55734b0e7d4902d4998192e03fe4e775
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quick-photos = django-quick-photos:main
    #
    # Would create an entry point called django-quick-photos that calls django-quick-photos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - python-instagram >=0.8.0

  run:
    - python
    - python-instagram >=0.8.0

test:
  # Python imports
  imports:
    - quickphotos
    - quickphotos.management
    - quickphotos.management.commands
    - quickphotos.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blancltd/django-quick-photos
  license: BSD License
  summary: 'Latest Photos from Instagram for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
