package:
  name: python-parameters
  version: "1.2.2"

source:
  fn: python-parameters-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-parameters/python-parameters-1.2.2.tar.gz
  md5: 2baaf3527306b00b8b808d28f449e93e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-parameters = python-parameters:main
    #
    # Would create an entry point called python-parameters that calls python-parameters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - parameters
    - parameters.utility

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.matthewwardrop.info/
  license: python-parsely
  summary: 'A parameter manager that keeps track of physical (or numerical) quantities, and the relationships between them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
