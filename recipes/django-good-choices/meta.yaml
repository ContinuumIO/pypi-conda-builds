package:
  name: django-good-choices
  version: "1.4"

source:
  fn: django-good-choices-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-good-choices/django-good-choices-1.4.tar.gz
  md5: bf7354bce226848e1938296b5da2ab4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-good-choices = django-good-choices:main
    #
    # Would create an entry point called django-good-choices that calls django-good-choices.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piotrekio/django-good-choices
  license: UNKNOWN
  summary: 'Simple and convenient approach to "choices" in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
