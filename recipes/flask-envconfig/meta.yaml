package:
  name: flask-envconfig
  version: "0.2.0"

source:
  fn: Flask-EnvConfig-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-EnvConfig/Flask-EnvConfig-0.2.0.tar.gz
  md5: aba424953e449af18c4e667905d8e280
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-envconfig = flask-envconfig:main
    #
    # Would create an entry point called flask-envconfig that calls flask-envconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/romabysen/flask-envconfig/
  license: BSD License
  summary: 'Configure Flask from environment variables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
