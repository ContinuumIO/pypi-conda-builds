package:
  name: products.pluggablecatalog
  version: "1.0"

source:
  fn: Products.pluggablecatalog-1.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.pluggablecatalog/Products.pluggablecatalog-1.0.tar.gz
  md5: 53cef522c444a7a52522bb783d6a9ef5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.pluggablecatalog = products.pluggablecatalog:main
    #
    # Would create an entry point called products.pluggablecatalog that calls products.pluggablecatalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.pluggablecatalog
    - Products.pluggablecatalog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.example
  license: GPL
  summary: "pluggablecatalog is a replacement (or rather: a wrapper) for Plone's portal catalog"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
