package:
  name: eternity
  version: "0.13"

source:
  fn: eternity-0.13.tar.gz
  url: http://appropriatesoftware.net/provide/docs/eternity-0.13.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eternity = eternity:main
    #
    # Would create an entry point called eternity that calls eternity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - eternity
    - eternity/apache
    - eternity/application
    - eternity/cli
    - eternity/command
    - eternity/django
    - eternity/django/apps
    - eternity/django/apps/eui
    - eternity/django/apps/eui/views
    - eternity/django/settings
    - eternity/django/settings/urls
    - eternity/dom
    - eternity/utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://appropriatesoftware.net/eternity/Home.html
  license: GNU General Public License (GPL)
  summary: 'The eternity system is a domainmodel  application which supports working in iterations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
