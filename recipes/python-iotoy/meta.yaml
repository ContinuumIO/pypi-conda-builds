package:
  name: python-iotoy
  version: "1.0.0"

source:
  fn: python-iotoy-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-iotoy/python-iotoy-1.0.0.tar.gz
  md5: e329e23ed0003959f072f4b792a195f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-iotoy = python-iotoy:main
    #
    # Would create an entry point called python-iotoy that calls python-iotoy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iotoy
    - iotoy.attic
    - iotoy.attic.oven_spike

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.rd.bbc.co.uk/~michael/
  license: Apache Software License
  summary: 'python-iotoy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
