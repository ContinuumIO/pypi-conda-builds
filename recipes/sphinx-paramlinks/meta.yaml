package:
  name: sphinx-paramlinks
  version: "0.2.2"

source:
  fn: sphinx-paramlinks-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-paramlinks/sphinx-paramlinks-0.2.2.tar.gz
  md5: 3e0a74b325b1cb65e917671a3790da7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-paramlinks = sphinx-paramlinks:main
    #
    # Would create an entry point called sphinx-paramlinks that calls sphinx-paramlinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sphinx_paramlinks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/zzzeek/sphinx-paramlinks
  license: MIT
  summary: 'Allows param links in Sphinx function/method descriptions to be linkable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
