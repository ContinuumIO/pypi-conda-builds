package:
  name: sqlalchemy-searchable
  version: "0.9.2"

source:
  fn: SQLAlchemy-Searchable-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Searchable/SQLAlchemy-Searchable-0.9.2.tar.gz
  md5: dd510bbf6ed7167df626de81d171a9c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-searchable = sqlalchemy-searchable:main
    #
    # Would create an entry point called sqlalchemy-searchable that calls sqlalchemy-searchable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing >=2.0.1
    - sqlalchemy >=0.9.0
    - sqlalchemy-utils >=0.29.0
    - validators >=0.3.0

  run:
    - python
    - pyparsing >=2.0.1
    - sqlalchemy >=0.9.0
    - sqlalchemy-utils >=0.29.0
    - validators >=0.3.0

test:
  # Python imports
  imports:
    - sqlalchemy_searchable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/sqlalchemy-searchable
  license: BSD License
  summary: 'Provides fulltext search capabilities for declarative SQLAlchemy models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
