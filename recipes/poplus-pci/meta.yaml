package:
  name: poplus-pci
  version: "0.1.12"

source:
  fn: poplus-pci-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/p/poplus-pci/poplus-pci-0.1.12.tar.gz
  md5: ac1130ad3993d8d6cce943af5f5362d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - poplus-pci = poplus-pci:main
    #
    # Would create an entry point called poplus-pci that calls poplus-pci.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tortilla

  run:
    - python
    - tortilla

test:
  # Python imports
  imports:
    - pci

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/openpolis/poplus-pci
  license: MIT License
  summary: 'Generic python bindings to connect to the Poplus components'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
