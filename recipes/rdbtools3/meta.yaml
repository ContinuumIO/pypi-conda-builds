package:
  name: rdbtools3
  version: "0.1.2"

source:
  fn: rdbtools3-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/r/rdbtools3/rdbtools3-0.1.2.tar.gz
  md5: 8b19e014a299e7084e68a8de9c1e5acb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rdbtools3 = rdbtools3:main
    #
    # Would create an entry point called rdbtools3 that calls rdbtools3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rdbtools3
    - rdbtools3.cli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/popravich/rdbtools3
  license: MIT License
  summary: 'Redis dump.rdb parser library and cli tool implemented with Python 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
