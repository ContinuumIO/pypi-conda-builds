package:
  name: collective.autosaveform
  version: "0.2"

source:
  fn: collective.autosaveform-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.autosaveform/collective.autosaveform-0.2.zip
  md5: 2f7b5264c4488a744b64db56f0248ccd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.autosaveform = collective.autosaveform:main
    #
    # Would create an entry point called collective.autosaveform that calls collective.autosaveform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - jquery.pyproxy

  run:
    - python
    - setuptools
    - setuptools
    - jquery.pyproxy

test:
  # Python imports
  imports:
    - collective
    - collective.autosaveform
    - collective.autosaveform.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Utilities to auto save forms while user fills them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
