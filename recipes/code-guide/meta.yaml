package:
  name: code-guide
  version: "1.0.2"

source:
  fn: code-guide-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/code-guide/code-guide-1.0.2.tar.gz
  md5: e539648217a4b5a6c1492b3c72ca2c38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - code-guide = code-guide:main
    #
    # Would create an entry point called code-guide that calls code-guide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown ==2.3.1
    - pygments ==1.6

  run:
    - python
    - markdown ==2.3.1
    - pygments ==1.6

test:
  # Python imports
  imports:
    - code_guide

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://natpryce.com
  license: Apache Software License or MIT License
  summary: 'Turn example code into interactive HTML documentation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
