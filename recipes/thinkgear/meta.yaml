package:
  name: thinkgear
  version: "0.2"

source:
  fn: thinkgear-0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/thinkgear/thinkgear-0.2.tar.gz
  md5: 83091706dc3e1710099879c57f99e403
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thinkgear = thinkgear:main
    #
    # Would create an entry point called thinkgear that calls thinkgear.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - thinkgear

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'thinkgear parses the ThinkGear protocol used by NeuroSky MindSet devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
