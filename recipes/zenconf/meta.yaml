package:
  name: zenconf
  version: "0.1.4"

source:
  fn: zenconf-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zenconf/zenconf-0.1.4.tar.gz
  md5: 543cb9dc0adc76ffee48089e5e2cdcef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zenconf = zenconf:main
    #
    # Would create an entry point called zenconf that calls zenconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - funcy ==0.9

  run:
    - python
    - funcy ==0.9

test:
  # Python imports
  imports:
    - zenconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nws-cip/zenconf
  license: MIT
  summary: 'Simple configuration system based on recursively merging dicts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
