package:
  name: brzipcode
  version: "0.1.2"

source:
  fn: brzipcode-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/brzipcode/brzipcode-0.1.2.tar.gz
  md5: b28084d9b1992c7d61d0f5526e695e48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brzipcode = brzipcode:main
    #
    # Would create an entry point called brzipcode that calls brzipcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - brzipcode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rpedigoni/brzipcode
  license: BSD
  summary: 'brzipcode is a lightweight app for retrieving brazilian addresses from a zip code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
