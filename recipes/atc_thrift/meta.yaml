package:
  name: atc_thrift
  version: "0.1.1"

source:
  fn: atc_thrift-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/atc_thrift/atc_thrift-0.1.1.tar.gz
  md5: 469ab1385af8e83067edddc8713a3d15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atc_thrift = atc_thrift:main
    #
    # Would create an entry point called atc_thrift that calls atc_thrift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - thrift

  run:
    - python
    - thrift

test:
  # Python imports
  imports:
    - atc_thrift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/facebook/augmented-traffic-control
  license: ATD_document
  summary: 'ATC Thrift Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
