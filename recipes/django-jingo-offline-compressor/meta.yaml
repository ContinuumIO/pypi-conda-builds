package:
  name: django-jingo-offline-compressor
  version: "0.0.7"

source:
  fn: django-jingo-offline-compressor-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jingo-offline-compressor/django-jingo-offline-compressor-0.0.7.tar.gz
  md5: a9fc4f50c27274984102ffe119739a4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jingo-offline-compressor = django-jingo-offline-compressor:main
    #
    # Would create an entry point called django-jingo-offline-compressor that calls django-jingo-offline-compressor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jingo_offline_compressor
    - jingo_offline_compressor.management
    - jingo_offline_compressor.management.commands
    - jingo_offline_compressor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/peterbe/django-jingo-offline-compressor/
  license: MIT License
  summary: 'Using jingo and django_compressor but miss offline compression?'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
