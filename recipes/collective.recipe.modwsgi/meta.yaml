package:
  name: collective.recipe.modwsgi
  version: "2.1"

source:
  fn: collective.recipe.modwsgi-2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.modwsgi/collective.recipe.modwsgi-2.1.tar.gz
  md5: 3c529d23c17e67009340657a0efaa9c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.modwsgi = collective.recipe.modwsgi:main
    #
    # Would create an entry point called collective.recipe.modwsgi that calls collective.recipe.modwsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.modwsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wichert/collective.recipe.modwsgi
  license: Zope Public License
  summary: 'WSGI from buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
