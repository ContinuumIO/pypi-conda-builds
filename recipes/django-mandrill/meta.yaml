package:
  name: django-mandrill
  version: "0.1.0"

source:
  fn: django-mandrill-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mandrill/django-mandrill-0.1.0.tar.gz
  md5: b31759507cfe5cb074b3a6ce72013ae1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mandrill = django-mandrill:main
    #
    # Would create an entry point called django-mandrill that calls django-mandrill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mandrill

  run:
    - python
    - mandrill

test:
  # Python imports
  imports:
    - django_mandrill
    - django_mandrill.mail
    - django_mandrill.mail.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sirpengi/django-mandrill
  license: MIT License
  summary: 'Mandrill integration for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
