package:
  name: cns.recipe.maildrophost
  version: "1.5.2"

source:
  fn: cns.recipe.maildrophost-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cns.recipe.maildrophost/cns.recipe.maildrophost-1.5.2.tar.gz
  md5: aae0ca267e8536853a6f700634ffc2b7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cns.recipe.maildrophost = cns.recipe.maildrophost:main
    #
    # Would create an entry point called cns.recipe.maildrophost that calls cns.recipe.maildrophost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - cns
    - cns.recipe
    - cns.recipe.maildrophost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/cns.recipe.maildrophost
  license: GPL
  summary: 'Recipe for installing MaildropHost'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
