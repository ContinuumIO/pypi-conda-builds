package:
  name: django-affect
  version: "1.1.0"

source:
  fn: django-affect-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-affect/django-affect-1.1.0.tar.gz
  md5: 7cd6c1fc2f8ee675694c8202a48aa3ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-affect = django-affect:main
    #
    # Would create an entry point called django-affect that calls django-affect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-extensions

  run:
    - python
    - django >=1.4
    - django-extensions

test:
  # Python imports
  imports:
    - affect
    - affect.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ConsumerAffairs/django-affect
  license: UNKNOWN
  summary: 'Request flagging engine inspired by django-waffle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
