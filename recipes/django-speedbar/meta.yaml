package:
  name: django-speedbar
  version: "0.1.3"

source:
  fn: django-speedbar-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-speedbar/django-speedbar-0.1.3.tar.gz
  md5: 87ff8c037e7037184f558d5f5e341e37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-speedbar = django-speedbar:main
    #
    # Would create an entry point called django-speedbar that calls django-speedbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - proxytypes >=0.9
    - django >=1.5,<1.8a0

  run:
    - python
    - proxytypes >=0.9
    - django >=1.5,<1.8a0

test:
  # Python imports
  imports:
    - speedbar
    - speedbar.modules
    - speedbar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/theospears/django-speedbar
  license: MIT License
  summary: 'Provides a break down of page loading time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
