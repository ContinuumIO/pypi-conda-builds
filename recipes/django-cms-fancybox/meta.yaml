package:
  name: django-cms-fancybox
  version: "1.0.2"

source:
  fn: django-cms-fancybox-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-fancybox/django-cms-fancybox-1.0.2.tar.gz
  md5: c248eea55d036d72394e364022d6323e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-fancybox = django-cms-fancybox:main
    #
    # Would create an entry point called django-cms-fancybox that calls django-cms-fancybox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-cms
    - django-filer
    - django-tinymce

  run:
    - python
    - setuptools
    - django
    - django-cms
    - django-filer
    - django-tinymce

test:
  # Python imports
  imports:
    - cmsplugin_fancybox
    - cmsplugin_fancybox.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/megamark16/django-cms-fancybox
  license: BSD License
  summary: 'A fancybox extension for Django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
