package:
  name: django-viewform
  version: "0.1.0"

source:
  fn: django-viewform-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewform/django-viewform-0.1.0.tar.gz
  md5: ae05ec05ea244778460e181f73ae5d6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewform = django-viewform:main
    #
    # Would create an entry point called django-viewform that calls django-viewform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-tag-parser >=2.0

  run:
    - python
    - django-tag-parser >=2.0

test:
  # Python imports
  imports:
    - viewform
    - viewform.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/viewflow/viewform
  license: MIT License
  summary: 'Template driven form rendering for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
