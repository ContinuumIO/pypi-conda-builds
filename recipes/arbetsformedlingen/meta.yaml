package:
  name: arbetsformedlingen
  version: "0.1.0"

source:
  fn: arbetsformedlingen-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/arbetsformedlingen/arbetsformedlingen-0.1.0.tar.gz
  md5: 0c6e3b03132ecf1197b2e15ba89cf316
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arbetsformedlingen = arbetsformedlingen:main
    #
    # Would create an entry point called arbetsformedlingen that calls arbetsformedlingen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - arbetsformedlingen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/willandskill/arbetsformedlingen
  license: BSD licence
  summary: 'Wrapper for http://api.arbetsformedlingen.se/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
