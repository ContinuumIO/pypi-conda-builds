package:
  name: apirator
  version: "0.1.0"

source:
  fn: apirator-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/apirator/apirator-0.1.0.tar.gz
  md5: 5a805a1100f08b4de98f96ab225ab960
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apirator = apirator:main
    #
    # Would create an entry point called apirator that calls apirator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - apirator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/barbuza/apirator
  license: MIT
  summary: 'easily add simple http api to your django web app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
