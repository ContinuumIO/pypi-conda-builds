package:
  name: expsmooth
  version: "2.1.2"

source:
  fn: expsmooth-2.1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/expsmooth/expsmooth-2.1.2.tar.gz
  md5: 00fc7052be4ece3d48b7a6d2282d525a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expsmooth = expsmooth:main
    #
    # Would create an entry point called expsmooth that calls expsmooth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - expsmooth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/box-and-whisker/expsmooth
  license: MIT License
  summary: 'Exponential smoothing library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
