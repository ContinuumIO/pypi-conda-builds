package:
  name: balbuzard
  version: "0.19"

source:
  fn: balbuzard-0.19.zip
  url: https://pypi.python.org/packages/source/b/balbuzard/balbuzard-0.19.zip
  md5: 956a34edbb6fe62725ed9ba9a6e13365
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - balbuzard = balbuzard:main
    #
    # Would create an entry point called balbuzard that calls balbuzard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - balbuzard
    - balbuzard.thirdparty
    - balbuzard.thirdparty.magic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.decalage.info/python/balbuzard
  license: BSD License
  summary: 'Malware analysis tools to extract patterns of interest from files and crack obfuscation such as XOR'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
