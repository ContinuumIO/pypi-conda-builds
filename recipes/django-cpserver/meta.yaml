package:
  name: django-cpserver
  version: "1.1"

source:
  fn: django-cpserver-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cpserver/django-cpserver-1.1.tar.gz
  md5: e9c17db9f36c1883f36235fafc27c689
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cpserver = django-cpserver:main
    #
    # Would create an entry point called django-cpserver that calls django-cpserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cpserver
    - cpserver.management
    - cpserver.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.piranha.org.ua/cpserver/
  license: BSD License
  summary: 'Django runserver-like commands to run CherryPy web server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
