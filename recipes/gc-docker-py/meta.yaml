package:
  name: gc-docker-py
  version: "0.4.0"

source:
  fn: gc-docker-py-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gc-docker-py/gc-docker-py-0.4.0.tar.gz
  md5: ab6f731cafc583fdcda9ad6b10f1cb25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gc-docker-py = gc-docker-py:main
    #
    # Would create an entry point called gc-docker-py that calls gc-docker-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.0
    - six >=1.3.0
    - websocket-client >=0.11.0
    - mock ==1.0.1
    - coverage ==3.7.1

  run:
    - python
    - requests >=2.0.0
    - six >=1.3.0
    - websocket-client >=0.11.0
    - mock ==1.0.1
    - coverage ==3.7.1

test:
  # Python imports
  imports:
    - docker
    - docker.auth
    - docker.ssladapter
    - docker.unixconn
    - docker.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Python client for Docker.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
