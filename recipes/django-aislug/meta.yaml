package:
  name: django-aislug
  version: "0.2.2"

source:
  fn: django-aislug-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aislug/django-aislug-0.2.2.tar.gz
  md5: 594cac668a97a456c48dc2951b07fbb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aislug = django-aislug:main
    #
    # Would create an entry point called django-aislug that calls django-aislug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-stringfield >=0.1.5

  run:
    - python
    - django-stringfield >=0.1.5

test:
  # Python imports
  imports:
    - aislug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/django-ainslug
  license: BSD License
  summary: 'Intelligent slug computing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
