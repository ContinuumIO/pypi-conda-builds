package:
  name: cyclone-sse
  version: "0.7.7"

source:
  fn: cyclone-sse-0.7.7.tar.gz
  url: https://pypi.python.org/packages/source/c/cyclone-sse/cyclone-sse-0.7.7.tar.gz
  md5: bacf9c843ede3e36bd1727dc37ce79cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyclone-sse = cyclone-sse:main
    #
    # Would create an entry point called cyclone-sse that calls cyclone-sse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - cyclone
    - txamqp

  run:
    - python
    - twisted
    - cyclone
    - txamqp

test:
  # Python imports
  imports:
    - cyclone_sse
    - twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FZambia/cyclone-sse
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'EventSource (SSE, Server-Sent Events) broadcasting server with channel support, Last-Event-Id etc, based on top of cyclone web server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
