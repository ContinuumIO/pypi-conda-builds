package:
  name: chembl_compatibility
  version: "0.5.5"

source:
  fn: chembl_compatibility-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/c/chembl_compatibility/chembl_compatibility-0.5.5.tar.gz
  md5: 757cae2b3ce10adb4a4b46d4e4c98f2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chembl_compatibility = chembl_compatibility:main
    #
    # Would create an entry point called chembl_compatibility that calls chembl_compatibility.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chembl_core_model >=0.5.8
    - chembl_migration_model >=0.5.4

  run:
    - python
    - chembl_core_model >=0.5.8
    - chembl_migration_model >=0.5.4

test:
  # Python imports
  imports:
    - chembl_compatibility

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.ebi.ac.uk/chembl/
  license: Apache Software License
  summary: 'Compatibility layer for running webservices from outside ChEMBL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
