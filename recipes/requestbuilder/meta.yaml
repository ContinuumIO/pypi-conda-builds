package:
  name: requestbuilder
  version: "0.2.3"

source:
  fn: requestbuilder-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/r/requestbuilder/requestbuilder-0.2.3.tar.gz
  md5: 3d9793e3a3b3dad23a8475e0480581db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requestbuilder = requestbuilder:main
    #
    # Would create an entry point called requestbuilder that calls requestbuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1
    - six

  run:
    - python
    - requests >=1
    - six

test:
  # Python imports
  imports:
    - requestbuilder
    - requestbuilder.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/boto/requestbuilder
  license: ISC License (ISCL)
  summary: 'Command line-driven HTTP request builder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
