package:
  name: census
  version: "0.7"

source:
  fn: census-0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/census/census-0.7.tar.gz
  md5: 9041e12ac9fa870e90717f444f41e54f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - census = census:main
    #
    # Would create an entry point called census that calls census.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1.0
    - us >=0.7

  run:
    - python
    - requests >=1.1.0
    - us >=0.7

test:
  # Python imports
  imports:
    - census

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/census
  license: BSD License
  summary: "A wrapper for the US Census Bureau's API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
