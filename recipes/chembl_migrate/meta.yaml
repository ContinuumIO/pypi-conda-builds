package:
  name: chembl_migrate
  version: "0.6.0"

source:
  fn: chembl_migrate-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chembl_migrate/chembl_migrate-0.6.0.tar.gz
  md5: ce9c6c0391b9537761548423fe09707a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chembl_migrate = chembl_migrate:main
    #
    # Would create an entry point called chembl_migrate that calls chembl_migrate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5.5

  run:
    - python
    - django ==1.5.5

test:
  # Python imports
  imports:
    - chembl_migrate
    - chembl_migrate.management
    - chembl_migrate.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.ebi.ac.uk/chembl/
  license: Apache Software License
  summary: 'Django custom management tool intended to perform data exports and migration of CheMBL database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
