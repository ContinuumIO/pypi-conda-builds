package:
  name: django-database-size
  version: "0.3.0"

source:
  fn: django-database-size-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-database-size/django-database-size-0.3.0.tar.gz
  md5: 9b927c08ae4f0cc03b9bb88f728c1603
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-database-size = django-database-size:main
    #
    # Would create an entry point called django-database-size that calls django-database-size.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - database_size

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chrisspen/django-database-size
  license: BSD License
  summary: 'Monitor the size of database tables from Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
