package:
  name: achemkit
  version: "0.3.0"

source:
  fn: AChemKit-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/A/AChemKit/AChemKit-0.3.0.tar.gz
  md5: fc80d5eda067f3dbbfb31fc45368a6bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - achemkit = achemkit:main
    #
    # Would create an entry point called achemkit that calls achemkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - AChemKit
    - AChemKit.tools
    - AChemKit.utils
    - achemkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/afaulconbridge/PyAChemKit
  license: BSD License
  summary: 'An Artificial Chemistry Tookit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
