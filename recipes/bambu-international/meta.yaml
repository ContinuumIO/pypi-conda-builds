package:
  name: bambu-international
  version: "0.1"

source:
  fn: bambu-international-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-international/bambu-international-0.1.tar.gz
  md5: 40705e8423228d7ba93aa6bdff5227ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-international = bambu-international:main
    #
    # Would create an entry point called bambu-international that calls bambu-international.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - pygeoip

  run:
    - python
    - django >=1.4
    - pygeoip

test:
  # Python imports
  imports:
    - bambu.international
    - bambu.international.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-international
  license: UNKNOWN
  summary: "Provides a list of countries and a helper function to retrieve the Country object from a user's IP address"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
