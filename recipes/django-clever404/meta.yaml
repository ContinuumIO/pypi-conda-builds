package:
  name: django-clever404
  version: "0.1"

source:
  fn: django-clever404-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clever404/django-clever404-0.1.tar.gz
  md5: dc1ef0d9298d61a4dbfea305a4d5e012
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clever404 = django-clever404:main
    #
    # Would create an entry point called django-clever404 that calls django-clever404.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - clever404
    - clever404.management
    - clever404.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A django app to intercept 404 pages, and allow retroactive redirects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
