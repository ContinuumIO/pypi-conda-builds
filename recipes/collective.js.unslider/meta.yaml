package:
  name: collective.js.unslider
  version: "1.0.1"

source:
  fn: collective.js.unslider-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.unslider/collective.js.unslider-1.0.1.zip
  md5: f605962f649c50e64347403b4a8ce363
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.unslider = collective.js.unslider:main
    #
    # Would create an entry point called collective.js.unslider that calls collective.js.unslider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.unslider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.js.unslider
  license: UNKNOWN
  summary: 'Unslider.js resource for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
