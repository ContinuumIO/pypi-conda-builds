package:
  name: eventful
  version: "0.3"

source:
  fn: eventful-0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/eventful/eventful-0.3.tar.gz
  md5: 5d5893366ce2fdb671cdee6f54f1f510
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eventful = eventful:main
    #
    # Would create an entry point called eventful that calls eventful.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - httplib2

  run:
    - python
    - simplejson
    - httplib2

test:
  # Python imports
  imports:
    - eventful

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://api.eventful.com/libs/python/
  license: MIT License
  summary: 'A client for the Eventful API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
