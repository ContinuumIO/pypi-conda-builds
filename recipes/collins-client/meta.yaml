package:
  name: collins-client
  version: "0.1.0"

source:
  fn: collins_client-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collins-client/collins_client-0.1.0.tar.gz
  md5: 87fe506fe4831acc5080f368931ff587
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collins-client = collins-client:main
    #
    # Would create an entry point called collins-client that calls collins-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grequests ==0.2.0

  run:
    - python
    - grequests ==0.2.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tumblr/collins/tree/master/support/python/collins-client
  license: Apache Software License
  summary: 'The python interface to the collins api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
