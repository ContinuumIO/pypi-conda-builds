package:
  name: python-timeuuid
  version: "0.3.5"

source:
  fn: python-timeuuid-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/p/python-timeuuid/python-timeuuid-0.3.5.tar.gz
  md5: 621017b4587d28352f390071c066ebca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-timeuuid = python-timeuuid:main
    #
    # Would create an entry point called python-timeuuid that calls python-timeuuid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - timeuuid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chronology/python-timeuuid/
  license: MIT License
  summary: 'python-timeuuid is a fast Python library for sensibly dealing with Version 1 UUID (or TimeUUID).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
