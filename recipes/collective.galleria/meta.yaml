package:
  name: collective.galleria
  version: "1.2.3"

source:
  fn: collective.galleria-1.2.3.zip
  url: https://pypi.python.org/packages/source/c/collective.galleria/collective.galleria-1.2.3.zip
  md5: fb643fc2156e853c93394ee74b5b77b3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.galleria = collective.galleria:main
    #
    # Would create an entry point called collective.galleria that calls collective.galleria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.gallery
    - collective.configviews
    - collective.js.galleria

  run:
    - python
    - setuptools
    - setuptools
    - collective.gallery
    - collective.configviews
    - collective.js.galleria

test:
  # Python imports
  imports:
    - collective
    - collective.galleria
    - collective.galleria.browser
    - collective.galleria.interfaces
    - collective.galleria.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.galleria
  license: GPL
  summary: 'Galleria integration into Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
