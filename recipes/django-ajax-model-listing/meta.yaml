package:
  name: django-ajax-model-listing
  version: "0.1.5"

source:
  fn: django-ajax-model-listing-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-model-listing/django-ajax-model-listing-0.1.5.tar.gz
  md5: 3657fd6efec1eb79d4f63ad1e7202699
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-model-listing = django-ajax-model-listing:main
    #
    # Would create an entry point called django-ajax-model-listing that calls django-ajax-model-listing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajax_model_listing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-ajax-model-listing/
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'Ajax-based model listing for Django. Supports pagination, sorting and filtering. Needs jQuery. Docs coming soon. Demo project here: https://bitbucket.org/victorsilv4/django_ajax_model_listing_demos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
