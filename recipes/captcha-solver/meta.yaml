package:
  name: captcha-solver
  version: "0.0.1"

source:
  fn: captcha-solver-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/captcha-solver/captcha-solver-0.0.1.tar.gz
  md5: af2e3532aaf2a0d796503f94b234eb39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - captcha-solver = captcha-solver:main
    #
    # Would create an entry point called captcha-solver that calls captcha-solver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - captcha_solver
    - captcha_solver.captcha_backend
    - captcha_solver.contrib
    - captcha_solver.contrib.grab
    - captcha_solver.transport_backend
    - script
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Universal API to different captcha solving services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
