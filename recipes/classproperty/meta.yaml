package:
  name: classproperty
  version: "1.0"

source:
  fn: classproperty-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/classproperty/classproperty-1.0.tar.gz
  md5: 5abd0527fcf8884b7d07203a0967a433
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - classproperty = classproperty:main
    #
    # Would create an entry point called classproperty that calls classproperty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - classproperty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.z3lab.org/sections/blogs/philipp-weitershausen/2006_05_29_pycon-06-lightning-talk
  license: ZPL
  summary: 'Class properties'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
