package:
  name: django-ulogin
  version: "0.2.2"

source:
  fn: django-ulogin-0.2.2.tar.bz2
  url: https://pypi.python.org/packages/source/d/django-ulogin/django-ulogin-0.2.2.tar.bz2
  md5: 404c3cdd7952451d9420f1a545af084a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ulogin = django-ulogin:main
    #
    # Would create an entry point called django-ulogin that calls django-ulogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - requests
    - mock >=0.8.0
    - six

  run:
    - python
    - django
    - requests
    - mock >=0.8.0
    - six

test:
  # Python imports
  imports:
    - django_ulogin
    - django_ulogin.templatetags
    - django_ulogin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-ulogin
  license: BSD License
  summary: 'User social authentication with ulogin.ru service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
