package:
  name: collective.recipe.isapiwsgi
  version: "1.0b1"

source:
  fn: collective.recipe.isapiwsgi-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.isapiwsgi/collective.recipe.isapiwsgi-1.0b1.tar.gz
  md5: 637d932d1ecd77dfe37a0409d4cdcf50
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.isapiwsgi = collective.recipe.isapiwsgi:main
    #
    # Would create an entry point called collective.recipe.isapiwsgi that calls collective.recipe.isapiwsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.isapiwsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Zope Public License
  summary: 'ISAPI-WSGI wrapper based on buildout configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
