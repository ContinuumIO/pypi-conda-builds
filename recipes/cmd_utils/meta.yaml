package:
  name: cmd_utils
  version: "0.4.1"

source:
  fn: cmd_utils-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmd_utils/cmd_utils-0.4.1.tar.gz
  md5: c31304053a49c6b3fdef77442b954f60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmd_utils = cmd_utils:main
    #
    # Would create an entry point called cmd_utils that calls cmd_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko

  run:
    - python
    - paramiko

test:
  # Python imports
  imports:
    - cmd_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/graze/pycmd-utils
  license: MIT License
  summary: 'Wrapper for subprocess.Popen and paramiko'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
