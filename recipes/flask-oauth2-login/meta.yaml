package:
  name: flask-oauth2-login
  version: "0.0.9"

source:
  fn: Flask-OAuth2-Login-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-OAuth2-Login/Flask-OAuth2-Login-0.0.9.tar.gz
  md5: 1d214eec86765b76a116e0fe2351fcc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-oauth2-login = flask-oauth2-login:main
    #
    # Would create an entry point called flask-oauth2-login that calls flask-oauth2-login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - requests-oauthlib

  run:
    - python
    - requests
    - requests-oauthlib

test:
  # Python imports
  imports:
    - flask_oauth2_login

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/flask-oauth2-login
  license: MIT
  summary: 'Simple OAuth2 login'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
