package:
  name: silva.core.contentlayout
  version: "3.0.5"

source:
  fn: silva.core.contentlayout-3.0.5.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.core.contentlayout/silva.core.contentlayout-3.0.5.tar.gz
  md5: 67a360521830446a861ea133c059ff7c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.core.contentlayout = silva.core.contentlayout:main
    #
    # Would create an entry point called silva.core.contentlayout that calls silva.core.contentlayout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - grokcore.chameleon
    - grokcore.component
    - grokcore.view
    - setuptools
    - silva.core.conf
    - silva.core.editor
    - silva.core.interfaces
    - silva.core.references
    - silva.core.services
    - silva.core.views
    - silva.core.xml
    - silva.ui
    - zeam.component
    - zeam.form.silva
    - zope.annotation
    - zope.cachedescriptors
    - zope.component
    - zope.event
    - zope.i18n
    - zope.interface
    - zope.intid
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema
    - zope.traversing

  run:
    - python
    - setuptools
    - five.grok
    - grokcore.chameleon
    - grokcore.component
    - grokcore.view
    - setuptools
    - silva.core.conf
    - silva.core.editor
    - silva.core.interfaces
    - silva.core.references
    - silva.core.services
    - silva.core.views
    - silva.core.xml
    - silva.ui
    - zeam.component
    - zeam.form.silva
    - zope.annotation
    - zope.cachedescriptors
    - zope.component
    - zope.event
    - zope.i18n
    - zope.interface
    - zope.intid
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema
    - zope.traversing

test:
  # Python imports
  imports:
    - silva
    - silva.core
    - silva.core.contentlayout
    - silva.core.contentlayout.blocks
    - silva.core.contentlayout.defaults
    - silva.core.contentlayout.designs
    - silva.core.contentlayout.silvaxml
    - silva.core.contentlayout.slots
    - silva.core.contentlayout.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.core.contentlayout
  license: GPL
  summary: 'Silva Content Layout engine for Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
