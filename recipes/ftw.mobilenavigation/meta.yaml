package:
  name: ftw.mobilenavigation
  version: "1.4.1"

source:
  fn: ftw.mobilenavigation-1.4.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.mobilenavigation/ftw.mobilenavigation-1.4.1.zip
  md5: 69426d4686ff3dcc18765fab86b08e15
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.mobilenavigation = ftw.mobilenavigation:main
    #
    # Would create an entry point called ftw.mobilenavigation that calls ftw.mobilenavigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade
    - missing

  run:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade
    - missing

test:
  # Python imports
  imports:
    - ftw
    - ftw.mobilenavigation
    - ftw.mobilenavigation.Extensions
    - ftw.mobilenavigation.browser
    - ftw.mobilenavigation.tests
    - ftw.mobilenavigation.upgrades
    - ftw.mobilenavigation.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.mobilenavigation
  license: GPL2
  summary: 'Defines a special navigation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
