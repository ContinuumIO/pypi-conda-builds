package:
  name: plone.locking
  version: "2.0.5"

source:
  fn: plone.locking-2.0.5.zip
  url: https://pypi.python.org/packages/source/p/plone.locking/plone.locking-2.0.5.zip
  md5: 10eb80eb6548ea76985895cb98a8e9f5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.locking = plone.locking:main
    #
    # Would create an entry point called plone.locking that calls plone.locking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.annotation
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.viewlet
    - acquisition
    - datetime
    - products.cmfcore
    - zodb3
    - zope2

  run:
    - python
    - setuptools
    - zope.annotation
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.viewlet
    - acquisition
    - datetime
    - products.cmfcore
    - zodb3
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.locking
    - plone.locking.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.locking
  license: GNU General Public License (GPL)
  summary: 'webdav locking support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
