package:
  name: aldryn-snippet
  version: "0.1.3"

source:
  fn: aldryn-snippet-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-snippet/aldryn-snippet-0.1.3.tar.gz
  md5: 2b85ffd7d75ba6a06d99ed97653735e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-snippet = aldryn-snippet:main
    #
    # Would create an entry point called aldryn-snippet that calls aldryn-snippet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aldryn_snippet
    - aldryn_snippet.management
    - aldryn_snippet.management.commands
    - aldryn_snippet.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-snippet
  license: BSD License
  summary: 'Snippets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
