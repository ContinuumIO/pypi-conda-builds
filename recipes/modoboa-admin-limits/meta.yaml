package:
  name: modoboa-admin-limits
  version: "1.0.1"

source:
  fn: modoboa-admin-limits-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/modoboa-admin-limits/modoboa-admin-limits-1.0.1.tar.gz
  md5: 53fbe45150a9fc146de2ec96f764f4c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modoboa-admin-limits = modoboa-admin-limits:main
    #
    # Would create an entry point called modoboa-admin-limits that calls modoboa-admin-limits.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - modoboa-admin

  run:
    - python
    - modoboa-admin

test:
  # Python imports
  imports:
    - modoboa_admin_limits
    - modoboa_admin_limits.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://modoboa.org/
  license: MIT License
  summary: 'Per administrator limits for Modoboa'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
