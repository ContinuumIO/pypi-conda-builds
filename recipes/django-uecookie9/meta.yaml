package:
  name: django-uecookie9
  version: "0.1"

source:
  fn: django-uecookie9-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uecookie9/django-uecookie9-0.1.tar.gz
  md5: f6d07d439c621030e3baab4d65f7f4b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uecookie9 = django-uecookie9:main
    #
    # Would create an entry point called django-uecookie9 that calls django-uecookie9.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pygeoip >=0.3.2

  run:
    - python
    - django >=1.4
    - pygeoip >=0.3.2

test:
  # Python imports
  imports:
    - uecookie9

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/9dev/django-uecookie9
  license: MIT License
  summary: 'Django app that shows notification about cookies to UE citizens.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
