package:
  name: exoline
  version: "0.9.14"

source:
  fn: exoline-0.9.14.tar.gz
  url: https://pypi.python.org/packages/source/e/exoline/exoline-0.9.14.tar.gz
  md5: 1f12e50ef6a5e9c05ddea91d0085473b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exoline = exoline:main
    #
    # Would create an entry point called exoline that calls exoline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt-unicode ==0.6.1
    - requests ==2.6.0
    - pyonep ==0.11.1
    - python-dateutil ==2.4.1
    - pyyaml ==3.11
    - six ==1.9.0
    - pytz ==2015.2
    - tzlocal ==1.1.2
    - jsonschema ==2.3.0
    - ordereddict >=1.1
    - unicodecsv ==0.9.4

  run:
    - python
    - docopt-unicode ==0.6.1
    - requests ==2.6.0
    - pyonep ==0.11.1
    - python-dateutil ==2.4.1
    - pyyaml ==3.11
    - six ==1.9.0
    - pytz ==2015.2
    - tzlocal ==1.1.2
    - jsonschema ==2.3.0
    - ordereddict >=1.1
    - unicodecsv ==0.9.4

test:
  # Python imports
  imports:
    - exoline
    - exoline.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/exosite/exoline
  license: UNKNOWN
  summary: 'Command line interface for Exosite platform.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
