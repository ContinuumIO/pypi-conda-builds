package:
  name: django-simple-settings
  version: "0.3.1"

source:
  fn: django-simple-settings-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-settings/django-simple-settings-0.3.1.tar.gz
  md5: 91de1c2612f75151621de7da70126423
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-settings = django-simple-settings:main
    #
    # Would create an entry point called django-simple-settings that calls django-simple-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - simple_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alikus/django-simple-settings
  license: MIT License
  summary: 'A very simple settings configurable in Django Admin Panel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
