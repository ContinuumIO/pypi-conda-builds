package:
  name: redomino.seosupport
  version: "1.1"

source:
  fn: redomino.seosupport-1.1.zip
  url: https://pypi.python.org/packages/source/r/redomino.seosupport/redomino.seosupport-1.1.zip
  md5: e27dbeb18b3363e61d54acad4d335c7c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.seosupport = redomino.seosupport:main
    #
    # Would create an entry point called redomino.seosupport that calls redomino.seosupport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - googlesitemap.common

  run:
    - python
    - setuptools
    - setuptools
    - googlesitemap.common

test:
  # Python imports
  imports:
    - redomino
    - redomino.seosupport
    - redomino.seosupport.browser
    - redomino.seosupport.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.seosupport
  license: GPL
  summary: 'Installing this plugin you will get a Plone site with the sitemap.xml.gz enabled by default'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
