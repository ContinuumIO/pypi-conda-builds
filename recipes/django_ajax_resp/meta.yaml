package:
  name: django_ajax_resp
  version: "0.0.1-dev"

source:
  fn: django_ajax_resp-0.0.1-dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django_ajax_resp/django_ajax_resp-0.0.1-dev.tar.gz
  md5: a8aad523e9dd185dbecd99c137f31471
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_ajax_resp = django_ajax_resp:main
    #
    # Would create an entry point called django_ajax_resp that calls django_ajax_resp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_ajax_resp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matteofattore-nextage/django-ajax-resp
  license: Apache 2
  summary: 'A lightweight ajax framework for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
