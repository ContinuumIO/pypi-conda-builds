package:
  name: searchpy
  version: "1.4.9"

source:
  fn: Searchpy-1.4.9.tar.gz
  url: https://pypi.python.org/packages/source/S/Searchpy/Searchpy-1.4.9.tar.gz
  md5: eca18b474a78ea903ec70797d0182ce0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - searchpy = searchpy:main
    #
    # Would create an entry point called searchpy that calls searchpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mechanize >=0.2.4
    - pyquery >=0.6.1

  run:
    - python
    - mechanize >=0.2.4
    - pyquery >=0.6.1

test:
  # Python imports
  imports:
    - searchpy
    - searchpy.adapters
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ixmatus/searchpy
  license: Python Software Foundation License
  summary: 'A google interface library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
