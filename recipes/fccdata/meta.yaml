package:
  name: fccdata
  version: "1.0.0"

source:
  fn: fccdata-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fccdata/fccdata-1.0.0.tar.gz
  md5: d56ce560e10dc5ec16acae93669f137b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fccdata = fccdata:main
    #
    # Would create an entry point called fccdata that calls fccdata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example_project
    - fccdata
    - fccdata.management
    - fccdata.management.commands
    - fccdata.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamfast/fccdata
  license: BSD License
  summary: 'Models and load scripts used to import FCC data (priarmily focused on amateur radio) into Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
