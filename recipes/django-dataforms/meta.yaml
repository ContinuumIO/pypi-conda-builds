package:
  name: django-dataforms
  version: "0.2.0"

source:
  fn: django-dataforms-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dataforms/django-dataforms-0.2.0.tar.gz
  md5: 05b7a8a827d6166ac04188e5c2e6ffa3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dataforms = django-dataforms:main
    #
    # Would create an entry point called django-dataforms that calls django-dataforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dataforms
    - dataforms.admin
    - dataforms.migrations
    - dataforms.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/django-dataforms/django-dataforms
  license: GNU General Public License (GPL)
  summary: 'Dynamic, database-driven Django forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
