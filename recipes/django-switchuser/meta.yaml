package:
  name: django-switchuser
  version: "0.3.3"

source:
  fn: django-switchuser-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-switchuser/django-switchuser-0.3.3.tar.gz
  md5: 1b85e94d66fa04195aa959a85669da12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-switchuser = django-switchuser:main
    #
    # Would create an entry point called django-switchuser that calls django-switchuser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_switchuser
    - django_switchuser.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wolever/django-switchuser
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
