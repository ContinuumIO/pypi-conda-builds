package:
  name: products.simpleuserfolder
  version: "2.0.0"

source:
  fn: Products.SimpleUserFolder-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SimpleUserFolder/Products.SimpleUserFolder-2.0.0.tar.gz
  md5: 62f7b47775be080ff0c79e74c372a78e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.simpleuserfolder = products.simpleuserfolder:main
    #
    # Would create an entry point called products.simpleuserfolder that calls products.simpleuserfolder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope2

  run:
    - python
    - zope2

test:
  # Python imports
  imports:
    - Products
    - Products.SimpleUserFolder
    - Products.SimpleUserFolder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplistix.co.uk/software/zope/simpleuserfolder
  license: MIT License
  summary: 'A scriptable, subclassable, fully documented and tested user folder implementation for Zope 2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
