package:
  name: infinity
  version: "1.3"

source:
  fn: infinity-1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/infinity/infinity-1.3.tar.gz
  md5: 0aa8feadec140b7d091bac142411e6d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infinity = infinity:main
    #
    # Would create an entry point called infinity that calls infinity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest >=2.2.3
    - pygments >=1.2
    - six >=1.4.1

  run:
    - python
    - pytest >=2.2.3
    - pygments >=1.2
    - six >=1.4.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/infinity
  license: BSD License
  summary: 'All-in-one infinity value for Python. Can be compared to any object.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
