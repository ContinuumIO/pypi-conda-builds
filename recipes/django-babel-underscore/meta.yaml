package:
  name: django-babel-underscore
  version: "0.3.0"

source:
  fn: django-babel-underscore-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-babel-underscore/django-babel-underscore-0.3.0.tar.gz
  md5: 0ad21fc6d39a59968ad7a20659a11081
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-babel-underscore = django-babel-underscore:main
    #
    # Would create an entry point called django-babel-underscore that calls django-babel-underscore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.8
    - babel >=1.3
    - django-babel >=0.3.9
    - markey >=0.4

  run:
    - python
    - setuptools
    - django >=1.4,<1.8
    - babel >=1.3
    - django-babel >=0.3.9
    - markey >=0.4

test:
  # Python imports
  imports:
    - django_babel_underscore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EnTeQuAk/django-babel-underscore
  license: BSD License
  summary: 'Implements a underscore extractor for django-babel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
