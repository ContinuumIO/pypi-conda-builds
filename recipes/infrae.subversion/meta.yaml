package:
  name: infrae.subversion
  version: "1.4.5"

source:
  fn: infrae.subversion-1.4.5.tar.gz
  url: https://pypi.python.org/packages/source/i/infrae.subversion/infrae.subversion-1.4.5.tar.gz
  md5: c69d2d695e9ec94bcc87fa9e9cf35b2e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infrae.subversion = infrae.subversion:main
    #
    # Would create an entry point called infrae.subversion that calls infrae.subversion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - py

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - py

test:
  # Python imports
  imports:
    - infrae
    - infrae.subversion
    - infrae.subversion.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.infrae.com/buildout/infrae.subversion/trunk/
  license: Zope Public License
  summary: 'Buildout recipe for checking out from subversion'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
