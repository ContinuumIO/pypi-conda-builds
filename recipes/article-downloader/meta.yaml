package:
  name: article-downloader
  version: "0.1"

source:
  fn: article-downloader-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/article-downloader/article-downloader-0.1.tar.gz
  md5: d39d219ed9df9cb76a1c70abaaa9be4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - article-downloader = article-downloader:main
    #
    # Would create an entry point called article-downloader that calls article-downloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - article-downloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eddotman/article-downloader
  license: UNKNOWN
  summary: 'A class for downloading scientific journal articles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
