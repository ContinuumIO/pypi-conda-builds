package:
  name: cartouche
  version: "1.1.2"

source:
  fn: cartouche-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cartouche/cartouche-1.1.2.tar.gz
  md5: ca1e35c286d6803510336eb4ae888285
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cartouche = cartouche:main
    #
    # Would create an entry point called cartouche that calls cartouche.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6

  run:
    - python
    - sphinx >=0.6

test:
  # Python imports
  imports:
    - cartouche

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rob-smallshire/cartouche
  license: BSD License
  summary: 'Sphinx "cartouche" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
