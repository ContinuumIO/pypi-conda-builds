package:
  name: django-postcodepy-proxy
  version: "0.0.2"

source:
  fn: django-postcodepy-proxy-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-postcodepy-proxy/django-postcodepy-proxy-0.0.2.tar.gz
  md5: c4fa095bc98a2c5b89cb2522c22296c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-postcodepy-proxy = django-postcodepy-proxy:main
    #
    # Would create an entry point called django-postcodepy-proxy that calls django-postcodepy-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.3
    - postcodepy >=0.0.4

  run:
    - python
    - requests >=2.4.3
    - postcodepy >=0.0.4

test:
  # Python imports
  imports:
    - postcodepy_proxy
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hootnot/django-postcodepy-proxy
  license: MIT
  summary: 'simple Django app to make your backend serve as a proxy for postcode.nl REST-API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
