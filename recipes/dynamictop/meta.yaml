package:
  name: dynamictop
  version: "0.1.0"

source:
  fn: dynamictop-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamictop/dynamictop-0.1.0.tar.gz
  md5: e38c10c635a0b43694a209c778f7c2c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamictop = dynamictop:main
    #
    # Would create an entry point called dynamictop that calls dynamictop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.11

  run:
    - python
    - pyyaml ==3.11

test:
  # Python imports
  imports:
    - dynamictop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/virtru/dynamictop
  license: MIT License
  summary: 'Tools for creating a dynamic top file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
