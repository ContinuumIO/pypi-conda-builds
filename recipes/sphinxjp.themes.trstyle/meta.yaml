package:
  name: sphinxjp.themes.trstyle
  version: "0.1.1"

source:
  fn: sphinxjp.themes.trstyle-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.themes.trstyle/sphinxjp.themes.trstyle-0.1.1.tar.gz
  md5: d34fa76a8d3928276825aa79f8d314f3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.themes.trstyle = sphinxjp.themes.trstyle:main
    #
    # Would create an entry point called sphinxjp.themes.trstyle that calls sphinxjp.themes.trstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - docutils
    - sphinx
    - sphinxjp.themecore

  run:
    - python
    - setuptools
    - setuptools
    - docutils
    - sphinx
    - sphinxjp.themecore

test:
  # Python imports
  imports:
    - sphinxjp
    - sphinxjp.themes
    - sphinxjp.themes.trstyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kironono/sphinxjp.themes.trstyle
  license: MIT License
  summary: 'A sphinx theme for TriAx Corp style Documentation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
