package:
  name: django-desktop-notifications
  version: "0.3"

source:
  fn: django-desktop-notifications-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-desktop-notifications/django-desktop-notifications-0.3.tar.gz
  md5: 4822378dbc08774b4a2b4f10ac364a61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-desktop-notifications = django-desktop-notifications:main
    #
    # Would create an entry point called django-desktop-notifications that calls django-desktop-notifications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - demo
    - desktopnotifications

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-desktop-notifications/
  license: UNKNOWN
  summary: 'Django / NodeJS / Chrome / websocket ultra fast desktop notifications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
