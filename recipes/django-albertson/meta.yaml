package:
  name: django-albertson
  version: "0.1.1"

source:
  fn: django-albertson-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-albertson/django-albertson-0.1.1.tar.gz
  md5: 77d438d356ef56b5a4d3d687cbf725dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-albertson = django-albertson:main
    #
    # Would create an entry point called django-albertson that calls django-albertson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - albertson
    - django

  run:
    - python
    - albertson
    - django

test:
  # Python imports
  imports:
    - dj_albertson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FocusLab/django-albertson
  license: BSD License
  summary: 'Integration library for Django and Albertson.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
