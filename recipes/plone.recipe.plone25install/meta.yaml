package:
  name: plone.recipe.plone25install
  version: "0.2"

source:
  fn: plone.recipe.plone25install-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.plone25install/plone.recipe.plone25install-0.2.tar.gz
  md5: 09abb78fadcefb05b2fad46bbe39587c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.plone25install = plone.recipe.plone25install:main
    #
    # Would create an entry point called plone.recipe.plone25install that calls plone.recipe.plone25install.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.plone25install

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/plone.recipe.plone25install/trunk
  license: Zope Public License
  summary: 'ZC Buildout recipe for installing Plone 2.5.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
