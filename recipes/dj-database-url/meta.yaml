package:
  name: dj-database-url
  version: "0.3.0"

source:
  fn: dj-database-url-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dj-database-url/dj-database-url-0.3.0.tar.gz
  md5: 28f89d403b565ee62dc25582852ae241
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj-database-url = dj-database-url:main
    #
    # Would create an entry point called dj-database-url that calls dj-database-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennethreitz/dj-database-url
  license: BSD License
  summary: 'Use Database URLs in your Django Application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
