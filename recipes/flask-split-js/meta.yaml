package:
  name: flask-split-js
  version: "0.2.1"

source:
  fn: Flask-Split-JS-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Split-JS/Flask-Split-JS-0.2.1.tar.gz
  md5: 8a0cd946a68effb38dff5e066bbe9fa3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-split-js = flask-split-js:main
    #
    # Would create an entry point called flask-split-js that calls flask-split-js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - flask-split >=0.2

  run:
    - python
    - flask >=0.8
    - flask-split >=0.2

test:
  # Python imports
  imports:
    - flask_split_js

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/FelixLoether/flask-split-js
  license: MIT License
  summary: 'A JavaScript library for Flask-Split.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
