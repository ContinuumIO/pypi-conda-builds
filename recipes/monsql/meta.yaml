package:
  name: monsql
  version: "0.2.0"

source:
  fn: monsql-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/monsql/monsql-0.2.0.tar.gz
  md5: 24eef539bfa153664b726483384a9f67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monsql = monsql:main
    #
    # Would create an entry point called monsql that calls monsql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mysql-python

  run:
    - python
    - mysql-python

test:
  # Python imports
  imports:
    - monsql
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/firstprayer/monsql.git
  license: UNKNOWN
  summary: 'MonSQL - Lightweight and use-friendly wrapper for multiple relational databases. Currently supporting MySQL, SQLite3, PostgreSQL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
