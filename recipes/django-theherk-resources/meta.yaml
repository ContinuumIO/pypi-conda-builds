package:
  name: django-theherk-resources
  version: "1.6.1"

source:
  fn: django-theherk-resources-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-theherk-resources/django-theherk-resources-1.6.1.tar.gz
  md5: 22cb62a802439d3cc0f1d0da894befb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-theherk-resources = django-theherk-resources:main
    #
    # Would create an entry point called django-theherk-resources that calls django-theherk-resources.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-localflavor

  run:
    - python
    - django-localflavor

test:
  # Python imports
  imports:
    - resources
    - resources.admin
    - resources.cms_plugins
    - resources.migrations
    - resources.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theherk/django-theherk-resources
  license: see file LICENSE
  summary: 'Django app for keeping data on organizations and people.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
