package:
  name: flask_yamlpage
  version: "0.0.6"

source:
  fn: flask_yamlpage-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_yamlpage/flask_yamlpage-0.0.6.tar.gz
  md5: 1b57b477218916eeccea169493143d15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_yamlpage = flask_yamlpage:main
    #
    # Would create an entry point called flask_yamlpage that calls flask_yamlpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - yamlpage
    - misaka

  run:
    - python
    - flask
    - yamlpage
    - misaka

test:
  # Python imports
  imports:
    - flask_yamlpage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imbolc/flask-yamlpage
  license: ISC License (ISCL)
  summary: 'Flatpages in yaml syntax'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
