package:
  name: django-facebook-api
  version: "0.5.4"

source:
  fn: django-facebook-api-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-api/django-facebook-api-0.5.4.tar.gz
  md5: 47e49d80fb06fd15b4b18c70d696a29c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-api = django-facebook-api:main
    #
    # Would create an entry point called django-facebook-api that calls django-facebook-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-annoying
    - django-oauth-tokens >=0.5.1
    - facebook-sdk >=1.0.0a0
    - python-dateutil >=1.5

  run:
    - python
    - django
    - django-annoying
    - django-oauth-tokens >=0.5.1
    - facebook-sdk >=1.0.0a0
    - python-dateutil >=1.5

test:
  # Python imports
  imports:
    - facebook_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-facebook-api
  license: BSD License
  summary: 'Django implementation for Facebook Graph API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
