package:
  name: django-boreas
  version: "0.1"

source:
  fn: django-boreas-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-boreas/django-boreas-0.1.tar.gz
  md5: 4b38b5d34e84e7ac19b369addfc859db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-boreas = django-boreas:main
    #
    # Would create an entry point called django-boreas that calls django-boreas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - boreas >=0.1

  run:
    - python
    - django >=1.5
    - boreas >=0.1

test:
  # Python imports
  imports:
    - djboreas
    - djboreas.management
    - djboreas.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django.boreas.readthedocs.org/
  license: JSON License
  summary: 'Django management commands for boreas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
