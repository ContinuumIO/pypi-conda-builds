package:
  name: flask-oidc
  version: "0.1.2"

source:
  fn: flask-oidc-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-oidc/flask-oidc-0.1.2.tar.gz
  md5: 12307535e8cd3ec17a745d068ce6b3ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-oidc = flask-oidc:main
    #
    # Would create an entry point called flask-oidc that calls flask-oidc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - itsdangerous
    - oauth2client
    - six

  run:
    - python
    - flask
    - itsdangerous
    - oauth2client
    - six

test:
  # Python imports
  imports:
    - flask_oidc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SteelPangolin/flask-oidc
  license: BSD License
  summary: 'OpenID Connect extension for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
