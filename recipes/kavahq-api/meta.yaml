package:
  name: kavahq-api
  version: "0.2.11"

source:
  fn: kavahq-api-0.2.11.tar.gz
  url: https://pypi.python.org/packages/source/k/kavahq-api/kavahq-api-0.2.11.tar.gz
  md5: 15421c1f21f3c51c6ad955342e91a4ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kavahq-api = kavahq-api:main
    #
    # Would create an entry point called kavahq-api that calls kavahq-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - kavahq
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/42cc/apiclient-kava
  license: GNU General Public License v2 (GPLv2)
  summary: 'wrapper over kavahq.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
