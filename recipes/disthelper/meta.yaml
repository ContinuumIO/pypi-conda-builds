package:
  name: disthelper
  version: "0.9.2"

source:
  fn: disthelper-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/disthelper/disthelper-0.9.2.tar.gz
  md5: ef3d219a1804e6675fd54e6d27ac4f06
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - disthelper = disthelper:main
    #
    # Would create an entry point called disthelper that calls disthelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pastescript

  run:
    - python
    - setuptools
    - pastescript

test:
  # Python imports
  imports:
    - disthelper
    - disthelper.cmd
    - disthelper.templates
    - disthelper.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/disthelper/
  license: New BSD License
  summary: 'helps you manage a Python distribution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
