package:
  name: codeskel
  version: "2.1.1"

source:
  fn: CodeSkel-2.1.1.zip
  url: https://pypi.python.org/packages/source/C/CodeSkel/CodeSkel-2.1.1.zip
  md5: 08739e09014fcd76b0471236ca0e9fc5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codeskel = codeskel:main
    #
    # Would create an entry point called codeskel that calls codeskel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zopeskel <=2.99
    - zopeskel.dexterity

  run:
    - python
    - setuptools
    - zopeskel <=2.99
    - zopeskel.dexterity

test:
  # Python imports
  imports:
    - codeskel
    - codeskel.localcommands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codesyntax/CodeSkel
  license: MIT License
  summary: 'A collection of skeletons for quickstarting CS projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
