package:
  name: jrpc-utils
  version: "0.2.1"

source:
  fn: JRPC-utils-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/J/JRPC-utils/JRPC-utils-0.2.1.tar.gz
  md5: fbe8be8e5f0150e6a020f0bb9da72ee9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jrpc-utils = jrpc-utils:main
    #
    # Would create an entry point called jrpc-utils that calls jrpc-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kaygorodov/jrpc-utils
  license: Python Software Foundation License
  summary: 'A set of useful classes and functions for working with JSON-RPC v.2.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
