package:
  name: collective.generic.skel
  version: "0.1.0"

source:
  fn: collective.generic.skel-0.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.generic.skel/collective.generic.skel-0.1.0.zip
  md5: 71fd9135e4260b5a549bdedf927811b3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.generic.skel = collective.generic.skel:main
    #
    # Would create an entry point called collective.generic.skel that calls collective.generic.skel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah
    - minitage.paste

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah
    - minitage.paste

test:
  # Python imports
  imports:
    - collective
    - collective.generic
    - collective.generic.skel
    - collective.generic.skel.common
    - collective.generic.skel.policy
    - collective.generic.skel.skin
    - collective.generic.skel.testing
    - collective.generic.skel.tma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://makina-corpus.com
  license: GPL
  summary: 'PasteScript templates for collective.generic suite sponsorised by Makina Corpus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
