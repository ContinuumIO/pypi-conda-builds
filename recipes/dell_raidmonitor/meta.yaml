package:
  name: dell_raidmonitor
  version: "0.0.1"

source:
  fn: Dell_RaidMonitor-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Dell_RaidMonitor/Dell_RaidMonitor-0.0.1.tar.gz
  md5: 38b9fa6139b1202bba6a64759f77cae4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dell_raidmonitor = dell_raidmonitor:main
    #
    # Would create an entry point called dell_raidmonitor that calls dell_raidmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dell_raidmonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.51cto.com
  license: BSD
  summary: 'Monitor Dell Raid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
