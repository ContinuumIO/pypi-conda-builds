package:
  name: raptus.person
  version: "1.0"

source:
  fn: raptus.person-1.0.zip
  url: https://pypi.python.org/packages/source/r/raptus.person/raptus.person-1.0.zip
  md5: b3eaa7444d793b2bb3f5f69a2f3cb2f6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.person = raptus.person:main
    #
    # Would create an entry point called raptus.person that calls raptus.person.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - raptus
    - raptus.person
    - raptus.person.browser
    - raptus.person.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Provides contenttype person'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
