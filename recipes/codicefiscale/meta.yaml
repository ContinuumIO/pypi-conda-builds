package:
  name: codicefiscale
  version: "0.8"

source:
  fn: codicefiscale-0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/codicefiscale/codicefiscale-0.8.tar.gz
  md5: e406656e7a7573e4e12afd892db31ff1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codicefiscale = codicefiscale:main
    #
    # Would create an entry point called codicefiscale that calls codicefiscale.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - codicefiscale

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ema/pycodicefiscale
  license: LGPL
  summary: 'Python library for Italian fiscal code (codicefiscale)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
