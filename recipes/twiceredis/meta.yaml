package:
  name: twiceredis
  version: "1.0.0"

source:
  fn: twiceredis-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/t/twiceredis/twiceredis-1.0.0.tar.gz
  md5: 421450e05ad1a3ac8d2f357735de2029
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - twiceredis = twiceredis:main
    #
    # Would create an entry point called twiceredis that calls twiceredis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - twiceredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tr3buchet/twiceredis
  license: Apache Software License
  summary: 'sentinel pool backed read and write redis client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
