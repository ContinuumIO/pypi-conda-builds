package:
  name: kss.plugin.cns
  version: "1.1"

source:
  fn: kss.plugin.cns-1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kss.plugin.cns/kss.plugin.cns-1.1.tar.gz
  md5: 3a59a40784177517872e13330130ed23
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kss.plugin.cns = kss.plugin.cns:main
    #
    # Would create an entry point called kss.plugin.cns that calls kss.plugin.cns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - kss.core

  run:
    - python
    - setuptools
    - setuptools
    - kss.core

test:
  # Python imports
  imports:
    - kss
    - kss.plugin
    - kss.plugin.cns
    - kss.plugin.cns.commands
    - kss.plugin.cns.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/kss.plugin.cns
  license: GPL
  summary: 'Various KSS plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
