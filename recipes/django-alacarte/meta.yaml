package:
  name: django-alacarte
  version: "0.1.5"

source:
  fn: django-alacarte-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-alacarte/django-alacarte-0.1.5.tar.gz
  md5: 13e1dbb98203dfe6c0dea52714367dbd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-alacarte = django-alacarte:main
    #
    # Would create an entry point called django-alacarte that calls django-alacarte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - alacarte
    - alacarte.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mstrcnvs/django-alacarte
  license: UNKNOWN
  summary: 'A minimalist Django menu app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
