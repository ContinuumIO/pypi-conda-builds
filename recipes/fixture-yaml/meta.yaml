package:
  name: fixture-yaml
  version: "0.1.2"

source:
  fn: fixture-yaml-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fixture-yaml/fixture-yaml-0.1.2.tar.gz
  md5: 2a176020541b7dde280b8ad13d3954f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fixture-yaml = fixture-yaml:main
    #
    # Would create an entry point called fixture-yaml that calls fixture-yaml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - fixture

  run:
    - python
    - pyyaml
    - fixture

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/w31rd0/fixture-yaml
  license: Public Domain
  summary: 'fixture-yaml is an extension that adds YAML support to fixture library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
