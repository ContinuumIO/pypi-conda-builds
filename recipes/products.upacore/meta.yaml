package:
  name: products.upacore
  version: "0.5.1.6"

source:
  fn: Products.upaCore-0.5.1.6.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.upaCore/Products.upaCore-0.5.1.6.tar.gz
  md5: 2e4b2886077b6be04f3c77a2910b511f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.upacore = products.upacore:main
    #
    # Would create an entry point called products.upacore that calls products.upacore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - archetypes.schemaextender
    - products.trustedexecutables

  run:
    - python
    - setuptools
    - archetypes.schemaextender
    - products.trustedexecutables

test:
  # Python imports
  imports:
    - Products
    - Products.upaCore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.upaCore
  license: GPL
  summary: 'Member info implementation for the GermanUPA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
