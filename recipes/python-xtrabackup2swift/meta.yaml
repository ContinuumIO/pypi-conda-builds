package:
  name: python-xtrabackup2swift
  version: "0.0.6"

source:
  fn: python-xtrabackup2swift-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/p/python-xtrabackup2swift/python-xtrabackup2swift-0.0.6.tar.gz
  md5: 8026e015266663e4fd80f7dbc96119f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-xtrabackup2swift = python-xtrabackup2swift:main
    #
    # Would create an entry point called python-xtrabackup2swift that calls python-xtrabackup2swift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto
    - python-swiftclient
    - python-keystoneclient

  run:
    - python
    - pycrypto
    - python-swiftclient
    - python-keystoneclient

test:
  # Python imports
  imports:
    - xtrabackup2swift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/moniker-dns/python-xtrabackup2swift
  license: Apache Software License
  summary: 'xtrabackup wrappers to store & retrieve from swift'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
