package:
  name: sqlalchemy-sluggable
  version: "0.1.1"

source:
  fn: SQLAlchemy-Sluggable-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Sluggable/SQLAlchemy-Sluggable-0.1.1.tar.gz
  md5: 3fca7c7bd2f4c9cb9c1f5143ac94278a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-sluggable = sqlalchemy-sluggable:main
    #
    # Would create an entry point called sqlalchemy-sluggable that calls sqlalchemy-sluggable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - inflection ==0.1.2
    - sqlalchemy ==0.7.8

  run:
    - python
    - inflection ==0.1.2
    - sqlalchemy ==0.7.8

test:
  # Python imports
  imports:
    - sqlalchemy_sluggable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/sqlalchemy-sluggable
  license: BSD License
  summary: 'Configurable slugs to SQLAlchemy models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
