package:
  name: django-insertblocks
  version: "0.1.0"

source:
  fn: django-insertblocks-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-insertblocks/django-insertblocks-0.1.0.tar.gz
  md5: 1a39671bb640070560c16cb4069cf257
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-insertblocks = django-insertblocks:main
    #
    # Would create an entry point called django-insertblocks that calls django-insertblocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - insertblocks
    - insertblocks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-insertblocks
  license: UNKNOWN
  summary: 'Django Insert Blocks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
