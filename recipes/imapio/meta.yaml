package:
  name: imapio
  version: "0.9.5"

source:
  fn: imapIO-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/i/imapIO/imapIO-0.9.5.tar.gz
  md5: 7f2fff01b4b7bffda0c78402ac71bb9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imapio = imapio:main
    #
    # Would create an entry point called imapio that calls imapio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chardet

  run:
    - python
    - chardet

test:
  # Python imports
  imports:
    - imapIO
    - imapio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/invisibleroads/imapIO
  license: MIT License
  summary: 'Convenience classes and methods for processing IMAP mailboxes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
