package:
  name: django-choice-object
  version: "0.6"

source:
  fn: django-choice-object-0.6.zip
  url: https://pypi.python.org/packages/source/d/django-choice-object/django-choice-object-0.6.zip
  md5: 39daf00eeac2fd429085c6451d07d7e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-choice-object = django-choice-object:main
    #
    # Would create an entry point called django-choice-object that calls django-choice-object.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - django_choice_object

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orf/django-choice-object
  license: UNKNOWN
  summary: 'A choice object for Django forms and models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
