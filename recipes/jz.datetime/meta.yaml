package:
  name: jz.datetime
  version: "0.1"

source:
  fn: jz.datetime-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jz.datetime/jz.datetime-0.1.tar.gz
  md5: 32a51b641b64fbd4a4e1cf66b7503c74
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jz.datetime = jz.datetime:main
    #
    # Would create an entry point called jz.datetime that calls jz.datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - jz
    - jz.datetime
    - jz.datetime.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jellofishi.com/zope
  license: GPL
  summary: 'Date and time helpers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
