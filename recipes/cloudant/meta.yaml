package:
  name: cloudant
  version: "0.5.9"

source:
  fn: cloudant-0.5.9.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudant/cloudant-0.5.9.tar.gz
  md5: fc838e558fbbc65c48ce0cf5e9dfed89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudant = cloudant:main
    #
    # Would create an entry point called cloudant that calls cloudant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-futures ==0.9.4

  run:
    - python
    - requests-futures ==0.9.4

test:
  # Python imports
  imports:
    - cloudant

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudant-labs/cloudant
  license: MIT License
  summary: 'Asynchronous Cloudant / CouchDB Interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
