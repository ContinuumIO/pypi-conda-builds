package:
  name: megrok.menu
  version: "0.4"

source:
  fn: megrok.menu-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.menu/megrok.menu-0.4.tar.gz
  md5: 3103f1c44edfbc70e7378981f6eb2baf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.menu = megrok.menu:main
    #
    # Would create an entry point called megrok.menu that calls megrok.menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - grokcore.component
    - grokcore.security
    - grokcore.view
    - grokcore.viewlet
    - martian
    - setuptools
    - zope.browsermenu
    - zope.configuration
    - zope.publisher

  run:
    - python
    - grokcore.component
    - grokcore.security
    - grokcore.view
    - grokcore.viewlet
    - martian
    - setuptools
    - zope.browsermenu
    - zope.configuration
    - zope.publisher

test:
  # Python imports
  imports:
    - megrok
    - megrok.menu
    - megrok.menu.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/megrok.menu
  license: Zope Public License
  summary: 'Grok extension to configure browser menus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
