package:
  name: docker-links-python
  version: "0.1.0"

source:
  fn: docker-links-python-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-links-python/docker-links-python-0.1.0.tar.gz
  md5: 9c26c4c0bfcf5db7547fbac74127d858
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-links-python = docker-links-python:main
    #
    # Would create an entry point called docker-links-python that calls docker-links-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JoakimSoderberg/docker-links-python
  license: MIT License
  summary: 'A helper for parsing Docker link environment variables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
