package:
  name: ipernity_api
  version: "0.13"

source:
  fn: ipernity_api-0.13.tar.gz
  url: https://pypi.python.org/packages/source/i/ipernity_api/ipernity_api-0.13.tar.gz
  md5: c71d18fb17a8c00b1e9760bc0e291eb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipernity_api = ipernity_api:main
    #
    # Would create an entry point called ipernity_api that calls ipernity_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ipernity_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oneyoung/python-ipernity-api
  license: Apache Software License
  summary: 'Python Ipernity API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
