package:
  name: flask-split
  version: "0.2.0"

source:
  fn: Flask-Split-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Split/Flask-Split-0.2.0.tar.gz
  md5: 1af63237672f0185a3d0797d0e1233a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-split = flask-split:main
    #
    # Would create an entry point called flask-split that calls flask-split.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7
    - redis >=2.4.13

  run:
    - python
    - flask >=0.7
    - redis >=2.4.13

test:
  # Python imports
  imports:
    - flask_split

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jpvanhal/flask-split
  license: BSD License
  summary: 'A/B testing for your Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
