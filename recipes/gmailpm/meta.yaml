package:
  name: gmailpm
  version: "0.0.3"

source:
  fn: gmailpm-0.0.3.zip
  url: https://pypi.python.org/packages/source/g/gmailpm/gmailpm-0.0.3.zip
  md5: b9dab78f6481a5037d5b40346053589e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gmailpm = gmailpm:main
    #
    # Would create an entry point called gmailpm that calls gmailpm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gmailpm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://homepage.univie.ac.at/wolfgang.lechner/gmailpm.html
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Project Monitoring based on GMail'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
