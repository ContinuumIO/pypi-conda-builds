package:
  name: metargs
  version: "0.1"

source:
  fn: metargs-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/metargs/metargs-0.1.tar.gz
  md5: ee7c68a3da8f0c4df95a640f3d3d2065
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metargs = metargs:main
    #
    # Would create an entry point called metargs that calls metargs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse

  run:
    - python
    - argparse

test:
  # Python imports
  imports:
    - metargs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Library that supports configuration using a combination of config files and command line arguments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
