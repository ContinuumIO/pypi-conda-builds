package:
  name: cp.recipe.cmd
  version: "0.5"

source:
  fn: cp.recipe.cmd-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cp.recipe.cmd/cp.recipe.cmd-0.5.tar.gz
  md5: b8047b5ad1da8d996685fd4bcc55dab2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cp.recipe.cmd = cp.recipe.cmd:main
    #
    # Would create an entry point called cp.recipe.cmd that calls cp.recipe.cmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zope.testing

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zope.testing

test:
  # Python imports
  imports:
    - cp
    - cp.recipe
    - cp.recipe.cmd
    - cp.recipe.cmd.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/csawyer/cp.recipe.cmd/overview
  license: GPL
  summary: "ZC Buildout recipe to execute commands in it's own shell"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
