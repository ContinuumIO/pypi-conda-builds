package:
  name: ucscgenome
  version: "0.1"

source:
  fn: ucscgenome-0.1.zip
  url: https://pypi.python.org/packages/source/u/ucscgenome/ucscgenome-0.1.zip
  md5: ad17b7f11399837f927c7265b5a55c06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ucscgenome = ucscgenome:main
    #
    # Would create an entry point called ucscgenome that calls ucscgenome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twobitreader

  run:
    - python
    - twobitreader

test:
  # Python imports
  imports:
    - ucscgenome

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/konstantint/ucscgenome
  license: MIT License
  summary: 'Simple access to the reference genomes at UCSC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
