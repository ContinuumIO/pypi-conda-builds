package:
  name: redis-shard
  version: "0.3.0"

source:
  fn: redis-shard-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-shard/redis-shard-0.3.0.tar.gz
  md5: 62a2e673aa9b7cfc9661691da8234587
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-shard = redis-shard:main
    #
    # Would create an entry point called redis-shard that calls redis-shard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.10.3

  run:
    - python
    - redis >=2.10.3

test:
  # Python imports
  imports:
    - redis_shard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zhihu/redis-shard
  license: BSD License
  summary: 'Redis Sharding API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
