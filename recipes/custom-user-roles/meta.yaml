package:
  name: custom-user-roles
  version: "1.0.0"

source:
  fn: custom-user-roles-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/custom-user-roles/custom-user-roles-1.0.0.tar.gz
  md5: 0d911b0a770e8255cec5152c46b89826
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - custom-user-roles = custom-user-roles:main
    #
    # Would create an entry point called custom-user-roles that calls custom-user-roles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - milkman

  run:
    - python
    - django
    - milkman

test:
  # Python imports
  imports:
    - userroles
    - userroles.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/laginha/django-user-roles
  license: MIT License
  summary: 'Simple role-based user permissions for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
