package:
  name: django-booking
  version: "0.7.1"

source:
  fn: django-booking-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-booking/django-booking-0.7.1.tar.gz
  md5: ca94b23e1520bb46213e29acbef21a77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-booking = django-booking:main
    #
    # Would create an entry point called django-booking that calls django-booking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.2,<1.7
    - django-hvad
    - django-libs >=1.35
    - django-international ==0.0.6

  run:
    - python
    - django >=1.5.2,<1.7
    - django-hvad
    - django-libs >=1.35
    - django-international ==0.0.6

test:
  # Python imports
  imports:
    - booking
    - booking.migrations
    - booking.tests
    - booking.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-booking
  license: The MIT License
  summary: 'A reusable Django app that manages bookings for various purposes.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
