package:
  name: zc.sentrywsgi
  version: "1.1.0"

source:
  fn: zc.sentrywsgi-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.sentrywsgi/zc.sentrywsgi-1.1.0.tar.gz
  md5: 10407b56588648e2d55498bd6e336365
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.sentrywsgi = zc.sentrywsgi:main
    #
    # Would create an entry point called zc.sentrywsgi that calls zc.sentrywsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - raven >=5.1.0
    - setuptools

  run:
    - python
    - setuptools
    - raven >=5.1.0
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.sentrywsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Sentry configuration middleware.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
