package:
  name: django-bootstrap-span
  version: "0.0.3"

source:
  fn: django-bootstrap-span-0.0.3.zip
  url: https://pypi.python.org/packages/source/d/django-bootstrap-span/django-bootstrap-span-0.0.3.zip
  md5: a6abd0d1bf65ee0492848be83e005b08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-span = django-bootstrap-span:main
    #
    # Would create an entry point called django-bootstrap-span that calls django-bootstrap-span.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - django
    - django-bootstrap

  run:
    - python
    - six
    - django
    - django-bootstrap

test:
  # Python imports
  imports:
    - bootstrap_span
    - bootstrap_span.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stumc/django-bootstrap-span
  license: BSD License
  summary: 'Django bootstrap span adds class=spanN support in forms input elements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
