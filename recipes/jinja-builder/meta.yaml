package:
  name: jinja-builder
  version: "0.1a"

source:
  fn: jinja-builder-0.1a.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja-builder/jinja-builder-0.1a.tar.gz
  md5: e10c77164f61393fc95d11ad91a83a3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja-builder = jinja-builder:main
    #
    # Would create an entry point called jinja-builder that calls jinja-builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jinja_builder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/StefanZimmermann/jinja-builder
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'a simple jinja2 based JinjaBuilder class for scons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
