package:
  name: django-keyed-urls
  version: "0.4.1"

source:
  fn: django-keyed-urls-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-keyed-urls/django-keyed-urls-0.4.1.tar.gz
  md5: 31694a5c6e05db4181bb1b6fc26da330
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-keyed-urls = django-keyed-urls:main
    #
    # Would create an entry point called django-keyed-urls that calls django-keyed-urls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - django-modeltranslation

  run:
    - python
    - django >=1.4.2
    - django-modeltranslation

test:
  # Python imports
  imports:
    - keyed_urls
    - keyed_urls.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/matthiask/django-keyed-urls/
  license: BSD License
  summary: "So simple you'll burst into tears right away."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
