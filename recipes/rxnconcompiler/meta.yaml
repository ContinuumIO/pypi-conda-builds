package:
  name: rxnconcompiler
  version: "1.1.0"

source:
  fn: rxnconcompiler-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/rxnconcompiler/rxnconcompiler-1.1.0.tar.gz
  md5: 1dec93c3313e6345f93dbbd985e8184c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rxnconcompiler = rxnconcompiler:main
    #
    # Would create an entry point called rxnconcompiler that calls rxnconcompiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlrd

  run:
    - python
    - xlrd

test:
  # Python imports
  imports:
    - rxnconcompiler
    - rxnconcompiler.biological_complex
    - rxnconcompiler.bngl
    - rxnconcompiler.contingency
    - rxnconcompiler.definitions
    - rxnconcompiler.molecule
    - rxnconcompiler.parser
    - rxnconcompiler.reaction
    - rxnconcompiler.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: None
  license: new BSD
  summary: 'Systems Biology iterative network building tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
