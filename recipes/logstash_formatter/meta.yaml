package:
  name: logstash_formatter
  version: "0.5.14"

source:
  fn: logstash_formatter-0.5.14.tar.gz
  url: https://pypi.python.org/packages/source/l/logstash_formatter/logstash_formatter-0.5.14.tar.gz
  md5: 3ec370f6eac93ebc7b9eed13f78eb7c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logstash_formatter = logstash_formatter:main
    #
    # Would create an entry point called logstash_formatter that calls logstash_formatter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logstash_formatter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/exoscale/python-logstash-formatter
  license: MIT License
  summary: 'JSON formatter meant for logstash'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
