package:
  name: hieropt
  version: "0.3"

source:
  fn: hieropt-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hieropt/hieropt-0.3.tar.gz
  md5: db891d63c83163834952fa9b10589674
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hieropt = hieropt:main
    #
    # Would create an entry point called hieropt that calls hieropt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hieropt
    - hieropt.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Simple, hierarchical configuration; able to read configuration\n    values from file, command line options, and environment variables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
