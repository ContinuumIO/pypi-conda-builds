package:
  name: django-ssl-auth
  version: "1.0"

source:
  fn: django-ssl-auth-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ssl-auth/django-ssl-auth-1.0.tar.gz
  md5: 25baa05cbeb890bf2858c9d3476a1f1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ssl-auth = django-ssl-auth:main
    #
    # Would create an entry point called django-ssl-auth that calls django-ssl-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_ssl_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kimvais/django-ssl-client-auth/
  license: UNKNOWN
  summary: 'Django SSL Client Authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
