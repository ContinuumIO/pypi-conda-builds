package:
  name: ebicspy
  version: "0.1"

source:
  fn: ebicsPy-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ebicsPy/ebicsPy-0.1.tar.gz
  md5: cb25c8e530ad175fe0081b9271ab0281
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ebicspy = ebicspy:main
    #
    # Would create an entry point called ebicspy that calls ebicspy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ebicspy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ebicsPy
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Client side library for the banking communication EBICS protocol'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
