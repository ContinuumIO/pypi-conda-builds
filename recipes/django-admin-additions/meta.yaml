package:
  name: django-admin-additions
  version: "1.0.2"

source:
  fn: django-admin-additions-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-additions/django-admin-additions-1.0.2.tar.gz
  md5: b78779f5be03237497e7672e0d49cbf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-additions = django-admin-additions:main
    #
    # Would create an entry point called django-admin-additions that calls django-admin-additions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - admin_additions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.schinckel.net/django-admin-additions
  license: BSD License
  summary: 'Admin additions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
