package:
  name: punkybrowster
  version: "0.3.1"

source:
  fn: PunkyBrowster-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/P/PunkyBrowster/PunkyBrowster-0.3.1.tar.gz
  md5: 2b0cd1ac7f6ef5c4e0320012d49ba7dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - punkybrowster = punkybrowster:main
    #
    # Would create an entry point called punkybrowster that calls punkybrowster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - punky
    - punkybrowster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/leapfrogdevelopment/punkybrowster/
  license: GNU General Public License (GPL)
  summary: 'Programmatic web browsing module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
