package:
  name: django-class-based-auth-views
  version: "0.3"

source:
  fn: django-class-based-auth-views-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-class-based-auth-views/django-class-based-auth-views-0.3.tar.gz
  md5: c7692ca6800508175927307b6755719e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-class-based-auth-views = django-class-based-auth-views:main
    #
    # Would create an entry point called django-class-based-auth-views that calls django-class-based-auth-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - class_based_auth_views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stefanfoulis/django-class-based-auth-views
  license: BSD License
  summary: 'A reimplementation of django.contrib.auth.views as class based views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
