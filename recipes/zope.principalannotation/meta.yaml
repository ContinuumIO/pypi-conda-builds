package:
  name: zope.principalannotation
  version: "4.1.0"

source:
  fn: zope.principalannotation-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.principalannotation/zope.principalannotation-4.1.0.tar.gz
  md5: e3ec0ce562d4d6b27dec2ddf36c9d7cf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.principalannotation = zope.principalannotation:main
    #
    # Would create an entry point called zope.principalannotation that calls zope.principalannotation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - persistent
    - setuptools
    - zope.annotation
    - zope.component
    - zope.interface
    - zope.location
    - zope.security
    - zope.site

  run:
    - python
    - persistent
    - setuptools
    - zope.annotation
    - zope.component
    - zope.interface
    - zope.location
    - zope.security
    - zope.site

test:
  # Python imports
  imports:
    - zope
    - zope.principalannotation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.principalannotation
  license: Zope Public License
  summary: 'Annotations for Zope Principals'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
