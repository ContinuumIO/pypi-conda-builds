package:
  name: django-insert-above
  version: "1.0.4"

source:
  fn: django-insert-above-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-insert-above/django-insert-above-1.0.4.tar.gz
  md5: 771387b402db5c5218b7350bdb8f02eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-insert-above = django-insert-above:main
    #
    # Would create an entry point called django-insert-above that calls django-insert-above.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - insert_above
    - insert_above.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yunmanger1/django-insert-above/
  license: WTFPL
  summary: 'These django templatetags is a hack making possible to insert "content" in some (maybe above the current or parent template) places.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
