package:
  name: python-breadcrumbs
  version: "0.1.2"

source:
  fn: python-breadcrumbs-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-breadcrumbs/python-breadcrumbs-0.1.2.tar.gz
  md5: c4334f90d134fb9dfe2e595a960c44c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-breadcrumbs = python-breadcrumbs:main
    #
    # Would create an entry point called python-breadcrumbs that calls python-breadcrumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaiyou/python-breadcrumbs
  license: GPLv3
  summary: 'Store breadcrumbs for later accessing an object internals'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
