package:
  name: cherryontop
  version: "0.0.5"

source:
  fn: CherryOnTop-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/C/CherryOnTop/CherryOnTop-0.0.5.tar.gz
  md5: a71a41333514b9bb01dd5e8a7720a53b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherryontop = cherryontop:main
    #
    # Would create an entry point called cherryontop that calls cherryontop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy
    - routes
    - ujson

  run:
    - python
    - cherrypy
    - routes
    - ujson

test:
  # Python imports
  imports:
    - cherryontop
    - cherryontop.cache
    - cherryontop.decorators
    - cherryontop.errors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/csira/cherryontop
  license: BSD License
  summary: 'Helper abstractions and utilities for building JSON APIs with CherryPy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
