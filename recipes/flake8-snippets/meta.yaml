package:
  name: flake8-snippets
  version: "0.1"

source:
  fn: flake8-snippets-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-snippets/flake8-snippets-0.1.tar.gz
  md5: c1724b6d6442f8c91f92bad33741d687
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-snippets = flake8-snippets:main
    #
    # Would create an entry point called flake8-snippets that calls flake8-snippets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8

  run:
    - python
    - setuptools
    - flake8

test:
  # Python imports
  imports:
    - flake8_snippets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/flake8-snippets
  license: The MIT License
  summary: "A flake8 extension to find any code snippets you don't like"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
