package:
  name: vecnet
  version: "1.0.0"

source:
  fn: vecnet-1.0.0.zip
  url: https://pypi.python.org/packages/source/v/vecnet/vecnet-1.0.0.zip
  md5: 1daa798c6a12109161e6b33225783631
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vecnet = vecnet:main
    #
    # Would create an entry point called vecnet that calls vecnet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vecnet.openmalaria
    - vecnet.emod

  run:
    - python
    - vecnet.openmalaria
    - vecnet.emod

test:
  # Python imports
  imports:
    - vecnet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vecnet/vecnet.openmalaria
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Virtual package for installing all packages in vecnet namespace'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
