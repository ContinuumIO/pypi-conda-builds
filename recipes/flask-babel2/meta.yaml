package:
  name: flask-babel2
  version: "0.1"

source:
  fn: Flask-Babel2-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Babel2/Flask-Babel2-0.1.tar.gz
  md5: 9934a48f573db3ea1086d66188d5bdb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-babel2 = flask-babel2:main
    #
    # Would create an entry point called flask-babel2 that calls flask-babel2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - babel
    - pytz
    - speaklater >=1.2
    - jinja2 >=2.5

  run:
    - python
    - flask
    - babel
    - pytz
    - speaklater >=1.2
    - jinja2 >=2.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lepture/flask-babel
  license: BSD License
  summary: 'Adds i18n/l10n support to Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
