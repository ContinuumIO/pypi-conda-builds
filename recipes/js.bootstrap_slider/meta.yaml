package:
  name: js.bootstrap_slider
  version: "4.8.1"

source:
  fn: js.bootstrap_slider-4.8.1.zip
  url: https://pypi.python.org/packages/source/j/js.bootstrap_slider/js.bootstrap_slider-4.8.1.zip
  md5: 32f70d89502bbd703d9bc192fb5a01fd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.bootstrap_slider = js.bootstrap_slider:main
    #
    # Would create an entry point called js.bootstrap_slider that calls js.bootstrap_slider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.bootstrap_slider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of bootstrap-slider'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
