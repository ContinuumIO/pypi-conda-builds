package:
  name: agx.generator.buildout
  version: "1.0a1"

source:
  fn: agx.generator.buildout-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/a/agx.generator.buildout/agx.generator.buildout-1.0a1.tar.gz
  md5: 9ff0e56700e8730a4c592b05d244e0f1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agx.generator.buildout = agx.generator.buildout:main
    #
    # Would create an entry point called agx.generator.buildout that calls agx.generator.buildout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - agx
    - agx.generator
    - agx.generator.buildout
    - agx.generator.buildout.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bluedynamics/agx.generator.buildout
  license: GNU General Public License (GPL)
  summary: 'AGX generator for buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
