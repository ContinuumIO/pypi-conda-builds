package:
  name: clustershell
  version: "1.6"

source:
  fn: clustershell-1.6.tar.gz
  url: https://pypi.python.org/packages/source/C/ClusterShell/clustershell-1.6.tar.gz
  md5: 84a832352c713623117ac175ea715d0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clustershell = clustershell:main
    #
    # Would create an entry point called clustershell that calls clustershell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ClusterShell
    - ClusterShell.CLI
    - ClusterShell.Engine
    - ClusterShell.Worker
    - clustershell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://clustershell.sourceforge.net/
  license: CeCILL-C (French equivalent to LGPLv2+)
  summary: 'ClusterShell library and tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
