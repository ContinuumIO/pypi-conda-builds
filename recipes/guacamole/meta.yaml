package:
  name: guacamole
  version: "0.8"

source:
  fn: guacamole-0.8.tar.gz
  url: https://pypi.python.org/packages/source/g/guacamole/guacamole-0.8.tar.gz
  md5: 70ec93b049a5a8f76d676254e2647e94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guacamole = guacamole:main
    #
    # Would create an entry point called guacamole that calls guacamole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - guacamole
    - guacamole.ingredients
    - guacamole.recipes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zyga/guacamole
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Guacamole is an command line tool library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
