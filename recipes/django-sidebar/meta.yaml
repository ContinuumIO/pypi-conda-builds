package:
  name: django-sidebar
  version: "0.2.0"

source:
  fn: django-sidebar-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sidebar/django-sidebar-0.2.0.tar.gz
  md5: 9d8de314c8fd5396cd4c87bafe4346d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sidebar = django-sidebar:main
    #
    # Would create an entry point called django-sidebar that calls django-sidebar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sidebar
    - sidebar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ekaputra07/django-sidebar
  license: BSD License
  summary: 'Dynamic Sidebar creation for Django. Easily create and manage sidebar from Django Admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
