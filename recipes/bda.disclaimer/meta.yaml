package:
  name: bda.disclaimer
  version: "1.1"

source:
  fn: bda.disclaimer-1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.disclaimer/bda.disclaimer-1.1.tar.gz
  md5: 5c4723c99b43a950e778d94fcedb5cd2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.disclaimer = bda.disclaimer:main
    #
    # Would create an entry point called bda.disclaimer that calls bda.disclaimer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - bda
    - bda.disclaimer
    - bda.disclaimer.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bda.disclaimer
  license: GPL
  summary: 'Cookie based access restriction for Plone sites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
