package:
  name: bam2fpkc
  version: "0.1.0"

source:
  fn: bam2fpkc-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bam2fpkc/bam2fpkc-0.1.0.tar.gz
  md5: 8af33a093636f016257619a778973736
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bam2fpkc = bam2fpkc:main
    #
    # Would create an entry point called bam2fpkc that calls bam2fpkc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pysam >=0.6
    - numpy >=1.6.1

  run:
    - python
    - pysam >=0.6
    - numpy >=1.6.1

test:
  # Python imports
  imports:
    - bam2fpkc
    - bam2fpkc.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bam2fpkc/
  license: LICENSE.txt
  summary: 'bam2fpkc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
