package:
  name: zconfig_watchedfile
  version: "1.0"

source:
  fn: zconfig_watchedfile-1.0.zip
  url: https://pypi.python.org/packages/source/z/zconfig_watchedfile/zconfig_watchedfile-1.0.zip
  md5: afc87dd43dbd1357fe9aebb54493f95b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zconfig_watchedfile = zconfig_watchedfile:main
    #
    # Would create an entry point called zconfig_watchedfile that calls zconfig_watchedfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zconfig
    - setuptools

  run:
    - python
    - zconfig
    - setuptools

test:
  # Python imports
  imports:
    - zconfig_watchedfile
    - zconfig_watchedfile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/zconfig_watchedfile/
  license: Zope Public License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
