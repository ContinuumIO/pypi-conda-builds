package:
  name: feed2twitter
  version: "0.6.6"

source:
  fn: feed2twitter-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/f/feed2twitter/feed2twitter-0.6.6.tar.gz
  md5: 26394298caef351fd1d8921fe8358cab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feed2twitter = feed2twitter:main
    #
    # Would create an entry point called feed2twitter that calls feed2twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - docs
    - feed2twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.assembla.com/spaces/feed2twitter
  license: AGPL
  summary: 'Publish your feed items to twitter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
