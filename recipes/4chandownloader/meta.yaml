package:
  name: 4chandownloader
  version: "0.3"

source:
  fn: 4chandownloader-0.3.tar.gz
  url: https://pypi.python.org/packages/source/4/4chandownloader/4chandownloader-0.3.tar.gz
  md5: 427a155947b7fdf5481c52ee9e29889b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - 4chandownloader = 4chandownloader:main
    #
    # Would create an entry point called 4chandownloader that calls 4chandownloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==0.14.0
    - docopt ==0.5.0

  run:
    - python
    - requests ==0.14.0
    - docopt ==0.5.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Socketubs/4chandownloader
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: '4chan thread downloader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
