package:
  name: bookofnova
  version: "0.007"

source:
  fn: bookofnova-0.007.tar.gz
  url: https://pypi.python.org/packages/source/b/bookofnova/bookofnova-0.007.tar.gz
  md5: a52e968e61a13eb308577f187779fb36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bookofnova = bookofnova:main
    #
    # Would create an entry point called bookofnova that calls bookofnova.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bookofnova

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudnull/bookofnova, https://pypi.python.org/pypi/bookofnova
  license: Apache Software License
  summary: 'Nova Compute Library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
