package:
  name: django-fairepart
  version: "0.2.4"

source:
  fn: django-fairepart-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fairepart/django-fairepart-0.2.4.tar.gz
  md5: ef1cb08a9bf03c1344bade1d74c8f2c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fairepart = django-fairepart:main
    #
    # Would create an entry point called django-fairepart that calls django-fairepart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - facepy
    - python-social-auth
    - gdata

  run:
    - python
    - facepy
    - python-social-auth
    - gdata

test:
  # Python imports
  imports:
    - fairepart
    - fairepart.backends
    - fairepart.migrations
    - fairepart.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-fairepart
  license: MIT License
  summary: 'A generic application to invite your contact from facebook, google, etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
