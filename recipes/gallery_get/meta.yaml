package:
  name: gallery_get
  version: "1.5.0"

source:
  fn: gallery_get-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gallery_get/gallery_get-1.5.0.tar.gz
  md5: 833f32221164788117ada233e72f0bfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gallery_get = gallery_get:main
    #
    # Would create an entry point called gallery_get that calls gallery_get.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gallery_get
    - gallery_plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/regosen/gallery_get
  license: MIT License
  summary: 'Gallery downloader - supports many galleries and reddit histories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
