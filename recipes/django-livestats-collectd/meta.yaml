package:
  name: django-livestats-collectd
  version: "0.0.2"

source:
  fn: django-livestats-collectd-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livestats-collectd/django-livestats-collectd-0.0.2.tar.gz
  md5: fc5963276c279499c07cb8f7c2f93853
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livestats-collectd = django-livestats-collectd:main
    #
    # Would create an entry point called django-livestats-collectd that calls django-livestats-collectd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-livestats

  run:
    - python
    - django-livestats

test:
  # Python imports
  imports:
    - livestats_collectd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A collectd import module for django-livestats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
