package:
  name: billogram_api
  version: "1.2"

source:
  fn: billogram_api-1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/billogram_api/billogram_api-1.2.tar.gz
  md5: 95e2340a1438d9f091e71d0f1bac9ac4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - billogram_api = billogram_api:main
    #
    # Would create an entry point called billogram_api that calls billogram_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - billogram_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://billogram.com/api/documentation
  license: MIT
  summary: 'Library for connecting to the Billogram v2 API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
