package:
  name: sphinxcontrib-smblink
  version: "0.2"

source:
  fn: sphinxcontrib-smblink-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-smblink/sphinxcontrib-smblink-0.2.tar.gz
  md5: 99a0002879d0308e93ec329ff14cd84d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-smblink = sphinxcontrib-smblink:main
    #
    # Would create an entry point called sphinxcontrib-smblink that calls sphinxcontrib-smblink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6

  run:
    - python
    - sphinx >=0.6

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goingmywaynet/sphinxcontrib-smblink.git
  license: BSD License
  summary: 'Sphinx Windows Share Links (WSL) role extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
