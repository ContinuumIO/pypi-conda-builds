package:
  name: products.ptprofiler
  version: "1.4"

source:
  fn: Products.PTProfiler-1.4.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PTProfiler/Products.PTProfiler-1.4.tar.gz
  md5: 60ef9b5f05903b67925b0431e12d5f67
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.ptprofiler = products.ptprofiler:main
    #
    # Would create an entry point called products.ptprofiler that calls products.ptprofiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PTProfiler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://infrae.com/download/ptprofiler
  license: BSD License
  summary: 'PageTemplate profiler for Zope 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
