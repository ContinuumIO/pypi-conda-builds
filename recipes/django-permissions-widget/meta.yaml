package:
  name: django-permissions-widget
  version: "1.4.0"

source:
  fn: django-permissions-widget-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-permissions-widget/django-permissions-widget-1.4.0.tar.gz
  md5: 2e1e1eb31ea9dddde3fa43839eff77e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-permissions-widget = django-permissions-widget:main
    #
    # Would create an entry point called django-permissions-widget that calls django-permissions-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - permissions_widget
    - permissions_widget.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-permissions-widget.rtfd.org
  license: MIT License
  summary: 'Fresh Django permissions widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
