package:
  name: django-admintools-bootstrap
  version: "0.0.1a1"

source:
  fn: django-admintools-bootstrap-0.0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admintools-bootstrap/django-admintools-bootstrap-0.0.1a1.tar.gz
  md5: 2407c1c6853abd5e60588c8df848322f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admintools-bootstrap = django-admintools-bootstrap:main
    #
    # Would create an entry point called django-admintools-bootstrap that calls django-admintools-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-admin-tools >=0.4.1
    - beautifulsoup >=3.2.0
    - django-appconf >=0.4.1
    - versiontools >=1.8.2

  run:
    - python
    - django-admin-tools >=0.4.1
    - beautifulsoup >=3.2.0
    - django-appconf >=0.4.1
    - versiontools >=1.8.2

test:
  # Python imports
  imports:
    - admintools_bootstrap
    - admintools_bootstrap.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/salvator/django-admintools-bootstrap
  license: MIT
  summary: 'Bootstrap theme for django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
