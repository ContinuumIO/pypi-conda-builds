package:
  name: collective.jazzport
  version: "1.0.0"

source:
  fn: collective.jazzport-1.0.0.zip
  url: https://pypi.python.org/packages/source/c/collective.jazzport/collective.jazzport-1.0.0.zip
  md5: b8e1280747da35bcf8fde38f6f7586ce
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jazzport = collective.jazzport:main
    #
    # Would create an entry point called collective.jazzport that calls collective.jazzport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.cmfplone
    - futures
    - plone.app.registry
    - plone.app.vocabularies
    - plone.registry
    - requests
    - zope.component
    - zope.interface
    - zope.i18nmessageid
    - zope.schema

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.cmfplone
    - futures
    - plone.app.registry
    - plone.app.vocabularies
    - plone.registry
    - requests
    - zope.component
    - zope.interface
    - zope.i18nmessageid
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.jazzport
    - collective.jazzport.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datakurre/collective.jazzport/
  license: GPL
  summary: 'A yet another Zip exporter for Plone content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
