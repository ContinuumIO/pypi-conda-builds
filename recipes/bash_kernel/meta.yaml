package:
  name: bash_kernel
  version: "0.3"

source:
  fn: bash_kernel-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bash_kernel/bash_kernel-0.3.tar.gz
  md5: 2a2eb3f5cff814ddb0f9512c5c97d3a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bash_kernel = bash_kernel:main
    #
    # Would create an entry point called bash_kernel that calls bash_kernel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pexpect >=3.3

  run:
    - python
    - pexpect >=3.3

test:
  # Python imports
  imports:
    - bash_kernel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/takluyver/bash_kernel
  license: BSD License
  summary: 'A bash kernel for IPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
