package:
  name: micropython-uaiohttpclient
  version: "0.4"

source:
  fn: micropython-uaiohttpclient-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-uaiohttpclient/micropython-uaiohttpclient-0.4.tar.gz
  md5: 79e7ed1ba4cd105a85226aca56ddfd05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-uaiohttpclient = micropython-uaiohttpclient:main
    #
    # Would create an entry point called micropython-uaiohttpclient that calls micropython-uaiohttpclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - micropython-uasyncio

  run:
    - python
    - micropython-uasyncio

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pfalcon/uaiohttpclient
  license: MIT
  summary: "HTTP client for MicroPython's uasyncio,\nroughly compatible with aiohttp module.\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
