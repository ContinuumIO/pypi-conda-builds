package:
  name: google_drive_sync
  version: "0.1.0"

source:
  fn: google_drive_sync-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/google_drive_sync/google_drive_sync-0.1.0.tar.gz
  md5: 0bb65ebaafd4ca893b076c51bd485d5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google_drive_sync = google_drive_sync:main
    #
    # Would create an entry point called google_drive_sync that calls google_drive_sync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google_drive_sync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxc0c0s/google_drive_sync
  license: BSD License
  summary: 'Sync The current folder and all its subfolders to Google Drive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
