package:
  name: colourdistance
  version: "1.2"

source:
  fn: colourdistance-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/colourdistance/colourdistance-1.2.tar.gz
  md5: ae96833a2db5e9f6c2d920c7936b6b80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colourdistance = colourdistance:main
    #
    # Would create an entry point called colourdistance that calls colourdistance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colourdistance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Colour distance routines based on http://www.compuphase.com/cmetric.htm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
