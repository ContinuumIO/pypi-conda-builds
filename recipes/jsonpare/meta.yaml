package:
  name: jsonpare
  version: "0.2.8"

source:
  fn: jsonpare-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonpare/jsonpare-0.2.8.tar.gz
  md5: fc0ce43c3528549fc99917edb336002a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonpare = jsonpare:main
    #
    # Would create an entry point called jsonpare that calls jsonpare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonpare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dsoprea/JsonPare
  license: GPL 2
  summary: 'A very simple utility to decode and unwind JSON into JSON from the command-line.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
