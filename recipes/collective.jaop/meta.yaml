package:
  name: collective.jaop
  version: "1.2"

source:
  fn: collective.jaop-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.jaop/collective.jaop-1.2.tar.gz
  md5: 44e87044733f84503934717ef8c640fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jaop = collective.jaop:main
    #
    # Would create an entry point called collective.jaop that calls collective.jaop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.jaop
    - collective.jaop.browser
    - collective.jaop.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/jaop-just-an-opensearch-product-1/
  license: GPL
  summary: 'This product implements OpenSearch interface in a PloneSite.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
