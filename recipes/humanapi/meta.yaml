package:
  name: humanapi
  version: "0.1.10"

source:
  fn: humanapi-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/h/humanapi/humanapi-0.1.10.tar.gz
  md5: 6490df860ade1c842d0196e833207654
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - humanapi = humanapi:main
    #
    # Would create an entry point called humanapi that calls humanapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=0.13.2
    - docopt ==0.4.0

  run:
    - python
    - requests >=0.13.2
    - docopt ==0.4.0

test:
  # Python imports
  imports:
    - humanapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/humanapi/humanapi-python-client/
  license: MIT
  summary: 'A CLI client and Python API library for the HumanAPI health data platform.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
