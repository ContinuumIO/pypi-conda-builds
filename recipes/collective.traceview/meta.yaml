package:
  name: collective.traceview
  version: "1.3"

source:
  fn: collective.traceview-1.3.zip
  url: https://pypi.python.org/packages/source/c/collective.traceview/collective.traceview-1.3.zip
  md5: 37c353c01092a964b77dc6884f561cac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.traceview = collective.traceview:main
    #
    # Would create an entry point called collective.traceview that calls collective.traceview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.traceview
    - collective.traceview.patches

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.traceview
  license: gpl
  summary: 'This package adds support for TraceView performance instrumentation in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
