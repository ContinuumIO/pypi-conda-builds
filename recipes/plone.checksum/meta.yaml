package:
  name: plone.checksum
  version: "0.1"

source:
  fn: plone.checksum-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.checksum/plone.checksum-0.1.tar.gz
  md5: 9a4a38144319bd824c40c335978ee0da
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.checksum = plone.checksum:main
    #
    # Would create an entry point called plone.checksum that calls plone.checksum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ruledispatch

  run:
    - python
    - setuptools
    - ruledispatch

test:
  # Python imports
  imports:
    - plone
    - plone.checksum
    - plone.checksum.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/plone.checksum
  license: GPL
  summary: 'Checksums for ZODB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
