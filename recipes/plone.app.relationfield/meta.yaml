package:
  name: plone.app.relationfield
  version: "1.3.1"

source:
  fn: plone.app.relationfield-1.3.1.zip
  url: https://pypi.python.org/packages/source/p/plone.app.relationfield/plone.app.relationfield-1.3.1.zip
  md5: e11036bcc907bad9687b6605ae5d2ea4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.relationfield = plone.app.relationfield:main
    #
    # Would create an entry point called plone.app.relationfield that calls plone.app.relationfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.schema
    - zope.intid
    - five.intid
    - plone.app.intid
    - z3c.form
    - z3c.relationfield >=0.4.2
    - z3c.formwidget.query
    - plone.autoform
    - plone.supermodel
    - plone.app.vocabularies
    - plone.schemaeditor >=1.3.5.dev0
    - products.cmfcore
    - plone.rfc822
    - plone.app.z3cform >=1.1.0.dev0

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.schema
    - zope.intid
    - five.intid
    - plone.app.intid
    - z3c.form
    - z3c.relationfield >=0.4.2
    - z3c.formwidget.query
    - plone.autoform
    - plone.supermodel
    - plone.app.vocabularies
    - plone.schemaeditor >=1.3.5.dev0
    - products.cmfcore
    - plone.rfc822
    - plone.app.z3cform >=1.1.0.dev0

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.relationfield
    - plone.app.relationfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/plone.app.relationfield
  license: GPL
  summary: 'Plone support for z3c.relationfield'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
