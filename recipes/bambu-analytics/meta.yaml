package:
  name: bambu-analytics
  version: "2.0.1"

source:
  fn: bambu-analytics-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-analytics/bambu-analytics-2.0.1.tar.gz
  md5: 515c2215245a457d627b005fd0485bca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-analytics = bambu-analytics:main
    #
    # Would create an entry point called bambu-analytics that calls bambu-analytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bambu_analytics
    - bambu_analytics.providers
    - bambu_analytics.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-analytics
  license: UNKNOWN
  summary: 'Provides a simple, pluggable system for analytics in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
