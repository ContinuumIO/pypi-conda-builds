package:
  name: django-dajax-ng
  version: "0.9.4"

source:
  fn: django-dajax-ng-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dajax-ng/django-dajax-ng-0.9.4.tar.gz
  md5: 8b53505c9e1f80ff57c0f65156fe7cb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dajax-ng = django-dajax-ng:main
    #
    # Would create an entry point called django-dajax-ng that calls django-dajax-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-dajaxice-ng >=0.5.6.4

  run:
    - python
    - django-dajaxice-ng >=0.5.6.4

test:
  # Python imports
  imports:
    - dajax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saz/django-dajax
  license: BSD License
  summary: 'Easy to use library to create asynchronous presentation logic with django and dajaxice-ng'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
