package:
  name: gitapi
  version: "1.1.0a2"

source:
  fn: gitapi-1.1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/g/gitapi/gitapi-1.1.0a2.tar.gz
  md5: ea45d1173bd4837041bc7ac32c570fcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitapi = gitapi:main
    #
    # Would create an entry point called gitapi that calls gitapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gitapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/haard/gitapi
  license: Do whatever you want, don't blame me
  summary: 'Python API to Git using the command-line interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
