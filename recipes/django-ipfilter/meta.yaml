package:
  name: django-ipfilter
  version: "0.1-dev"

source:
  fn: django-ipfilter-0.1-dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ipfilter/django-ipfilter-0.1-dev.tar.gz
  md5: f7fe38dd41aea8f9283a3d4dd80ab6c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ipfilter = django-ipfilter:main
    #
    # Would create an entry point called django-ipfilter that calls django-ipfilter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ipfilter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-ipfilter/
  license: BSD License
  summary: 'A django application to filter ip addresses in views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
