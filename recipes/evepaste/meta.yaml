package:
  name: evepaste
  version: "0.9"

source:
  fn: evepaste-0.9.tar.gz
  url: https://pypi.python.org/packages/source/e/evepaste/evepaste-0.9.tar.gz
  md5: b6b840afe056308b2c8bff9a77db8626
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evepaste = evepaste:main
    #
    # Would create an entry point called evepaste that calls evepaste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - evepaste
    - evepaste.parsers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'A Python library to help parse various things that are copy/pastable from the Eve Online UI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
