package:
  name: django-basicauth
  version: "0.1"

source:
  fn: django-basicauth-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-basicauth/django-basicauth-0.1.tar.gz
  md5: f599b3cf7c93f6f9ac5fad73a899fd73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-basicauth = django-basicauth:main
    #
    # Would create an entry point called django-basicauth that calls django-basicauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7,<1.9

  run:
    - python
    - django >=1.7,<1.9

test:
  # Python imports
  imports:
    - basicauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Basic auth utilities for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
