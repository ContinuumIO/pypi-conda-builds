package:
  name: vtc_scrypt
  version: "1.0"

source:
  fn: vtc_scrypt-1.0.tar.gz
  url: https://pypi.python.org/packages/source/v/vtc_scrypt/vtc_scrypt-1.0.tar.gz
  md5: ac51fb951f75b97d6f6963859f933035
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vtc_scrypt = vtc_scrypt:main
    #
    # Would create an entry point called vtc_scrypt that calls vtc_scrypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - vtc_scrypt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Bindings for scrypt-n proof of work used by Vertcoin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
