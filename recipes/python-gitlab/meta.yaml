package:
  name: python-gitlab
  version: "0.8"

source:
  fn: python-gitlab-0.8.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gitlab/python-gitlab-0.8.tar.gz
  md5: 00cf3f923a28edccd643d4fdb951e2d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gitlab = python-gitlab:main
    #
    # Would create an entry point called python-gitlab that calls python-gitlab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - six

  run:
    - python
    - requests
    - six

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gpocentek/python-gitlab
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Interact with GitLab API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
