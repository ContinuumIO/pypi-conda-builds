package:
  name: django-clever-pages
  version: "0.3.1"

source:
  fn: django-clever-pages-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clever-pages/django-clever-pages-0.3.1.tar.gz
  md5: 90ed7a8cf41e18f069d731f7ce9873de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clever-pages = django-clever-pages:main
    #
    # Would create an entry point called django-clever-pages that calls django-clever-pages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mptt >=0.6.0
    - django-meta >=0.0.3
    - feincms ==1.9.3

  run:
    - python
    - django-mptt >=0.6.0
    - django-meta >=0.0.3
    - feincms ==1.9.3

test:
  # Python imports
  imports:
    - page
    - page.migrations
    - page.templatetags
    - page.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/artofhuman/django-clever-pages
  license: UNKNOWN
  summary: 'App for create pages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
