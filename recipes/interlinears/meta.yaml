package:
  name: interlinears
  version: "0.2"

source:
  fn: Interlinears-0.2.tar.gz
  url: https://pypi.python.org/packages/source/I/Interlinears/Interlinears-0.2.tar.gz
  md5: d30c00bb473e94d9abd4611333fbdd83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - interlinears = interlinears:main
    #
    # Would create an entry point called interlinears that calls interlinears.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - interlinears

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleissin/Interlinears
  license: MIT License
  summary: 'Pretty-printing of linguistic interlinears'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
