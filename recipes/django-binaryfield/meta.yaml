package:
  name: django-binaryfield
  version: "0.4.0"

source:
  fn: django-binaryfield-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-binaryfield/django-binaryfield-0.4.0.tar.gz
  md5: 32a4487ac14cefdf3382ab59401134e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-binaryfield = django-binaryfield:main
    #
    # Would create an entry point called django-binaryfield that calls django-binaryfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - binaryfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/slafs/django-binaryfield/
  license: BSD License
  summary: 'a generic app to provide a way to handle database binary data in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
