package:
  name: linkheader
  version: "0.4.2"

source:
  fn: LinkHeader-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/L/LinkHeader/LinkHeader-0.4.2.tar.gz
  md5: e824e31baeec4b394ade8f67c681a9c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkheader = linkheader:main
    #
    # Would create an entry point called linkheader that calls linkheader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - linkheader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/asplake/link_header
  license: BSD License
  summary: 'Parse and format link headers according to RFC 5988 "Web Linking"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
