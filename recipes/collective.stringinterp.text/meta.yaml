package:
  name: collective.stringinterp.text
  version: "0.2"

source:
  fn: collective.stringinterp.text-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.stringinterp.text/collective.stringinterp.text-0.2.zip
  md5: ce6b02005c4ccbcb3f85a9f81952bb0e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.stringinterp.text = collective.stringinterp.text:main
    #
    # Would create an entry point called collective.stringinterp.text that calls collective.stringinterp.text.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.stringinterp

  run:
    - python
    - setuptools
    - setuptools
    - plone.stringinterp

test:
  # Python imports
  imports:
    - collective
    - collective.stringinterp
    - collective.stringinterp.text
    - collective.stringinterp.text.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.stringinterp.text
  license: GNU General Public License (GPL)
  summary: 'Additional string interpolation for Plone: add content text in your contentrule e-mail messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
