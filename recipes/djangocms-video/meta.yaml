package:
  name: djangocms-video
  version: "0.1"

source:
  fn: djangocms-video-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-video/djangocms-video-0.1.tar.gz
  md5: c6dbb3b52a7f42d7252d74fbc88493d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-video = djangocms-video:main
    #
    # Would create an entry point called djangocms-video that calls djangocms-video.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_video
    - djangocms_video.migrations
    - djangocms_video.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-video
  license: BSD License
  summary: 'Video plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
