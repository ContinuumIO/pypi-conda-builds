package:
  name: dacapo
  version: "0.1.9.h"

source:
  fn: dacapo-0.1.9.h.tar.gz
  url: https://pypi.python.org/packages/source/d/dacapo/dacapo-0.1.9.h.tar.gz
  md5: f6e1cbaede87dbfdd828cd84edbbdc38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dacapo = dacapo:main
    #
    # Would create an entry point called dacapo that calls dacapo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dacapo
    - dacapo.config
    - dacapo.data
    - dacapo.errorhandling
    - dacapo.metadata
    - dacapo.playlist
    - dacapo.qtflac2mp3
    - dacapo.ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dacapo.netztakt.de
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Lightweight Music Player with cover- and lyrics-display'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
