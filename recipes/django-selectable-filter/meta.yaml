package:
  name: django-selectable-filter
  version: "0.1.5"

source:
  fn: django-selectable-filter-0.1.5.zip
  url: https://pypi.python.org/packages/source/d/django-selectable-filter/django-selectable-filter-0.1.5.zip
  md5: 57a344fbbb02a8f770cce185483ecf8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-selectable-filter = django-selectable-filter:main
    #
    # Would create an entry point called django-selectable-filter that calls django-selectable-filter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-selectable

  run:
    - python
    - django
    - django-selectable

test:
  # Python imports
  imports:
    - selectable_filter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Gustavosdo/django-selectable-filter
  license: MIT License
  summary: 'The filterspec to the Django administration that allow you to filter records using the resources of django-selectable library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
