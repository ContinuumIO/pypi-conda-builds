package:
  name: django-metaimage
  version: "0.4"

source:
  fn: django-metaimage-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-metaimage/django-metaimage-0.4.tar.gz
  md5: 704d1347cafb2b8d98e8d9d3ea6d08a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-metaimage = django-metaimage:main
    #
    # Would create an entry point called django-metaimage that calls django-metaimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-autoslug
    - django-photologue
    - django-taggit
    - pil

  run:
    - python
    - setuptools
    - django
    - django-autoslug
    - django-photologue
    - django-taggit
    - pil

test:
  # Python imports
  imports:
    - metaimage
    - metaimage.templatetags
    - metaimage.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/limist/django-metaimage
  license: GNU General Public License (GPL)
  summary: 'Wrapper around django-photologue dealing w/ remote images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
