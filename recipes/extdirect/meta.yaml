package:
  name: extdirect
  version: "0.4"

source:
  fn: extdirect-0.4.tar.gz
  url: https://pypi.python.org/packages/source/e/extdirect/extdirect-0.4.tar.gz
  md5: c1d25b94c5f5d04a46f45ebcc642989f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extdirect = extdirect:main
    #
    # Would create an entry point called extdirect that calls extdirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - extdirect
    - extdirect.django
    - extdirect.django.templatetags
    - extdirect.zope

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/extdirect
  license: BSD
  summary: 'Python implementation of an Ext.Direct router'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
