package:
  name: win7ools
  version: "0.3.0"

source:
  fn: win7ools-0.3.0.zip
  url: https://pypi.python.org/packages/source/w/win7ools/win7ools-0.3.0.zip
  md5: ceab5766461d3129ee18b96aaa662a5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - win7ools = win7ools:main
    #
    # Would create an entry point called win7ools that calls win7ools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - crypto
    - reportlab

  run:
    - python
    - crypto
    - reportlab

test:
  # Python imports
  imports:
    - win7ools
    - win7ools.system

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nexuslevel/Win7ools
  license: Apache Software License
  summary: 'Python project that provides programmatic access to the Windows OS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
