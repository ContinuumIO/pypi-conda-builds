package:
  name: mechanoid
  version: "0.6.9"

source:
  fn: mechanoid-0.6.9.tar.gz
  url: https://pypi.python.org/packages/source/m/mechanoid/mechanoid-0.6.9.tar.gz
  md5: 57935bed2df6cd83c3f2268cf1557c6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mechanoid = mechanoid:main
    #
    # Would create an entry point called mechanoid that calls mechanoid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mechanoid
    - mechanoid.clientform
    - mechanoid.clientform.formparser
    - mechanoid.clientform.html_controls
    - mechanoid.clientform.html_controls.alist
    - mechanoid.cookiejar
    - mechanoid.http_processors
    - mechanoid.misc
    - mechanoid.pullparser
    - mechanoid.seek_wrapper
    - mechanoid.sites
    - mechanoid.useragent
    - mechanoid.useragent.http_handlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://python.org/pypi/mechanoid
  license: GNU General Public License (GPL)
  summary: 'Python Programmatic Web Browser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
