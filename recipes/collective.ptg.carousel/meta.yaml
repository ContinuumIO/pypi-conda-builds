package:
  name: collective.ptg.carousel
  version: "0.2.2"

source:
  fn: collective.ptg.carousel-0.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.carousel/collective.ptg.carousel-0.2.2.zip
  md5: 718e56c13e6f56135b62b41e51587c60
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.carousel = collective.ptg.carousel:main
    #
    # Would create an entry point called collective.ptg.carousel that calls collective.ptg.carousel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.carousel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.carousel
  license: GPL
  summary: 'carousel gallery type for plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
