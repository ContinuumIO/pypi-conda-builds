package:
  name: django-fs-trumbowyg
  version: "0.1.4"

source:
  fn: django-fs-trumbowyg-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fs-trumbowyg/django-fs-trumbowyg-0.1.4.tar.gz
  md5: 961a3f29901e884074d6a32b011125e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fs-trumbowyg = django-fs-trumbowyg:main
    #
    # Would create an entry point called django-fs-trumbowyg that calls django-fs-trumbowyg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - trumbowyg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fogstream/django-fs-trumbowyg
  license: The MIT License (MIT)
  summary: 'Trumbowyg (WYSIWYG editor) integration app for Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
