package:
  name: collectfast
  version: "0.2.3"

source:
  fn: Collectfast-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/C/Collectfast/Collectfast-0.2.3.tar.gz
  md5: 596a256534e7be8abf40bdf887704e87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collectfast = collectfast:main
    #
    # Would create an entry point called collectfast that calls collectfast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - python-dateutil >=2.1
    - pytz >=2014.2

  run:
    - python
    - django >=1.4
    - python-dateutil >=2.1
    - pytz >=2014.2

test:
  # Python imports
  imports:
    - collectfast
    - collectfast.management
    - collectfast.management.commands
    - collectfast.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/antonagestam/collectfast/
  license: Creative Commons Attribution-ShareAlike 3.0 Unported License
  summary: 'Collectstatic on Steroids'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
