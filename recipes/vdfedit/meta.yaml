package:
  name: vdfedit
  version: "1.0.0"

source:
  fn: vdfedit-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/v/vdfedit/vdfedit-1.0.0.tar.gz
  md5: 10f0206f90291dc0258513193a0c413b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vdfedit = vdfedit:main
    #
    # Would create an entry point called vdfedit that calls vdfedit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyvdf >=1.0.4

  run:
    - python
    - pyvdf >=1.0.4

test:
  # Python imports
  imports:
    - vdfedit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amreuland/vdfedit
  license: MIT License
  summary: 'Fast and Easy Python Valve Data File (VDF) Reader and Writer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
