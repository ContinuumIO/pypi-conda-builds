package:
  name: membug
  version: "0.1"

source:
  fn: membug-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/membug/membug-0.1.tar.gz
  md5: 1f037b1d826b8ba80eb680da3834a695
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - membug = membug:main
    #
    # Would create an entry point called membug that calls membug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - membug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yandex-sysmon/membug
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Small set of memory debugging tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
