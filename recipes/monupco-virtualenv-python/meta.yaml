package:
  name: monupco-virtualenv-python
  version: "0.4"

source:
  fn: monupco-virtualenv-python-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/monupco-virtualenv-python/monupco-virtualenv-python-0.4.tar.gz
  md5: c2db13c832cb43b2b23f15d99231e30f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monupco-virtualenv-python = monupco-virtualenv-python:main
    #
    # Would create an entry point called monupco-virtualenv-python that calls monupco-virtualenv-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - monupco_virtualenv_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/monupco/monupco-virtualenv-python
  license: MIT License
  summary: 'monupco.com registration agent for stand-alone Python virtualenv applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
