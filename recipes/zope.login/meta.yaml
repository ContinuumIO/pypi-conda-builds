package:
  name: zope.login
  version: "2.0.0"

source:
  fn: zope.login-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.login/zope.login-2.0.0.tar.gz
  md5: 0b239ea287250ee36be8226d07826ea5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.login = zope.login:main
    #
    # Would create an entry point called zope.login that calls zope.login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.authentication
    - zope.component
    - zope.interface
    - zope.publisher >=4.0.0

  run:
    - python
    - setuptools
    - zope.authentication
    - zope.component
    - zope.interface
    - zope.publisher >=4.0.0

test:
  # Python imports
  imports:
    - zope
    - zope.login
    - zope.login.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.login
  license: Zope Public License
  summary: 'Login helpers for zope.publisher / authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
