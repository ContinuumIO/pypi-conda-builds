package:
  name: django-lawnchair
  version: "0.1.2"

source:
  fn: django-lawnchair-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lawnchair/django-lawnchair-0.1.2.tar.gz
  md5: 1f67ecf9517068b5df14475e15f37a9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lawnchair = django-lawnchair:main
    #
    # Would create an entry point called django-lawnchair that calls django-lawnchair.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lawnchair

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/andymckay/django-lawnchair
  license: UNKNOWN
  summary: 'Libraries to make working with Django and Lawnchair easier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
