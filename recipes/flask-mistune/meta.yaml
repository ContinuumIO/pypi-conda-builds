package:
  name: flask-mistune
  version: "0.1.1"

source:
  fn: Flask-Mistune-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Mistune/Flask-Mistune-0.1.1.tar.gz
  md5: ddfe3d5d943695b4584ea475d03f31b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mistune = flask-mistune:main
    #
    # Would create an entry point called flask-mistune that calls flask-mistune.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7
    - mistune

  run:
    - python
    - flask >=0.7
    - mistune

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ellisonleao/flask-mistune/
  license: MIT License
  summary: u'A interface between the Flask web framework and the Mistune Markdown parser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
