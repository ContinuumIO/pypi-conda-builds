package:
  name: torcast
  version: "0.1.1.4"

source:
  fn: TorCast-0.1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/T/TorCast/TorCast-0.1.1.4.tar.gz
  md5: 9fb21f96ca71c215e996aa4cd808dae4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torcast = torcast:main
    #
    # Would create an entry point called torcast that calls torcast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=2.4.1

  run:
    - python
    - tornado >=2.4.1

test:
  # Python imports
  imports:
    - TorCast
    - torcast

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ipconfiger/TorCast
  license: LGPL
  summary: 'Broadcast messages to all tornado process subcribed on Redis asynchronously Or Block on Redis Queue asynchronously'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
