package:
  name: django-cloudslave
  version: "0.2.2"

source:
  fn: django-cloudslave-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cloudslave/django-cloudslave-0.2.2.tar.gz
  md5: bb5bbb11f5d67f81bdcee0ed9f8f7677
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cloudslave = django-cloudslave:main
    #
    # Would create an entry point called django-cloudslave that calls django-cloudslave.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - python-novaclient
    - south

  run:
    - python
    - django
    - python-novaclient
    - south

test:
  # Python imports
  imports:
    - cloudslave
    - cloudslave.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sorenh/python-django-cloudslave
  license: Apache Software License
  summary: 'Create, use and destroy temporary slaves in the cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
