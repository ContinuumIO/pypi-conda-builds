package:
  name: bm_common
  version: "0.0.1b"

source:
  fn: bm_common-0.0.1b.tar.gz
  url: https://pypi.python.org/packages/source/b/bm_common/bm_common-0.0.1b.tar.gz
  md5: a7f6af25a812cb97c2d86a74bfa47fa4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bm_common = bm_common:main
    #
    # Would create an entry point called bm_common that calls bm_common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bm_common
    - bm_common.maths

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'This is shared code between projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
