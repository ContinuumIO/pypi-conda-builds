package:
  name: jsontv
  version: "0.1.1"

source:
  fn: jsonTV-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonTV/jsonTV-0.1.1.tar.gz
  md5: 572a6936604bf578b555c5c70a524b4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsontv = jsontv:main
    #
    # Would create an entry point called jsontv that calls jsontv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - jsontv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/jsonTV/
  license: APACHE 2.0
  summary: 'A client for the Schedules Direct JSON API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
