package:
  name: flask-spyne
  version: "0.2"

source:
  fn: Flask-Spyne-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Spyne/Flask-Spyne-0.2.tar.gz
  md5: 9690e0e51d2b011c04954c9c95a26679
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-spyne = flask-spyne:main
    #
    # Would create an entry point called flask-spyne that calls flask-spyne.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - spyne
    - sec-wall
    - pyyaml

  run:
    - python
    - flask
    - spyne
    - sec-wall
    - pyyaml

test:
  # Python imports
  imports:
    - flask_spyne

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rayrapetyan/flask-spyne
  license: BSD License
  summary: 'A Flask extension, provides support for Spyne.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
