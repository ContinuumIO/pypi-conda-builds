package:
  name: django-merged-inlines
  version: "0.2"

source:
  fn: django-merged-inlines-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-merged-inlines/django-merged-inlines-0.2.tar.gz
  md5: ce53fd3de069717a0397b32aaddd6bb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-merged-inlines = django-merged-inlines:main
    #
    # Would create an entry point called django-merged-inlines that calls django-merged-inlines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - merged_inlines

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MattBroach/Django-Merged-Inlines
  license: MIT License
  summary: 'A Django Admin extension that allows you to mix and reorder multiple inline classes together'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
