package:
  name: cmsplugin-twcarousel
  version: "0.1c0"

source:
  fn: cmsplugin-twcarousel-0.1c0.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-twcarousel/cmsplugin-twcarousel-0.1c0.tar.gz
  md5: 133ff10101b56f5140a6d3a38d203003
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-twcarousel = cmsplugin-twcarousel:main
    #
    # Would create an entry point called cmsplugin-twcarousel that calls cmsplugin-twcarousel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - twcarousel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevwilde/cmsplugin-twcarousel
  license: BSD License
  summary: 'A Twitter Bootstrap Carousel plugin for Django CMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
