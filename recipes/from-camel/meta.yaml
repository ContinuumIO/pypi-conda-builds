package:
  name: from-camel
  version: "0.2.0"

source:
  fn: from-camel-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/f/from-camel/from-camel-0.2.0.tar.gz
  md5: 69b7a27095ea8a3a1dda236fc9f9742b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - from-camel = from-camel:main
    #
    # Would create an entry point called from-camel that calls from-camel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lars512/from-camel/
  license: BSD
  summary: 'A script to convert python files from camel case variable naming to underscore variable naming.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
