package:
  name: cubicweb-i18ncontent
  version: "0.1.2"

source:
  fn: cubicweb-i18ncontent-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-i18ncontent/cubicweb-i18ncontent-0.1.2.tar.gz
  md5: 2f6002b8f0ba40edea28fdc7b20f85ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-i18ncontent = cubicweb-i18ncontent:main
    #
    # Would create an entry point called cubicweb-i18ncontent that calls cubicweb-i18ncontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.10.0

  run:
    - python
    - cubicweb >=3.10.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-i18ncontent
  license: LGPL
  summary: 'internationalized content system for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
