package:
  name: mekk.xmind
  version: "0.5.0"

source:
  fn: mekk.xmind-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mekk.xmind/mekk.xmind-0.5.0.tar.gz
  md5: a0afc14b0adafb8cde09c632d6483e70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mekk.xmind = mekk.xmind:main
    #
    # Would create an entry point called mekk.xmind that calls mekk.xmind.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.1.1

  run:
    - python
    - lxml >=2.1.1

test:
  # Python imports
  imports:
    - mekk
    - mekk.xmind

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/Mekk/mekk.xmind/
  license: Mozilla Public License 1.1 (MPL 1.1) or Artistic License
  summary: 'XMind data files reading and writing library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
