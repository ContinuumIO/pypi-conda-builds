package:
  name: amigocloud
  version: "1.0.8"

source:
  fn: amigocloud-1.0.8.tar.gz
  url: https://pypi.python.org/packages/source/a/amigocloud/amigocloud-1.0.8.tar.gz
  md5: 328ac96c6c2f6305d50906511d62de19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amigocloud = amigocloud:main
    #
    # Would create an entry point called amigocloud that calls amigocloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.6.0
    - socketio-client ==0.5.3

  run:
    - python
    - requests ==2.6.0
    - socketio-client ==0.5.3

test:
  # Python imports
  imports:
    - amigocloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amigocloud/python-amigocloud
  license: MIT
  summary: 'Python client for the AmigoCloud REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
