package:
  name: doctest-ignore-unicode
  version: "0.1.2"

source:
  fn: doctest-ignore-unicode-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/doctest-ignore-unicode/doctest-ignore-unicode-0.1.2.tar.gz
  md5: ba64fe1ef48e8ed68d9ac9a9a03d0c65
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doctest-ignore-unicode = doctest-ignore-unicode:main
    #
    # Would create an entry point called doctest-ignore-unicode that calls doctest-ignore-unicode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - setuptools
    - nose

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gnublade/doctest-ignore-unicode
  license: Apache Software License
  summary: 'Add flag to ignore unicode literal prefixes in doctests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
