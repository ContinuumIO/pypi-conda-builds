package:
  name: pythoninterfaces
  version: "0.0.2"

source:
  fn: PythonInterfaces-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/PythonInterfaces/PythonInterfaces-0.0.2.tar.gz
  md5: 56b994acd3e07907471d64829ab996f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythoninterfaces = pythoninterfaces:main
    #
    # Would create an entry point called pythoninterfaces that calls pythoninterfaces.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - interfaces
    - pythoninterfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theojulienne/PythonInterfaces
  license: UNKNOWN
  summary: 'Interface implementation checking for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
