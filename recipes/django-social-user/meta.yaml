package:
  name: django-social-user
  version: "0.1.9"

source:
  fn: django-social-user-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-user/django-social-user-0.1.9.tar.gz
  md5: 09992d9d9a90142d64f976aeb6b31ab2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-user = django-social-user:main
    #
    # Would create an entry point called django-social-user that calls django-social-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_social_user
    - django_social_user.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattsnider/Django-Social-User
  license: MIT License
  summary: 'A generic system for interacting with remote APIs that need to create Django users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
