package:
  name: hurry.filesize
  version: "0.9"

source:
  fn: hurry.filesize-0.9.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.filesize/hurry.filesize-0.9.tar.gz
  md5: 8549ccd09bb31b5ff1e8e8c1eacc7794
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.filesize = hurry.filesize:main
    #
    # Would create an entry point called hurry.filesize that calls hurry.filesize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hurry
    - hurry.filesize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL 2.1
  summary: 'A simple Python library for human readable file sizes (or anything sized in bytes).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
