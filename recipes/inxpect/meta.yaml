package:
  name: inxpect
  version: "0.4.1"

source:
  fn: inxpect-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/i/inxpect/inxpect-0.4.1.tar.gz
  md5: 3eba9f0c8b4659068ce59263dbaadbcd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inxpect = inxpect:main
    #
    # Would create an entry point called inxpect that calls inxpect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonpickle

  run:
    - python
    - jsonpickle

test:
  # Python imports
  imports:
    - inxpect
    - inxpect.expect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/inxpect
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Intuitive inspect and expect'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
