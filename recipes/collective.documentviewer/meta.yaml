package:
  name: collective.documentviewer
  version: "3.0.2"

source:
  fn: collective.documentviewer-3.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.documentviewer/collective.documentviewer-3.0.2.zip
  md5: b89a43de791e86047aca5626e19a3886
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.documentviewer = collective.documentviewer:main
    #
    # Would create an entry point called collective.documentviewer that calls collective.documentviewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - zope.browserresource
    - repoze.catalog
    - plone.app.z3cform
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - zope.browserresource
    - repoze.catalog
    - plone.app.z3cform
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - collective
    - collective.documentviewer
    - collective.documentviewer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: "Document cloud's document viewer integration into plone."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
