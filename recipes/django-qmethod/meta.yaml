package:
  name: django-qmethod
  version: "0.0.3"

source:
  fn: django-qmethod-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qmethod/django-qmethod-0.0.3.tar.gz
  md5: 25a635acc067a1fad5b16faa3b4d7446
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qmethod = django-qmethod:main
    #
    # Would create an entry point called django-qmethod that calls django-qmethod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zacharyvoase/django-qmethod
  license: UNKNOWN
  summary: 'Define methods on QuerySets without custom manager and QuerySet subclasses.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
