package:
  name: gallery
  version: "0.1.0"

source:
  fn: Gallery-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/G/Gallery/Gallery-0.1.0.tar.gz
  md5: e4f703b62ba31edada318cdea3702c4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gallery = gallery:main
    #
    # Would create an entry point called gallery that calls gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - commandtool >=0.3.0
    - barenecessities

  run:
    - python
    - setuptools
    - commandtool >=0.3.0
    - barenecessities

test:
  # Python imports
  imports:
    - gallery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/gallery/index.html
  license: GPLv3
  summary: 'Convert photos and videos for use on the web, integrates with SiteTool to produce static HTML galleries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
