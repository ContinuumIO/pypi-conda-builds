package:
  name: elpylog
  version: "0.1.1"

source:
  fn: elpylog-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/elpylog/elpylog-0.1.1.tar.gz
  md5: 1d301586f4b7f11474924a9cbec3940c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elpylog = elpylog:main
    #
    # Would create an entry point called elpylog that calls elpylog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - elpylog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: git@github.com:ondergetekende/elpylog.git
  license: BSD License
  summary: "Python logger sending it's logs to elasticsearch."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
