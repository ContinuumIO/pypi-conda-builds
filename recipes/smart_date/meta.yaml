package:
  name: smart_date
  version: "2.0.3"

source:
  fn: smart_date-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/smart_date/smart_date-2.0.3.tar.gz
  md5: cedc42580a60f185f2b05a3cee9cbb68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smart_date = smart_date:main
    #
    # Would create an entry point called smart_date that calls smart_date.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smart_date
    - smart_date.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Ravall/smart_date.git
  license: MIT License
  summary: 'Interpreter string into an array of dates. A clever feature of Alexei Klimenko.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
