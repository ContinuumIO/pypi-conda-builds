package:
  name: represent-boundaries
  version: "0.7.0"

source:
  fn: represent-boundaries-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/r/represent-boundaries/represent-boundaries-0.7.0.tar.gz
  md5: ee4293eaea6332befd6cb2835369b7e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - represent-boundaries = represent-boundaries:main
    #
    # Would create an entry point called represent-boundaries that calls represent-boundaries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonfield >=0.9.20,<1
    - django-appconf

  run:
    - python
    - jsonfield >=0.9.20,<1
    - django-appconf

test:
  # Python imports
  imports:
    - boundaries
    - boundaries.management
    - boundaries.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://represent.poplus.org/
  license: MIT License
  summary: 'A web API to geographic boundaries loaded from shapefiles, packaged as a Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
