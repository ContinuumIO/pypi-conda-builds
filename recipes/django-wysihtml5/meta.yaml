package:
  name: django-wysihtml5
  version: "1.2b3"

source:
  fn: django-wysihtml5-1.2b3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wysihtml5/django-wysihtml5-1.2b3.tar.gz
  md5: 5c7127ccf21520748265f8ba509231cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wysihtml5 = django-wysihtml5:main
    #
    # Would create an entry point called django-wysihtml5 that calls django-wysihtml5.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - wysihtml5
    - wysihtml5.conf
    - wysihtml5.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-wysihtml5/
  license: MIT License
  summary: 'Simple Django app that provides a Wysihtml5 rich text editor textarea widget.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
