package:
  name: django-everlasting-sessions
  version: "0.0.4"

source:
  fn: django-everlasting-sessions-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-everlasting-sessions/django-everlasting-sessions-0.0.4.tar.gz
  md5: cc357e5368e9879e9665bcd7dd828edd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-everlasting-sessions = django-everlasting-sessions:main
    #
    # Would create an entry point called django-everlasting-sessions that calls django-everlasting-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_everlasting_sessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IlyaSemenov/django-everlasting-sessions
  license: BSD
  summary: 'Django middleware that prevents user sessions from expiration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
