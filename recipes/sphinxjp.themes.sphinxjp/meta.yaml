package:
  name: sphinxjp.themes.sphinxjp
  version: "0.3.1"

source:
  fn: sphinxjp.themes.sphinxjp-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.themes.sphinxjp/sphinxjp.themes.sphinxjp-0.3.1.tar.gz
  md5: a612f232ab6ab67fa3abc2686d89902d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.themes.sphinxjp = sphinxjp.themes.sphinxjp:main
    #
    # Would create an entry point called sphinxjp.themes.sphinxjp that calls sphinxjp.themes.sphinxjp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sphinx

  run:
    - python
    - setuptools
    - setuptools
    - sphinx

test:
  # Python imports
  imports:
    - sphinxjp
    - sphinxjp.themes
    - sphinxjp.themes.sphinxjp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/shimizukawa/sphinxjp.themes.sphinxjp
  license: MIT License
  summary: 'A sphinx theme for sphinx-users.jp site. #sphinxjp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
