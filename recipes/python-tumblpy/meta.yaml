package:
  name: python-tumblpy
  version: "1.0.4"

source:
  fn: python-tumblpy-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/python-tumblpy/python-tumblpy-1.0.4.tar.gz
  md5: 5a8e575a4d1ea195dadf1126afd71f56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-tumblpy = python-tumblpy:main
    #
    # Would create an entry point called python-tumblpy that calls python-tumblpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.2
    - requests_oauthlib >=0.3.2

  run:
    - python
    - requests >=1.2.2
    - requests_oauthlib >=0.3.2

test:
  # Python imports
  imports:
    - tumblpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/michaelhelmick/python-tumblpy/
  license: MIT License
  summary: 'A Python Library to interface with Tumblr v2 REST API & OAuth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
