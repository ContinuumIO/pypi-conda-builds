package:
  name: django-twittersync-oauth
  version: "0.1.1"

source:
  fn: django-twittersync-oauth-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twittersync-oauth/django-twittersync-oauth-0.1.1.tar.gz
  md5: be8fa08c0bdd66ec6d345f90d4bf7fd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twittersync-oauth = django-twittersync-oauth:main
    #
    # Would create an entry point called django-twittersync-oauth that calls django-twittersync-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - twitter
    - django-twittersync

  run:
    - python
    - django
    - twitter
    - django-twittersync

test:
  # Python imports
  imports:
    - twittersync_oauth
    - twittersync_oauth.management
    - twittersync_oauth.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vad/django-twittersync-oauth
  license: BSD License
  summary: 'OAuth Plugin for django-twittersync'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
