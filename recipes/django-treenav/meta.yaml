package:
  name: django-treenav
  version: "0.9.1"

source:
  fn: django-treenav-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-treenav/django-treenav-0.9.1.tar.gz
  md5: 9ef85a1432fd92df55d82694ebf141f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-treenav = django-treenav:main
    #
    # Would create an entry point called django-treenav that calls django-treenav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mptt >=0.5.2

  run:
    - python
    - django-mptt >=0.5.2

test:
  # Python imports
  imports:
    - treenav
    - treenav.migrations
    - treenav.templatetags
    - treenav.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/caktus/django-treenav
  license: BSD License
  summary: 'Extensible, hierarchical, and pluggable navigation system for Django sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
