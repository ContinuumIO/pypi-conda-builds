package:
  name: bitarray2d
  version: "2.1"

source:
  fn: BitArray2D-2.1.tar.gz
  url: https://engineering.purdue.edu/kak/dist2d/BitArray2D-2.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitarray2d = bitarray2d:main
    #
    # Would create an entry point called bitarray2d that calls bitarray2d.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - bitarray2d

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://RVL4.ecn.purdue.edu/~kak/dist2d/BitArray2D-2.1.html
  license: Python Software Foundation License
  summary: 'A memory-efficient packed representation for 2D bit arrays (This is a renamed version of the Bit2DArray module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
