package:
  name: commondata.be
  version: "0.0.1"

source:
  fn: commondata.be-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/commondata.be/commondata.be-0.0.1.tar.gz
  md5: 33ea975933bcb7dd0237e87949a5d4f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commondata.be = commondata.be:main
    #
    # Would create an entry point called commondata.be that calls commondata.be.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - commondata

  run:
    - python
    - commondata

test:
  # Python imports
  imports:
    - commondata.be

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://commondata.lino-framework.org
  license: GNU General Public License (GPL)
  summary: 'Common data about Belgium'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
