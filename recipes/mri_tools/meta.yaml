package:
  name: mri_tools
  version: "0.1.3"

source:
  fn: mri_tools-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mri_tools/mri_tools-0.1.3.tar.gz
  md5: 216b7bf034a180b6c65ace209fc2a7b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mri_tools = mri_tools:main
    #
    # Would create an entry point called mri_tools that calls mri_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wheel >=0.23.0
    - numpy

  run:
    - python
    - wheel >=0.23.0
    - numpy

test:
  # Python imports
  imports:
    - examples
    - mri_tools
    - mri_tools.dvs
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robbert-harms/mri-tools
  license: BSD License
  summary: 'Various tools for MRI related work.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
