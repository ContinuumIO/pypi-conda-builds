package:
  name: collective.pdfleadimage
  version: "0.2"

source:
  fn: collective.pdfLeadImage-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.pdfLeadImage/collective.pdfLeadImage-0.2.tar.gz
  md5: e26bd5bd5477b96f034bf269874ac96b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pdfleadimage = collective.pdfleadimage:main
    #
    # Would create an entry point called collective.pdfleadimage that calls collective.pdfleadimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.contentleadimage

  run:
    - python
    - setuptools
    - setuptools
    - collective.contentleadimage

test:
  # Python imports
  imports:
    - collective
    - collective.pdfLeadImage
    - collective.pdfLeadImage.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Automatically creates contentleadimage from pdf cover'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
