package:
  name: easyshop.search
  version: "0.1a1"

source:
  fn: easyshop.search-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.search/easyshop.search-0.1a1.tar.gz
  md5: b7428ddba4a16a2a227e02fbda75d08f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.search = easyshop.search:main
    #
    # Would create an entry point called easyshop.search that calls easyshop.search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.textindexng3

  run:
    - python
    - setuptools
    - setuptools
    - products.textindexng3

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.search
    - easyshop.search.browser
    - easyshop.search.content
    - easyshop.search.portlets
    - easyshop.search.profiles
    - easyshop.search.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'Search replacement for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
