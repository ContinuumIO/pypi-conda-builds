package:
  name: django-fixture-generator
  version: "0.2.0"

source:
  fn: django-fixture-generator-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fixture-generator/django-fixture-generator-0.2.0.tar.gz
  md5: ae737ebe5f71df19b3bc320c6fb6c70a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fixture-generator = django-fixture-generator:main
    #
    # Would create an entry point called django-fixture-generator that calls django-fixture-generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fixture_generator
    - fixture_generator.management
    - fixture_generator.management.commands
    - fixture_generator.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alex/django-fixture-generator/tree/master
  license: BSD License
  summary: 'django-fixture-generator is a reusable django application to make writing fixtures not suck.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
