package:
  name: dehydrate
  version: "0.3.7"

source:
  fn: dehydrate-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dehydrate/dehydrate-0.3.7.tar.gz
  md5: cd185c4348b49406850c9f6bdf59bacd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dehydrate = dehydrate:main
    #
    # Would create an entry point called dehydrate that calls dehydrate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - dehydrate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/l0kix2/python-dehydrate
  license: MIT License
  summary: 'Small lib for representing python objects as a dicts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
