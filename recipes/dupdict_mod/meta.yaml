package:
  name: dupdict_mod
  version: "1.0"

source:
  fn: dupdict_mod-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dupdict_mod/dupdict_mod-1.0.tar.gz
  md5: cd36de6b4ca33ac27530419c7207bebf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dupdict_mod = dupdict_mod:main
    #
    # Would create an entry point called dupdict_mod that calls dupdict_mod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dupdict_mod

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stromberg.dnsalias.org/~strombrg/dupdict_mod/
  license: Apache v2
  summary: 'Pure Python dictionary wrapper that allows duplicates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
