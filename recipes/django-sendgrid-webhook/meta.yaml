package:
  name: django-sendgrid-webhook
  version: "0.2.2"

source:
  fn: django-sendgrid-webhook-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sendgrid-webhook/django-sendgrid-webhook-0.2.2.tar.gz
  md5: ee08048cc5de65b0eb64d219d5dd6b86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sendgrid-webhook = django-sendgrid-webhook:main
    #
    # Would create an entry point called django-sendgrid-webhook that calls django-sendgrid-webhook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sendgrid
    - sendgrid.migrations
    - sendgrid.south_migrations
    - sendgrid.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/resmio/django-sendgrid
  license: BSD 2-Clause
  summary: 'A small django app around sendgrid and its webhooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
