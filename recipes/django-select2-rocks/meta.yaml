package:
  name: django-select2-rocks
  version: "0.2"

source:
  fn: django-select2-rocks-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-select2-rocks/django-select2-rocks-0.2.tar.gz
  md5: 6463871576895a9e2359dc889aa3d409
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-select2-rocks = django-select2-rocks:main
    #
    # Would create an entry point called django-select2-rocks that calls django-select2-rocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - select2rocks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A Django application to provide AJAX autocomplete with Select2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
