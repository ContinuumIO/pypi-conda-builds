package:
  name: collective.powertoken.workflow
  version: "0.2.0"

source:
  fn: collective.powertoken.workflow-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.powertoken.workflow/collective.powertoken.workflow-0.2.0.tar.gz
  md5: 75662d2d23ade132aa897d6b00e4fb52
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.powertoken.workflow = collective.powertoken.workflow:main
    #
    # Would create an entry point called collective.powertoken.workflow that calls collective.powertoken.workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.powertoken.core >=0.3.0

  run:
    - python
    - setuptools
    - setuptools
    - collective.powertoken.core >=0.3.0

test:
  # Python imports
  imports:
    - collective
    - collective.powertoken
    - collective.powertoken.workflow
    - collective.powertoken.workflow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.powertoken.core
  license: GPL
  summary: 'Workflow add-ons for collective.powertoken support for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
