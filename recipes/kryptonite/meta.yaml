package:
  name: kryptonite
  version: "0.1.3"

source:
  fn: kryptonite-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/k/kryptonite/kryptonite-0.1.3.tar.gz
  md5: d3c7cb68c7cb29abcec4236087a2df1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kryptonite = kryptonite:main
    #
    # Would create an entry point called kryptonite that calls kryptonite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pycrypto
    - passlib

  run:
    - python
    - pycrypto
    - passlib

test:
  # Python imports
  imports:
    - kryptonite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gilsho/kryptonite
  license: MIT License
  summary: 'Cryptography for humans'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
