package:
  name: distributedlock
  version: "1.2"

source:
  fn: DistributedLock-1.2.tar.gz
  url: https://pypi.python.org/packages/source/D/DistributedLock/DistributedLock-1.2.tar.gz
  md5: 80e5c533923042835e748d6de4a81bd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distributedlock = distributedlock:main
    #
    # Would create an entry point called distributedlock that calls distributedlock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached >=1.40

  run:
    - python
    - python-memcached >=1.40

test:
  # Python imports
  imports:
    - distributedlock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/snbuback/DistributedLock
  license: LICENSE.txt
  summary: 'Python Distributed Lock with memcached support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
