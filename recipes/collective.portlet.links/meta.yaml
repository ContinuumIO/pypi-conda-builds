package:
  name: collective.portlet.links
  version: "0.2"

source:
  fn: collective.portlet.links-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.links/collective.portlet.links-0.2.tar.gz
  md5: 20721142fdb98ae9ea65a77d2038cafc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.links = collective.portlet.links:main
    #
    # Would create an entry point called collective.portlet.links that calls collective.portlet.links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.links
    - collective.portlet.links.browser
    - collective.portlet.links.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'A Plone portlet with configureable links.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
