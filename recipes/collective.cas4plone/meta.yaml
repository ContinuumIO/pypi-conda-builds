package:
  name: collective.cas4plone
  version: "1.0a3"

source:
  fn: collective.cas4plone-1.0a3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.cas4plone/collective.cas4plone-1.0a3.tar.gz
  md5: d1360291603770c3bff08ef7c15f0c40
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cas4plone = collective.cas4plone:main
    #
    # Would create an entry point called collective.cas4plone that calls collective.cas4plone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.cas4plone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL
  summary: 'A PAS plugin that authenticates users against a CAS (Central Authentication Service) server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
