package:
  name: redmodel
  version: "0.3.1"

source:
  fn: redmodel-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redmodel/redmodel-0.3.1.tar.gz
  md5: b0b232b31c5b084709b57e5b16d8aeba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redmodel = redmodel:main
    #
    # Would create an entry point called redmodel that calls redmodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redmodel
    - redmodel.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/beatmax/redmodel
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python Library for Data Models Persisted in Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
