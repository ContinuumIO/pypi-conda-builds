package:
  name: sortedfile
  version: "0.3"

source:
  fn: sortedfile-0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sortedfile/sortedfile-0.3.tar.gz
  md5: 5b086f946085b17e646a2ed757a6f06f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sortedfile = sortedfile:main
    #
    # Would create an entry point called sortedfile that calls sortedfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sortedfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dw/sortedfile/
  license: Apache 2
  summary: 'Logarithmic search for sorted text files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
