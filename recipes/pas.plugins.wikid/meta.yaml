package:
  name: pas.plugins.wikid
  version: "0.1"

source:
  fn: pas.plugins.wikid-0.1.zip
  url: https://pypi.python.org/packages/source/p/pas.plugins.wikid/pas.plugins.wikid-0.1.zip
  md5: 6350fc2cb52ea0d4255967313163407d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pas.plugins.wikid = pas.plugins.wikid:main
    #
    # Would create an entry point called pas.plugins.wikid that calls pas.plugins.wikid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pyopenssl

  run:
    - python
    - setuptools
    - setuptools
    - pyopenssl

test:
  # Python imports
  imports:
    - pas
    - pas.plugins
    - pas.plugins.wikid
    - pas.plugins.wikid.browser
    - pas.plugins.wikid.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/pas.plugins.wikid
  license: GNU General Public License (GPL)
  summary: 'This is a WiKID Strong Authentication System Plugin for the Zope2 Pluggable Authentication Service (PAS).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
