package:
  name: fv_email
  version: "0.9"

source:
  fn: fv_email-0.9.tar.gz
  url: https://pypi.python.org/packages/source/f/fv_email/fv_email-0.9.tar.gz
  md5: b585b1601e751f4f6aca3e053397b1f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fv_email = fv_email:main
    #
    # Would create an entry point called fv_email that calls fv_email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - formencode

  run:
    - python
    - formencode

test:
  # Python imports
  imports:
    - fv_email

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/josephtate/formencode-email/
  license: Python Software Foundation License
  summary: 'Improved FormEncode e-mail validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
