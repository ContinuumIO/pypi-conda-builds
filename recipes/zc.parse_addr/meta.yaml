package:
  name: zc.parse_addr
  version: "1.0.0"

source:
  fn: zc.parse_addr-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.parse_addr/zc.parse_addr-1.0.0.tar.gz
  md5: f1fc2b9fbaa8362039b30adbd20e07f9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.parse_addr = zc.parse_addr:main
    #
    # Would create an entry point called zc.parse_addr that calls zc.parse_addr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Parse network addresses of the form: HOST:PORT'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
