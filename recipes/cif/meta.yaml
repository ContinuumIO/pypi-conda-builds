package:
  name: cif
  version: "0.01-14"

source:
  fn: cif-0.01-14.tar.gz
  url: https://pypi.python.org/packages/source/c/cif/cif-0.01-14.tar.gz
  md5: 15bd56b4b0c7ffe81c3cb339d4217b61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cif = cif:main
    #
    # Would create an entry point called cif that calls cif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.7.1
    - simplejson >=2.1.1
    - texttable
    - argparse
    - socksipy-branch >=1.02

  run:
    - python
    - httplib2 >=0.7.1
    - simplejson >=2.1.1
    - texttable
    - argparse
    - socksipy-branch >=1.02

test:
  # Python imports
  imports:
    - cif

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv2
  summary: 'a sample python client for accessing the CIF interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
