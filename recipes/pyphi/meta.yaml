package:
  name: pyphi
  version: "0.6.0"

source:
  fn: pyphi-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyphi/pyphi-0.6.0.tar.gz
  md5: 4237fab183fbdb1353413c7bd6bc3561
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyphi = pyphi:main
    #
    # Would create an entry point called pyphi that calls pyphi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8.1,<2.0.0
    - scipy >=0.13.3,<1.0.0
    - pyemd >=0.0.7,<1.0.0
    - joblib >=0.8.0a3,<1.0.0
    - psutil >=2.1.1,<3.0.0
    - marbl-python >=2.0.0,<3.0.0
    - pymongo >=2.7.1,<3.0.0
    - pyyaml >=3.11,<4.0

  run:
    - python
    - numpy >=1.8.1,<2.0.0
    - scipy >=0.13.3,<1.0.0
    - pyemd >=0.0.7,<1.0.0
    - joblib >=0.8.0a3,<1.0.0
    - psutil >=2.1.1,<3.0.0
    - marbl-python >=2.0.0,<3.0.0
    - pymongo >=2.7.1,<3.0.0
    - pyyaml >=3.11,<4.0

test:
  # Python imports
  imports:
    - pyphi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A Python library for computing integrated information.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
