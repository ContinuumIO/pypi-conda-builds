package:
  name: smartcoin
  version: "0.1"

source:
  fn: smartcoin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/smartcoin/smartcoin-0.1.tar.gz
  md5: 7eda5da6967843602081781299b6142f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smartcoin = smartcoin:main
    #
    # Would create an entry point called smartcoin that calls smartcoin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - smartcoin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git@github.com/smartcoinpayments/smartcoin-python
  license: MIT
  summary: 'The Smartcoin provides a Python REST APIs to create, process and manage payments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
