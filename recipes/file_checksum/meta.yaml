package:
  name: file_checksum
  version: "0.3.0"

source:
  fn: file_checksum-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/f/file_checksum/file_checksum-0.3.0.tar.gz
  md5: 2808ea4bffb2d2649e07c1ba0ebcbb5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - file_checksum = file_checksum:main
    #
    # Would create an entry point called file_checksum that calls file_checksum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - file_checksum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marcoagner/file_checksum.py
  license: UNKNOWN
  summary: 'A hashlib package encapsulation for file integrity verification.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
