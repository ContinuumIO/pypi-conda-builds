package:
  name: plonesocial.theme
  version: "0.5.4"

source:
  fn: plonesocial.theme-0.5.4.zip
  url: https://pypi.python.org/packages/source/p/plonesocial.theme/plonesocial.theme-0.5.4.zip
  md5: 4493185e56a1b412c5744a79e526521a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonesocial.theme = plonesocial.theme:main
    #
    # Would create an entry point called plonesocial.theme that calls plonesocial.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonesocial
    - plonesocial.theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cosent/plonesocial.theme
  license: Apache License 2.0
  summary: "Plone theme based on Twitter's Bootstrap CSS"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
