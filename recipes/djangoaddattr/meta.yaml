package:
  name: djangoaddattr
  version: "0.0.1"

source:
  fn: djangoaddattr-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangoaddattr/djangoaddattr-0.0.1.tar.gz
  md5: a46ea8931af3c186cfc80327a46a879f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoaddattr = djangoaddattr:main
    #
    # Would create an entry point called djangoaddattr that calls djangoaddattr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - addattr
    - addattr.templatetags
    - djangoaddattr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/neuman/django-template-addattr
  license: BSD
  summary: 'djangoaddattr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
