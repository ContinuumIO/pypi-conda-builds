package:
  name: plone.jsonapi.core
  version: "0.4"

source:
  fn: plone.jsonapi.core-0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.jsonapi.core/plone.jsonapi.core-0.4.tar.gz
  md5: 7c65e7e6d1bf002a47de28a349f5e692
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.jsonapi.core = plone.jsonapi.core:main
    #
    # Would create an entry point called plone.jsonapi.core that calls plone.jsonapi.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - werkzeug >=0.7.2
    - simplejson >=2.0.9

  run:
    - python
    - setuptools
    - setuptools
    - werkzeug >=0.7.2
    - simplejson >=2.0.9

test:
  # Python imports
  imports:
    - plone
    - plone.jsonapi
    - plone.jsonapi.core
    - plone.jsonapi.core.browser
    - plone.jsonapi.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramonski/plone.jsonapi
  license: MIT
  summary: 'Plone JSON API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
