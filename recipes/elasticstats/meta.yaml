package:
  name: elasticstats
  version: "0.1.0"

source:
  fn: elasticstats-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/elasticstats/elasticstats-0.1.0.tar.gz
  md5: ab8365d6af16b6d5950d441563ae39bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticstats = elasticstats:main
    #
    # Would create an entry point called elasticstats that calls elasticstats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - elasticstats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ssaw/elasticstats
  license: LGPLv3
  summary: 'elasticstats populates a Graphite server with stats from Elasticsearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
