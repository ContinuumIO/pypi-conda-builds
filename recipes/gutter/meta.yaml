package:
  name: gutter
  version: "0.5.0"

source:
  fn: gutter-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gutter/gutter-0.5.0.tar.gz
  md5: 90f94fad3c297764f4439ec9f9657126
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gutter = gutter:main
    #
    # Would create an entry point called gutter that calls gutter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - durabledict >=0.9.0
    - jsonpickle
    - werkzeug

  run:
    - python
    - durabledict >=0.9.0
    - jsonpickle
    - werkzeug

test:
  # Python imports
  imports:
    - gutter
    - gutter.client
    - gutter.client.arguments
    - gutter.client.operators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/disqus/gutter
  license: Apache License 2.0
  summary: 'Client to gutter feature switches backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
