package:
  name: flask-qrcode
  version: "0.5.1"

source:
  fn: Flask-QRcode-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-QRcode/Flask-QRcode-0.5.1.tar.gz
  md5: 16d8d4fe54962dd7e725a2583a9935e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-qrcode = flask-qrcode:main
    #
    # Would create an entry point called flask-qrcode that calls flask-qrcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - qrcode
    - pillow

  run:
    - python
    - flask
    - qrcode
    - pillow

test:
  # Python imports
  imports:
    - flask_qrcode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marcoagner/Flask-QRcode
  license: MIT License
  summary: 'A simple flask extension to render QR codes on template'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
