package:
  name: gocept.zestreleaser.customupload
  version: "1.4"

source:
  fn: gocept.zestreleaser.customupload-1.4.zip
  url: https://pypi.python.org/packages/source/g/gocept.zestreleaser.customupload/gocept.zestreleaser.customupload-1.4.zip
  md5: 13383cb434c4f4a4c4e77db2b2d8df8a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.zestreleaser.customupload = gocept.zestreleaser.customupload:main
    #
    # Would create an entry point called gocept.zestreleaser.customupload that calls gocept.zestreleaser.customupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zest.releaser >=3.12

  run:
    - python
    - setuptools
    - setuptools
    - zest.releaser >=3.12

test:
  # Python imports
  imports:
    - gocept
    - gocept.zestreleaser
    - gocept.zestreleaser.customupload
    - gocept.zestreleaser.customupload.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.gocept.com/hg/public/gocept.zestreleaser.customupload
  license: Zope Public License
  summary: 'Plugin for zest.releaser to allow uploading the created egg via SCP to configurable destinations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
