package:
  name: tornado-stub-client
  version: "0.2"

source:
  fn: tornado-stub-client-0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-stub-client/tornado-stub-client-0.2.tar.gz
  md5: 283c2bf1ab2327dfea0124af9f88cce5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-stub-client = tornado-stub-client:main
    #
    # Would create an entry point called tornado-stub-client that calls tornado-stub-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tornado_stub_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dcosson/tornado-stub-client
  license: MIT
  summary: 'Stubs out tornado AsyncHTTPClient.fetch with a nice interface, for testing code that relies on async code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
