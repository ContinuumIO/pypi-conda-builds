package:
  name: django-dbdump
  version: "1.1"

source:
  fn: django-dbdump-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbdump/django-dbdump-1.1.tar.gz
  md5: 8a0da912ce11aea818e3edb588b2848d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbdump = django-dbdump:main
    #
    # Would create an entry point called django-dbdump that calls django-dbdump.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - dbdump
    - dbdump.management
    - dbdump.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vitaliyf/django-dbdump/
  license: BSD License
  summary: 'Database backup management command.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
