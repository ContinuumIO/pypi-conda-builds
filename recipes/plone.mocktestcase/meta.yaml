package:
  name: plone.mocktestcase
  version: "1.0b3"

source:
  fn: plone.mocktestcase-1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.mocktestcase/plone.mocktestcase-1.0b3.tar.gz
  md5: 6de66da6d610537d1f5c31e2ab0f36ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.mocktestcase = plone.mocktestcase:main
    #
    # Would create an entry point called plone.mocktestcase that calls plone.mocktestcase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mocker

  run:
    - python
    - setuptools
    - setuptools
    - mocker

test:
  # Python imports
  imports:
    - plone
    - plone.mocktestcase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: LGPL
  summary: 'Mock unit test case based on ``mocker``'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
