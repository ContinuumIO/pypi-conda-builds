package:
  name: django-poll-system
  version: "0.2.3"

source:
  fn: django-poll-system-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-poll-system/django-poll-system-0.2.3.tar.gz
  md5: e7562f554bbc7a12735100009f13fa0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-poll-system = django-poll-system:main
    #
    # Would create an entry point called django-poll-system that calls django-poll-system.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - poll
    - poll.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://polltest.pythonism.ru
  license: UNKNOWN
  summary: 'The application to organize polling (or voting) on your site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
