package:
  name: simpleodspy
  version: "1.4.1"

source:
  fn: simpleodspy-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/s/simpleodspy/simpleodspy-1.4.1.tar.gz
  md5: 960845b31c99792e60b0ef728bbf2344
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleodspy = simpleodspy:main
    #
    # Would create an entry point called simpleodspy that calls simpleodspy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - xlwt-future >=0.8.0

  run:
    - python
    - xlwt-future >=0.8.0

test:
  # Python imports
  imports:
    - simpleodspy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://simple-odspy.sourceforge.net/
  license: UNKNOWN
  summary: 'Simple spreadsheet class with ods export'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
