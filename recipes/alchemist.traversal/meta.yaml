package:
  name: alchemist.traversal
  version: "0.4.0"

source:
  fn: alchemist.traversal-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/a/alchemist.traversal/alchemist.traversal-0.4.0.tar.gz
  md5: d118e70c3e74ce354e683fc2f4f7b21a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alchemist.traversal = alchemist.traversal:main
    #
    # Would create an entry point called alchemist.traversal that calls alchemist.traversal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ore.alchemist
    - z3c.traverser

  run:
    - python
    - setuptools
    - ore.alchemist
    - z3c.traverser

test:
  # Python imports
  imports:
    - alchemist
    - alchemist.traversal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LGPL
  summary: 'Foreign Key Traversal mechanisms for alchemist containers and domain objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
