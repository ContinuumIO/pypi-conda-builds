package:
  name: bda.intellidatetime
  version: "1.2.1"

source:
  fn: bda.intellidatetime-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.intellidatetime/bda.intellidatetime-1.2.1.tar.gz
  md5: c21f0c1f5e9f71240734e5c05d988aae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.intellidatetime = bda.intellidatetime:main
    #
    # Would create an entry point called bda.intellidatetime that calls bda.intellidatetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface

  run:
    - python
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - bda
    - bda.intellidatetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/bda.intellidatetime
  license: GNU General Public Licence
  summary: 'bda.intellidatetime'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
