package:
  name: flask-mime
  version: "0.1.0"

source:
  fn: Flask-Mime-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Mime/Flask-Mime-0.1.0.tar.gz
  md5: da68e86ca1df2be711b45d28b63f5a0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mime = flask-mime:main
    #
    # Would create an entry point called flask-mime that calls flask-mime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lanius/flask-mime/
  license: BSD License
  summary: 'Provides MIME type based request dispatching mechanism.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
