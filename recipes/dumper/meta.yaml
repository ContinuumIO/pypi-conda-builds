package:
  name: dumper
  version: "1.0.4"

source:
  fn: Dumper-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/D/Dumper/Dumper-1.0.4.tar.gz
  md5: 91af7da4e1b319fe4c639bc7f72b1795
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dumper = dumper:main
    #
    # Would create an entry point called dumper that calls dumper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dumper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jric/Dumper.py
  license: MIT License
  summary: 'Tool to conveniently describe any Python datastructure'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
