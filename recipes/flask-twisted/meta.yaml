package:
  name: flask-twisted
  version: "0.1.0"

source:
  fn: Flask-Twisted-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Twisted/Flask-Twisted-0.1.0.tar.gz
  md5: de9da79ab0a10c06f580c74286beb133
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-twisted = flask-twisted:main
    #
    # Would create an entry point called flask-twisted that calls flask-twisted.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - twisted
    - observable

  run:
    - python
    - flask
    - twisted
    - observable

test:
  # Python imports
  imports:
    - flask_twisted

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cravler/flask-twisted/
  license: MIT License
  summary: 'Simple integration of Flask and Twisted'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
