package:
  name: event_days_indexer
  version: "0.0.1"

source:
  fn: event_days_indexer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/event_days_indexer/event_days_indexer-0.0.1.tar.gz
  md5: 7fc461ad825c39560c33b7f26948b175
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - event_days_indexer = event_days_indexer:main
    #
    # Would create an entry point called event_days_indexer that calls event_days_indexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - event_days_indexer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
