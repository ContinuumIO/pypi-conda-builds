package:
  name: dataone.test_utilities
  version: "1.3.0"

source:
  fn: dataone.test_utilities-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.test_utilities/dataone.test_utilities-1.3.0.tar.gz
  md5: 0f67ecb23b3dae81dab3031f8fc5a93c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.test_utilities = dataone.test_utilities:main
    #
    # Would create an entry point called dataone.test_utilities that calls dataone.test_utilities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - multi-mechanize
    - dataone.libclient ==1.2.5
    - dataone.certificate_extensions ==1.1.1

  run:
    - python
    - multi-mechanize
    - dataone.libclient ==1.2.5
    - dataone.certificate_extensions ==1.1.1

test:
  # Python imports
  imports:
    - d1_test
    - d1_test.instance_generator
    - d1_test.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'Utilities for testing DataONE infrastructure components'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
