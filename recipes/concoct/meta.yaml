package:
  name: concoct
  version: "0.4.0"

source:
  fn: concoct-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/concoct/concoct-0.4.0.tar.gz
  md5: 0e783d0e6dabbb7329ff07c11bfd7299
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - concoct = concoct:main
    #
    # Would create an entry point called concoct that calls concoct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cython >=0.19.1
    - numpy >=1.7.1
    - scipy >=0.12.0
    - pandas >=0.11.0
    - biopython >=1.62b
    - scikit-learn >=0.13.1
    - nose ==1.3.0
    - sphinx-rtd-theme >=0.1.6
    - sphinx >=1.2.2

  run:
    - python
    - setuptools
    - cython >=0.19.1
    - numpy >=1.7.1
    - scipy >=0.12.0
    - pandas >=0.11.0
    - biopython >=1.62b
    - scikit-learn >=0.13.1
    - nose ==1.3.0
    - sphinx-rtd-theme >=0.1.6
    - sphinx >=1.2.2

test:
  # Python imports
  imports:
    - concoct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BinPro/CONCOCT
  license: FreeBSD
  summary: 'Clustering cONtigs with COverage and ComposiTion'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
