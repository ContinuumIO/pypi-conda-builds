package:
  name: aikif
  version: "0.1.0"

source:
  fn: AIKIF-0.1.0.zip
  url: https://pypi.python.org/packages/source/A/AIKIF/AIKIF-0.1.0.zip
  md5: 1a8fdd8f6f9dcb42b223f3c8b58bf3c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aikif = aikif:main
    #
    # Would create an entry point called aikif that calls aikif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - aikif
    - aikif.agents
    - aikif.agents.aggregate
    - aikif.agents.explore
    - aikif.agents.gather
    - aikif.agents.learn
    - aikif.dataTools
    - aikif.environments
    - aikif.examples
    - aikif.lib
    - aikif.ontology
    - aikif.toolbox
    - aikif.web_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/acutesoftware/AIKIF
  license: LICENSE.txt
  summary: 'Artificial Intelligence Knowledge Information Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
