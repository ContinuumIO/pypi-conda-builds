package:
  name: django-include-strip-tag
  version: "0.1.0"

source:
  fn: django-include-strip-tag-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-include-strip-tag/django-include-strip-tag-0.1.0.tar.gz
  md5: c2a8175e0ece5d736509f7717957f1fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-include-strip-tag = django-include-strip-tag:main
    #
    # Would create an entry point called django-include-strip-tag that calls django-include-strip-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - include_strip_tag
    - include_strip_tag.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twidi/django-include-strip-tag
  license: MIT License
  summary: "A 'include_strip' templatetag for django to strip trailing and leading spaces/newlines"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
