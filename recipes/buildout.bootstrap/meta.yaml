package:
  name: buildout.bootstrap
  version: "1.4.6"

source:
  fn: buildout.bootstrap-1.4.6.zip
  url: https://pypi.python.org/packages/source/b/buildout.bootstrap/buildout.bootstrap-1.4.6.zip
  md5: f87cf43164b4cf5cc9613cc18d528551
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.bootstrap = buildout.bootstrap:main
    #
    # Would create an entry point called buildout.bootstrap that calls buildout.bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - buildout
    - buildout.bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://collective.github.com/buildout.bootstrap
  license: Zope Public License
  summary: 'A buildout extension that adds a buildout        bootstrap.py file to your buildout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
