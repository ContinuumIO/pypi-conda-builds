package:
  name: django-runuwsgi
  version: "0.1"

source:
  fn: django-runuwsgi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-runuwsgi/django-runuwsgi-0.1.tar.gz
  md5: 90618601c64d1cd382d8d6adc85b2abf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-runuwsgi = django-runuwsgi:main
    #
    # Would create an entry point called django-runuwsgi that calls django-runuwsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - runuwsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silentsokolov/django-runuwsgi
  license: MIT License
  summary: 'Application for Django, to run uwsgi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
