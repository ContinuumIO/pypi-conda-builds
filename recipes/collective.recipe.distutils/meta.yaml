package:
  name: collective.recipe.distutils
  version: "0.1"

source:
  fn: collective.recipe.distutils-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.distutils/collective.recipe.distutils-0.1.tar.gz
  md5: 3e9e9458d384ca8446c7a2e75d822558
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.distutils = collective.recipe.distutils:main
    #
    # Would create an entry point called collective.recipe.distutils that calls collective.recipe.distutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.distutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/collective.recipe.distutils
  license: GNU General Public License (GPL)
  summary: 'A buildout recipe to install distutils Python packages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
