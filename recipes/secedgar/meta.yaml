package:
  name: secedgar
  version: "0.1.1"

source:
  fn: SECEdgar-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/S/SECEdgar/SECEdgar-0.1.1.tar.gz
  md5: 62b0be944861b2c8231b6a2b6903274f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secedgar = secedgar:main
    #
    # Would create an entry point called secedgar that calls secedgar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4
    - configparser

  run:
    - python
    - setuptools
    - requests
    - beautifulsoup4
    - configparser

test:
  # Python imports
  imports:
    - SECEdgar
    - secedgar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rahulrrixe/SEC-Edgar
  license: Apache Software License
  summary: 'SEC-Edgar implements a basic Sphinx crwaler for downloading the                    filings. It provides an interface to extract the filing from the SEC.gov site                  You might find it most useful for tasks involving automated                   data collection of filings from SEC.gov'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
