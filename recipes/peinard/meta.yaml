package:
  name: peinard
  version: "0.1"

source:
  fn: peinard-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/peinard/peinard-0.1.tar.gz
  md5: 4de99d89f61562173acc6a91a28685d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - peinard = peinard:main
    #
    # Would create an entry point called peinard that calls peinard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - peinard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/feth/peinard
  license: GNU Affero General Public License v3
  summary: 'Algorithm to balance debts and credits with the least exchanges.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
