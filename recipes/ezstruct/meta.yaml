package:
  name: ezstruct
  version: "0.1.0"

source:
  fn: ezstruct-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/ezstruct/ezstruct-0.1.0.tar.gz
  md5: fc49082f6ffb99f5533e4b70dfbaaec6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezstruct = ezstruct:main
    #
    # Would create an entry point called ezstruct that calls ezstruct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - ezstruct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/matthewg/EzStruct/
  license: Apache Software License
  summary: 'Expressive syntax for working with binary data formats and network protocols.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
