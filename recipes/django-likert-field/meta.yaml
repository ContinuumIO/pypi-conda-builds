package:
  name: django-likert-field
  version: "0.1.0"

source:
  fn: django-likert-field-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-likert-field/django-likert-field-0.1.0.tar.gz
  md5: 2ab9ced6055c6e3a2fea5fcf003b4aac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-likert-field = django-likert-field:main
    #
    # Would create an entry point called django-likert-field that calls django-likert-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - likert_field
    - likert_field.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kelvinwong-ca/django-likert-field
  license: BSD License
  summary: 'A Likert field for Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
