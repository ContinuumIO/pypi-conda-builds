package:
  name: sphinxcontrib-spam
  version: "0.4.2"

source:
  fn: sphinxcontrib-spam-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-spam/sphinxcontrib-spam-0.4.2.tar.gz
  md5: 6a61134c8b7d3625341f59913ad808db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-spam = sphinxcontrib-spam:main
    #
    # Would create an entry point called sphinxcontrib-spam that calls sphinxcontrib-spam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - sphinx >=1.0.0

  run:
    - python
    - distribute
    - sphinx >=1.0.0

test:
  # Python imports
  imports:
    - sphinxcontrib
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jptomo/sphinxcontrib-spam
  license: BSD License
  summary: 'output spam! spam! spam!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
