package:
  name: simplemongo
  version: "0.2"

source:
  fn: simplemongo-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/simplemongo/simplemongo-0.2.tar.gz
  md5: 7192004cc157e576bfccfbb7d70537a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplemongo = simplemongo:main
    #
    # Would create an entry point called simplemongo that calls simplemongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - simplemongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stevenewey/simplemongo
  license: Apache2
  summary: 'Super simple ORM wrapper for MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
