package:
  name: django-cache-purge-hooks
  version: "0.4.1"

source:
  fn: django-cache-purge-hooks-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-purge-hooks/django-cache-purge-hooks-0.4.1.tar.gz
  md5: e3e062b657897a869437adb47142bcb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-purge-hooks = django-cache-purge-hooks:main
    #
    # Would create an entry point called django-cache-purge-hooks that calls django-cache-purge-hooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - cache_purge_hooks
    - cache_purge_hooks.backends
    - sampleproject
    - sampleproject.sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RealGeeks/django-cache-purge-hooks
  license: MIT License
  summary: 'Drop-in cache (e.g. varnish) purging hooks for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
