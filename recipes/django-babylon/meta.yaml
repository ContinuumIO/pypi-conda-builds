package:
  name: django-babylon
  version: "0.1.13"

source:
  fn: django-babylon-0.1.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-babylon/django-babylon-0.1.13.tar.gz
  md5: c6a181e822742ba76daa68ad24c85fff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-babylon = django-babylon:main
    #
    # Would create an entry point called django-babylon that calls django-babylon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.1

  run:
    - python
    - django >=1.4.1

test:
  # Python imports
  imports:
    - babylon
    - babylon.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/radiosilence/django-babylon
  license: LICENSE.txt
  summary: 'Django infinite caching for mandems.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
