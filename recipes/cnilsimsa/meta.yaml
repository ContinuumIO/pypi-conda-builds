package:
  name: cnilsimsa
  version: "0.1.2"

source:
  fn: cnilsimsa-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cnilsimsa/cnilsimsa-0.1.2.tar.gz
  md5: eb3be0bf94ea71b2c050ac7fddcd0fde
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cnilsimsa = cnilsimsa:main
    #
    # Would create an entry point called cnilsimsa that calls cnilsimsa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cnilsimsa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lukearno/cnilsimsa
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'A C implementation of Nilsimsa for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
