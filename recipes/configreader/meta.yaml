package:
  name: configreader
  version: "0.0.6"

source:
  fn: configreader-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/configreader/configreader-0.0.6.tar.gz
  md5: 8462b1d46734321158393111ec9ddcca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configreader = configreader:main
    #
    # Would create an entry point called configreader that calls configreader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - configreader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mortbauer/configreader
  license: MIT License
  summary: 'An easy-to-use, powerful configuration module for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
