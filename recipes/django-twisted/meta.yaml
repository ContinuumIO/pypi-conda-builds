package:
  name: django-twisted
  version: "0.1.0"

source:
  fn: django-twisted-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twisted/django-twisted-0.1.0.tar.gz
  md5: 4eee93f6fcc2d84d9c6b11756052e250
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twisted = django-twisted:main
    #
    # Would create an entry point called django-twisted that calls django-twisted.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - twango

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gdoermann/twango
  license: MIT License
  summary: 'Django ORM + Twisted - Blocking'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
