package:
  name: wimms
  version: "0.3"

source:
  fn: wimms-0.3.tar.gz
  url: https://pypi.python.org/packages/source/w/wimms/wimms-0.3.tar.gz
  md5: 7a6146f367ded0c710028b85e9c37329
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wimms = wimms:main
    #
    # Would create an entry point called wimms that calls wimms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unittest2
    - mozsvc
    - sqlalchemy

  run:
    - python
    - unittest2
    - mozsvc
    - sqlalchemy

test:
  # Python imports
  imports:
    - wimms
    - wimms.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla-services/wimms
  license: UNKNOWN
  summary: 'Where Is My Mozilla Service ?'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
