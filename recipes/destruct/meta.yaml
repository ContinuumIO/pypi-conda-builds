package:
  name: destruct
  version: "0.1.0"

source:
  fn: destruct-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/destruct/destruct-0.1.0.tar.gz
  md5: 434676d9a560068b48f5f15bbc084459
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - destruct = destruct:main
    #
    # Would create an entry point called destruct that calls destruct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - destruct
    - destruct.fields
    - destruct.protocols
    - destruct.types

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drdaeman/destruct
  license: MIT License
  summary: 'Tiny library to parse binary structures into Python objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
