package:
  name: h5dj
  version: "0.1.0"

source:
  fn: h5dj-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/h5dj/h5dj-0.1.0.tar.gz
  md5: 7e76acb7ffcaf7c6768e114b04f65fba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - h5dj = h5dj:main
    #
    # Would create an entry point called h5dj that calls h5dj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - h5py
    - django
    - pil
    - imread
    - requests

  run:
    - python
    - numpy
    - h5py
    - django
    - pil
    - imread
    - requests

test:
  # Python imports
  imports:
    - h5dj
    - tests
    - tests.h5dj_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fish2000/h5dj/
  license: BSD License
  summary: 'HDF5 interfaces for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
