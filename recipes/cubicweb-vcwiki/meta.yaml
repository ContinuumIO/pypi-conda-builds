package:
  name: cubicweb-vcwiki
  version: "0.3.0"

source:
  fn: cubicweb-vcwiki-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-vcwiki/cubicweb-vcwiki-0.3.0.tar.gz
  md5: b80dc2c094b0593e8ec2dc2e5de7395e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-vcwiki = cubicweb-vcwiki:main
    #
    # Would create an entry point called cubicweb-vcwiki that calls cubicweb-vcwiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.2
    - docutils >=0.8.1
    - cubicweb-preview >=1.1.0
    - cubicweb-vcsfile >=2.0.0
    - hglib >=1.2.0

  run:
    - python
    - cubicweb >=3.17.2
    - docutils >=0.8.1
    - cubicweb-preview >=1.1.0
    - cubicweb-vcsfile >=2.0.0
    - hglib >=1.2.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-vcwiki
  license: LGPL
  summary: 'Version controlled wiki component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
