package:
  name: horae.calendar
  version: "1.0a1"

source:
  fn: horae.calendar-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.calendar/horae.calendar-1.0a1.tar.gz
  md5: 18289f5202585277eea076f662b25ed2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.calendar = horae.calendar:main
    #
    # Would create an entry point called horae.calendar that calls horae.calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - fanstatic
    - zope.fanstatic
    - horae.core
    - horae.layout

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - fanstatic
    - zope.fanstatic
    - horae.core
    - horae.layout

test:
  # Python imports
  imports:
    - horae
    - horae.calendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides generic calendar views (month, week and day) using horae.timeaware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
