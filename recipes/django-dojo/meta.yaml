package:
  name: django-dojo
  version: "0.0.1"

source:
  fn: django-dojo-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dojo/django-dojo-0.0.1.tar.gz
  md5: 78f876128c0b94e7c47b32568c733e2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dojo = django-dojo:main
    #
    # Would create an entry point called django-dojo that calls django-dojo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - dojo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skibblenybbles/django-dojo
  license: BSD License
  summary: 'Add Dojo to your Django projects and run Dojo builds with manage.py commands'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
