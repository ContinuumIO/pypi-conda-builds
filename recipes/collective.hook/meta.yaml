package:
  name: collective.hook
  version: "1.0b1"

source:
  fn: collective.hook-1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.hook/collective.hook-1.0b1.zip
  md5: fdee0f632656f5cc31a039e8a858074c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.hook = collective.hook:main
    #
    # Would create an entry point called collective.hook that calls collective.hook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.event
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.event
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.hook
    - collective.hook.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.hook
  license: GPL
  summary: 'A very simple hook system for zope application by Makina-Corpus.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
