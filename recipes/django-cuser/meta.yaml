package:
  name: django-cuser
  version: "2014.9.28"

source:
  fn: django-cuser-2014.9.28.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cuser/django-cuser-2014.9.28.tar.gz
  md5: 8fe7840e91bf9acfc66b842c8e87db20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cuser = django-cuser:main
    #
    # Would create an entry point called django-cuser that calls django-cuser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - cuser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Alir3z4/django-cuser
  license: BSD License
  summary: 'Middleware to make user information always available.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
