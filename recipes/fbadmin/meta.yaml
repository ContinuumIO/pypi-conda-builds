package:
  name: fbadmin
  version: "0.1.0.5"

source:
  fn: fbadmin-0.1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fbadmin/fbadmin-0.1.0.5.tar.gz
  md5: ebdbe37bff9f2d4ae34cfbeac353cd5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fbadmin = fbadmin:main
    #
    # Would create an entry point called fbadmin that calls fbadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - selenium

  run:
    - python
    - selenium

test:
  # Python imports
  imports:
    - fbadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thekindlyone/fbadmin
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'A python library to automate facebook group administration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
