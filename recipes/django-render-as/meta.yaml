package:
  name: django-render-as
  version: "0.1"

source:
  fn: django-render-as-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-render-as/django-render-as-0.1.tar.gz
  md5: 516e46145911bb6bbbb7e5516f95c5a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-render-as = django-render-as:main
    #
    # Would create an entry point called django-render-as that calls django-render-as.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - render_as
    - render_as/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Template rendering indirector based on object class'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
