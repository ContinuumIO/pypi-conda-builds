package:
  name: customizeme
  version: "0.0dev"

source:
  fn: CustomizeMe-0.0dev.tar.gz
  url: https://pypi.python.org/packages/source/C/CustomizeMe/CustomizeMe-0.0dev.tar.gz
  md5: 22bfd33911aa66177feee0f2ae2e4ada
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - customizeme = customizeme:main
    #
    # Would create an entry point called customizeme that calls customizeme.main()

    - customizeme = customizeme.servidor:iniciar

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - customizeme

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - customizeme --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
