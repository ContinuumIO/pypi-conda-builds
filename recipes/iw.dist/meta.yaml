package:
  name: iw.dist
  version: "0.1.8"

source:
  fn: iw.dist-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.dist/iw.dist-0.1.8.tar.gz
  md5: 211c8302406c70550792617ebaaa747b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.dist = iw.dist:main
    #
    # Would create an entry point called iw.dist that calls iw.dist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - iw
    - iw.dist
    - iw.dist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://ingeniweb.svn.sourceforge.net/svnroot/ingeniweb/iw.dist
  license: GPL
  summary: 'Distutils commands to upload files to several servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
