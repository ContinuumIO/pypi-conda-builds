package:
  name: clepy
  version: "0.3.23"

source:
  fn: clepy-0.3.23.tar.gz
  url: https://pypi.python.org/packages/source/c/clepy/clepy-0.3.23.tar.gz
  md5: 97ab4ac822e82949f436a373c10ba6fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clepy = clepy:main
    #
    # Would create an entry point called clepy that calls clepy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator >=3.0
    - nose
    - mock

  run:
    - python
    - decorator >=3.0
    - nose
    - mock

test:
  # Python imports
  imports:
    - clepy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/clepy
  license: MIT License
  summary: 'utilities from the Cleveland Python user group'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
