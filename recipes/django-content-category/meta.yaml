package:
  name: django-content-category
  version: "1.0.4"

source:
  fn: django-content-category-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-content-category/django-content-category-1.0.4.tar.gz
  md5: eddbb286c01d5bd85bcb59c39dfebb57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-content-category = django-content-category:main
    #
    # Would create an entry point called django-content-category that calls django-content-category.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - category
    - category.templatetags
    - category.tests
    - category.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yunmanger1/django-content-category/
  license: WTFPL
  summary: 'This django application gives category functionality. Can be used by other applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
