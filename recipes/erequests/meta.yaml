package:
  name: erequests
  version: "0.4.1"

source:
  fn: erequests-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/e/erequests/erequests-0.4.1.tar.gz
  md5: 0168a61a8349ba2a4e75145868d31bcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - erequests = erequests:main
    #
    # Would create an entry point called erequests that calls erequests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eventlet
    - requests

  run:
    - python
    - eventlet
    - requests

test:
  # Python imports
  imports:
    - erequests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saghul/erequests
  license: BSD License
  summary: 'Requests + Eventlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
