package:
  name: veritranspay
  version: "0.6"

source:
  fn: VeritransPay-0.6.tar.gz
  url: https://pypi.python.org/packages/source/V/VeritransPay/VeritransPay-0.6.tar.gz
  md5: 791b9594d1d55e81d662e5396fa9cae8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - veritranspay = veritranspay:main
    #
    # Would create an entry point called veritranspay that calls veritranspay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0

  run:
    - python
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - veritranspay
    - veritranspay.response

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/derekjamescurtis/veritranspay
  license: BSD
  summary: 'Veritrans.co.id API Client Library '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
