package:
  name: xstatic-jquery-ui
  version: "1.11.0.1"

source:
  fn: XStatic-jquery-ui-1.11.0.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-jquery-ui/XStatic-jquery-ui-1.11.0.1.tar.gz
  md5: 03d8ea7a0dab29d548e7bc195703b04f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-jquery-ui = xstatic-jquery-ui:main
    #
    # Would create an entry point called xstatic-jquery-ui that calls xstatic-jquery-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xstatic-jquery

  run:
    - python
    - xstatic-jquery

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.jquery_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jqueryui.com/
  license: (same as jquery-ui)
  summary: 'jquery-ui 1.11.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
