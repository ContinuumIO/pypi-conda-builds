package:
  name: lisa-plugin-shopping
  version: "1.0.4"

source:
  fn: lisa-plugin-Shopping-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/l/lisa-plugin-Shopping/lisa-plugin-Shopping-1.0.4.tar.gz
  md5: 66fe4376160698a60c8b25021807dec9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lisa-plugin-shopping = lisa-plugin-shopping:main
    #
    # Would create an entry point called lisa-plugin-shopping that calls lisa-plugin-shopping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.2,<1.7
    - django-tastypie >=0.11.0

  run:
    - python
    - django >=1.6.2,<1.7
    - django-tastypie >=0.11.0

test:
  # Python imports
  imports:
    - lisa.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.lisa-project.net
  license: MIT License
  summary: 'LISA home automation system - Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
