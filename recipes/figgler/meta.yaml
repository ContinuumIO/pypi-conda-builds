package:
  name: figgler
  version: "1.1.4"

source:
  fn: figgler-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/f/figgler/figgler-1.1.4.tar.gz
  md5: 8e0e881e71a4a193c5a85321c31676e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - figgler = figgler:main
    #
    # Would create an entry point called figgler that calls figgler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - figgler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/karolmajta/figgler-python
  license: MIT
  summary: 'Utilities for reading environment variables in containers instrumented with fig.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
