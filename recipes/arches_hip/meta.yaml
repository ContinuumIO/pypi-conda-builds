package:
  name: arches_hip
  version: "1.0"

source:
  fn: arches_hip-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/arches_hip/arches_hip-1.0.0.tar.gz
  md5: de2125c7e2cba731aa0be1a897d2f1f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arches_hip = arches_hip:main
    #
    # Would create an entry point called arches_hip that calls arches_hip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - arches >=3.0

  run:
    - python
    - arches >=3.0

test:
  # Python imports
  imports:
    - arches_hip
    - arches_hip.logs
    - arches_hip.models
    - arches_hip.source_data
    - arches_hip.source_data.validation
    - arches_hip.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://archesproject.org/
  license: GNU AGPL
  summary: 'The Heritage Information Package (HIP)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
