package:
  name: hypem-python
  version: "1.0"

source:
  fn: hypem-python-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hypem-python/hypem-python-1.0.tar.gz
  md5: abb9d1d9164e8b741a9fe99b292e2264
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hypem-python = hypem-python:main
    #
    # Would create an entry point called hypem-python that calls hypem-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hypem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/systemizer/hypem-python
  license: UNKNOWN
  summary: 'Python Client to Hype Machine Public API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
