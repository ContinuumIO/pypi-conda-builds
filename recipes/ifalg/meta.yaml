package:
  name: ifalg
  version: "0.1"

source:
  fn: ifalg-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ifalg/ifalg-0.1.tar.gz
  md5: 08b9de3ba1c5be68b7b1642f48dd5084
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ifalg = ifalg:main
    #
    # Would create an entry point called ifalg that calls ifalg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi >=0.9.2
    - six >=1.9.0

  run:
    - python
    - cffi >=0.9.2
    - six >=1.9.0

test:
  # Python imports
  imports:
    - ifalg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/manologab/python-ifalg
  license: MIT License
  summary: 'Library to interface with the Linux kernel crypto API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
