package:
  name: guesser
  version: "0.0.1"

source:
  fn: guesser-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/guesser/guesser-0.0.1.tar.gz
  md5: 2ad4bc85c47d48e620f2d4fbb1491816
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guesser = guesser:main
    #
    # Would create an entry point called guesser that calls guesser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - guesser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spulec/guesser
  license: UNKNOWN
  summary: 'Guesser is a library that allows you to smartly guess values to find an unkownn boundary on a function.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
