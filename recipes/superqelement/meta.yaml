package:
  name: superqelement
  version: "0.6"

source:
  fn: superqelement-0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/superqelement/superqelement-0.6.tar.gz
  md5: 9bab6d6d0fc88e70df2b78a37e346229
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - superqelement = superqelement:main
    #
    # Would create an entry point called superqelement that calls superqelement.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - marektools
    - superqelement

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Tool which parsing data from request into Q elements for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
