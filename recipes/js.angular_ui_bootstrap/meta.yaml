package:
  name: js.angular_ui_bootstrap
  version: "0.6.0-2"

source:
  fn: js.angular_ui_bootstrap-0.6.0-2.tar.gz
  url: https://pypi.python.org/packages/source/j/js.angular_ui_bootstrap/js.angular_ui_bootstrap-0.6.0-2.tar.gz
  md5: 7807bc6db54e4d1c1c7814f547815ab4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.angular_ui_bootstrap = js.angular_ui_bootstrap:main
    #
    # Would create an entry point called js.angular_ui_bootstrap that calls js.angular_ui_bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.angular
    - js.bootstrap
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.angular
    - js.bootstrap
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.angular_ui_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abulte/js.angular_ui_bootstrap
  license: MIT
  summary: 'Fanstatic packaging of Angular UI Boostrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
