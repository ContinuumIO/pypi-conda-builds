package:
  name: django-blockedemails
  version: "0.1.0"

source:
  fn: django-blockedemails-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-blockedemails/django-blockedemails-0.1.0.tar.gz
  md5: a3772e8c48987edb960e8952869f33a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-blockedemails = django-blockedemails:main
    #
    # Would create an entry point called django-blockedemails that calls django-blockedemails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - blockedemails

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstufft/django-blockedemails/
  license: BSD License
  summary: 'A Django reusable app that provides a form field, models and validators for blocking email addresses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
