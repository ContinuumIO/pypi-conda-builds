package:
  name: experimental.aggressiveopaquespeedup
  version: "1.1"

source:
  fn: experimental.aggressiveopaquespeedup-1.1.zip
  url: https://pypi.python.org/packages/source/e/experimental.aggressiveopaquespeedup/experimental.aggressiveopaquespeedup-1.1.zip
  md5: abf27261476ec807b0e0f67e34ebdaf3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.aggressiveopaquespeedup = experimental.aggressiveopaquespeedup:main
    #
    # Would create an entry point called experimental.aggressiveopaquespeedup that calls experimental.aggressiveopaquespeedup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - experimental
    - experimental.aggressiveopaquespeedup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/experimental.aggressiveopaquespeedup
  license: GPL
  summary: 'A more aggressive opaquespeedup package to improve performance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
