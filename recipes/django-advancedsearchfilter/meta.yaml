package:
  name: django-advancedsearchfilter
  version: "0.2.1"

source:
  fn: django-advancedsearchfilter-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-advancedsearchfilter/django-advancedsearchfilter-0.2.1.tar.gz
  md5: 7285ba42adefe540de1908aac161d040
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-advancedsearchfilter = django-advancedsearchfilter:main
    #
    # Would create an entry point called django-advancedsearchfilter that calls django-advancedsearchfilter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - advancedsearchfilter
    - advancedsearchfilter.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wildcardcorp/django-advancedsearchfilter
  license: MIT License
  summary: 'Advanced Search Filter for Django Contrib Admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
