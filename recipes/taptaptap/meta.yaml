package:
  name: taptaptap
  version: "1.1.3"

source:
  fn: taptaptap-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/t/taptaptap/taptaptap-1.1.3.tar.gz
  md5: 286d8461d61e648ddb707f349a2c4ce5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taptaptap = taptaptap:main
    #
    # Would create an entry point called taptaptap that calls taptaptap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - yamlish

  run:
    - python
    - yamlish

test:
  # Python imports
  imports:
    - taptaptap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lukas-prokop.at/proj/taptaptap/
  license: BSD License
  summary: 'Test Anything Protocol handling for cats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
