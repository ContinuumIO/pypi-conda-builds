package:
  name: abstract_rendering
  version: "0.5.1"

source:
  fn: abstract_rendering-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/a/abstract_rendering/abstract_rendering-0.5.1.tar.gz
  md5: 164d446ab5c01ae9d4308767d35093ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abstract_rendering = abstract_rendering:main
    #
    # Would create an entry point called abstract_rendering that calls abstract_rendering.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - abstract_rendering

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JosephCottam/AbstractRendering
  license: UNKNOWN
  summary: 'Rendering as a binning process'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
