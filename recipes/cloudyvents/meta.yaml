package:
  name: cloudyvents
  version: "0.1"

source:
  fn: cloudyvents-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudyvents/cloudyvents-0.1.tar.gz
  md5: 6c0a8205c31aa8c39b572e8209c32114
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudyvents = cloudyvents:main
    #
    # Would create an entry point called cloudyvents that calls cloudyvents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - nose

test:
  # Python imports
  imports:
    - cloudyvents
    - cloudyvents.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nimbusproject.org/
  license: Apache2
  summary: 'An event management library for the EPU services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
