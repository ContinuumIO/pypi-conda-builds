package:
  name: terminal_velocity
  version: "0.1.8"

source:
  fn: terminal_velocity-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/t/terminal_velocity/terminal_velocity-0.1.8.tar.gz
  md5: 8fb7ed76df7b65a442ff32ec97a65b74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - terminal_velocity = terminal_velocity:main
    #
    # Would create an entry point called terminal_velocity that calls terminal_velocity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urwid ==1.1.1
    - chardet ==2.1.1

  run:
    - python
    - urwid ==1.1.1
    - chardet ==2.1.1

test:
  # Python imports
  imports:
    - terminal_velocity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://seanh.github.com/terminal_velocity/
  license: GNU General Public License, Version 3
  summary: 'A fast note-taking app for the UNIX terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
