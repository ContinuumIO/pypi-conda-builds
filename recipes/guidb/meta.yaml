package:
  name: guidb
  version: "0.1.5"

source:
  fn: guidb-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/g/guidb/guidb-0.1.5.tar.gz
  md5: 4ad1c27408eeb54ae711b8428cc07293
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guidb = guidb:main
    #
    # Would create an entry point called guidb that calls guidb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq
    - protobuf
    - leveldb

  run:
    - python
    - pyzmq
    - protobuf
    - leveldb

test:
  # Python imports
  imports:
    - guidb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://guillemborrell.es
  license: BSD License
  summary: 'Very simple parallel database based on leveldb and zeromq.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
