package:
  name: gxmail
  version: "1.1.6"

source:
  fn: gxmail-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/g/gxmail/gxmail-1.1.6.tar.gz
  md5: aeeaf4c03288b9d7c3506a6ba494f30b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gxmail = gxmail:main
    #
    # Would create an entry point called gxmail that calls gxmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gxmail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GaboXandre/gxmail
  license: LICENSE.txt
  summary: 'A simple smtp user agent designed to send emails from the linux command line.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
