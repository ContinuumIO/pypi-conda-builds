package:
  name: ajenti.plugin.augeas
  version: "0.6"

source:
  fn: ajenti.plugin.augeas-0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.augeas/ajenti.plugin.augeas-0.6.tar.gz
  md5: a00dcf60dc93dacdfafdcec2b2d0be7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.augeas = ajenti.plugin.augeas:main
    #
    # Would create an entry point called ajenti.plugin.augeas that calls ajenti.plugin.augeas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core
    - python-augeas

  run:
    - python
    - aj
    - ajenti.plugin.core
    - python-augeas

test:
  # Python imports
  imports:
    - ajenti_plugin_augeas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Augeas API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
