package:
  name: better_od
  version: "0.2.1"

source:
  fn: better_od-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/better_od/better_od-0.2.1.tar.gz
  md5: b065350cbb23b85cf7275ec9f6df214c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - better_od = better_od:main
    #
    # Would create an entry point called better_od that calls better_od.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - better_od
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/therealfakemoot/BetterOrderedDict/
  license: UNKNOWN
  summary: 'A more flexible implementation of OrderedDict'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
