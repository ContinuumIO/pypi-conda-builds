package:
  name: django-sites-templatetags
  version: "0.0.3"

source:
  fn: Django-Sites-Templatetags-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Sites-Templatetags/Django-Sites-Templatetags-0.0.3.tar.gz
  md5: ca07d33f63305c8048aeb613614f9381
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sites-templatetags = django-sites-templatetags:main
    #
    # Would create an entry point called django-sites-templatetags that calls django-sites-templatetags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sites_templatetags
    - sites_templatetags.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RichardOfWard/django-sites-templatetags
  license: MIT
  summary: 'The missing templatetags to get the current site from django.contrib.sites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
