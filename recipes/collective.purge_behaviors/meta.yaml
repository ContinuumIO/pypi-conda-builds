package:
  name: collective.purge_behaviors
  version: "0.1.1"

source:
  fn: collective.purge_behaviors-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.purge_behaviors/collective.purge_behaviors-0.1.1.tar.gz
  md5: 6f3e039e240937438bbb355195bc0855
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.purge_behaviors = collective.purge_behaviors:main
    #
    # Would create an entry point called collective.purge_behaviors that calls collective.purge_behaviors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity

test:
  # Python imports
  imports:
    - collective
    - collective.purge_behaviors
    - collective.purge_behaviors.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.purge_behaviors
  license: BSD
  summary: 'A set of dexterity behaviors for Plone that are assignable to custom dexterity types for complex system setups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
