package:
  name: configcascade
  version: "1.0.0"

source:
  fn: configcascade-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/configcascade/configcascade-1.0.0.tar.gz
  md5: 3445540f6ff60b5a20e82d2f8be82e9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configcascade = configcascade:main
    #
    # Would create an entry point called configcascade that calls configcascade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - configcascade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/felixcarmona/configcascade
  license: UNKNOWN
  summary: 'A simple Configuration System which allows you to import and override or merge configuration parameters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
