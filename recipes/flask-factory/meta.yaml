package:
  name: flask-factory
  version: "0.1.0dev"

source:
  fn: Flask-Factory-0.1.0dev.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Factory/Flask-Factory-0.1.0dev.tar.gz
  md5: c68afb6597c93dc11ff3c404b3959931
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-factory = flask-factory:main
    #
    # Would create an entry point called flask-factory that calls flask-factory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1

  run:
    - python
    - flask >=0.10.1

test:
  # Python imports
  imports:
    - flask_factory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kroisse/flask-factory
  license: BSD License
  summary: 'Provide a general-purpose application factory of the Flask application, and the configurator that is independent of the app object.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
