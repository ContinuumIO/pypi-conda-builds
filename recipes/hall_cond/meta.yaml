package:
  name: hall_cond
  version: "0.1.0"

source:
  fn: hall_cond-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hall_cond/hall_cond-0.1.0.tar.gz
  md5: ece3727584e2092426c55a2146e40507
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hall_cond = hall_cond:main
    #
    # Would create an entry point called hall_cond that calls hall_cond.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.7.1
    - matplotlib >=1.2.1

  run:
    - python
    - numpy >=1.7.1
    - matplotlib >=1.2.1

test:
  # Python imports
  imports:
    - hall_cond

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3 or later
  summary: 'A Lightweight Hall Conductivity Computational Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
