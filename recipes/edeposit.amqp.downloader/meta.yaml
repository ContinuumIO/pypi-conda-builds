package:
  name: edeposit.amqp.downloader
  version: "0.3.1"

source:
  fn: edeposit.amqp.downloader-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/e/edeposit.amqp.downloader/edeposit.amqp.downloader-0.3.1.tar.gz
  md5: d02423f87e8291dddca18f85fea98a68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edeposit.amqp.downloader = edeposit.amqp.downloader:main
    #
    # Would create an entry point called edeposit.amqp.downloader that calls edeposit.amqp.downloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - edeposit
    - edeposit.amqp
    - edeposit.amqp.downloader
    - edeposit.amqp.downloader.structures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edeposit/edeposit.amqp.downloader/
  license: GNU General Public License v2 or later (GPLv2+)
  summary: "E-Deposit's AMQP bindings to download workers."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
