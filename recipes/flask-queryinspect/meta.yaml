package:
  name: flask-queryinspect
  version: "0.1.2"

source:
  fn: Flask-QueryInspect-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-QueryInspect/Flask-QueryInspect-0.1.2.tar.gz
  md5: a90ea874bcef0b6c118f86ba60ab9719
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-queryinspect = flask-queryinspect:main
    #
    # Would create an entry point called flask-queryinspect that calls flask-queryinspect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - sqlalchemy

  run:
    - python
    - flask
    - sqlalchemy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/noise/flask-queryinspect
  license: MIT License
  summary: 'Flask extension to provide metrics on SQL queries per request.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
