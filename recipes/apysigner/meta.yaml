package:
  name: apysigner
  version: "2.1.0"

source:
  fn: apysigner-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/apysigner/apysigner-2.1.0.tar.gz
  md5: 61b7fc4d830691e3300327d99ce0f1b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apysigner = apysigner:main
    #
    # Would create an entry point called apysigner that calls apysigner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - apysigner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madisona/apysigner
  license: BSD License
  summary: 'Basic API request signing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
