package:
  name: django-bootstrap-typeahead
  version: "1.1.5"

source:
  fn: django-bootstrap-typeahead-1.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-typeahead/django-bootstrap-typeahead-1.1.5.tar.gz
  md5: d87caae403754498a1e5eef5933156a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-typeahead = django-bootstrap-typeahead:main
    #
    # Would create an entry point called django-bootstrap-typeahead that calls django-bootstrap-typeahead.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_bootstrap_typeahead

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://thegoods.aj7may.com/django-bootstrap-typeahead
  license: MIT License
  summary: 'Convert django model choice fields into typeahead fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
