package:
  name: sqlalchemy-batteries
  version: "0.4.5"

source:
  fn: sqlalchemy-batteries-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-batteries/sqlalchemy-batteries-0.4.5.tar.gz
  md5: 79e087893cd9679280bdf1c939f25056
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-batteries = sqlalchemy-batteries:main
    #
    # Would create an entry point called sqlalchemy-batteries that calls sqlalchemy-batteries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - geoalchemy2
    - shapely
    - six
    - python-dateutil <=1.5

  run:
    - python
    - sqlalchemy
    - geoalchemy2
    - shapely
    - six
    - python-dateutil <=1.5

test:
  # Python imports
  imports:
    - batteries
    - batteries.model
    - batteries.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jessedhillon/batteries
  license: UNKNOWN
  summary: 'Various batteries for SQLAlchemy models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
