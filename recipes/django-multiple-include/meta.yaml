package:
  name: django-multiple-include
  version: "0.0.1"

source:
  fn: django-multiple-include-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multiple-include/django-multiple-include-0.0.1.tar.gz
  md5: a13ba864e5eb1855bd44365cdb3d9a6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multiple-include = django-multiple-include:main
    #
    # Would create an entry point called django-multiple-include that calls django-multiple-include.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - multiple_include
    - multiple_include.templatetags
    - multiple_include.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fcurella/django-multiple-include
  license: MIT License
  summary: 'A version of ``{% include %}`` that accepts multiple template names.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
