package:
  name: collective.plonebookmarklets
  version: "3.2"

source:
  fn: collective.plonebookmarklets-3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.plonebookmarklets/collective.plonebookmarklets-3.2.tar.gz
  md5: c8829a8ccf2e3c878c41928cab5177ef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.plonebookmarklets = collective.plonebookmarklets:main
    #
    # Would create an entry point called collective.plonebookmarklets that calls collective.plonebookmarklets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.plonebookmarklets
    - collective.plonebookmarklets.browser
    - collective.plonebookmarklets.tests
    - collective.plonebookmarklets.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plonebookmarklets
  license: GPL
  summary: "This product installs an 'Add Bookmarklet' document_action to your Plone site"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
