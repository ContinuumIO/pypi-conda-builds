package:
  name: gargant.dispatch
  version: "0.1"

source:
  fn: gargant.dispatch-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gargant.dispatch/gargant.dispatch-0.1.tar.gz
  md5: 1738586d57e01637e86b21a71f1ae56d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gargant.dispatch = gargant.dispatch:main
    #
    # Would create an entry point called gargant.dispatch that calls gargant.dispatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gargant
    - gargant.dispatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hirokiky/gargant.dispatch
  license: MIT License
  summary: 'Flexible dispatcher for WSGI Applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
