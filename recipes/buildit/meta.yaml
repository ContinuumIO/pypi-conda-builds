package:
  name: buildit
  version: "0.81"

source:
  fn: buildit-0.81.tar.gz
  url: https://pypi.python.org/packages/source/b/buildit/buildit-0.81.tar.gz
  md5: d6115cc94f2fac27b6cce58e954c9e3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildit = buildit:main
    #
    # Would create an entry point called buildit that calls buildit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - ushlex

  run:
    - python
    - ushlex

test:
  # Python imports
  imports:
    - buildit
    - buildit.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mixmastamyk/buildit
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A simple build tool for small projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
