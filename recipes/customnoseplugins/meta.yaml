package:
  name: customnoseplugins
  version: "3.0.9"

source:
  fn: customnoseplugins-3.0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/customnoseplugins/customnoseplugins-3.0.9.tar.gz
  md5: 8fc2966d794780ea3be47e5a6922ac3e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - customnoseplugins = customnoseplugins:main
    #
    # Would create an entry point called customnoseplugins that calls customnoseplugins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - customnoseplugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'test plan plugins for the nose testing framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
