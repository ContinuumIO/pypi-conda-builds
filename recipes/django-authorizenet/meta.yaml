package:
  name: django-authorizenet
  version: "2.0"

source:
  fn: django-authorizenet-2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-authorizenet/django-authorizenet-2.0.tar.gz
  md5: 72cc38dd91f60989f672160d3c4b003d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-authorizenet = django-authorizenet:main
    #
    # Would create an entry point called django-authorizenet that calls django-authorizenet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - authorizenet
    - authorizenet.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zen4ever/django-authorizenet/tree/master
  license: MIT License
  summary: 'Django and Authorize.NET payment gateway integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
