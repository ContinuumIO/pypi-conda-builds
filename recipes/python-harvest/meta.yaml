package:
  name: python-harvest
  version: "1.0.4"

source:
  fn: python-harvest-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/python-harvest/python-harvest-1.0.4.tar.gz
  md5: 5e93c21e2a8eef0192525bf1875d9c21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-harvest = python-harvest:main
    #
    # Would create an entry point called python-harvest that calls python-harvest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - harvest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lionheart/python-harvest
  license: MIT License
  summary: 'Harvest api client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
