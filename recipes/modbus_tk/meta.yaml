package:
  name: modbus_tk
  version: "0.4.3"

source:
  fn: modbus_tk-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/m/modbus_tk/modbus_tk-0.4.3.tar.gz
  md5: 9441b1e1b5262bf4e752e20a9e82b098
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modbus_tk = modbus_tk:main
    #
    # Would create an entry point called modbus_tk that calls modbus_tk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - modbus_tk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/modbus-tk/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Implementation of modbus protocol in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
