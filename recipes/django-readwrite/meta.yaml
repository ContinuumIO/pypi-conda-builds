package:
  name: django-readwrite
  version: "0.0.1"

source:
  fn: django-readwrite-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-readwrite/django-readwrite-0.0.1.tar.gz
  md5: d451b26ac49e68a4140f74383236c6f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-readwrite = django-readwrite:main
    #
    # Would create an entry point called django-readwrite that calls django-readwrite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_readwrite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apn-online/django-readwrite
  license: MIT
  summary: 'Read/write database splitting for Django, based on the HTTP request method.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
