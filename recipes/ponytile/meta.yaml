package:
  name: ponytile
  version: "0.1.1"

source:
  fn: Ponytile-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Ponytile/Ponytile-0.1.1.tar.gz
  md5: 4476b989247a19c68d6ff8bc4d4c63c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ponytile = ponytile:main
    #
    # Would create an entry point called ponytile that calls ponytile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pillow >=1.0

  run:
    - python
    - pillow >=1.0

test:
  # Python imports
  imports:
    - ponytile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Xowap/Ponytile
  license: LICENSE.txt
  summary: 'A library bundled with a CLI tool to generate CSS sprites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
