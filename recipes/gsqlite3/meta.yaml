package:
  name: gsqlite3
  version: "0.1.2"

source:
  fn: gsqlite3-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gsqlite3/gsqlite3-0.1.2.tar.gz
  md5: 8a8e2132dffa14d7bef5a5823da3223a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gsqlite3 = gsqlite3:main
    #
    # Would create an entry point called gsqlite3 that calls gsqlite3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=1.0.1

  run:
    - python
    - setuptools
    - gevent >=1.0.1

test:
  # Python imports
  imports:
    - gsqlite3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gilesbrown/gsqlite3
  license: BSD License
  summary: "A wrapper for 'sqlite3' to make it play better with 'gevent'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
