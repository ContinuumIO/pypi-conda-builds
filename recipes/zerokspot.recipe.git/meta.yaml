package:
  name: zerokspot.recipe.git
  version: "0.6.1"

source:
  fn: zerokspot.recipe.git-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zerokspot.recipe.git/zerokspot.recipe.git-0.6.1.tar.gz
  md5: cf5c81bb6eaf7603ae7eab1765039582
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zerokspot.recipe.git = zerokspot.recipe.git:main
    #
    # Would create an entry point called zerokspot.recipe.git that calls zerokspot.recipe.git.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - zerokspot
    - zerokspot.recipe
    - zerokspot.recipe.git

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zerok/zerokspot.gitrecipe/
  license: BSD License
  summary: 'Simple zc.buildout recipe for sources in a git repository'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
