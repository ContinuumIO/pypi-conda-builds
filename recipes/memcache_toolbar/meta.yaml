package:
  name: memcache_toolbar
  version: "0.5.6"

source:
  fn: memcache_toolbar-0.5.6.tar.gz
  url: https://pypi.python.org/packages/source/m/memcache_toolbar/memcache_toolbar-0.5.6.tar.gz
  md5: 35d8c45cbee0d5138b1d2c04ebc187e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memcache_toolbar = memcache_toolbar:main
    #
    # Would create an entry point called memcache_toolbar that calls memcache_toolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - memcache_toolbar
    - memcache_toolbar.panels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ross/memcache-debug-panel
  license: UNKNOWN
  summary: 'Add-on for Django Debug Toolbar for tracking memcached usage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
