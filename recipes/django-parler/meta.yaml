package:
  name: django-parler
  version: "1.4"

source:
  fn: django-parler-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-parler/django-parler-1.4.tar.gz
  md5: a95ce855fbdc8fe0f809c321ba6695b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-parler = django-parler:main
    #
    # Would create an entry point called django-parler that calls django-parler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - parler
    - parler.contrib
    - parler.templatetags
    - parler.tests
    - parler.tests.testapp
    - parler.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/django-parler
  license: Apache Software License
  summary: 'Simple Django model translations without nasty hacks, featuring nice admin integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
