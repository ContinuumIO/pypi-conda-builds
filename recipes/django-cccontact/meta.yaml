package:
  name: django-cccontact
  version: "1.6.4"

source:
  fn: django-cccontact-1.6.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cccontact/django-cccontact-1.6.4.tar.gz
  md5: 7c628f699a20adc6dcee015648b1655b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cccontact = django-cccontact:main
    #
    # Would create an entry point called django-cccontact that calls django-cccontact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-mailer ==0.2a1

  run:
    - python
    - django-mailer ==0.2a1

test:
  # Python imports
  imports:
    - cccontact
    - cccontact.templatetags
    - cccontact.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/designcc/django-cccontact
  license: BSD 3 Clause
  summary: 'A minimal contact form application for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
