package:
  name: boundary_cli
  version: "0.0.1"

source:
  fn: boundary_cli-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/boundary_cli/boundary_cli-0.0.1.tar.gz
  md5: bf0472a9941f01a8044dffb922a5504a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boundary_cli = boundary_cli:main
    #
    # Would create an entry point called boundary_cli that calls boundary_cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.3.0

  run:
    - python
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - boundary_cli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Command line interface to Boundary REST APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
