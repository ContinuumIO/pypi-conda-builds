package:
  name: wtforms
  version: "2.0.2"

source:
  fn: WTForms-2.0.2.zip
  url: https://pypi.python.org/packages/source/W/WTForms/WTForms-2.0.2.zip
  md5: 613cf723ab40537705bec02733c78d95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms = wtforms:main
    #
    # Would create an entry point called wtforms that calls wtforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - babel >=1.3
    - ordereddict

  run:
    - python
    - babel >=1.3
    - ordereddict

test:
  # Python imports
  imports:
    - wtforms
    - wtforms.csrf
    - wtforms.ext
    - wtforms.ext.appengine
    - wtforms.ext.csrf
    - wtforms.ext.dateutil
    - wtforms.ext.django
    - wtforms.ext.django.templatetags
    - wtforms.ext.i18n
    - wtforms.ext.sqlalchemy
    - wtforms.fields
    - wtforms.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wtforms.simplecodes.com/
  license: BSD License
  summary: 'A flexible forms validation and rendering library for python web development.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
