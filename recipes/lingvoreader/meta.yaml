package:
  name: lingvoreader
  version: "0.1.4"

source:
  fn: lingvoreader-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/l/lingvoreader/lingvoreader-0.1.4.tar.gz
  md5: 88684484ae159eda39ff67a96f46af9a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lingvoreader = lingvoreader:main
    #
    # Would create an entry point called lingvoreader that calls lingvoreader.main()

    - lsdreader = lingvoreader.lsdreader:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lingvoreader
    - test

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - lsdreader --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sv99/lsdreader
  license: UNKNOWN
  summary: 'Lingvo X5 lsd reader Utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
