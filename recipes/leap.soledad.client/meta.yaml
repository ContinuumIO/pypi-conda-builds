package:
  name: leap.soledad.client
  version: "0.6.3"

source:
  fn: leap.soledad.client-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/l/leap.soledad.client/leap.soledad.client-0.6.3.tar.gz
  md5: dde00218a4d81e6670b7df5b556d43c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leap.soledad.client = leap.soledad.client:main
    #
    # Would create an entry point called leap.soledad.client that calls leap.soledad.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysqlcipher
    - simplejson
    - u1db
    - scrypt
    - pycryptopp
    - cchardet
    - taskthread
    - zope.proxy
    - leap.soledad.common >=0.3.8
    - oauth
    - pysqlite

  run:
    - python
    - pysqlcipher
    - simplejson
    - u1db
    - scrypt
    - pycryptopp
    - cchardet
    - taskthread
    - zope.proxy
    - leap.soledad.common >=0.3.8
    - oauth
    - pysqlite

test:
  # Python imports
  imports:
    - leap
    - leap.soledad
    - leap.soledad.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://leap.se/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Synchronization of locally encrypted data among devices (client components).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
