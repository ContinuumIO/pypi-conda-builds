package:
  name: brightcove
  version: "0.2"

source:
  fn: brightcove-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/brightcove/brightcove-0.2.tar.gz
  md5: 20f664b7832ccc1ac807d8f50fa18471
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brightcove = brightcove:main
    #
    # Would create an entry point called brightcove that calls brightcove.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brightcove
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3
  summary: 'A python wrapper for the Brightcove read-only API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
