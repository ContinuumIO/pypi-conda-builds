package:
  name: pennyblack
  version: "0.3.5"

source:
  fn: pennyblack-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pennyblack/pennyblack-0.3.5.tar.gz
  md5: 5c967de8605b1d5aacbf96bbfc6361d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pennyblack = pennyblack:main
    #
    # Would create an entry point called pennyblack that calls pennyblack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pennyblack
    - pennyblack.content
    - pennyblack.management
    - pennyblack.management.commands
    - pennyblack.models
    - pennyblack.module
    - pennyblack.module.subscriber
    - pennyblack.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/allink/pennyblack/
  license: BSD License
  summary: 'django based newsletter toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
