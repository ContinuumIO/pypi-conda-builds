package:
  name: django-featured-item
  version: "0.4.1"

source:
  fn: django-featured-item-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-featured-item/django-featured-item-0.4.1.tar.gz
  md5: 941fa9fd9b079c7d243b617ca8f84332
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-featured-item = django-featured-item:main
    #
    # Would create an entry point called django-featured-item that calls django-featured-item.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - featureditem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-featured-item
  license: UNKNOWN
  summary: 'Mark a single record in a model as featured'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
