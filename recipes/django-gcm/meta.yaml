package:
  name: django-gcm
  version: "1.0.6"

source:
  fn: django-gcm-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gcm/django-gcm-1.0.6.tar.gz
  md5: 36d694c70afd56610fd75e08b31ece2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gcm = django-gcm:main
    #
    # Would create an entry point called django-gcm that calls django-gcm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-tastypie >=0.10.0
    - pytz >=2013.8
    - requests >=1.2.0

  run:
    - python
    - django >=1.6
    - django-tastypie >=0.10.0
    - pytz >=2013.8
    - requests >=1.2.0

test:
  # Python imports
  imports:
    - gcm
    - gcm.management
    - gcm.management.commands
    - gcm.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bogdal/django-gcm
  license: BSD License
  summary: 'Google Cloud Messaging Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
