package:
  name: botbits
  version: "0.0.1"

source:
  fn: botbits-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/botbits/botbits-0.0.1.tar.gz
  md5: 0d134d9362f9b5d857894d2672096d78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - botbits = botbits:main
    #
    # Would create an entry point called botbits that calls botbits.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - greenlet ==0.4.2
    - gevent ==1.0
    - tweepy
    - pytumblr
    - geventirc

  run:
    - python
    - greenlet ==0.4.2
    - gevent ==1.0
    - tweepy
    - pytumblr
    - geventirc

test:
  # Python imports
  imports:
    - botbits
    - botbits/conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/awentzonline/botbits
  license: MIT
  summary: 'Bits for building bots.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
