package:
  name: django-profiler
  version: "2.0.0"

source:
  fn: django-profiler-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-profiler/django-profiler-2.0.0.tar.gz
  md5: 1704a167cb0dadb1f77652d923d09032
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-profiler = django-profiler:main
    #
    # Would create an entry point called django-profiler that calls django-profiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - profiling

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codescale.net/en/community#django-profiler
  license: BSD License
  summary: 'util for profiling python code mainly in django projects, but can be used also on ordinary python code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
