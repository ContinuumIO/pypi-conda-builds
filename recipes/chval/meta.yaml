package:
  name: chval
  version: "0.6.7"

source:
  fn: chval-0.6.7.tar.gz
  url: https://pypi.python.org/packages/source/c/chval/chval-0.6.7.tar.gz
  md5: 8fbd8476f2679c9bd400070d2aa1f9cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chval = chval:main
    #
    # Would create an entry point called chval that calls chval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chval
    - chval_core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://forge.spalax.fr.eu.org/chval
  license: GPLv3 or any later version
  summary: 'Wrapper for getmail, providing daemon mode and encrypted passwords.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
