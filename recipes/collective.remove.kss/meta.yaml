package:
  name: collective.remove.kss
  version: "1.0"

source:
  fn: collective.remove.kss-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.remove.kss/collective.remove.kss-1.0.zip
  md5: 25046583ef7f06f3a3ac5f2511ea9a88
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.remove.kss = collective.remove.kss:main
    #
    # Would create an entry point called collective.remove.kss that calls collective.remove.kss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.remove
    - collective.remove.kss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.remove/kss/trunk
  license: GPL
  summary: 'Like all other collective.remove it remove a feature from Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
