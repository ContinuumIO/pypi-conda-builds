package:
  name: kytten
  version: "6.0.0"

source:
  fn: kytten-6.0.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kytten/kytten-6.0.0.tar.gz
  md5: 0e82644bf17acfd95c0a466220c75b5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kytten = kytten:main
    #
    # Would create an entry point called kytten that calls kytten.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyglet >=1.1.4

  run:
    - python
    - pyglet >=1.1.4

test:
  # Python imports
  imports:
    - editor
    - kytten

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kitanata/kytten
  license: BSD License
  summary: 'GUI Framework for Pyglet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
