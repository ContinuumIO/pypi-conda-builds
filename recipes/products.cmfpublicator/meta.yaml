package:
  name: products.cmfpublicator
  version: "1.3.1"

source:
  fn: Products.CMFPublicator-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.CMFPublicator/Products.CMFPublicator-1.3.1.tar.gz
  md5: 151edd864eb22f0cee6efe509e49d297
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.cmfpublicator = products.cmfpublicator:main
    #
    # Would create an entry point called products.cmfpublicator that calls products.cmfpublicator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.CMFPublicator
    - Products.CMFPublicator.browser
    - Products.CMFPublicator.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/Products.CMFPublicator
  license: GPL
  summary: 'CMFPublicator provides a METAL mechanism to publish contents in an arbitrary order, inside classic portlets or home boxes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
