package:
  name: message_amqp
  version: "0.9.4"

source:
  fn: message_amqp-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/m/message_amqp/message_amqp-0.9.4.tar.gz
  md5: b2d0216ce663b72833e67e8a240277f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - message_amqp = message_amqp:main
    #
    # Would create an entry point called message_amqp that calls message_amqp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - message_amqp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: git@github.com:borrey/message-amqp.git
  license: UNKNOWN
  summary: 'amqp setup for python nodejs and php'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
