package:
  name: django-model-admin-helper
  version: "0.2.1"

source:
  fn: django-model-admin-helper-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-admin-helper/django-model-admin-helper-0.2.1.tar.gz
  md5: 4ee8227d9a6041865ffdb66427947592
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-admin-helper = django-model-admin-helper:main
    #
    # Would create an entry point called django-model-admin-helper that calls django-model-admin-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Arpaso/django-model-admin-helper
  license: UNKNOWN
  summary: 'Admin helpers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
