package:
  name: googleads
  version: "3.3.0"

source:
  fn: googleads-3.3.0.tar.gz
  url: https://pypi.python.org/packages/source/g/googleads/googleads-3.3.0.tar.gz
  md5: 996f4a1465743f53ddb8973c64059855
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googleads = googleads:main
    #
    # Would create an entry point called googleads that calls googleads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - oauth2client
    - suds-jurko
    - pysocks
    - pytz
    - pyyaml

  run:
    - python
    - httplib2
    - oauth2client
    - suds-jurko
    - pysocks
    - pytz
    - pyyaml

test:
  # Python imports
  imports:
    - googleads

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/googleads/googleads-python-lib
  license: Apache License 2.0
  summary: 'Google Ads Python Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
