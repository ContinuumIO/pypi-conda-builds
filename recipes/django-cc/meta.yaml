package:
  name: django-cc
  version: "0.1.1"

source:
  fn: django-cc-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cc/django-cc-0.1.1.tar.gz
  md5: 0950598af451a90b8c549f76eeff5d55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cc = django-cc:main
    #
    # Would create an entry point called django-cc that calls django-cc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - celery

  run:
    - python
    - django >=1.7
    - celery

test:
  # Python imports
  imports:
    - bitcoinaddress
    - bitcoinrpc
    - cc
    - cc.migrations
    - jsonrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/limpbrains/django-cc
  license: MIT License
  summary: 'Django wallet for Bitcoin and other cryptocurrencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
