package:
  name: afn
  version: "0.1.2012.05.17.22.08"

source:
  fn: afn-0.1.2012.05.17.22.08.tar.gz
  url: https://pypi.python.org/packages/source/a/afn/afn-0.1.2012.05.17.22.08.tar.gz
  md5: 39e38bd0b21d89c4d756a4fba785ba1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - afn = afn:main
    #
    # Would create an entry point called afn that calls afn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - activehomed
    - afn
    - afn.backports
    - afn.bf
    - afn.dnsd
    - afn.dnsd.examples
    - afn.landfill
    - afn.tools
    - afn.ucreditu
    - afn.utils
    - afn.utils.cad
    - afntest
    - autobus
    - autobus2
    - autobus2.examples
    - autobus2.landfill
    - autobus2.proxy
    - autobus2.tools
    - libautobus
    - librtk
    - librtk.examples
    - librtk.schema
    - traytimer
    - uofu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A collection of useful tools and libraries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
