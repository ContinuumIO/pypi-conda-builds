package:
  name: minitage.recipe
  version: "1.41"

source:
  fn: minitage.recipe-1.41.zip
  url: https://pypi.python.org/packages/source/m/minitage.recipe/minitage.recipe-1.41.zip
  md5: e70cffd301b25a433142d06e7d7db449
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minitage.recipe = minitage.recipe:main
    #
    # Would create an entry point called minitage.recipe that calls minitage.recipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - minitage.core
    - iniparse
    - minitage.recipe.common
    - minitage.recipe.cmmi
    - minitage.recipe.du
    - minitage.recipe.egg
    - minitage.recipe.scripts

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - minitage.core
    - iniparse
    - minitage.recipe.common
    - minitage.recipe.cmmi
    - minitage.recipe.du
    - minitage.recipe.egg
    - minitage.recipe.scripts

test:
  # Python imports
  imports:
    - minitage
    - minitage.recipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/minitage.recipe
  license: BSD License
  summary: 'zc.buildout recipes to compile and install software or python packages and generate scripts or configuration files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
