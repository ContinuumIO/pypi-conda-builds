package:
  name: python-tuenti
  version: "1.1.0"

source:
  fn: python-tuenti-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-tuenti/python-tuenti-1.1.0.tar.gz
  md5: 995d26262c89927fdb867e5ff24d6400
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-tuenti = python-tuenti:main
    #
    # Would create an entry point called python-tuenti that calls python-tuenti.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.0.1

  run:
    - python
    - requests ==2.0.1

test:
  # Python imports
  imports:
    - tuenti

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/juanriaza/python-tuenti
  license: BSD License
  summary: 'Wrapper around the latest Tuenti API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
