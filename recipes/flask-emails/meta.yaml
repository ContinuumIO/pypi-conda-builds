package:
  name: flask-emails
  version: "0.4"

source:
  fn: flask-emails-0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-emails/flask-emails-0.4.tar.gz
  md5: 45d7f5464e9da99cb3c6a72e05551684
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-emails = flask-emails:main
    #
    # Would create an entry point called flask-emails that calls flask-emails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - emails

  run:
    - python
    - emails

test:
  # Python imports
  imports:
    - flask_emails

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lavr/flask-emails
  license: BSD License
  summary: 'Elegant and simple email library for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
