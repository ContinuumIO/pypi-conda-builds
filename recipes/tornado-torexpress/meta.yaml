package:
  name: tornado-torexpress
  version: "0.1.5"

source:
  fn: tornado-torexpress-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-torexpress/tornado-torexpress-0.1.5.tar.gz
  md5: 8747d0c5f144343c54064d5f261ff962
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-torexpress = tornado-torexpress:main
    #
    # Would create an entry point called tornado-torexpress that calls tornado-torexpress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - tornado >=3.1.1
    - sqlalchemy >=0.8.2
    - simplejson >=2.3.2
    - pyyaml >=3.10

  run:
    - python
    - tornado >=3.1.1
    - sqlalchemy >=0.8.2
    - simplejson >=2.3.2
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - torexpress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/archsh/tornado-torexpress
  license: Public domain
  summary: 'A RESTful extention on Tornado. Mingcai SHEN <archsh@gmail.com> '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
