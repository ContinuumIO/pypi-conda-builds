package:
  name: c2.patch.dateforlisting
  version: "1.0b5"

source:
  fn: c2.patch.dateforlisting-1.0b5.tar.gz
  url: https://pypi.python.org/packages/source/c/c2.patch.dateforlisting/c2.patch.dateforlisting-1.0b5.tar.gz
  md5: 12cbac3118b11b4af4b10fa76dcd0d2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2.patch.dateforlisting = c2.patch.dateforlisting:main
    #
    # Would create an entry point called c2.patch.dateforlisting that calls c2.patch.dateforlisting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - c2.patch.contentslist

  run:
    - python
    - setuptools
    - setuptools
    - c2.patch.contentslist

test:
  # Python imports
  imports:
    - c2
    - c2.patch
    - c2.patch.dateforlisting
    - c2.patch.dateforlisting.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cmscom.jp
  license: GPL
  summary: 'This patch product is to show effective contents on folder contents.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
