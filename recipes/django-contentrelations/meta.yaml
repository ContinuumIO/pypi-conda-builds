package:
  name: django-contentrelations
  version: "1.1.1"

source:
  fn: django-contentrelations-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-contentrelations/django-contentrelations-1.1.1.tar.gz
  md5: ed313505b4b42a2160d4660c39d546a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contentrelations = django-contentrelations:main
    #
    # Would create an entry point called django-contentrelations that calls django-contentrelations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - contentrelations
    - contentrelations.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://callowayproject.com/
  license: Apache Software License
  summary: 'Allows connecting any object to any other object and unifying the interface between all the objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
