package:
  name: django-jqueryfileupload
  version: "0.0.1"

source:
  fn: django-jqueryfileupload-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jqueryfileupload/django-jqueryfileupload-0.0.1.tar.gz
  md5: 290ddc6a42ea87a6deefadb47564b251
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jqueryfileupload = django-jqueryfileupload:main
    #
    # Would create an entry point called django-jqueryfileupload that calls django-jqueryfileupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - jqueryfileupload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://desarrollo.witoi.com/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Django app for handle multiple file uploads via jquery-file-upload plugin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
