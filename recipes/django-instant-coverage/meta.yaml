package:
  name: django-instant-coverage
  version: "0.0.14"

source:
  fn: django-instant-coverage-0.0.14.tar.gz
  url: https://pypi.python.org/packages/source/d/django-instant-coverage/django-instant-coverage-0.0.14.tar.gz
  md5: a8295b25438f0e01be2999f600249c27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-instant-coverage = django-instant-coverage:main
    #
    # Would create an entry point called django-instant-coverage that calls django-instant-coverage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - mock
    - six
    - beautifulsoup4
    - requests
    - html5lib

  run:
    - python
    - django
    - mock
    - six
    - beautifulsoup4
    - requests
    - html5lib

test:
  # Python imports
  imports:
    - instant_coverage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/colons/instant-coverage
  license: BSD License
  summary: 'Better-than-nothing testing for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
