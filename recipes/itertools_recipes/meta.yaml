package:
  name: itertools_recipes
  version: "0.1"

source:
  fn: itertools_recipes-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/itertools_recipes/itertools_recipes-0.1.tar.gz
  md5: 2ec2066053d5716e45280354acfdc0db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - itertools_recipes = itertools_recipes:main
    #
    # Would create an entry point called itertools_recipes that calls itertools_recipes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - itertools_recipes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Pasted from the "Recipes" section of the "itertools" documentations: An extended toolset using the existing itertools as building blocks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
