package:
  name: onesky-python
  version: "1.0.2"

source:
  fn: onesky-python-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/o/onesky-python/onesky-python-1.0.2.tar.gz
  md5: 3fd1e09f88ba306527574a64868ce9ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onesky-python = onesky-python:main
    #
    # Would create an entry point called onesky-python that calls onesky-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.3.0
    - termcolor ==1.1.0

  run:
    - python
    - requests ==2.3.0
    - termcolor ==1.1.0

test:
  # Python imports
  imports:
    - onesky

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jana-Mobile/onesky-python
  license: LICENSE.txt
  summary: 'python wrapper for the OneSky REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
