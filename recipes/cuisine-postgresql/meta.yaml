package:
  name: cuisine-postgresql
  version: "0.2.1"

source:
  fn: cuisine-postgresql-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cuisine-postgresql/cuisine-postgresql-0.2.1.tar.gz
  md5: d8b40268606fe8b09ec977ea4c7743fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cuisine-postgresql = cuisine-postgresql:main
    #
    # Would create an entry point called cuisine-postgresql that calls cuisine-postgresql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cuisine
    - fabric

  run:
    - python
    - cuisine
    - fabric

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/muhuk/cuisine-postgresql
  license: cuisine_sweet
  summary: 'Cuisine methods for PosgreSQL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
