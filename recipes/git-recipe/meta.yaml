package:
  name: git-recipe
  version: "0.2.2"

source:
  fn: git-recipe-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/g/git-recipe/git-recipe-0.2.2.tar.gz
  md5: 9f7c923ecc3a811e10fb089c8fa5e09d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-recipe = git-recipe:main
    #
    # Would create an entry point called git-recipe that calls git-recipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - recipe
    - recipe.git

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/youngking/git-recipe
  license: UNKNOWN
  summary: 'Simple buildout recipe for downloading git repositories. It uses system git command and its syntax'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
