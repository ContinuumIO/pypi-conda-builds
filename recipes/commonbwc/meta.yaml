package:
  name: commonbwc
  version: "0.1.3"

source:
  fn: CommonBWC-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/C/CommonBWC/CommonBWC-0.1.3.tar.gz
  md5: 2b1092aead1694af2b2c8d0b32833cd8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commonbwc = commonbwc:main
    #
    # Would create an entry point called commonbwc that calls commonbwc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blazeform >=0.3.0
    - blazeweb >=0.3.0

  run:
    - python
    - blazeform >=0.3.0
    - blazeweb >=0.3.0

test:
  # Python imports
  imports:
    - commonbwc
    - commonbwc.config
    - commonbwc.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/rsyring/commonbwc/
  license: BSD License
  summary: 'A BlazeWeb component to hold libraries shared by other components and apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
