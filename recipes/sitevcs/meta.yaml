package:
  name: sitevcs
  version: "0.1.4"

source:
  fn: sitevcs-0.1.4.zip
  url: https://pypi.python.org/packages/source/s/sitevcs/sitevcs-0.1.4.zip
  md5: 4ec1a6bd8463072c9e877951e13a03ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sitevcs = sitevcs:main
    #
    # Would create an entry point called sitevcs that calls sitevcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sitevcs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/eyeofhell/sitevcs
  license: GNU General Public License v3 (GPLv3)
  summary: 'sitevcs v. 0.1.4\n\nAfter importing, will add nearest version control system root as site-packagedirectory. Used in scripts that are stored in VCS and need to importthird-party python packages that are stored in same VCS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
