package:
  name: django-jsonmodel
  version: "0.0.6"

source:
  fn: django-jsonmodel-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsonmodel/django-jsonmodel-0.0.6.tar.gz
  md5: c0483cb33ed44ad65227bfd0f75c3ba6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsonmodel = django-jsonmodel:main
    #
    # Would create an entry point called django-jsonmodel that calls django-jsonmodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangojsonmodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/futurice-oss/django-jsonmodel
  license: MIT
  summary: 'Convert Django model structure into a JSON representation for client-side applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
