package:
  name: bit2darray
  version: "2.0"

source:
  fn: Bit2DArray-2.0.tar.gz
  url: http://RVL4.ecn.purdue.edu/~kak/distBit2DArray/Bit2DArray-2.0.tar.gz?download
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bit2darray = bit2darray:main
    #
    # Would create an entry point called bit2darray that calls bit2darray.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - bit2darray

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://RVL4.ecn.purdue.edu/~kak/distBit2DArray/Bit2DArray-2.0.html
  license: Python Software Foundation License
  summary: 'A memory-efficient packed representation for 2D bit arrays'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
