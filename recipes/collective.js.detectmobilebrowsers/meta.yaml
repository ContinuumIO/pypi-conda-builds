package:
  name: collective.js.detectmobilebrowsers
  version: "1.0"

source:
  fn: collective.js.detectmobilebrowsers-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.js.detectmobilebrowsers/collective.js.detectmobilebrowsers-1.0.zip
  md5: b73d5b856eceacfac4ad438d45a1576f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.detectmobilebrowsers = collective.js.detectmobilebrowsers:main
    #
    # Would create an entry point called collective.js.detectmobilebrowsers that calls collective.js.detectmobilebrowsers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.detectmobilebrowsers
    - collective.js.detectmobilebrowsers.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.js.detectmobilebrowsers
  license: GNU General Public License (GPL)
  summary: 'Add detectmobilebrowsers script to plone resource registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
