package:
  name: chameleon.zpt
  version: "1.1.3"

source:
  fn: chameleon.zpt-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/chameleon.zpt/chameleon.zpt-1.1.3.tar.gz
  md5: 90c83830c328f18f70084d0548d29272
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chameleon.zpt = chameleon.zpt:main
    #
    # Would create an entry point called chameleon.zpt that calls chameleon.zpt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chameleon >=1.0.1

  run:
    - python
    - setuptools
    - chameleon >=1.0.1

test:
  # Python imports
  imports:
    - chameleon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Zope Page Template engine based on Chameleon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
