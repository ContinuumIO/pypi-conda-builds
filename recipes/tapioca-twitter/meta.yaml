package:
  name: tapioca-twitter
  version: "0.2.1"

source:
  fn: tapioca-twitter-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tapioca-twitter/tapioca-twitter-0.2.1.tar.gz
  md5: 21eadb7d0572891d0be07e049621c9a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tapioca-twitter = tapioca-twitter:main
    #
    # Would create an entry point called tapioca-twitter that calls tapioca-twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tapioca-wrapper ==0.2.1
    - requests-oauthlib ==0.4.2

  run:
    - python
    - tapioca-wrapper ==0.2.1
    - requests-oauthlib ==0.4.2

test:
  # Python imports
  imports:
    - tapioca_twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vintasoftware/tapioca-twitter
  license: BSD License
  summary: 'Twitter API wrapper using tapioca'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
