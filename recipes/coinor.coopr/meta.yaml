package:
  name: coinor.coopr
  version: "2.4"

source:
  fn: coinor.coopr-2.4.tar.gz
  url: https://pypi.python.org/packages/source/c/coinor.coopr/coinor.coopr-2.4.tar.gz
  md5: 3aec421e3d48de782bfc7a110366a59d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinor.coopr = coinor.coopr:main
    #
    # Would create an entry point called coinor.coopr that calls coinor.coopr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coopr >=2.4
    - pyro

  run:
    - python
    - coopr >=2.4
    - pyro

test:
  # Python imports
  imports:
    - coinor
    - coinor.coopr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://projects.coin-or.org/CoinBazaar/wiki/Projects/coinor.coopr
  license: BSD License
  summary: 'COIN-OR project for Coopr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
