package:
  name: django-wattup
  version: "1.0"

source:
  fn: django-wattup-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wattup/django-wattup-1.0.tar.gz
  md5: 065b3298c122f95fa3fdbfec5e24f5c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wattup = django-wattup:main
    #
    # Would create an entry point called django-wattup that calls django-wattup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - wattup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dusette.net/
  license: GPL v3
  summary: 'A very simple contact form application for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
