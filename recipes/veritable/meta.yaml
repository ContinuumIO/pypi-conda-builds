package:
  name: veritable
  version: "0.9.9pre138"

source:
  fn: veritable-0.9.9pre138.tar.gz
  url: https://pypi.python.org/packages/source/v/veritable/veritable-0.9.9pre138.tar.gz
  md5: d13b563346c6f4d3be99766916fb5191
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - veritable = veritable:main
    #
    # Would create an entry point called veritable that calls veritable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - veritable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.priorknowledge.com/
  license: MIT License
  summary: 'Python Client Library for Veritable API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
