package:
  name: django-grappelli-filters
  version: "0.2.1"

source:
  fn: django-grappelli-filters-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-grappelli-filters/django-grappelli-filters-0.2.1.tar.gz
  md5: 044ad40f2fdfa16ed013394de8fe94a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-grappelli-filters = django-grappelli-filters:main
    #
    # Would create an entry point called django-grappelli-filters that calls django-grappelli-filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-grappelli

  run:
    - python
    - django
    - django-grappelli

test:
  # Python imports
  imports:
    - grappelli_filters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frnhr/django-grappelli-filters/
  license: Unlicense
  summary: 'Additional filters for Djagno Grappelli admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
