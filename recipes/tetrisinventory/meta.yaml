package:
  name: tetrisinventory
  version: "1.0a2"

source:
  fn: tetrisinventory-1.0a2.zip
  url: https://pypi.python.org/packages/source/t/tetrisinventory/tetrisinventory-1.0a2.zip
  md5: a79b3c6c0bc1c39e4311e6b6439be91b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tetrisinventory = tetrisinventory:main
    #
    # Would create an entry point called tetrisinventory that calls tetrisinventory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tetrisinventory
    - tetrisinventory.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/devin.jeanpierre/tetris-inventory
  license: MIT License
  summary: u'Diablo\xae-style "tetris inventory"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
