package:
  name: collective.azindexpage
  version: "1.0.3"

source:
  fn: collective.azindexpage-1.0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.azindexpage/collective.azindexpage-1.0.3.zip
  md5: c11d13cee672430ade048625076c7192
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.azindexpage = collective.azindexpage:main
    #
    # Would create an entry point called collective.azindexpage that calls collective.azindexpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.linguakeywordwidget

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.linguakeywordwidget

test:
  # Python imports
  imports:
    - collective
    - collective.azindexpage
    - collective.azindexpage.browser
    - collective.azindexpage.tests
    - collective.azindexpage.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.azindexpage
  license: GPL
  summary: 'Create a A-Z index page for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
