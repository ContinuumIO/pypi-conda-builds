package:
  name: beautifulcharts
  version: "0.6.5"

source:
  fn: BeautifulCharts-0.6.5.tar.gz
  url: https://pypi.python.org/packages/source/B/BeautifulCharts/BeautifulCharts-0.6.5.tar.gz
  md5: 08e42afcb62fa487f94531a2a0418771
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beautifulcharts = beautifulcharts:main
    #
    # Would create an entry point called beautifulcharts that calls beautifulcharts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - beautifulcharts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License / GPL v3
  summary: 'Simple, elegant and fast charting library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
