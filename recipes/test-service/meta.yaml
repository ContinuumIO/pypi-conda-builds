package:
  name: test-service
  version: "1.2"

source:
  fn: test-service-1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/test-service/test-service-1.2.tar.gz
  md5: 432a77c83fc1c1a24a59ca1d5e3d5ac5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - test-service = test-service:main
    #
    # Would create an entry point called test-service that calls test-service.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - django

  run:
    - python
    - six
    - django

test:
  # Python imports
  imports:
    - test_service
    - test_service.management
    - test_service.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kylinfish/test-service
  license: MIT License
  summary: 'test-service is a Python package that test web service base on linux curl.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
