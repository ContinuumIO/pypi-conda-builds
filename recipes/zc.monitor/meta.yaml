package:
  name: zc.monitor
  version: "0.3.1"

source:
  fn: zc.monitor-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.monitor/zc.monitor-0.3.1.tar.gz
  md5: 55cd4a5a3cc8c0b5745d2dbfbe9823ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.monitor = zc.monitor:main
    #
    # Would create an entry point called zc.monitor that calls zc.monitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.ngi
    - zope.component
    - zope.testing

  run:
    - python
    - setuptools
    - zc.ngi
    - zope.component
    - zope.testing

test:
  # Python imports
  imports:
    - zc
    - zc.monitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'A network-accessible command-line monitoring interface.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
