package:
  name: django-vkontakte-users
  version: "0.7.4"

source:
  fn: django-vkontakte-users-0.7.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-users/django-vkontakte-users-0.7.4.tar.gz
  md5: 8fbaae831f8c43211ad87c7b77361381
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-users = django-vkontakte-users:main
    #
    # Would create an entry point called django-vkontakte-users that calls django-vkontakte-users.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil ==1.5
    - django-vkontakte-api >=0.7.0
    - django-vkontakte-places >=0.3.0

  run:
    - python
    - python-dateutil ==1.5
    - django-vkontakte-api >=0.7.0
    - django-vkontakte-places >=0.3.0

test:
  # Python imports
  imports:
    - vkontakte_users
    - vkontakte_users.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-vkontakte-users
  license: BSD License
  summary: 'Django implementation for vkontakte API Users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
