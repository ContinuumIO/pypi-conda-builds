package:
  name: betterimageextension
  version: "0.0.1"

source:
  fn: BetterImageExtension-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BetterImageExtension/BetterImageExtension-0.0.1.tar.gz
  md5: 7dccdcfcc567b5333644a890e1e6e7b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betterimageextension = betterimageextension:main
    #
    # Would create an entry point called betterimageextension that calls betterimageextension.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown >=2.0

  run:
    - python
    - markdown >=2.0

test:
  # Python imports
  imports:
    - betterimageextension

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://neurobashing.com/
  license: BSD License
  summary: 'Python-Markdown extension to add additional image syntax eg print and web sizes from a single image'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
