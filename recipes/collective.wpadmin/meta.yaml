package:
  name: collective.wpadmin
  version: "1.0a3"

source:
  fn: collective.wpadmin-1.0a3.zip
  url: https://pypi.python.org/packages/source/c/collective.wpadmin/collective.wpadmin-1.0a3.zip
  md5: aff178cc70fb605f4e412ff47fb95cde
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.wpadmin = collective.wpadmin:main
    #
    # Would create an entry point called collective.wpadmin that calls collective.wpadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.configviews
    - collective.js.jqueryui
    - collective.js.datatables
    - collective.quickupload
    - plone.api
    - plone.formwidget.namedfile
    - plone.namedfile

  run:
    - python
    - setuptools
    - setuptools
    - collective.configviews
    - collective.js.jqueryui
    - collective.js.datatables
    - collective.quickupload
    - plone.api
    - plone.formwidget.namedfile
    - plone.namedfile

test:
  # Python imports
  imports:
    - collective
    - collective.wpadmin
    - collective.wpadmin.pages
    - collective.wpadmin.tests
    - collective.wpadmin.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.wpadmin
  license: GPL
  summary: 'Wordpress like admin page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
