package:
  name: django-yplaces
  version: "0.2.9"

source:
  fn: django-yplaces-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yplaces/django-yplaces-0.2.9.tar.gz
  md5: 61469f74ee17c289b0dc266d1e4dc8b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yplaces = django-yplaces:main
    #
    # Would create an entry point called django-yplaces that calls django-yplaces.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil ==1.1.7
    - django-yapi
    - django-yutils

  run:
    - python
    - pil ==1.1.7
    - django-yapi
    - django-yutils

test:
  # Python imports
  imports:
    - yplaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Places app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
