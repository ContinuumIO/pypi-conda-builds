package:
  name: mokacms
  version: "0.1dev"

source:
  fn: MokaCMS-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/M/MokaCMS/MokaCMS-0.1dev.tar.gz
  md5: 4a531307b2286ee2a1d84b79698e5eeb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mokacms = mokacms:main
    #
    # Would create an entry point called mokacms that calls mokacms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - pyramid_debugtoolbar
    - waitress

  run:
    - python
    - setuptools
    - pyramid
    - pyramid_debugtoolbar
    - waitress

test:
  # Python imports
  imports:
    - mokacms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asidev/MokaCMS
  license: UNKNOWN
  summary: 'MokaCMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
