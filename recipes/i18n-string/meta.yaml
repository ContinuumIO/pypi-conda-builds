package:
  name: i18n-string
  version: "0.2.1"

source:
  fn: i18n-string-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/i18n-string/i18n-string-0.2.1.tar.gz
  md5: eb9e48a56d8f1a30e18f526cc2a5a2d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - i18n-string = i18n-string:main
    #
    # Would create an entry point called i18n-string that calls i18n-string.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lig/i18n-string
  license: BSD License
  summary: u'MultilingualString Python class that allows to store all translations of the particular string incapsulated into only string like object.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
