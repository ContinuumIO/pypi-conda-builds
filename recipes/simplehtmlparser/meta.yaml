package:
  name: simplehtmlparser
  version: "1.0.2"

source:
  fn: SimpleHTMLParser-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleHTMLParser/SimpleHTMLParser-1.0.2.tar.gz
  md5: 4ef31415aa0547ef7099dd776ede7d7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplehtmlparser = simplehtmlparser:main
    #
    # Would create an entry point called simplehtmlparser that calls simplehtmlparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - SimpleHTMLParser
    - simplehtmlparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'An HTML Parser that builds a tree and exposes common getElementsBy* functions as found in JS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
