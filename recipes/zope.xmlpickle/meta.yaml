package:
  name: zope.xmlpickle
  version: "3.4.0"

source:
  fn: zope.xmlpickle-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.xmlpickle/zope.xmlpickle-3.4.0.tar.gz
  md5: b579f35546b095aec2c890d3f8a46911
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.xmlpickle = zope.xmlpickle:main
    #
    # Would create an entry point called zope.xmlpickle that calls zope.xmlpickle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.xmlpickle
    - zope.xmlpickle.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.xmlpickle
  license: Zope Public License
  summary: 'XML-based Pickling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
