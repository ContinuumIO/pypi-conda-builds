package:
  name: measure-it
  version: "0.4.1"

source:
  fn: measure-it-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/measure-it/measure-it-0.4.1.tar.gz
  md5: b15738c4afaf89076ffae61d8647f2e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - measure-it = measure-it:main
    #
    # Would create an entry point called measure-it that calls measure-it.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - measure_it

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wearpants/measure_it
  license: BSD License
  summary: 'time and count measurement for iterators and other code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
