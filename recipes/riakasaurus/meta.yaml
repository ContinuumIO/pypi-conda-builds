package:
  name: riakasaurus
  version: "1.1.1"

source:
  fn: riakasaurus-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/r/riakasaurus/riakasaurus-1.1.1.tar.gz
  md5: 406742b5a03640a49becc78cb71cbace
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - riakasaurus = riakasaurus:main
    #
    # Would create an entry point called riakasaurus that calls riakasaurus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - riakasaurus
    - riakasaurus.test_crosscompat
    - riakasaurus.tests
    - riakasaurus.transport
    - riakasaurus.transport.pbc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/calston/riakasaurus
  license: Apache 2.0
  summary: 'Riakasaurus is a Twisted client library for Riak'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
