package:
  name: pyconvert
  version: "0.4.alpha"

source:
  fn: pyconvert-0.4.alpha.tar.gz
  url: https://pypi.python.org/packages/source/p/pyconvert/pyconvert-0.4.alpha.tar.gz
  md5: 423502c016c4aa33ab4b5a3bab8c898c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyconvert = pyconvert:main
    #
    # Would create an entry point called pyconvert that calls pyconvert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demos
    - pyconvert

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'A simple and useful python API for convert python object to xml and json documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
