package:
  name: cioppino.twothumbs
  version: "1.8"

source:
  fn: cioppino.twothumbs-1.8.zip
  url: https://pypi.python.org/packages/source/c/cioppino.twothumbs/cioppino.twothumbs-1.8.zip
  md5: 2cc76169fc52e3e027c575f4239b8efa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cioppino.twothumbs = cioppino.twothumbs:main
    #
    # Would create an entry point called cioppino.twothumbs that calls cioppino.twothumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone
    - plone.behavior
    - setuptools

  run:
    - python
    - setuptools
    - plone
    - plone.behavior
    - setuptools

test:
  # Python imports
  imports:
    - cioppino
    - cioppino.twothumbs
    - cioppino.twothumbs.browser
    - cioppino.twothumbs.tests
    - cioppino.twothumbs.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eleddy/cioppino.twothumbs
  license: GPL
  summary: 'Rating widget based on thumbs up and down.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
