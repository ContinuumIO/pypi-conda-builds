package:
  name: agile_conf
  version: "1.0.3"

source:
  fn: agile_conf-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/agile_conf/agile_conf-1.0.3.tar.gz
  md5: 3d61c853adf438906a7b9353cf12fbcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agile_conf = agile_conf:main
    #
    # Would create an entry point called agile_conf that calls agile_conf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.0
    - jinja2
    - docopt >=0.6.2

  run:
    - python
    - pyyaml >=3.0
    - jinja2
    - docopt >=0.6.2

test:
  # Python imports
  imports:
    - agile_conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tly1980/agile_conf
  license: BSD License
  summary: 'A config files (in [YAML](http://yaml.org) format) and template engine ([Jinja2](http://jinja.pocoo.org)) based configuration compile / management tool to make DevOp tasks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
