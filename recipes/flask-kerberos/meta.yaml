package:
  name: flask-kerberos
  version: "1.0.4"

source:
  fn: Flask-Kerberos-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Kerberos/Flask-Kerberos-1.0.4.tar.gz
  md5: 88586cb11c7d53303a4b5ec56a0d6d27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-kerberos = flask-kerberos:main
    #
    # Would create an entry point called flask-kerberos that calls flask-kerberos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - kerberos

  run:
    - python
    - flask
    - kerberos

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mkomitee/flask-kerberos
  license: BSD License
  summary: 'Kerberos authentication support for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
