package:
  name: django-fbrealtime
  version: "0.1.0"

source:
  fn: django-fbrealtime-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fbrealtime/django-fbrealtime-0.1.0.tar.gz
  md5: 721cee3ffbff7e96ef069da41fb31bb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fbrealtime = django-fbrealtime:main
    #
    # Would create an entry point called django-fbrealtime that calls django-fbrealtime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.0

  run:
    - python
    - django >=1.4.0

test:
  # Python imports
  imports:
    - fbrealtime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/n8j/django-fbrealtime
  license: MIT
  summary: 'Django app for receiving realtime facebook updates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
