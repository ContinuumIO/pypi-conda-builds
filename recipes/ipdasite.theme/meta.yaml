package:
  name: ipdasite.theme
  version: "2.1.8"

source:
  fn: ipdasite.theme-2.1.8.zip
  url: http://oodt.jpl.nasa.gov/dist/ipdasite/ipdasite.theme-2.1.8.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipdasite.theme = ipdasite.theme:main
    #
    # Would create an entry point called ipdasite.theme that calls ipdasite.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.theming
    - z3c.autoinclude
    - z3c.jbot
    - five.grok

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.theming
    - z3c.autoinclude
    - z3c.jbot
    - five.grok

test:
  # Python imports
  imports:
    - ipdasite
    - ipdasite.theme
    - ipdasite.theme.browser
    - ipdasite.theme.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ipdasite.theme/
  license: Proprietary
  summary: 'International Planetary Data Alliance Site Theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
