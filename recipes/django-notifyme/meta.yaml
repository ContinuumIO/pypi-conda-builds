package:
  name: django-notifyme
  version: "0.0.2a1"

source:
  fn: django-notifyme-0.0.2a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-notifyme/django-notifyme-0.0.2a1.tar.gz
  md5: 43829e73acf6604daab16864876c6738
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-notifyme = django-notifyme:main
    #
    # Would create an entry point called django-notifyme that calls django-notifyme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - notifyme
    - notifyme.delivery_backends
    - notifyme.notification_types

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stefanfoulis/django-notifyme
  license: BSD License
  summary: 'A notification framework for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
