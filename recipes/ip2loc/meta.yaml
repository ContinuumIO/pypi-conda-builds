package:
  name: ip2loc
  version: "0.1.3"

source:
  fn: ip2loc-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/ip2loc/ip2loc-0.1.3.tar.gz
  md5: 86b405128bc2d546f0dd93c03885be9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ip2loc = ip2loc:main
    #
    # Would create an entry point called ip2loc that calls ip2loc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ip2loc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/numbbbbb/python-ip
  license: WTFPL
  summary: '\xe8\x8e\xb7\xe5\x8f\x96\xe5\x9f\x9f\xe5\x90\x8d\xe6\x88\x96\xe8\x80\x85IP\xe7\x9a\x84\xe6\x89\x80\xe5\x9c\xa8\xe5\x9c\xb0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
