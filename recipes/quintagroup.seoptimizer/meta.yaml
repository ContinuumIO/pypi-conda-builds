package:
  name: quintagroup.seoptimizer
  version: "4.3"

source:
  fn: quintagroup.seoptimizer-4.3.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.seoptimizer/quintagroup.seoptimizer-4.3.zip
  md5: 96b3a7e3b1824dff4c05384073aca98a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.seoptimizer = quintagroup.seoptimizer:main
    #
    # Would create an entry point called quintagroup.seoptimizer that calls quintagroup.seoptimizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer
    - quintagroup.canonicalpath >=0.6
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer
    - quintagroup.canonicalpath >=0.6
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.seoptimizer
    - quintagroup.seoptimizer.browser
    - quintagroup.seoptimizer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://quintagroup.com/services/plone-development/products/qSEOptimizer/
  license: GNU General Public License (GPL)
  summary: 'Quintagroup Search Engine Optimization Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
