package:
  name: django-inline-orderable
  version: "0.1.1"

source:
  fn: django-inline-orderable-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inline-orderable/django-inline-orderable-0.1.1.tar.gz
  md5: d4fc1fc49c1c345f7a1c341c26da63b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inline-orderable = django-inline-orderable:main
    #
    # Would create an entry point called django-inline-orderable that calls django-inline-orderable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - inline_orderable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marcofucci/django-inline-orderable
  license: BSD License
  summary: 'An easy way of making inlines orderable using drag-and-drop.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
