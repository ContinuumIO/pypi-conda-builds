package:
  name: django-randomslugfield
  version: "0.3.0"

source:
  fn: django-randomslugfield-0.3.0.zip
  url: https://pypi.python.org/packages/source/d/django-randomslugfield/django-randomslugfield-0.3.0.zip
  md5: 4516f4210a28d8c9e2dfcd9192949647
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-randomslugfield = django-randomslugfield:main
    #
    # Would create an entry point called django-randomslugfield that calls django-randomslugfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - randomslugfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mkrjhnsn/django-randomslugfield
  license: MIT License
  summary: 'A Django field that automatically generates random slugs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
