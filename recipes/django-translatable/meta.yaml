package:
  name: django-translatable
  version: "0.1.1"

source:
  fn: django-translatable-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-translatable/django-translatable-0.1.1.tar.gz
  md5: b87a24a278ab2b60509fdb6bffe34785
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-translatable = django-translatable:main
    #
    # Would create an entry point called django-translatable that calls django-translatable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - translatable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikoskay/django-translatable
  license: BSD License
  summary: 'Django app providing simple translatable models system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
