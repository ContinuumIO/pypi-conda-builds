package:
  name: cartridge
  version: "0.9.5"

source:
  fn: Cartridge-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/C/Cartridge/Cartridge-0.9.5.tar.gz
  md5: d9b26e7a33e1dbafbee3f99b42f1d408
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cartridge = cartridge:main
    #
    # Would create an entry point called cartridge that calls cartridge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mezzanine >=3.1
    - xhtml2pdf

  run:
    - python
    - mezzanine >=3.1
    - xhtml2pdf

test:
  # Python imports
  imports:
    - cartridge
    - cartridge.project_template
    - cartridge.shop
    - cartridge.shop.management
    - cartridge.shop.management.commands
    - cartridge.shop.migrations
    - cartridge.shop.payment
    - cartridge.shop.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cartridge.jupo.org/
  license: BSD License
  summary: 'A Django shopping cart application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
