package:
  name: highfield
  version: "1.0.21"

source:
  fn: highfield-1.0.21.tar.gz
  url: https://pypi.python.org/packages/source/h/highfield/highfield-1.0.21.tar.gz
  md5: 31f6c547a08d27493411e3b70d567400
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - highfield = highfield:main
    #
    # Would create an entry point called highfield that calls highfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - highfield
    - highfield.helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kmyr/highfield
  license: LICENSE
  summary: 'Structured flask with mongo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
