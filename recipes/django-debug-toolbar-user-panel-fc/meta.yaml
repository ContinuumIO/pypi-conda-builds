package:
  name: django-debug-toolbar-user-panel-fc
  version: "1.0.1"

source:
  fn: django-debug-toolbar-user-panel-fc-1.0.1.zip
  url: https://pypi.python.org/packages/source/d/django-debug-toolbar-user-panel-fc/django-debug-toolbar-user-panel-fc-1.0.1.zip
  md5: 2b58cb0266507f0b005b83e471ae18b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-toolbar-user-panel-fc = django-debug-toolbar-user-panel-fc:main
    #
    # Would create an entry point called django-debug-toolbar-user-panel-fc that calls django-debug-toolbar-user-panel-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - debug_toolbar_user_panel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-debug-toolbar-user-panel
  license: BSD
  summary: 'Panel for the Django Debug toolbar to quickly switch between users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
