package:
  name: infi.logs_collector
  version: "0.1.9"

source:
  fn: infi.logs_collector-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.logs_collector/infi.logs_collector-0.1.9.tar.gz
  md5: bebfaff248131f6e755ec14ecea94a09
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.logs_collector = infi.logs_collector:main
    #
    # Would create an entry point called infi.logs_collector that calls infi.logs_collector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - colorama
    - infi.eventlog
    - infi.execute
    - infi.pyutils
    - infi.traceback
    - setuptools

  run:
    - python
    - colorama
    - infi.eventlog
    - infi.execute
    - infi.pyutils
    - infi.traceback
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.logs_collector
    - infi.logs_collector.collectables
    - infi.logs_collector.collectables.windows
    - infi.logs_collector.scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.logs_collector
  license: Python Software Foundation License
  summary: 'helper for logs collection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
