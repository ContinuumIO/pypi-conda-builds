package:
  name: django-mesh
  version: "0.1.0"

source:
  fn: django-mesh-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mesh/django-mesh-0.1.0.tar.gz
  md5: f1d934c32a3a2c218bfc175747ddf5bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mesh = django-mesh:main
    #
    # Would create an entry point called django-mesh that calls django-mesh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-pagination

  run:
    - python
    - django >=1.2
    - django-pagination

test:
  # Python imports
  imports:
    - django_mesh
    - django_mesh.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SeanHayes/django-mesh
  license: GNU General Public License (GPL)
  summary: 'A Django blog.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
