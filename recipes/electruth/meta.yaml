package:
  name: electruth
  version: "0.2.0"

source:
  fn: electruth-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/electruth/electruth-0.2.0.tar.gz
  md5: 83ce1f553f6829bb1d249c9c0319e019
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - electruth = electruth:main
    #
    # Would create an entry point called electruth that calls electruth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - electruth
    - electruth.external

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metanohi.org/projects/electruth/
  license: GNU General Public License (GPL)
  summary: 'A collection of boolean logic tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
